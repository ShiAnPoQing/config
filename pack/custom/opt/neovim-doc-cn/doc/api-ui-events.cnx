*api-ui-events.txt*	Nvim


			    NVIM 参考手册


Nvim UI 协议					*UI* *ui* *api-ui-events*

本文档描述了 UI 协议。有关面向用户的 UI 组件和功能，请参阅 |gui| 和 |tui|。

                                      键入 |gO| 查看目录。

==============================================================================
UI 事件						*ui-protocol* *ui-events*

UI 可以作为通过 RPC API 与 Nvim 通信的外部客户端进程来实现。默认的 UI 模型是一个具有单一等宽字体的终端风格网格。UI 可以选择让窗口在单独的网格上绘制，并让某些元素（“小部件”）由 UI 自身而不是 Nvim 呈现（“外部化”）。

							*ui-option*
调用 |nvim_ui_attach()| 以告知 Nvim 您的程序希望绘制一个大小为 width × height 个单元格的 Nvim 屏幕网格。这通常由嵌入器在启动时完成（参见 |ui-startup|），但 UI 也可以连接到正在运行的 Nvim 实例并调用 nvim_ui_attach()。`options` 参数是一个包含以下（可选）键的映射：

							*ui-rgb*
- `rgb`			决定颜色格式。
			- true：（默认）24 位 RGB 颜色
			- false：终端颜色（8 位，最多 256）

							*ui-override*
- `override`		决定 UI 功能的解析方式。
			- true：启用请求的 UI 功能，即使并非所有已连接的 UI（包括 |TUI|）都支持。
			- false：（默认）禁用并非所有已连接的 UI（包括 TUI）都支持的 UI 功能。

							*ui-ext-options*
- `ext_cmdline`		外部化命令行。|ui-cmdline|
- `ext_hlstate`		详细的高亮状态。|ui-hlstate|
			隐式设置 `ext_linegrid`。
- `ext_linegrid`	基于行的网格事件。|ui-linegrid|
			隐式停用 |ui-grid-old|。
- `ext_messages`	外部化消息。|ui-messages|
			隐式设置 `ext_linegrid` 和 `ext_cmdline`。
- `ext_multigrid`	每窗口网格事件。|ui-multigrid|
			隐式设置 `ext_linegrid`。
- `ext_popupmenu`	外部化 |popupmenu-completion| 和 'wildmenu'。|ui-popupmenu|
- `ext_tabline`		外部化标签栏。|ui-tabline|
- `ext_termcolors`	使用外部默认颜色。
- `term_name`		设置终端名称 'term'。
- `term_colors`		设置支持的颜色数 't_Co'。
- `stdin_fd`		当使用 |--| 时，将此文件描述符视为标准输入。
			仅在启动时从 |--embed| UI 调用。|ui-startup-stdin|
- `stdin_tty`		指示 `stdin` 是否为 `tty`。
- `stdout_tty`		指示 `stdout` 是否为 `tty`。

指定一个未知选项会导致错误；UI 可以检查 |api-metadata| 的 `ui_options` 键以获取支持的选项。

默认情况下，Nvim 要求所有已连接的 UI 支持相同的功能，因此活动的功能是所请求功能的交集。UI 可以指定 |ui-override| 来反转此行为（对调试有用）。"option_set" 事件会宣布哪些功能处于活动状态。

Nvim 会向所有已连接的 UI 发送 RPC 通知，方法名称为 "redraw"，并附带一个参数：屏幕“更新事件”的数组（批次）。每个更新事件本身是一个数组，其第一个元素是事件名称，其余元素是事件参数元组。因此，相同类型的事件可以连续发送，而无需重复事件名称。

单个 RPC 通知中典型 "redraw" 批次的示例：>

    ['notification', 'redraw',
      [
	['grid_resize', [2, 77, 36]],
	['grid_line',
	  [2, 0, 0, [[' ' , 0, 77]], false],
	  [2, 1, 0, [['~', 7], [' ', 7, 76]], false],
	  [2, 9, 0, [['~', 7], [' ', 7, 76]], false],
	  ...
	  [2, 35, 0, [['~', 7], [' ', 7, 76]], false],
	  [1, 36, 0, [['[', 9], ['N'], ['o'], [' '], ['N'], ['a'], ['m'], ['e'], [']']], false],
	  [1, 36, 9, [[' ', 9, 50]], false],
	  [1, 36, 59, [['0', 9], [','], ['0'], ['-' ], ['1'], [' ', 9, 10], ['A'], ['l', 9, 2]], false]
	],
	['msg_showmode', [[]]],
	['win_pos', [2, 1000, 0, 0, 77, 36]],
	['grid_cursor_goto', [2, 0, 0]],
	['flush', []]
      ]
    ]

事件必须按顺序处理。当 Nvim 完成整个屏幕的重绘（即所有窗口对缓冲区状态、选项等具有一致的视图）时，它会发送一个 "flush" 事件。在整个屏幕重绘完成之前，可能会发送多个 "redraw" 批次，只有最后一个批次后面跟着 "flush"。用户应该只看到最终状态（发送 "flush" 时的状态），而不应该看到处理批次数组部分时的任何中间状态，也不应该看到未以 "flush" 结尾的批次之后的状态。

默认情况下，Nvim 会发送 |ui-global| 和 |ui-grid-old| 事件（为了向后兼容）；这些事件足以实现终端风格的界面。然而，新的 |ui-linegrid| 能更高效地表示文本（尤其是高亮文本），并允许需要多个网格的 UI 功能。新的 UI 应实现 |ui-linegrid| 而不是 |ui-grid-old|。

Nvim 可选地将各种屏幕元素作为结构化事件而不是原始网格线“语义化”地发送，由 |ui-ext-options| 指定。UI 必须自行呈现这些元素，Nvim 将不会在网格上绘制它们。

Nvim 的未来版本可能会添加新的更新类型，并可能向现有更新类型追加新参数。客户端必须准备好忽略此类扩展，以实现向前兼容性。|api-contract|

==============================================================================
UI 启动							   *ui-startup*

UI 嵌入器（使用 |--embed| 启动 Nvim 并在之后调用 |nvim_ui_attach()| 的客户端）必须在没有 |--headless| 的情况下启动 Nvim：>bash
    nvim --embed
Nvim 会在加载启动文件和读取缓冲区之前暂停，以便 UI 有机会调用请求并进行早期初始化。当 UI 调用 |nvim_ui_attach()| 时，启动将继续。

一个简单的 UI 只需要执行一次 |nvim_ui_attach()| 请求，然后准备处理任何 UI 事件。一个功能更丰富的 UI（可能需要配置 Nvim 进程）应使用以下启动过程：

1. 如果需要设置客户端库和/或获取支持的 UI 扩展列表，则调用 |nvim_get_api_info()|。
2. 执行任何应在用户配置加载之前发生的配置。此时缓冲区和窗口不可用，但这可用于设置对 init.vim 可见的 |g:| 变量。
3. 如果 UI 希望在用户配置加载后进行额外的设置，请注册一个 VimEnter 自动命令：>lua
      nvim_command("autocmd VimEnter * call rpcrequest(1, 'vimenter')")
4. 现在调用 |nvim_ui_attach()|。此时 UI 必须处理用户输入：加载 init.vim 和缓冲区可能会导致阻塞提示。
5. 如果使用了步骤 3，Nvim 将向 UI 发送一个阻塞的 "vimenter" 请求。在此请求处理程序内部，UI 可以安全地执行任何在进入普通模式之前的初始化，例如读取 init.vim 设置的变量。

							   *ui-startup-stdin*
UI 可以支持从标准输入读取（如 `command | nvim -`，参见 |--|），如下所示：

1. 嵌入进程检测到标准输入文件描述符不是终端。
2. 然后它需要将此文件描述符转发给 Nvim。因为文件描述符 0 已被用于从嵌入器向 Nvim 发送 RPC 数据，它必须使用其他文件描述符，如 fd=3 或更高。
3. 然后将该文件描述符作为 `nvim_ui_attach` 的 `stdin_fd` 参数传递。Nvim 会将其作为文本读入缓冲区 1。

==============================================================================
全局事件							    *ui-global*

以下 UI 事件总是被发出，并描述编辑器的全局状态。

["set_title", title] ~
["set_icon", icon] ~
	分别设置窗口标题和图标（最小化）窗口标题。在未区分两者的窗口系统中，可以忽略 "set_icon"。

["mode_info_set", cursor_style_enabled, mode_info] ~
	`cursor_style_enabled` 是一个布尔值，指示 UI 是否应设置光标样式。`mode_info` 是模式属性映射的列表。当前模式由 `mode_change` 事件的 `mode_idx` 字段给出。

	每个模式属性映射可能包含以下键：

	键		描述 ~
	`cursor_shape`:	"block", "horizontal", "vertical"
	`cell_percentage`: 光标占用的单元格百分比。
	`blinkwait`, `blinkon`, `blinkoff`: 参见 |cursor-blinking|。
	`attr_id`:	光标属性 id（由 `hl_attr_define` 定义）。
			当 attr_id 为 0 时，应交换背景色和前景色。
	`attr_id_lm`:	当 |:lmap| 开启时的光标属性 id。
	`short_name`:	模式代码名称，参见 'guicursor'。
	`name`:		模式描述性名称。
	`mouse_shape`:	（待实现）。

	某些模式中缺少某些键。

	以下键已被弃用：

	`hl_id`:	改用 `attr_id`。
	`id_lm`:	改用 `attr_id_lm`。

["option_set", name, value] ~
	与 UI 相关的选项发生更改，其中 `name` 是以下之一：

	- 'arabicshape'
	- 'ambiwidth'
	- 'emoji'
	- 'guifont'
	- 'guifontwide'
	- 'linespace'
	- 'mousefocus'
	- 'mousehide'
	- 'mousemoveevent'
	- 'pumblend'
	- 'showtabline'
	- 'termguicolors'
	- "ext_*"（所有 |ui-ext-options|）

	当 UI 首次连接到 Nvim 时，以及用户或插件更改选项时触发。

	如果选项的效果通过其他 UI 事件传达，则此处不会表示它们。例如，不转发 'mouse' 选项的值，而是使用 "mouse_on" 和 "mouse_off" UI 事件直接指示鼠标支持是否处于活动状态。某些选项（如 'ambiwidth'）已在网格上生效，并添加了适当的空单元格，但是 UI 在呈现从 Nvim 发送的原始文本（例如对于 |ui-cmdline|）时仍可能使用此类选项。

["chdir", path] ~
	|current-directory| 更改为 `path`。

["mode_change", mode, mode_idx] ~
	编辑器模式更改。`mode` 参数是一个表示当前模式的字符串。`mode_idx` 是在 `mode_info_set` 事件中发出的数组的索引。UI 应根据相应项目中指定的属性更改光标样式。报告的模式集将在 Nvim 的新版本中发生变化，例如，更多子模式和临时状态可能被表示为单独的模式。

["mouse_on"] ~
["mouse_off"] ~
	'mouse' 在当前编辑器模式中被启用/禁用。对于终端 UI 或嵌入到鼠标与 Nvim 冲突的应用程序中很有用。其他 UI 可以忽略此事件。

["busy_start"] ~
["busy_stop"] ~
	指示 UI 必须停止渲染光标。此事件名称有误，实际上与忙碌无关。

["restart", progpath, argv] ~
	已使用 |:restart| 命令，Nvim 服务器即将退出。UI 应等待服务器退出，然后使用 `progpath` 作为 Nvim 可执行文件的完整路径 |v:progpath| 和 `argv` 作为其参数 |v:argv| 启动一个新服务器，并重新连接到新服务器。
	注意：|--embed| 和 |--headless| 从 `argv` 中排除，客户端应自行决定是否添加任一标志。

["suspend"] ~
	使用 |:suspend| 命令或 |CTRL-Z| 映射。终端客户端（或其他有意义的客户端）可以挂起自身。其他客户端可以安全地忽略它。

["update_menu"] ~
	菜单映射已更改。

["bell"] ~
["visual_bell"] ~
	分别以可听或可视的方式通知用户。

["flush"] ~
	Nvim 已完成屏幕重绘。对于渲染到内部缓冲区的实现，这是将重绘部分显示给用户的时候。

["ui_send", content] ~
	将 {content} 写入已连接的 TTY。只有设置了 "stdout_tty" |ui-option| 的 UI 才会收到此事件。

==============================================================================
网格事件（基于行）					  *ui-linegrid*

由 `ext_linegrid` |ui-option| 激活。推荐所有新 UI 使用。隐式停用 |ui-grid-old|。

与 |ui-grid-old| 不同，此 UI 扩展发出单个 `grid_line` 事件来更新屏幕行（而旧协议为每个屏幕行发出单独的光标、高亮和文本事件）。

这些事件中的大多数将 `grid` 索引作为第一个参数。网格 1 是整个编辑器屏幕状态默认使用的全局网格。`ext_linegrid` 功能本身不会创建任何额外的网格；要启用每窗口网格，请激活 |ui-multigrid|。

高亮属性组是预定义的。UI 应维护一个表，将数字高亮 id 映射到实际属性。

["grid_resize", grid, width, height] ~
	调整 `grid` 的大小。如果客户端之前未见过 `grid`，则正在创建一个具有此大小的新网格。

["default_colors_set", rgb_fg, rgb_bg, rgb_sp, cterm_fg, cterm_bg] ~
	前三个参数分别设置默认前景色、背景色和特殊颜色。`cterm_fg` 和 `cterm_bg` 指定在 256 色终端中使用的默认颜色代码。

	默认情况下，RGB 值始终是有效的颜色。如果未设置颜色，它们将默认为黑色和白色，具体取决于 'background'。通过设置 `ext_termcolors` 选项，未设置的颜色将使用 -1。这对于 TUI 实现最有用，因为预期使用终端内置（"ANSI"）默认值。

	注意：与相应的 |ui-grid-old| 事件不同，发送此事件后不会总是清除屏幕。UI 必须自行使用更改的背景色重新绘制屏幕。

							*ui-event-hl_attr_define*
["hl_attr_define", id, rgb_attr, cterm_attr, info] ~
	将具有 `id` 的高亮添加到高亮表，其属性由 `rgb_attr` 和 `cterm_attr` 字典指定，具有以下（均为可选）键。

	`foreground`:		前景色。
	`background`:		背景色。
	`special`:		用于各种下划线的颜色，当存在时。
	`reverse`:		反转视频。交换前景色和背景色。
	`italic`:		斜体文本。
	`bold`:			粗体文本。
	`strikethrough`:	带删除线的文本。
	`underline`:		带下划线的文本。线条具有 `special` 颜色。
	`undercurl`:		带波浪下划线的文本。波浪线具有 `special` 颜色。
	`underdouble`:		带双下划线的文本。线条具有 `special` 颜色。
	`underdotted`:		带点线下划线的文本。点具有 `special` 颜色。
	`underdashed`:		带虚线下划线的文本。虚线具有 `special` 颜色。
	`altfont`:		备用字体。
	`dim`:			半亮/暗淡文本。
	`blink`:		闪烁文本。
	`conceal`:		隐藏/不可见文本。
	`overline`:		带上划线的文本。
	`blend`:		混合级别 (0-100)。UI 可用于支持将浮动窗口与背景混合或指示透明光标。
	`url`:			与此高亮关联的 URL。UI 应将此区域呈现为可点击的超链接。

	对于缺失的颜色键，应使用默认颜色。不要在表中存储默认值，而是存储一个哨兵值，以便更改的默认颜色生效。
	所有布尔键默认为 false，并且仅在其为 true 时发送。

	高亮始终以 RGB 格式和终端 256 色代码形式传输，分别作为 `rgb_attr` 和 `cterm_attr` 参数。|ui-rgb| 选项不再有效。
	大多数外部 UI 只需要存储和使用 `rgb_attr` 属性。

	`id` 0 将始终用于由 `default_colors_set` 定义颜色且未应用任何样式的默认高亮。

	注意：如果其内部高亮表已满，Nvim 可能会重用 `id` 值。在这种情况下，Nvim 将始终重绘受重新定义 id 影响的屏幕单元格，因此 UI 无需自行跟踪这一点。

	`info` 是一个空数组，除非启用了 |ui-hlstate|。

["hl_group_set", name, hl_id] ~
	内置高亮组 `name` 被设置为使用先前 `hl_attr_define` 调用定义的属性 `hl_id`。渲染直接使用属性 id 的网格不需要此事件，但对于希望使用一致高亮渲染自身元素的 UI 很有用。例如，使用 |ui-popupmenu| 事件的 UI 可能会使用 |hl-Pmenu| 系列的内置高亮。

							    *ui-event-grid_line*
["grid_line", grid, row, col_start, cells, wrap] ~
	重绘 `grid` 上 `row` 的连续部分，从列 `col_start` 开始。`cells` 是一个数组的数组，每个数组有 1 到 3 个项目：
	`[text(, hl_id, repeat)]` 。`text` 是应放入单元格的 UTF-8 文本，具有先前 `hl_attr_define` 调用定义的高亮 `hl_id`。如果不存在 `hl_id`，则应使用同一调用中最近看到的 `hl_id`（对于事件中的第一个单元格总是会发送）。如果存在 `repeat`，则单元格应重复 `repeat` 次（包括第一次），否则只一次。

	双宽字符的右侧单元格将表示为空字符串。双宽字符从不使用 `repeat`。

	如果单元格更改数组未到达行尾，则其余部分应保持不变。当应清除行的其余部分时，将发送一个空格字符，重复足够多次以覆盖剩余行。

	`wrap` 是一个布尔值，指示此行是否换行到下一行。当重绘换行到下一行的行时，Nvim 将发出一个 `grid_line` 事件，覆盖该行的最后一列，并将 `wrap` 设置为 true，然后紧接着发出一个从下一行第一列开始的 `grid_line` 事件。

["grid_clear", grid] ~
	清除 `grid`。

["grid_destroy", grid] ~
	`grid` 将不再被使用，UI 可以释放与其关联的任何数据。

["grid_cursor_goto", grid, row, col] ~
	使 `grid` 成为当前网格，`row, col` 成为此网格上的光标位置。此事件在 `redraw` 批次中最多发送一次，并指示可见光标位置。

["grid_scroll", grid, top, bot, left, right, rows, cols] ~
	滚动 `grid` 的区域。这在语义上与编辑器 |scrolling| 无关，而是表示“复制这些屏幕单元格”的优化方式。

	以下图表显示了每个滚动方向发生的情况。
	"===" 表示 SR（滚动区域）边界。
	"---" 表示移动的矩形。
	注意，dst 和 src 共享一个公共区域。

	如果 `rows` 大于 0，则在 SR 中向上移动一个矩形，这可能在向下滚动时发生。
>
		+-------------------------+
		| (clipped above SR)      |            ^
		|=========================| dst_top    |
		| dst (still in SR)       |            |
		+-------------------------+ src_top    |
		| src (moved up) and dst  |            |
		|-------------------------| dst_bot    |
		| src (invalid)           |            |
		+=========================+ src_bot
<
	如果 `rows` 小于 0，则在 SR 中向下移动一个矩形，这可能在向上滚动时发生。
>
		+=========================+ src_top
		| src (invalid)           |            |
		|------------------------ | dst_top    |
		| src (moved down) and dst|            |
		+-------------------------+ src_bot    |
		| dst (still in SR)       |            |
		|=========================| dst_bot    |
		| (clipped below SR)      |            v
		+-------------------------+
<
	在此版本的 Nvim 中，`cols` 始终为零，并保留供将来使用。

	注意从 |ui-grid-old| 事件更新代码时：范围是结束排他的，这与 API 约定一致，但与 `set_scroll_region` 不同，后者是结束包含的。

	滚动进入的区域将直接在滚动事件之后使用 |ui-event-grid_line| 填充。因此，UI 不需要作为处理滚动事件的一部分来清除此区域。

==============================================================================
网格事件（基于单元格）					   *ui-grid-old*

这是屏幕网格的传统表示形式，在未激活 |ui-linegrid| 时发出。新的 UI 应实现 |ui-linegrid| 而不是此协议。

["resize", width, height] ~
	网格大小调整为 `width` 和 `height` 个单元格。

["clear"] ~
	清除网格。

["eol_clear"] ~
	从光标位置清除到当前行末尾。

["cursor_goto", row, col] ~
	将光标移动到位置 (row, col)。目前，同一光标用于定义文本插入的位置和可见光标。但是，只有在处理完 "redraw" 事件中的整个数组后，最后一个光标位置才旨在成为可见光标位置。

["update_fg", color] ~
["update_bg", color] ~
["update_sp", color] ~
	分别设置默认前景色、背景色和特殊颜色。

							*ui-event-highlight_set*
["highlight_set", attrs] ~
	设置下一个放置在网格上的文本将具有的属性。`attrs` 是一个包含以下键的字典。任何缺失的键都将重置为其默认值。颜色默认值由 `update_fg` 等更新设置。所有布尔键默认为 false。

	`foreground`:	前景色。
	`background`:	背景色。
	`special`:	用于各种下划线的颜色，当存在时。
	`reverse`:	反转视频。交换前景色和背景色。
	`italic`:	斜体文本。
	`bold`:		粗体文本。
	`strikethrough`:  带删除线的文本。
	`underline`:	带下划线的文本。线条具有 `special` 颜色。
	`undercurl`:	带波浪下划线的文本。波浪线具有 `special` 颜色。
	`underdouble`:	带双下划线的文本。线条具有 `special` 颜色。
	`underdotted`:	带点线下划线的文本。点具有 `special` 颜色。
	`underdashed`:	带虚线下划线的文本。虚线具有 `special` 颜色。
	`dim`:		半亮/暗淡文本。
	`blink`:	闪烁文本。
	`conceal`:	隐藏/不可见文本。
	`overline`:	带上划线的文本。
["put", text] ~
	将（utf-8 编码的）字符串 `text` 放置在光标位置（光标随之移动），并使用上次 `highlight_set` 更新设置的高亮。

["set_scroll_region", top, bot, left, right] ~
	定义下方 `scroll` 使用的滚动区域。

	注意：范围是结束包含的，这与 API 约定不一致。

["scroll", count] ~
	在滚动区域内滚动文本。下面的图表说明了将发生的情况，具体取决于滚动方向。"=" 用于表示 SR（滚动区域）边界，"-" 用于表示移动的矩形。注意 dst 和 src 共享一个公共区域。

	如果 count 大于 0，则在 SR 中向上移动一个矩形，这可能在向下滚动时发生。
>
		+-------------------------+
		| (clipped above SR)      |            ^
		|=========================| dst_top    |
		| dst (still in SR)       |            |
		+-------------------------+ src_top    |
		| src (moved up) and dst  |            |
		|-------------------------| dst_bot    |
		| src (cleared)           |            |
		+=========================+ src_bot
<
	如果 count 小于 0，则在 SR 中向下移动一个矩形，这可能在向上滚动时发生。
>
		+=========================+ src_top
		| src (cleared)           |            |
		|------------------------ | dst_top    |
		| src (moved down) and dst|            |
		+-------------------------+ src_bot    |
		| dst (still in SR)       |            |
		|=========================| dst_bot    |
		| (clipped below SR)      |            v
		+-------------------------+
<
==============================================================================
高亮事件						  *ui-hlstate*

由 `ext_hlstate` |ui-option| 激活。
隐式激活 |ui-linegrid|。

如果启用了 `ext_hlstate`，Nvim 将在 |ui-linegrid| 事件中发出详细的高亮状态。否则（默认情况下），Nvim 仅使用在 |ui-event-highlight_set| 处描述的最终计算出的高亮属性来描述网格单元格。

`ext_hlstate` 为每个网格单元格提供活动高亮的语义描述。高亮在表中预定义，参见 |ui-event-hl_attr_define| 和 |ui-event-grid_line|。

`hl_attr_define` 中的 `info` 参数包含高亮的语义描述。因为高亮组可以组合，所以这是一个数组，其中优先级最高的项目在最后。每个项目是一个包含以下键的字典：

    `kind`:	始终存在。为以下值之一：
	"ui":       内置 UI 高亮。|highlight-groups|
	"syntax":   由语法声明或其他运行时/插件功能（如 |nvim_buf_set_extmark()|）应用于缓冲区的高亮
	"terminal": 来自 |terminal-emulator| 中运行的进程的高亮。不包含进一步的语义信息。
    `ui_name`:	来自 |highlight-groups| 的高亮名称。仅适用于 "ui" 类型。
    `hi_name`:	定义所用属性的最终 |:highlight| 组的名称。
    `id`:	表示此项目的唯一数字 id。

注意："ui" 项目将同时具有 `ui_name` 和 `hi_name`。它们可能不同，因为内置组链接到另一个组 |:hi-link|，或者因为使用了 'winhighlight'。即使高亮组被清除，UI 项目也会被传输，因此即使未应用任何属性，`ui_name` 也始终可用于可靠地识别屏幕元素。

==============================================================================
多网格事件						 *ui-multigrid*

由 `ext_multigrid` |ui-option| 激活。
隐式激活 |ui-linegrid|。

网格事件参见 |ui-linegrid|。
请求更改网格大小参见 |nvim_ui_try_resize_grid()|。
向 Nvim 发送鼠标事件参见 |nvim_input_mouse()|。

多网格扩展让 UI 能更好地控制窗口的显示方式：
- UI 在每个窗口的单独网格上接收更新。
- UI 可以独立于窗口在全局布局上占用的空间来设置网格大小。因此，UI 可以为每个窗口使用不同的字体大小。或者为窗口边框周围保留空间，用于 UI 工具包中的滚动条等自身元素。
- 一个专用的网格用于消息，这些消息可能会在窗口区域上滚动。（或者可以使用 |ui-messages|）。

默认情况下，网格大小由 Nvim 处理，并在创建拆分时设置为外部网格大小（即 Nvim 中窗口框架的大小）。一旦 UI 设置了网格大小，Nvim 就不再处理该网格的大小，当外部大小改变时，UI 必须更改网格大小。要将网格大小处理委托回 Nvim，请请求大小 (0, 0)。

窗口可以隐藏并重新显示，而无需释放其网格。对于同一个窗口，这种情况可能发生多次，例如在切换标签页时。

["win_pos", grid, win, start_row, start_col, width, height] ~
	在 Nvim 中设置网格的位置和大小（即外部网格大小）。如果窗口之前是隐藏的，现在应再次显示。

["win_float_pos", grid, win, anchor, anchor_grid, anchor_row, anchor_col, mouse_enabled, zindex, compindex, screen_row, screen_col] ~
	显示或重新配置浮动窗口 `win`。

	有两种定位窗口的方式可供选择
	  - 手动 - 窗口应显示在另一个网格 `anchor_grid` 之上，位于指定位置 `anchor_row` 和 `anchor_col`。关于 `anchor` 的含义和定位的更多细节，请参见 |nvim_open_win()|。注意：您必须手动确保窗口适合屏幕，可能需要进一步重新定位。在这种情况下忽略 `screen_row` 和 `screen_col`。
	  - 让 nvim 负责定位 - 您可以忽略 `anchor` 并将窗口显示在 `screen_row` 和 `screen_col`。

	如果窗口可以接收鼠标事件，则 `mouse_enabled` 为 true。

	`zindex` 是配置的 zindex，而 `compindex` 是由 nvim 确定的精确渲染顺序。要完全像 TUI 一样渲染，首先渲染所有非浮动窗口，然后按 `compindex` 顺序渲染，覆盖任何浮动窗口单元格。最后，将浮动窗口单元格与非浮动背景混合。要添加更多混合，可以按 zindex 对窗口进行分组，并在层之间进行混合。但请注意，同一 zindex 内的窗口仍应覆盖同一层内的先前单元格，而不进行混合。这确保了渲染多个窗口（例如添加边框）的插件能按预期工作。

["win_external_pos", grid, win] ~
	显示或重新配置外部窗口 `win`。该窗口应在桌面环境中显示为一个单独的顶层窗口，或类似形式。

["win_hide", grid] ~
	停止显示窗口。窗口稍后可以再次显示。

["win_close", grid] ~
	关闭窗口。

["msg_set_pos", grid, row, scrolled, sep_char, zindex, compindex] ~
	在 `grid` 上显示消息。该网格将显示在默认网格（grid=1）上的 `row` 处，覆盖全列宽。`scrolled` 指示消息区域是否已滚动以覆盖其他网格。绘制分隔符然后 |msgsep| 可能很有用。内置 TUI 绘制一条用 `sep_char`（'fillchars' msgsep 字段）填充的完整线，并使用 |hl-MsgSeparator| 高亮。

	当 |ui-messages| 处于活动状态时，不使用消息网格，也不会发送此事件。

	`zindex` 和 `compindex` 的含义与 `win_float_pos` 相同。`zindex` 始终具有固定值 200，为完整性而包含。

["win_viewport", grid, win, topline, botline, curline, curcol, line_count, scroll_delta] ~
	指示窗口中显示的缓冲区文本范围，以及缓冲区中的光标位置。所有位置均从零开始。如果末尾之后有填充行，`botline` 设置为缓冲区行数加一。`scroll_delta` 包含自上次发出 `win_viewport` 以来窗口顶行移动的量。它旨在用于实现平滑滚动。为此，它只计算“虚拟”或“显示”行，因此折叠仅计为一行。当滚动超过整个屏幕时，它是一个近似值。

	批次中的所有更新（如 `grid_line`）都会影响新的视口，尽管 `win_viewport` 是在更新之后接收的。实现平滑滚动等功能的应用程序应考虑到这一点，将网格与屏幕上显示的内容分开，并在收到 `win_viewport` 后将其复制到视口目标。

["win_viewport_margins", grid, win, top, bottom, left, right] ~
	指示窗口网格的边距，这些边距 _不_ 属于 `win_viewport` 事件指示的视口。例如，在存在 'winbar' 和浮动窗口边框时会发生这种情况。

["win_extmark", grid, win, ns_id, mark_id, row, col] ~
	更新当前在窗口中可见的 extmark 的位置。仅当标记具有 `ui_watched` 属性时才发出。

==============================================================================
弹出菜单事件						 *ui-popupmenu*

由 `ext_popupmenu` |ui-option| 激活。

此 UI 扩展委托呈现 |popupmenu-completion| 和命令行 'wildmenu'。

UI 决定如何呈现菜单。例如，根据上一个 `mode_change` 事件，命令行 wildmenu 可能水平呈现，而插入模式补全则会显示垂直弹出菜单。

["popupmenu_show", items, selected, row, col, grid] ~
	显示 |popupmenu-completion|。`items` 是要显示的补全项数组；每个项是一个格式为 [word, kind, menu, info] 的数组，定义见 |complete-items|，区别在于如果存在 `abbr`，则 `word` 被 `abbr` 替换。`selected` 是初始选中的项，是项数组的从零开始的索引（-1 表示未选中任何项）。`row` 和 `col` 给出锚点位置，已补全单词的第一个字符将在此处。当使用 |ui-multigrid| 时，`grid` 是锚点位置的网格。当 `ext_cmdline` 处于活动状态时，`grid` 设置为 -1，表示弹出菜单应锚定到外部命令行。然后 `col` 将是命令行文本中的字节位置。

["popupmenu_select", selected] ~
	在当前弹出菜单中选择一个项。`selected` 是来自上一个 popupmenu_show 事件的项目数组的从零开始的索引，如果未选中任何项则为 -1。

["popupmenu_hide"] ~
	隐藏弹出菜单。

==============================================================================
标签栏事件							   *ui-tabline*

由 `ext_tabline` |ui-option| 激活。

["tabline_update", curtab, tabs, curbuf, buffers] ~
	标签栏已更新。UI 应在自定义标签栏小部件中呈现此数据。注意：选项 `curbuf` + `buffers` 是在 API7 中添加的。
	curtab:   当前标签页
	tabs:     字典列表 [{ "tab": Tabpage, "name": String }, ...]
	curbuf:   当前缓冲区句柄。
	buffers:  字典列表 [{ "buffer": buffer handle, "name": String}, ...]

==============================================================================
命令行事件							   *ui-cmdline*

由 `ext_cmdline` |ui-option| 激活。

此 UI 扩展委托呈现 |cmdline|（'wildmenu' 除外）。对于命令行 'wildmenu' UI 事件，请激活 |ui-popupmenu|。

["cmdline_show", content, pos, firstc, prompt, indent, level, hl_id] ~
        content: [attrs, string, hl_id] 的列表
	         [[{}, "t", hl_id], [attrs, "est", hl_id], ...]

	当命令行显示或更改时触发。
	`content` 是应在命令行中显示的完整内容，`pos` 是命令行中光标的位置。内容被划分为具有不同高亮属性的块，这些属性表示为字典（参见 |ui-event-highlight_set|）。

	`firstc` 和 `prompt` 是文本，如果非空，应显示在命令行前面。`firstc` 始终指示内置命令行，如 `:`（ex 命令）和 `/` `?`（搜索），而 `prompt` 是 |input()| 提示，使用 `hl_id` 高亮。`indent` 指示内容应缩进多少个空格。

	Nvim 命令行可以递归调用，例如通过在命令行提示符下键入 `<c-r>=`。`level` 字段用于区分同时活动的不同命令行。第一个调用的命令行级别为 1，下一个递归调用的提示级别为 2。从 |cmdline-window| 调用的命令行级别高于被编辑的命令行。

["cmdline_pos", pos, level] ~
	更改命令行中的光标位置。

["cmdline_special_char", c, shift, level] ~
	在命令行中的光标位置显示一个特殊字符。这通常用于指示待处理状态，例如在 |c_CTRL-V| 之后。如果 `shift` 为 true，则光标后的文本应移动，否则应覆盖光标处的字符。

	应在下一个 cmdline_show 时隐藏。

["cmdline_hide", level, abort] ~
	隐藏命令行。`level` 是被隐藏命令行的嵌套级别。`abort` 为 true 表示命令行在因中止条件（|c_Esc| 或 |c_CTRL-C|）而隐藏。

["cmdline_block_show", lines] ~
	向当前命令行显示一个上下文块。例如，如果用户以交互方式定义 |:function|：>vim
	    :function Foo()
	    :  echo "foo"
	    :
<
	`lines` 是高亮块的行列表，格式与 "cmdline_show" 的 `contents` 参数相同。

["cmdline_block_append", line] ~
	在当前显示的块末尾追加一行。

["cmdline_block_hide"] ~
	隐藏该块。

==============================================================================
消息/对话框事件					   *ui-messages*

由 `ext_messages` |ui-option| 激活。
隐式激活 |ui-linegrid| 和 |ui-cmdline|。

此 UI 扩展委托呈现消息和对话框。否则将在消息/命令行屏幕空间中渲染的消息，将作为 UI 事件发出。

Nvim 不会为命令行或消息分配屏幕空间。'cmdheight' 将设置为零，但可以更改并用于替换命令行或消息窗口。命令行状态作为 |ui-cmdline| 事件发出，UI 必须处理这些事件。

["msg_show", kind, content, replace_last, history, append, id, trigger] ~
	向用户显示一条消息。更新（替换）任何与 `id` 匹配的现有消息。

	kind
	    指示消息类型的名称：
		"" (empty)	未知（考虑 |feature-request|）
		"empty"		空消息（`:echo ""`），带有空 `content`。
				如果它是批次中唯一的消息，则应清除共享 'cmdheight' 区域的消息。
		"bufwrite"	|:write| 消息
		"confirm"	提示前的消息（|:confirm|、|confirm()|、|inputlist()|、|z=|、…）
		"emsg"		错误（|errors|、内部错误、|:throw|、…）
		"echo"		|:echo| 消息
		"echomsg"	|:echomsg| 消息
		"echoerr"	|:echoerr| 消息
		"completion"    |ins-completion-menu| 消息
		"list_cmd"	各种命令的列表输出（|:ls|、|:set|、…）
		"lua_error"	|:lua| 代码中的错误
		"lua_print"	|:lua| 代码中的 |print()|
		"progress"	由 |nvim_echo()| 发出的进度消息
		"rpc_error"	来自 |rpcrequest()| 的错误响应
		"quickfix"	Quickfix 导航消息
		"search_cmd"	输入的搜索命令
		"search_count"	搜索计数消息（'shortmess' 的 "S" 标志）
		"shell_cmd"	|:!cmd| 执行的命令
		"shell_err"	|:!cmd| shell stderr 输出
		"shell_out"	|:!cmd| shell stdout 输出
		"shell_ret"	|:!cmd| shell 返回代码
		"undo"		|:undo| 和 |:redo| 消息
		"verbose"	'verbose' 消息
		"wildlist"	'wildmode' "list" 消息
		"wmsg"		警告（"search hit BOTTOM"、|W10|、…）
	    未来可能会添加新的类型；客户端应将未知类型视为空类型。

	content
	    `[attr_id, text_chunk, hl_id]` 元组的数组，构建由不同高亮块组成的消息文本。块之间不应添加额外的间距，`text_chunk` 本身包含任何必要的空白。消息可以包含换行符 "\n"。

	replace_last
	    决定应如何显示多条消息：
	    false: 将消息与所有仍可见的先前消息一起显示。
	    true:  替换最近一次 `msg_show` 调用中的消息，但任何其他可见消息应保留。

	history
	    如果消息已添加到 |:messages| 历史记录中，则为 True。

	append
	    如果消息应追加到前一条消息，而不是从新行开始，则为 True。为 |:echon| 设置。

	id
	    消息的唯一标识符。可以是整数或字符串。应替换具有相同 id 的（可见）消息。

	trigger
	    触发消息的操作类型：
		"" (empty)      未知（考虑 |feature-request|）
		"typed_cmd"     在 |cmdline| 上交互式键入的命令

["msg_clear"] ~
	清除当前由 "msg_show" 显示的所有消息，在清除屏幕后发出（下面其他 "msg_" 事件发送的消息不应受影响）。

	指导： “清除消息”行为是 UI 特定的。如果 UI 在新窗口中呈现消息，它可能会选择在几秒后清除消息。如果 UI 在持久区域（例如命令行）中呈现消息，则应在下一批次（通常是下一个事件循环周期）开始时清除消息。

["msg_showmode", content] ~
	显示 'showmode' 和 |recording| 消息。`content` 的格式与 "msg_show" 中的相同。此事件在发送空 `content` 时隐藏最后一条消息。

["msg_showcmd", content] ~
	显示 'showcmd' 消息。`content` 的格式与 "msg_show" 中的相同。此事件在发送空 `content` 时隐藏最后一条消息。

["msg_ruler", content] ~
	当状态栏中没有空间显示标尺时，用于显示 'ruler'。`content` 的格式与 "msg_show" 中的相同。此事件在发送空 `content` 时隐藏最后一条消息。

["msg_history_show", entries, prev_cmd] ~
	当调用 |:messages| 或 |g<| 命令时发送。历史记录作为条目列表发送，其中每个条目是一个 `[kind, content, append]` 元组。

	prev_cmd
	    当使用 |g<| 命令发送时为 True，使用 |:messages| 时为 False。

 vim:tw=78:ts=8:noet:ft=help:norl:
