*change.txt*    Nvim


		  VIM 参考手册	 由 Bram Moolenaar 编写

此文件描述了删除或更改文本的命令。在此上下文中，
更改文本意味着删除文本并使用一个命令将其替换为其他文本。
您可以撤消所有这些命令。您可以使用 "." 命令重复非 Ex 命令。

有关插入文本，请参见 |insert.txt|。

键入 |gO| 可查看目录。

==============================================================================
1. 删除文本						*deleting* *E470*

["x]<Del>	或					*<Del>* *x* *dl*
["x]x		删除光标下及之后 [count] 个字符
			[存入寄存器 x]（非 |行方式|）。与
			"dl" 功能相同。
			<Del> 键不接受 [count]。相反，它会
			删除计数中的最后一个字符。
			关于删除换行符（连接行），请参见 'whichwrap'。

							*X* *dh*
["x]X		删除光标前 [count] 个字符 [存入
			寄存器 x]（非 |行方式|）。与 "dh" 功能相同。
			另请参见 'whichwrap'。

							*d*
["x]d{motion}		删除 {motion} 所覆盖的文本 [存入寄存器
			x]。例外情况见下文。

							*dd*
["x]dd		删除 [count] 行 [存入寄存器 x] |行方式|。

							*D*
["x]D		删除从光标位置到行尾的字符以及
			接下来的 [count]-1 行 [存入寄存器
			x]；是 "d$" 的同义词。
			（非 |行方式|）

{Visual}["x]x	或					*v_x* *v_d* *v_<Del>*
{Visual}["x]d   或
{Visual}["x]<Del>	删除高亮显示的文本 [存入寄存器 x]（关于
			{Visual} 请参见 |Visual-mode|）。

{Visual}["x]CTRL-H   或					*v_CTRL-H* *v_<BS>*
{Visual}["x]<BS>	在选择模式下：删除高亮显示的文本 [存入
			寄存器 x]。

{Visual}["x]X	或					*v_X* *v_D* *v_b_D*
{Visual}["x]D		删除高亮显示的行 [存入寄存器 x]（关于
			{Visual} 请参见 |Visual-mode|）。在可视块模式下，
			"D" 会删除高亮显示的文本以及直到
			行尾的所有文本。

					*:d* *:de* *:del* *:delete* *:dl* *:dp*
:[range]d[elete] [x]	删除 [range] 行（默认：当前行）[存入
			寄存器 x]。
			注意这些奇怪的缩写：
			  :dl		删除并列出
			  :dell		同上
			  :delel	同上
			  :deletl	同上
			  :deletel	同上
			  :dp		删除并打印
			  :dep		同上
			  :delp		同上
			  :delep	同上
			  :deletp	同上
			  :deletep	同上

:[range]d[elete] [x] {count}
			删除 {count} 行，从 [range] 开始
			（默认：当前行 |cmdline-ranges|）[存入
			寄存器 x]。

这些命令删除文本。您可以使用 `.` 命令（除了 `:d`）重复它们，
并可以撤消它们。使用可视模式删除文本块。关于寄存器的说明，
请参见 |registers|。
							*d-special*
d{motion} 命令的例外：如果移动不是行方式的，
移动的起点和终点不在同一行，并且起点之前只有
空白字符，且移动终点之后没有非空白字符，
则删除变为行方式的。这意味着删除也会移除
您可能期望保留的空白行。使用 |o_v| 操作符强制
移动为字符方式。

尝试删除空文本区域（例如，在第一列执行 "d0"）
当 'cpoptions' 包含 'E' 标志时是一个错误。

							*J*
J		连接 [count] 行，至少两行。
			移除缩进并插入最多两个空格（参见
			下文）。当在缓冲区的最后一行时失败。
			如果 [count] 太大，则减少为可用的
			行数。

							*v_J*
{Visual}J		连接高亮显示的行，至少两行。
			移除缩进并插入最多两个空格（参见下文）。

							*gJ*
gJ		连接 [count] 行，至少两行。
			不插入或删除任何空格。

							*v_gJ*
{Visual}gJ		连接高亮显示的行，至少两行。
			不插入或删除任何空格。

							*:j* *:join*
:[range]j[oin][!] [flags]
			连接 [range] 行。与 "J" 相同，但使用 [!] 时，
			连接不插入或删除任何空格。
			如果 [range] 的起始值和结束值相同，则此
			命令不执行任何操作。默认行为是
			连接当前行及其下方的一行。
			关于 [flags]，请参见 |ex-flags|。

:[range]j[oin][!] {count} [flags]
			连接 {count} 行，从 [range] 开始（默认：
			当前行 |cmdline-ranges|）。与 "J" 相同，但使用
			[!] 时，连接不插入或删除任何空格。
			关于 [flags]，请参见 |ex-flags|。

这些命令删除行间的 <EOL>。其效果是将多行
连接成一行。您可以重复这些命令（除了 `:j`）并
撤消它们。

除了 "gJ" 之外，这些命令会用空格替换 <EOL>，除非
有尾随空格或下一行以 ')' 开头。除了 "gJ" 之外，
这些命令会删除下一行开头的任何前导空格。如果
'joinspaces' 选项开启，这些命令会在 '.'、'!' 或 '?' 后插入两个空格。
'formatoptions' 中的 'B' 和 'M' 标志会改变在多字节字符前后
插入空格的行为 |fo-table|。

|'[| 标记设置在已连接的第一行末尾，|']| 设置在
结果行的末尾。


==============================================================================
2. 删除并插入				*delete-insert* *replacing*

							*R*
R		进入替换模式：您键入的每个字符都会替换
			一个现有字符，从光标下的字符开始。
			重复输入文本 [count]-1 次。更多细节请参见
			|Replace-mode|。

							*gR*
gR		进入虚拟替换模式：您键入的每个字符都会在屏幕空间
			中替换现有字符。因此，一个 <Tab> 可能一次替换多个字符。
			重复输入文本 [count]-1 次。更多细节请参见
			|Virtual-Replace-mode|。

							*c*
["x]c{motion}		删除 {motion} 文本 [存入寄存器 x] 并开始
			插入。当 'cpoptions' 包含 'E' 标志且
			没有要删除的文本时（例如，光标刚在 'x' 之后执行 "cTx"），
			会发生错误且插入模式不会启动（这与 Vi 兼容）。
			当 'cpoptions' 不包含 'E' 标志时，
			"c" 命令总是启动插入模式，即使
			没有要删除的文本。

							*cc*
["x]cc		删除 [count] 行 [存入寄存器 x] 并开始
			插入 |行方式|。如果 'autoindent' 开启，则保留
			第一行的缩进。

							*C*
["x]C		删除从光标位置到行尾以及接下来的
			[count]-1 行 [存入寄存器 x]，
			并开始插入。是 c$ 的同义词（非 |行方式|）。

							*s*
["x]s		删除 [count] 个字符 [存入寄存器 x] 并开始
			插入（s 代表替换）。是 "cl" 的同义词
			（非 |行方式|）。

							*S*
["x]S		删除 [count] 行 [存入寄存器 x] 并开始
			插入。是 "cc" 的同义词 |行方式|。

{Visual}["x]c	或					*v_c* *v_s*
{Visual}["x]s		删除高亮显示的文本 [存入寄存器 x] 并
			开始插入（关于 {Visual} 请参见 |Visual-mode|）。

							*v_r*
{Visual}r{char}		将所有选中的字符替换为 {char}。
			CTRL-C 将被原样插入。

							*v_C*
{Visual}["x]C		删除高亮显示的行 [存入寄存器 x] 并
			开始插入。在可视块模式下，其工作方式
			不同 |v_b_C|。
							*v_S*
{Visual}["x]S		删除高亮显示的行 [存入寄存器 x] 并
			开始插入（关于 {Visual} 请参见 |Visual-mode|）。
							*v_R*
{Visual}["x]R		目前与 {Visual}["x]S 相同。在下一个版本中
			可能工作方式不同。

注意：
- 您可以使用 <Esc> 结束插入和替换模式。
- 关于这些模式中的其他特殊字符，请参见章节 "插入和替换模式" |mode-ins-repl|。
- [count] 的效果在 Vim 退出插入或替换模式后生效。
- 当 'cpoptions' 选项包含 '$' 且更改在同一行内时，
  Vim 会继续显示要删除的文本，并在最后一个
  被删除的字符处放置 '$'。

关于寄存器的说明，请参见 |registers|。

替换模式与插入模式类似，但您输入的每个字符都会删除一个字符。
如果到达行尾，Vim 会追加更多字符（就像插入模式一样）。
在替换模式下，退格键会恢复原始文本（如果有的话）。
（参见章节 "插入和替换模式" |mode-ins-repl|）。

						*cw* *cW*
特殊情况：当光标位于单词中时，"cw" 和 "cW" 不包括单词后的
空白字符，它们只更改到单词末尾。这是因为
Vim 将 "cw" 解释为更改单词，而单词不包含
后面的空白字符。

如果您希望 "cw" 包含单词后的空格，请参见 |cpo-_| 以更改
行为。或者，使用 "caw"（参见 |aw|）。

							*:c* *:ch* *:change*
:{range}c[hange][!]	用一些不同的文本替换文本行。
			键入一个仅包含 "." 的行以停止替换。
			如果没有 {range}，此命令仅更改当前行。
			添加 [!] 会在执行此命令期间临时切换 'autoindent'。

==============================================================================
3. 简单更改				*simple-change* *changing*

							*r*
r{char}		用 {char} 替换光标下的字符。
			如果 {char} 是 <CR> 或 <NL>，则换行符会替换该
			字符。要替换为真正的 <CR>，请使用 CTRL-V
			<CR>。CTRL-V <NL> 替换为 <Nul>。

			如果 {char} 是 CTRL-E 或 CTRL-Y，则使用下方或上方
			行的字符，就像 |i_CTRL-E| 和 |i_CTRL-Y| 一样。
			这也适用于计数，因此 `10r<C-E>` 会从下一行复制 10 个字符。

			如果指定了 [count]，Vim 会将 [count] 个字符
			替换为 [count] 个 {char}。但是，当 {char} 是 <CR> 或 <NL> 时，
			Vim 仅插入一个 <CR>："5r<CR>" 将五个字符替换为单个换行符。
			当 {char} 是 <CR> 或 <NL> 时，Vim 会执行
			自动缩进。这就像删除被替换的
			字符然后执行 "i<CR><Esc>" 一样。
			{char} 可以作为双字符组合输入 |digraph-arg|。
			|:lmap| 映射适用于 {char}。插入模式下的 CTRL-^ 命令
			可用于切换此功能的开关 |i_CTRL-^|。
			关于在 'encoding' 为 Unicode 时使用组合字符，
			请参见 |utf-8-char-arg|。

							*gr*
gr{char}		用 {char} 替换光标下的虚拟字符。
			这在屏幕空间中进行替换，而不是文件
			空间。更多细节请参见 |gR| 和 |Virtual-Replace-mode|。
			与 |r| 一样，可以指定计数。
			{char} 的输入方式与 |r| 类似，但
			在插入模式中有特殊含义的字符，例如
			大多数 CTRL 键，不能使用。

							*gr-default*
			Nvim 创建 |lsp-defaults| 映射，这可能会抑制
			|gr| 的内置行为。使用此命令恢复
			内置行为： >
				nnoremap <nowait> gr gr
<

						*digraph-arg*
像 |r| 和 |t| 这样的普通模式命令的参数是单个字符。
当 'cpo' 不包含 'D' 标志时，此字符也可以像 |digraphs| 一样输入。
先键入 CTRL-K，然后键入两个双字符组合字符。

						*case*
以下命令更改字母的大小写。使用当前激活的
|locale|。请参见 |:language|。LC_CTYPE 值在此起作用。

							*~*
~			'notildeop' 选项：切换光标下字符的大小写，
			并将光标向右移动。
			如果指定了 [count]，则对那么多个字符进行操作。

~{motion}		'tildeop' 选项：切换 {motion} 文本的大小写。

							*g~*
g~{motion}		切换 {motion} 文本的大小写。

g~g~							*g~g~* *g~~*
g~~		切换当前行的大小写。

							*v_~*
{Visual}~		切换高亮显示文本的大小写（关于 {Visual} 请参见
			|Visual-mode|）。

							*v_U*
{Visual}U		将高亮显示的文本转换为大写（关于 {Visual} 请参见
			|Visual-mode|）。

							*gU* *uppercase*
gU{motion}		将 {motion} 文本转换为大写。
			示例： >
				:map! <C-F> <Esc>gUiw`]a
<			这适用于插入模式：按 CTRL-F 可将
			光标前的单词转换为大写。方便先输入
			小写单词，然后将其转换为大写。


gUgU							*gUgU* *gUU*
gUU		将当前行转换为大写。

							*v_u*
{Visual}u		将高亮显示的文本转换为小写（关于 {Visual} 请参见
			|Visual-mode|）。

							*gu* *lowercase*
gu{motion}		将 {motion} 文本转换为小写。

gugu							*gugu* *guu*
guu		将当前行转换为小写。

							*g?* *rot13*
g?{motion}		对 {motion} 文本进行 Rot13 编码。

							*v_g?*
{Visual}g?		对高亮显示的文本进行 Rot13 编码（关于 {Visual} 请参见
			|Visual-mode|）。

g?g?							*g?g?* *g??*
g??		对当前行进行 Rot13 编码。

要将一行转换为标题大小写，使每个单词的首字母
大写： >
	:s/\v<(.)(\w*)/\u\1\L\2/g


加法和减法 ~
							*CTRL-A*
CTRL-A		将 [count] 加到光标处或之后的数字或字母字符上。

                                                       *v_CTRL-A*
{Visual}CTRL-A		将 [count] 加到高亮显示文本中的数字或字母字符上。

							*v_g_CTRL-A*
{Visual}g CTRL-A	将 [count] 加到高亮显示文本中的数字或字母字符上。
			如果高亮显示了多行，则每一行都会额外增加
			[count]（因此有效地创建了一个 [count] 递增序列）。
			例如，如果您有以下数字列表： >
				1.
				1.
				1.
				1.
<			移动到第二个 "1." 并使用可视方式选择三行，
			按 g CTRL-A 结果如下： >
				1.
				2.
				3.
				4.
<
							*CTRL-X*
CTRL-X		从光标处或之后的数字或字母字符中减去 [count]。

							*v_CTRL-X*
{Visual}CTRL-X		从高亮显示文本中的数字或字母字符中减去 [count]。

							*v_g_CTRL-X*
{Visual}g CTRL-X	从高亮显示文本中的数字或字母字符中减去 [count]。
			如果高亮显示了多行，则每一行的值都会额外减去
			[count]（因此有效地创建了一个 [count] 递减序列）。

CTRL-A 和 CTRL-X 命令适用于（带符号的）十进制数、无符号的
二进制/八进制/十六进制数和字母字符。

这取决于 'nrformats' 选项：
- 当 'nrformats' 包含 "bin" 时，Vim 假定以 '0b' 或 '0B' 开头的数字是二进制的。
- 当 'nrformats' 包含 "octal" 时，Vim 认为以 '0' 开头的数字是八进制的，
  除非该数字包含 '8' 或 '9'。其他数字是十进制的，并且可能带有前导减号。
  如果光标位于数字上，则命令适用于该数字；否则
  Vim 使用光标右侧的数字。
- 当 'nrformats' 包含 "hex" 时，Vim 假定以 '0x' 或 '0X' 开头的数字是十六进制的。
  数字中最右边字母的大小写决定了结果十六进制数的大小写。
  如果当前数字中没有字母，Vim 使用先前检测到的大小写。
- 当 'nrformats' 包含 "alpha" 时，Vim 将更改光标下或之后的字母字符。
  这对于创建带有字母索引的列表很有用。

对于十进制数，前导负号在增加或减少时会被考虑，对于二进制、八进制和十六进制值，则不会。
要在使用 CTRL-A 或 CTRL-X 之前忽略符号，请使用可视方式选择数字。

对于带有前导零的数字（包括所有八进制和十六进制数），
Vim 会尽可能保留数字中的字符数。对 "0077" 执行 CTRL-A 结果为 "0100"，
对 "0x100" 执行 CTRL-X 结果为 "0x0ff"。
有一个例外：当发现以零开头的数字不是八进制数（包含 '8' 或 '9'），
但 'nrformats' 包含 "octal" 时，会移除前导零以避免结果可能被识别为
八进制数。

注意，当 'nrformats' 包含 "octal" 时，带有前导零的十进制数会导致错误，
因为它们可能与八进制数混淆。

类似地注意，当 'nrformats' 同时包含 "bin" 和 "hex" 时，带有前导 '0x' 或 '0X' 的二进制数
可能被解释为十六进制数而不是二进制数，因为 '0b' 是有效的十六进制数字。
对 "0x0b11" 执行 CTRL-A 结果为 "0x0b12"，而不是 "0x0b100"。
当 'nrformats' 包含 "bin" 但不包含 "hex" 时，对 "0x0b11" 中的 "0b11" 执行 CTRL-A
结果为 "0x0b100"。

当光标下的数字太大而无法放入 64 位时，它将被
舍入到最接近的可表示数字，并且
加法/减法将被跳过。例如，对 18446744073709551616 执行 CTRL-X
结果为 18446744073709551615。对于更大的数字，如 18446744073709551618，也是如此。

CTRL-A 命令在宏中非常有用。示例：使用以下步骤创建一个编号列表。

1. 创建第一个列表条目，确保它以数字开头。
2. qa	     - 开始录制到寄存器 'a'
3. Y	     - 复制该条目
4. p	     - 将副本放在第一个条目下方
5. CTRL-A    - 增加数字
6. q	     - 停止录制
7. <count>@a - 重复复制、放置和增加 <count> 次


左右移动行				*shift-left-right*

							*<*
 <{motion}		将 {motion} 行向左移动一个 'shiftwidth'。

			如果 'shiftwidth' 选项设置为零，则缩进量
			由行中第一个非空白字符计算。
							*<<*
 <<		将 [count] 行向左移动一个 'shiftwidth'。

							*v_<*
{Visual}[count]<	将高亮显示的行向左移动 [count] 个 'shiftwidth'
			（关于 {Visual} 请参见 |Visual-mode|）。

							*>*
 >{motion}		将 {motion} 行向右移动一个 'shiftwidth'。

			如果 'shiftwidth' 选项设置为零，则缩进量
			由行中第一个非空白字符计算。
							*>>*
 >>		将 [count] 行向右移动一个 'shiftwidth'。

							*v_>*
{Visual}[count]>	将高亮显示的行向右移动 [count] 个 'shiftwidth'
			（关于 {Visual} 请参见 |Visual-mode|）。

							*:<*
:[range]<		将 [range] 行向左移动一个 'shiftwidth'。重复 '<'
			可移动多个 'shiftwidth'。

:[range]< {count}	将 {count} 行向左移动一个 'shiftwidth'，从
			[range] 开始（默认当前行 |cmdline-ranges|）。
			重复 '<' 可移动多个 'shiftwidth'。

:[range]le[ft] [indent]	将 [range] 中的行左对齐。将行中的缩进设置为
			[indent]（默认为 0）。

							*:>*
:[range]> [flags]	将 [range] 行向右移动一个 'shiftwidth'。
			重复 '>' 可移动多个 'shiftwidth'。
			关于 [flags]，请参见 |ex-flags|。

:[range]> {count} [flags]
			将 {count} 行向右移动一个 'shiftwidth'，从
			[range] 开始（默认当前行 |cmdline-ranges|）。
			重复 '>' 可移动多个 'shiftwidth'。
			关于 [flags]，请参见 |ex-flags|。

">" 和 "<" 命令对于更改程序中的缩进非常方便。
使用 'shiftwidth' 选项设置这些命令插入或删除的空格大小。
通常 'shiftwidth' 选项为 8，但您可以将其设置为，例如，3 以进行较小的缩进。
向左移动在没有缩进时停止。向右移动不影响空行。

如果 'shiftround' 选项开启，缩进会舍入到 'shiftwidth' 的倍数。

如果 'smartindent' 选项开启，或者 'cindent' 开启且 'cinkeys' 包含
值为零的 '#'，则向右移动不影响以 '#' 开头的行（这些行应该是 C 预处理行，
必须保持在第一列）。这可以通过 'cino' 选项更改，请参见 |cino-#|。

当 'expandtab' 选项关闭时（这是默认设置），Vim 尽可能使用 <Tab>
来生成缩进。您可以使用 ">><<" 将由空格组成的缩进替换为
由 <Tab>（以及必要时的一些空格）组成的相同缩进。
如果 'expandtab' 选项开启，Vim 仅使用空格。然后
您可以使用 ">><<" 将缩进中的 <Tab> 替换为空格（或使用
`:retab!`）。

要移动一行多个 'shiftwidth'，请使用可视模式或 `:` 命令。
例如： >
	Vjj4>		将三行向右移动 4 个缩进
	:<<<		将当前行向左移动 3 个缩进
	:>> 5		将 5 行向右移动 2 个缩进
	:5>>		将第 5 行向右移动 2 个缩进

==============================================================================
4. 复杂更改					*complex-change*

4.1 过滤器命令					*filter*

过滤器是一个程序，它接受标准输入中的文本，以某种方式更改它，
并将其发送到标准输出。您可以使用下面的命令将一些文本
发送给过滤器，以便将其替换为过滤器的输出。
过滤器的示例包括 "sort"（按字母顺序排序行）和
"indent"（格式化 C 程序文件；您需要一个可以像过滤器一样工作的 indent 版本；
并非所有版本都可以）。'shell' 选项指定 Vim 用于执行过滤器命令的 shell。
您可以使用 "." 重复过滤器命令。Vim 在 `:!` 命令之后不识别注释（以 '"' 开头）。

							*!*
!{motion}{filter}	通过外部程序 {filter} 过滤 {motion} 文本行。

							*!!*
!!{filter}		通过外部程序 {filter} 过滤 [count] 行。

							*v_!*
{Visual}!{filter}	通过外部程序 {filter} 过滤高亮显示的行
			（关于 {Visual} 请参见 |Visual-mode|）。

:{range}![!]{filter} [!][arg]				*:range!*
			关于执行外部命令，请参见 |:!|

			通过外部程序 {filter} 过滤 {range} 行。
			Vim 将可选的 ! 替换为最近给出的命令，并附加可选的 [arg]。
			Vim 将过滤器命令的输出保存在一个临时文件中，
			然后将该文件读入缓冲区 |tempfile|。
			Vim 使用 'shellredir' 选项将过滤器输出重定向到
			临时文件。但是，如果 'shelltemp' 选项关闭，
			则在可能的情况下使用管道（在 Unix 上）。
			当 'cpoptions' 中包含 'R' 标志时，已过滤行中的标记
			会被删除，除非使用 |:keepmarks| 命令。示例： >
				:keepmarks '<,'>!sort
<			当过滤后的行数少于之前时，丢失行中的标记
			无论如何都会被删除。

							*=*
={motion}		通过 'equalprg' 选项给定的外部程序过滤
			{motion} 行。当 'equalprg'
			选项为空（这是默认值）时，使用内部
			格式化功能 |C-indenting| 和 'lisp'。
			但当 'indentexpr' 不为空时，将使用它
			代替 |indent-expression|。

							*==*
==		像 ={motion} 一样过滤 [count] 行。

							*v_=*
{Visual}=		像 ={motion} 一样过滤高亮显示的行。


						*tempdir* *tempfile* *setuid*
Nvim 使用临时文件进行过滤和生成差异。插件通常也
会使用 |tempname()| 用于自己的目的。在首次请求
临时文件时，Nvim 会创建一个公共目录（"Nvim tempdir"），
作为当前会话中所有临时文件（包括 `stdpath("run")` 文件
|$XDG_RUNTIME_DIR|）的存储位置。

Nvim tempdir 在第一个可用的系统临时目录中创建：
	Unix:    $TMPDIR, /tmp, current-dir, $HOME。
	Windows: $TMPDIR, $TMP, $TEMP, $USERPROFILE, current-dir。

在 Unix 上，tempdir 以权限 0700 创建（仅当前用户可访问）
以避免安全问题（例如符号链接攻击）。退出时，
Nvim 会删除 tempdir 及其内容。
							*E5431*
如果您看到类似以下的错误或 |log| 消息： >
	E5431: tempdir disappeared (2 times)
这意味着您系统上的外部进程删除了 Nvim tempdir。
通常这是由 "防病毒软件" 或配置错误的清理服务引起的。

如果 Nvim 设置了 setuid 位，这可能会导致问题：临时文件
归 setuid 用户所有，但过滤器命令可能以原始用户身份运行。


4.2 替换						*:substitute*
							*:s* *:su*
:[range]s[ubstitute]/{pattern}/{string}/[flags] [count]
			对于 [range] 中的每一行，将 {pattern} 的匹配项
			替换为 {string}。
			关于 {pattern}，请参见 |pattern|。
			{string} 可以是文字字符串，或一些特殊的
			内容；请参见 |sub-replace-special|。
			当省略 [range] 和 [count] 时，仅在当前行替换。
			当指定 [count] 时，在 [count] 行中替换，
			从 [range] 中的最后一行开始。
			当省略 [range] 时，从当前行开始。
							*E939* *E1510*
			[count] 必须是正数（最大 2147483647）。
			另请参见 |cmdline-ranges|。

			关于 [flags]，请参见 |:s_flags|。
			分隔符不必是 /，请参见
			|pattern-delimiter|。

:[range]s[ubstitute] [flags] [count]
:[range]&[&][flags] [count]					*:&*
			重复上次的 :substitute，使用相同的搜索模式和
			替换字符串，但不使用相同的标志。您
			可以添加 [flags]，请参见 |:s_flags|。
			注意，在 `:substitute` 之后，'&' 和 '#' 标志
			不能使用，它们被识别为模式分隔符。
			`:substitute` 和 'c'、'g'、'i'、'I' 及 'r' 标志之间的空格
			不是必需的，但在脚本中最好保留它以避免混淆。
			另请参见下面用于重复 :substitute 的两个字母和三个字母命令
			|:substitute-repeat|。

:[range]~[&][flags] [count]					*:~*
			重复上次替换，使用相同的替换字符串
			但使用上次使用的搜索模式。这类似于
			`:&r`。关于 [flags]，请参见 |:s_flags|。

								*&*
&			`:s` 的同义词（重复上次替换）。注意
			标志不会被记住，因此它的实际工作方式可能不同。
			您可以使用 `:&&` 来保留标志。

								*&-default*
			默认映射为 ":&&<CR>"。 |default-mappings|

								*g&*
g&			`:%s//~/&` 的同义词（在所有行上使用相同标志重复
			上次替换，使用上次搜索模式）。
			例如，当您首先使用 `:s/pattern/repl/flags` 进行替换，
			然后使用 `/search` 搜索其他内容时，`g&` 将执行
			`:%s/search/repl/flags`。
			助记符：全局替换。

						*:snomagic* *:sno*
:[range]sno[magic] ...	与 `:substitute` 相同，但始终使用 'nomagic'。

						*:smagic* *:sm*
:[range]sm[agic] ...	与 `:substitute` 相同，但始终使用 'magic'。

							*:s_flags*
可用于替换命令的标志：

							*:&&*
[&]	必须是第一个：保留上次替换命令的标志。
			示例： >
				:&&
				:s/this/that/&
<			注意，`:s` 和 `:&` 不会保留标志。

[c]	确认每个替换。Vim 高亮显示匹配的字符串（使用
	|hl-IncSearch|）。您可以键入：				*:s_c*
	    'y'	    替换此匹配项
	    'l'	    替换此匹配项然后退出（"last"）
	    'n'	    跳过此匹配项
	    <Esc>   退出替换
	    'a'	    替换此匹配项以及所有剩余的匹配项
	    'q'	    退出替换
	    CTRL-E  向上滚动屏幕
	    CTRL-Y  向下滚动屏幕

							*:s_e*
[e]     当搜索模式失败时，不发出错误消息，并且，特别是在映射中继续执行，就像没有发生错误一样。
	这对于防止 "No match" 错误中断映射最有用。
	但是，Vim 不会抑制以下错误消息：
		正则表达式不能由字母分隔
		\ 后面应跟 /、? 或 &
		没有之前的替换正则表达式
		尾随字符
		被中断

							*:s_g*
[g]	替换行中所有出现的内容。如果没有此参数，
	每行仅替换第一次出现的内容。如果
	'gdefault' 选项开启，则此标志默认开启，[g]
	参数会将其关闭。

							*:s_i*
[i]	忽略模式的大小写。'ignorecase' 和 'smartcase' 选项
	不会被使用。

							*:s_I*
[I]	不忽略模式的大小写。'ignorecase' 和 'smartcase' 选项
	不会被使用。

							*:s_n*
[n]	报告匹配数量，但不实际替换。[c]
	标志被忽略。匹配报告时如同 'report' 为零。
	可用于 |count-items|。
	如果使用 \= |sub-replace-expression|，则表达式将在每个匹配项处
	在 |sandbox| 中求值。

[p]	打印包含最后一次替换的行。  *:s_p*

[#]	类似于 [p] 并在前面加上行号。  *:s_#*

[l]	类似于 [p] 但像 |:list| 一样打印文本。  *:s_l*

							*:s_r*
[r]	仅在与 `:&` 或没有参数的 `:s` 结合使用时有用。`:&r`
	的工作方式与 `:~` 相同：当搜索模式为空时，使用
	先前使用的搜索模式，而不是来自上次替换或 `:global` 的搜索模式。
	如果最后一个执行搜索的命令是替换或 `:global`，则没有效果。
	如果最后一个命令是搜索命令，如 "/"，则使用该命令的模式。
	对于带参数的 `:s`，这已经发生： >
		:s/blue/red/
		/green
		:s//red/   或  :~   或  :&r
<	最后的命令会将 "green" 替换为 "red"。 >
		:s/blue/red/
		/green
		:&
<	最后的命令会将 "blue" 替换为 "red"。

注意，没有标志可以更改模式的 "magicness"。而是使用
不同的命令，或者您可以使用 |/\v| 等。原因是
标志只能通过跳过模式来找到，而为了
跳过模式，必须知道 "magicness"。这是第二十二条军规！

如果替换命令的 {pattern} 为空，则该命令使用
上次替换或 `:global` 命令中的模式。如果没有，但
有之前的搜索模式，则使用该模式。使用 [r] 标志时，
该命令使用上次替换、`:global` 或搜索命令中的模式。

如果省略 {string}，则替换执行时如同它是空的。因此，
匹配的模式将被删除。{pattern} 之后的分隔符也可以省略。
示例： >
	:%s/TESTING
这将从所有行中删除 "TESTING"，但每行仅删除一个。

为了与 Vi 兼容，允许这两个例外：
"\/{string}/" 和 "\?{string}?" 的功能与 "//{string}/r" 相同。
"\&{string}&" 的功能与 "//{string}/" 相同。
						*pattern-delimiter* *E146*
您可以使用其他单字节字符来代替包围模式和替换字符串的 '/'。
如果您想在搜索模式或替换字符串中包含 '/'，这很有用。
示例： >
	:s+/+//+

您可以使用大多数字符，但不能使用字母数字字符、'\'、'"' 或 '|'。

关于模式的定义，请参见 |pattern|。在可视块模式下，在模式中使用
|/\%V| 可使替换仅在块内工作。
否则，它无论如何都会在整个行上工作。

					*sub-replace-special* *:s\=*
当 {string} 以 "\=" 开头时，它会被当作表达式求值，请参见
|sub-replace-expression|。您可以将其用于复杂替换或特殊字符。

替换的递归限制为 4 层。 *E1290*

否则，{string} 中的这些字符具有特殊含义：

magic	nomagic	  动作    ~
  &	  \&	  替换为整个匹配的模式	     *s/\&*
 \&	   &	  替换为 &
      \0	  替换为整个匹配的模式	   *\0* *s/\0*
      \1	  替换为第一对 () 中匹配的模式	     *s/\1*
      \2	  替换为第二对 () 中匹配的模式	     *s/\2*
      ..	  ..						     *s/\3*
      \9	  替换为第九对 () 中匹配的模式	     *s/\9*
  ~	  \~	  替换为上次替换的 {string}	     *s~*
 \~	   ~	  替换为 ~				     *s/\~*
      \u	  使下一个字符变为大写			     *s/\u*
      \U	  使后面的字符变为大写，直到 \E      *s/\U*
      \l	  使下一个字符变为小写			     *s/\l*
      \L	  使后面的字符变为小写，直到 \E      *s/\L*
      \e	  结束 \u、\U、\l 和 \L（注意：不是 <Esc>！）    *s/\e*
      \E	  结束 \u、\U、\l 和 \L		     *s/\E*
      <CR>	  在此处将行分成两行
		  （将 <CR> 输入为 CTRL-V <Enter>）	     *s<CR>*
      \r	  同上						     *s/\r*
      \<CR>	  插入一个回车符（CTRL-M）
		  （将 <CR> 输入为 CTRL-V <Enter>）	     *s/\<CR>*
      \n	  插入一个 <NL>（文件中的 <NUL>）
		  （不会断开行）				     *s/\n*
      \b	  插入一个 <BS>				     *s/\b*
      \t	  插入一个 <Tab>				     *s/\t*
      \\	  插入一个反斜杠			     *s/\\*
      \x	  其中 x 是上面未提及的任何字符：
		  保留用于将来的扩展

特殊含义也用于 |substitute()| 函数的第三个参数 {sub} 中，
但有以下例外：
  - % 原样插入百分比，不考虑 'cpoptions'。
  - magic 始终设置，不考虑 'magic'。
  - ~ 原样插入波浪号。
  - <CR> 和 \r 插入一个回车符（CTRL-M）。
  - \<CR> 没有特殊含义。它只是 \x 中的一个。

示例： >
  :s/a\|b/xxx\0xxx/g		 将 "a b" 修改为 "xxxaxxx xxxbxxx"
  :s/\([abc]\)\([efg]\)/\2\1/g	 将 "af fa bg" 修改为 "fa fa gb"
  :s/abcde/abc^Mde/		 将 "abcde" 修改为 "abc"、"de"（两行）
  :s/$/\^M/			 将 "abcde" 修改为 "abcde^M"
  :s/\w\+/\u\0/g			 将 "bla bla" 修改为 "Bla Bla"
  :s/\w\+/\L\u\0/g		 将 "BLA bla" 修改为 "Bla Bla"

注意："\L\u" 可用于将单词的首字母大写。这与
Vi 和旧版 Vim 不兼容，旧版中 "\u" 会抵消
"\L"。"\U\l" 也是如此。

注意：在以前的版本中，CTRL-V 以特殊方式处理。由于这与
Vi 不兼容，因此已删除此功能。请改用反斜杠。

命令		文本		结果 ~
:s/aa/a^Ma/	aa		a<line-break>a
:s/aa/a\^Ma/	aa		a^Ma
:s/aa/a\\^Ma/	aa		a\<line-break>a

（您需要在此处键入 CTRL-V <CR> 才能得到 ^M）

"\1"、"\2" 等的编号基于模式中 "\(" 出现的顺序（从左到右）。
当一个括号组匹配多次时，最后一个将用于 "\1"、"\2" 等。
示例： >
  :s/\(\(a[a-d] \)*\)/\2/	  将 "aa ab x" 修改为 "ab x"
"\2" 对应于 "\(a[a-d] \)"。它首先匹配 "aa "，然后匹配 "ab "。

当括号与 '|' 结合使用时，如 \([ab]\)\|\([cd]\)，
第一或第二个括号中的模式没有匹配，因此 \1 或 \2 为空。
示例： >
  :s/\([ab]\)\|\([cd]\)/\1x/g   将 "a b c d" 修改为 "ax bx x x"
<

		*:sc* *:sce* *:scg* *:sci* *:scI* *:scl* *:scp* *:sg* *:sgc*
		*:sge* *:sgi* *:sgI* *:sgl* *:sgn* *:sgp* *:sgr* *:sI* *:si*
		*:sic* *:sIc* *:sie* *:sIe* *:sIg* *:sIl* *:sin* *:sIn* *:sIp*
		*:sip* *:sIr* *:sir* *:sr* *:src* *:srg* *:sri* *:srI* *:srl*
		*:srn* *:srp* *:substitute-repeat*
2 字母和 3 字母的 :substitute 命令 ~

这些命令使用给定的标志重复之前的 `:substitute` 命令。
第一个字母总是 "s"，后跟一个或两个可能的标志字符。
例如 ":sce" 的工作方式类似于 ":s ce"。表格列出了
可能的组合，并非所有标志都可能，因为该命令是
另一个命令的简写。

     :substitute 命令列表
     |      c    e    g    i    I    n    p    l    r
     | c  :sc  :sce :scg :sci :scI :scn :scp :scl
     | e
     | g  :sgc :sge :sg  :sgi :sgI :sgn :sgp :sgl :sgr
     | i  :sic :sie      :si  :siI :sin :sip      :sir
     | I  :sIc :sIe :sIg :sIi :sI  :sIn :sIp :sIl :sIr
     | n
     | p
     | l
     | r  :src      :srg :sri :srI :srn :srp :srl :sr

例外：
     :scr  是  `:scriptnames`
     :se   是  `:set`
     :sig  是  `:sign`
     :sil  是  `:silent`
     :sn   是  `:snext`
     :sp   是  `:split`
     :sl   是  `:sleep`
     :sre  是  `:srewind`


使用表达式替换			*sub-replace-expression*
						*sub-replace-\=* *s/\=*
当替换字符串以 "\=" 开头时，剩余部分将被解释为表达式。

|sub-replace-special| 中提到的字符的特殊含义不适用，除了 "<CR>"。
<NL> 字符用作换行符，您可以通过双引号字符串获得一个："\n"。
在反斜杠前添加反斜杠以获得真正的 <NL> 字符（在文件中将是 NUL）。

"\=" 表示法也可以用在 |substitute()| 函数的第三个参数 {sub} 中。
在这种情况下，|sub-replace-special| 中提到的字符的特殊含义完全不适用。
特别是，<CR> 和 <NL> 不被解释为换行符，而是分别解释为回车符和换行符。

当结果是 |List| 时，列表项将用换行符连接起来。
因此，每个项都变成一行，但它们本身可以包含换行符。

|submatch()| 函数可用于获取匹配的文本。整个匹配的文本
可以通过 "submatch(0)" 访问。第一对 () 匹配的文本通过
"submatch(1)" 访问。对于 () 中的后续子匹配，依此类推。

注意：分隔符不得出现在表达式中！
考虑使用像 "@" 或 ":" 这样的字符。如果表达式的结果包含
分隔符，则没有问题。

示例： >
	:s@\n@\="\r" .. expand("$HOME") .. "\r"@
这将行尾替换为一个包含 $HOME 值的新行。 >

	s/E/\="\<Char-0x20ac>"/g
这将每个 'E' 字符替换为欧元符号。在 |<Char->| 中阅读更多信息。


4.3 更改制表符					*change-tabs*
							*:ret* *:retab* *:retab!*
:[range]ret[ab][!] [-indentonly] [{new-tabstop}]
			将所有包含 <Tab> 的空格序列替换为使用
			{new-tabstop} 的新空格字符串。如果未指定
			{new-tabstop} 或其为零，Vim 使用 'tabstop' 的当前值。
			始终使用 'tabstop' 的当前值来计算现有制表符的宽度。
			使用 ! 时，Vim 还会在适当的情况下将仅包含普通空格的字符串
			替换为制表符。
			当 'expandtab' 开启时，Vim 将所有制表符替换为适当数量的空格。
			此命令将 'tabstop' 设置为 {new-tabstop}，并且如果
			在整个文件上执行（这是默认设置），则不应产生任何可见更改。

			当指定 [-indentonly] 时，仅处理前导空白。
			任何其他连续空白将不会被更改。

			警告：此命令会修改 C 程序字符串中的任何 <Tab> 字符。
			请使用 "\t" 来避免这种情况（这无论如何都是个好习惯）。
			`:retab!` 也可能将空格序列更改为
			<Tab> 字符，这可能会破坏 printf()。
			可以使用逗号分隔的制表符宽度列表来代替单个制表符宽度。
			列表中的每个值代表一个制表符的宽度，除了最后一个
			值适用于所有后续制表符。

							*retab-example*
使用自动命令和 ":retab" 编辑文件的示例，该文件以
制表符宽度 8 存储，但以制表符宽度 4 编辑。警告：字符串内的
空格可能会更改！另请参见 'softtabstop' 选项。 >

  :auto BufReadPost	*.xx	retab! 4
  :auto BufWritePre	*.xx	retab! 8
  :auto BufWritePost	*.xx	retab! 4
  :auto BufNewFile	*.xx	set ts=4

==============================================================================
5. 复制和移动文本				*copy-move*

							*quote*
"{register}		为下一次删除、复制或放置操作使用 {register}。
			使用大写字母可追加到删除和复制的内容中。
			寄存器 "."、"%"、"#" 和 ":" 仅适用于放置操作。

							*:reg* *:registers*
:reg[isters]		显示所有编号寄存器和命名寄存器的类型和内容。
			如果寄存器被写入用于 |:redir|，则不会被列出。
			类型可以是以下之一：
			"c"	表示 |characterwise| 文本
			"l"	表示 |linewise| 文本
			"b"	表示 |blockwise-visual| 文本


:reg[isters] {arg}	显示 {arg} 中提到的编号寄存器和命名寄存器的
			内容。例如： >
				:reg 1a
<			显示寄存器 '1' 和 'a'。{arg} 中允许有空格。

							*:di* *:dis* *:display*
:di[splay] [arg]	与 :registers 相同。

							*y* *yank*
["x]y{motion}		复制 {motion} 文本 [存入寄存器 x]。当没有
			要复制的字符时（例如，在第 1 列执行 "y0"），
			如果 'cpoptions' 包含 'E' 标志，则这是一个错误。

							*yy*
["x]yy		复制 [count] 行 [存入寄存器 x] |行方式|。

							*Y*
["x]Y		复制 [count] 行 [存入寄存器 x]（yy 的同义词，
			|行方式|）。
							*Y-default*
			默认映射为 "y$"。 |default-mappings|

							*zy*
["x]zy{motion}		复制 {motion} 文本 [存入寄存器 x]。仅在选择
			文本块时与 `y` 不同，请参见 |v_zy|。

							*v_y*
{Visual}["x]y		复制高亮显示的文本 [存入寄存器 x]（关于
			{Visual} 请参见 |Visual-mode|）。

							*v_Y*
{Visual}["x]Y		复制高亮显示的行 [存入寄存器 x]（关于
			{Visual} 请参见 |Visual-mode|）。

							*v_zy*
{Visual}["x]zy		复制高亮显示的文本 [存入寄存器 x]。选定块中每行末尾的
			尾随空格不会被复制。与 `zp` 结合使用时
			特别有用。（关于 {Visual} 请参见 |Visual-mode|）

							*:y* *:yank* *E850*
:[range]y[ank] [x]	复制 [range] 行 [存入寄存器 x]。

:[range]y[ank] [x] {count}
			复制 {count} 行，从 [range] 中的最后行号开始
			（默认：当前行 |cmdline-ranges|），
			[存入寄存器 x]。

						*p* *put* *E353* *E1240*
["x]p		将文本 [从寄存器 x] 放在光标之后
			[count] 次。

							*P*
["x]P		将文本 [从寄存器 x] 放在光标之前
			[count] 次。

							*<MiddleMouse>*
["x]<MiddleMouse>	将寄存器中的文本放在光标之前 [count]
			次。除非指定了其他寄存器，否则使用 "* 寄存器。
			将光标留在新文本的末尾。
			仅当 'mouse' 包含 'n' 或 'a' 时，
			鼠标操作才有效。
			如果您有滚轮并且经常不小心粘贴文本，
			您可以使用以下映射来禁用
			使用鼠标中键粘贴： >
				:map <MiddleMouse> <Nop>
				:imap <MiddleMouse> <Nop>
<			您可能还想禁用多点单击版本，
			请参见 |double-click|。

							*gp*
["x]gp		类似于 "p"，但将光标留在新文本之后。

							*gP*
["x]gP		类似于 "P"，但将光标留在新文本之后。

							*:pu* *:put*
:[line]pu[t] [x]	将文本 [从寄存器 x] 放在 [line] 之后（默认
			当前行）。此操作始终以 |行方式| 工作，因此
			此命令可用于将复制的块作为新行放置。
			如果未指定寄存器，则取决于
			'clipboard' 选项：如果 'clipboard' 包含
			"unnamedplus"，则从 + 寄存器粘贴 |quoteplus|。
			否则，如果 'clipboard' 包含 "unnamed"，则从
			* 寄存器粘贴 |quotestar|。否则，从
			未命名寄存器粘贴 |quote_quote|。
			寄存器也可以是 '=' 后跟可选的表达式。
			表达式会一直持续到命令结束。您需要转义 '|' 和 '"'
			字符以防止它们终止命令。示例： >
				:put ='path' .. \",/test\"
<			如果 '=' 后面没有表达式，Vim 使用
			之前的表达式。您可以使用 ":dis =" 查看它。

:[line]pu[t]! [x]	将文本 [从寄存器 x] 放在 [line] 之前（默认
			当前行）。

							*:ip* *:iput*
:[line]ip[ut] [x]	类似于 |:put|，但将缩进调整为当前行。

:[line]ip[ut]! [x]	类似于 |:put|!，但将缩进调整为当前行。

["x]]p		   或					*]p* *]<MiddleMouse>*
["x]]<MiddleMouse>	类似于 "p"，但将缩进调整为当前行。
			仅当 'mouse' 包含 'n' 或 'a' 时，
			鼠标操作才有效。

["x][P		   或					*[P*
["x]]P		   或					*]P*
["x][p		   或					*[p* *[<MiddleMouse>*
["x][<MiddleMouse>	类似于 "P"，但将缩进调整为当前行。
			仅当 'mouse' 包含 'n' 或 'a' 时，
			鼠标操作才有效。

["x]zp		   或					*zp* *zP*
["x]zP		类似于 "p" 和 "P"，但在粘贴块时不添加尾随空格。
			因此，插入的文本不总是矩形。与 |v_zy|
			结合使用时特别有用。

您可以使用这些命令将文本从一个地方复制到另一个地方。方法是
首先通过复制、删除或更改命令将文本放入寄存器中，
然后使用放置命令插入寄存器内容。您也可以
使用这些命令将文本从一个文件移动到另一个文件，因为 Vim
在切换缓冲区时会保留所有寄存器（CTRL-^ 命令是一种在
两个文件之间快速切换的方法）。

				*linewise-register* *charwise-register*
您可以使用 "." 重复放置命令（除了 :put）并撤消它们。如果
用于将文本放入寄存器的命令是 |行方式| 的，
Vim 会将文本插入光标所在行的下方（"p"）或上方（"P"）。
否则，Vim 会将文本插入光标之后（"p"）或之前（"P"）。
使用 ":put" 命令时，Vim 始终将文本插入到下一行。
您可以使用命令序列 "xp" 交换两个字符。
您可以使用命令序列 "ddp" 交换两行。
您可以使用命令序列 "deep" 交换两个单词（从第一个单词前的空白处开始光标）。
您可以在放置命令后使用 |']| 或 |`]| 命令将光标移动到
插入文本的末尾，或使用 |'[| 或 |`[| 将光标移动到开头。

							     *put-Visual-mode*
在可视模式下使用像 |p| 或 |P| 这样的放置命令时，Vim 会尝试用
寄存器的内容替换选定的文本。其工作方式取决于选择类型和文本。
对于块状选择，它还取决于块的大小以及角点是否位于
现有字符上。（实现细节：它实际上是通过先将寄存器放在
选择之后，然后删除选择来工作的。）
									 *v_p*
可视模式下的 |p| 会放置文本，并将默认寄存器（未命名、
选择或剪贴板）设置为先前选择的文本。如果您想
将该文本放在其他地方，这很有用。但您不能重复相同的更改。
									 *v_P*
可视模式下的 |P| 会放置文本，但不会设置默认寄存器。
您可以重复更改，但删除的文本不能使用。如果您确实需要它，
可以使用带有其他寄存器的 |p|。例如，复制要复制的文本，使用可视方式
选择要替换的文本，然后使用 "0p。您可以随意重复此操作，
但未命名寄存器每次都会被更改。
							*blockwise-put*
当寄存器包含来自一行的文本（字符方式）时，使用块状
可视选择，放置该寄存器会将文本重复粘贴到每个选定的
行中，从而将块状选择的区域替换为寄存器文本的多个副本。
例如：
	- 使用 `yw` 将单词 "TEXT" 复制到寄存器中
	- 选择一个可视块，在此文本中用 "v" 标记：
	    aaavvaaa
	    bbbvvbbb
	    cccvvccc
	- 按 `p`，结果为：
	    aaaTEXTaaa
	    bbbTEXTbbb
	    cccTEXTccc

							*blockwise-register*
如果您使用块状可视模式命令将文本放入寄存器，
则文本块将被插入当前行和后续行的光标列之前（"P"）或之后（"p"）。
Vim 使整个文本块从同一列开始。
因此，插入的文本看起来与复制或删除时相同。
Vim 可能会将一些 <Tab> 字符替换为空格以实现此目的。
但是，如果块的宽度不是 <Tab> 宽度的倍数，并且
插入块之后的文本包含 <Tab>，则该文本可能会错位。

使用 |zP|/|zp| 粘贴块状复制的寄存器，而不追加尾随
空格。

注意，在字符方式复制命令之后，Vim 将光标留在
最靠近缓冲区开头的第一个复制字符上。这意味着
"yl" 不移动光标，但 "yh" 将光标向左移动一个字符。
理由：在 Vi 中，"y" 命令后跟向后移动有时不会
将光标移动到第一个复制的字符，因为重绘被跳过了。
在 Vim 中，它总是移动到第一个字符，正如 Posix 所指定的。
对于行方式复制命令，光标放在第一行，
但列保持不变，因此它可能不在第一个复制的字符上。

=============================================================================
寄存器				       *registers* *{register}* *E354*

有十种类型的寄存器：
1. 未命名寄存器 ""
2. 10 个编号寄存器 "0 到 "9
3. 小删除寄存器 "-
4. 26 个命名寄存器 "a 到 "z 或 "A 到 "Z
5. 三个只读寄存器 ":, "., "%
6. 交替缓冲区寄存器 "#
7. 表达式寄存器 "=
8. 选择寄存器 "* 和 "+
9. 黑洞寄存器 "_
10. 上次搜索模式寄存器 "/

-----------------------------------------------------------------------------
1. 未命名寄存器 ""				    *quote_quote* *quotequote*

Vim 使用 "d"、"c"、"s"、"x" 命令删除的文本或使用复制 "y" 命令
复制的文本填充此寄存器，无论是否使用了特定寄存器
（例如 "xdd"）。这就像未命名寄存器指向最后使用的寄存器。
因此，当使用大写寄存器名称追加时，未命名寄存器包含与
命名寄存器相同的文本。'_' 寄存器是例外："_dd 不会将删除的文本
存储在任何寄存器中。
Vim 将未命名寄存器的内容用于任何未指定寄存器的放置命令
（p 或 P）。此外，您可以使用名称 '"' 访问它。这意味着您必须键入
两个双引号。写入 "" 寄存器会写入寄存器 "0。

-----------------------------------------------------------------------------
2. 编号寄存器 "0 到 "9					*quote_number*
			 *quote0* *quote1* *quote2* *quote3* *quote4* *quote9*

Vim 使用复制和删除命令中的文本填充这些寄存器。
编号寄存器 0 包含最近复制命令的文本，
除非该命令使用 ["x] 指定了另一个寄存器。

编号寄存器 1 包含最近删除或更改命令删除的文本
（即使该命令指定了另一个寄存器），除非文本少于一行
（此时会使用小删除寄存器）。对于带有这些移动命令的删除操作符，
有一个例外：|%|、|(|、|)|、|`|、|/|、|?|、|n|、|N|、|{| 和 |}|。

此时始终使用寄存器 "1（这与 Vi 兼容）。如果删除操作
在一行内，也会使用 "- 寄存器。注意这些字符可能被映射。
例如，matchit 插件映射了 |%|。

随着每次连续的删除或更改，Vim 将寄存器 1 的先前内容
移到寄存器 2，2 移到 3，依此类推，丢失寄存器 9 的先前内容。
							*yankring*
要将复制（不仅仅是删除）也存储在寄存器 1-9 中，请尝试以下操作： >lua
    -- Yank-ring: store yanked text in registers 1-9.
    vim.api.nvim_create_autocmd('TextYankPost', {
      callback = function()
        if vim.v.event.operator == 'y' then
          for i = 9, 1, -1 do -- Shift all numbered registers.
            vim.fn.setreg(tostring(i), vim.fn.getreg(tostring(i - 1)))
          end
        end
      end,
    })
<
-----------------------------------------------------------------------------
3. 小删除寄存器 "-                                 *quote_-* *quote-*

此寄存器包含来自删除少于一行文本的命令的文本，
除非该命令使用 ["x] 指定了寄存器。

-----------------------------------------------------------------------------
4. 命名寄存器 "a 到 "z 或 "A 到 "Z                 *quote_alpha* *quotea*

Vim 仅在您指定时填充这些寄存器。使用小写字母指定
以替换其先前内容，或使用大写字母指定以追加
到其先前内容。当 'cpoptions' 中存在 '>' 标志时，
会在追加的文本之前插入一个换行符。

-----------------------------------------------------------------------------
5. 只读寄存器 ":, ". 和 "%

这些是 '%'、':' 和 '.'。您只能将它们与 "p"、"P"、
和 ":put" 命令以及 CTRL-R 一起使用。

						*quote_.* *quote.* *E29*
   - "."	包含最后插入的文本（与插入模式命令 CTRL-A 和 CTRL-@
	插入的文本相同）。注意：这不适用于命令行上的 CTRL-R。
	它的工作方式略有不同，就像插入文本而不是放置它（'textwidth' 和
	其他选项会影响插入的内容）。
							*quote_%* *quote%*
   - "%	包含当前文件的名称。
						*quote_:* *quote:* *E30*
   - ":"	包含最近执行的命令行。示例：使用
	"@:" 重复之前的命令行命令。只有在至少键入了
	一个字符时，命令行才会存储在此寄存器中。
	因此，如果命令完全从映射执行，则它保持不变。

-----------------------------------------------------------------------------
6. 交替文件寄存器 "#                          *quote_#* *quote#* *@#*

包含当前窗口的 |alternate-file| 名称。
此寄存器可使用 `:let` 和 |setreg()| 写入。这会更改
CTRL-^ 进入的缓冲区。您不能将内容复制或删除到此寄存器中。
字符串会与现有缓冲区名称匹配，类似于 |:buffer|： >
    let @# = 'buffer_name'
也支持使用缓冲区编号和 |file-pattern|。

抛出
  |E86| 当缓冲区编号不存在时
  |E93| 当多个缓冲区匹配时
  |E94| 当没有匹配项时

使用空字符串清除寄存器： >
    let @# = ''

-----------------------------------------------------------------------------
7. 表达式寄存器 "=                              *quote_=* *quote=* *@=*

这不是真正存储文本的寄存器，而是在使用寄存器的命令中
使用表达式的一种方式。表达式寄存器是可读写的。

- 在 " 或 CTRL-R 后键入 '=' 时，光标移至命令行，
  您可以在其中输入任何表达式（参见 |expression|）。所有正常的
  命令行编辑命令都可用，包括表达式的特殊历史记录。
  当您通过键入 <CR> 结束命令行时，Vim 会计算
  表达式的结果。如果您用 <Esc> 结束它，Vim 会放弃该
  表达式。如果您没有输入表达式，Vim 会使用先前的
  表达式（就像 "/" 命令一样）。
- 表达式必须计算为字符串。数字总是自动
  转换为字符串。对于 "p" 和 ":put" 命令，如果结果是
  Float，则将其转换为字符串。如果结果是 List，则每个元素
  都将转换为字符串并用作一行。Dictionary 将转换为
  字符串。Funcref 会导致错误消息（使用 string() 进行转换）。
- 如果 "= 寄存器用于 "p" 命令，则字符串会在 <NL> 字符处
  分割。如果字符串以 <NL> 结尾，则它被视为行方式
  寄存器。

-----------------------------------------------------------------------------
8. 选择寄存器 "* 和 "+

使用这些寄存器来存储和检索 GUI 的选定文本。
请参见 |quotestar| 和 |quoteplus|。当剪贴板不可用或
无法工作时，将使用未命名寄存器代替。对于 Unix 系统和 Mac OS X，
请参见 |primary-selection|。

-----------------------------------------------------------------------------
9. 黑洞寄存器 "_					      *quote_*

写入此寄存器时，什么也不会发生。这可用于删除文本
而不影响普通寄存器。从此寄存器读取时，
不返回任何内容。

-----------------------------------------------------------------------------
10. 上次搜索模式寄存器 "/			    *quote_/* *quote/*

包含最近的搜索模式。这用于 "n" 和 'hlsearch'。
此寄存器可使用 `:let` 和 |setreg()| 写入。您可以更改它以使
'hlsearch' 高亮显示其他匹配项，而无需实际搜索。您不能将
内容复制或删除到此寄存器中。搜索方向可在
|v:searchforward| 中获得。
注意，从函数返回时，该值会被恢复
|function-search-undo|。

							*@/*
您可以使用 `:let` 命令写入寄存器 |:let-@|。示例： >
	:let @/ = "the"

如果您使用放置命令而未指定寄存器，Vim 使用
最后填充的寄存器（这也是未命名寄存器的内容）。如果
您感到困惑，请使用 `:dis` 命令查看 Vim 将要放置的内容（此
命令显示所有命名和编号寄存器；未命名寄存器标记
为 '"'）。

接下来的三个命令始终处理整行。

:[range]co[py] {address}				*:co* *:copy*
			复制 [range] 给定的行到 {address} 给定的行下方。

							*:t*
:t			copy 的同义词。

:[range]m[ove] {address}			*:m* *:mo* *:move* *E134*
			移动 [range] 给定的行到 {address} 给定的行下方。

==============================================================================
6. 格式化文本					*formatting*

:[range]ce[nter] [width]				*:ce* *:center*
			将 [range] 中的行在 [width] 列之间居中
			（默认 'textwidth'，如果 'textwidth' 为 0 则为 80）。

:[range]ri[ght] [width]					*:ri* *:right*
			将 [range] 中的行在 [width] 列右对齐
			（默认 'textwidth'，如果 'textwidth' 为 0 则为 80）。

							*:le* *:left*
:[range]le[ft] [indent]
			将 [range] 中的行左对齐。将行中的缩进设置为
			[indent]（默认为 0）。

							*gq*
gq{motion}		格式化 {motion} 所覆盖的行。
			格式化通过三种方法之一完成：
			1. 如果 'formatexpr' 不为空，则计算该表达式。
			   每个缓冲区可能不同。
			2. 如果 'formatprg' 不为空，则使用外部程序。
			3. 否则，内部进行格式化。

			在第三种情况下，'textwidth' 选项控制
			每个格式化行的长度（参见下文）。
			如果 'textwidth' 选项为 0，则格式化行的
			长度为屏幕宽度（最大宽度为 79）。
			'formatoptions' 选项控制格式化的类型
			|fo-table|。
			光标停留在最后一个格式化行的第一个非空白字符上。

gqgq							*gqgq* *gqq*
gqq		格式化当前行。使用计数可格式化那么
			多行。

							*v_gq*
{Visual}gq		格式化高亮显示的文本。（关于 {Visual} 请参见
			|Visual-mode|）。

							*gw*
gw{motion}		格式化 {motion} 所覆盖的行。类似于
			|gq|，但将光标放回文本中的相同位置。
			但是，'formatprg' 和 'formatexpr' 不会被使用。

gwgw							*gwgw* *gww*
gww		像 "gw" 一样格式化当前行。

							*v_gw*
{Visual}gw		像 "gw" 一样格式化高亮显示的文本。（关于
			{Visual} 请参见 |Visual-mode|）。

示例：要格式化当前段落，请使用：			*gqap*  >
	gqap

"gq" 命令将光标留在移动命令所到达的行中。
这允许您使用 "." 重复格式化。这与 "gqj"（格式化当前行和下一行）
和 "gq}"（格式化直到段落末尾）配合得很好。
注意：当设置了 'formatprg' 时，"gq" 将光标留在
第一个格式化行上（就像使用过滤器命令一样）。

如果您想格式化当前段落并继续您原来的位置，请使用： >
	gwap
如果您总是希望保持段落格式，您可能希望将 'a'
标志添加到 'formatoptions'。请参见 |auto-format|。

如果 'autoindent' 选项开启，Vim 对后续行使用
第一行的缩进。

格式化不会更改空行（但会更改仅包含空白的行！）。

当行连接在一起时，会使用 'joinspaces' 选项。

您可以将 'formatexpr' 选项设置为表达式，或将 'formatprg' 选项
设置为外部程序的名称，以供 Vim 用于文本格式化。对于外部程序
格式化，'textwidth' 和其他选项没有影响。

                                                        *format-formatexpr*
'formatexpr' 选项可以设置为执行缓冲区重新格式化的 Vim 脚本函数。
这通常应该在 |ftplugin| 中完成，因为格式化高度依赖于
文件类型。使用 |autoload| 脚本是有意义的，这样相应的脚本
仅在实际需要时加载，并且脚本应命名为 <filetype>format.vim。

例如，Vim 在 $VIMRUNTIME/ftplugin 目录中分发的 XML 文件类型插件，
将 'formatexpr' 选项设置为： >

   setlocal formatexpr=xmlformat#Format()

这意味着，您将在文件 `$VIMRUNTIME/autoload/xmlformat.vim` 中找到
定义 xmlformat#Format() 函数的相应脚本。

以下是一个从选定文本中删除尾随空格的示例。将其放在您的
|ftplugin| 中：
>lua
  vim.bo.formatexpr = function()
    -- only reformat on explicit gq command
    if vim.fn.mode() ~= 'n' then
      -- fall back to the internal reformatting
      return 1
    end
    local lines = vim.fn.getline(vim.v.lnum, vim.v.lnum + vim.v.count - 1)
    for i, val in ipairs(lines) do
      lines[i] = (val:gsub('%s+$', ''))
    end
    vim.fn.setline('.', lines)

    -- do not run internal formatter!
    return 0
  end

注意：此函数在从插入模式调用时（基本上意味着，文本插入超过了
'textwidth' 限制）显式返回非零值。这会导致
Vim 回退到使用内部格式化程序重新格式化文本。

但是，如果使用 |gq| 命令重新格式化文本，则函数
将接收选定的行，从这些行中修剪尾随空格，并将它们放回
原处。如果您要将单行拆分为多行，
请注意不要覆盖任何内容。

如果您允许从插入或替换模式重新格式化文本，则必须非常
小心，因为该函数可能被递归调用。对于调试，设置
'debug' 选项会有所帮助。

							*right-justify*
Vim 中没有右对齐文本的命令。您可以使用
外部命令来完成，例如 "par"（例如：`:.,}!par` 格式化直到段落末尾）
或将 'formatprg' 设置为 "par"。

							*format-comments*
注释格式的概述在用户手册的 |30.6| 节中。

Vim 可以以特殊方式自动插入和格式化注释。Vim 通过
行首（忽略空白）的特定字符串来识别注释。可以使用三种类型的注释：

- 在每行开头重复的注释字符串。一个例子是
  shell 脚本中使用的以 "#" 开头的注释类型。
- 仅出现在第一行，而不出现在后续行中的注释字符串。
  一个例子是带破折号的列表。
- 具有开始字符串、结束字符串和可选中间行的三段式注释。
  开始、中间和结束的字符串不同。一个例子是
  C 风格的注释： >
	/*
	 * this is a C comment
	 */

'comments' 选项是以逗号分隔的部分列表。每个部分定义一种
注释字符串类型。一个部分由以下组成：
	{flags}:{string}

{string} 是必须出现的文字文本。

{flags}：
  n	嵌套注释。允许使用混合部分进行嵌套。如果 'comments'
	是 "n:),n:>"，则以 "> ) >" 开头的行是注释。

  b	在 {string} 之后需要空白（<Space>、<Tab> 或 <EOL>）。

  f	只有第一行具有注释字符串。不在下一行重复注释，
	但保留缩进（例如，项目符号列表）。

  s	三段式注释的开始

  m	三段式注释的中间部分

  e	三段式注释的结束

  l	左对齐。与 's' 或 'e' 一起使用时，开始或结束的最左边
	字符将与中间部分的最左边字符对齐。
	这是默认设置，可以省略。更多细节请参见下文。

  r	右对齐。与上述相同，但使用最右边而不是最左边。
	更多细节请参见下文。

  O	不要将此注释视为 "O" 命令的注释。

  x	允许通过在新行上键入结束注释字符串的最后一个字符作为
	第一个操作来结束三段式注释，当中间注释字符串已
	自动插入时。更多细节请参见下文。

  {digits}
	与 's' 或 'e' 一起使用时：为自动插入的中间或结束注释引导符
	增加 {digit} 量的偏移。偏移从左对齐开始。
	更多细节请参见下文。

  -{digits}
	类似于 {digits}，但减少缩进。这仅在开始或结束部分
	存在一些可以移除的缩进时有效。

当字符串没有 'f'、's'、'm' 或 'e' 标志时，Vim 假定
注释字符串在每行开头重复。{flags} 字段可以为空。

{string} 之前和之后的任何空白都是 {string} 的一部分，
因此除非空白是注释字符串的必需部分，否则不要包含
前导或尾随空白。

当一个注释引导符是另一个的一部分时，在整体之后指定该部分。
例如，要同时包含 "-" 和 "->"，请使用 >
	:set comments=f:->,f:-

三段式注释必须始终按开始、中间、结束的顺序给出，
中间不能有其他部分。三段式注释的示例是 >
	sr:/*,mb:*,ex:*/
用于 C 注释。为避免将 "*ptr" 识别为注释，中间字符串
包含 'b' 标志。对于三段式注释，Vim 会检查开始和
中间字符串之后是否有结束字符串。如果 Vim 找到结束字符串，
则注释不会在下一行继续。三段式注释必须
有中间字符串，否则 Vim 无法识别中间行。

注意在上述三段式注释定义中使用了 "x" 标志。
当您在 C 注释中按回车时，Vim 会为新行插入
中间注释引导符：" * "。要结束此注释，您只需在新行上
键入 "/" 而不先键入任何其他内容。这将用
结束注释引导符替换中间注释引导符，并应用任何指定的
对齐，只留下 `" */"`。无需先按退格键。

当与中间部分匹配，但同时存在更长的匹配结束
部分时，将使用结束部分。这使得 C 风格注释能够工作，
而不要求中间部分以空格结尾。

以下是一个对齐标志工作的示例，以使注释突出
（看起来有点像 1）。考虑注释字符串： >vim
	:set comments=sr:/***,m:**,ex-2:******/
>
                                   /***
                                     **<--从 "r" 标志右对齐
                                     **
 offset 2 spaces for the "-2" flag-->**
                                   ******/
在这种情况下，首先键入了注释，然后按了 4 次回车，
然后键入了 "/" 以结束注释。

以下是三段式注释的一些细节。在三个时刻会考虑
对齐和偏移标志：在开始注释后打开新行，
在结束注释前打开新行，以及自动结束三段式注释。
结束对齐标志具有反向视角；结果是，与 "s" 和 "e"
一起使用的相同对齐标志将导致开始部分和结束部分
具有相同的缩进。每个注释部分只打算使用一个对齐，
但偏移数字会覆盖 "r" 和 "l" 标志。

在许多情况下，启用 'cindent' 将覆盖对齐标志。
使用像 |gq| 或 |=| 这样的不同方法重新缩进也不会参考
对齐标志。可以在那些其他格式化选项中定义相同的行为。
一个考虑因素是 'cindent' 具有用于基于上下文的注释缩进的
附加选项，但无法复制许多三段式缩进对齐。
然而，'indentexpr' 能够更好地处理三段式注释。

其他示例： >
   "b:*"	包括以 "*" 开头的行，但如果 "*" 后面跟有非空白，
		则不包括。这可以避免将像 "*str" 这样的指针解引用
		识别为注释。
   "n:>"	包括以 ">"、" >>"、">>>" 等开头的行。
   "fb:-"	格式化以 "- " 开头的列表。

默认情况下，包含 "b:#"。这意味着以 "#include" 开头的行
不被识别为注释行。但以 "# define" 开头的行
被识别。这是一种折衷方案。

							*fo-table*
您可以使用 'formatoptions' 选项来影响 Vim 格式化文本的方式。
'formatoptions' 是一个字符串，可以包含以下任何字母。您可以
用逗号分隔选项字母以便阅读。

字母	 当出现在 'formatoptions' 中时的含义    ~
							*fo-t*
t	使用 'textwidth' 自动换行文本
							*fo-c*
c	使用 'textwidth' 自动换行注释，自动插入当前注释
	引导符。
							*fo-r*
r	在插入模式下按 <Enter> 后自动插入当前注释引导符。
							*fo-o*
o	在普通模式下按 'o' 或 'O' 后自动插入当前注释引导符。
	如果在特定位置不需要注释，请使用 CTRL-U 快速删除它。|i_CTRL-U|
							*fo-/*
/	当包含 'o' 时：不要为语句后的 // 注释插入注释引导符，
	仅当 // 在行首时。
							*fo-q*
q	允许使用 "gq" 格式化注释。
	注意，格式化不会更改空行或仅包含注释引导符的行。
	新段落在此类行之后或注释引导符更改时开始。
							*fo-w*
w	尾随空格表示下一行继续同一个段落。
	以非空白字符结尾的行结束一个段落。
							*fo-a*
a	段落的自动格式化。每次插入或删除文本时，段落都会被重新格式化。
	请参见 |auto-format|。
	当存在 'c' 标志时，这仅对识别的注释发生。
							*fo-n*
n	格式化文本时，识别编号列表。这实际上使用了
	'formatlistpat' 选项，因此可以使用任何类型的列表。数字后的
	文本缩进用于下一行。默认是查找一个数字，
	可选后跟 '.'、':'、')'、']' 或 '}'。注意 'autoindent'
	也必须设置。与 "2" 配合不佳。
	示例： >
		1. the first item
		   wraps
		2. the second item
<							*fo-2*
2	格式化文本时，使用段落第二行的缩进
	作为段落其余部分的缩进，而不是第一行的缩进。
	这支持第一行缩进与其余部分不同的段落。
	注意 'autoindent' 也必须设置。示例： >
			first line of a paragraph
		second line of the same paragraph
		third line.
<	这在注释内部也有效，忽略注释引导符。
							*fo-v*
v	插入模式下的 Vi 兼容自动换行：仅在当前插入命令期间
	在您输入的空格处断行。（注意：这不是 100% Vi 兼容。
	Vi 在这方面有一些 "意外功能" 或错误。它使用
	屏幕列而不是行列。）
							*fo-b*
b	类似于 'v'，但仅当您在换行边距处或之前输入空格时才自动换行。
	如果当您开始插入时，行已经比 'textwidth' 长，
	或者您在达到 'textwidth' 之前没有在插入中输入空格，
	Vim 不会执行自动换行。
							*fo-l*
l	插入模式下不断开长行：当插入命令开始时行已经比
	'textwidth' 长时，Vim 不会自动格式化它。
							*fo-m*
m	也在大于 255 的多字节字符处断行。这对于
	亚洲文本很有用，因为每个字符本身就是一个单词。注意
	也可能会在标点符号（如冒号）后添加换行符，以匹配
	CJK 换行规则。
							*fo-M*
M	连接行时，不要在多字节字符前后插入空格。
	覆盖 'B' 标志。
							*fo-B*
B	连接行时，不要在两个多字节字符之间插入空格。
	被 'M' 标志覆盖。
							*fo-1*
1	不要在一个字母的单词后断行。而是在它之前断行
	（如果可能）。
							*fo-]*
]	严格尊重 'textwidth'。设置此标志后，任何行都不能超过
	'textwidth'，除非换行禁止规则使其不可能。
	主要用于 CJK 脚本，并且仅在 'encoding' 为 "utf-8" 时有效。
							*fo-j*
j	在合理的情况下，连接行时删除注释引导符。例如，连接：
		int i;   // the index ~
			 // in the list ~
	变为：
		int i;   // the index in the list ~
							*fo-p*
p	不要在句点后的单个空格处断行。这是为了补充
	'joinspaces' 和 |cpo-J|，适用于句子由两个空格分隔的
	散文。例如，'textwidth' 设置为 28： >
		Surely you're joking, Mr. Feynman!
<	变为： >
		Surely you're joking,
		Mr. Feynman!
<	而不是： >
		Surely you're joking, Mr.
		Feynman!


使用 't' 和 'c'，您可以指定 Vim 何时执行自动换行：
值	动作	~
""	无自动格式化（您可以使用 "gq" 进行手动格式化）
"t"	文本的自动格式化，但不是注释
"c"	注释的自动格式化，但不是文本（适用于 C 代码）
"tc"	文本和注释的自动格式化

注意，当 'textwidth' 为 0 时，Vim 无论如何都不会执行自动格式化（但
会根据 'comments' 选项插入注释引导符）。当存在 'a' 标志时
是一个例外。|auto-format|

注意，即使 Vim 从未执行自动换行，'textwidth' 也可以非零；
'textwidth' 对于使用 "gq" 格式化仍然很有用。

如果 'comments' 选项包含 "/*"、"*" 和/或 "*/"，则 Vim 有一些
内置功能可以更巧妙地处理这些类型的注释。
在 "/*" 或 "*/" 之前或之后打开新行（当 'r' 或 'o' 存在于
'formatoptions' 中）会自动给出正确的行首。格式化和
自动换行时也是如此。在以 "/*" 或 "*" 开头并包含 "*/" 的行之后
打开一行，将不会插入注释引导符，并且新行的缩进
取自包含注释开始的行。
例如： >
    /*
     * Your typical comment.
     */
    The indent on this line is the same as the start of the above
    comment.

所有这些都应该非常酷，尤其是与新的 :autocmd 命令结合使用，
为不同类型的文件准备不同的设置。

一些示例：
  对于 C 代码（仅格式化注释）： >
	:set fo=croq
< 对于邮件/新闻（格式化所有，不用 "o" 命令开始注释）： >
	:set fo=tcrq
<

自动格式化				*auto-format* *autoformat*

当 'formatoptions' 中存在 'a' 标志时，在插入文本或删除文本时
会自动格式化文本。这对于编辑文本段落效果很好。关于如何使用
的一些提示：

- 您需要正确定义段落。最简单的是用空行分隔的段落。
  如果没有分隔的空行，请考虑使用 'w' 标志，并在段落中
  除最后一行外的每行末尾添加一个空格。

- 您可以根据文件类型 |filetype| 设置 'formatoptions'，或者
  使用 |modeline| 专门为某个文件设置。

- 将 'formatoptions' 设置为 "aw2tq" 以处理像这样的缩进文本：

	    bla bla foobar bla 
	bla foobar bla foobar bla
	    bla bla foobar bla 
	bla foobar bla bla foobar

- 添加 'c' 标志以仅自动格式化注释。在源代码中很有用。

- 将 'textwidth' 设置为所需宽度。如果为零，则使用 79，
  如果屏幕宽度更小，则使用屏幕宽度。

以及一些警告：

- 当文本的一部分没有正确分隔为段落时，在此文本中进行更改
  仍会导致其被格式化。考虑执行 >

	:set fo-=a

- 当使用 'w' 标志（尾随空格表示段落继续）并使用 |dd|
  删除段落的最后一行时，该段落将与下一个段落连接。

- 更改的文本会保存以用于撤消。格式化也是一种更改。因此，
  每个格式化操作都会保存文本以供撤消。这可能会消耗相当多的内存。

- 格式化长段落和/或具有复杂缩进的段落可能会很慢。

==============================================================================
7. 排序文本						*sorting*

Vim 有一个排序函数和一个排序命令。排序函数可以在这里找到：
|sort()|、|uniq()|。
另请参见 |:uniq|。

							*:sor* *:sort*
:[range]sor[t][!] [b][f][i][l][n][o][r][u][x] [/{pattern}/]
			对 [range] 中的行进行排序。当未指定范围时，
			对所有行进行排序。

			使用 [!] 时，顺序反转。

			使用 [i] 时，忽略大小写。
							*:sort-l*
			使用 [l] 时，排序使用当前排序规则区域设置。
			实现细节：使用 strcoll() 比较
			字符串。请参见 |:language| 以检查或设置排序规则
			区域设置。示例： >
				:language collate en_US.UTF-8
				:%sort l
<			|v:collate| 也可用于检查当前区域设置。
			使用区域设置排序通常忽略大小写。
			这在 Mac 上无法正常工作。

			选项 [n][f][x][o][b] 互斥。

			使用 [n] 时，排序基于行中的第一个十进制数
			（在 {pattern} 匹配之后或内部）。
			数字中包含一个前导 '-'。

			使用 [f] 时，排序基于行中的 Float。
			Float 的值确定方式类似于将文本
			（在 {pattern} 匹配之后或内部）传递给
			str2float() 函数。

			使用 [x] 时，排序基于行中的第一个十六进制数
			（在 {pattern} 匹配之后或内部）。
			忽略前导的 "0x" 或 "0X"。
			数字中包含一个前导 '-'。

			使用 [o] 时，排序基于行中的第一个八进制数
			（在 {pattern} 匹配之后或内部）。

			使用 [b] 时，排序基于行中的第一个二进制数
			（在 {pattern} 匹配之后或内部）。
							*:sort-u* *:sort-uniq*
			使用 [u]（u 代表唯一）时，仅保留相同行的序列中的
			第一行（当使用 [i] 时忽略大小写）。
			没有此标志时，相同行的序列将保留其原始顺序。
			注意，前导和尾随空格可能导致
			行看起来不同。
			如果您只想使内容唯一，请使用 |:uniq|。

			当指定 /{pattern}/ 且没有 [r] 标志时，
			将跳过与 {pattern} 匹配的文本，以便
			您可以对匹配后的内容进行排序。
			'ignorecase' 适用于模式，但 'smartcase'
			不会被使用。
			可以使用任何非字母字符代替斜杠。
			例如，要对第二个逗号分隔的字段进行排序： >
				:sort /[^,]*,/
<			要对虚拟列 10 处的文本进行排序（从而
			忽略制表符和空格之间的差异）： >
				:sort /.*\%10v/
<			要对行中的第一个数字进行排序，无论
			它前面是什么： >
				:sort /.\{-}\ze\d/
<			（解释：".\{-}" 匹配任何文本，"\ze" 设置
			匹配的结束位置，\d 匹配一个数字。）
			使用 [r] 时，排序基于匹配的 {pattern}，
			而不是像上面那样跳过它。
			例如，仅根据每行的前三个字母进行排序： >
				:sort /\a\a\a/ r

<			如果使用了 {pattern}，任何不匹配 {pattern} 的行
			将保持其当前顺序，但与匹配 {pattern} 的行
			分开。如果您按反向排序，它们将
			位于已排序行之后，否则它们将以
			原始顺序位于已排序行之前。

			如果 {pattern} 为空（例如指定了 //），
			则使用上次搜索模式。这允许先尝试
			模式。

注意，将 `:sort` 与 `:global` 一起使用不会对匹配的行进行排序，
这非常没用。

除非使用 l 标志，否则 `:sort` 不使用当前区域设置。
Vim 执行 "稳定" 排序。

排序可以中断，但如果您在过程中太晚中断，
您可能会得到重复的行。这也取决于所使用的系统库函数。

==============================================================================
8. 去重文本				*deduplicating* *unique*

Vim 有一个去重函数和一个去重命令。去重函数可以在这里找到：
|uniq()|。
另请参见 |:sort-uniq|。

							*:uni* *:uniq*
:[range]uni[q][!] [i][l][r][u] [/{pattern}/]
			删除 [range] 中彼此相邻的重复行。
			当未指定范围时，处理所有行。

			使用 [i] 时，比较行时忽略大小写。

			使用 [l] 时，比较使用当前排序规则区域设置。
			更多细节请参见 |:sort-l|。

			使用 [r] 时，比较基于匹配 /{pattern}/ 的文本，
			而不是整行。

			使用 [u] 时，仅保留不重复的行（即，不
			紧跟相同行的行）。

			使用 [!] 时，仅保留紧跟有重复项的行。

			如果同时指定了 [!] 和 [u]，则忽略 [u]，
			[!] 生效。

			当指定 /{pattern}/ 且未使用 [r] 时，
			跳过与 {pattern} 匹配的文本，比较
			在匹配之后的内容上进行。
			'ignorecase' 适用于模式，但 'smartcase'
			不会被使用。
			可以使用任何非字母字符代替斜杠。

			例如，根据第二个逗号分隔的字段删除相邻的
			重复行： >
				:uniq /[^,]*,/
<			或者仅保留忽略前 5 个字符的唯一行： >
				:uniq u /.\{5}/
<			如果 {pattern} 为空（例如使用了 //），
			则使用上次搜索模式。

			注意，前导和尾随空格可能导致
			行被认为不同。
			要删除所有重复项，无论位置如何，请使用
			|:sort-u| 或外部工具。

 vim:tw=78:ts=8:noet:ft=help:norl:
