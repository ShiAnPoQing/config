*autocmd.txt*   Nvim


		  VIM 参考手册	 由 Bram Moolenaar 编写


自动命令					*autocmd* *autocommand*

有关基本说明，请参见用户手册中的 |40.3| 节。

键入 |gO| 可查看目录。

==============================================================================
1. 介绍						*autocmd-intro*

您可以在读取或写入文件、进入或离开缓冲区或窗口以及退出 Vim 时，
指定自动执行的命令。例如，您可以创建一个自动命令，为匹配
`*.c` 的文件设置 'cindent' 选项。您也可以使用自动命令来实现高级
功能，例如编辑压缩文件（参见 |gzip-example|）。通常将自动命令
放在您的 vimrc 文件中。

				*E203* *E204* *E143* *E855* *E937* *E952* *E1312*
警告：使用自动命令非常强大，可能导致意外的副作用。
请注意不要破坏您的文本。
- 最好先在文件的可丢弃副本上进行一些测试。例如：如果您使用自动命令
  在开始编辑文件时解压缩它，请确保在写入时压缩的自动命令
  正常工作。
- 为中途发生错误做好准备（例如，磁盘已满）。Vim 大多
  能够撤消对缓冲区的更改，但您可能需要手动清理
  对其他文件的更改（例如，压缩已解压缩的文件）。
- 如果 BufRead* 事件允许您编辑压缩文件，则 FileRead*
  事件也应执行相同的操作（这在某些罕见情况下使恢复成为可能）。
  如果可能，最好对 File* 和 Buf* 事件使用相同的自动命令。

==============================================================================
2. 定义自动命令				*autocmd-define*

使用 |nvim_create_autocmd()| 从 Lua 定义事件处理程序。

Vimscript 命令在下面描述。

							*:au* *:autocmd*
:au[tocmd] [group] {event} {aupat} [++once] [++nested] {cmd}
			将 {cmd} 添加到 Vim 将自动执行的命令列表中，
			当匹配 {aupat} 的文件发生 {event} 时 |autocmd-pattern|。
			此处 {event} 不能是 "*"。  *E1155*
			注意：引号字符被视为 :autocmd 的参数，不会开始注释。
			Nvim 始终将 {cmd} 添加到现有自动命令之后，因此它们
			按照定义的顺序执行。
			关于 [++nested]，请参见 |autocmd-nested|。
							*autocmd-once*
			如果提供了 [++once]，则该命令执行一次
			（每个 {event}，可以是逗号分隔的列表），
			然后被移除（"一次性"）。

特殊模式 <buffer> 或 <buffer=N> 定义缓冲区本地自动命令。
请参见 |autocmd-buflocal|。

注意：只有当 "|" 出现在预期模式的位置时，":autocmd" 命令后面才能跟另一个命令。
这有效： >
	:augroup mine | au! BufRead | augroup END
但这样会将 "augroup" 视为所定义命令的一部分： >
	:augroup mine | au! BufRead * | augroup END
	:augroup mine | au BufRead * set tw=70 | augroup END
相反，您可以将组名放入命令中： >
	:au! mine BufRead *
	:au mine BufRead * set tw=70
或使用 `:execute`： >
	:augroup mine | exe "au! BufRead *" | augroup END
	:augroup mine | exe "au BufRead * set tw=70" | augroup END

<							*autocmd-expand*
注意，":autocmd" 参数中的特殊字符（例如 "%"、"<cword>"）在定义
自动命令时不会被展开。这些将在识别事件并执行 {cmd}
时被展开。唯一的例外是 "<sfile>"（与 "<script>" 不同）会在
定义自动命令时被展开。示例：
>
	:au BufNewFile,BufRead *.html so <sfile>:h/html.vim

这里 Vim 将 <sfile> 展开为包含此行的文件名。
但是，<sfile> 在函数中的工作方式不同，在这种情况下，最好
使用 `:execute` 和 <script> 来达到相同目的：
>
	:exe $'au BufNewFile,BufRead *.html so {expand("<script>:h")}/html.vim'

`:autocmd` 会向自动命令列表中添加内容，无论它们是否
已经存在。当您的 .vimrc 文件被多次加载时，自动命令
将出现多次。为避免这种情况，请在组中定义您的自动命令，以便
您可以轻松清除它们： >

	augroup vimrc
	  " 移除所有 vimrc 自动命令
	  autocmd!
	  au BufNewFile,BufRead *.html so <sfile>:h/html.vim
	augroup END

如果您不想移除所有自动命令，可以使用变量
来确保 Vim 仅包含一次自动命令： >

	:if !exists("autocommands_loaded")
	:  let autocommands_loaded = 1
	:  au ...
	:endif

当未给出 [group] 参数时，Vim 使用当前组（由 ":augroup" 定义）；
否则，Vim 使用 [group] 定义的组。注意，[group]
必须事先已定义。您不能使用 ":au group ..." 定义新组；请使用
":augroup" 来定义。

在测试自动命令时，您可能会发现 'verbose' 选项很有用： >
	:set verbose=9
此设置使 Vim 在执行自动命令时回显它们。

在脚本中定义自动命令时，它将能够调用脚本本地
的函数并使用脚本本地的映射。当事件被触发且命令执行时，
它将在定义它的脚本上下文中运行。如果在命令中使用了
|<SID>|，这一点很重要。

执行命令时，一个命令的消息会覆盖前一个消息。
这与手动执行命令时不同。大多数情况下，屏幕不会
向上滚动，因此没有 hit-enter 提示。当一个命令输出两条消息时，
这种情况仍可能发生。

==============================================================================
3. 移除自动命令			*autocmd!* *autocmd-remove*

使用 Lua，这些函数可以移除自动命令和自动命令组：

- |nvim_del_autocmd()|
- |nvim_del_augroup_by_id()|
- |nvim_del_augroup_by_name()|

Vimscript 命令在下面描述。

:au[tocmd]! [group] {event} {aupat} [++once] [++nested] {cmd}
			移除与 {event} 和 {aupat} 关联的所有自动命令，
			并添加命令 {cmd}。
			关于 [++once]，请参见 |autocmd-once|。
			关于 [++nested]，请参见 |autocmd-nested|。

:au[tocmd]! [group] {event} {aupat}
			移除与 {event} 和 {aupat} 关联的所有自动命令。

:au[tocmd]! [group] * {aupat}
			移除所有事件中与 {aupat} 关联的所有自动命令。

:au[tocmd]! [group] {event}
			移除 {event} 的所有自动命令。
			警告：您不应该在没有组的情况下对 |BufRead|
			和其他常见事件执行此操作，这可能会破坏
			插件、语法高亮等。

:au[tocmd]! [group]	移除所有自动命令。
			注意：引号将被视为 :autocmd 的参数，
			不会开始注释。
			警告：通常您不应该在没有组的情况下执行此操作，
			这会破坏插件、语法高亮等。

当未给出 [group] 参数时，Vim 使用当前组（由 ":augroup" 定义）；
否则，Vim 使用 [group] 定义的组。

==============================================================================
4. 列出自动命令					*autocmd-list*

:au[tocmd] [group] {event} {aupat}
			显示与 {event} 和 {aupat} 关联的自动命令。

:au[tocmd] [group] * {aupat}
			显示所有事件中与 {aupat} 关联的自动命令。

:au[tocmd] [group] {event}
			显示 {event} 的所有自动命令。

:au[tocmd] [group]	显示所有自动命令。

如果提供了 [group] 参数，Vim 仅列出该 [group] 的自动命令；
否则，Vim 列出所有组的自动命令。注意，此参数行为
与定义和移除自动命令时不同。

要列出缓冲区本地自动命令，请使用 <buffer> 或 <buffer=N> 形式的模式。
请参见 |autocmd-buflocal|。

							*:autocmd-verbose*
当 'verbose' 非零时，列出自动命令还会显示它最后定义的位置。
示例： >

    :verbose autocmd BufEnter
    FileExplorer  BufEnter
	*	  call s:LocalBrowse(expand("<amatch>"))
	    Last set from /usr/share/vim/vim-7.0/plugin/NetrwPlugin.vim
<
更多信息请参见 |:verbose-cmd|。

==============================================================================
5. 事件					*autocmd-events* *E215* *E216*

您可以指定一个逗号分隔的事件名称列表。此列表中不能使用空格。
该命令适用于列表中的所有事件。

对于读取文件，有四种可能的事件类型：
	BufNewFile			开始编辑不存在的文件
	BufReadPre	BufReadPost	开始编辑现有文件
	FilterReadPre	FilterReadPost	读取包含过滤器输出的临时文件
	FileReadPre	FileReadPost	任何其他文件读取
Vim 在读取文件时仅使用这四种类型之一。在读取文件之前和之后，
都会触发 "Pre" 和 "Post" 事件。

注意，"*ReadPre" 事件和所有 Filter 事件的自动命令
不允许更改当前缓冲区（如果发生这种情况，您将收到错误消息）。
这是为了防止将文件读入错误的缓冲区。

注意，'modified' 标志在执行 BufReadPost 和 BufNewFile 自动命令
*之后*被重置。但是，如果 'modified' 选项由自动命令设置，
则不会发生这种情况。

您可以使用 'eventignore' 选项来忽略多个事件或所有事件。

							*event-data-types*
提供 |event-data| 的事件在 `runtime/lua/vim/_meta/events.lua` 中定义了相应的
`vim.event.<name>.data` 类型。如果您启用了 Lua LSP，
您可以注释您的处理程序以获得类型检查和补全： >lua

    vim.api.nvim_create_autocmd('LspAttach', {
      ---@param ev {data: vim.event.lspattach.data}
      callback = function(ev)
        local client = vim.lsp.get_client_by_id(ev.data.client_id)
      end,
    })
<
							*events* *{event}*
Nvim 识别以下事件。名称不区分大小写。

							*BufAdd*
BufAdd				将新缓冲区或现有的未列出缓冲区添加到
				缓冲区列表之后（启动期间除外，
				参见 |VimEnter|），或重命名已列出的
				缓冲区之后。
				在 |BufEnter| 之前。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|

							*BufDelete*
BufDelete			从缓冲区列表中删除缓冲区之前。
				如果缓冲区已加载，可能首先调用 BufUnload。
				也用于缓冲区列表中的缓冲区被重命名之前。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|
				不要切换到另一个缓冲区。

							*BufEnter*
BufEnter			进入（访问、切换到）新缓冲区或
				现有缓冲区之后。对于设置文件类型选项很有用。
				与 |BufNew| 比较，后者不会为现有缓冲区触发。
				在 |BufAdd| 之后。
				在 |BufReadPost| 之后。

							*BufFilePost*
BufFilePost			使用 ":file" 或 ":saveas" 命令更改当前
				缓冲区名称之后。

							*BufFilePre*
BufFilePre			使用 ":file" 或 ":saveas" 命令更改当前
				缓冲区名称之前。

							*BufHidden*
BufHidden			缓冲区变为隐藏之前：当不再有窗口显示
				该缓冲区时，但缓冲区未被卸载或删除。

				退出 Vim 时，不用于 ":qa" 或 ":q"。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|

							*BufLeave*
BufLeave			离开到另一个缓冲区之前。也适用于离开或
				关闭当前窗口，且新的当前窗口不用于
				同一缓冲区时。

				退出 Vim 时，不用于 ":qa" 或 ":q"。

							*BufNew*
BufNew				创建新缓冲区之后（启动期间除外，
				参见 |VimEnter|）或重命名现有缓冲区之后。
				与 |BufEnter| 不同，访问（切换到）现有缓冲区
				不会再次触发此事件。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|
				另请参见 |BufAdd|、|BufNewFile|。

							*BufNewFile*
BufNewFile			开始编辑不存在的文件时。
				可用于读取骨架文件。

						*BufRead* *BufReadPost*
BufRead 或 BufReadPost		开始编辑新缓冲区时，将文件读入缓冲区之后，
				处理 modeline 之前。参见 |BufWinEnter| 以
				在处理 modeline 之后执行操作。
				也触发于：
				- 以某种方式写入未命名缓冲区，使
				  缓冲区获得名称时
				- 成功恢复文件之后
				- 执行 ":filetype detect" 时的
				  "filetypedetect" 组
				不触发于：
				- `:read file` 命令
				- 文件不存在时

							*BufReadCmd*
BufReadCmd			开始编辑新缓冲区之前。应将文件读入
				缓冲区。 |Cmd-event|

						*BufReadPre* *E200* *E201*
BufReadPre			开始编辑新缓冲区时，将文件读入缓冲区之前。
				如果文件不存在，则不使用。

							*BufUnload*
BufUnload			卸载缓冲区之前，当缓冲区中的文本将被
				释放时。
				在 BufWritePost 之后。
				在 BufDelete 之前。
				当 Vim 即将退出时，为所有已加载的缓冲区触发。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|
								*E1546*
				不要切换缓冲区或窗口！
				当 v:dying 为 2 或更高时退出时不触发。

							*BufWinEnter*
BufWinEnter			缓冲区显示在窗口中之后。这可能发生在
				缓冲区加载时（处理 modeline 之后）或隐藏缓冲区
				显示时（且不再是隐藏状态）。

				对于不带参数的 |:split| 不触发，
				因为缓冲区没有更改，或者对于已在一个窗口中
				打开的文件进行 :split 也不触发。
				对于使用当前缓冲区名称的 ":split" 触发，
				因为它会重新加载该缓冲区。

							*BufWinLeave*
BufWinLeave			从窗口中移除缓冲区之前。如果它在另一个
				窗口中仍然可见，则不触发。
				退出时也触发。
				在 BufUnload、BufHidden 之前。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|
				当 v:dying 为 2 或更高时退出时不触发。

							*BufWipeout*
BufWipeout			完全删除缓冲区之前。
				如果缓冲区已加载且位于缓冲区列表中，可能
				首先调用 BufUnload 和 BufDelete 事件。
				也用于缓冲区被重命名之前（即使它不在
				缓冲区列表中）。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>"、"<abuf>"。 |<buffer=abuf>|
				不要切换到另一个缓冲区。

						*BufWrite* *BufWritePre*
BufWrite 或 BufWritePre		将整个缓冲区写入文件之前。

							*BufWriteCmd*
BufWriteCmd			将整个缓冲区写入文件之前。
				应执行文件的写入，如果成功则重置
				'modified'，除非 'cpo' 中包含 '+' 并且
				写入到另一个文件 |cpo-+|。
				不应更改缓冲区内容。
				当命令重置 'modified' 时，撤消信息会被
				调整，以将较旧的撤消状态标记为 'modified'，
				就像 |:write| 那样。使用 |'[| 和 |']| 标记
				作为行范围。 |Cmd-event|

							*BufWritePost*
BufWritePost			将整个缓冲区写入文件之后
				（应撤消 BufWritePre 的命令）。

							*ChanClose*
ChanClose			通道关闭之后，被移除之前。
				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。
				设置这些 |v:event| 键：
				    info	来自 |nvim_get_chan_info()|

							*ChanInfo*
ChanInfo			通道状态发生变化，例如 RPC 通道的
				客户端描述了自身。
				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。
				设置这些 |v:event| 键：
				    info	来自 |nvim_get_chan_info()|

							*ChanOpen*
ChanOpen			通道刚打开之后。
				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。
				设置这些 |v:event| 键：
				    info	来自 |nvim_get_chan_info()|

							*CmdAtom*
CmdAtom				在用户操作（一个输入 "原子"）之后：任何
				普通命令、移动、操作符、插入会话、可视序列、
				Ex 命令行（":cnext<CR>"）、映射（及子原子）、宏、
				或滚动/鼠标。

				<amatch> 是原子 "类型"（见下文）。
				事件是 |deferred| 的。

				仅针对用户输入，而非编程输入：
					输入			原子 ~
					键入的键		是
					映射、宏 "@q"		是
					|nvim_input()|		是
					|nvim_feedkeys()|	带有 "t"（见注释）
					Hydrogen		是
					|:normal|		否
					API 请求		否
					由脚本输入的 "@q"	否
					|multicursor| 级联	否
					中止的操作		否
					终端模式键		否
					鼠标拖动/释放		否（待办？）

				注意：映射执行期间输入的所有键都是已解析的
				`keys` 的一部分。但进入 |Terminal-mode|
				（例如选择器/模糊查找器 UI）会完成该原子：
				终端输入不会被捕获，因为没有定义的方法来
				界定此类操作的 "结束"。

				插件不应通过 `nvim_exec_autocmds` 发布 "虚假"
				CmdAtom 事件。

				|event-data| 包含以下字段（类型：`vim.event.cmdatom.data`）：

				- atoms：复合原子（多命令映射、可视序列）的
				  有序 "子原子"。
				- changed：缓冲区被该原子更改。
				- cmd：命令/移动/对象名称："w"、"f"、
				  "iw"、"gJ"、"<Home>" 等。 |key-notation|
				- cmdarg：`cmd` 的操作数："fx" 中的 "x"，
				  |r| 的替换字符。 |key-notation|
				- count：有效的 |count|，或 nil。
				- keys：已解析的（映射后）按键序列。
				  原始内部字节（使用 |keytrans()| 获取
				  按键表示法）：输入给 |nvim_feedkeys()|
				  （模式 "n"）以重放。空：不可重放。
				  Nil：有损捕获，重放 `lhs`（模式 "m"）。
				- lhs：用户输入，在它被解析/转换之前。
				  映射 LHS 加上它读取的任何有效载荷
				  （|getchar()|）、宏寄存器（"@q"）或
				  可视序列。原始字节，类似于 `keys`。
				- motionforce |forced-motion|："v"、"V" 或
				  "<C-V>"（|key-notation|）。
				- moved：光标移动了。
				- operator：操作符："d"、"g@"、"zf" 等。
				  |key-notation|，类似于 `cmd`。
				- pos：起始位置：[行，列]，行从 1 开始索引，
				  列从 0 开始索引。
				- reg：寄存器名称。
				- text：有效载荷文本：插入会话的已插入文本
				  （在其最后一次光标移动之后，类似于 |quote.|）、
				  Ex 命令行或搜索。
				  文字文本，不是按键编码。
				  示例：
				    - 键入 "iab<Esc>" → text="ab"
				    - 键入 ":cnext<CR>" → text="cnext"
				    - 键入 "<Cmd>cnext<CR>" → text="cnext"
				    - 键入 "iab<Left>c<Esc>" → text="c"
				- type：
				    - "excmd"
				    - "insert"
				    - "jump"：通过绝对/共享导航状态
				      （|jumplist|、标记、|star|）移动光标。
				    - "mapping"
				    - "motion"：非跳转移动。可能发出
				      `moved=false`（缓冲区末尾的 "w"、失败的
				      "fx" 等）。
				    - "mouse"
				    - "normal"：非移动/跳转命令（"u"、
				      CTRL-R、"za"、CTRL-W 等）。
				    - "operator"
				    - "scroll"
				    - "visual"
				- undoseq：操作后的撤消状态
				  （|undotree()| `seq_cur`）。撤消时减少。

							*CmdlineChanged*
CmdlineChanged			命令行中每次更改之后。在映射期间也会触发！
				请在映射中使用 |<Cmd>| 代替 ":" 以避免这种情况。

				<afile> 展开为 |cmdline-char|。

							*CmdlineEnter*
CmdlineEnter			进入命令行之后（包括映射中非交互式使用
				":"：请改用 |<Cmd>| 以避免这种情况）。
				模式与 |cmdline-char| 匹配。
				<afile> 展开为 |cmdline-char|。
				设置这些 |v:event| 键：
				    cmdlevel
				    cmdtype

							*CmdlineLeave*
CmdlineLeave			离开命令行之前（包括映射中非交互式使用
				":"：请改用 |<Cmd>| 以避免这种情况）。
				<afile> 展开为 |cmdline-char|。
				将 |v:char| 设置为退出命令行的键
				（例如 <CR>、<CTRL-C>、<Esc>）。
				设置这些 |v:event| 键：
				    abort（可变的）
				    cmdlevel
				    cmdtype
				注意：`abort` 只能从 false 更改为 true：
				无法通过将其更改为 false 来执行已中止的
				命令行。

							*CmdlineLeavePre*
CmdlineLeavePre			即将离开命令行时，在 |CmdlineLeave| 之前。
				对于使用 |cmdcomplete_info()| 捕获补全信息
				很有用，因为这些信息在 |CmdlineLeave|
				触发之前被清除。对于映射中非交互式使用 ":"
				会触发，但使用 |<Cmd>| 时不会触发。通过键入
				CTRL-C 或 <Esc> 放弃命令行时也会触发。
				<afile> 设置为 |cmdline-char|。
				设置 |v:char|，与 |CmdlineLeave| 相同。

							*CmdUndefined*
CmdUndefined			当用户命令被使用但未定义时。
				对于仅在需要时定义命令很有用。模式与
				命令名称匹配。<amatch> 和 <afile> 都展开为
				命令名称。
				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。
				注意：在命令定义之前，自动补全将无法工作。
				另一种方法是始终定义用户命令，并让它
				调用一个自动加载的函数。请参见 |autoload|。

							*CmdwinEnter*
CmdwinEnter			进入命令行窗口之后。
				对于专门为此特殊类型的窗口设置选项
				很有用。
				<afile> 展开为单个字符，
				表示命令行的类型。
				|cmdwin-char|

							*CmdwinLeave*
CmdwinLeave			离开命令行窗口之前。
				对于清理使用 CmdwinEnter 进行的任何全局设置
				很有用。
				<afile> 展开为单个字符，
				表示命令行的类型。
				|cmdwin-char|

							*ColorScheme*
ColorScheme			加载颜色方案之后。 |:colorscheme|
				如果未找到颜色方案，则不触发。
				模式与颜色方案名称匹配。<afile> 可用于
				设置此选项的实际文件的名称，<amatch> 用于
				新的颜色方案名称。

							*ColorSchemePre*
ColorSchemePre			加载颜色方案之前。 |:colorscheme|
				对于设置在加载另一个颜色方案之前移除
				当前颜色方案添加的内容很有用。

CompleteChanged					*CompleteChanged*
				插入模式补全菜单每次更改之后。弹出菜单隐藏时
				不触发，请改用 |CompleteDonePre| 或 |CompleteDone|。

				设置这些 |v:event| 键：
				    completed_item	参见 |complete-items|。
				    height		可见项目数
				    width		屏幕单元格数
				    row			顶部屏幕行
				    col			最左侧屏幕列
				    size		项目总数
				    scrollbar		如果可见则为 TRUE

				非递归（事件不能触发自身）。
				无法更改文本。 |textlock|

				弹出菜单的大小和位置也可以通过调用
				|pum_getpos()| 获得。

							*CompleteDone*
CompleteDone			插入模式补全完成之后。无论是完成了
				补全还是放弃了补全。
				|ins-completion|
				|complete_info()| 在此之前被清除；如果您需要，
				请使用 CompleteDonePre。
				|v:completed_item| 给出已完成的项，
				如果补全被放弃，则为空字典。

				设置这些 |v:event| 键：
				    complete_word	被选中的词，如果补全
						被放弃（丢弃）则为空。
				    complete_type	|complete_info_mode|
				    reason		补全完成的原因。
						可以是以下之一：
					- "accept"：补全被
					  |complete_CTRL-Y| 接受。
					- "cancel"：补全被
					  |complete_CTRL-E| 停止。
					- "discard"：补全因其他原因
					  被放弃。

							*CompleteDonePre*
CompleteDonePre			插入模式补全完成之后。无论是完成了
				补全还是放弃了补全。
				|ins-completion|
				|complete_info()| 在此事件期间有效。
				|v:completed_item| 给出已完成的项。

							*CursorHold*
CursorHold			在普通模式下，'updatetime' 持续时间内没有
				用户输入时触发。在等待命令参数或操作符后的
				移动时，以及录制宏时，不触发。
				参见 |CursorHold-example|。

				注意：此事件不能使用交互式命令。没有
				hit-enter 提示，屏幕会直接更新（如果需要）。
				注意：将来可能会有另一个选项来设置时间。
				提示：要强制更新状态行，请使用： >
					:let &ro = &ro
<
							*CursorHoldI*
CursorHoldI			类似于 CursorHold，但在插入模式下。在等待
				另一个键时（例如在 CTRL-V 之后）不触发，在
				CTRL-X 模式 |insert_expand| 下也不触发。

							*CursorMoved*
CursorMoved			在普通模式或可视模式下光标移动之后，或
				移动到另一个窗口之后。也适用于光标行的文本
				被更改时，例如使用 "x"、"rx" 或 "p"。
				当存在 typeahead、执行脚本文件中的命令时，
				或操作符挂起时，并不总是触发。
				示例请参见 |match-parens|。
				注意：不能使用 |:noautocmd| 跳过。
				注意：此事件触发非常频繁，不要
				执行用户不期望或速度慢的操作。

							*CursorMovedC*
CursorMovedC			命令行中光标移动之后。请注意不要弄乱
				命令行，这可能导致 Vim 锁定。
				<afile> 展开为 |cmdline-char|。

							*CursorMovedI*
CursorMovedI			插入模式下光标移动之后。当弹出菜单可见时
				不触发。其他方面与 CursorMoved 相同。

							*DiffUpdated*
DiffUpdated			差异更新之后。根据所使用的差异类型
				（内部或外部），这可能每次更改时触发，
				也可能在执行 |:diffupdate| 时触发。

							*DirChanged*
DirChanged			|current-directory| 更改之后。
				模式可以是：
					"auto"     在 'autochdir' 上触发。
					"buffer"   在 `:bcd` 上触发。
					"global"   在 `:cd` 上触发。
					"tabpage"  在 `:tcd` 上触发。
					"window"   在 `:lcd` 上触发。
				设置这些 |v:event| 键：
				    cwd:            当前工作目录
				    scope:          "global"、"tabpage"、"window"、"buffer"
				    changed_window: 如果我们在切换窗口（或标签页）
				                    时触发事件，则为 v:true
				<afile> 设置为新目录名称。
				非递归（事件不能触发自身）。

							*DirChangedPre*
DirChangedPre			|current-directory| 即将更改时，与 |DirChanged|
				类似。
				模式与 |DirChanged| 类似。
				设置这些 |v:event| 键：
				    directory:      新的工作目录
				    scope:          "global"、"tabpage"、"window"、"buffer"
				    changed_window: 如果我们在切换窗口（或标签页）
				                    时触发事件，则为 v:true
				<afile> 设置为新目录名称。
				非递归（事件不能触发自身）。

							*ExitPre*
ExitPre				使用 `:quit`、`:wq` 以导致 Vim 退出的方式，
				或使用 `:qall` 时，在 |QuitPre| 之后。
				可用于关闭任何非必要的窗口。如果存在
				未自动保存的已修改缓冲区，退出仍可能被取消，
				请使用 |VimLeavePre| 以真正退出。
				另请参见 |QuitPre|、|WinClosed|。

							*FileAppendCmd*
FileAppendCmd			追加到文件之前。应执行追加到文件的操作。
				使用 '[ 和 '] 标记作为行范围。 |Cmd-event|

							*FileAppendPost*
FileAppendPost			追加到文件之后。

							*FileAppendPre*
FileAppendPre			追加到文件之前。使用 '[ 和 '] 标记作为
				行范围。

							*FileChangedRO*
FileChangedRO			首次更改只读文件之前。可用于从源代码
				控制系统中签出文件。如果更改由自动命令
				引起，则不触发。
				在缓冲区中进行首次更改或设置 'readonly'
				后的首次更改时触发，就在更改应用于
				文本之前。
				警告：如果自动命令移动光标，
				更改的效果是未定义的。
							*E788*
				不能切换缓冲区。您可以重新加载
				缓冲区，但不能编辑另一个缓冲区。
							*E881*
				如果行数发生变化，保存撤消信息可能会失败，
				更改将被中止。

							*FileChangedShell*
FileChangedShell		当 Vim 注意到自开始编辑以来文件的修改时间
				发生变化时。也适用于文件属性发生变化或
				文件大小发生变化时。 |timestamp|
				为每个更改的文件触发，在以下操作之后：
				- 执行 shell 命令
				- |:checktime|
				- |FocusGained|

				当设置了 'autoread' 且缓冲区未被更改时，
				不使用。如果存在 FileChangedShell 自动命令，
				则不会给出警告消息和提示。
				|v:fcs_reason| 指示发生了什么。设置
				|v:fcs_choice| 以控制接下来发生什么。
				注意：当前缓冲区 "%" 不是目标
				缓冲区 "<afile>" 和 "<abuf>"。 |<buffer=abuf>|
							*E246* *E811*
				不能切换、跳转到或删除缓冲区。
				非递归（事件不能触发自身）。

							*FileChangedShellPost*
FileChangedShellPost		处理在 Vim 外部更改的文件之后。
				可用于更新状态行。

							*FileReadCmd*
FileReadCmd			使用 ":read" 命令读取文件之前。
				应执行文件的读取。 |Cmd-event|

							*FileReadPost*
FileReadPost			使用 ":read" 命令读取文件之后。
				注意，Vim 将 '[ 和 '] 标记设置为读取的
				第一行和最后一行。这可用于操作刚
				读取的行。

							*FileReadPre*
FileReadPre			使用 ":read" 命令读取文件之前。

							*FileType*
FileType			当 'filetype' 选项被设置时。模式与
				文件类型匹配。<afile> 是设置此选项的
				文件的名称。<amatch> 是 'filetype' 的
				新值。
				不能切换窗口或缓冲区。
				参见 |filetypes|。

							*FileWriteCmd*
FileWriteCmd			写入文件之前，当不是写入整个
				缓冲区时。应执行写入文件的操作。
				不应更改缓冲区。使用 |'[| 和 |']| 标记
				作为行范围。 |Cmd-event|

							*FileWritePost*
FileWritePost			写入文件之后，当不是写入整个
				缓冲区时。

							*FileWritePre*
FileWritePre			写入文件之前，当不是写入整个
				缓冲区时。使用 |'[| 和 |']| 标记作为
				行范围。

							*FilterReadPost*
FilterReadPost			从过滤器命令读取文件之后。
				Vim 将模式与当前缓冲区的名称进行匹配，
				就像 FilterReadPre 一样。
				当 'shelltemp' 关闭时不触发。

							*FilterReadPre* *E135*
FilterReadPre			从过滤器命令读取文件之前。
				Vim 将模式与当前缓冲区的名称进行匹配，
				而不是与作为过滤器命令输出的临时文件的
				名称匹配。
				当 'shelltemp' 关闭时不触发。

							*FilterWritePost*
FilterWritePost			为过滤器命令写入文件之后，或使用
				外部差异进行比较之后（内部差异请参见
				|DiffUpdated|）。
				Vim 将模式与当前缓冲区的名称进行匹配，
				就像 FilterWritePre 一样。
				当 'shelltemp' 关闭时不触发。

							*FilterWritePre*
FilterWritePre			为过滤器命令写入文件之前，或使用
				外部差异进行比较之前。
				Vim 将模式与当前缓冲区的名称进行匹配，
				而不是与作为过滤器命令输出的临时文件的
				名称匹配。
				当 'shelltemp' 关闭时不触发。

							*FocusGained*
FocusGained			Nvim 获得焦点。

							*FocusLost*
FocusLost			Nvim 失去焦点。也（可能）在弹出
				GUI 对话框时。

							*FuncUndefined*
FuncUndefined			当用户函数被使用但未定义时。
				对于仅在需要时定义函数很有用。模式与
				函数名称匹配。<amatch> 和 <afile> 都设置为
				函数的名称。
				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。
				注意：编写 Vim 脚本时，更好的替代方案
				是使用自动加载函数。请参见 |autoload-functions|。

							*InsertChange*
InsertChange			在插入模式或替换模式下键入 <Insert> 时。
				|v:insertmode| 变量指示新模式。
				请注意不要移动光标或执行用户不期望的
				任何其他操作。

							*InsertCharPre*
InsertCharPre			在插入模式下键入字符时，在插入字符
				之前。
				|v:char| 变量指示键入的字符，可以在事件
				期间更改以插入不同的字符。当 |v:char|
				设置为多个字符时，此文本将被原样插入。

				无法更改文本。 |textlock|

							*InsertEnter*
InsertEnter			即将开始插入模式之前。也适用于替换模式
				和虚拟替换模式。|v:insertmode| 变量指示
				模式。请注意不要执行用户不期望的
				任何其他操作。
				光标之后会被恢复。如果您不希望这样，
				请将 |v:char| 设置为非空字符串。

							*InsertLeavePre*
InsertLeavePre			即将离开插入模式之前。也适用于使用
				CTRL-O |i_CTRL-O| 时。请注意不要更改模式
				或使用 `:normal`，这很可能会导致问题。

							*InsertLeave*
InsertLeave			刚刚离开插入模式之后。也适用于使用
				CTRL-O |i_CTRL-O| 时。但不适用于 |i_CTRL-C|。

LspAttach			参见 |LspAttach|
LspDetach			参见 |LspDetach|
LspNotify			参见 |LspNotify|
LspProgress			参见 |LspProgress|
LspRequest			参见 |LspRequest|
LspTokenUpdate			参见 |LspTokenUpdate|

							*MarkSet*
MarkSet				在通过 |m|、|:mark|、|nvim_buf_set_mark()| 设置
				|mark| 之后，或通过 |:delmarks| 和
				|nvim_buf_del_mark()| 删除之后。支持 `[a-zA-Z]`
				标记（将来可能支持更多）。
				|autocmd-pattern| 与标记名称匹配（例如 `[ab]`
				匹配 `a` 或 `b`，`*` 匹配所有）。如果行号为 0，
				则表示标记已被删除。

				|event-data| 包含以下键（类型：`vim.event.markset.data`）：
				- name：标记名称（例如 "a"）
				- line：标记行号。
				- col：标记列号。

							*MenuPopup*
MenuPopup			即将显示弹出菜单（在右键单击下方）
				之前。对于根据光标或鼠标指针下的
				内容调整菜单很有用。
				模式与表示模式的一个或两个字符匹配：
					n	普通
					v	可视
					o	操作符挂起
					i	插入
					c	命令行
					tl	终端

							*ModeChanged*
ModeChanged			更改模式之后。模式与 `'old_mode:new_mode'` 匹配，
				例如匹配 `*:c*` 以模拟 |CmdlineEnter|。
				设置以下 |v:event| 值：
				   old_mode	更改之前的模式。
				   new_mode	新模式，也与使用非零参数调用的
					|mode()| 返回的值相同。
				当 ModeChanged 触发时，old_mode 将
				具有上次触发事件时 new_mode 的值。
				这将针对每次次要模式更改触发。
				使用示例：进入可视模式时使用相对行号： >vim
		:au ModeChanged [vV\x16]*:* let &l:rnu = mode() =~# '^[vV\x16]'
		:au ModeChanged *:[vV\x16]* let &l:rnu = mode() =~# '^[vV\x16]'
		:au WinEnter,WinLeave * let &l:rnu = mode() =~# '^[vV\x16]'
<
							*OptionSet*
OptionSet			设置选项之后（启动期间除外）。
				|autocmd-pattern| 与长选项名称匹配。
				|<amatch>| 指示设置了哪个选项。

				|v:option_type| 指示它是全局作用域
				还是局部作用域。
				|v:option_command| 指示使用了哪种 set/let
				命令类型（请跟随标签查看表格）。
				|v:option_new| 指示新设置的值。
				|v:option_oldlocal| 具有旧的局部值。
				|v:option_oldglobal| 具有旧的全局值。
				|v:option_old| 指示旧的选项值。

				|v:option_oldlocal| 仅在 |:set|、|:setlocal|
				或 |modeline| 用于设置选项时设置。
				类似地，|v:option_oldglobal| 仅在
				使用 |:set| 或 |:setglobal| 时设置。

				这不会设置 |<abuf>|，您可以使用 |bufnr()|。

				注意，当使用 |:set| 设置 |global-local| 选项时，
				|v:option_old| 是旧的全局值。然而，
				对于所有非全局-局部的选项，它是旧的局部
				值。

				使用示例：检查 'backupdir' 和 'undodir'
				选项中的目录是否存在，如果目录不存在则
				创建它。

				注意：在此自动命令期间不要重置相同的选项，
				这可能会破坏插件。您始终可以使用 |:noautocmd|
				来防止触发 OptionSet。

				注意：当由 'modified' 触发时，OptionSet 是
				|deferred| 的，即使通过 |:set| 设置也是如此。

				非递归：自动命令中的 |:set| 不会再次触发
				OptionSet。

				启动时不触发。

PackChangedPre			参见 |PackChangedPre|
PackChanged			参见 |PackChanged|

Progress					*Progress*
				通过 `nvim_echo` 创建或更新 |progress-message| 之后。
				模式与消息的 `source` 匹配。

				|event-data| 包含以下键（类型：`vim.event.progress.data`）：
				- id：消息的 id
				- text：消息的文本
				- title：进度消息的标题
				- source：进度消息的来源
				- status：进度消息的状态
				- percent：已完成进度的百分比
				- data：随消息发送的任意数据

				使用示例：
>lua
		vim.api.nvim_create_autocmd('Progress', {
		  pattern={'term'},
		  callback = function(ev)
		    print(string.format('event fired: %s', vim.inspect(ev)))
		  end
		})
		local id = vim.api.nvim_echo({{'searching...'}}, true, {kind='progress', status='running', percent=10, title='term', source='search'})
		vim.api.nvim_echo({{'searching'}}, true, {id = id, kind='progress', status='running', percent=50, title='term', source='search'})
		vim.api.nvim_echo({{'done'}}, true, {id = id, kind='progress', status='success', percent=100, title='term', source='search'})

<
							*QuickFixCmdPre*
QuickFixCmdPre			运行 quickfix 命令之前（|:make|、
				|:lmake|、|:grep|、|:lgrep|、|:grepadd|、
				|:lgrepadd|、|:vimgrep|、|:lvimgrep|、
				|:vimgrepadd|、|:lvimgrepadd|、
				|:cfile|、|:cgetfile|、|:caddfile|、|:lfile|、
				|:lgetfile|、|:laddfile|、|:helpgrep|、
				|:lhelpgrep|、|:cexpr|、|:cgetexpr|、
				|:caddexpr|、|:cbuffer|、|:cgetbuffer|、
				|:caddbuffer|）。
				模式与正在运行的命令匹配。当使用 |:grep|
				但 'grepprg' 设置为 "internal" 时，它仍然匹配
				"grep"。
				此命令不能用于设置 'makeprg' 和
				'grepprg' 变量。
				如果此命令导致错误，则不会执行 quickfix
				命令。

							*QuickFixCmdPost*
QuickFixCmdPost			类似于 QuickFixCmdPre，但在 quickfix 命令
				运行之后，跳转到第一个位置之前。
				对于 |:cfile| 和 |:lfile| 命令，它在错误文件
				被读取之后、移动到第一个错误之前运行。
				参见 |QuickFixCmdPost-example|。

							*QuitPre*
QuitPre				使用 `:quit`、`:wq` 或 `:qall` 时，在决定
				是关闭当前窗口还是退出 Vim 之前。
				对于 `:wq`，缓冲区在 QuitPre 触发之前写入。
				如果当前窗口是最后一个普通窗口，可用于
				关闭任何非必要窗口。
				另请参见 |ExitPre|、|WinClosed|。

							*RecordingEnter*
RecordingEnter			宏开始录制时。
				模式是当前文件名，|reg_recording()| 是
				正在使用的当前寄存器。

							*RecordingLeave*
RecordingLeave			宏停止录制时。
				模式是当前文件名，|reg_recording()| 是
				已录制的寄存器。
				|reg_recorded()| 在此事件之后才更新。
				设置这些 |v:event| 键：
				    regcontents
				    regname

							*RemoteReply*
RemoteReply			当从作为服务器的 Vim 收到 server2client()
				的回复时。模式与 {serverid} 匹配。
				<amatch> 等于发送回复的 {serverid}，
				<afile> 是实际的回复字符串。
				注意，即使定义了自动命令，
				也应使用 remote_read() 读取回复以消费它。

							*SafeState*
SafeState			当没有挂起的操作，即将等待用户键入
				字符时。
				在以下情况下不会触发：
				- 操作符挂起
				- 使用 "r 输入寄存器
				- 命令执行到一半
				- 执行映射
				- 存在 typeahead
				- 插入模式补全处于活动状态
				- 命令行补全处于活动状态
				您可以使用 `mode()` 来查看 Vim 处于什么状态。
				可能是：
				- 可视模式
				- 普通模式
				- 插入模式
				- 命令行模式
				根据您想要执行的操作，您可能还需要
				使用 `state()` 进行更多检查，例如屏幕是否
				因消息而滚动。

							*SearchWrapped*
SearchWrapped			使用 |n| 或 |N| 进行搜索后，如果搜索分别
				环绕文档回到开头/结尾。

							*SessionLoadPre*
SessionLoadPre			加载使用 |:mksession| 命令创建的会话文件之前。

							*SessionLoadPost*
SessionLoadPost			加载使用 |:mksession| 命令创建的会话文件之后。

							*SessionWritePre*
SessionWritePre			写入使用 |:mksession| 命令创建的会话文件之前。

							*SessionWritePost*
SessionWritePost		通过调用 |:mksession| 命令写入会话文件之后。

							*ShellCmdPost*
ShellCmdPost			使用 |:!cmd|、|:make| 和 |:grep| 执行 shell
				命令之后。可用于检查任何更改的文件。
				对于非阻塞 shell 命令，请参见
				|job-control|。

							*ShellFilterPost*
ShellFilterPost			使用 ": {range}!cmd"、":w !cmd" 或 ":r !cmd"
				执行 shell 命令之后。
				可用于检查任何更改的文件。

							*Signal*
Signal				Nvim 收到信号之后。模式与信号名称匹配。仅
				支持 "SIGUSR1" 和 "SIGWINCH"。
				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。
				示例： >vim
				    autocmd Signal SIGUSR1 call some#func()

<							*SourceCmd*
SourceCmd			加载 Vimscript/Lua 文件时。|:source|
				<afile> 是被加载文件的名称。
				自动命令必须加载该文件。
				|Cmd-event|

							*SourcePre*
SourcePre			加载 Vimscript/Lua 文件之前。|:source|
				<afile> 是被加载文件的名称。

							*SourcePost*
SourcePost			加载 Vimscript/Lua 文件之后。|:source|
				<afile> 是被加载文件的名称。
				当加载被中断时不触发。
				在触发了 SourceCmd 自动命令之后也会触发。

							*SpellFileMissing*
SpellFileMissing		尝试加载拼写检查文件但找不到时。
				模式与语言匹配。<amatch> 是语言，
				'encoding' 也很重要。参见
				|spell-SpellFileMissing|。

							*StdinReadPost*
StdinReadPost			启动期间，从 stdin 读取到缓冲区之后，
				执行 modeline 之前。 |--|

							*StdinReadPre*
StdinReadPre			启动期间，从 stdin 读取到缓冲区之前。 |--|

							*SwapExists*
SwapExists			开始编辑文件时检测到现有的交换文件。
				仅在可以选择处理方式时，即 Vim
				会询问用户该做什么时。
				|v:swapname| 变量包含找到的交换文件的名称，
				<afile> 是被编辑的文件。
				|v:swapcommand| 可能包含要在打开的文件中
				执行的命令。
				命令应设置 |v:swapchoice| 变量为
				一个单字符字符串，告诉 Vim 接下来该做什么：
					'o'	以只读方式打开
					'e'	仍然编辑该文件
					'r'	恢复
					'd'	删除交换文件
					'q'	退出，不编辑该文件
					'a'	中止，类似于按 CTRL-C
				当设置为空字符串时，将询问用户，
				就像没有 SwapExists 自动命令一样。
							*E812*
				不能切换到另一个缓冲区、更改缓冲区名称
				或更改目录。

							*Syntax*
Syntax				当 'syntax' 选项被设置时。模式与语法名称
				匹配。<afile> 展开为设置此选项的文件的
				名称。<amatch> 展开为 'syntax' 的新值。
				参见 |:syn-on|。

							*TabClosed*
TabClosed			关闭标签页之后。<afile> 展开为已关闭的
				|tabpage-number|。

							*TabClosedPre*
TabClosedPre			关闭标签页之前。当前标签页
				（|tabpagenr()|）是即将关闭的那个。窗口布局
				被锁定，因此禁止打开和关闭窗口。

							*TabEnter*
TabEnter			刚进入 |tabpage| 之后。
				在 WinEnter 之后。
				在 BufEnter 之前。

							*TabLeave*
TabLeave			即将离开 |tabpage| 之前。
				在 WinLeave 之后。

							*TabMoved*
TabMoved			移动 |tabpage| 之后（通过 |:tabmove| 或
				鼠标拖动）。<amatch> 展开为移动前的旧
				|tabpage-number|。

				|event-data| 包含以下键（类型：`vim.event.tabmoved.data`）：
				- tabnr_old：移动前的 |tabpage-number|。
				- tabnr_new：移动后的 |tabpage-number|。

							*TabNew*
TabNew				创建新的 |tabpage| 时。
				在 WinEnter 之后。
				在 TabEnter 之前。

							*TabNewEntered*
TabNewEntered			进入新的 |tabpage| 之后。
				在 BufEnter 之后。

							*TermClose*
TermClose			当 |terminal| 作业结束时。
				设置这些 |v:event| 键：
				    status	作业退出状态，如果终端缓冲区
						被删除则为 -1

							*TermEnter*
TermEnter			进入 |Terminal-mode| 之后。
				在 TermOpen 之后。

							*TermLeave*
TermLeave			离开 |Terminal-mode| 之后。
				在 TermClose 之后。

							*TermOpen*
TermOpen			当 |terminal| 作业启动时。可用于
				配置终端缓冲区。要获取终端进程详细信息： >lua
				    vim.print(vim.api.nvim_get_chan_info(vim.bo.channel))
<
							*TermRequest*
TermRequest			当 |:terminal| 子进程发出 OSC、DCS 或 APC
				序列时。设置 |v:termrequest|。

				|event-data| 包含以下键（类型：`vim.event.termrequest.data`）：
				- sequence：接收到的序列
				- terminator：接收到的序列终止符（即 BEL 或 ST）
				- cursor：接收到序列时光标的位置
				  （(1,0)-索引，相对于缓冲区）
				  （如果位置不再存在于缓冲区中，行号可能 <= 0）

				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。

							*TermResponse*
TermResponse			当 Nvim 从宿主终端接收到 DA1、OSC、DCS 或 APC
				响应时。设置 |v:termresponse|。

				|event-data| 包含以下键（类型：`vim.event.termresponse.data`）：
				- chan：源通道 id
				- sequence：接收到的序列

				即使在未使用 |autocmd-nested| 定义的
				自动命令内部也会触发。

				可能在另一个事件（文件 I/O、shell 命令或
				任何其他耗时操作）期间触发。

				示例： >lua

				-- 使用 OSC 4 查询终端调色板中颜色 1（红色）的 RGB 值
				vim.api.nvim_create_autocmd('TermResponse', {
				  once = true,
				  callback = function(ev)
				    local resp = ev.data.sequence
				    local r, g, b = resp:match("\027%]4;1;rgb:(%w+)/(%w+)/(%w+)")
				  end,
				})
				vim.api.nvim_ui_send("\027]4;1;?\027\\")
<
							*TextChanged*
TextChanged			在普通模式下当前缓冲区中的文本发生更改之后。
				即在 |b:changedtick| 更改之后（即使这发生在
				TextChanged 自动命令定义之前）。
				当存在 typeahead 或操作符挂起时
				不触发。
				注意：不能使用 `:noautocmd` 跳过。
				注意：此事件触发非常频繁，不要
				执行用户不期望或速度慢的操作。

							*TextChangedI*
TextChangedI			在插入模式下当前缓冲区中的文本发生更改之后。
				当弹出菜单可见时不触发。
				其他方面与 TextChanged 相同。

							*TextChangedP*
TextChangedP			在插入模式下当前缓冲区中的文本发生更改之后，
				仅当弹出菜单可见时。其他方面与
				TextChanged 相同。

							*TextChangedT*
TextChangedT			在 |Terminal-mode| 下当前缓冲区中的文本发生更改
				之后。其他方面与 TextChanged 相同。

							*TextPutPost*
TextPutPost			文本被放入当前缓冲区之后。
				|v:event| 中的以下值大多与 |TextYankPost|
				相同：
				   operator	执行的操作，
						'p' 或 'P'。
				   regcontents	被放置的文本。对于
						|quote_=|，这是表达式
						的结果。
				   regname	寄存器名称，对于未命名
						寄存器则为空字符串。
						对于 |nvim_put()|，
						此值设置为 '_'。
				   regtype	寄存器的类型，参见
						|getregtype()|。
				   visual	如果操作在 |Visual| 模式下
						执行，则为 True。
				当使用 |quote_| 或递归调用时不触发。
				不允许更改缓冲区文本，
				参见 |textlock|。
				注意，对于 |quote_.| 寄存器，由于
				最后插入的文本被缓冲到输入缓冲区中
				（缓冲区未被直接修改），
				此自动命令直接在 |TextPutPre| 之后调用。

							*TextPutPre*
TextPutPre			文本被放入当前缓冲区之前。
				|v:event| 中的值与 |TextPutPost| 相同。
				在此自动命令中调用 |setreg()| 是有效的，
				允许您在 "regcontents" 中的文本被放置之前
				处理并修改它。但是这不适用于 |quote_#|、
				|quote_=|、|quote_%|、|quote_:|、|quote_/|
				或 |quote_.|。
				当使用 |quote_| 或递归调用时不触发。
				不允许更改缓冲区文本，
				参见 |textlock|。

							*TextYankPost*
TextYankPost			在 |yank| 或 |deleting| 命令之后，但如果使用
				黑洞寄存器 |quote_| 或用于 |setreg()| 则不触发。
				模式必须为 "*"。
				设置这些 |v:event| 键：
				    inclusive
				    operator
				    regcontents
				    regname
				    regtype
				    visual
				`inclusive` 标志与 |'[| 和 |']| 标记结合使用，
				可以计算操作的精确区域。

				非递归（事件不能触发自身）。
				无法更改文本。 |textlock|

							*UIEnter*
UIEnter				UI 通过 |nvim_ui_attach()| 连接之后，或在
				内置 TUI 启动之后，在 |VimEnter| 之后。
				设置这些 |v:event| 键：
				    chan：UI 的 |channel-id|

							*UILeave*
UILeave				UI 从 Nvim 断开连接之后，或在内置 TUI
				停止之后，在 |VimLeave| 之后。
				设置这些 |v:event| 键：
				    chan：UI 的 |channel-id|

							*User*
User				不自动执行。使用 |:doautocmd| 触发此事件，
				通常用于插件中的 "自定义事件"。示例： >
				    :autocmd User MyPlugin echom 'got MyPlugin event'
				    :doautocmd User MyPlugin

<							*UserGettingBored*
UserGettingBored		当用户按同一键 42 次时。
				开玩笑！:-)

							*VimEnter*
VimEnter			完成所有启动工作之后，包括加载 vimrc
				文件、执行 "-c cmd" 参数、创建所有窗口
				并在其中加载缓冲区。
				在此事件触发之前，|v:vim_did_enter| 变量
				被设置，因此您可以执行： >
				    if v:vim_did_enter
				      call s:init()
				    else
				      au VimEnter * call s:init()
				    endif

<							*VimLeave*
VimLeave			退出 Vim 之前，在写入 .shada 文件之后。
				仅执行一次，类似于 VimLeavePre。
				使用 |v:dying| 检测异常退出。
				使用 |v:exiting| 获取退出代码。
				如果 |v:dying| 为 2 或更高，则不触发。

							*VimLeavePre*
VimLeavePre			退出 Vim 之前，在写入 |shada| 文件之前。
				仅执行一次，如果模式在退出时匹配当前缓冲区。
				通常与 "*" 模式一起使用。 >
				   :autocmd VimLeavePre * call CleanupStuff()
<				使用 |v:dying| 检测异常退出。
				使用 |v:exiting| 获取退出代码。
				如果 |v:dying| 为 2 或更高，则不触发。

							*VimResized*
VimResized			Nvim 自身调整大小之后：'lines' 和/或
				'columns' 更改时。但不在 |startup| 期间。

				为确保窗口布局在调整大小时始终
				"均衡"： >
				    :autocmd VimResized * wincmd =
<
							*VimResume*
VimResume			Nvim 从 |suspend| 状态恢复之后。

							*VimSuspend*
VimSuspend			Nvim 进入 |suspend| 状态之前。

							*WinClosed*
WinClosed			关闭窗口时，在将其从窗口布局中移除
				之前。模式与 |window-ID| 匹配。
				<amatch> 和 <afile> 都设置为 |window-ID|。
				在 WinLeave 之后。
				非递归（事件不能触发自身）。
				另请参见 |ExitPre|、|QuitPre|。

							*WinEnter*
WinEnter			进入另一个窗口之后。对于 Vim 刚启动时
				的第一个窗口不执行。
				对于设置窗口高度很有用。
				如果该窗口用于另一个缓冲区，Vim
				会在 WinEnter 自动命令之后执行 BufEnter
				自动命令。
				注意：对于 split 和 tabpage 命令，
				WinEnter 事件在 split 或 tab 命令之后、
				但在文件加载之前触发。

							*WinLeave*
WinLeave			离开窗口之前。如果下一个要进入的
				窗口用于不同的缓冲区，Vim
				在 WinLeave 自动命令之前执行 BufLeave
				自动命令（但不适用于 ":new"）。
				退出 Vim 时，不用于 ":qa" 或 ":q"。
				在 WinClosed 之前。

							*WinNewPre*
WinNewPre			创建新窗口之前。在通过创建分割来修改
				窗口布局的命令之前触发。
				创建标签页和第一个窗口时不执行，
				因为窗口结构尚未初始化，因此通常
				不安全。
				在执行 WinNewPre 事件的命令时，
				不允许修改窗口布局。
				最常用于存储当前窗口布局，并在
				窗口创建后与新布局进行比较。

							*WinNew*
WinNew				新窗口创建时。对于 Vim 刚启动时的
				第一个窗口不执行。
				在 WinEnter 之前。

							*WinResized*
WinResized			当前标签页中的窗口更改宽度或高度
				之后。
				参见 |win-scrolled-resized|。

				|v:event| 设置了有关大小更改的信息。
				|WinResized-event|

				与 |WinScrolled| 在模式、触发和递归性方面
				具有相同的行为。

							*WinScrolled*
WinScrolled			当前标签页中的任何窗口滚动文本
				（水平或垂直）或更改宽度或高度之后。
				参见 |win-scrolled-resized|。

				注意：这不能使用 `:noautocmd` 跳过，
				因为它在处理普通命令之后、Vim 回到
				主循环时触发。如果您想禁用此功能，
				请考虑设置 'eventignore' 选项。

				模式与第一个滚动或调整大小的窗口的
				|window-ID| 匹配。
				<amatch> 和 <afile> 都设置为 |window-ID|。

				|v:event| 设置了有关大小和滚动更改的信息。
				|WinScrolled-event|

				仅在启动完成且第一次屏幕重绘完成后
				开始触发。
				定义第一个 WinScrolled 或 WinResized 事件时
				不触发，但添加更多事件时可能触发。

				非递归：在执行 WinScrolled 事件的命令期间，
				该事件不会触发。但是，如果命令导致
				窗口滚动或更改大小，则稍后将触发另一个
				WinScrolled 事件。

==============================================================================
6. 模式					*autocmd-pattern* *{aupat}*

`:autocmd` 的 {aupat} 参数可以是一个逗号分隔的列表。其工作方式
如同分别对每个模式给出命令。因此此命令： >
	:autocmd BufRead *.txt,*.info set et
等同于： >
	:autocmd BufRead *.txt set et
	:autocmd BufRead *.info set et

文件模式 {aupat} 通过以下两种方式之一与文件名进行匹配测试：
1. 当模式中没有 '/' 时，Vim 仅检查与文件名的尾随部分
   （不带前导目录路径）的匹配。
2. 当模式中有 '/' 时，Vim 检查与短文件名（按您键入的）
   和完整文件名（展开为完整路径并解析符号链接后）的匹配。

特殊模式 <buffer> 或 <buffer=N> 用于缓冲区本地
自动命令 |autocmd-buflocal|。此模式不与缓冲区的名称
匹配。

示例： >
	:autocmd BufRead *.txt		set et
为所有文本文件设置 'et' 选项。 >

	:autocmd BufRead /vim/src/*.c	set cindent
为 /vim/src 目录中的 C 文件设置 'cindent' 选项。 >

	:autocmd BufRead /tmp/*.c	set ts=5
如果您有一个从 "/tmp/test.c" 到 "/home/nobody/vim/src/test.c" 的链接，
并且您开始编辑 "/tmp/test.c"，此自动命令将匹配。

注意：要匹配路径的一部分，但不是从根目录开始，请使用 "*" 作为
第一个字符。示例： >
	:autocmd BufRead */doc/*.txt	set tw=78
例如，此自动命令将针对 "/tmp/doc/xx.txt" 和
"/usr/home/piet/doc/yy.txt" 执行。目录数量在此无关紧要。


模式匹配的文件名是在展开通配符之后进行的。因此，如果您发出此命令： >
	:e $ROOTDIR/main.$EXT
参数首先被展开为： >
	/usr/root/main.py
然后才与自动命令的模式进行匹配。在使用 FileReadCmd 等事件时，
请注意这一点，<amatch> 的值可能不是您所期望的。


模式中可以使用环境变量： >
	:autocmd BufRead $VIMRUNTIME/doc/*.txt  set expandtab
并且 ~ 可用于主目录（如果定义了 $HOME）： >
	:autocmd BufWritePost ~/.config/nvim/init.vim   so <afile>
	:autocmd BufRead ~archive/*      set readonly
环境变量在定义自动命令时展开，而不是在
执行自动命令时展开。这与命令不同！

							*file-pattern*
模式的解释方式与文件名中的大多数用法类似：
	*	匹配任何字符序列；异常：包含路径分隔符
	?	匹配任何单个字符
	\?	匹配 '?'
	.	匹配 '.'
	~	匹配 '~'
	,	分隔模式
	\,	匹配 ','
	{ }	类似于 |pattern| 中的 \( \)
	,	在 { } 内部：类似于 |pattern| 中的 \|
	\}	字面量 }
	\{	字面量 {
	\\\{n,m\} 类似于 |pattern| 中的 \{n,m}
	\	特殊含义，类似于 |pattern| 中的
	[ch]	匹配 'c' 或 'h'
	[^ch]   匹配除 'c' 和 'h' 之外的任何字符

注意，在所有系统中，'/' 字符都用作路径分隔符（即使
在 MS-Windows 上）。这样做是因为反斜杠在模式中难以使用，
并且为了使自动命令在不同系统之间可移植。要仅在所有平台上匹配
'/'（例如，在非文件模式中），请使用 "\/"。

可以使用 |pattern| 项，但由于上述转换，
它们可能无法按预期工作。

							*autocmd-changes*
在触发事件时进行模式匹配。在其中一个自动命令中更改
缓冲区名称，甚至删除缓冲区，都不会改变
将要执行的自动命令。示例： >

	au BufEnter *.foo  bdel
	au BufEnter *.foo  set modified

这将删除当前缓冲区，然后在已成为当前缓冲区的缓冲区中
设置 'modified'。Vim 不考虑 "*.foo" 不再匹配该
缓冲区名称。它使用触发事件时缓冲区的名称
来匹配 "*.foo"。

但是，对于已使用 |:bwipe| 清除的缓冲区，缓冲区本地自动命令
不会执行。使用 |:bdel| 删除缓冲区后，缓冲区
实际上仍然存在（它变为未列出），因此自动命令仍会
执行。

==============================================================================
7. 缓冲区本地自动命令	*autocmd-buflocal* *autocmd-buffer-local*
				*<buffer>* *<buffer=N>* *<buffer=abuf>* *E680*

缓冲区本地自动命令附加到特定缓冲区。如果缓冲区没有
名称，或者名称不匹配特定模式，它们很有用。
但这同时也意味着它们必须显式地添加到每个缓冲区。

缓冲区本地自动命令使用以下形式之一来代替模式：
	<buffer>	当前缓冲区
	<buffer=99>	缓冲区编号 99
	<buffer=abuf>	使用 <abuf>（仅在执行自动命令时）
			|<abuf>|

示例： >
    :au CursorHold <buffer>  echo 'hold'
    :au CursorHold <buffer=33>  echo 'hold'
    :au BufNewFile * au CursorHold <buffer=abuf>  echo 'hold'

所有用于自动命令的命令也适用于缓冲区本地自动命令，
只需使用特殊字符串代替模式即可。示例： >
    :au! * <buffer>		      " 移除当前缓冲区的缓冲区本地
				      " 自动命令
    :au! * <buffer=33>		      " 移除缓冲区 #33 的缓冲区本地
				      " 自动命令
    :bufdo :au! CursorHold <buffer>   " 为所有缓冲区移除给定事件的
				      " 自动命令
    :au * <buffer>		      " 列出当前缓冲区的缓冲区本地
				      " 自动命令

注意，当为当前缓冲区定义自动命令时，它会被存储
在缓冲区编号下。因此它使用 "<buffer=12>" 形式，其中 12 是
当前缓冲区的编号。例如，您在列出自动命令时会看到这一点。

要测试缓冲区本地自动命令是否存在，请使用 |exists()| 函数，
如下所示： >
    :if exists("#CursorHold#<buffer=12>") | ... | endif
    :if exists("#CursorHold#<buffer>") | ... | endif    " 针对当前缓冲区

当缓冲区被清除时，其缓冲区本地自动命令当然也会
消失。注意，当删除缓冲区时（例如使用 ":bdel"），它只是被
取消列出，自动命令仍然存在。要查看缓冲区本地自动命令的
移除情况： >
    :set verbose=6

无法为不存在的缓冲区定义缓冲区本地自动命令。

==============================================================================
8. 组						*autocmd-groups*

自动命令可以组合在一个组中。这对于移除或执行
一组自动命令很有用。例如，所有用于语法高亮的自动命令
都放在 "highlight" 组中，以便在 GUI 启动时能够执行
":doautoall highlight BufRead"。

当未选择特定组时，Vim 使用默认组。默认组没有
名称。您不能单独执行默认组中的自动命令；您只能
通过执行所有组的自动命令来执行它们。

通常，当自动执行自动命令时，Vim 会使用所有组的
自动命令。组仅在您使用 ":doautocmd" 或 ":doautoall" 执行
自动命令，或定义或删除自动命令时才重要。

组名可以包含除空格之外的任何字符。组名 "end"
是保留的（大写形式也保留）。

组名区分大小写。注意，这与事件名称不同！

								*:aug* *:augroup*
:aug[roup] {name}		为后续的 ":autocmd" 命令定义自动命令组
				名称。名称 "end" 或 "END" 选择默认组。
				为避免混淆，名称应与现有的 {event} 名称
				不同，因为这很可能不会达到您的预期。

					*:augroup-delete* *E367* *W19* *E936*
:aug[roup]! {name}		删除自动命令组 {name}。如果仍有自动命令
				使用此组，请不要使用此命令！如果
				您执意要这样做，您将收到警告。当该组是
				当前组时，您将收到错误 E936。

要为特定组输入自动命令，请使用此方法：
1. 使用 ":augroup {name}" 选择组。
2. 使用 ":au!" 删除任何旧的自动命令。
3. 定义自动命令。
4. 使用 "augroup END" 返回到默认组。

示例： >
	:augroup uncompress
	:  au!
	:  au BufEnter *.gz	%!gunzip
	:augroup END

这可以防止自动命令被定义两次（例如，在再次加载
vimrc 文件之后）。

						*FileExplorer*
有一个 Vim 识别的组：FileExplorer。如果此组存在，
Vim 假定可以编辑目录，并将触发列出该目录中文件的
插件。这由目录浏览器插件使用。这允许您执行： >
	browse edit

==============================================================================
9. 执行自动命令				*autocmd-execute*

Vim 也可以非自动地执行自动命令。如果您更改了自动命令，
或者 Vim 执行了错误的自动命令（例如，文件模式匹配错误），
这很有用。

注意，'eventignore' 选项也适用于此处。此选项中列出的事件
不会导致执行任何命令。

				*:do* *:doau* *:doaut* *:doautocmd* *E217*
:do[autocmd] [<nomodeline>] [group] {event} [fname]
			将匹配 [fname]（默认：当前文件名）的自动命令
			应用于当前缓冲区，用于 {event}。
			当当前文件名不匹配正确的模式时，在更改设置
			之后，或为某个事件执行自动命令时，您可以使用此命令。
			也可以在自动命令内部使用它，
			因此您可以将一个扩展名的自动命令基于另一个
			扩展名。示例： >
				:au BufEnter *.cpp so ~/.config/nvim/init_cpp.vim
				:au BufEnter *.cpp doau BufEnter x.c
<			请注意避免无限循环。 |autocmd-nested|

			当未给出 [group] 参数时，Vim 执行所有组的
			自动命令。当包含 [group] 参数时，Vim 仅执行
			该组的匹配自动命令。未定义的组是一个错误。
							*<nomodeline>*
			应用自动命令之后，处理 modeline，
			以便它们的设置在对文件进行编辑时覆盖自动命令
			的设置。如果指定了 <nomodeline>，则跳过此步骤。
			对于不用于加载缓冲区的事件（例如 |User|），
			您可能希望使用 <nomodeline>。
			当没有执行匹配的自动命令时，也会跳过
			Modeline。

						*:doautoa* *:doautoall*
:doautoa[ll] [<nomodeline>] [group] {event} [fname]
			类似于 ":doautocmd"，但将自动命令应用于每个
			已加载的缓冲区。当前缓冲区最后执行。

			注意，[fname] 用于选择自动命令，
			而不是选择它们应用于的缓冲区。示例： >
				augroup mine
				  autocmd!
				  autocmd FileType * echo expand('<amatch>')
				augroup END
				doautoall mine FileType Loaded-Buffer
<			加载此脚本后，您将看到与已加载缓冲区数量
			相同的 "Loaded-Buffer" 回显。

			注意：不要将此用于删除缓冲区、切换到另一个缓冲区
			或更改缓冲区内容的自动命令；结果不可预测。
			此命令用于设置选项、更改高亮显示等
			自动命令。

==============================================================================
10. 使用自动命令					*autocmd-use*

对于写入文件，有四种可能的事件集。对于写入命令，
Vim 仅使用这些事件集之一：

BufWriteCmd	BufWritePre	BufWritePost	写入整个缓冲区
		FilterWritePre	FilterWritePost	写入过滤器临时文件
FileAppendCmd	FileAppendPre	FileAppendPost	追加到文件
FileWriteCmd	FileWritePre	FileWritePost	任何其他文件写入

当存在匹配的 "*Cmd" 自动命令时，假定它将执行写入。
不再进行进一步写入，也不会触发其他事件。
|Cmd-event|

注意，"*WritePost" 命令应撤消 "*WritePre" 命令对缓冲区
造成的任何更改；否则，写入文件将产生更改缓冲区的
副作用。

在执行自动命令之前，要写入其行的缓冲区临时成为
当前缓冲区。除非自动命令更改当前缓冲区或删除
先前的当前缓冲区，否则先前的当前缓冲区将再次成为
当前缓冲区。

"*WritePre" 和 "*AppendPre" 自动命令不得删除要写入
其行的缓冲区。

'[ 和 '] 标记具有特殊位置：
- 在 "*ReadPre" 事件之前，'[ 标记设置在新行将要插入的
  位置之上的一行。
- 在 "*ReadPost" 事件之前，'[ 标记设置在刚读取的第一行，
  '] 标记设置在最后一行。
- 在执行 "*WriteCmd"、"*WritePre" 和 "*AppendPre" 自动命令之前，'[
  标记设置在将要写入的第一行，'] 标记设置在最后一行。
注意：当使用更改缓冲区的命令时，'[ 和 '] 会更改。

在需要文件名的命令中，您可以使用 "<afile>" 来表示正在读取的
文件名 |:<afile>|（您也可以使用 "%" 表示当前文件
名）。"<abuf>" 可用于当前有效缓冲区的缓冲区编号。
这也适用于没有名称的缓冲区。但它不适用于没有缓冲区的文件
（例如，使用 ":r file"）。

							*gzip-example*
读取和写入压缩文件的示例： >
  :augroup gzip
  :  autocmd!
  :  autocmd BufReadPre,FileReadPre	*.gz set bin
  :  autocmd BufReadPost,FileReadPost	*.gz '[,']!gunzip
  :  autocmd BufReadPost,FileReadPost	*.gz set nobin
  :  autocmd BufReadPost,FileReadPost	*.gz execute ":doautocmd BufReadPost " .. expand("%:r")
  :  autocmd BufWritePost,FileWritePost	*.gz !mv <afile> <afile>:r
  :  autocmd BufWritePost,FileWritePost	*.gz !gzip <afile>:r

  :  autocmd FileAppendPre		*.gz !gunzip <afile>
  :  autocmd FileAppendPre		*.gz !mv <afile>:r <afile>
  :  autocmd FileAppendPost		*.gz !mv <afile> <afile>:r
  :  autocmd FileAppendPost		*.gz !gzip <afile>:r
  :augroup END

使用 "gzip" 组是为了在文件被加载两次时，能够使用
":autocmd!" 删除任何现有的自动命令。

（"<afile>:r" 是不带扩展名的文件名，参见 |:_%:|）

为 BufNewFile、BufRead/BufReadPost、BufWritePost、
FileAppendPost 和 VimLeave 事件执行的命令不会设置或重置缓冲区的
更改标志。当您使用 BufReadPost 自动命令解压缩缓冲区时，
您仍然可以使用 ":q" 退出。当您在 BufWritePost 中使用 ":undo"
来撤消 BufWritePre 命令所做的更改时，您仍然可以执行 ":q"
（这也使 "ZZ" 起作用）。如果您确实希望将缓冲区标记为已修改，
请设置 'modified' 选项。

要从自动命令执行普通模式命令，请使用 ":normal" 命令。
请小心使用！如果普通模式命令未完成，用户需要
键入字符（例如，在 ":normal m" 之后，您需要键入一个标记
名称）。

如果您希望在更改缓冲区后使其处于未修改状态，请重置
'modified' 选项。这允许您使用 ":q" 而不是 ":q!" 退出缓冲区。

							*autocmd-nested* *E218*
默认情况下，自动命令不嵌套。例如，如果您在自动命令中
使用 ":e" 或 ":w"，Vim 不会为这些命令执行 BufRead 和
BufWrite 自动命令。如果您确实希望这样做，请在您希望嵌套的
命令中使用 "++nested" 标志。例如： >
  :autocmd FileChangedShell *.c ++nested e!
嵌套限制为 10 层，以摆脱递归循环。

可以在自动命令中使用 ":au" 命令。这可以是一个
自修改命令！这对于只应执行一次的自动命令
很有用。

如果您想为一个命令跳过自动命令，请使用 |:noautocmd| 命令
修饰符或 'eventignore' 选项。

注意：当读取文件（使用 ":read file" 或过滤器命令）且文件中
的最后一行没有 <EOL> 时，Vim 会记住这一点。在下次写入
（使用 ":write file" 或过滤器命令）时，如果同一行再次作为文件中的
最后一行被写入且 'binary' 被设置，Vim 不会提供 <EOL>。
这使得对刚读取的行执行的过滤器命令写入与读取的文件
相同的文件，并使对刚过滤的行执行的写入命令写入
与从过滤器读取的文件相同的文件。例如，写入压缩文件的
另一种方式： >

  :autocmd FileWritePre *.gz   set bin|'[,']!gzip
  :autocmd FileWritePost *.gz  undo|set nobin
<
							*autocommand-pattern*
您可以指定多个模式，用逗号分隔。以下是一些示例： >

  :autocmd BufRead   *		set tw=79 nocin ic infercase fo=2croq
  :autocmd BufRead   .letter	set tw=72 fo=2tcrq
  :autocmd BufEnter  .letter	set dict=/usr/lib/dict/words
  :autocmd BufLeave  .letter	set dict=
  :autocmd BufRead,BufNewFile   *.c,*.h	set tw=0 cin noic
  :autocmd BufEnter  *.c,*.h	abbr FOR for (i = 0; i < 3; ++i)<CR>{<CR>}<Esc>O
  :autocmd BufLeave  *.c,*.h	unabbr FOR

对于 makefile（makefile、Makefile、imakefile、makefile.unix 等）： >

  :autocmd BufEnter  ?akefile*	set include=^s\=include
  :autocmd BufLeave  ?akefile*	set include&

要始终从第一个函数开始编辑 C 文件： >

  :autocmd BufRead   *.c,*.h	1;/^{

如果没有上面的 "1;"，搜索将从进入文件的任何位置开始，
而不是从文件的开头开始。

						*skeleton* *template*
在打开新文件时读取骨架文件： >

  :autocmd BufNewFile  *.c	0r ~/vim/skeleton.c
  :autocmd BufNewFile  *.h	0r ~/vim/skeleton.h
  :autocmd BufNewFile  *.java	0r ~/vim/skeleton.java

在写入 "*.html" 文件时插入当前日期和时间： >

  :autocmd BufWritePre,FileWritePre *.html   ks|call LastMod()|'s
  :fun LastMod()
  :  if line("$") > 20
  :    let l = 20
  :  else
  :    let l = line("$")
  :  endif
  :  exe "1," .. l .. "g/Last modified: /s/Last modified: .*/Last modified: " ..
  :  \ strftime("%Y %b %d")
  :endfun

您需要在文件的前 20 行中有一行 "Last modified: <date time>"，
此功能才能正常工作。Vim 将 <date time>（以及同一行中它后面的
任何内容）替换为当前日期和时间。解释：
	ks		用标记 's' 标记当前位置
	call LastMod()	调用 LastMod() 函数来完成工作
	's		将光标返回到旧位置
LastMod() 函数检查文件是否短于 20 行，然后
使用 ":g" 命令查找包含 "Last modified: " 的行。对于这些
行，执行 ":s" 命令以将现有日期替换为当前日期。
":execute" 命令用于能够对 ":g" 和 ":s" 命令使用表达式。
日期通过 strftime() 函数获取。您可以更改其参数以获取
其他日期字符串。

在命令行中输入 :autocmd 时，可以在适当的情况下完成事件和
命令名称的补全（使用 <Tab>、CTRL-D 等）。

Vim 按照您指定的顺序执行所有匹配的自动命令。
建议您的第一个自动命令通过使用 "*" 作为文件模式来
应用于所有文件。这意味着您可以在此处为任何设置定义
您喜欢的默认值，如果有另一个匹配的自动命令，它将覆盖
这些默认值。但如果没有其他匹配的自动命令，那么至少
您的默认设置会被恢复（如果从另一个匹配自动命令的
文件进入此文件）。注意，"*" 也会匹配以 "." 开头的文件，
这与 Unix shell 不同。

						    *autocmd-searchpat*
自动命令不会更改当前的搜索模式。Vim 在执行自动命令之前
保存当前搜索模式，然后在自动命令完成后恢复它们。
这意味着自动命令不会影响使用 'hlsearch' 选项高亮显示的
字符串。在自动命令内部，您仍然可以正常使用搜索模式，
例如使用 "n" 命令。
如果您希望自动命令设置搜索模式，以便在自动命令
完成后使用它，请使用 ":let @/ =" 命令。
在自动命令中不能使用 ":nohlsearch" 关闭搜索高亮显示。
使用 'shada' 选项中的 'h' 标志可在启动 Vim 时禁用搜索
高亮显示。

							*Cmd-event*
当使用 "*Cmd" 事件之一时，匹配的自动命令应执行
文件读取、写入或加载。这可用于处理特殊类型的文件，
例如在远程系统上。
注意：如果您错误地使用这些事件，可能会导致无法
读取或写入匹配的文件！请确保正确测试您的
自动命令。最好使用一个永远不会匹配普通文件名的模式，
例如 "ftp://*"。

当定义 BufReadCmd 时，Vim 将很难恢复崩溃的编辑会话。
当从原始文件恢复时，Vim 只读取文件中未在
交换文件中找到的部分。由于使用 BufReadCmd 无法做到这一点，
请使用 |:preserve| 命令确保原始文件不需要用于恢复。
您可能只想在预期文件会被修改时才这样做。

对于文件读取和写入命令，|v:cmdarg| 变量保存有效的
"++enc=" 和 "++ff=" 参数。这些应用于读取/写入文件的
命令。|v:cmdbang| 变量在使用 "!" 时为 1，否则为 0。

示例请参见 $VIMRUNTIME/plugin/net.lua。

==============================================================================
11. 禁用自动命令				*autocmd-disable*

要在一段时间内禁用自动命令，请使用 'eventignore' 选项。
注意，这可能导致意外行为，请确保之后使用 |:try| 块
和 |:finally| 恢复 'eventignore'。

要在特定窗口中无限期禁用自动命令，请使用
'eventignorewin' 选项。这只能用于忽略与窗口和缓冲区
相关的事件。

							*:noautocmd* *:noa*
要仅为一条命令禁用自动命令，请使用 ":noautocmd" 命令
修饰符。这将在后续命令期间将 'eventignore' 设置为 "all"。
示例： >

	:noautocmd w fname.gz

这将写入文件，而不会触发 gzip 插件定义的自动命令。

注意，某些自动命令不会立即触发，而只会稍后触发。
这特别适用于 |CursorMoved| 和 |TextChanged|。


 vim:tw=78:ts=8:noet:ft=help:norl:
