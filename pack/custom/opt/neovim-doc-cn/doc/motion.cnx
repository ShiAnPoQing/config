*motion.txt*    Nvim


		  VIM 参考手册	  by Bram Moolenaar


光标移动					*cursor-motions* *navigation*

这些命令用于移动光标位置。如果新位置移出屏幕，屏幕会滚动以显示光标
（另请参见 'scrolljump' 和 'scrolloff' 选项）。

一般说明：

如果您想知道自己在文件中的位置，请使用 "CTRL-G" 命令 |CTRL-G| 或 "g CTRL-G" 命令 |g_CTRL-G|。
如果设置了 'ruler' 选项，光标位置会持续显示在状态行中（这会使 Vim 稍微变慢）。

可以设置 'virtualedit' 选项，使光标可以移动到没有字符的位置或多列字符（如制表符）内。

                                      输入 |gO| 查看目录。

==============================================================================
1. 移动和操作符				*operator*

移动命令可以在操作符命令之后使用，使操作符作用于移动经过的文本。
即光标在移动前和移动后位置之间的文本。
操作符通常用于删除或更改文本。
以下操作符可用：

	|c|	c	更改
	|d|	d	删除
	|y|	y	抽出到寄存器（不更改文本）
	|~|	~	大小写互换（仅当设置了 'tildeop' 时）
	|g~|	g~	大小写互换
	|gu|	gu	转换为小写
	|gU|	gU	转换为大写
	|!|	!	通过外部程序过滤
	|=|	=	通过 'equalprg' 过滤，如果为空则进行 C 缩进
	|gq|	gq	文本格式化
	|gw|	gw	文本格式化，光标不移动
	|g?|	g?	ROT13 编码
	|>|	>	向右缩进
	|<|	<	向左缩进
	|zf|	zf	定义一个折叠
	|g@|	g@	调用通过 'operatorfunc' 选项设置的函数
						*motion-count-multiplied*
如果移动包含一个计数，并且操作符之前也有一个计数，则两个计数相乘。
例如："2d3w" 删除六个单词。
						*operator-doubled*
当操作符重复两次时，它作用于一行。
如果使用计数（在第一个字符之前或之后），则作用于那么多行。
因此 `3dd` 删除三行。
第一个字符之前和之后的计数相乘，因此 `2y3y` 抽出六行。
						*operator-resulting-pos*
应用操作符后，光标通常位于被操作文本的开头。
例如，"yfe" 不会移动光标，但 "yFe" 将光标向左移动到抽出开始的 "e" 处。
'startofline' 选项仅适用于 "d"、"<<", "==" 和 ">>" 这些行操作。

						*linewise* *charwise* *characterwise*
操作符要么影响整行，要么影响开始和结束位置之间的字符。
通常，在行间移动的移动是行操作（linewise），而在行内移动的移动是字符操作（charwise）。
但是，也有一些例外。

						*exclusive* *inclusive*
字符移动要么是排他性的（exclusive），要么是包含性的（inclusive）。
当是包含性时，移动的开始和结束位置都包含在操作中。
当是排他性时，靠近缓冲区末尾的最后一个字符不包含在内。行操作始终包含开始和结束位置。
插件可以检查 |TextYankPost| 事件的 v:event.inclusive 标志。

哪些移动是行操作、包含性或排他性，会在命令说明中提及。但是，有两个一般性例外：
1. 如果移动是排他性的，并且移动的终点在第 1 列，则移动的终点移动到前一行的末尾，并且该移动变为包含性。
示例："}" 移动到段落后的第一行，但 "d}" 不会包含该行。
						*exclusive-linewise*
2. 如果移动是排他性的，移动的终点在第 1 列，并且移动的起点在行中第一个非空白字符处或之前，则该移动变为行操作。
示例：如果一个段落以一些空白开始，并且您在第一个非空白字符处执行 "d}"，则该段落的所有行都将被删除，包括空白。
如果您现在执行粘贴，被删除的行将插入到光标位置下方。

请注意，当操作符挂起时（已输入操作符命令，但尚未输入移动），可以使用一组特殊的映射。请参阅 |:omap|。

除了先给出操作符然后给出移动之外，您还可以使用可视模式：
用 "v" 标记文本的开头，将光标移动到要操作的文本的末尾，然后按操作符键。
开始和光标位置之间的文本会被高亮显示，因此您可以看到将要操作哪些文本。
这提供了更大的自由度，但需要更多的按键，并且重做功能有限。请参阅有关可视模式的章节 |Visual-mode|。

您可以使用 ":" 命令作为移动。例如 "d:call FindEnd()"。但如果命令超过一行，则无法用 "." 重复。这可以重复： >
	d:call search("f")<CR>
这不能重复： >
	d:if 1<CR>
	   call search("f")<CR>
	endif<CR>
请注意，使用 ":" 时，任何移动都会变为字符操作且排他性。

					*inclusive-motion-selection-exclusive*
当 'selection' 为 "exclusive" 时， |Visual| 模式处于活动状态，并且已使用包含性移动，
光标位置将向右调整一个字符，以便可视选择包含预期的文本并可以对其执行操作。

								*forced-motion*
强制移动为行操作、字符操作或块操作

如果移动不是您想要使用的类型，您可以在操作符后立即使用 "v"、"V" 或 CTRL-V 来强制另一种类型。
示例： >
	dj
删除两行 >
	dvj
从光标位置删除到光标下方的字符 >
	d<C-V>j
删除光标下的字符和光标下方的字符。 >

强制行操作移动用作字符操作或块操作时要小心，因为列可能并不总是被定义。

							*o_v*
v		在操作符之后、移动命令之前使用时：强制操作符按字符操作工作，即使移动是行操作也是如此。
		如果移动是行操作，它将变为 |exclusive|。

		如果移动已经是字符操作，则切换包含性/排他性。
		这可用于使排他性移动变为包含性，使包含性移动变为排他性。

							*o_V*
V		在操作符之后、移动命令之前使用时：强制操作符按行操作工作，即使移动是字符操作也是如此。

							*o_CTRL-V*
CTRL-V		在操作符之后、移动命令之前使用时：强制操作符按块操作工作。
		这类似于可视块模式选择，其角由移动前和移动后的光标位置定义。

==============================================================================
2. 左右移动					*left-right-motions*

这些命令将光标移动到当前行的指定列。
它们停在第一列和行尾，但 "$" 除外，它可以移动到下一行。
请参阅 'whichwrap' 选项以使某些命令可以跨行边界移动。

h		或					*h*
<Left>		或					*<Left>*
CTRL-H		或					*CTRL-H* *<BS>*
<BS>			向左移动 [count] 个字符。 |exclusive| 移动。
			注意：如果您希望 <BS> 删除一个字符，请使用映射： >
				:map CTRL-V<BS>		X
<			（要输入 "CTRL-V<BS>"，请按 CTRL-V 键，然后按 <BS> 键）

l		或					*l*
<Right>		或					*<Right>* *<Space>*
<Space>			向右移动 [count] 个字符。 |exclusive| 移动。
			请参阅 'whichwrap' 选项以调整行尾的行为。

							*0*
0			移动到行的第一个字符。 |exclusive| 移动。

							*<Home>* *<kHome>*
<Home>			移动到行的第一个字符。 |exclusive| 移动。
			当接下来向上或向下移动时，尽可能保持在同一文本列。
			大多数其他命令保持在同一屏幕列。
			<Home> 的工作方式类似于 "1|"，当行以 <Tab> 开头时，它与 "0" 不同。

							*^*
^			移动到行的第一个非空白字符。 |exclusive| 移动。
			任何计数都被忽略。

							*$* *<End>* *<kEnd>*
$  或 <End>		移动到行尾。当给出计数时，也会向下移动 [count - 1] 行，或尽可能远。
			|inclusive| 移动。
			如果给出 2 或更大的计数并且光标在最后一行，则会出错且光标不移动。
			在可视模式下，光标移动到行中最后一个字符之后。
			当 'virtualedit' 处于活动状态时，"$" 可能会将光标从行尾之后移回到行中的最后一个字符。

							*g_*
g_			移动到行的最后一个非空白字符，并向下移动 [count - 1] 行 |inclusive|。

							*g0* *g<Home>*
g0 或 g<Home>		当行换行时 ('wrap' 开启)：移动到屏幕行的第一个字符。
			|exclusive| 移动。当行宽于屏幕时，与 "0" 不同。
			当行不换行时 ('wrap' 关闭)：移动到当前行在屏幕上最左边的字符。
			当行的第一个字符不在屏幕上时，与 "0" 不同。

							*g^*
g^			当行换行时 ('wrap' 开启)：移动到屏幕行的第一个非空白字符。
			|exclusive| 移动。当行宽于屏幕时，与 "^" 不同。
			当行不换行时 ('wrap' 关闭)：移动到当前行在屏幕上的最左边非空白字符。
			当行的第一个非空白字符不在屏幕上时，与 "^" 不同。

							*gm*
gm			类似于 "g0"，但向右移动半个屏幕宽度（或尽可能多）。

							*gM*
gM			类似于 "g0"，但移动到行文本的中间位置。
			带计数时：移动到行文本的此百分比位置。
			因此 "10gM" 靠近文本开头，"90gM" 靠近文本结尾。

							*g$*
g$			当行换行时 ('wrap' 开启)：移动到屏幕行的最后一个字符，
			并向下移动 [count - 1] 屏幕行 |inclusive|。
			当行宽于屏幕时，与 "$" 不同。
			当行不换行时 ('wrap' 关闭)：移动到当前行在屏幕上可见的最右边字符。
			当行的最后一个字符不在屏幕上或使用计数时，与 "$" 不同。
			此外，垂直移动保持列，而不是移动到行尾。
			当启用 'virtualedit' 时，移动到屏幕行的末尾。

							*g<End>* *g<kEnd>*
g<End>			类似于 |g$|，但移动到最后一个非空白字符而不是最后一个字符。

							*bar*
|			移动到当前行的屏幕列 [count]。 |exclusive| 移动。
			这不是一个管道符号。

							*f*
f{char}			向右移动到第 [count] 个 {char} 出现的位置。光标放在 {char} 上 |inclusive|。
			{char} 可以作为二合字母输入 |digraph-arg|。
			当 'encoding' 设置为 Unicode 时，可以使用组合字符，请参见 |utf-8-char-arg|。
			|:lmap| 映射适用于 {char}。插入模式下的 CTRL-^ 命令可用于打开/关闭此功能 |i_CTRL-^|。

							*F*
F{char}			向左移动到第 [count] 个 {char} 出现的位置。光标放在 {char} 上 |exclusive|。
			{char} 的输入方式与 |f| 命令相同。

							*t*
t{char}			向右移动到第 [count] 个 {char} 出现之前。光标放在 {char} 左边的字符上 |inclusive|。
			{char} 的输入方式与 |f| 命令相同。

							*T*
T{char}			向左移动到第 [count] 个 {char} 出现之后。光标放在 {char} 右边的字符上 |exclusive|。
			{char} 的输入方式与 |f| 命令相同。

							*;*
;			重复最新的 f、t、F 或 T 命令 [count] 次。请参阅 |cpo-;|

							*,*
,			以相反方向重复最新的 f、t、F 或 T 命令 [count] 次。另请参阅 |cpo-;|

==============================================================================
3. 上下移动					*up-down-motions*

k		或					*k*
<Up>		或					*<Up>* *CTRL-P*
CTRL-P			[count] 行向上 |linewise|。

j		或					*j*
<Down>		或					*<Down>*
CTRL-J		或					*CTRL-J*
<NL>		或					*<NL>* *CTRL-N*
CTRL-N			[count] 行向下 |linewise|。

gk		或					*gk* *g<Up>*
g<Up>			[count] 显示行向上。 |exclusive| 移动。
			当行换行时，以及当与操作符一起使用时，与 'k' 不同，因为它不是行操作。

gj		或					*gj* *g<Down>*
g<Down>			[count] 显示行向下。 |exclusive| 移动。
			当行换行时，以及当与操作符一起使用时，与 'j' 不同，因为它不是行操作。

							*-*
`-`  <减号>		[count] 行向上，移动到第一个非空白字符 |linewise|。

`+`		或					*+*
CTRL-M		或					*CTRL-M* *<CR>*
<CR>			[count] 行向下，移动到第一个非空白字符 |linewise|。

							*_*
_  <下划线>		[count] - 1 行向下，移动到第一个非空白字符 |linewise|。

							*G*
G			跳转到第 [count] 行，默认最后一行，移动到第一个非空白字符 |linewise|。
			如果未设置 'startofline'，则保持同一列。
			G 是一个 |jump-motions|。

							*<C-End>*
<C-End>			跳转到第 [count] 行，默认最后一行，移动到最后一个字符 |inclusive|。

<C-Home>		或					*gg* *<C-Home>*
gg			跳转到第 [count] 行，默认第一行，移动到第一个非空白字符 |linewise|。
			如果未设置 'startofline'，则保持同一列。

							*:[range]*
:[range]		将光标设置在 [range] 中的最后一个行号上。
			在 Ex 模式下，打印 [range] 中的行。
			[range] 也可以只是一个行号，例如 ":1" 或 ":'m"。
			与 |G| 相比，此命令不会修改 |jumplist|。
							*N%*
{count}%		跳转到文件中 {count}% 的位置，移动到行的第一个非空白字符 |linewise|。
			使用以下公式计算新行号：
			    ({count} * 行数 + 99) / 100
			另请参见 'startofline' 选项。

:[range]go[to] [count]					*:go* *:goto* *go*
[count]go		跳转到缓冲区中的第 [count] 个字节。 |exclusive| 移动。
			默认 [count] 为 1，即文件开头。当指定 [range] 时，使用其中的最后一个数字作为字节计数。
			行尾字符根据当前 'fileformat' 设置进行计数。
			另请参见 |line2byte()| 函数，以及 'statusline' 中的 'o' 选项。

这些命令移动到指定的行。
它们到达第一行或最后一行时停止。
前两个命令将光标放在与上次更改列的命令之后相同的列（如果可能），但 "$" 命令之后除外，光标将放在行的最后一个字符上。

==============================================================================
4. 单词移动						*word-motions*

<S-Right>	或					*<S-Right>* *w*
w			[count] 个单词向前。 |exclusive| 移动。

<C-Right>	或					*<C-Right>* *W*
W			[count] 个 WORD 向前。 |exclusive| 移动。

							*e*
e			向前移动到第 [count] 个单词的末尾 |inclusive|。
			不会停在空行上。

							*E*
E			向前移动到第 [count] 个 WORD 的末尾 |inclusive|。
			不会停在空行上。

<S-Left>	或					*<S-Left>* *b*
b			[count] 个单词向后。 |exclusive| 移动。

<C-Left>	或					*<C-Left>* *B*
B			[count] 个 WORD 向后。 |exclusive| 移动。

							*ge*
ge			向后移动到第 [count] 个单词的末尾 |inclusive|。

							*gE*
gE			向后移动到第 [count] 个 WORD 的末尾 |inclusive|。

这些命令在单词或 WORD 上移动。
							*word*
一个单词由一系列字母、数字和下划线组成，或一系列其他非空白字符组成，由空白字符（空格、制表符、<EOL>）分隔。
这可以通过 'iskeyword' 选项更改。对于 255 以上的字符，当 Unicode 字符类更改时（例如，在字母、下标、表情符号等之间），
单词结束。空行也被视为一个单词。
							*WORD*
一个 WORD 由一系列非空白字符组成，由空白字符分隔。空行也被视为一个 WORD。

一系列折叠行被计为单个字符的一个单词。
"w" 和 "W"、"e" 和 "E" 移动到一系列折叠行之后的第一个单词或 WORD 的开始/结束。
"b" 和 "B" 移动到折叠之前的第一个单词或 WORD 的开始。

特殊情况：如果光标在非空白字符上，"cw" 和 "cW" 被视为 "ce" 和 "cE"。这是 Vi 兼容的，请参阅 |cpo-_| 以更改行为。

另一个特殊情况：当将 "w" 移动与操作符结合使用，并且移动到的最后一个单词在行尾时，
该单词的末尾成为操作文本的末尾，而不是下一行的第一个单词。

原始的 Vi 对 "e" 的实现是有缺陷的。例如，如果上一行是空的，"e" 命令将停在行的第一个字符上。
但是当您使用 "2e" 时，这种情况不会发生。在 Vim 中，"ee" 和 "2e" 是相同的，这更符合逻辑。
然而，这导致了 Vi 和 Vim 之间的小小不兼容。

==============================================================================
5. 文本对象移动					*object-motions*

							*(*
(			[count] 个 |sentence| 向后。 |exclusive| 移动。

							*)*
)			[count] 个 |sentence| 向前。 |exclusive| 移动。

							*{*
{			[count] 个 |paragraph| 向后。 |exclusive| 移动。

							*}*
}			[count] 个 |paragraph| 向前。 |exclusive| 移动。

							*]]*
]]			[count] 个 |section| 向前，或移动到第一列的下一个 "{"。
			在操作符之后使用时，也会在第一列 "}" 下方停止。 |exclusive|
			注意 |exclusive-linewise| 通常适用。
			在 :terminal 缓冲区中，每个 shell 提示符都被视为一个节。 |terminal_]]|

							*][*
][			[count] 个 |section| 向前，或移动到第一列的下一个 "}"。 |exclusive|
			注意 |exclusive-linewise| 通常适用。

							*[[*
[[			[count] 个 |section| 向后，或移动到第一列的上一个 "{"。 |exclusive|
			注意 |exclusive-linewise| 通常适用。
			在 :terminal 缓冲区中，每个 shell 提示符都被视为一个节。 |terminal_]]|

							*[]*
[]			[count] 个 |section| 向后，或移动到第一列的上一个 "}"。 |exclusive|
			注意 |exclusive-linewise| 通常适用。

这些命令在三种文本对象上移动。

							*sentence*
一个句子定义为以 '.'、'!' 或 '?' 结尾，后跟行尾，或后跟空格或制表符。在 '.'、'!' 或 '?' 之后，空格、制表符或行尾之前，可以出现任意数量的闭合字符 ')'、']'、'"' 和 "'"。段落和节的边界也是句子的边界。
如果 'cpoptions' 中存在 'J' 标志，则标点符号后必须至少有两个空格；<Tab> 不被视为空白。
句子的定义无法更改。

							*paragraph*
段落从每个空行之后开始，也在一组段落宏处开始，由 'paragraphs' 选项中的字符对指定。默认值为 "IPLPPPQPP TPHPLIPpLpItpplpipbp"，对应于宏 ".IP"、".LP" 等（这些是 nroff 宏，因此点必须在第一列）。节的边界也是段落的边界。
请注意，空行（仅包含空白字符）不是段落边界。
注意：这不包括第一列的 '{' 或 '}'。
                                                        *section*
一个节从第一列的换页符 (<C-L>) 之后开始，也在一组节宏处开始，由 'sections' 选项中的字符对指定。默认值为 "SHNHH HUnhsh"，它定义了节从 nroff 宏 ".SH"、".NH"、".H"、".HU"、".nh" 和 ".sh" 开始。
在 :terminal 缓冲区中，每个 shell 提示符都被视为一个节。 |terminal_]]|

"]]" 和 "[[" 命令停在第一列的 '{' 处。这对于在 C 程序中查找函数的开始很有用。要搜索第一列的 '}'（C 函数的结尾），请使用 "]["（向前）或 "[]"（向后）。请注意，命令的第一个字符决定了搜索方向。

如果您的 '{' 或 '}' 不在第一列，但您仍想使用 "[[" 和 "]]"，请尝试这些映射： >
   :map [[ ?{<CR>w99[{
   :map ][ /}<CR>b99]}
   :map ]] j0[[%/{<CR>
   :map [] k$][%?}<CR>
[按字面输入这些内容，请参见 |<>|]

==============================================================================
6. 文本对象选择			*object-select* *text-objects*
						*v_a* *v_i*

这是一系列命令，只能在可视模式下或操作符之后使用。
以 "a" 开头的命令选择包括空白的 "a" 对象，
以 "i" 开头的命令选择不包括空白（或仅空白）的 "inner" 对象。
因此，"inner" 命令选择的文本总是比 "a" 命令少。

另请参见 `gn` 和 `gN`，它们作用于最后的搜索模式。

							*v_aw* *aw*
aw			"a word"，选择 [count] 个单词（参见 |word|）。
			包含前导或尾随的空白，但不计入计数。
			当在可视行模式下使用时，"aw" 切换到可视字符模式。

							*v_iw* *iw*
iw			"inner word"，选择 [count] 个单词（参见 |word|）。
			单词之间的空白也被计入。
			当在可视行模式下使用时，"iw" 切换到可视字符模式。

							*v_aW* *aW*
aW			"a WORD"，选择 [count] 个 WORD（参见 |WORD|）。
			包含前导或尾随的空白，但不计入计数。
			当在可视行模式下使用时，"aW" 切换到可视字符模式。

							*v_iW* *iW*
iW			"inner WORD"，选择 [count] 个 WORD（参见 |WORD|）。
			单词之间的空白也被计入。
			当在可视行模式下使用时，"iW" 切换到可视字符模式。

							*v_as* *as*
as			"a sentence"，选择 [count] 个句子（参见 |sentence|）。
			在可视模式下使用时，它变为字符操作。

							*v_is* *is*
is			"inner sentence"，选择 [count] 个句子（参见 |sentence|）。
			在可视模式下使用时，它变为字符操作。

							*v_ap* *ap*
ap			"a paragraph"，选择 [count] 个段落（参见 |paragraph|）。
			例外：空行（仅包含空白字符）也是段落边界。
			在可视模式下使用时，它变为行操作。

							*v_ip* *ip*
ip			"inner paragraph"，选择 [count] 个段落（参见 |paragraph|）。
			例外：空行（仅包含空白字符）也是段落边界。
			在可视模式下使用时，它变为行操作。

a]						*v_a]* *v_a[* *a]* *a[*
a[			"a [] block"，选择 [count] 个 '[' ']' 块。它向后查找第 [count] 个未闭合的 '['，并找到匹配的 ']'。选中的文本包括 '[' 和 ']'。
			|cpo-M| 选项标志用于处理转义的括号。
			在可视模式下使用时，它变为字符操作。

i]						*v_i]* *v_i[* *i]* *i[*
i[			"inner [] block"，选择 [count] 个 '[' ']' 块。它向后查找第 [count] 个未闭合的 '['，并找到匹配的 ']'。选中的文本不包括 '[' 和 ']'。选择像 "[]" 这样的空内部块是错误的。 |cpo-M| 选项标志用于处理转义的括号。
			在可视模式下使用时，它变为字符操作。

a)							*v_a)* *a)* *a(*
a(							*vab* *v_ab* *v_a(* *ab*
ab			"a block"，选择 [count] 个块，从 "[count] [(" 到匹配的 ')'，包括 '(' 和 ')'（参见 |[(|）。不包括括号外部的空白。 |cpo-M| 选项标志用于处理转义的括号。
			在可视模式下使用时，它变为字符操作。

i)							*v_i)* *i)* *i(*
i(							*vib* *v_ib* *v_i(* *ib*
ib			"inner block"，选择 [count] 个块，从 "[count] [(" 到匹配的 ')'，不包括 '(' 和 ')'（参见 |[(|）。
			如果光标不在 () 块内，则查找下一个 "("。
			选择像 "()" 这样的空内部块是错误的。
			|cpo-M| 选项标志用于处理转义的括号。
			在可视模式下使用时，它变为字符操作。

a>						*v_a>* *v_a<* *a>* *a<*
a<			"a <> block"，选择 [count] 个 <> 块，从向后查找的第 [count] 个未匹配的 '<' 到匹配的 '>'，包括 '<' 和 '>'。 |cpo-M| 选项标志用于处理转义的 '<' 和 '>'。
			在可视模式下使用时，它变为字符操作。

i>						*v_i>* *v_i<* *i>* *i<*
i<			"inner <> block"，选择 [count] 个 <> 块，从向后查找的第 [count] 个未匹配的 '<' 到匹配的 '>'，不包括 '<' 和 '>'。选择像 "<>" 这样的空内部块是错误的。 |cpo-M| 选项标志用于处理转义的 '<' 和 '>'。
			在可视模式下使用时，它变为字符操作。

						*v_at* *at*
at			"a tag block"，选择 [count] 个标签块，从向后查找的第 [count] 个未匹配的 "<aaa>" 到匹配的 "</aaa>"，包括 "<aaa>" 和 "</aaa>"。
			关于详细信息，请参见 |tag-blocks|。
			在可视模式下使用时，它变为字符操作。

						*v_it* *it*
it			"inner tag block"，选择 [count] 个标签块，从向后查找的第 [count] 个未匹配的 "<aaa>" 到匹配的 "</aaa>"，不包括 "<aaa>" 和 "</aaa>"。
			关于详细信息，请参见 |tag-blocks|。
			在可视模式下使用时，它变为字符操作。

a}							*v_a}* *a}* *a{*
a{							*v_aB* *v_a{* *aB*
aB			"a Block"，选择 [count] 个 Block，从 `[count] [{`
  到匹配的 "}"，包括 "{" 和 "}"（参见 |[{|）。 |cpo-M| 选项标志用于处理转义的括号。
			在可视模式下使用时，它变为字符操作。

i}							*v_i}* *i}* *i{*
i{							*v_iB* *v_i{* *iB*
iB			"inner Block"，选择 [count] 个 Block，从 `[count] [{` 到匹配的 "}"，不包括 "{" 和 "}"（参见 |[{|）。选择像 "{}" 这样的空内部块是错误的。 |cpo-M| 选项标志用于处理转义的括号。
			在可视模式下使用时，它变为字符操作。

a"							*v_aquote* *aquote*
a'							*v_a'* *a'*
a`							*v_a`* *a`*
			"a quoted string"。选择从前一个引号到下一个引号的文本。'quoteescape' 选项用于跳过转义的引号。
			仅在一行内有效。
			当光标从引号开始时，Vim 将通过从行首搜索来确定哪些引号对构成一个字符串。
			包含任何尾随空白，如果没有，则包含前导空白。
			在可视模式下使用时，它变为字符操作。
			在可视模式下重复此对象将包含另一个字符串。当前不使用计数。

i"							*v_iquote* *iquote*
i'							*v_i'* *i'*
i`							*v_i`* *i`*
			类似于 a"、a' 和 a`，但不包括引号，并且重复不会扩展可视选择。
			特殊情况：当计数为 2 时，包括引号，但没有像 a"/a'/a` 那样的额外空白。

							*o_object-select*
在操作符之后使用时：
对于非块对象：
	对于 "a" 命令：操作符应用于对象和对象后的空白。
	如果对象后没有空白，或者光标在对象前的空白中，则包括对象前的空白。
	对于 "inner" 命令：如果光标在对象上，操作符应用于对象。如果光标在空白上，操作符应用于空白。
对于块对象：
	操作符应用于光标所在的块，或者光标位于其括号之一的块。
	对于 "inner" 命令，不包括周围的括号。
	对于 "a" 命令，包括括号。

							*v_object-select*
在可视模式下使用时：
当可视区域的开始和结束相同时（刚输入 "v" 之后）：
	选择一个对象，与使用操作符时相同。
当可视区域的开始和结束不相同时：
	对于非块对象，区域将扩展一个对象或直到下一个对象的空白，或者对于 "a" 对象两者都扩展。
	此操作的方向取决于光标位于可视区域的哪一侧。
	对于块对象，块向外扩展一级。

为了说明，这里列出了一些删除命令，按从小到大的对象分组。注意，对于单个字符和整行，使用的是现有的 vi 移动命令。
	"dl"	删除字符（别名："x"）		|dl|
	"diw"	删除内部单词			*diw*
	"daw"	删除一个单词			*daw*
	"diW"	删除内部 WORD（参见 |WORD|）		*diW*
	"daW"	删除一个 WORD（参见 |WORD|）		*daW*
	"dgn"   删除下一个搜索模式匹配项    *dgn*
	"dd"	删除一行				|dd|
	"dis"	删除内部句子			*dis*
	"das"	删除一个句子			*das*
	"dib"	删除内部 '(' ')' 块		*dib*
	"dab"	删除一个 '(' ')' 块			*dab*
	"dip"	删除内部段落			*dip*
	"dap"	删除一个段落			*dap*
	"diB"	删除内部 '{' '}' 块		*diB*
	"daB"	删除一个 '{' '}' 块			*daB*

注意使用移动命令和对象之间的区别。
移动命令从这里（光标位置）操作到移动带我们去的地方。
当使用对象时，无论光标在对象上的哪个位置，都会操作整个对象。
例如，比较 "dw" 和 "daw"："dw" 从光标位置删除到下一个单词的开头，"daw" 删除光标下的单词以及其后的或之前的空格。


标签块						*tag-blocks*

对于 "it" 和 "at" 文本对象，会尝试在 HTML 和 XML 的匹配标签之间选择块。
但由于它们并不完全兼容，因此存在一些限制。

通常的方法是选择 <tag> 直到匹配的 </tag>。
对于 "at"，包括标签；对于 "it"，不包括标签。但是当重复 "it" 时，将包括标签（否则什么都不会改变）。
此外，在没有内容的标签块上使用 "it" 将选择前导标签。

跳过 "<aaa/>" 项。忽略大小写，也适用于区分大小写的 XML。

在 HTML 中，可能存在像 <br> 或 <meta ...> 这样的标签，没有匹配的结束标签。这些会被忽略。

文本对象对错误具有容忍性。不匹配的结束标签会被忽略。

==============================================================================
7. 标记					*mark-motions* *E20* *E78*

跳转到标记可以通过两种方式完成：
1. 使用 `（反引号）：光标定位在指定位置，移动是 |exclusive|。
2. 使用 '（单引号）：光标定位在指定位置行中的第一个非空白字符上，移动是行操作。
						*mark-view*
3. 除了上述之外，如果 'jumpoptions' 包含 "view"，它们还将尝试恢复标记视图。这是光标位置与设置标记时的窗口顶行（窗口中显示的第一个缓冲区行）之间的行数。

						*m* *mark* *Mark*
m{a-zA-Z}		在光标位置设置标记 {a-zA-Z}（不移动光标，这不是移动命令）。

						*m'* *m`*
m'  或  m`		设置上一个上下文标记。可以使用 "''" 或 "``" 命令跳转到该标记（不移动光标，这不是移动命令）。

						*m[* *m]*
m[  或  m]		设置 |'[| 或 |']| 标记。当需要通过多个命令模拟操作符时很有用。（不移动光标，这不是移动命令）。

						*m<* *m>*
m<  或  m>		设置 |'<| 或 |'>| 标记。对于更改 `gv` 命令选择的内容很有用。（不移动光标，这不是移动命令）。
			注意：不能设置可视模式，只能设置开始和结束位置。

						*:ma* *:mark* *E191*
:[range]ma[rk] {a-zA-Z'}	在 [range] 的最后一行号（第 0 列）设置标记 {a-zA-Z'}。默认为光标行。

						*:k*
:[range]k{a-zA-Z'}	与 :mark 相同，但标记名称前的空格可以省略。

						*'* *'a* *`* *`a*
'{a-z}  `{a-z}		跳转到当前缓冲区中的标记 {a-z}。

						*'A* *'0* *`A* *`0*
'{A-Z0-9}  `{A-Z0-9}	跳转到设置该标记的文件中的标记 {A-Z0-9}（在另一个文件中时不是移动命令）。

						*g'* *g'a* *g`* *g`a*
g'{mark}  g`{mark}		跳转到 {mark}，但在当前缓冲区内跳转时不更改跳转列表。示例： >
				g`"
<			跳转到文件中的最后已知位置。
			另请参见 |:keepjumps|。

						*:marks*
:marks			列出所有当前标记（不是移动命令）。
			|'(|、 |')|、 |'{| 和 |'}| 标记不会列出。
			第一列编号为零。
						*E283*
:marks {arg}		列出 {arg} 中提到的标记（不是移动命令）。例如： >
				:marks aB
<			列出标记 'a' 和 'B'。

							*:delm* *:delmarks*
:delm[arks] {marks}	删除指定的标记。可以删除的标记包括 A-Z 和 0-9。不能删除 ' 标记。
			可以通过给出标记名称列表或使用由破折号分隔的范围来指定标记。空格被忽略。示例： >
			   :delmarks a	      删除标记 a
			   :delmarks a b 1    删除标记 a、b 和 1
			   :delmarks Aa       删除标记 A 和 a
			   :delmarks p-z      删除 p 到 z 范围内的标记
			   :delmarks ^.[]     删除标记 ^ . [ ]
			   :delmarks \"	      删除标记 "
<

:delm[arks]!		删除当前缓冲区的所有标记，但不删除 A-Z 或 0-9。同时清除 |changelist|。

标记不可见。它只是文件中记住的一个位置。不要将标记与命名寄存器混淆，它们完全不相关。

'a - 'z		小写标记，在一个文件内有效
'A - 'Z		大写标记，也称为文件标记，在文件之间有效
'0 - '9		编号标记，从 |shada| 文件设置

只要文件保留在缓冲区列表中，小写标记 'a 到 'z 就会被记住。如果从缓冲区列表中删除文件，其所有标记都会丢失。如果删除包含标记的行，则该标记会被擦除。

小写标记可以与操作符结合使用。例如："d't" 删除从光标位置到标记 't' 的行。提示：使用标记 't' 表示 Top（顶部），'b' 表示 Bottom（底部）等。使用撤消和重做时，小写标记会被恢复。

大写标记 'A 到 'Z 包含文件名。您可以使用它们在文件之间跳转。只有当标记在当前文件中时，才能将大写标记与操作符一起使用。即使您插入/删除行或暂时编辑另一个文件，标记的行号也会保持不变。当 'shada' 选项不为空时，大写标记会保存在 .shada 文件中。参见 |shada-file-marks|。

编号标记 '0 到 '9 非常不同。它们不能直接设置。它们仅在使用 shada 文件时存在 |shada-file|。基本上 '0 是您上次退出 Vim 时光标的位置，'1 是倒数第二次的位置，依此类推。使用 'shada' 中的 "r" 标志来指定不应存储编号标记的文件。参见 |shada-file-marks|。


							*'[* *`[*
'[  `[		到先前更改或抽出的文本的第一个字符。写入缓冲区时也会设置。

							*']* *`]*
']  `]		到先前更改或抽出的文本的最后一个字符。写入缓冲区时也会设置。

执行操作符后，光标放在被操作文本的开头。在执行 put 命令（"p" 或 "P"）后，光标有时放在第一个插入行，有时放在最后一个插入字符上。上述四个命令将光标放在任一端。示例：抽出 10 行后，您想转到最后一行："10Y']"。使用 "p" 命令插入几行后，您想跳转到插入的最低行："p']"。这也适用于已插入的文本。

注意：删除文本后，开始和结束位置相同，除非使用块模式可视模式。如果在当前文件中尚未进行任何更改，这些命令不起作用。

							*'<* *`<*
'<  `<		到当前缓冲区中最后选中的可视区域的第一行或第一个字符。对于块模式，它也可能是第一行的最后一个字符（以便能够定义块）。

							*'>* *`>*
'>  `>		到当前缓冲区中最后选中的可视区域的最后一行或最后一个字符。对于块模式，它也可能是最后一行的第一个字符（以便能够定义块）。注意 'selection' 选项会生效，位置可能刚好在可视区域之后。

							*''* *``*
''  ``		到最新跳转之前的位置，或最后执行 "m'" 或 "m`" 命令的位置。当使用 |:keepjumps| 命令修饰符时不会设置。
			另请参见 |restore-position|。

							*'quote* *`quote*
'"  `"		到上次退出当前缓冲区时的光标位置。默认为第一行的第一个字符。关于如何为每个打开的文件使用此功能，请参见 |last-position-jump|。
			每个缓冲区只记住一个位置，而不是每个窗口一个。只要缓冲区在窗口中可见，位置就不会改变。当运行 |:wshada| 时，标记也会重置。

							*'^* *`^*
'^  `^		到上次停止插入模式时光标所在的位置。由 |gi| 命令使用。当使用 |:keepjumps| 命令修饰符时不会设置。

							*'.* *`.*
'.  `.		到最后一次更改的位置。该位置在更改开始的位置或其附近。有时命令作为多次更改执行，那么位置可能靠近命令更改的末尾。例如，当插入一个单词时，位置将在最后一个字符上。
			要跳转到更早的更改，请使用 |g;|。

							*':* *`:*
':		在提示缓冲区中，是当前用户输入的开始位置。从此标记到缓冲区末尾的文本将在用户提交提示时提交。

							*'(* *`(*
'(  `(		到当前句子的开始，类似于 |(| 命令。

							*')* *`)*
')  `)		到当前句子的结束，类似于 |)| 命令。

							*'{* *`{*
'{  `{		到当前段落的开始，类似于 |{| 命令。

							*'}* *`}*
'}  `}		到当前段落的结束，类似于 |}| 命令。

这些命令本身不是标记，但会跳转到标记：

							*]'*
]'			[count] 次跳转到光标下方有（小写）标记的下一行，位于该行的第一个非空白字符上。

							*]`*
]`			[count] 次跳转到光标之后的小写标记。

							*['*
['			[count] 次跳转到光标上方有（小写）标记的上一行，位于该行的第一个非空白字符上。

							*[`*
[`			[count] 次跳转到光标之前的小写标记。


:loc[kmarks] {command}				*:loc* *:lock* *:lockmarks*
			执行 {command} 而不调整标记。当以某种方式更改文本，使得更改完成后行数相同时，这很有用。
			警告：当行数确实发生变化时，更改下方的标记将保持其行号，因此会移动到另一行文本。
			以下项目不会针对已删除/插入的行进行调整：
			- 小写字母标记 'a - 'z
			- 大写字母标记 'A - 'Z
			- 编号标记 '0 - '9
			- 最后插入位置 '^
			- 最后更改位置 '.
			- 最后受影响的文本区域 '[ 和 ']
			- 可视区域 '< 和 '>
			- 放置的符号中的行号
			- quickfix 位置中的行号
			- |jumplist| 中的位置
			- |tagstack| 中的位置
			以下项目仍将被调整：
			- 上一个上下文标记 ''
			- 光标位置
			- 缓冲区窗口的视图
			- 折叠
			- 差异

:kee[pmarks] {command}				*:kee* *:keep* *:keepmarks*
			目前仅对过滤器命令 |:range!| 有效：
			- 当过滤后的行数等于或大于之前时，所有标记都保持在同一行号。
			- 当行数减少时，消失行中的标记将被删除。
			在任何情况下，过滤文本下方的标记都会照常调整行号，从而粘附在文本上。
			当 'cpoptions' 中缺少 'R' 标志时，这与使用 ":keepmarks" 具有相同的效果。

							*:keepj* *:keepjumps*
:keepj[umps] {command}
			在 {command} 中移动不会更改 |''|、 |'.| 和 |'^| 标记、 |jumplist| 或 |changelist|。
			当自动进行更改或插入文本，而用户不想转到该位置时很有用。例如，当更新第一行中的“最后更改”时间戳时： >

				:let lnum = line(".")
				:keepjumps normal gg
				:call SetLastChange()
				:keepjumps exe "normal " .. lnum .. "G"
<
			注意，每个命令都必须使用 ":keepjumps"。当调用函数时，该函数中的命令仍然可以更改跳转列表。此外，对于 `:keepjumps exe 'command '`，“command”不会保持跳转。请改用：`:exe 'keepjumps command'`

==============================================================================
8. 跳转					*jump-motions*

“跳转”是通常将光标移动几行距离的命令。如果光标“跳转”，则跳转前光标的位置会被记住。您可以使用 "''" 和 "``" 命令返回到该位置，除非包含该位置的行已被更改或删除。以下命令是“跳转”命令："'", "`", "G", "/", "?", "n", "N", "%", "(", ")", "[[", "]]", "{", "}", ":s", ":tag", "L", "M", "H" 以及开始编辑新文件的命令。

							*CTRL-O*
CTRL-O			跳转到跳转列表中的第 [count] 个较旧的光标位置（不是移动命令）。

<Tab>		或					*CTRL-I* *<Tab>*
CTRL-I			跳转到跳转列表中的第 [count] 个较新的光标位置（不是移动命令）。

			注意：在 GUI 和支持 |tui-modifyOtherKeys| 或 |tui-csiu| 的终端中，CTRL-I 可以与 <Tab> 分开映射，条件是这两个键都被映射，否则映射适用于两者。tmux 除外：https://github.com/tmux/tmux/issues/2705

*:ju* *:jumps*
:ju[mps]		打印跳转列表（不是移动命令）。

							*:cle* *:clearjumps*
:cle[arjumps]		清除当前窗口的跳转列表。

							*jumplist*
跳转会记录在跳转列表中。使用 CTRL-O 和 CTRL-I 命令，您可以转到较旧跳转之前的光标位置，然后再返回。因此，您可以在列表中上下移动。每个窗口都有一个单独的跳转列表。最大条目数固定为 100。

例如，在三个跳转命令之后，您将获得以下跳转列表： >

    jump line  col file/text
      3     1    0 some text
      2    70    0 another line
      1  1154   23 end.
   >
<
"file/text" 列显示文件名，或者如果跳转在当前文件中，则显示该位置的文本（缩进会被移除，长行会被截断以适应窗口）。

标记 ">" 指示跳转列表中的当前位置。当使用 |:filter| 过滤 |:jumps| 命令时，它可能不会显示。

您当前在第 1167 行。如果您随后使用 CTRL-O 命令，光标将被放置在第 1154 行。这将导致： >

    jump line  col file/text
      2     1    0 some text
      1    70    0 another line
   >  0  1154   23 end.
      1  1167    0 foo bar
<
指针将设置在上次使用的跳转位置。下一个 CTRL-O 命令将使用其上面的条目，下一个 CTRL-I 命令将使用其下面的条目。
如果指针在最后一个条目下方，则表示您之前没有使用过 CTRL-I 或 CTRL-O。
在这种情况下，CTRL-O 命令将导致光标位置被添加到跳转列表中，因此您可以返回到 CTRL-O 之前的位置。
在这种情况下，这是第 1167 行。

使用更多的 CTRL-O 命令，您将转到第 70 行和第 1 行。如果您使用 CTRL-I，您可以再次返回到第 1154 行和第 1167 行。
请注意，“jump”列中的数字表示将您带到此位置的 CTRL-O 或 CTRL-I 命令的计数。

如果您使用跳转命令，当前行号将插入到跳转列表的末尾。如果同一行已经在跳转列表中，它将被移除。
结果是，当重复 CTRL-O 时，您只会返回到旧位置一次。

当使用 |:keepjumps| 命令修饰符时，跳转不会存储在跳转列表中。
在其他情况下，例如在 |:global| 命令中，跳转也不会被存储。
您可以通过使用 "m'" 设置 ' 标记来显式添加跳转。
请注意，调用 setpos() 不会执行此操作。

在通过 CTRL-O 命令进入第 1154 行后，您可以发出另一个跳转命令（例如，"G"）。然后跳转列表将变为： >

    jump line  col file/text
      4     1    0 some text
      3    70    0 another line
      2  1167    0 foo bar
      1  1154   23 end.
   >
<
行号将针对已删除和插入的行进行调整。如果您在不写入的情况下停止编辑文件，例如使用 ":n!"，则此操作会失败。

当您分割窗口时，跳转列表将复制到新窗口。

如果您在 'shada' 选项中包含了 ' 项，则跳转列表将存储在 ShaDa 文件中，并在启动 Vim 时恢复。

							*jumplist-stack*
当 'jumpoptions' 选项包含 "stack" 时，跳转列表的行为类似于标签栈。
当从跳转列表中间跳转到新位置时，当前位置之后的位置将被丢弃。
设置此选项后，您可以在跳转位置树中移动。当沿一个分支返回，然后沿另一个分支向下时， CTRL-O 仍会将您进一步沿树上移。

给定一个如下所示的跳转列表，其中已使用 CTRL-O 向后移动三次到位置 X： >

     jump line  col file/text
       2  1260    8 mark.c		<-- 位置 X-2
       1   685    0 eval.c		<-- 位置 X-1
    >  0   462   36 eval.c		<-- 位置 X
       1   479   39 eval.c
       2   213    2 mark.c
       3   181    0 mark.c
<
跳转到（新）位置 Y 将导致当前位置之后的位置被移除： >

     jump line  col file/text
       3  1260    8 mark.c		<-- 位置 X-2
       2   685    0 eval.c		<-- 位置 X-1
       1   462   36 eval.c		<-- 位置 X
    >
<
然后，当跳转到另一个位置 Z 时，新位置 Y 会直接出现在跳转列表中位置 X 之后，而位置 X 相对于其之前的位置（X-1、X-2 等）保持在原始从 X 跳转到 Y 之前相同的相对位置： >

     jump line  col file/text
       4  1260    8 mark.c		<-- 位置 X-2
       3   685    0 eval.c		<-- 位置 X-1
       2   462   36 eval.c		<-- 位置 X
       1   100    0 buffer.c		<-- 位置 Y
    >
<
更改列表跳转			*changelist* *change-list-jumps* *E664*

当进行更改时，光标位置会被记住。每个可以撤消的更改都会记住一个位置，除非它靠近之前的更改。
可以使用两个命令跳转到更改的位置，也包括那些已被撤消的更改：

							*g;* *E662*
g;			转到更改列表中第 [count] 个较旧的位置。
			如果 [count] 大于较旧更改位置的数量，则转到最旧的更改。
			如果没有较旧的更改，则给出错误消息。
			（不是移动命令）

							*g,* *E663*
g,			转到更改列表中第 [count] 个较新的位置。
			与 |g;| 类似，但方向相反。
			（不是移动命令）

当使用计数时，您可以尽可能向后或向前跳转。因此，您可以使用 "999g;" 转到仍被记住的第一个更改位置。
更改列表中的条目数是固定的，并且与 |jumplist| 相同。

当两个可撤消的更改在同一行且列位置相距小于 'textwidth' 时，只记住最后一个。
这可以避免一行中的一系列小更改（例如 "xxxxx"）向更改列表添加许多位置。
当 'textwidth' 为零时，使用 'wrapmargin'。
当该选项也未设置时，使用固定数字 79。
详细信息：在计算中使用的是字节，而不是字符，以避免速度损失（这仅对多字节编码有影响）。

请注意，当插入或删除文本时，光标位置可能与更改的位置略有不同。尤其是在删除行时。

当使用 `:keepjumps` 命令修饰符时，更改的位置不会被记住。

							*:changes*
:changes		打印更改列表。一个 ">" 字符指示当前位置。
			在更改之后，它位于最新条目的下方，表示 `g;` 会将您带到最新条目的位置。
			第一列表示将您带到此位置所需的计数。
			示例：

				change line  col text ~
				    3     9    8 bla bla bla
				    2    11   57 foo is a bar
				    1    14   54 the latest changed line
				>

			`3g;` 命令将您带到第 9 行。然后 `:changes` 的输出为：

				change line  col text ~
				>   0     9    8 bla bla bla
				    1    11   57 foo is a bar
				    2    14   54 the latest changed line

			现在您可以使用 "g," 转到第 11 行，使用 "2g," 转到第 14 行。

==============================================================================
9. 各种移动				*various-motions*

							*%*
%			在当前行中光标之后或之下找到下一个项目，并跳转到其匹配项。 |inclusive| 移动。
			项目可以是：
			([{}])		括号或（花括号/方括号）
					（这可以通过 'matchpairs' 选项更改）
			`/* */`		C 样式注释的开始或结束
			#if, #ifdef, #else, #elif, #endif
					C 预处理器条件（当光标在 # 上或之后没有 ([{ 时）
			对于其他项目，可以使用 matchit 插件，参见 |matchit|。
			该插件还有助于跳过注释中的匹配项。

			当 'cpoptions' 包含 "M" |cpo-M| 时，括号和大括号前的反斜杠将被忽略。
			如果没有 "M"，反斜杠的数量很重要：偶数个与奇数个不匹配。
			因此，在 "( \) )" 和 "\( ( \)" 中，第一个和最后一个括号匹配。

			当 'cpoptions' 中不存在 '%' 字符 |cpo-%| 时，双引号内的括号和大括号将被忽略，
			除非一行中括号/大括号的数量为奇数，并且该行和上一行不以反斜杠结尾。
			'('、'{'、'['、']'、'}' 和 ')' 也会被忽略（单引号内的括号和大括号）。
			请注意，这对 C 语言有效，但对 Perl 无效，在 Perl 中单引号用于字符串。

			对于注释中的匹配项没有特殊处理。您可以使用 |matchit| 插件，或者在匹配项周围加上引号。

			不允许使用计数，{count}% 跳转到文件下方 {count} 百分比的行 |N%|。
			在 #if/#else/#endif 上使用 '%' 会使移动变为行操作。

						*[(*
[(			转到第 [count] 个之前的未匹配 '('。
			|exclusive| 移动。

						*[{*
[{			转到第 [count] 个之前的未匹配 '{'。
			|exclusive| 移动。

						*])*
])			转到第 [count] 个之后的未匹配 ')'。
			|exclusive| 移动。

						*]}*
]}			转到第 [count] 个之后的未匹配 '}'。
			|exclusive| 移动。

上述四个命令可用于转到当前代码块的开始或结束。
它类似于在代码块另一端的 "("、")"、"{" 或 "}" 上执行 "%"，但您可以从代码块中的任何位置执行此操作。
对于 C 程序非常有用。示例：当位于 "case x:" 上时，`[{` 会将您带回 switch 语句。

						*]m*
]m			转到第 [count] 个之后的方法开始（对于 Java 或类似的结构化语言）。
			如果不在方法开始之前，则跳转到类的开始或结束。 |exclusive| 移动。
						*]M*
]M			转到第 [count] 个之后的方法结束（对于 Java 或类似的结构化语言）。
			如果不在方法结束之前，则跳转到类的开始或结束。 |exclusive| 移动。
						*[m*
[m			转到第 [count] 个之前的方法开始（对于 Java 或类似的结构化语言）。
			如果不在方法开始之后，则跳转到类的开始或结束。
			如果在光标之前未找到 '{'，则这是一个错误。 |exclusive| 移动。
						*[M*
[M			转到第 [count] 个之前的方法结束（对于 Java 或类似的结构化语言）。
			如果不在方法结束之后，则跳转到类的开始或结束。
			如果在光标之前未找到 '}'，则这是一个错误。 |exclusive| 移动。

上述四个命令假定文件包含一个带有方法的类。类定义包含在 '{' 和 '}' 中。
类中的每个方法也包含在 '{' 和 '}' 中。这适用于 Java 语言。
文件如下所示： >java
	// 注释
	class foo {
		int method_one() {
			body_one();
		}
		int method_two() {
			body_two();
		}
	}

[要尝试此功能，请将文本复制并放入新缓冲区，上面的帮助文本会干扰跳转命令]

从光标位于 "body_two()" 开始，使用 "[m" 将跳转到 "method_two()" 开头的 '{'（显然，当方法很长时，这更有用！）。
使用 "2[m" 将跳转到 "method_one()" 的开始。使用 "3[m" 将跳转到类的开始。

						*[#*
[#			转到第 [count] 个之前的未匹配 "#if" 或 "#else"。
			|exclusive| 移动。

						*]#*
]#			转到第 [count] 个之后的未匹配 "#else" 或 "#endif"。
			|exclusive| 移动。

这两个命令适用于包含 #if/#else/#endif 结构的 C 程序。
它们将您带到当前行所在的 #if/#else/#endif 的开始或结束。然后您可以使用 "%" 转到匹配行。

						*[star* *[/*
[*  或  [/		转到第 [count] 个之前的 C 注释 "/*" 开始。
			|exclusive| 移动。

						*]star* *]/*
]*  或  ]/		转到第 [count] 个之后的 C 注释 "*/" 结束。
			|exclusive| 移动。


						*H*
H			从窗口顶部（Home）算起的第 [count] 行（默认：窗口的第一行），
			位于第一个非空白字符上 |linewise|。另请参见 'startofline' 选项。
			光标会根据 'scrolloff' 选项进行调整，除非操作符挂起，
			在这种情况下文本可能会滚动。例如，"yH" 从第一个可见行抽出到光标行（包含）。

						*M*
M			窗口的中间行，位于第一个非空白字符上 |linewise|。另请参见 'startofline' 选项。

						*L*
L			从窗口底部算起的第 [count] 行（默认：窗口的最后一行），
			位于第一个非空白字符上 |linewise|。另请参见 'startofline' 选项。
			光标会根据 'scrolloff' 选项进行调整，除非操作符挂起，在这种情况下文本可能会滚动。
			例如，"yL" 从光标抽出到最后一个可见行。

<LeftMouse>		移动到屏幕上鼠标点击的位置 |exclusive|。另请参见 |<LeftMouse>|。
			如果该位置在状态行中，则该窗口将成为活动窗口，光标不会移动。

 vim:tw=78:ts=8:noet:ft=help:norl:
