*starting.txt*  Nvim


                  VIM 参考手册    作者：Bram Moolenaar


启动 Vim                                            *starting*

                                      输入 |gO| 查看目录。

==============================================================================
Nvim 参数                                          *cli-arguments*

大多数情况下，Nvim 是使用以下命令启动以编辑单个文件的： >

        nvim filename

更一般地，Nvim 使用以下方式启动： >

        nvim [option | filename] ..

选项参数和文件名参数可以混合，并且可以给出任意数量的参数。但是，要注意那些带参数的选项。

以下项目决定如何开始编辑：

                                                        *-file* *---*
filename        一个或多个文件名。第一个将是当前文件并读入缓冲区。光标将定位在缓冲区的第一行。
                为了避免以 '-' 开头的文件名被解释为选项，请在参数列表前加上 "--"，例如： >
                        nvim -- -filename
<                在 "--" 之后的所有参数都被解释为文件名，后面不能跟其他选项或 "+command" 参数。

                                                        *--*
`-`               stdin（标准输入）的别名。
                示例： >
                        echo text | nvim - file
<                "text" 被读入缓冲区 1，"file" 作为缓冲区 2 打开。
                在大多数情况下（除了 -s、-es、|--embed|、--headless），如果 stdin 不是 TTY，则它会被作为文本读取，因此 "-" 是隐含的： >
                        echo text | nvim file
<                缓冲区将被标记为已修改，因为它包含需要保存的文本（只读 |-R| 模式除外）。
                如果你不喜欢这样，可以将这些行放入你的 init.vim 中： >
                        " 从 stdin 读取时不要设置 'modified'
                        au StdinReadPost * set nomodified
<
                要使用 |-s| 将 stdin 读取为普通模式命令，请与 "-" 一起使用： >
                        echo "ifoo" | nvim -s -
<                要使用 |-es| 或 |-e| 将 stdin 读取为 Ex 命令： >
                        echo "echo getpid()" | nvim -e - -V1
<                要打开一个字面名为 "-" 的文件，将其放在 "--" 之后： >
                        echo foo | nvim -- -
<                要使用 |--headless| 将 stdin 读取为文本，请使用 "-"。

                                                        *-t* *-tag*
-t {tag}        一个标签。在标签文件中查找 "tag"，关联的文件成为当前文件，并执行关联的命令。这主要用于 C 程序，在这种情况下 "tag" 通常是一个函数名。效果是包含该函数的文件成为当前文件，光标定位在函数的开始处（参见 |tags|）。

                                                        *-q* *-qf*
-q [errorfile]  快速修复模式。读取名为 [errorfile] 的文件并显示第一个错误。参见 |quickfix|。
                如果未给出 [errorfile]，则使用 'errorfile' 选项作为文件名。默认值请参见 'errorfile'。

(nothing)       如果没有上述四个项目，Vim 将开始编辑一个新缓冲区。它是空的，没有文件名。

                                                        *startup-options*
选项参数可以按任何顺序给出。单字母选项可以在一个破折号后组合。在 "--" 参数之后不能有选项参数。

--help                                                  *-h* *--help* *-?*
-?
-h              显示用法（帮助）消息并退出。

--version                                               *-v* *--version*
-v              打印版本信息并退出。与 |:version| 命令的输出相同。

                                                        *--clean*
--clean         模拟 Nvim 的全新安装：
                - 跳过文件和环境变量的初始化。
                - 不读取或写入 'shada' 文件。
                - 从 'runtimepath' 中排除用户目录。
                - 加载内置插件，与 "-u NONE -i NONE" 不同。

                                                        *--noplugin*
--noplugin      跳过加载插件。重置 'loadplugins' 选项。
                注意，|-u| 参数也可能禁用加载插件：
                        参数              加载 vimrc 文件        加载插件 >
                        (nothing)               yes                 yes
                        -u NONE                 no                  no
                        -u NORC                 no                  yes
                        --noplugin              yes                 no
<
--startuptime {fname}                                   *--startuptime*
                在启动期间将计时消息写入文件 {fname}。
                这可用于找出加载你的 |config|、插件和打开第一个文件时时间花费在哪里。
                当 {fname} 已存在时，新消息将被追加。

                                                        *-+*
+[num]          光标将定位在正在编辑的第一个文件的第 "num" 行。如果省略 "num"，光标将定位在最后一行。

                                                        *-+/*
+/{pat}         光标将定位在正在编辑的第一个文件中包含 "pat" 的第一行（有关可用的搜索模式，请参见 |pattern|）。搜索从光标位置开始，该位置可以是第一行，也可以是上次从 |shada| 使用的光标位置。要强制从第一行开始搜索，请使用 "+1 +/pat"。

+{command}                                              *-+c* *-c*
-c {command}    {command} 将在读取第一个文件之后（并在处理该文件的自动命令和模型行之后）执行。"command" 被解释为 Ex 命令。
                如果 "command" 包含空格，则必须用双引号括起来（这取决于所使用的 shell）。
                示例： >
                        vim  "+set si"  main.c
                        vim  "+find stdio.h"
                        vim  -c "set ff=dos"  -c wq  mine.mak
<
                注意：你可以在一个 Vim 命令中使用最多 10 个 "+" 或 "-c" 参数。它们按给定的顺序执行。一个 "-S" 参数也算作一个 "-c" 参数。

--cmd {command}                                         *--cmd*
                {command} 将在处理任何 vimrc 文件之前执行。否则，它类似于 -c {command}。你可以使用最多 10 个这样的命令，独立于 "-c" 命令。

                                                        *-S*
-S [file]       在读取第一个文件之后执行 Vimscript 或 Lua（".lua"）[file]。另请参见 |:source|。如果未给出 [file]，默认为 "Session.vim"。相当于： >
                        -c "source {file}"
<                可以像 "-c" 一样重复，受限于 10 个 "-c" 参数的相同限制。{file} 不能以 "-" 开头。

-L                                                      *-L* *-r*
-r              恢复模式。如果没有文件名参数，则给出现有交换文件的列表。如果有文件名，则读取交换文件以恢复崩溃的编辑会话。参见 |crash-recovery|。

                                                        *-R*
-R              只读模式。将为所有正在编辑的文件设置 'readonly' 选项。你仍然可以编辑缓冲区，但可以防止意外覆盖文件。如果你忘记自己处于视图模式并且确实做了一些更改，你可以通过在 Ex 命令中添加感叹号来覆盖文件，如 ":w!"。'readonly' 选项可以通过 ":set noro" 重置（参见选项章节，|options|）。后续编辑将不会以只读模式进行。
                'updatecount' 选项将设置为 10000，这意味着交换文件不会很频繁地自动更新。有关禁止修改，请参见 |-M|。

                                                        *-m*
-m              不允许写入修改。将重置 'write' 选项，以便禁用写入文件。但是，可以设置 'write' 选项以重新启用写入。

                                                        *-M*
-M              不允许修改。将重置 'modifiable' 选项，以便不允许更改。将重置 'write' 选项，以便禁用写入文件。但是，可以设置 'modifiable' 和 'write' 选项以启用更改和写入。

-e                                                      *-e* *-E*
-E              以 Ex 模式 |gQ| 启动 Nvim，参见 |Ex-mode|。

                如果 stdin 不是 TTY：
                • -e 将 stdin 读取/执行为 Ex 命令。
                • -E 将 stdin 读取为文本（进入缓冲区 1）。

-es                                             *-es* *-Es* *-s-ex* *silent-mode*
-Es             脚本模式，也称为“静默模式”，也称为“批处理模式”。无 UI，禁用大多数提示和消息。与 |-s| 无关。
                另请参见 |-S| 以运行脚本文件。

                -es 将 stdin 读取/执行为 Ex 命令。 >
                        printf "put ='foo'\n%%print\n" | nvim -es
<
                -Es 将 stdin 读取为文本（进入缓冲区 1）。使用 |-c| 或 "+" 发送命令。 >
                        printf "foo\n" | nvim -Es +"%print"
<
                这些命令显示在 stdout 上：
                • :list
                • :number
                • :print
                • :set

                使用 |:verbose| 或 'verbose'，其他命令显示在 stderr 上： >
                        nvim -es +"verbose echo 'foo'"
                        nvim -V1 -es +"echo 'foo'"
<
                除非给出 |-u|，否则跳过用户 |config|。
                除非给出 |-i|，否则禁用 |shada|。
                禁用交换文件（类似于 |-n|）。

                                                        *-l*
-l {script} [args]
                在处理任何前面的 Nvim |cli-arguments| 之后，以非交互方式（无 UI）执行 Lua {script}，带有可选的 [args]，然后退出。Lua 错误时退出码为 1。参见 |-S| 以在带有 UI 的情况下运行多个不带参数的 Lua 脚本。
                                                                    *lua-args*
                所有 [args] 都被视为 {script} 参数并存储在 Lua `_G.arg` 全局表中，因此 "-l" 结束 Nvim 参数的处理。{script} 名称存储在 `_G.arg[0]` 中。

                将 'verbose' 设置为 1（类似于 "-V1"），以便 Lua `print()` 写入输出，以及其他消息发送函数，如 |:echo|。
                如果 {script} 打印消息并且不导致 Nvim 退出，Nvim 确保输出以换行符结尾。

                "-l" 之前的参数在 {script} 执行之前被处理。此示例在执行 "foo.lua" 之前退出： >
                        nvim +q -l foo.lua
<                此示例在执行 "foo.lua" 之前加载 Lua 模块 "bar"： >
                        nvim +"lua require('bar')" -l foo.lua
<                                                                   *lua-shebang*
                你可以设置脚本的 "shebang"，以便从 shell 中使用 "./" 调用时 Nvim 执行该脚本（记住要 "chmod u+x"）： >
                        #!/usr/bin/env -S nvim -l
<
                除非给出 |-u|，否则跳过用户 |config|。
                除非设置了 'loadplugins'，否则禁用插件。
                除非给出 |-i|，否则禁用 |shada|。
                禁用交换文件（类似于 |-n|）。

                                                        *-b*
-b              二进制模式。文件 I/O 将仅识别 <NL> 来分隔行。将重置 'expandtab' 选项。'textwidth' 选项设置为 0。重置 'modeline'。设置 'binary' 选项。这是在读取 |vimrc| 之后但在读取 arglist 中的任何文件之前完成的。另请参见 |edit-binary|。

                                                        *-A*
-A              阿拉伯语模式。设置 'arabic' 选项。

                                                        *-H*
-H              希伯来语模式。设置 'rightleft' 选项并将 'keymap' 选项设置为 "hebrew"。

                                                        *-V* *verbose*
-V[N]           详细。将 'verbose' 选项设置为 [N]（默认值：10）。对于每个被 ":source" 的文件以及读取或写入 ShaDa 文件时，都会给出消息。可用于查明启动和退出时发生了什么。
                示例： >
                        nvim -V8

-V[N]{file}
                类似于 -V 并将 'verbosefile' 设置为 {file}（不能以数字开头）。不显示消息，而是将它们写入 {file}。
                示例： >
                        nvim -V20vimlog
<
                                                        *-D*
-D              调试。在执行脚本中的第一个命令时进入调试模式。|debug-mode|

                                                        *-n*
-n              通过将 'updatecount' 设置为 0（在执行任何 |vimrc| 之后）来禁用 |swap-file|。崩溃后恢复将是不可能的。在非常慢的介质（USB 驱动器、网络共享）上处理文件时提高性能。

                通过将 'updatecount' 设置为某个值来重新启用它，例如 ":set updatecount=100"。

                为了减少对磁盘的访问，不要使用 "-n"，而是将 'updatetime' 和 'updatecount' 设置为非常大的数字，并在想要保存工作时输入 ":preserve"。这样你仍保留了崩溃恢复的可能性。

                                                        *-o*
-o[N]           打开 N 个窗口，水平拆分。如果未给出 [N]，则为每个作为参数给出的文件打开一个窗口。如果没有足够的空间，只有前几个文件获得窗口。如果窗口多于参数，最后几个窗口将编辑一个空文件。

                                                        *-O*
-O[N]           打开 N 个窗口，垂直拆分。否则，它类似于 -o。如果同时给出了 -o 和 -O 选项，命令行中最后一个确定窗口的拆分方式。

                                                        *-p*
-p[N]           打开 N 个标签页。如果未给出 [N]，则为每个作为参数给出的文件打开一个标签页。最大值由 'tabpagemax' 设置（默认 50）。如果标签页多于参数，最后几个标签页将编辑一个空文件。另请参见 |tabpage|。
                                                        *-d*
-d              以 |diff-mode| 启动。

                                                        *-u* *E282*
-u {vimrc}      读取文件 {vimrc} 进行初始化。跳过大多数其他初始化；参见 |initialization|。

                这可用于以特殊模式启动 Vim，具有特殊的映射和设置。可以使用 shell 别名使其易于使用。例如，在 C shell 衍生版本中： >
                        alias vimc 'nvim -u ~/.config/nvim/c_init.vim \!*'
<                在 Bash shell 中： >
                        alias vimc='nvim -u ~/.config/nvim/c_init.vim'
<                也可以考虑使用自动命令；参见 |autocommand|。

                当 {vimrc} 为 "NONE"（全部大写）时，跳过来自文件和环境变量的所有初始化。插件和语法高亮也被跳过。

                当 {vimrc} 为 "NORC"（全部大写）时，其效果与 "NONE" 相同，但插件和语法高亮不会被跳过。

                                                        *-i*
-i {shada}      使用文件 {shada} 代替默认的 ShaDa 文件。如果使用名称 "NONE"（全部大写），则不读取或写入 ShaDa 文件，即使设置了 'shada' 或使用了 ":rsh" 或 ":wsh"。另请参见 |shada-file|。

                                                        *-s*
-s {scriptin}   读取脚本文件 {scriptin}，将字符解释为普通模式输入。同样可以通过 ":source!" 完成： >
                        :source! {scriptin}
<                如果 {scriptin} 为 "-"，则从 stdin 读取： >
                        echo "ifoo" | nvim -s -
<                如果在 Nvim 退出之前到达文件末尾，则从键盘读取更多字符。

                不适用于 |-es|。另请参见 |complex-repeat|。

                                                        *-w_nr*
-w {number}
-w{number}      将 'window' 选项设置为 {number}。

                                                        *-w*
-w {scriptout}  你输入的所有键都被记录在文件 "scriptout" 中，直到你退出 Vim。对于创建与 "vim -s" 或 ":source!" 一起使用的脚本文件很有用。如果 "scriptout" 文件已存在，则追加。{scriptout} 不能以数字开头。
                另请参见 |vim.on_key()|。
                另请参见 |complex-repeat|。

                                                        *-W*
-W {scriptout}  类似于 -w，但不追加，覆盖现有文件。

                                                        *--api-info*
--api-info      打印 msgpack 编码的 |api-metadata| 并退出。

                                                        *--embed*
--embed         使用 stdin/stdout 作为 msgpack-RPC 通道，以便应用程序可以通过 RPC |API| 嵌入和控制 Nvim。如果通道关闭（除了通过 |:detach|），Nvim 退出。

                在加载启动文件和读取缓冲区之前，等待客户端调用 |nvim_ui_attach()|，以便 UI 可以处理（显示）早期消息、对话框等。客户端可以在 `nvim_ui_attach` 之前执行其他请求（例如，用于功能检测的 `nvim_get_api_info`）。在此预启动阶段，用户配置当然不可用（类似于 `--cmd`）。

                非 UI 嵌入器必须传递 |--headless|，然后启动将在不等待 `nvim_ui_attach` 的情况下继续： >
                    nvim --embed --headless
<                这相当于： >
                    nvim --headless --cmd "call stdioopen({'rpc': v:true})"
<
                想要在套接字（而不是 stdio）上使用 UI 协议的 UI 嵌入器必须同时传递 |--listen| 和 |--embed|： >
                    nvim --embed --listen addr

<                另请参见：|ui-startup| |channel-stdio|

                                                        *--headless*
--headless      无 UI 启动，并且不等待 `nvim_ui_attach`。不使用内置 TUI，因此 stdio 作为任意通信通道工作。|channel-stdio|

                对于脚本编写（测试）也很有用，可以看到 |-es| 不会打印的消息。

                要检测 UI 是否可用，请在 |VimEnter| 期间或之后检查 |nvim_list_uis()| 是否为空。

                要将 stdin 读取为文本，必须显式给出 "-"：
                --headless 不能假设 stdin 只是文本。 >
                        echo foo | nvim --headless +"%print" +"q!" -
<
                另请参见 |--embed|。
                另请参见 |-es|，它也会禁用大多数消息。

--listen {addr}                                         *--listen*
                在管道或 TCP 地址 {addr} 上启动 |RPC| 服务器。将主侦听地址 |v:servername| 设置为 {addr}。|serverstart()|

                要使用 systemd 按需启动服务器，请使用 systemd 套接字单元和相关服务单元运行： >
                        systemd-socket-proxyd --exit-idle-time
<
==============================================================================
初始化                                  *initialization* *startup*

在启动时，Nvim 检查环境变量和文件并相应地设置值，按以下步骤进行：

1. 设置 |v:starttime| 和 |v:startreason|。

2. 设置 'shell' 选项。                      *SHELL* *COMSPEC*
   如果存在环境变量 SHELL，则用于设置 'shell' 选项。在 Win32 上，如果未设置 SHELL，则使用 COMSPEC 变量。

3. 处理参数。
   检查启动 Vim 的命令中的选项和文件名。可以使用 |-V| 参数来显示或记录接下来发生的事情，这对于调试初始化很有用。执行 |--cmd| 参数。
   为所有文件创建缓冲区（但尚未加载）。

4. 启动服务器（除非给出了 |--listen|）并设置 |v:servername|。

5. 等待 UI 连接。
   使用 |--embed| 启动的 Nvim 在继续加载用户配置之前等待 UI 连接。

6. 设置 |default-mappings| 和 |default-autocmds|。创建 |popup-menu|。

7. 启用文件类型和缩进插件。
   如果给出了 "-u NONE" 命令行参数，则跳过。
   这与执行以下操作相同： >
        :runtime! ftplugin.vim indent.vim

8. 加载用户配置（从文件、环境等执行 Ex 命令）。
   将 $VIMINIT 环境变量读取为一个 Ex 命令行（用 '|' 或 <NL> 分隔多个命令）。
                                        *config* *init.vim* *init.lua* *vimrc* *exrc*
   - 包含初始化命令的文件通常称为 "vimrc" 或配置文件。它可以是 Vimscript（"init.vim"）或 Lua（"init.lua"），但不能同时是两者。*E5422* 参见 |vimrc-intro| |base-directories|。
     配置文件位于：
     - Unix                  ~/.config/nvim/init.vim         （或 init.lua）
     - Windows               ~/AppData/Local/nvim/init.vim   （或 init.lua）
     - |$XDG_CONFIG_HOME|      $XDG_CONFIG_HOME/nvim/init.vim  （或 init.lua）

   - 如果 Nvim 使用 "-u {file}" 启动，则 {file} 用作配置文件，并且直到 8. 的所有初始化都被跳过。不设置 $MYVIMRC。
     "nvim -u NORC" 可用于跳过这些初始化而不读取文件。"nvim -u NONE" 也会跳过插件和语法高亮。|-u|

   - 如果 Nvim 使用 |-es| 或 |-Es| 或 |-l| 启动，则直到 "8" 的所有初始化都被跳过。

   - a. *system-vimrc* *sysinit.vim*
        读取系统 vimrc 文件进行初始化。如果 nvim/sysinit.vim 文件存在于 $XDG_CONFIG_DIRS 之一中，则将使用它。否则，使用系统 vimrc 文件。此文件的路径由 |:version| 命令给出。通常是 "$VIM/sysinit.vim"。

   - b. *VIMINIT* *EXINIT* *$MYVIMRC*
        按优先顺序搜索初始化位置。
        除非已经设置或使用 $VIMINIT，否则 $MYVIMRC 设置为第一个有效位置：
        -  $VIMINIT 环境变量（Ex 命令行）。
        -  用户 |config|：$XDG_CONFIG_HOME/nvim/init.vim（或 init.lua）。
        -  其他配置：{dir}/nvim/init.vim（或 init.lua），其中 {dir} 是 $XDG_CONFIG_DIRS 中的任何目录。
        -  $EXINIT 环境变量（Ex 命令行）。

   - c. 如果 'exrc' 选项打开，则搜索 |current-directory|，直到找到以下文件之一，按优先级顺序：
        - ".nvim.lua"
        - ".nvimrc"
        - ".exrc"

9. 启用文件类型检测。
   如果调用了 ":filetype off" 或给出了 "-u NONE" 命令行参数，则跳过。这与执行以下操作相同： >
        :runtime! filetype.lua

10. 启用语法高亮。
   如果调用了 ":syntax off" 或给出了 "-u NONE" 命令行参数，则跳过。这与执行以下操作相同： >
        :runtime! syntax/syntax.vim

11. 将 |v:vim_did_init| 变量设置为 1。

12. 加载插件脚本。                                    *load-plugins*
    这与执行以下操作相同：
        `:runtime! plugin/**/*.{vim,lua}`
    - 结果是，将搜索 'runtimepath' 中的所有目录以查找 "plugin" 子目录，并且所有以 ".vim" 或 ".lua" 结尾的文件都将被源操作（每个目录按字母顺序），也包括子目录。首先源操作 "*.vim"，然后源操作 "*.lua" 文件，按目录进行。
    - 但是，'runtimepath' 中以 "after" 结尾的目录在此处被跳过，仅在包之后加载，见下文。
    - 在以下情况下不会加载插件：
      - 在 vimrc 文件中重置了 'loadplugins' 选项。
      - 使用了 |--noplugin| 命令行参数。
      - 使用了 |--clean| 命令行参数。
      - 使用了 "-u NONE" 命令行参数 |-u|。
    - 注意，使用 `-c 'set noloadplugins'` 不起作用，因为命令行中的命令尚未执行。你可以使用 `--cmd 'set noloadplugins'` 或 `--cmd 'set loadplugins'` |--cmd|。
    - 加载包。这些是插件，如上所述，但在 'packpath' 中每个条目的 "start" 目录中找到。找到的每个插件目录都添加到 'runtimepath' 中，然后对插件进行源操作。参见 |packages|。
    - 加载插件脚本，如上所述，但现在只使用以 "after" 结尾的目录。注意，如果找到包，'runtimepath' 将已更改，但这不应添加以 "after" 结尾的目录。

13. 根据 'shell' 选项的值设置 'shellpipe' 和 'shellredir'，除非它们已被设置。除非你自己设置了它们，否则 Nvim 将为你计算出 'shellpipe' 和 'shellredir' 的值。

14. 如果给出了 |-n|，则将 'updatecount' 设置为零。

15. 如果给出了 |-b|，则设置二进制选项。

16. 读取 |shada-file|。

17. 如果给出了 |-q|，则读取快速修复文件，否则在失败时退出。

18. 打开所有窗口。
    - 当给出了 |-o| 时，将打开窗口（但尚未显示）。
    - 当给出了 |-p| 时，将创建标签页（但尚未显示）。
    - 切换屏幕时，现在发生。开始重绘。
    - 如果给出了 |-q|，则跳转到第一个错误。
    - 所有窗口的缓冲区将被加载，而不会触发 |BufAdd|。

19. 执行启动命令。
    - 如果给出了 |-t|，则跳转到标签。
    - 使用 |-c| 和 |+cmd| 给出的命令将被执行。
    - 重置 |vim_starting| 标志，`has("vim_starting")` 现在将返回零。
    - |v:vim_did_enter| 变量设置为 1。
    - 执行 |VimEnter| 自动命令。


将 Vim 的当前状态保存到文件 ~

每当你更改了选项值或创建了映射时，你可能希望将它们保存在 vimrc 文件中以供以后使用。参见 |save-settings| 关于将设置的当前状态保存到文件。


避免特洛伊木马 ~
                                                        *trojan-horse*
在读取当前目录中的 "vimrc" 或 "exrc" 文件时，可以通过设置 'secure' 选项来禁用某些命令以确保安全。这总是在从标签文件执行命令时完成。否则，你可能意外使用别人创建的包含恶意命令的 vimrc 或 tags 文件。被禁用的命令是启动 shell 的命令、写入文件的命令和 ":autocmd"。":map" 命令会被回显，以便你可以看到哪些键正在被映射。

如果你希望 Vim 执行本地 vimrc 文件中的所有命令，你可以在 EXINIT 或 VIMINIT 环境变量中或全局 exrc 或 vimrc 文件中重置 'secure' 选项。出于明显的原因，这在当前目录的 vimrc 或 exrc 中是不可能的。

在 Unix 系统上，只有当你不是 vimrc 文件的所有者时才会发生这种情况。警告：如果你解压一个包含 vimrc 或 exrc 文件的归档文件，它将归你所有。你将没有安全保护。在你在该目录中启动 Vim 之前检查 vimrc 文件，或者重置 'exrc' 选项。一些 Unix 系统允许用户对文件执行 "chown"。这使得其他用户有可能创建一个恶意 vimrc 并让你成为所有者。要小心！

当使用标签搜索命令时，执行搜索命令（tags 文件中行的最后部分）总是在安全模式下进行。这就像从当前目录中的 vimrc 执行命令一样。


如果 Vim 启动缓慢 ~
                                                        *slow-start*
如果 Vim 启动需要很长时间，请使用 |--startuptime| 参数来查明发生了什么。

如果你启用了 'shada'，加载 ShaDa 文件可能需要一段时间。你可以通过暂时禁用 ShaDa 来查明是否是这个问题（使用 Vim 参数 "-i NONE"，|-i|）。尝试通过 ":set shada='20,<50,s10" 减少寄存器中存储的行数。|shada-file|。


排查损坏的配置 ~
                                                        *bisect*
像 Vim 和 Emacs 这样的编辑器的极端灵活性意味着任何插件或设置都可能以最初不明显的方式影响整个编辑器。

要找到配置中的问题原因，你必须对其进行 "二分查找"：
1. 移除或禁用一半的 |config|。
2. 重启 Nvim（|:restart|）。
3. 如果问题仍然存在，转到 1。
4. 如果问题消失，恢复一半被移除的行。
5. 继续以这种方式缩小你的配置范围，直到找到导致问题的设置或插件。


介绍消息 ~
                                                        *:intro*
当 Vim 在没有文件名的情况下启动时，会显示介绍消息。一旦显示被重绘，它就会被移除。要再次查看该消息，请使用 ":intro" 命令。要避免在启动时显示介绍消息，请将 "I" 标志添加到 'shortmess'。

==============================================================================
$VIM 和 $VIMRUNTIME
                                                                *$VIM*
环境变量 "$VIM" 用于定位 Nvim 的各种用户文件，例如用户 |config|。这取决于系统，参见 |startup|。

Nvim 将按以下顺序尝试获取 $VIM 的值：

1. 环境变量 $VIM（如果已设置）。
2. 从 'helpfile' 选项派生的路径，除非它也包含某个环境变量（默认是 "$VIMRUNTIME/doc/help.txt"）。移除文件名（"help.txt" 等）。按顺序移除尾部目录名："doc"、"runtime"。
3. 从 `nvim` 可执行文件的位置派生的路径。
4. 编译时定义的安装目录（参见 ":version" 的输出）。

完成一次后，Nvim 设置 $VIM 环境变量。

                                                                *$VIMRUNTIME*
环境变量 "$VIMRUNTIME" 用于定位各种支持文件，例如文档和语法高亮文件。例如，主帮助文件通常是 "$VIMRUNTIME/doc/help.txt"。

Nvim 将按以下顺序尝试获取 $VIMRUNTIME 的值：

1. 环境变量 $VIMRUNTIME（如果已设置）。
2. 目录路径 "$VIM/runtime"（如果存在）。
3. $VIM 环境变量的值。这是为了与旧版 Vim 向后兼容。
4. 如果相对于 |v:progpath| 存在 "../share/nvim/runtime"，则使用它。
5. 从 'helpfile' 选项派生的路径（如果它不包含 '$'），并移除末尾的 "doc/help.txt"。

完成一次后，Nvim 设置 $VIMRUNTIME 环境变量。

如果你在 shell 中需要 $VIMRUNTIME 的值（例如，对于在帮助文件中 grep 的脚本），你可以使用以下命令： >

        VIMRUNTIME="$(nvim --clean --headless --cmd 'echo $VIMRUNTIME|q')"

==============================================================================
挂起                                              *suspend*

                                        *CTRL-Z* *v_CTRL-Z*
CTRL-Z                  挂起 Nvim，类似于 ":stop"。
                        在普通模式和可视模式下工作。在插入模式和命令行模式下，CTRL-Z 作为普通字符插入。在可视模式下，Nvim 在挂起之前返回普通模式。

:sus[pend][!]   or                      *:sus* *:suspend* *:st* *:stop*
:st[op][!]              使用操作系统“作业控制”挂起 Nvim；如果你再次将其设为前台作业，它将继续。在挂起之前触发 |VimSuspend|，在恢复时触发 |VimResume|。

                        如果未给出 "!" 并且设置了 'autowrite'，则写入每个有更改且有文件名的缓冲区。
                        如果给出了 "!" 或未设置 'autowrite'，则不写入更改的缓冲区，不要忘记稍后将 Nvim 带回前台！

在 GUI 中，挂起是实现定义的。

==============================================================================
退出                                                 *exiting*

有几种方法可以退出 Vim：
- 使用 `:quit` 关闭最后一个窗口。仅在没有更改时。
- 使用 `:quit!` 关闭最后一个窗口。即使有更改时也可以。
- 使用 `:qall` 关闭所有窗口。仅在没有更改时。
- 使用 `:qall!` 关闭所有窗口。即使有更改时也可以。
- 使用 `:cquit`。即使有更改时也可以。

当使用 `:cquit` 或出现错误消息时，Vim 以退出码 1 退出。可以通过使用 `:silent!` 或使用 `:catch` 来避免错误。

==============================================================================
保存设置                                         *save-settings*

大多数情况下，你会手动编辑你的 vimrc 文件。这给了你最大的灵活性。有几个命令可以自动生成 vimrc 文件。你可以按原样使用这些文件，或复制/粘贴行以包含在另一个 vimrc 文件中。

                                                        *:mk* *:mkexrc*
:mk[exrc] [file]        将当前的键映射和更改的选项写入 [file]（默认是当前目录中的 ".exrc"），除非它已存在。

:mk[exrc]! [file]       始终将当前的键映射和更改的选项写入 [file]（默认是当前目录中的 ".exrc"）。

                                                *:mkv* *:mkvi* *:mkvimrc*
:mkv[imrc][!] [file]    类似于 ":mkexrc"，但默认是当前目录中的 ".nvimrc"。":version" 命令也被写入文件。

这些命令将 ":map" 和 ":set" 命令写入文件，这样当这些命令执行时，当前的键映射和选项将被设置为相同的值。选项 'columns'、'endofline'、'fileformat'、'lines'、'modified' 和 'scroll' 不包括在内，因为这些是终端或文件相关的。
注意，选项 'binary'、'paste' 和 'readonly' 包括在内，这可能并不总是你想要的。

当在映射中使用特殊键时，'cpoptions' 选项将临时设置为其 Vim 默认值，以避免映射被误解。这使得文件与 Vi 不兼容，但确保它可以与不同的终端一起使用。

仅存储全局映射，不存储缓冲区本地映射。

一种常见的方法是使用默认的 |config| 文件，使用 ":map" 和 ":set" 命令进行一些修改，然后写入修改后的文件。首先使用类似 ":source ~piet/.vimrc.Cprogs" 的命令读取默认的 vimrc，更改设置，然后使用 ":mkvimrc!" 将它们保存在当前目录中。如果你希望将此文件设为默认的 |config|，请将其移动到 $XDG_CONFIG_HOME/nvim。你也可以使用自动命令 |autocommand| 和/或模型行 |modeline|。

                                                *vimrc-option-example*
如果你只想向你的 vimrc 添加一个选项设置，你可以使用以下步骤：
1. 使用 Vim 编辑你的 vimrc 文件。
2. 调整选项直到正确。例如，尝试 'guifont' 的不同值。
3. 使用表达式寄存器 '=' 输入值，附加一行来设置选项的值。例如，对于 'guifont' 选项： >
   o:set guifont=<C-R>=&guifont<CR><Esc>
<  [<C-R> 是 CTRL-R，<CR> 是回车，<Esc> 是退出键]
   你需要转义特殊字符，尤其是空格。

==============================================================================
视图和会话                                      *views-sessions*

这在用户手册的章节 |21.4| 和 |21.5| 中介绍。

                                                *View* *view-file*
视图是应用于一个窗口的设置集合。你可以保存一个视图，并在稍后恢复它时，文本将以相同的方式显示。此窗口中的选项和映射也将被恢复，以便你可以像保存视图时一样继续编辑。

                                                *Session* *session-file*
会话保存所有窗口的视图，以及全局设置。你可以保存一个会话，并在稍后恢复它时，窗口布局看起来相同。你可以使用会话在不同的项目之间快速切换，自动加载你在该项目中最后处理的文件。

视图和会话是对 ShaDa 文件的一个很好的补充，ShaDa 文件用于一起记住所有视图和会话的信息 |shada-file|。

你可以使用 |-S| 参数快速开始编辑先前保存的视图或会话： >
        vim -S Session.vim
<
                                                        *:mks* *:mksession*
:mks[ession][!] [file]  写入一个 Vim 脚本，用于恢复当前的编辑会话。
                        当包含 [!] 时，覆盖现有文件。
                        当省略 [file] 时，使用 "Session.vim"。

":mksession" 的输出类似于 ":mkvimrc"，但会向文件添加额外的命令。哪些命令取决于 'sessionoptions' 选项。生成的文件，当使用 ":source" 命令执行时：
1. 如果 'sessionoptions' 包含 "options"，则恢复全局映射和选项。不会写入脚本本地映射。
2. 如果 'sessionoptions' 包含 "globals"，则恢复以大写字母开头且包含至少一个小写字母的全局变量。
3. 关闭当前标签页中的所有窗口，除了当前窗口；关闭所有标签页，除了当前标签页（这导致当前加载的缓冲区被卸载，如果设置了 'hidden' 或以其他方式指定，一些可能变为隐藏）；如果当前缓冲区为空且未命名，则将其擦除。
4. 如果 'sessionoptions' 包含 "curdir"，则恢复当前目录；如果 'sessionoptions' 包含 "sesdir"，则将当前目录设置为会话文件所在的位置。
5. 如果 'sessionoptions' 包含 "winpos"，则恢复 GUI Vim 窗口位置。
6. 如果 'sessionoptions' 包含 "resize"，则恢复屏幕大小。
7. 重新加载缓冲区列表，使用最后的光标位置。如果 'sessionoptions' 包含 "buffers"，则恢复所有缓冲区，包括隐藏和未加载的缓冲区。否则，仅恢复窗口中的缓冲区。
8. 以相同的布局恢复所有窗口。如果 'sessionoptions' 包含 "help"，则恢复帮助窗口。如果 'sessionoptions' 包含 "blank"，则恢复编辑无名缓冲区的窗口。
   如果 'sessionoptions' 包含 "winsize" 并且没有（帮助/空白）窗口被遗漏，则恢复窗口大小（相对于屏幕大小）。否则，窗口只被赋予合理的大小。
9. 恢复所有窗口的视图，如同使用 |:mkview|。但使用 'sessionoptions' 而不是 'viewoptions'。
10. 如果存在与会话文件同名但以 "x.vim" 结尾的文件（表示 eXtra），也执行它。你可以使用 `*x.vim` 文件来指定与给定会话关联的额外设置和操作，例如在 GUI 版本中创建菜单项。

恢复会话后，当前会话的完整文件名可在内部变量 |v:this_session| 中获得。
一个示例映射： >
  :nmap <F2> :wa<Bar>exe "mksession! " .. v:this_session<CR>:so ~/sessions/
这会保存当前会话，并开始执行加载另一个会话的命令。

会话包含所有标签页，除非从 'sessionoptions' 中移除了 "tabpages"。|tabpage|

在加载/源操作会话文件之前触发 |SessionLoadPre| 自动命令事件，之后触发 |SessionLoadPost| 自动命令事件。
                                                *SessionLoad-variable*
在加载会话文件时，`g:SessionLoad` 设置为 1。插件可以使用它来将一些工作推迟到 SessionLoadPost 事件触发之后。

                                                        *:mkvie* *:mkview*
:mkvie[w][!] [file]     写入一个 Vim 脚本，用于恢复当前窗口的内容。
                        当包含 [!] 时，覆盖现有文件。
                        当省略 [file] 或 [file] 是 1 到 9 的数字时，生成一个名称并添加 'viewdir' 前缀。当 'viewdir' 的最后路径部分不存在时，创建此目录。例如，当 'viewdir' 为 "$VIM/vimfiles/view" 时，在 "$VIM/vimfiles" 中创建 "view"。
                        然后始终覆盖现有文件。使用 |:loadview| 再次加载此视图。
                        当 [file] 是文件名时（不使用 'viewdir'），将编辑该文件的命令添加到生成的文件中。

":mkview" 的输出包含以下项目：
1. 窗口中使用的参数列表。当使用全局参数列表时，将其重置为全局列表。
   参数列表中的索引也被恢复。
2. 窗口中正在编辑的文件。如果没有文件，则使窗口为空。
3. 如果 'viewoptions' 包含 "options" 或 "localoptions"，则恢复窗口本地的映射、缩写和选项。仅恢复当前缓冲区和当前窗口本地的选项值。当存储视图作为会话的一部分且 "options" 在 'sessionoptions' 中时，也将存储本地选项的全局值。
4. 当使用手动折叠且 'viewoptions' 包含 "folds" 时，恢复折叠。恢复手动打开和关闭的折叠。
5. 文件中的滚动位置和光标位置。当存在关闭的折叠时，效果不太好。
6. 如果本地当前目录与全局当前目录不同且 'viewoptions' 包含 "curdir"，则恢复本地当前目录。

注意，视图和会话并不完美：
- 它们不会恢复所有内容。例如，定义的函数、自动命令和 ":syntax on" 不包括在内。像寄存器内容和命令行历史之类的东西在 ShaDa 中，而不在会话或视图中。
- 全局选项值仅在它们与默认值不同时设置。当当前值不是默认值时，加载会话不会将其设置回默认值。本地选项将被设置回默认值。
- 现有映射将被覆盖而没有警告。现有映射可能导致歧义错误。
- 当存储手动折叠以及存储手动打开/关闭的折叠时，保存和加载视图之间文件中的更改会使其混乱。
- Vim 脚本不是非常高效。但仍然比你自己输入命令快！

                                                        *:lo* *:loadview*
:lo[adview] [nr]        加载当前文件的视图。当省略 [nr] 时，加载使用 ":mkview" 存储的视图。当指定 [nr] 时，加载使用 ":mkview [nr]" 存储的视图。

":mkview" 和 ":loadview" 的组合可用于存储文件的多达十种不同视图。这些存储在由 'viewdir' 选项指定的目录中。视图使用文件名存储。如果文件被重命名或通过（符号）链接访问，则不会找到该视图。

你可能需要不时清理你的 'viewdir' 目录。

要自动保存和恢复 `*.c` 文件的视图： >
        au BufWinLeave *.c mkview
        au BufWinEnter *.c silent! loadview

==============================================================================
ShaDa（“共享数据”）文件                      *shada* *shada-file*

如果你退出 Vim 然后再启动它，你通常会丢失大量信息。ShaDa 文件可用于记住这些信息，使你能够从上次中断的地方继续。它的名称是 SHAred DAta 的缩写，因为它用于在 Nvim 会话之间共享数据。

这在用户手册的章节 |21.3| 中介绍。

ShaDa 文件用于存储：
- 命令行历史。
- 搜索字符串历史。
- 输入行历史。
- 非空寄存器的内容。
- 多个文件的标记。
- 文件标记，指向文件中的位置。
- 最后的搜索/替换模式（用于 'n' 和 '&'）。
- 缓冲区列表。
- 全局变量。

你也可以使用会话文件。区别在于 ShaDa 文件不依赖于你正在处理的内容。通常只有一个 ShaDa 文件。会话文件用于保存特定编辑会话的状态。你可以有多个会话文件，每个你正在处理的项目一个。ShaDa 和会话文件一起可以有效地进入 Vim 并直接开始在你期望的设置中工作。|session-file|

                                                        *shada-read*
当 Vim 启动且 'shada' 选项非空时，读取 ShaDa 文件的内容，并且信息可以在适当的地方使用。填充 |v:oldfiles| 变量。标记在启动时不会被读取（但文件标记会）。有关如何在启动时设置 'shada' 选项，请参见 |initialization|。

                                                        *shada-write*
当 Vim 退出且 'shada' 非空时，信息存储在 ShaDa 文件中（实际上是合并到现有的文件中，如果存在一个的话 |shada-merging|）。
'shada' 选项是一个包含有关应存储哪些信息以及应存储多少的限制的字符串（参见 'shada'）。

Unix 的注意事项：
- ShaDa 文件的文件保护将被设置以防止其他用户读取它，因为它可能包含你处理过的任何文本或命令。
- 如果你想与其他用户共享 ShaDa 文件（例如，当你 "su" 到另一个用户时），你可以使该文件对组或所有人可写。Vim 在写入新的 ShaDa 文件时将保留这一点。要小心，不要允许任何人读取和写入你的 ShaDa 文件！
- Vim 不会覆盖当前“真实”用户不可写的 ShaDa 文件。这有助于当你 "su" 成为 root 时，但你的 $HOME 仍然设置为普通用户的家目录。否则，Vim 将创建一个 root 拥有的、其他任何人都无法读取的 ShaDa 文件。
- ShaDa 文件不能是符号链接。这是为了避免安全问题。

标记为每个文件单独存储。当读取文件且 'shada' 非空时，从 ShaDa 文件中读取该文件的标记。注意：标记仅在退出 Vim 时写入，这很好，因为标记会为你在当前编辑会话中打开的所有文件记住，除非使用 ":bdel"。如果你想为即将通过 ":bdel" 放弃的文件保存标记，请使用 ":wsh"。'[' 和 ']' 标记不会被存储，但 '"' 标记会被存储。'"' 标记对于跳转到上次退出文件时的光标位置非常有用。不会为以 'shada' 中 "r" 标志给出的任何字符串开头的文件保存标记。这可用于避免为可移动介质上的文件保存标记（对于 MS-Windows，你会使用 "ra:,rb:"）。
|v:oldfiles| 变量会填充 ShaDa 文件有标记的文件名。

                                                        *shada-file-marks*
大写标记（'A 到 'Z）在写入 ShaDa 文件时存储。编号标记（'0 到 '9）有点特殊。当写入 ShaDa 文件时（退出时或使用 |:wshada| 命令），'0 被设置为当前光标位置和文件。旧的 '0 移到 '1，'1 移到 '2，依此类推。这类似于 "1 到 "9 删除寄存器发生的情况。如果当前光标位置已经存在于 '0 到 '9 中，则将其移到 '0，以避免相同位置出现两次。结果是，使用 "'0"，你可以跳回到你退出 Vim 时的文件和行。要立即执行此操作，请尝试使用以下命令： >

        vim -c "normal '0"

在 C shell 衍生版本中，你可以为其创建一个别名： >

        alias lvim vim -c '"'normal "'"0'"'

对于类似 Bash 的 shell： >

        alias lvim='vim -c "normal '\''0"'

使用 'shada' 中的 "r" 标志来指定哪些文件不应记住标记。

合并                                                 *shada-merging*

当使用不带感叹号的 |:wshada| 写入 ShaDa 文件或在常规退出时，现有 ShaDa 文件中的信息将与当前 Nvim 实例中的信息合并。为此，ShaDa 文件存储与 ShaDa 条目关联的时间戳。具体地，执行以下操作：

1. 历史行按时间戳合并。ShaDa 文件中项目的最大数量由 'shada' 选项定义（|shada-/|、|shada-:|、|shada-@| 等：每个代表历史名称的字符一个子选项（|:history|））。
2. 未由 Nvim 打开的文件的本地标记和更改被复制到新的 ShaDa 文件。由 Nvim 打开的文件的标记被合并，对由 Nvim 打开的文件的更改被忽略。|shada-'|
3. 跳转列表被合并：跳转按时间戳排序，相同的跳转（相同的位置和时间戳）被压缩。
4. 搜索模式和替换字符串不会被合并：具有最大时间戳的搜索模式或替换字符串将是唯一复制到 ShaDa 文件的一个。
5. 对于每个寄存器，只保存具有最大时间戳的实体。|shada-<|
6. 所有保存的变量都从当前 Nvim 实例保存。此外，现有的变量值被复制，这意味着从 ShaDa 文件中删除变量的唯一方法是要么手动删除它，要么完全禁用写入变量。|shada-!|
7. 对于每个全局标记，只保存具有最大时间戳的实体。
8. 缓冲区列表和头部是唯一不以任何方式合并的条目：存在的唯一头部和缓冲区列表是来自最后写入文件的 Nvim 实例的那些。|shada-%|

兼容性                                           *shada-compatibility*

ShaDa 文件是向前和向后兼容的。这意味着

1. 具有未知类型（即包含未识别数据）的条目在读取时被忽略，在写入时被盲目复制。
2. 具有未知寄存器名称的寄存器条目在读取时被忽略，在写入时被盲目复制。限制：仅支持使用代码在区间 [1, 255] 内的名称的寄存器。|registers|
3. 具有未知寄存器类型的寄存器条目在读取时被忽略，在写入时正常合并。|getregtype()|
4. 具有未知标记名称的本地和全局标记条目在读取时被忽略。在写入时，全局标记条目被盲目复制，本地标记条目也被盲目复制，但仅当它们附加的文件符合 |shada-'| 限制时。未知本地标记条目的时间戳也被考虑在内，以计算哪些文件应该正好适合此限制。限制：仅支持使用代码在区间 [1, 255] 内的名称的标记。|mark-motions|
5. 具有未知历史类型的历史条目在读取时被忽略，在写入时被盲目复制。限制：最多只能有 256 种历史类型。|history|
6. 在寄存器、本地标记、全局标记、更改、跳转和搜索模式条目中找到的未知键在内部保存，并在写入时转储。在 Nvim 会话期间创建的条目从未有此类附加信息。
7. 在替换字符串和历史条目中找到的附加元素在内部保存并转储。在 Nvim 会话期间创建的条目从未有此类附加信息。
8. 在变量条目中找到的附加元素在读取时被简单地忽略。在写入新变量时，它们将在合并期间被保留，但仅此而已。从当前 Nvim 会话转储的变量值从未有附加元素，即使变量本身是通过读取 ShaDa 文件获得的。

这里的“盲目”意味着不会尝试以任何方式合并它们，即使其他条目（具有已知名称/类型等）被合并。|shada-merging|

SHADA 文件名                                         *shada-file-name*

- |shada| 文件的默认名称是：
      Unix：     "$XDG_STATE_HOME/nvim/shada/main.shada"
      Windows：  "$XDG_STATE_HOME/nvim-data/shada/main.shada"
  另请参见 |base-directories|。
- 要选择不同的文件名，你可以使用：
    - 'shada' 选项中的 "n" 标志。
    - |-i| 启动参数。"NONE" 表示从不读取或写入 shada 文件。也不适用于下面的命令！
    - 'shadafile' 选项。来自 "-i" 参数的值（如果有）存储在 'shadafile' 选项中。
- 对于下面的命令，可以给出另一个文件名，覆盖默认值以及 'shada' 或 "-i" 给出的名称（除非是 NONE）。


手动读取和写入                            *shada-read-write*

有两个命令可以手动读取和写入 ShaDa 文件。这可用于在两个正在运行的 Vim 程序之间交换寄存器：首先在一个中键入 ":wsh"，然后在另一个中键入 ":rsh"。注意，如果寄存器已包含某些内容，则需要 ":rsh!"。但也要注意，这意味着所有内容都将被第一个 Vim 中的信息覆盖，包括命令行历史等。

ShaDa 文件本身也可以手动编辑，尽管我们建议你从现有的文件开始以掌握正确的格式。你需要了解 MessagePack（或者，更可能的是，找到能够使用它的软件）格式才能做到这一点。这对于创建第二个文件（例如 "~/.my.shada"）可能很有用，该文件可以包含你第一次启动 Nvim 时始终想要的某些设置。例如，你可以用特定数据预加载寄存器，或将某些命令放入命令行历史中。你的 |config| 文件中的一行，如 >
        :rshada! ~/.my.shada
可用于加载此信息。你甚至可以为不同类型的文件（例如，C 代码）使用不同的 ShaDa 文件，并根据文件名使用 ":autocmd" 命令加载它们（参见 |:autocmd|）。有关 ShaDa 文件格式的更多信息包含在 |shada-format| 部分中。

                                          *E136* *E929* *shada-error-handling*
某些错误导致 Nvim 留下名为 `{basename}.tmp.X` 的临时文件（X 是 `a` 到 `z` 中的任何空闲字母），而通常它会创建此文件，写入它，然后将 `{basename}.tmp.X` 重命名为 `{basename}`。此类错误包括：

- 使 Nvim 认为正在读取的文件根本不是 ShaDa 文件的错误：
  出于安全原因，非 ShaDa 文件不会被覆盖，以避免意外破坏不相关的文件。这可能发生在例如在 "nvim -R file" 的位置输入 "nvim -i file" 时（是的，至少有人对 Vim 这样做过）。
  此类错误列于 |shada-critical-contents-errors|。
- 如果写入临时文件失败：例如因为剩余空间不足。
- 如果重命名文件失败：例如因为权限不足。
- 如果目标 ShaDa 文件与 Nvim 实例的所有者（用户和组）不同，并且更改它们失败。特定于 Unix，仅当 Nvim 从 root 启动时适用。

在遇到上述错误之一后，不要忘记删除临时文件或用临时文件替换目标文件，否则所有创建 ShaDa 文件的尝试都可能失败并出现 |E929|。如果你在使用 |:wshada| 时遇到其中一个错误（而不是在退出 Nvim 时：即当你有一个正在运行的 Nvim 会话时），你有额外的选项：

- 如果你遇到任何错误（除了写入临时文件失败），你应该考虑的第一件事是：删除现有文件并将其替换为临时文件。即使你有正在运行的 Nvim 实例也要这样做。
- 修复权限和/或文件所有权，释放一些空间，然后再次尝试写入。不要删除现有文件。
- 使用带感叹号的 |:wshada|。在权限错误的情况下没有帮助。如果目标文件实际上是 ShaDa 文件，则在这种情况下可能会丢失一些信息。为了使情况稍好一些，在写入之前使用 |:rshada|，但这仍将丢失当前 Nvim 实例中未打开的任何文件的缓冲区本地标记和更改列表条目。
- 从 shell 中删除目标文件并使用 |:wshada|。后果与使用带感叹号的 |:wshada| 没有不同，但在某些情况下，当你没有写入权限时，"rm -f" 有效。

                                                    *:rsh* *:rshada* *E886*
:rsh[ada][!] [file]     从 ShaDa 文件 [file] 读取（默认值：见上文）。
                        如果给出 [!]，则任何已设置的信息（寄存器、标记、|v:oldfiles| 等）将被覆盖。

                                                    *:wsh* *:wshada* *E137* *E138*
:wsh[ada][!] [file]     写入 ShaDa 文件 [file]（默认值：见上文）。
                        首先读取文件中的信息以在旧信息和新信息之间进行合并。当使用 [!] 时，不首先读取旧信息，只写入内部信息（也禁用 |shada-error-handling| 中描述的安全检查）。如果 'shada' 为空，将写入最多 100 个文件的标记。
                        当你收到错误 "E929：所有 .tmp.X 文件都存在，无法写入 ShaDa 文件！" 时，检查是否遗留了旧的临时文件（例如 ~/.local/state/nvim/shada/main.shada.tmp*）。

                        注意：执行 :wshada 将重置所有 |'quote| 标记。

                                                *:o* *:ol* *:oldfiles*
:o[ldfiles]             列出在 ShaDa 文件中存储了标记的文件。此列表在启动时读取，之后仅通过 `:rshada!` 更改。另请参见 |v:oldfiles|。
                        该编号可与 |c_#<| 一起使用。
                        输出可以使用 |:filter| 过滤，例如： >
                                filter /\.vim/ oldfiles
<                        过滤发生在文件名上。

:bro[wse] o[ldfiles][!]
                        列出文件名，如同使用 |:oldfiles|，然后提示输入一个数字。当数字有效时，编辑列表中的该文件。

                        使用 [!] 来放弃已修改的缓冲区。|abandon|

SHADA 文件格式                                               *shada-format*

ShaDa 文件是 MessagePack 条目的连接。每个条目恰好是四个 MessagePack 对象的连接：

1. 首先是条目的类型。对象类型必须是无符号整数。对象类型不得等于零。
2. 其次是条目时间戳。它也必须是无符号整数。
3. 第三是第四个条目的长度。也是无符号整数，用于快速跳过而无需解析。
4. 第四是实际的条目数据。所有当前使用的 ShaDa 条目都使用容器来保存数据：映射或数组。这些容器中的所有字符串值要么是二进制的（适用于文件名），要么是 UTF-8，但解析器需要预期无效字节可能出现在 UTF-8 字符串中。

   确切格式取决于条目类型：

   条目类型（名称）   条目数据 ~

   - 1（头部）        包含描述生成此 ShaDa 文件的生成器实例的数据的映射。在读取 ShaDa 文件时被忽略。包含以下数据：
                       键       数据 >
                       generator  二进制，用于生成 ShaDa 文件的软件。
                                  当 ShaDa 文件由 Nvim 写入时等于 "nvim"。
                       version    二进制，生成器版本。
                       encoding   二进制，有效的 'encoding' 值。
                       max_kbyte  整数，有效的 |shada-s| 限制值。
                       pid        整数，实例进程 ID。
                       *          允许任意数量的额外键和任何数据。
<
   - 2（搜索模式）    包含描述最后使用的搜索或替换模式的数据的映射。通常 ShaDa 文件包含两个这样的条目：一个将 "ss" 键设置为 true（描述替换模式，参见 |:substitute|），一个设置为 false（描述搜索模式，参见 |search-commands|）。其中一个条目上的 "su" 键应为 true。如果键值等于默认值，则通常不存在。键：
                       键  类型     默认值  描述 >
                       sm  Boolean  true     有效的 'magic' 值。
                       sc  Boolean  false    有效的 'smartcase' 值。
                       sl  Boolean  true     如果搜索模式带有行偏移，则为 true。
                                             参见 |search-offset|。
                       se  Boolean  false    如果 |search-offset| 请求将光标放置在模式末尾（相对于），则为 true。
                       so  Integer  0        偏移值。|search-offset|
                       su  Boolean  false    如果当前条目是最后使用的搜索模式，则为 true。
                       ss  Boolean  false    如果当前条目描述 |:substitute| 模式，则为 true。
                       sh  Boolean  false    如果 |v:hlsearch| 打开，则为 true。
                                             使用 |shada-h| 或 'nohlsearch' 时，此键始终为 false。
                       sp  Binary   N/A      实际模式。必需。
                       sb  Boolean  false    如果搜索方向是向后，则为 true。
                       *   any      none     出于兼容性原因，允许其他键，请参见 |shada-compatibility|。
<
   - 3（子字符串）     包含最后 |:substitute| 替换字符串的数组。包含单个条目：二进制，使用的替换字符串。出于兼容性原因，允许更多条目，请参见 |shada-compatibility|。
   - 4（历史条目）     包含历史中一个条目的数组。应该有两个或三个条目。第一个是历史类型（无符号整数），第二个是历史行（二进制），第三个是分隔符字符（无符号整数，必须在区间 [0, 255] 内）。第三个项目仅对搜索历史有效。可能的历史类型列于 |hist-names|，这里是相应的数字：0 - cmd，1 - search，2 - expr，3 - input，4 - debug。
   - 5（寄存器）       描述一个寄存器的映射（|registers|）。如果键值等于默认值，则通常不存在。键：
                       键  类型              默认值  描述 >
                       rt  UInteger         0       寄存器类型：
                                                   编号  描述 ~
                                                   0    |charwise-register|
                                                   1    |linewise-register|
                                                   2    |blockwise-register|
                       rw  UInteger         0       寄存器宽度。仅对 |blockwise-register| 有效。
                       rc  Array of binary  N/A     寄存器内容。数组中的每个条目代表其自己的行。
                                                   行内的 NUL 字符应根据 |NL-used-for-Nul| 表示为 NL。
                       ru  Boolean          false   未命名寄存器。未命名寄存器是否指向此寄存器。
                       n   UInteger         N/A     寄存器名称：区间 [1, 255] 内的字符代码。
                                                   示例：|quote0| 寄存器的名称为 48（零字符的 ASCII 码）。
                       *   any              none    出于兼容性原因，允许其他键，请参见 |shada-compatibility|。
<
   - 6（变量）         包含两个项目的数组：变量名称（二进制）和变量值（任何对象）。值使用与 |msgpackparse()| 读取时相同的代码转换，|msgpackdump()| 写入时，因此可能会出现 |msgpack-special-dict|。如果超过两个条目，则忽略其余部分（|shada-compatibility|）。
   - 7（全局标记）
   - 8（跳转）
   - 10（本地标记）
   - 11（更改）        包含某些位置描述的映射：
                       条目       位置 >
                       全局标记   全局标记位置。|'A|
                       本地标记   本地标记位置。|'a|
                       跳转       |jumplist| 中的一个位置。
                       更改       |changelist| 中的一个位置。

                       映射中包含的数据：
                       键  类型      默认值  描述 ~
                       l   UInteger  1       位置行号。必须大于零。
                       c   UInteger  0       位置列号。
                       n   UInteger  34 ('"') 标记名称。仅对全局标记和本地标记条目有效。
                       f   Binary    N/A     文件名。必需。
                       *   any       none    出于兼容性原因，允许其他键，请参见 |shada-compatibility|。
<
   - 9（缓冲区列表）   包含映射的数组。数组中的每个映射代表一个缓冲区。可能的键：
                       键  类型      默认值  描述 >
                       l   UInteger  1       位置行号。必须大于零。
                       c   UInteger  0       位置列号。
                       f   Binary    N/A     文件名。必需。
                       *   any       none    出于兼容性原因，允许其他键，请参见 |shada-compatibility|。
<
   - `*`（未知）        出于兼容性原因，允许任何其他条目类型，请参见 |shada-compatibility|。

                                                        *E574* *E575* *E576*
ShaDa 文件中的错误可能有两种类型：
1. E575 表示“逻辑”错误。
2. E576 表示“关键”错误。

写入时，关键错误触发 |shada-error-handling| 中描述的行为。
读取时，关键错误导致跳过文件的其余部分。

关键错误包括：
                                            *shada-critical-contents-errors*
- 前三个 MessagePack 对象中的任何一个不是无符号整数。
- 第三个对象请求的字节数大于 ShaDa 文件中剩余的字节数。
- 类型为零的条目。即第一个对象等于零。
- MessagePack 解析器无法解析条目数据。
- MessagePack 解析器消耗的字节数少于或请求的字节数多于解析第四个对象时第三个对象中描述的字节数。即当第四个对象包含多个一个 MessagePack 对象或者它不包含完整的 MessagePack 对象时。

==============================================================================
标准路径                                  *standard-path*

Nvim 将配置、数据和日志存储在标准位置。强烈建议插件也遵循此模式。使用 |stdpath()| 获取路径。

                                                *base-directories* *xdg*
“基础”（根）目录符合 XDG 基础目录规范。
https://specifications.freedesktop.org/basedir/latest/

如果定义了 $XDG_CONFIG_HOME、$XDG_DATA_HOME、$XDG_RUNTIME_DIR、$XDG_STATE_HOME、$XDG_CACHE_HOME、$XDG_CONFIG_DIRS 和 $XDG_DATA_DIRS 环境变量，则使用它们，否则使用下面列出的默认值。

注意：在帮助中，这些默认值用作占位符，例如 "~/.config" 被理解为 "$XDG_CONFIG_HOME 或 ~/.config"。

配置目录（默认） ~
                  *$XDG_CONFIG_HOME*            Nvim：stdpath("config") >
    Unix：         ~/.config                   ~/.config/nvim
    Windows：      ~/AppData/Local             ~/AppData/Local/nvim

数据目录（默认） ~
                  *$XDG_DATA_HOME*              Nvim：stdpath("data") >
    Unix：         ~/.local/share              ~/.local/share/nvim
    Windows：      ~/AppData/Local             ~/AppData/Local/nvim-data

运行目录（默认） ~
                  *$XDG_RUNTIME_DIR*            Nvim：stdpath("run") >
    Unix：         /tmp/nvim.user/xxx          /tmp/nvim.user/xxx
    Windows：      $TMP/nvim.user/xxx          $TMP/nvim.user/xxx

状态目录（默认） ~
                  *$XDG_STATE_HOME*             Nvim：stdpath("state") >
    Unix：         ~/.local/state              ~/.local/state/nvim
    Windows：      ~/AppData/Local             ~/AppData/Local/nvim-data

缓存目录（默认） ~
                  *$XDG_CACHE_HOME*             Nvim：stdpath("cache") >
    Unix：         ~/.cache                    ~/.cache/nvim
    Windows：      ~/AppData/Local/Temp        ~/AppData/Local/Temp/nvim-data

日志目录（默认） ~
                  *$XDG_STATE_HOME*/.../logs    Nvim：stdpath("log") >
    Unix：         不适用                      ~/.local/state/nvim/logs
    Windows：      不适用                      ~/AppData/Local/nvim-data/logs

日志文件（默认） ~
                  `$NVIM_LOG_FILE`              Nvim：stdpath("log")/nvim.log >
    Unix：         ~/.local/state/nvim         ~/.local/state/nvim/logs/nvim.log
    Windows：      ~/AppData/Local/nvim-data   ~/AppData/Local/nvim-data/logs/nvim.log

额外配置目录（默认） ~
                  *$XDG_CONFIG_DIRS*            Nvim：stdpath("config_dirs") >
    Unix：         /etc/xdg/                   /etc/xdg/nvim
    Windows：      不适用                      不适用

额外数据目录（默认） ~
                  *$XDG_DATA_DIRS*              Nvim：stdpath("data_dirs") >
    Unix：         /usr/local/share            /usr/local/share/nvim
                  /usr/share                  /usr/share/nvim
    Windows：      不适用                      不适用

NVIM_APPNAME                                    *$NVIM_APPNAME*
标准目录可以通过 `$NVIM_APPNAME` 环境变量进一步配置。此变量控制 Nvim 将在每个基础目录中读取（和自动创建）的子目录。例如，在启动之前将 `$NVIM_APPNAME` 设置为 "foo" 将导致 Nvim 在 `$XDG_CONFIG_HOME/foo` 中查找配置文件，而不是在 `$XDG_CONFIG_HOME/nvim` 中。`$NVIM_APPNAME` 必须是一个名称，例如 "foo"，或一个相对路径，例如 "foo/bar"。

注意：在帮助中，无论哪里提到 `$XDG_CONFIG_…/nvim`，都被理解为 `$XDG_CONFIG_…/$NVIM_APPNAME`。

                                                *state-isolation*
$NVIM_APPNAME 的一个用例是“隔离”Nvim 应用程序。或者，为了实现真正的隔离，在 Linux 上你可以使用 cgroups 命名空间： >
    systemd-run --user -qt -p PrivateUsers=yes -p BindPaths=/home/user/profile_xy:/home/user/.config/nvim nvim
<
                                                *stateless*
要在不创建任何目录或数据文件的情况下运行 Nvim： >
    NVIM_LOG_FILE=/dev/null nvim -n -i NONE

日志文件                                        *log* *$NVIM_LOG_FILE* *E5430*
除了 'debug' 和 'verbose'，Nvim 还为内部调试、插件和 RPC 客户端保留一个通用日志文件。日志文件可以通过以下方式查看： >
        :log nvim

默认位置是 stdpath("log")/nvim.log（$XDG_STATE_HOME/nvim/logs/nvim.log），除非该路径不可访问或在 |startup| 之前设置了 $NVIM_LOG_FILE。

有关 Nvim 在运行时或由插件创建的其他日志文件的详细信息，请参见 |log-files|。


 vim:et:tw=78:ts=8:sw=4:ft=help:norl:
