*windows.txt*   Nvim


		  VIM 参考手册	  by Bram Moolenaar


使用多个窗口和缓冲区进行编辑。		*windows* *buffers*

此处说明了为使用多个窗口和缓冲区而添加的命令。此外，还解释了与多个窗口结合使用时行为不同的命令。

基础知识在用户手册第 7 章和第 8 章中解释 |usr_07.txt| |usr_08.txt|。

                                      输入 |gO| 查看目录。

==============================================================================
1. 引言					*windows-intro* *window*

概述：
   缓冲区是文件在内存中的文本。
   窗口是缓冲区的视口。
   标签页是窗口的集合。

窗口是缓冲区的视口。您可以在一个缓冲区上使用多个窗口，也可以在不同缓冲区上使用多个窗口。

缓冲区是加载到内存中以供编辑的文件。原始文件保持不变，直到您将缓冲区写入文件。

缓冲区可以处于三种状态之一：

							*active-buffer*
活动 (active):   缓冲区显示在窗口中。如果此缓冲区有对应的文件，则该文件已被读入缓冲区。此后缓冲区可能已被修改，因此可能与文件不同。
							*hidden-buffer*
隐藏 (hidden):   缓冲区未显示。如果此缓冲区有对应的文件，则该文件已被读入缓冲区。否则，它与活动缓冲区相同，只是您看不到它。
							*inactive-buffer*
非活动 (inactive): 缓冲区未显示且不包含任何内容。如果文件曾经被加载过，则缓冲区的选项会被记住。它可以包含来自 |shada| 文件的标记。但缓冲区不包含文本。

在表格中：

状态		显示在窗口中	已加载		":buffers" 显示 ~
活动		  是		 是		  'a'
隐藏		  否		 是		  'h'
非活动		  否		 否		  ' '

							*buffer-reuse*
每个缓冲区都有一个唯一的编号，该编号在 Vim 会话期间不会更改。|bufnr()| 和 |bufname()| 函数可用于在缓冲区名称和缓冲区编号之间进行转换。有一个例外：如果创建了一个新的空缓冲区且未被修改，则在将另一个文件加载到该缓冲区时，该缓冲区将被重用。这也意味着缓冲区编号不会改变。

主 Vim 窗口可以容纳多个分割窗口。还有标签页 |tab-page|，每个标签页可以容纳多个窗口。

							*focusable*
如果一个窗口是可聚焦的，则它是“导航堆栈”的一部分，也就是说，编辑器命令如 :windo、|CTRL-W| 等会将此窗口视为可以成为“当前窗口”的窗口。不可聚焦的窗口将被此类命令跳过，因为它没有被分配窗口编号。它可以通过 |nvim_set_current_win()| 显式聚焦，因为它仍然被分配了一个 |window-ID|。如果它被聚焦，它也将有一个窗口编号。不可聚焦的窗口不会被 |:tabs| 列出，也不会被默认的 'tabline' 计数。它们的缓冲区内容不包含在 'complete' 的 "w" 补全中。

窗口（尤其是浮动窗口）可以具有许多其他 |api-win_config| 属性，例如 "hide" 和 "fixed"，这些属性也会影响行为。

					*window-ID* *winid* *windowid*
每个窗口都有一个称为窗口 ID 的唯一标识符。此标识符在 Vim 会话期间不会更改。|win_getid()| 和 |win_id2tabwin()| 函数可用于在窗口/标签页编号和标识符之间进行转换。还有窗口编号，每当打开或关闭窗口时，该编号可能会更改，请参见 |winnr()|。
窗口编号仅在一个特定的标签页中有效。窗口 ID 在各个标签页中均有效。对于大多数接受窗口 ID 或窗口编号的函数，窗口编号仅适用于当前标签页，而窗口 ID 可以引用任何标签页中的窗口。


==============================================================================
2. 启动 Vim						*windows-starting*

默认情况下，Vim 启动时只有一个窗口，就像 Vi 一样。

Vim 的 "-o" 和 "-O" 参数可用于为参数列表中的每个文件打开一个窗口。"-o" 参数将水平分割窗口；"-O" 参数将垂直分割窗口。如果同时给出 "-o" 和 "-O"，则使用最后一个遇到的参数来确定分割方向。例如，以下命令将打开三个水平分割的窗口： >
	vim -o file1 file2 file3

"-oN"，其中 N 是一个十进制数，打开 N 个水平分割的窗口。如果文件名多于窗口数，则仅打开 N 个窗口，某些文件没有窗口。如果窗口数多于文件名，则最后几个窗口将编辑空缓冲区。类似地，"-ON" 打开 N 个垂直分割的窗口，具有相同的限制。

如果文件名很多，窗口会变得非常小。您可能需要设置 'winheight' 和/或 'winwidth' 选项来创建一个可行的状态。

打开新窗口和读取文件时，不会执行 Buf/Win Enter/Leave |autocommand|，只有在真正进入它们时才会执行。

							*status-line*
状态行将用于分隔窗口。'laststatus' 选项指示最后一个窗口是否也有状态行：
	'laststatus' = 0	从不显示状态行
	'laststatus' = 1	如果有多个窗口，则显示状态行
	'laststatus' = 2	始终显示状态行
	'laststatus' = 3	在底部有一个全局状态行，而不是每个窗口一个

您可以使用 'statusline' 选项更改状态行的内容。此选项可以是窗口本地的，因此您可以在每个窗口中有不同的状态行。

通常，使用反色显示状态行。这可以通过 |hl-StatusLine| 高亮组进行更改。如果没有为状态行使用高亮，则当前窗口使用 '^' 字符，其他窗口使用 '=' 字符。如果启用了 'mouse'，则可以拖动状态行来调整窗口大小。

							*filler-lines*
窗口中最后一个缓冲区行之后的行称为填充行。默认情况下，这些行以波浪号 (~) 字符开头。'fillchars' 选项中的 "eob" 项可用于更改此字符。默认情况下，这些字符高亮为 NonText (|hl-NonText|)。EndOfBuffer 高亮组 (|hl-EndOfBuffer|) 可用于更改填充字符的高亮。

==============================================================================
3. 打开和关闭窗口				*opening-window*

CTRL-W s						*CTRL-W_s*
CTRL-W S						*CTRL-W_S*
CTRL-W CTRL-S						*CTRL-W_CTRL-S*
:[N]sp[lit] [++opt] [+cmd]				*:sp* *:split*
		将当前窗口一分为二。结果是同一个文件的两个视口。

		使新窗口高度为 N（默认使用当前窗口高度的一半）。减少当前窗口高度以腾出空间（如果设置了 'equalalways' 选项，'eadirection' 不是 "hor"，并且其中一个窗口高于当前窗口或新窗口，则也会减少其他窗口）。

		注意：CTRL-S 并非在所有终端上都有效，可能会阻止进一步输入，请使用 CTRL-Q 恢复。
		另请参见 |++opt| 和 |+cmd|。
							*E242* *E1159*
		在自动命令中分割窗口时要小心，如果这发生在进行其他窗口布局更改时，可能会弄乱窗口布局。

:[N]sp[lit] [++opt] [+cmd] {file}			*:split_f*
		类似于 |:split|，但创建一个新窗口并在其中开始编辑文件 {file}。
		这几乎类似于先执行 ":split"，然后执行 ":edit" 命令，但原始窗口中的备用文件名设置为 {file}。
		如果给出了 [+cmd]，则在文件加载后执行该命令 |+cmd|。
		另请参见 |++opt|。
		使新窗口高度为 N（默认使用现有高度的一半）。减少当前窗口高度以腾出空间（如果设置了 'equalalways' 选项，也会减少其他窗口）。

CTRL-W CTRL-V						*CTRL-W_CTRL-V*
CTRL-W v						*CTRL-W_v*
:[N]vs[plit] [++opt] [+cmd] [file]			*:vs* *:vsplit*
		类似于 |:split|，但垂直分割。窗口将水平展开，如果：
		1. 未指定宽度，
		2. 设置了 'equalalways'，
		3. 'eadirection' 不是 "ver"，并且
		4. 其他窗口之一宽于当前窗口或新窗口。
		如果给出了 N，则尽可能使新窗口宽度为 N 列。
		注意：在其他地方，CTRL-Q 与 CTRL-V 的作用相同，但在这里不是！

CTRL-W n						*CTRL-W_n*
CTRL-W CTRL-N						*CTRL-W_CTRL-N*
:[N]new [++opt] [+cmd]					*:new*
		创建一个新窗口并在其中开始编辑一个空文件。
		使新窗口高度为 N（默认使用现有高度的一半）。减少当前窗口高度以腾出空间（如果设置了 'equalalways' 选项且 'eadirection' 不是 "hor"，也会减少其他窗口）。
		另请参见 |++opt| 和 |+cmd|。
		如果 'fileformats' 不为空，则使用该选项给出的第一个格式作为新缓冲区的格式。如果 'fileformats' 为空，则使用当前缓冲区的 'fileformat'。这可以通过 |++opt| 参数覆盖。
		自动命令按以下顺序执行：
		1. 当前窗口的 WinLeave
		2. 新窗口的 WinEnter
		3. 当前缓冲区的 BufLeave
		4. 新缓冲区的 BufEnter
		这类似于先执行 ":split"，然后执行 ":enew" 命令。

:[N]new [++opt] [+cmd] {file}
		类似于 |:split_f|，创建一个新窗口并开始编辑 {file}。

:[N]vne[w] [++opt] [+cmd] [file]			*:vne* *:vnew*
		类似于 |:new|，但垂直分割。如果设置了 'equalalways' 且 'eadirection' 不是 "ver"，窗口将水平展开，除非指定了宽度。

:[N]sv[iew] [++opt] [+cmd] [file]		*:sv* *:sview* *splitview*
		与 ":split" 相同，但为此缓冲区设置 'readonly' 选项。

:[N]sf[ind] [++opt] [+cmd] {file}	     *:sf* *:sfi* *:sfind* *splitfind*
		与 ":split" 相同，但在 'path' 中搜索 {file}，如同在 |:find| 中一样。如果未找到 {file}，则不进行分割。

CTRL-W CTRL-^					*CTRL-W_CTRL-^* *CTRL-W_^*
CTRL-W ^	将当前窗口一分为二并编辑备用文件。
		当给出计数 N 时，分割当前窗口并编辑缓冲区 N。类似于 ":sp #" 和 ":sp #N"，但它允许备用缓冲区未命名。此命令匹配 |CTRL-^| 的行为，不同之处在于它首先分割一个窗口。

CTRL-W ge						*CTRL-W_ge*
		将当前窗口分离为外部窗口。
		仅在使用支持 |ui-multigrid| 的 UI 时可用。

请注意，'splitbelow' 和 'splitright' 选项会影响新窗口出现的位置。
								*E36*
如果没有足够的空间，创建窗口将失败。每个窗口至少需要一行和一列屏幕空间，有时需要更多。选项 'winminheight' 和 'winminwidth' 与此相关。

						*:vert* *:vertical*
:vert[ical] {cmd}
		执行 {cmd}。如果它包含一个分割窗口的命令，则将垂直分割窗口。对于 `vertical wincmd =`，窗口将仅垂直等宽。
		不适用于 |:execute| 和 |:normal|。

						*:hor* *:horizontal*
:hor[izontal] {cmd}
		在水平分割窗口中执行 {cmd}。支持以下命令：
		- `:wincmd =`：仅水平等宽窗口。
		- |:terminal|：在分割窗口中打开 |terminal| 缓冲区。
		- |:checkhealth|：在分割窗口中打开健康检查缓冲区。

:lefta[bove] {cmd}				*:lefta* *:leftabove*
:abo[veleft] {cmd}				*:abo* *:aboveleft*
		执行 {cmd}。如果它包含一个分割窗口的命令，则它将在当前窗口左侧（垂直分割）或上方（水平分割）打开。覆盖 'splitbelow' 和 'splitright'。
		不适用于 |:execute| 和 |:normal|。

:rightb[elow] {cmd}				*:rightb* *:rightbelow*
:bel[owright] {cmd}				*:bel* *:belowright*
		执行 {cmd}。如果它包含一个分割窗口的命令，则它将在当前窗口右侧（垂直分割）或下方（水平分割）打开。覆盖 'splitbelow' 和 'splitright'。
		不适用于 |:execute| 和 |:normal|。

						*:topleft* *E442*
:to[pleft] {cmd}
		执行 {cmd}。如果它包含一个分割窗口的命令，则它将出现在顶部并占据 Vim 窗口的整个宽度。当分割是垂直的时，窗口出现在最左侧并占据 Vim 窗口的整个高度。
		不适用于 |:execute| 和 |:normal|。

						*:bo* *:botright*
:bo[tright] {cmd}
		执行 {cmd}。如果它包含一个分割窗口的命令，则它将出现在底部并占据 Vim 窗口的整个宽度。当分割是垂直的时，窗口出现在最右侧并占据 Vim 窗口的整个高度。
		不适用于 |:execute| 和 |:normal|。

这些命令修饰符可以组合使用，以使垂直分割的窗口占据整个高度。示例： >
	:vertical topleft split tags
在 Vim 窗口的最左侧打开一个垂直分割、全高的 "tags" 文件窗口。


------------------------------------------------------------------------------
关闭窗口

:q[uit]
:{count}q[uit]						*:count_quit*
CTRL-W q						*CTRL-W_q*
CTRL-W CTRL-Q						*CTRL-W_CTRL-Q*
		不带 {count}：退出当前窗口。如果给出了 {count}，则退出第 {count} 个窗口。
							*edit-window*
		当退出最后一个编辑窗口（不计帮助窗口或预览窗口）时，退出 Vim。

		当设置了 'hidden'，并且当前缓冲区只有一个窗口时，它变为隐藏。当未设置 'hidden'，并且当前缓冲区只有一个窗口，且该缓冲区已被更改时，命令失败。
		（注意：CTRL-Q 并非在所有终端上都有效）。
		如果 [count] 大于最后一个窗口编号，则关闭最后一个窗口： >
		    :1quit  " 退出第一个窗口
		    :$quit  " 退出最后一个窗口
		    :9quit  " 退出最后一个窗口
			    " 如果打开的窗口少于 9 个
		    :-quit  " 退出上一个窗口
		    :+quit  " 退出下一个窗口
		    :+2quit " 退出下下一个窗口
<
		当关闭一个帮助窗口，并且这不是唯一的窗口时，Vim 将尝试恢复之前的窗口布局，参见 |:helpclose|。

:q[uit]!
:{count}q[uit]!
		不带 {count}：退出当前窗口。如果给出了 {count}，则退出第 {count} 个窗口。
		如果这是缓冲区的最后一个窗口，则对该缓冲区的任何更改都将丢失。当退出最后一个窗口（不计帮助窗口）时，退出 Vim。即使设置了 'hidden'，缓冲区的内容也会丢失。

:clo[se][!]
:{count}clo[se][!]
CTRL-W c					*CTRL-W_c* *:clo* *:close*
		不带 {count}：关闭当前窗口。如果给出，则关闭第 {count} 个窗口。

		当设置了 'hidden'，或者缓冲区已被更改且使用了 [!] 时，缓冲区变为隐藏（除非有另一个窗口正在编辑它）。

		当当前标签页中只有一个 |edit-window| 并且还有另一个标签页时，这将关闭当前标签页。|tab-page|。

		此命令在以下情况下失败：			*E444*
		- 屏幕上只有一个窗口。
		- 未设置 'hidden'，未使用 [!]，缓冲区有更改，并且没有其他窗口在此缓冲区上。
		对缓冲区的更改不会被写入，也不会丢失，因此这是一个“安全”的命令。

CTRL-W CTRL-C						*CTRL-W_CTRL-C*
		您可能期望 CTRL-W CTRL-C 会关闭当前窗口，但这不起作用，因为 CTRL-C 会取消该命令。

							*:hide*
:hid[e]
:{count}hid[e]
		不带 {count}：退出当前窗口，除非它是屏幕上的最后一个窗口。
		如果给出了 {count}，则退出第 {count} 个窗口。

		缓冲区变为隐藏（除非有另一个窗口正在编辑它，或者 'bufhidden' 是 `unload`、`delete` 或 `wipe`）。
		如果该窗口是当前标签页中的最后一个窗口，则关闭该标签页。|tab-page|

		'hidden' 的值与此命令无关。
		对缓冲区的更改不会被写入，也不会丢失，因此这是一个“安全”的命令。

:hid[e] {cmd}	在设置 'hidden' 的情况下执行 {cmd}。在 {cmd} 执行后恢复 'hidden' 的先前值。
		示例： >
		    :hide edit Makefile
<		这将编辑 "Makefile"，如果当前缓冲区有任何更改，则将其隐藏。

:on[ly][!]
:{count}on[ly][!]
CTRL-W o						*CTRL-W_o* *E445*
CTRL-W CTRL-O					*CTRL-W_CTRL-O* *:on* *:only*
		使当前窗口成为屏幕上唯一的窗口。所有其他窗口都将关闭。有关 {count}，请参见上面的 `:quit` 命令 |:count_quit|。

		当设置了 'hidden' 选项时，已关闭窗口中的所有缓冲区将变为隐藏。

		当未设置 'hidden'，但设置了 'autowrite' 选项时，会写入已修改的缓冲区。否则，如果缓冲区已修改的窗口不会被移除，除非给出了 [!]，在这种情况下它们将变为隐藏。但已修改的缓冲区永远不会被丢弃，因此更改不会丢失。

							*:fc* *:fclose*
:[count]fc[lose][!]
		关闭具有最高 zindex 值的 [count] 个浮动窗口。使用 '!' 关闭所有浮动窗口。

==============================================================================
4. 将光标移动到其他窗口			*window-move-cursor*

CTRL-W <Down>					*CTRL-W_<Down>*
CTRL-W CTRL-J					*CTRL-W_CTRL-J* *CTRL-W_j*
CTRL-W j	将光标移动到当前窗口下方的第 N 个窗口。使用光标位置在备选窗口之间进行选择。

CTRL-W <Up>					*CTRL-W_<Up>*
CTRL-W CTRL-K					*CTRL-W_CTRL-K* *CTRL-W_k*
CTRL-W k	将光标移动到当前窗口上方的第 N 个窗口。使用光标位置在备选窗口之间进行选择。

CTRL-W <Left>					*CTRL-W_<Left>*
CTRL-W CTRL-H					*CTRL-W_CTRL-H*
CTRL-W <BS>					*CTRL-W_<BS>* *CTRL-W_h*
CTRL-W h	将光标移动到当前窗口左侧的第 N 个窗口。使用光标位置在备选窗口之间进行选择。

CTRL-W <Right>					*CTRL-W_<Right>*
CTRL-W CTRL-L					*CTRL-W_CTRL-L* *CTRL-W_l*
CTRL-W l	将光标移动到当前窗口右侧的第 N 个窗口。使用光标位置在备选窗口之间进行选择。

CTRL-W w					*CTRL-W_w* *CTRL-W_CTRL-W*
CTRL-W CTRL-W	不带计数：将光标移动到当前窗口下方/右侧的 |focusable| 窗口。如果没有，则转到左上角窗口。带计数：转到第 N 个窗口（从上到下、从左到右编号），跳过不可聚焦的窗口。要获取窗口编号，请参见 |bufwinnr()| 和 |winnr()|。当 N 大于窗口数时，转到最后一个可聚焦窗口。

						*CTRL-W_W*
CTRL-W W	不带计数：将光标移动到当前窗口上方/左侧的 |focusable| 窗口。如果没有，则转到右下角窗口。带计数：转到第 N 个窗口，类似于 CTRL-W w。

CTRL-W t					*CTRL-W_t* *CTRL-W_CTRL-T*
CTRL-W CTRL-T	将光标移动到左上角窗口。

CTRL-W b					*CTRL-W_b* *CTRL-W_CTRL-B*
CTRL-W CTRL-B	将光标移动到右下角窗口。

CTRL-W p					*CTRL-W_p* *CTRL-W_CTRL-P*
CTRL-W CTRL-P	转到上一个（最后访问的）窗口。

						*CTRL-W_P* *E441*
CTRL-W P	转到预览窗口。如果没有预览窗口，这是一个错误。

如果可视模式处于活动状态，并且新窗口不在同一个缓冲区上，则可视模式结束。如果窗口在同一个缓冲区上，则设置光标位置以保持相同的可视区域被选中。

						*:winc* *:wincmd*
这些命令也可以通过 ":wincmd" 执行：

:[count]winc[md] {arg}
:winc[md] [count] {arg}
		类似于执行 CTRL-W [count] {arg}。示例： >
			:wincmd j
<		移动到当前窗口下方的窗口。
		当无法使用普通模式（对于 |CursorHold| 自动命令事件）或者普通模式命令不方便时，此命令很有用。
		计数也可以是窗口编号。示例： >
			:exe nr .. "wincmd w"
<		这将转到窗口 "nr"。

注意：所有 CTRL-W 命令也可以使用 |:wincmd| 执行，适用于那些无法使用普通模式命令或使用不方便的地方（例如，在基于浏览器的终端中）。

==============================================================================
5. 移动窗口				*window-moving*

CTRL-W r				*CTRL-W_r* *CTRL-W_CTRL-R* *E443*
CTRL-W CTRL-R	向下/向右旋转窗口。第一个窗口变成第二个，第二个变成第三个，依此类推。最后一个窗口变成第一个。光标停留在同一个窗口中。
		这仅适用于当前窗口所在的行或列中的窗口。

						*CTRL-W_R*
CTRL-W R	向上/向左旋转窗口。第二个窗口变成第一个，第三个变成第二个，依此类推。第一个窗口变成最后一个。光标停留在同一个窗口中。
		这仅适用于当前窗口所在的行或列中的窗口。

CTRL-W x					*CTRL-W_x* *CTRL-W_CTRL-X*
CTRL-W CTRL-X	不带计数：交换当前窗口与下一个窗口。如果没有下一个窗口，则与上一个窗口交换。
		带计数：交换当前窗口与第 N 个窗口（第一个窗口为 1）。光标放在另一个窗口中。
		当垂直和水平窗口分割混合时，交换仅在当前窗口所在的行或列中的窗口内进行。

以下命令可用于更改窗口布局。例如，当有两个垂直分割的窗口时，CTRL-W K 会将其更改为水平分割的窗口。CTRL-W H 则相反。

						*CTRL-W_K*
CTRL-W K	将当前窗口移动到最顶部，使用屏幕的整个宽度。这类似于 `:topleft split`，不同之处在于它应用于当前窗口，并且不创建新窗口。

						*CTRL-W_J*
CTRL-W J	将当前窗口移动到最底部，使用屏幕的整个宽度。这类似于 `:botright split`，不同之处在于它应用于当前窗口，并且不创建新窗口。

						*CTRL-W_H*
CTRL-W H	将当前窗口移动到最左侧，使用屏幕的整个高度。这类似于 `:vert topleft split`，不同之处在于它应用于当前窗口，并且不创建新窗口。

						*CTRL-W_L*
CTRL-W L	将当前窗口移动到最右侧，使用屏幕的整个高度。这类似于 `:vert botright split`，不同之处在于它应用于当前窗口，并且不创建新窗口。

						*CTRL-W_T*
CTRL-W T	将当前窗口移动到一个新的标签页。如果当前标签页中只有一个窗口，则此操作失败。
		这类似于 `:tab split`，不同之处在于前一个窗口会被关闭。
		当指定计数时，新标签页将在此索引的标签页之前打开。否则，它将在当前标签页之后打开。

==============================================================================
6. 调整窗口大小					*window-resize*

						*CTRL-W_=*
CTRL-W =	使所有窗口（几乎）等高和等宽，但对当前窗口使用 'winheight' 和 'winwidth'。
		设置了 'winfixheight' 的窗口保持其高度，设置了 'winfixwidth' 的窗口保持其宽度。
		要仅垂直等宽（使窗口等高），请使用 `vertical wincmd =`。
		要仅水平等宽（使窗口等宽），请使用 `horizontal wincmd =`。

:res[ize] -N					*:res* *:resize* *CTRL-W_-*
CTRL-W -	将当前窗口高度减少 N（默认为 1）。
		如果在 |:vertical| 之后使用：将宽度减少 N。

:res[ize] +N					*CTRL-W_+*
CTRL-W +	将当前窗口高度增加 N（默认为 1）。
		如果在 |:vertical| 之后使用：将宽度增加 N。

:res[ize] [N]
CTRL-W CTRL-_					*CTRL-W_CTRL-_* *CTRL-W__*
CTRL-W _	将当前窗口高度设置为 N（默认：尽可能高）。

:{winnr}res[ize] [+-]N
		类似于上面的 `:resize`，但将大小应用于窗口 {winnr} 而不是当前窗口。

z{nr}<CR>	将当前窗口高度设置为 {nr}。

						*CTRL-W_<*
CTRL-W <	将当前窗口宽度减少 N（默认为 1）。

						*CTRL-W_>*
CTRL-W >	将当前窗口宽度增加 N（默认为 1）。

:vert[ical] res[ize] [N]			*:vertical-resize* *CTRL-W_bar*
CTRL-W |	将当前窗口宽度设置为 N（默认：尽可能宽）。

您还可以通过用鼠标向上或向下拖动状态行来调整窗口大小。或者向左或向右拖动垂直分隔线。这仅在所使用的 Vim 版本支持鼠标且已设置 'mouse' 选项以启用它时有效。

选项 'winheight' ('wh') 用于设置当前窗口的最小窗口高度。每次另一个窗口成为当前窗口时，都会使用此选项。如果该选项为 '0'，则禁用。将 'winheight' 设置为一个非常大的值，例如 '9999'，以使当前窗口始终填充所有可用空间。将其设置为一个合理的值，例如 '10'，以使在当前窗口中编辑舒适。

等效的 'winwidth' ('wiw') 选项用于设置当前窗口的最小宽度。

当设置选项 'equalalways' ('ea') 时，在分割或关闭窗口后，所有窗口都会自动调整为相同大小。如果您未设置此选项，分割窗口将减小当前窗口的大小，并保持其他窗口不变。关闭窗口时，额外的行将分配给其上方的窗口。

'eadirection' 选项限制了应用 'equalalways' 选项的方向。默认值 "both" 在两个方向上调整大小。当值为 "ver" 时，仅等宽窗口的高度。当您手动调整了垂直分割窗口的大小并希望保持此宽度时，请使用此选项。同样，"hor" 导致仅等宽窗口的宽度。

选项 'cmdheight' ('ch') 用于设置命令行的高度。如果您对长消息的 |hit-enter| 提示感到烦恼，请将此选项设置为 2 或 3。

如果只有一个窗口，调整该窗口的大小也将改变命令行的高度。如果有多个窗口，调整当前窗口的大小也将改变其下方窗口的高度（有时也改变其上方窗口的高度）。

窗口的最小高度和宽度通过 'winminheight' 和 'winminwidth' 设置。这些是硬性值，窗口永远不会变得比它们小。


WinScrolled 和 WinResized 自动命令 ~
						*win-scrolled-resized*
如果您想收到窗口大小更改的通知，可以使用 |WinResized| 自动命令事件。
如果您想收到窗口中文本垂直或水平滚动的通知，可以使用 |WinScrolled| 自动命令事件。窗口大小更改时也会触发此事件。
例外：当文本因 'incsearch' 而滚动时，不会触发这些事件。
							*WinResized-event*
|WinResized| 事件在更新显示后触发，此时可能有几个窗口更改了大小。自上次以来更改的窗口 ID 列表在 v:event.windows 变量中提供，例如：
	[1003, 1006]
							*WinScrolled-event*
|WinScrolled| 事件在 |WinResized| 之后触发，并且如果窗口滚动也会触发。这可能是垂直滚动（窗口顶部的文本已更改）或水平滚动（当 'wrap' 关闭或第一行显示的第一部分更改时）。请注意，|WinScrolled| 触发的次数比 |WinResized| 多得多，可能会稍微减慢编辑速度。

|WinScrolled| 提供的信息是每个有更改的窗口的一个字典，使用窗口 ID 作为键，以及带有键 "all" 的更改总数。|v:event| 的示例值： >
	{
	   all:  {width: 0, height:  2, leftcol: 0, skipcol: 0, topline: 1, topfill: 0},
	   1003: {width: 0, height: -1, leftcol: 0, skipcol: 0, topline: 0, topfill: 0},
	   1006: {width: 0, height:  1, leftcol: 0, skipcol: 0, topline: 1, topfill: 0},
	}

请注意，"all" 条目具有各个窗口绝对值的累积。

如果您需要有关更改的更多信息，或者您想要“防抖动”事件（不处理每个事件以避免做太多工作），您可能需要使用 `winlayout()` 和 `getwininfo()` 函数。

|WinScrolled| 和 |WinResized| 在添加第一个自动命令时不会触发，仅在第一次滚动或调整大小后才会触发。切换到另一个标签页时它们可能会触发。

执行的命令预计不会导致窗口大小或滚动更改。如果仍然发生，事件将很快再次触发。换句话说：就在触发事件之前，当前的大小和滚动位置会被存储，并用于判断是否发生了更改。

==============================================================================
7. 参数和缓冲区列表命令			*buffer-list*

      args list		       buffer list	   含义 ~
>
 1. :[N]argument [N]	11. :[N]buffer [N]	到第 N 个 arg/buf
 2. :[N]next [file ..]	12. :[N]bnext [N]	到第 N 个下一个 arg/buf
 3. :[N]Next [N]	13. :[N]bNext [N]	到第 N 个上一个 arg/buf
 4. :[N]previous [N]	14. :[N]bprevious [N]	到第 N 个上一个 arg/buf
 5. :rewind / :first	15. :brewind / :bfirst	到第一个 arg/buf
 6. :last		16. :blast		到最后一个 arg/buf
 7. :all		17. :ball		编辑所有 args/buffers
 			18. :unhide		编辑所有已加载的缓冲区
 			19. :[N]bmod [N]	到第 N 个修改的 buf
<
  split & args list	   split & buffer list	   含义 ~
>
 21. :[N]sargument [N]   31. :[N]sbuffer [N]	分割 + 到第 N 个 arg/buf
 22. :[N]snext [file ..] 32. :[N]sbnext [N]      分割 + 到第 N 个下一个 arg/buf
 23. :[N]sNext [N]       33. :[N]sbNext [N]      分割 + 到第 N 个上一个 arg/buf
 24. :[N]sprevious [N]   34. :[N]sbprevious [N]  分割 + 到第 N 个上一个 arg/buf
 25. :srewind / :sfirst	 35. :sbrewind / :sbfirst 分割 + 到第一个 arg/buf
 26. :slast		 36. :sblast		分割 + 到最后一个 arg/buf
 27. :sall		 37. :sball		编辑所有 args/buffers
 			 38. :sunhide		编辑所有已加载的缓冲区
 			 39. :[N]sbmod [N]	分割 + 到第 N 个修改的 buf

 40. :args		参数列表
 41. :buffers		缓冲区列表
<
[N] 的含义取决于命令：
 [N] 是在 2/12/22/32、3/13/23/33 和 4/14/24/34 上前进/后退的缓冲区数量
 [N] 是参数编号，默认为当前参数，用于 1 和 21
 [N] 是缓冲区编号，默认为当前缓冲区，用于 11 和 31
 [N] 是 19 和 39 的计数

注意：":next" 是个例外，因为它必须接受文件名列表以与 Vi 兼容。


------------------------------------------------------------------------------
参数列表和多个窗口

参数列表中的当前位置对于每个窗口可以是不同的。请记住，执行 ":e file" 时，参数列表中的位置保持不变，但您并未编辑该位置的文件。为表示这一点，文件消息（以及标题，如果有）会显示 "(file (N) of M)"，其中 "(N)" 是文件列表中的当前位置，"M" 是文件列表中的文件数。

参数列表中的所有条目都会添加到缓冲区列表中。因此，您也可以通过缓冲区列表命令（如 ":bnext"）访问它们。

:[N]al[l][!] [N]				*:al* *:all* *:sal* *:sall*
:[N]sal[l][!] [N]
		重新排列屏幕，为每个参数打开一个窗口。所有其他窗口都将关闭。当给出计数时，这是要打开的最大窗口数。
		使用 |:tab| 修饰符为每个参数打开一个标签页。
		当参数多于 'tabpagemax' 时，其余参数将在最后一个标签页中变成分割窗口。
		当设置了 'hidden' 选项时，已关闭窗口中的所有缓冲区将变为隐藏。
		当未设置 'hidden'，但设置了 'autowrite' 选项时，会写入已修改的缓冲区。否则，如果缓冲区已修改的窗口不会被移除，除非给出了 [!]，在这种情况下它们将变为隐藏。但已修改的缓冲区永远不会被丢弃，因此更改不会丢失。
		[N] 是要打开的最大窗口数。'winheight' 也限制了打开的窗口数（如果前面加了 |:vertical|，则为 'winwidth'）。
		这里不会为新窗口执行 Buf/Win Enter/Leave 自动命令，只有在真正进入它们时才会执行。
		如果在此命令忙碌时自动命令更改了窗口布局，将给出一个错误。 *E249*

:[N]sa[rgument][!] [++opt] [+cmd] [N]			*:sa* *:sargument*
		是 ":split | argument [N]" 的缩写：分割窗口并转到第 N 个参数。但是，当没有这样的参数时，窗口不会分割。另请参见 |++opt| 和 |+cmd|。

:[N]sn[ext][!] [++opt] [+cmd] [file ..]			*:sn* *:snext*
		是 ":split | [N]next" 的缩写：分割窗口并转到第 N 个下一个参数。但是，当没有下一个文件时，窗口不会分割。另请参见 |++opt| 和 |+cmd|。

:[N]spr[evious][!] [++opt] [+cmd] [N]			*:spr* *:sprevious*
:[N]sN[ext][!] [++opt] [+cmd] [N]			*:sN* *:sNext*
		是 ":split | [N]Next" 的缩写：分割窗口并转到第 N 个上一个参数。但是，当没有上一个文件时，窗口不会分割。另请参见 |++opt| 和 |+cmd|。

						*:sre* *:srewind*
:sre[wind][!] [++opt] [+cmd]
		是 ":split | rewind" 的缩写：分割窗口并转到第一个参数。但是，当没有参数列表时，窗口不会分割。另请参见 |++opt| 和 |+cmd|。

						*:sfir* *:sfirst*
:sfir[st] [++opt] [+cmd]
		与 ":srewind" 相同。

						*:sla* *:slast*
:sla[st][!] [++opt] [+cmd]
		是 ":split | last" 的缩写：分割窗口并转到最后一个参数。但是，当没有参数列表时，窗口不会分割。另请参见 |++opt| 和 |+cmd|。

						*:dr* *:drop*
:dr[op] [++opt] [+cmd] {file} ..
		在窗口中编辑第一个 {file}。
		- 如果文件已经在某个窗口中打开，则切换到该窗口。
		- 如果文件未在窗口中打开，则在当前窗口中编辑该文件。如果无法 |abandon| 当前缓冲区，则首先分割窗口。
		- 如果可能，将关闭不在参数列表中或不处于全宽状态的窗口。
		设置 |argument-list|，就像使用 |:next| 命令一样。
		此命令的目的是可供希望 Vim 编辑另一个文件的程序（例如调试器）使用。
		当使用 |:tab| 修饰符时，每个参数都在一个标签页中打开。如果最后一个窗口为空，则使用它。
		另请参见 |++opt| 和 |+cmd|。

==============================================================================
8. 在所有缓冲区或窗口中执行命令			*list-repeat*

							*:windo*
:[range]windo {cmd}	在每个 |focusable| 窗口中执行 {cmd}，或者仅对给定窗口编号 [range] 中的窗口执行。它的工作方式类似于执行以下操作： >
				CTRL-W t
				:{cmd}
				CTRL-W w
				:{cmd}
				等等。
<		这仅在当前标签页中操作。
		当在一个窗口上检测到错误时，将不会访问后续窗口。
		最后一个窗口（或发生错误的窗口）成为当前窗口。
		{cmd} 可以包含 '|' 以连接多个命令。
		{cmd} 不得打开或关闭窗口，也不得重新排序它们。

		另请参见 |:tabdo|、|:argdo|、|:bufdo|、|:cdo|、|:ldo|、|:cfdo| 和 |:lfdo|。

							*:bufdo*
:[range]bufdo[!] {cmd}	在缓冲区列表中的每个缓冲区中执行 {cmd}，或者如果给出了 [range]，则仅对缓冲区编号在 [range] 内的缓冲区执行。它的工作方式类似于执行以下操作： >
				:bfirst
				:{cmd}
				:bnext
				:{cmd}
				等等。
<		当无法 |abandon| 当前文件且未给出 [!] 时，命令失败。
		当在一个缓冲区上检测到错误时，将不会访问后续缓冲区。
		跳过未列出的缓冲区。
		最后一个缓冲区（或发生错误的缓冲区）成为当前缓冲区。
		{cmd} 可以包含 '|' 以连接多个命令。
		{cmd} 不得删除缓冲区或向缓冲区列表添加缓冲区。
		注意：执行此命令时，语法自动命令事件会通过将其添加到 'eventignore' 而被禁用。这大大加快了每个缓冲区的编辑速度。

		另请参见 |:tabdo|、|:argdo|、|:windo|、|:cdo|、|:ldo|、|:cfdo| 和 |:lfdo|。

示例： >

	:windo set nolist foldcolumn=0 | normal! zn

这将在所有窗口中重置 'list' 选项并禁用折叠。 >

	:bufdo set fileencoding= | update

这将在每个缓冲区中重置 'fileencoding'，如果这更改了缓冲区，则写入它。结果是所有缓冲区都将使用 'encoding' 编码（如果转换成功）。

==============================================================================
9. 光标下的标签或文件名			*window-tag*

							*:sta* *:stag*
:sta[g][!] [tagname]
		执行 ":tag[!] [tagname]" 并为找到的标签分割窗口。有关跳转到垂直分割窗口或新标签页中的标签，请参阅 'switchbuf'。另请参见 |:tag|。

CTRL-W ]					*CTRL-W_]* *CTRL-W_CTRL-]*
CTRL-W CTRL-]	将当前窗口一分为二。使用光标下的标识符作为标签，并在新的上方窗口中跳转到它。
		在可视模式下，使用可视选择的文本作为标签。
		使新窗口高度为 N。有关跳转到垂直分割窗口或新标签页中的标签，请参阅 'switchbuf'。

							*CTRL-W_g]*
CTRL-W g ]	将当前窗口一分为二。使用光标下的标识符作为标签，并在新的上方窗口中对它执行 ":tselect"。
		在可视模式下，使用可视选择的文本作为标签。
		使新窗口高度为 N。

							*CTRL-W_g_CTRL-]*
CTRL-W g CTRL-]	将当前窗口一分为二。使用光标下的标识符作为标签，并在新的上方窗口中对它执行 ":tjump"。
		在可视模式下，使用可视选择的文本作为标签。
		使新窗口高度为 N。

CTRL-W f					*CTRL-W_f* *CTRL-W_CTRL-F*
CTRL-W CTRL-F	将当前窗口一分为二。编辑光标下的文件名。
		类似于 ":split gf"，但如果文件不存在，则不分割窗口。
		使用 'path' 变量作为查找文件的目录名列表。还使用当前文件的路径来搜索文件名。
		如果名称是看起来像 "type://machine/path" 的超文本链接，则仅使用 "/path"。
		如果给出了计数，则编辑第 count 个匹配的文件。

CTRL-W F						*CTRL-W_F*
		将当前窗口一分为二。编辑光标下的文件名，并跳转到文件名后的行号。有关如何获取行号的详细信息，请参见 |gF|。

CTRL-W gf						*CTRL-W_gf*
		打开一个新标签页并编辑光标下的文件名。类似于 "tab split" 和 "gf"，但如果文件不存在，则不创建新标签页。

CTRL-W gF						*CTRL-W_gF*
		打开一个新标签页，编辑光标下的文件名，并跳转到文件名后的行号。类似于 "tab split" 和 "gF"，但如果文件不存在，则不创建新标签页。

CTRL-W gt						*CTRL-W_gt*
		转到下一个标签页，与 `gt` 相同。

CTRL-W gT						*CTRL-W_gT*
		转到上一个标签页，与 `gT` 相同。

另请参见 |CTRL-W_CTRL-I|：为包含光标下关键字的包含文件打开窗口。

==============================================================================
10. 预览窗口				*preview-window*

预览窗口是一个特殊的窗口，用于显示（预览）另一个文件。它通常是一个小窗口，用于显示包含文件或函数定义。

每个标签页只能有一个预览窗口。它由以下命令之一创建。可以设置 'previewheight' 选项来指定打开预览窗口时的高度。'previewwindow' 选项在预览窗口中设置，以便能够识别它。'winfixheight' 选项被设置，以便在打开/关闭其他窗口时保持其高度不变。

						*:pt* *:ptag*
:pt[ag][!] [tagname]
		执行 ":tag[!] [tagname]"，并在 "预览" 窗口中显示找到的标签，而不更改当前缓冲区或光标位置。如果 "预览" 窗口已存在，则重用（类似于帮助窗口）。如果打开一个新窗口，则使用 'previewheight' 作为窗口的高度。另请参见 |:tag|。
		有关示例，请参见下文。|CursorHold-example|
		与 |:tag| 的小区别：当 [tagname] 等于已显示的标签时，匹配标签列表中的位置不会重置。这使得 CursorHold 示例在 |:ptnext| 之后也能工作。

CTRL-W z					*CTRL-W_z*
CTRL-W CTRL-Z					*CTRL-W_CTRL-Z* *:pc* *:pclose*
:pc[lose][!]	关闭任何当前打开的 "预览" 窗口。当设置了 'hidden' 选项，或者缓冲区已更改且使用了 [!] 时，缓冲区将变为隐藏（除非有另一个窗口正在编辑它）。如果任何 "预览" 缓冲区无法关闭，则命令失败。另请参见 |:close|。

							*:pp* *:ppop*
:[count]pp[op][!]
		在预览窗口中执行 ":[count]pop[!]"。参见 |:pop| 和 |:ptag|。

CTRL-W }						*CTRL-W_}*
		使用光标下的标识符作为标签，并对其执行 :ptag。使新的预览窗口（如果需要）高度为 N。如果未给出 N，则使用 'previewheight'。

CTRL-W g }						*CTRL-W_g}*
		使用光标下的标识符作为标签，并对其执行 :ptjump。使新的预览窗口（如果需要）高度为 N。如果未给出 N，则使用 'previewheight'。

							*:pb* *:pbuffer*
:[N]pb[uffer][!] [+cmd] [N]
		在预览窗口中编辑缓冲区列表中的缓冲区 [N]。如果未给出 [N]，则继续编辑当前缓冲区。有关 [!]，请参见 |:buffer-!|。这也会编辑不在缓冲区列表中的缓冲区，而不设置 'buflisted' 标志。
		另请参见 |+cmd|。

							*:ped* *:pedit*
:ped[it][!] [++opt] [+cmd] {file}
		在预览窗口中编辑 {file}。预览窗口像 |:ptag| 一样打开。当前窗口和光标位置不会更改。有用的示例： >
			:pedit +/fputc /usr/include/stdio.h
<
		另请参见 |++opt| 和 |+cmd|。

							*:ps* *:psearch*
:[range]ps[earch][!] [count] [/]pattern[/]
		类似于 |:ijump|，但在预览窗口中显示找到的匹配项。预览窗口像 |:ptag| 一样打开。当前窗口和光标位置不会更改。有用的示例： >
			:psearch popen
<		与 |:ptag| 命令一样，您可以使用它自动显示光标下单词的信息。这不如使用 |:ptag| 聪明，但您不需要 tags 文件，它也会在系统包含文件中找到匹配项。示例： >
  :au! CursorHold *.[ch] ++nested exe "silent! psearch " .. expand("<cword>")
<		警告：这可能很慢。

示例						*CursorHold-example*  >

  :au! CursorHold *.[ch] ++nested exe "silent! ptag " .. expand("<cword>")

这将在光标在 'updatetime' 设置的时间内未移动时，对光标下的关键字执行 ":ptag"。"++nested" 使其他自动命令得以执行，以便语法高亮在预览窗口中工作。"silent!" 避免了当找不到标签时的错误消息。另请参见 |CursorHold|。要再次禁用它： >

  :au! CursorHold

一个不错的补充是突出显示找到的标签，在光标下没有单词时避免 ":ptag"，以及其他一些事情： >

  :au! CursorHold *.[ch] ++nested call PreviewWord()
  :func PreviewWord()
  :  if &previewwindow			" 不要在预览窗口中执行此操作
  :    return
  :  endif
  :  let w = expand("<cword>")		" 获取光标下的单词
  :  if w =~ '\a'			" 如果单词包含字母
  :
  :    " 在显示另一个标签之前删除任何现有的高亮
  :    silent! wincmd P			" 跳转到预览窗口
  :    if &previewwindow		" 如果我们真的到达那里...
  :      match none			" 删除现有的高亮
  :      wincmd p			" 返回到旧窗口
  :    endif
  :
  :    " 尝试显示光标下单词的匹配标签
  :    try
  :       exe "ptag " .. w
  :    catch
  :      return
  :    endtry
  :
  :    silent! wincmd P			" 跳转到预览窗口
  :    if &previewwindow		" 如果我们真的到达那里...
  :	 if has("folding")
  :	   silent! .foldopen		" 不想要折叠关闭
  :	 endif
  :	 call search("$", "b")		" 到前一行的末尾
  :	 let w = substitute(w, '\\', '\\\\', "")
  :	 call search('\<\V' .. w .. '\>')	" 将光标定位在匹配项上
  :	 " 在此位置为单词添加匹配高亮
  :      hi previewWord term=bold ctermbg=green guibg=green
  :	 exe 'match previewWord "\%' .. line(".") .. 'l\%' .. col(".") .. 'c\k*"'
  :      wincmd p			" 返回到旧窗口
  :    endif
  :  endif
  :endfun

==============================================================================
11. 使用隐藏缓冲区				*buffer-hidden*

隐藏缓冲区不显示在窗口中，但仍加载到内存中。这使得可以在文件之间跳转，而无需每次在窗口中获取另一个缓冲区时读取或写入文件。

							*:buffer-!*
如果设置了 'hidden' ('hid') 选项，则对于所有开始编辑另一个文件的命令（":edit"、":next"、":tag" 等），被放弃的缓冲区将被保留。有时，即使未设置 'hidden' 选项，在缓冲区列表中移动的命令也会使当前缓冲区隐藏。当缓冲区被修改，但被强制（使用 '!'）从窗口中移除，并且 'autowrite' 关闭或缓冲区无法写入时，会发生这种情况。

您可以通过使用任何命令开始编辑隐藏缓冲区来使其不再隐藏，或者使用 ":bdelete" 命令将其删除。

'hidden' 是全局的，它适用于所有缓冲区。可以使用 'bufhidden' 选项为特定缓冲区设置例外。它可以采用以下值：
	<empty>		使用 'hidden' 的值。
	hide		隐藏此缓冲区，即使未设置 'hidden'。
	unload		不隐藏，而是卸载此缓冲区，即使设置了 'hidden'。
	delete		删除缓冲区。

							*hidden-quit*
当您尝试退出 Vim 时，如果存在隐藏的、已修改的缓冲区，您将收到一条错误消息，并且 Vim 将使该缓冲区成为当前缓冲区。然后，您可以选择写入此缓冲区 (":wq") 或不写入退出 (":q!")。注意：可能还有更多隐藏的、已修改的缓冲区！

缓冲区也可以未列出。这意味着它存在，但不在缓冲区列表中。|unlisted-buffer|


:files[!] [flags]				*:files*
:buffers[!] [flags]				*:buffers* *:ls*
:ls[!] [flags]
		显示所有缓冲区。示例：

			1 #h   "/test/text"		line 1 ~
			2u     "asdf"			line 0 ~
			3 %a + "version.c"		line 1 ~

		当包含 [!] 时，列表将显示未列出的缓冲区（术语“unlisted”此时有点令人困惑...）。

		每个缓冲区都有一个唯一的编号。该编号不会更改，因此您始终可以使用 ":buffer N" 或 "N CTRL-^" 转到特定缓冲区，其中 N 是缓冲区编号。

		对于文件名，使用这些特殊值：
			"[Prompt]"	|prompt-buffer|
			"[Scratch]"	'buftype' 是 "nofile"
			"[No Name]"	未指定文件名

		指示符（同一列中的字符互斥）：
		u	未列出的缓冲区（仅当使用 [!] 时才显示）
			   |unlisted-buffer|
		 %	当前窗口中的缓冲区
		 #	":e #" 和 CTRL-^ 的备用缓冲区
		  a	活动缓冲区：已加载且可见
		  h	隐藏缓冲区：已加载，但当前未显示在窗口中 |hidden-buffer|
		   `-`	'modifiable' 关闭的缓冲区
		   =	只读缓冲区
		   R	具有正在运行作业的终端缓冲区
		   F	具有已完成作业的终端缓冲区
		   ?    没有作业的终端缓冲区：`:terminal NONE`
		    +	已修改的缓冲区
		    x   有读取错误的缓冲区

		[flags] 可以是以下字符的组合，用于限制列出的缓冲区：
			+	已修改的缓冲区
			`-`	'modifiable' 关闭的缓冲区
			=	只读缓冲区
			a	活动缓冲区
			u	未列出的缓冲区（覆盖 "!"）
			h	隐藏缓冲区
			x	有读取错误的缓冲区
			%	当前缓冲区
			#	备用缓冲区
			R	具有正在运行作业的终端缓冲区
			F	具有已完成作业的终端缓冲区
			t	显示上次使用的时间并对缓冲区进行排序
		组合标志意味着它们被“与”在一起，例如：
			h+	已修改的隐藏缓冲区
			a+	已修改的活动缓冲区

		当使用 |:filter| 时，模式与显示的缓冲区名称匹配，例如： >
			filter /\.vim/ ls
<
						*:bad* *:badd*
:bad[d]	[+lnum] {fname}
		将文件名 {fname} 添加到缓冲区列表中，而不加载它（如果尚未列出）。如果缓冲区之前被删除（未清除），则它将再次被列出。
		如果指定了 "lnum"，则在首次进入缓冲区时光标将定位在该行。请注意，+ 后面的其他命令将被忽略。

						 *:balt*
:balt [+lnum] {fname}
		类似于 `:badd`，同时将当前窗口的备用文件设置为 {fname}。

:[N]bd[elete][!]			*:bd* *:bdel* *:bdelete* *E516*
:bd[elete][!] [N]
		卸载缓冲区 [N]（默认：当前缓冲区）并将其从缓冲区列表中删除。如果缓冲区已更改，此操作将失败，除非指定了 [!]，在这种情况下更改将丢失。
		文件保持不变。此缓冲区的所有窗口都将关闭。如果缓冲区 [N] 是当前缓冲区，则将显示另一个缓冲区。这是跳转列表中指向已加载缓冲区的最新条目。

		实际上，缓冲区并未完全删除，它已从缓冲区列表 |unlisted-buffer| 中移除，并且缓冲区的选项值、变量和映射/缩写将被清除。如果 [N] 是窗口中的最后一个列出的缓冲区（即，没有其他列出的缓冲区可以切换），则缓冲区将被清空而不是卸载。窗口不会关闭，缓冲区可能会被重用为新缓冲区 |buffer-reuse|。这确保了每个窗口始终有一个有效的缓冲区。

		示例： >
		    :.,$-bdelete    " 从当前缓冲区删除到倒数第二个缓冲区
		    :%bdelete	    " 删除所有缓冲区
<

:bd[elete][!] {bufname}						*E93* *E94*
		类似于 ":bdelete[!] [N]"，但缓冲区由名称给出，参见 |{bufname}|。

:bd[elete][!] N1 N2 ...
		对缓冲区 N1、N2 等执行 ":bdelete[!]"。参数可以是缓冲区编号或缓冲区名称（但不能是数字形式的缓冲区名称）。在缓冲区名称中的空格前插入反斜杠。

:N,Mbd[elete][!]
		对范围 N 到 M 内的所有缓冲区执行 ":bdelete[!]" |inclusive|。

:[N]bw[ipeout][!]			*:bw* *:bwipe* *:bwipeout* *E517*
:bw[ipeout][!] {bufname}
:N,Mbw[ipeout][!]
:bw[ipeout][!] N1 N2 ...
		类似于 |:bdelete|，但真正删除缓冲区。与缓冲区相关的所有内容都会丢失。此缓冲区中的所有标记都将失效，选项设置将丢失，跳转列表和标签栈数据将被清除等。除非您知道自己在做什么，否则不要使用此命令。示例： >
		    :.+,$bwipeout   " 清除当前缓冲区之后的所有缓冲区
		    :%bwipeout	    " 清除所有缓冲区
<

:[N]bun[load][!]				*:bun* *:bunload* *E515*
:bun[load][!] [N]
		卸载缓冲区 [N]（默认：当前缓冲区）。将释放为此缓冲区分配的内存。缓冲区保留在缓冲区列表中。
		如果缓冲区已更改，此操作将失败，除非指定了 [!]，在这种情况下更改将丢失。
		此缓冲区的所有窗口都将关闭。如果缓冲区 [N] 是当前缓冲区，则将显示另一个缓冲区。这是跳转列表中指向已加载缓冲区的最新条目。

:bun[load][!] {bufname}
		类似于 ":bunload[!] [N]"，但缓冲区由名称给出。
		另请参见 |{bufname}|。

:N,Mbun[load][!]
		对范围 N 到 M 内的所有缓冲区执行 ":bunload[!]" |inclusive|。

:bun[load][!] N1 N2 ...
		对缓冲区 N1、N2 等执行 ":bunload[!]"。参数可以是缓冲区编号或缓冲区名称（但不能是数字形式的缓冲区名称）。在缓冲区名称中的空格前插入反斜杠。

:[N]b[uffer][!] [+cmd] [N]		*:b* *:bu* *:buf* *:buffer* *E86*
		从缓冲区列表编辑缓冲区 [N]。如果未给出 [N]，则继续编辑当前缓冲区。有关 [!]，请参见 |:buffer-!|。这也会编辑不在缓冲区列表中的缓冲区，而不设置 'buflisted' 标志。
		另请参见 |+cmd|。

:[N]b[uffer][!] [+cmd] {bufname}				*{bufname}*
		从缓冲区列表编辑 {bufname} 的缓冲区。部分名称也有效，只要它在缓冲区列表中是唯一的。
		请注意，名称为数字的缓冲区不能通过该名称引用；请改用缓冲区编号。如果缓冲区名称以 `+` 开头，同样如此，它将被解释为 |+cmd| 的开始。
		在缓冲区名称中的空格前插入反斜杠。
		有关 [!]，请参见 |:buffer-!|。
		这也会编辑不在缓冲区列表中的缓冲区，而不设置 'buflisted' 标志。
		另请参见 |+cmd|。

:[N]sb[uffer] [+cmd] [N]				*:sb* *:sbuffer*
		分割窗口并从缓冲区列表编辑缓冲区 [N]。如果未给出 [N]，则编辑当前缓冲区。分割时遵守 'switchbuf' 的 "useopen" 设置。这也会编辑不在缓冲区列表中的缓冲区，而不设置 'buflisted' 标志。
		另请参见 |+cmd|。

:[N]sb[uffer] [+cmd] {bufname}
		分割窗口并从缓冲区列表编辑 |{bufname}| 的缓冲区。这也会编辑不在缓冲区列表中的缓冲区，而不设置 'buflisted' 标志。
		注意：如果您想做的是分割缓冲区，并以另一个名称复制，可以通过以下方式完成： >
			:w foobar | sp #
<		另请参见 |+cmd|。

:[N]bn[ext][!] [+cmd] [N]				*:bn* *:bnext* *E87*
		转到缓冲区列表中的第 [N] 个下一个缓冲区。[N] 默认为 1。
		绕缓冲区列表末尾循环。
		有关 [!]，请参见 |:buffer-!|。
		另请参见 |+cmd|。
		如果您在帮助缓冲区中，这将带您到下一个帮助缓冲区（如果有）。类似地，如果您在普通（非帮助）缓冲区中，这将带您到下一个普通缓冲区。这样，如果您调用了帮助，当您浏览代码/文本缓冲区时，它不会妨碍您。接下来的三个命令也是如此。

								*]b*
]b		映射到 |:bnext|。|default-mappings|

							*:sbn* *:sbnext*
:[N]sbn[ext] [+cmd] [N]
		分割窗口并转到缓冲区列表中的第 [N] 个下一个缓冲区。
		绕缓冲区列表末尾循环。使用 'switchbuf'。
		另请参见 |+cmd|。

:[N]bN[ext][!] [+cmd] [N]		*:bN* *:bNext* *:bp* *:bprevious* *E88*

								*[b*
[b		映射到 |:bprevious|。|default-mappings|

:[N]bp[revious][!] [+cmd] [N]
		转到缓冲区列表中的第 [N] 个上一个缓冲区。[N] 默认为 1。
		绕缓冲区列表开头循环。
		有关 [!] 和 'switchbuf'，请参见 |:buffer-!|。
		另请参见 |+cmd|。

:[N]sbN[ext] [+cmd] [N]			*:sbN* *:sbNext* *:sbp* *:sbprevious*
:[N]sbp[revious] [+cmd] [N]
		分割窗口并转到缓冲区列表中的第 [N] 个上一个缓冲区。
		绕缓冲区列表开头循环。
		使用 'switchbuf'。
		另请参见 |+cmd|。

:br[ewind][!] [+cmd]					*:br* *:bre* *:brewind*
		转到缓冲区列表中的第一个缓冲区。如果缓冲区列表为空，则转到第一个未列出的缓冲区。
		有关 [!]，请参见 |:buffer-!|。

								*[B*
[B		映射到 |:brewind|。|default-mappings|

:bf[irst] [+cmd]					*:bf* *:bfirst*
		与 |:brewind| 相同。
		另请参见 |+cmd|。

:sbr[ewind] [+cmd]					*:sbr* *:sbrewind*
		分割窗口并转到缓冲区列表中的第一个缓冲区。如果缓冲区列表为空，则转到第一个未列出的缓冲区。
		遵守 'switchbuf' 选项。
		另请参见 |+cmd|。

:sbf[irst] [+cmd]					*:sbf* *:sbfirst*
		与 ":sbrewind" 相同。

:bl[ast][!] [+cmd]					*:bl* *:blast*
		转到缓冲区列表中的最后一个缓冲区。如果缓冲区列表为空，则转到最后一个未列出的缓冲区。
		有关 [!]，请参见 |:buffer-!|。

								*]B*
]B		映射到 |:blast|。|default-mappings|

:sbl[ast] [+cmd]					*:sbl* *:sblast*
		分割窗口并转到缓冲区列表中的最后一个缓冲区。如果缓冲区列表为空，则转到最后一个未列出的缓冲区。
		遵守 'switchbuf' 选项。

:[N]bm[odified][!] [+cmd] [N]			*:bm* *:bmodified* *E84*
		转到第 [N] 个下一个已修改的缓冲区。注意：此命令也会查找未列出的缓冲区。如果没有已修改的缓冲区，则命令失败。

:[N]sbm[odified] [+cmd] [N]				*:sbm* *:sbmodified*
		分割窗口并转到第 [N] 个下一个已修改的缓冲区。
		遵守 'switchbuf' 选项。
		注意：此命令也会查找不在缓冲区列表中的缓冲区。

:[N]unh[ide] [N]			*:unh* *:unhide* *:sun* *:sunhide*
:[N]sun[hide] [N]
		重新排列屏幕，为缓冲区列表中的每个已加载缓冲区打开一个窗口。当给出计数时，这是要打开的最大窗口数。

:[N]ba[ll] [N]					*:ba* *:ball* *:sba* *:sball*
:[N]sba[ll] [N]	重新排列屏幕，为缓冲区列表中的每个缓冲区打开一个窗口。当给出计数时，这是要打开的最大窗口数。'winheight' 也限制了打开的窗口数（如果前面加了 |:vertical|，则为 'winwidth'）。
		这里不会为新窗口执行 Buf/Win Enter/Leave 自动命令，只有在真正进入它们时才会执行。
		当使用 |:tab| 修饰符时，新窗口将在一个新标签页中打开，最多不超过 'tabpagemax'。

注意：上面所有开始编辑另一个缓冲区的命令，都会保持 'readonly' 标志不变。这与 ":edit" 命令不同，后者每次读取文件时都会设置 'readonly' 标志。

==============================================================================
12. 特殊类型的缓冲区			*special-buffers*

除了包含文件文本外，缓冲区还可以用于其他目的。可以设置几个选项来更改缓冲区的行为：
	'bufhidden'	当缓冲区不再显示在窗口中时会发生什么。
	'buftype'	这是什么类型的缓冲区
	'swapfile'	缓冲区是否会有一个交换文件
	'buflisted'	缓冲区是否显示在缓冲区列表中

几种有用的缓冲区类型：

quickfix	用于包含错误列表或位置列表。参见 |:cwindow| 和 |:lwindow|。此命令将 'buftype' 选项设置为 "quickfix"。您不应该更改它！'swapfile' 关闭。

help		包含一个帮助文件。只能通过 |:help| 命令创建。指示帮助缓冲区的标志是内部的，无法更改。帮助缓冲区的 'buflisted' 选项将被重置。

terminal	终端窗口缓冲区，参见 |terminal|。在作业结束之前，无法读取或更改其内容。

directory	显示目录内容。可由文件浏览器插件使用。使用以下设置创建缓冲区： >
			:setlocal buftype=nowrite
			:setlocal bufhidden=delete
			:setlocal noswapfile
<		缓冲区名称是目录的名称，并在使用 |:cd| 命令时进行调整。

						*scratch-buffer*
scratch		包含可以随时丢弃的文本。关闭窗口时会保留，必须显式删除。
		设置： >
			:setlocal buftype=nofile
			:setlocal bufhidden=hide
			:setlocal noswapfile
<		如果您为缓冲区指定一个有意义的名称，则可以使用该名称来标识缓冲区。

						*unlisted-buffer*
unlisted	缓冲区不在缓冲区列表中。它不用于正常编辑，而是用于显示帮助文件、记住文件名或标记。":bdelete" 命令也会设置此选项，因此它不会完全删除缓冲区。
		设置： >
			:setlocal nobuflisted
<
 vim:tw=78:ts=8:noet:ft=help:norl:
