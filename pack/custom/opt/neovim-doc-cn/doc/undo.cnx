*undo.txt*      Nvim


		  VIM 参考手册	  by Bram Moolenaar


撤销和重做						*undo-redo*

基础知识在用户手册的 |02.5| 节中解释。

                                      键入 |gO| 查看目录。

==============================================================================
1. 撤销和重做命令				*undo-commands*

<Undo>		或					*undo* *<Undo>* *u*
u			撤销 [count] 次更改。

							*:u* *:un* *:undo*
:u[ndo]			撤销一次更改。
								*E830*
:u[ndo] {N}		跳转到更改编号 {N} 之后。有关 {N} 的含义，请参见 |undo-branches|。

:u[ndo]!		撤销一次更改并将其从撤消历史记录中移除。
								*E5767*
:u[ndo]! {N}		类似于 ":u[ndo] {N}"，但会忘记当前撤消分支中直到 {N} 的所有更改。您只能使用 ":undo! {N}" 在同一撤消分支中向后移动，不能重做或切换到不同的撤消分支。

							*CTRL-R*
CTRL-R			重做 [count] 次已撤销的更改。

							*:red* *:redo* *redo*
:red[o]			重做一次已撤销的更改。

							*U*
U			撤销一行上的所有最新更改，即发生最新更改的那一行。|U| 本身也算作一次更改，因此 |U| 会撤销之前的 |U|。

最近的更改会被记住。您可以使用上面的撤销和重做命令将文本恢复到每次更改前的状态。您也可以再次应用更改，回到撤销前的文本。

"U" 命令被撤销/重做视为与其他任何命令一样。因此，"u" 命令撤销 "U" 命令，而 'CTRL-R' 命令则重做它。当混合使用 "U"、"u" 和 'CTRL-R' 时，您会注意到 "U" 命令会将一行的情况恢复到上一个 "U" 命令之前的状态。这可能令人困惑。请尝试一下以适应它。
"U" 命令将始终将缓冲区标记为已更改。当 "U" 将缓冲区更改回未更改时的状态时，它仍然被视为已更改。使用 "u" 撤销更改，直到缓冲区变为未更改状态。

==============================================================================
2. 两种撤销方式					*undo-two-ways*

撤销和重做命令的工作方式取决于 'cpoptions' 中的 'u' 标志。有 Vim 方式（排除 'u'）和 Vi 兼容方式（包含 'u'）。
在 Vim 方式中，"uu" 撤销两次更改。在 Vi 兼容方式中，"uu" 什么也不做（撤销一个撤销）。

排除 'u'，Vim 方式：
您可以使用撤销命令回到过去。然后您可以使用重做命令再次前进。如果您在撤销命令之后进行新更改，则重做将不再可能。

包含 'u'，Vi 兼容方式：
撤销命令撤销之前的更改，以及之前的撤销命令。重做命令重复之前的撤销命令。它不会重复更改命令，请使用 "." 来重复更改。

示例		Vim 方式			Vi 兼容方式	~
"uu"		两次撤销			无操作
"u CTRL-R"	无操作			两次撤销

理由：Nvi 使用 "." 命令而不是 CTRL-R。不幸的是，这与 Vi 不兼容。例如，在 Vi 中 "dwdwu." 删除两个单词，而在 Nvi 中它什么也不做。

==============================================================================
3. 撤销块						*undo-blocks*

一个撤销命令通常撤销一个键入的命令，无论该命令进行了多少次更改。这一系列可撤销的更改形成一个撤销块。因此，如果键入的键调用一个函数，则该函数中的所有命令将一起被撤销。

如果您想编写一个不创建新的可撤销更改，而是加入上一次更改的函数或脚本，请使用此命令：

						*:undoj* *:undojoin* *E790*
:undoj[oin]		将后续更改与上一个撤销块合并。
			警告：请小心使用，它可能会阻止用户正确撤销更改。不要在撤销或重做后使用此命令。

当您需要在更改中途提示用户时，这最有用。例如在调用 |getchar()| 的函数中。确保在此之前有一个相关的更改可以与之合并。

这本身不起作用，因为下一次按键将再次开始一个新的更改。但是您可以执行如下操作： >

	:undojoin | delete

之后，"u" 命令将撤销删除命令和之前的更改。
					*undo-break* *undo-close-block*
要执行相反的操作，为下一次更改使用一个新的撤销块，在插入模式下使用 CTRL-G u。如果您希望一个插入命令可以部分撤销，这很有用。例如，为每个句子。|i_CTRL-G_u|

设置 'undolevels' 的值也会关闭撤销块。即使新值等于旧值也是如此。使用 `g:undolevels` 显式读取和写入 'undolevels' 的全局值。 >
	let &g:undolevels = &g:undolevels

请注意，看起来相似的赋值 `let &undolevels=&undolevels` 在局部选项被设置为不同值的情况下，不会保留 'undolevels' 的全局选项值。例如： >
	" 开始为 'undolevels' 设置不同的全局和局部值。
	let &g:undolevels = 1000
	let &l:undolevels = 2000
	" 此赋值将全局选项更改为 2000：
	let &undolevels = &undolevels

==============================================================================
4. 撤销分支				*undo-branches* *undo-tree*

上面我们只讨论了一条撤销/重做线。但也可以分支。当您撤销几次更改然后进行新更改时，就会发生这种情况。被撤销的更改成为一个分支。您可以使用以下命令转到该分支。文件的撤销树可以使用 |undotree-plugin| 可视化和交互式应用。

这在用户手册中解释：|usr_32.txt|。

							*:undol* *:undolist*
:undol[ist]		列出更改树中的叶子。示例：
			   number changes  when               saved ~
			       88      88  2010/01/04 14:25:53
			      108     107  08/07 12:47:51
			      136      46  13:33:01             7
			      166     164  3 seconds ago

			"number" 列是更改编号。此编号不断增加，可用于标识特定的可撤销更改，参见 |:undo|。
			"changes" 列是从树的根到此叶子的更改次数。
			"when" 列是进行此更改的日期和时间。四种可能的格式是：
			    N seconds ago
			    HH:MM:SS             小时，分钟，秒
			    MM/DD HH:MM:SS       同上，包含月份和日期
			    YYYY/MM/DD HH:MM:SS  同上，包含年份
			"saved" 列指定此更改是否已写入磁盘以及它是第几次文件写入。这可以与 |:later| 和 |:earlier| 命令一起使用。
			有关更多详细信息，请使用 |undotree()| 函数。

							*g-*
g-			转到更旧的文本状态。使用计数重复那么多次。
							*:ea* *:earlier*
:ea[rlier] {count}	转到更旧的文本状态 {count} 次。
:ea[rlier] {N}s		转到大约 {N} 秒前的更旧文本状态。
:ea[rlier] {N}m		转到大约 {N} 分钟前的更旧文本状态。
:ea[rlier] {N}h		转到大约 {N} 小时前的更旧文本状态。
:ea[rlier] {N}d		转到大约 {N} 天前的更旧文本状态。

:ea[rlier] {N}f		转到 {N} 次文件写入前的更旧文本状态。
			如果自上次写入以来进行了更改，":earlier 1f" 将使文本恢复到写入时的状态。否则，它将转到更早的写入。
			当处于第一次文件写入的状态，或者文件尚未写入时，":earlier 1f" 将转到第一次更改之前。

							*g+*
g+			转到更新的文本状态。使用计数重复那么多次。
							*:lat* *:later*
:lat[er] {count}	转到更新的文本状态 {count} 次。
:lat[er] {N}s		转到大约 {N} 秒后的更新文本状态。
:lat[er] {N}m		转到大约 {N} 分钟后的更新文本状态。
:lat[er] {N}h		转到大约 {N} 小时后的更新文本状态。
:lat[er] {N}d		转到大约 {N} 天后的更新文本状态。

:lat[er] {N}f		转到 {N} 次文件写入后的更新文本状态。
			当处于最后一次文件写入的状态时，":later 1f" 将转到最新的文本状态。


请注意，当为 'undolevels' 清除撤销信息时，文本状态将变得不可达。

当在时间中移动时，不要惊讶于有时会看到多个更改同时发生。当在撤销树中移动然后进行新更改时，就会发生这种情况。

示例

以以下文本开头：
	one two three ~

通过按三次 "x" 删除第一个单词：
	ne two three ~
	e two three ~
	 two three ~

现在通过按三次 "u" 撤销：
	e two three ~
	ne two three ~
	one two three ~

通过按三次 "x" 删除第二个单词：
	one wo three ~
	one o three ~
	one  three ~

现在通过使用三次 "g-" 撤销：
	one o three ~
	one wo three ~
	 two three ~

您现在回到了第一个撤销分支中，在删除 "one" 之后。重复 "g-" 现在将带您回到原始文本：
	e two three ~
	ne two three ~
	one two three ~

使用 ":later 1h" 跳转到最后一次更改：
	one  three ~

再使用 ":earlier 1h" 回到开头：
	one two three ~


请注意，使用 "u" 和 CTRL-R 不会让您访问所有可能的文本状态，而重复 "g-" 和 "g+" 则可以。

==============================================================================
5. 撤销持久化		*undo-persistence* *persistent-undo*

卸载缓冲区时，Vim 通常会销毁为该缓冲区创建的撤销树。通过设置 'undofile' 选项，Vim 将在您写入文件时自动保存撤消历史记录，并在您再次编辑文件时恢复撤消历史记录。

'undofile' 选项在写入文件之后、BufWritePost 自动命令之前检查。如果您想控制哪些文件写入撤消信息，可以使用 BufWritePre 自动命令： >
	au BufWritePre /tmp/* setlocal noundofile

Vim 将撤销树保存在一个单独的撤消文件中，每个编辑过的文件对应一个，使用一个简单的方案将文件系统路径直接映射到撤消文件。Vim 将检测撤消文件是否与其所针对的文件不再同步（通过文件内容的哈希值），并忽略在撤消文件写入后文件发生更改的撤消文件，以防止损坏。如果撤消文件的所有者与编辑文件的所有者不同，但撤消文件的所有者是当前用户，则也会忽略该撤消文件。打开文件时，设置 'verbose' 以获取有关此消息。

撤消文件的位置由 'undodir' 选项控制，默认情况下它们保存在应用程序数据文件夹中的专用目录中。

您也可以分别使用 ":wundo" 和 ":rundo" 来保存和恢复撤消历史记录：
							*:wu* *:wundo*
:wu[ndo][!] {file}
		将撤消历史记录写入 {file}。
		当 {file} 存在并且看起来不像撤消文件（文件开头的幻数错误）时，除非添加了 !，否则此操作失败。
		如果它存在并且看起来像撤消文件，则会被覆盖。如果没有撤消历史记录，则不会写入任何内容。
		实现细节：覆盖通过首先删除现有文件，然后使用相同名称创建新文件来完成。因此，无法在受写保护的目录中覆盖现有的撤消文件。

							*:rund* *:rundo*
:rund[o] {file}	从 {file} 读取撤消历史记录。

您可以在自动命令中使用这些来显式指定历史文件的名称。例如： >

	au BufReadPost * call ReadUndo()
	au BufWritePost * call WriteUndo()
	func ReadUndo()
	  if filereadable(expand('%:h') .. '/UNDO/' .. expand('%:t'))
	    rundo %:h/UNDO/%:t
	  endif
	endfunc
	func WriteUndo()
	  let dirname = expand('%:h') .. '/UNDO'
	  if !isdirectory(dirname)
	    call mkdir(dirname)
	  endif
	  wundo %:h/UNDO/%:t
	endfunc

您应该关闭 'undofile'，否则每次写入都会得到两个撤消文件。

您可以使用 |undofile()| 函数来找出 Vim 将使用的文件名。

请注意，在读写文件时，如果设置了 'undofile'，大多数错误将是静默的，除非设置了 'verbose'。使用 :wundo 和 :rundo 时，您将获得更多错误消息，例如，当无法读取或写入文件时。

注意：Vim 从不删除撤消文件。您需要自己删除它们。

读取现有的撤消文件可能因多种原因失败：
*E822*	无法打开，因为文件权限不允许。
*E823*	文件开头的幻数不匹配。这通常意味着它不是撤消文件。
*E824*	撤消文件的版本号表明它是由更新版本的 Vim 写入的。您需要该更新版本来打开它。如果您想保留撤消信息在文件中，请不要写入缓冲区。
"File contents changed, cannot use undo info"
	文件文本与写入撤消文件时不同。这意味着撤消文件无法使用，否则会损坏文本。当 'encoding' 与写入撤消文件时不同时，也会发生这种情况。
*E825*  撤消文件不包含有效内容，无法使用。
"Not reading undo file, owner differs"
	撤消文件的所有者与文本文件的所有者不同。出于安全考虑，不读取撤消文件。

写入撤消文件可能因以下原因失败：
*E828*	无法创建要写入的文件。可能您没有目录的写权限。
"Cannot write undo file in any directory in 'undodir'"
	'undodir' 中的任何目录都无法使用。
"Will not overwrite with undo file, cannot read"
	存在一个文件，其名称与要写入的撤消文件相同，但无法读取。您可能需要删除此文件或重命名它。
"Will not overwrite, this is not an undo file"
	存在一个文件，其名称与要写入的撤消文件相同，但它不以正确的幻数开头。您可能需要删除此文件或重命名它。
"Skipping undo file write, nothing to undo"
	没有要写入的撤消信息，未进行任何更改或 'undolevels' 为负数。
*E829*	写入撤消文件时发生错误。您可能需要重试。

==============================================================================
6. 关于撤销的说明					*undo-remarks*

记住的更改次数由 'undolevels' 选项设置。如果为零，则始终使用 Vi 兼容方式。如果为负数，则无法撤销。如果您内存不足，请使用此选项。

							*clear-undo*
当您将 'undolevels' 设置为 -1 时，撤销信息不会立即清除，而是在下一次更改时发生。要强制清除撤销信息，您可以使用以下命令： >
	:let old_undolevels = &l:undolevels
	:setlocal undolevels=-1
	:exe "normal a \<BS>\<Esc>"
	:let &l:undolevels = old_undolevels
	:unlet old_undolevels

注意使用 `&l:undolevels` 显式读取 'undolevels' 的本地值，以及使用 `:setlocal` 仅更改局部选项（该选项优先于相应的全局选项值）。通过 `&undolevels` 保存选项值是不可预测的；它读取本地值（如果已设置）或全局值（否则）。此外，如果已设置本地值，通过 `:set undolevels` 更改选项将同时更改全局值和本地值，需要额外的工作来保存和恢复这两个值。

缓冲区的标记（'a 到 'z）也会与文本一起保存和恢复。

当所有更改都已撤销时，缓冲区不被视为已更改。这样就可以使用 ":q" 而不是 ":q!" 退出 Vim。
请注意，这是相对于文件的最后写入。在 ":w" 之后键入 "u" 实际上会更改缓冲区（与写入的内容相比），因此缓冲区那时被视为已更改。

当使用手动 |folding| 时，折叠不会被保存和恢复。只有完全在折叠内的更改会保持折叠原样，因为折叠的第一行和最后一行没有改变。

编号寄存器也可用于撤销删除。每次删除文本时，它都会被放入寄存器 "1。寄存器 "1 的内容将移动到 "2，依此类推。寄存器 "9 的内容将丢失。您现在可以使用放置命令取回最近删除的文本：'"1P'。（此外，如果删除的文本是上一次删除或复制操作的结果，则 'P' 或 'p' 也可以工作，因为它们放置的是无名寄存器的内容）。您可以使用 '"3P' 取回三次删除前的文本。

						*redo-register*
如果您想取回多个部分删除的文本，可以使用重复命令 "." 的一个特殊功能。它将增加所使用的寄存器的编号。因此，如果您先执行 '"1P'，后续的 "." 将导致 '"2P'。重复此操作将导致插入所有编号寄存器的内容。

示例：如果您使用 'dd....' 删除了文本，可以使用 '"1P....' 恢复它。

如果您不知道删除的文本在哪个寄存器中，可以使用 :display 命令。另一种方法是尝试第一个寄存器 '"1P'，如果不是您想要的，则执行 'u.'。这将移除第一次放置的内容，并重复为第二个寄存器执行放置命令。重复 'u.' 直到得到您想要的内容。

 vim:tw=78:ts=8:noet:ft=help:norl:
