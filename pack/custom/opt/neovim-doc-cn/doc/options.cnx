*options.txt*	Nvim


		  VIM 参考手册	  作者：Bram Moolenaar


选项							*options*

有关选项的概述，请参阅 quickref.txt |option-list|。

Vim 有许多内部变量和开关，可以设置它们以实现特殊效果。这些选项有三种形式：
	boolean		（布尔）	只能是开或关			*boolean* *toggle*
	number		（数字）	具有数值
	string		（字符串）	具有字符串值

				      键入 |gO| 可查看目录。

==============================================================================
1. 设置选项					*set-option* *E764*

							*:se* *:set*
:se[t][!]		显示所有与默认值不同的选项。
			当存在 [!] 时，每个选项单独占一行。

:se[t][!] all		显示所有选项。
			当存在 [!] 时，每个选项单独占一行。

								*E518* *E519*
:se[t] {option}?	显示 {option} 的值。
			注意：一些遗留选项已被移除。|nvim-removed|

:se[t] {option}		切换选项：设置，将其打开。
			数字选项：显示值。
			字符串选项：显示值。

:se[t] no{option}	切换选项：重置，将其关闭。

							   *:set-!* *:set-inv*
:se[t] {option}!   或
:se[t] inv{option}	切换选项：反转值。

				*:set-default* *:set-&* *:set-&vi* *:set-&vim*
:se[t] {option}&	将选项重置为其默认值。
:se[t] {option}&vi	将选项重置为其 Vi 默认值。
:se[t] {option}&vim	将选项重置为其 Vim 默认值。

:se[t] all&		将所有选项设置为其默认值。以下选项的值不会更改：
			  'columns'
			  'lines'
			警告：这可能会产生很多副作用。

					    *:set-args* *:set=* *E487* *E521*
:se[t] {option}={value}		或
:se[t] {option}:{value}
			将字符串或数字选项设置为 {value}。
			对于数字选项，值可以用十进制、十六进制（以 0x 开头）或
			八进制（以 '0' 或 '0o' 开头）给出。
			可以通过键入 'wildchar'（默认是 <Tab>）插入旧值。
			许多具有固定语法的字符串选项也支持补全已知值。
			请参阅 |cmdline-completion| 和 |complete-set-option|。
			{option} 和 '=' 之间允许有空格，且会被忽略。'=' 和
			{value} 之间不允许有空格。
			有关在 {value} 中使用空格和反斜杠的信息，请参阅
			|option-backslash|。

:se[t] {option}+={value}				*:set+=*
			将 {value} 加到数字选项上，或将 {value} 追加到字符串
			选项末尾。当选项是逗号分隔的列表时，会添加一个逗号，
			除非值为空。
			如果选项是标志列表，多余标志会被移除。当添加一个已存在
			的标志时，选项值不会改变。
			当选项支持 "key:value" 项，且 {value} 包含一个
			"key:value" 项或多个逗号分隔的项时，每个项会被单独
			处理：
			- 对于 "key:value" 项，如果键已存在但值不同：旧项被
			  移除，新项追加到末尾。
			- 完全重复的 "key:value" 项保持不变。
			- 其他已存在的项保持不变。
			- 新项追加到末尾。
			另请参阅上面的 |:set-args|。

:se[t] {option}^={value}				*:set^=*
			将数字选项乘以 {value}，或将 {value} 前置到字符串
			选项开头。当选项是逗号分隔的列表时，会添加一个逗号，
			除非值为空。
			当选项支持 "key:value" 项，且 {value} 包含一个
			"key:value" 项或多个逗号分隔的项时，每个项会被单独
			处理。工作方式类似于 |:set+=|，但新项会前置到开头
			而不是追加到末尾。
			另请参阅上面的 |:set-args|。

:se[t] {option}-={value}				*:set-=*
			从数字选项中减去 {value}，或从字符串选项中移除
			{value}（如果存在）。如果在字符串选项中未找到
			{value}，不会报错或警告。当选项是逗号分隔的列表时，
			会删除一个逗号，除非选项变为空。
			当选项是标志列表时，{value} 必须与它们在选项中的显示
			完全一致。逐个移除标志以避免问题。
			当选项支持 "key:value" 项，且 {value} 包含一个
			"key:value" 项或多个逗号分隔的项时，每个项会被单独
			处理。一个 "key:value" 项会移除具有该键的现有项，
			无论其值如何。一个 "key:" 项也通过键匹配来移除。
			可以通过键入 'wildchar' 插入逗号分隔列表或标志列表中
			的单个值。请参阅 |complete-set-option|。
			另请参阅上面的 |:set-args|。

":set" 的 {option} 参数可以重复。例如： >
	:set ai nosi sw=3 ts=3
如果其中一个参数出错，将显示错误消息，后续参数将被忽略。

							*:set-verbose*
当 'verbose' 非零时，显示选项值还会告知它最后被设置的位置。示例： >
	:verbose set shiftwidth cindent?
<  shiftwidth=4 ~
	  Last set from modeline line 1 ~
  cindent ~
	  Last set from /usr/local/share/vim/vim60/ftplugin/c.vim line 30 ~
这仅在请求特定选项值时才会显示，对于 ":verbose set all" 或无参数的
":verbose set" 则不会显示。
当选项是手动设置时，没有 "Last set" 消息。
当选项在执行函数、用户命令或自动命令时被设置，则会报告定义它的脚本。
一些特殊的文本：
	Last set from modeline line 1 ~
		选项在 |modeline| 中设置。
	Last set from --cmd argument ~
		选项通过命令行参数 |--cmd| 或 + 设置。
	Last set from -c argument ~
		选项通过命令行参数 |-c|、+、|-S| 或 |-q| 设置。
	Last set from environment variable ~
		选项通过 $VIMINIT 设置。
	Last set from error handler ~
		选项在评估导致错误时被清除。

							*option-backslash*
要在字符串选项值中包含空格，必须在它前面加一个反斜杠。要包含反斜杠，
必须使用两个反斜杠。实际上，这意味着选项值中的反斜杠数量会减半（向下取整）。
在选项 'path'、'cdpath' 和 'tags' 中，空格前面需要加三个反斜杠，因为
它们可以用逗号或空格分隔。
像 'backupdir' 和 'tags' 这样的逗号分隔选项，其中的逗号也需要用两个反斜杠
转义，而对于像 'makeprg' 这样的非逗号分隔选项则不需要。
使用 |:let| 和 |literal-string| 设置选项时，需要少一层反斜杠。
几个示例： >
   :set makeprg=make\ file	    结果为 "make file"
   :let &makeprg='make file'	    （同上）
   :set makeprg=make\\\ file	    结果为 "make\ file"
   :set tags=tags\ /usr/tags	    结果为 "tags" 和 "/usr/tags"
   :set tags=tags\\\ file	    结果为 "tags file"
   :let &tags='tags\ file'	    （同上）

   :set makeprg=make,file	    结果为 "make,file"
   :set makeprg=make\\,file	    结果为 "make\,file"
   :set tags=tags,file		    结果为 "tags" 和 "file"
   :set tags=tags\\,file	    结果为 "tags\,file"
   :let &tags='tags\,file'	    （同上）

"|" 字符将 ":set" 命令与后续命令分隔开。要在选项值中包含 "|"，请改用 "\|"。
此示例将 'titlestring' 选项设置为 "hi|there"： >
   :set titlestring=hi\|there
此示例将 'titlestring' 选项设置为 "hi"，将 'iconstring' 设置为 "there"： >
   :set titlestring=hi|set iconstring=there

类似地，双引号字符开始注释。要在选项值中包含 '"'，请改用 '\"'。
此示例将 'titlestring' 选项设置为 "hi "there""： >
   :set titlestring=hi\ \"there\"

对于 Win32，文件名中的反斜杠大多不会被移除。更准确地说：对于期望文件名的选项
（那些会展开环境变量的选项），普通文件名前面的反斜杠不会被移除。但特殊字符
（空格、反斜杠、逗号等）前面的反斜杠会像上面解释的那样被处理。
有一种特殊情况，当值以 "\\" 开头时： >
   :set dir=\\machine\path	    结果为 "\\machine\path"
   :set dir=\\\\machine\\path	    结果为 "\\machine\path"
   :set dir=\\path\\file	    结果为 "\\path\file" （错误！）
对于第一个，开头的反斜杠被保留，但对于第二个，反斜杠数量减半。这确保无论你期望
反斜杠减半还是期望保留反斜杠，它都能工作。第三个给出了可能不是你期望的结果。
请避免使用。

				*add-option-flags* *remove-option-flags*
				*E539*
一些选项是标志列表。当你想向这样的选项添加一个标志，而不改变现有标志时，
可以这样做： >
   :set guioptions+=a
像这样从选项中移除一个标志： >
   :set guioptions-=a
这将从 'guioptions' 中移除 'a' 标志。
注意，你应该一次添加或移除一个标志。如果 'guioptions' 的值是 "ab"，
使用 "set guioptions-=ba" 将不起作用，因为字符串 "ba" 不存在。

			   *:set_env* *expand-env* *expand-environment-var*
特定字符串选项中的环境变量会被展开。如果环境变量存在，'$' 及其后的环境变量名
会被替换为其值。如果环境变量不存在，'$' 和名称不会被修改。任何非标识符字符
（不是字母、数字或 '_'）可以跟在环境变量名后面。该字符及其后的内容会追加到
环境变量的值后面。示例： >
   :set term=$TERM.new
   :set path=/usr/$INCLUDE,$HOME/include,.
使用 ":set opt-=val" 或 ":set opt+=val" 从选项中添加或移除字符串时，
展开操作会在添加或移除之前进行。


本地选项的处理			*local-options*

注意：以下内容也适用于 |global-local| 选项。

某些选项仅适用于窗口或缓冲区。每个窗口或缓冲区都有自己的一份该选项的副本，
因此每个都可以有自己的值。这允许你在一个窗口中设置 'list' 而在另一个窗口中不设置。
也可以在一个缓冲区中将 'shiftwidth' 设置为 3，在另一个缓冲区中设置为 4。

以下内容解释了这些本地选项在特定情况下会发生什么。你实际上不需要知道所有这些，
因为 Vim 大多使用你期望的选项值。不幸的是，做用户期望的事情有点复杂...

当分割窗口时，本地选项会被复制到新窗口。因此，在分割后，两个窗口的内容看起来相同。

当编辑一个新缓冲区时，其本地选项值必须被初始化。由于当前缓冲区的本地选项可能
特定于该缓冲区，因此不会使用它们。相反，对于每个缓冲区本地选项，还存在一个全局值，
该值用于新缓冲区。使用 ":set" 时，本地值和全局值都会被更改。使用 "setlocal" 时，
仅更改本地值，因此该值不会在编辑新缓冲区时使用。

当编辑一个之前编辑过的缓冲区时，会再次使用最后一个关闭窗口中的选项。如果该缓冲区
曾在此窗口中编辑过，则使用那时的值。否则，使用缓冲区最后一次被编辑时最后关闭的
窗口中的值。

可以专门为一种缓冲区类型设置本地窗口选项。当在同一窗口中编辑另一个缓冲区时，
你不想继续使用这些本地窗口选项。因此，Vim 保留了本地窗口选项的全局值，
用于在编辑另一个缓冲区时使用。每个窗口都有自己的这些值的副本。因此，
这些值是窗口本地的，但对窗口中的所有缓冲区是全局的。通过这种方式，你可以： >
	:e one
	:set list
	:e two
现在 'list' 选项也将在 "two" 中被设置，因为使用 ":set list" 命令你也设置了
全局值。 >
	:set nolist
	:e one
	:setlocal list
	:e two
现在 'list' 选项未被设置，因为 ":set nolist" 重置了全局值，":setlocal list"
仅更改了本地值，而 ":e two" 获取了全局值。注意，如果你接下来执行： >
	:e one
你将恢复上次编辑 "one" 时的 'list' 值。每个缓冲区的窗口本地选项都会被记住。
即使缓冲区未加载，这也会发生，但当缓冲区被擦除时，这些选项会丢失 |:bwipe|。

特殊的本地窗口选项			*local-noglobal*

以下本地窗口选项在创建新窗口时不会被复制，因此它们的行为略有不同：

	选项		原因	~
	'previewwindow'	只能有一个
	'scroll'	特定于现有窗口
	'winfixbuf'	特定于现有窗口
	'winfixheight'	特定于现有窗口
	'winfixwidth'	特定于现有窗口

特殊的本地缓冲区选项

以下本地缓冲区选项在创建新缓冲区时不会被复制，因此它们的行为略有不同：

	选项		原因	~
	'filetype'	由自动命令显式设置
	'syntax'	由自动命令显式设置
	'bufhidden'	表示 |special-buffers|
	'buftype'	表示 |special-buffers|
	'readonly'	会自动检测
	'modified'	会自动检测

							*:setl* *:setlocal*
:setl[ocal][!] ...	类似于 ":set"，但仅设置当前缓冲区或窗口的本地值。
			并非所有选项都有本地值。如果选项没有本地值，则设置
			全局值。
			使用 "all" 参数：显示所有本地选项的本地值。
			无参数：显示所有与默认值不同的本地选项的本地值。
			当显示特定的本地选项时，显示其本地值。对于全局/本地
			布尔选项，当使用全局值时，选项名称前会显示 "--"。
			对于全局选项，显示的是全局值（但将来可能会改变）。

:se[t] {option}<	将 {option} 的有效值设置为其全局值。
			对于 |global-local| 选项，本地值被移除，以便使用
			全局值。
			对于所有其他选项，全局值被复制到本地值。

:setl[ocal] {option}<	通过将全局值复制到本地值，将 {option} 的有效值设置
			为其全局值。

请注意，对于 |global-local| 选项，基于字符串和基于数字的选项之间的行为
略有不同。

							*:setg* *:setglobal*
:setg[lobal][!] ...	类似于 ":set"，但仅设置本地选项的全局值，不改变
			本地值。
			当显示选项时，显示全局值。
			使用 "all" 参数：显示所有本地选项的全局值。
			无参数：显示所有与默认值不同的本地选项的全局值。

对于缓冲区本地和窗口本地选项：
	命令		 全局值	  本地值	       条件 ~
      :set option=value	     设置	     设置
 :setlocal option=value	      -		     设置
:setglobal option=value	     设置	       -
      :set option?	      -		     显示	本地值已设置
      :set option?	   显示	       -		本地值未设置
 :setlocal option?	      -		     显示
:setglobal option?	   显示	       -


具有本地值的全局选项			*global-local*

当大多数缓冲区和窗口都使用同一个值时，选项是全局的。对于某些全局选项，
有时拥有不同的本地值会很有用。你可以使用 ":setlocal" 设置本地值。该缓冲区
或窗口将使用本地值，而其他缓冲区和窗口继续使用全局值。

例如，你有两个窗口，都在编辑 C 源代码。它们使用全局的 'makeprg' 选项。
如果你在两个窗口之一执行： >
	:set makeprg=gmake
那么另一个窗口也将切换到相同的值。没有必要也在另一个 C 源窗口设置
'makeprg' 选项。
但是，如果你在一个新窗口中开始编辑一个 Perl 文件，你想为它使用另一个
'makeprg'，而不改变 C 源文件使用的值。你可以使用这个命令： >
	:setlocal makeprg=perlmake
你可以通过清空本地值来切换回使用全局值： >
	:setlocal makeprg=
这仅适用于字符串选项。对于数字或布尔选项，你需要使用 "<" 标志，如下所示： >
	:setlocal autoread<
请注意，对于非布尔和非数字选项，使用 "<" 会将全局值复制到本地值，而不会切换回
使用全局值（当全局值稍后改变时，这一点很重要）。你也可以使用： >
	:set path<
这会使 'path' 的本地值为空，以便使用全局值。因此它等同于： >
	:setlocal path=
注意：将来更多的全局选项可能被设为 |global-local|。在全局选项上使用
":setlocal" 可能会有不同的行为。

						*option-value-function*
某些选项（'completefunc'、'findfunc'、'omnifunc'、'operatorfunc'、
'quickfixtextfunc'、'tagfunc' 和 'thesaurusfunc'）被设置为函数名、
函数引用或 lambda 函数。当使用 lambda 时，它会被转换为名称，
例如 "<lambda>123"。
示例：
>
	set opfunc=MyOpFunc
	set opfunc=function('MyOpFunc')
	set opfunc=funcref('MyOpFunc')
	set opfunc={a\ ->\ MyOpFunc(a)}

设置为脚本本地函数： >
	set opfunc=s:MyLocalFunc
	set opfunc=<SID>MyLocalFunc

使用 funcref 变量设置： >
	let Fn = function('MyTagFunc')
	let &tagfunc = Fn

使用 lambda 表达式设置： >
	let &tagfunc = {t -> MyTagFunc(t)}

使用带有 lambda 表达式的变量设置： >
	let L = {a, b, c -> MyTagFunc(a, b , c)}
	let &tagfunc = L

在 expr 选项中调用函数			*expr-option-function*

某些选项的值，如 'foldexpr'，是一个被评估以获取值的表达式。评估可能会产生
相当大的开销。最小化开销的一种方法，同时也能保持选项值非常简单，是定义一个
函数并将选项设置为无参数调用它。也可以使用 |v:lua-call|。示例： >vim
	lua << EOF
	  function _G.MyFoldFunc()
	    -- ... 计算行 v:lnum 的折叠级别
	    return level
	  end
	EOF
	set foldexpr=v:lua.MyFoldFunc()


设置文件类型

:setf[iletype] [FALLBACK] {filetype}			*:setf* *:setfiletype*
			将 'filetype' 选项设置为 {filetype}，但仅当在
			（嵌套的）自动命令序列中尚未设置时执行。
			这相当于： >
				:if !did_filetype()
				:  setlocal filetype={filetype}
				:endif
<			此命令用于 filetype.vim 文件中，以避免两次设置
			'filetype' 选项，导致加载不同的设置和语法文件。

			当存在可选的 FALLBACK 参数时，后续的 :setfiletype
			命令将覆盖 'filetype'。这用于仅仅是猜测的文件类型检测。
			在此命令之后，|did_filetype()| 将返回 false。

				*option-window* *optwin*
:bro[wse] se[t]			*:set-browse* *:browse-set* *:opt* *:options*
:opt[ions]		打开一个窗口，用于查看和设置所有选项。
			选项按功能分组。
			为每个选项提供简短帮助。在简短帮助上按 <CR> 可打开
			帮助窗口，获取有关该选项的更多帮助。
			修改选项的值，然后在 "set" 行上按 <CR> 即可设置新值。
			对于窗口和缓冲区特定的选项，选项值会在最后访问的窗口中
			设置，除非该窗口是帮助窗口，在这种情况下，会使用帮助
			窗口下方的窗口（跳过选项窗口）。

								*$HOME*
使用 "~" 类似于使用 "$HOME"，但它仅在选项开头、空格或逗号之后被识别。

在 Unix 系统上，也可以使用 "~user"。它会被替换为用户 "user" 的主目录。
示例： >
    :set path=~mool/include,/usr/include,.

在 Unix 系统上，也可以使用 "${HOME}" 形式。花括号之间的名称可以包含非标识符
字符。请注意，如果你想将其用于 "gf" 命令，需要将 '{' 和 '}' 字符添加到
'isfname' 中。

注意：环境变量和 "~/" 的展开仅在使用 ":set" 命令时进行，在使用 ":let" 为
选项赋值时不会进行。

							*$HOME-windows*
在 MS-Windows 上，如果 $HOME 未定义为环境变量，则在运行时 Vim 会将其设置为
$HOMEDRIVE$HOMEPATH 的展开值。如果 $HOMEDRIVE 未设置，则使用 $USERPROFILE。

此展开值不会导出到环境中，这在运行外部命令时很重要： >
	:echo system('set | findstr ^HOME=')
以及 >
	:echo luaeval('os.getenv("HOME")')
应该不显示任何内容（空字符串），尽管 exists('$HOME') 为真。
当将 $HOME 设置为非空字符串时，它会被导出到子进程。

注意展开的选项的最大长度是有限的。具体长度取决于系统，通常是 256 或 1024 个
字符。

==============================================================================
2. 自动设置选项					*auto-setting*

除了使用 ":set" 命令更改选项外，你还可以通过多种方式自动设置选项：

1. 使用 |config| 文件或 |startup| 参数。你可以使用 |:mkvimrc|、|:mkview|
   和 |:mksession| 创建初始化文件。
2. 编辑文件时执行的 |autocommand|。
3. 如果启用了 'exrc'，当前目录中的 ".nvim.lua" 文件。
4. 当前缓冲区目录或其祖先目录中的 |editorconfig|。
5. 在文件开头或结尾找到的 'modeline' 设置。见下文。

					*modeline* *vim:* *vi:* *ex:* *E520*
有两种形式的 modeline。第一种形式：
	[text{white}]{vi:|vim:|ex:}[white]{options}

[text{white}]		空或任何文本，后跟至少一个空白字符（<Space> 或
			<Tab>）；"ex:" 总是需要至少一个空白字符
{vi:|vim:|ex:}		字符串 "vi:"、"vim:" 或 "ex:"
[white]			可选的空白字符
{options}		选项设置列表，以空白字符或 ':' 分隔，其中 ':' 之间的
			每个部分都是一个 ":set" 命令的参数（可以为空）

示例：
   vi:noai:sw=3 ts=6 ~
   vim: tw=77 ~

第二种形式（与某些版本的 Vi 兼容）：

	[text{white}]{vi:|vim:|Vim:|ex:}[white]se[t] {options}:[text]

[text{white}]		空或任何文本，后跟至少一个空白字符（<Space> 或
			<Tab>）；"ex:" 总是需要至少一个空白字符
{vi:|vim:|Vim:|ex:}	字符串 "vi:"、"vim:"、"Vim:" 或 "ex:"
[white]			可选的空白字符
se[t]			字符串 "set " 或 "se "（注意空格）；当使用 "Vim"
			时，必须是 "set"
{options}		选项列表，以空白字符分隔，是 ":set" 命令的参数
:			一个冒号
[text]			任意文本或空

示例： >
   /* vim: set ai tw=75: */
   /* Vim: set ai tw=75: */

{vi:|vim:|Vim:|ex:} 之前的空白字符是必需的。这可以最大限度地减少像 "lex:"
这样的普通单词被误匹配的可能性。有一个例外："vi:" 和 "vim:" 也可以出现在
行首（为了与版本 3.0 兼容）。行首的 "ex:" 将被忽略（这可能是 "example:" 的缩写）。

如果 modeline 在 modeline 内部被禁用，后续的 modeline 将被忽略。这是为了允许
在每个文件的基础上关闭 modeline。当一行看起来像 modeline 但实际上不是时，
这很有用。例如，最好在包含 "vim:" 这样的字符串的 YAML 文件开头加上
    # vim: nomodeline ~
以避免错误检测 modeline。在禁用 modeline 的同一行上，其后的选项（如果有）
仍会被评估（但通常你不会有任何选项）。

							*modeline-local*
选项的设置方式类似于 ":setlocal"：新值仅适用于包含该文件的缓冲区和窗口。
尽管可以从 modeline 设置全局选项，但这并不常见。如果你打开了两个窗口，
且其中的文件将同一个全局选项设置为不同的值，则结果取决于最后打开的是哪个窗口。

当编辑一个已经加载的文件时，仅使用 modeline 中的窗口本地选项。因此，如果你
在打开文件后手动更改了缓冲区本地选项，当你在另一个窗口中编辑同一个缓冲区时，
它不会被更改。但窗口本地选项会被设置。

							*modeline-version*
如果 modeline 仅用于某些版本的 Vim，可以在使用 "vim:" 或 "Vim:" 的地方指定
版本号：
	vim{vers}:	版本 {vers} 或更高
	vim<{vers}:	版本早于 {vers}
	vim={vers}:	版本 {vers}
	vim>{vers}:	版本晚于 {vers}
{vers} 对于 Vim 7.0 是 700（百倍的主版本号加次版本号）。
例如，要使用仅适用于 Vim 7.0 的 modeline： >
	/* vim700: set foldmethod=marker */
要使用适用于 Vim 7.2 之后版本的 modeline： >
	/* vim>702: set cole=2: */
"vim" 和 ":" 之间不能有空格。
如果 {vers} 不符合整数格式，则忽略该 modeline。

要检查的行数可以通过 'modelines' 选项设置。如果 'modeline' 关闭或
'modeline' 为 0，则不检查任何行。

注意，对于第一种形式，会使用该行的其余部分，因此像这样的行： >
   /* vi:ts=4: */
会因末尾的 "*/" 而产生错误消息。这一行没问题： >
   /* vi:set ts=4: */

如果检测到错误，则会跳过该行的其余部分。

如果要在 set 命令中包含 ':'，请在它前面加一个 '\'。'：' 前面的反斜杠将被
删除。示例： >
   /* vi:set fillchars=stl\:^,vert\:\|: */
这将 'fillchars' 选项设置为 "stl:^,vert:\|"。只有 ':' 前面的一个反斜杠
会被删除。因此，要包含 "\:"，你必须指定 "\\:"。
							*E992*
出于安全原因（有人可能使用 modeline 创建特洛伊木马文本文件），不支持除
"set" 以外的其他命令。并且并非所有选项都可以设置。对于某些选项，会设置一个
标志，以便在使用该值时 |sandbox| 生效。某些选项仅在设置 'modelineexpr'
（默认关闭）时才能从 modeline 设置。

尽管如此，modeline 总是存在导致问题的小风险。例如，当某个恶作剧者将
'textwidth' 设置为 5 时，你所有的行都会意外换行。因此，在编辑不可信的
文本之前，请禁用 modeline。例如，邮件 ftplugin 就是这样做的。

提示：如果你想做除设置选项之外的其他事情，你可以定义一个自动命令来检查
文件中是否有特定的字符串。例如： >
	au BufReadPost * if getline(1) =~ "VAR" | call SetVar() | endif
并定义一个函数 SetVar()，对包含 "VAR" 的行执行某些操作。

==============================================================================
3. 选项摘要					*option-summary*

在下面的列表中，提到了所有选项的全名，以及如果有缩写的话也会列出。两种形式
都可以使用。

在本文档中，当一个布尔选项被 "set" 时，意味着输入了 ":set option"。当一个
选项被 "reset" 时，则使用 ":set nooption"。

大多数选项在所有窗口和缓冲区中都是相同的。有一些选项特定于文本在窗口中的
显示方式。这些选项可以在每个窗口中设置为不同的值。例如，'list' 选项可以在
一个窗口中设置，在另一个窗口中重置，从而同时获得两种视图类型。有一些选项
特定于某个文件。这些选项可以为每个文件或缓冲区设置不同的值。例如，
'textwidth' 选项对于普通文本文件可以是 78，对于 C 程序可以是 0。

	global			所有缓冲区和窗口共用一个选项
	local to window		每个窗口都有自己的一份该选项的副本
	local to buffer		每个缓冲区都有自己的一份该选项的副本

当创建新窗口时，当前活动窗口的选项值将用作窗口特定选项的默认值。对于缓冲区
特定选项，这取决于 'cpoptions' 选项中的 's' 和 'S' 标志。如果包含 's'
（默认情况），则当首次进入缓冲区时，缓冲区选项的值会从当前活动缓冲区复制。
如果存在 'S'，则每次进入缓冲区时都会复制选项，这几乎等同于使用全局选项。
如果 's' 和 'S' 都不存在，则在创建缓冲区时，选项会从当前活动缓冲区复制。

隐藏选项						*hidden-options*

并非所有版本都支持所有选项。这取决于支持的功能，有时也取决于系统。相关说明
用花括号括在下面。当一个选项不被支持时，它被称为隐藏选项。尝试获取隐藏选项
的值不会产生错误，而是返回该选项的默认值。你不能更改隐藏选项的值。

要测试 "foo" 是否是一个有效的选项名称，可以使用类似这样的方法： >
	if exists('&foo')
这对于隐藏选项也返回 true。要测试选项 "foo" 是否真正受支持，可以使用类似
这样的方法： >
	if exists('+foo')
<
							*E355*
带有简短描述的选项跳转表可以在 |Q_op| 找到。

			*'allowrevins'* *'ari'* *'noallowrevins'* *'noari'*
'allowrevins' 'ari'	布尔	(默认关闭)
			全局
	允许在插入模式下使用 CTRL-_。默认关闭，以避免用户不小心按了 CTRL-_
	而不是 SHIFT-_ 而进入反向插入模式，并且不知道如何退出。参见 'revins'。

						*'ambiwidth'* *'ambw'*
'ambiwidth' 'ambw'	字符串	(默认 "single")
			全局
	告诉 Vim 如何处理具有东亚宽度类 Ambiguous（不确定）的字符（如欧元符号、
	注册商标符号、版权符号、希腊字母、西里尔字母）。

	目前有两个可能的值：
	"single":	使用与 US-ASCII 字符相同的宽度。这是大多数用户期望的。
	"double":	使用 ASCII 字符两倍的宽度。
							*E834* *E835*
	如果 'listchars' 或 'fillchars' 包含一个会是双倍宽度的字符，则不能
	使用 "double" 值。这些错误也可能在调用 setcellwidths() 时出现。

	这些值会被用 |setcellwidths()| 指定的字符覆盖。

	有许多 CJK 字体，其中这些字符的字形宽度完全取决于它们在传统 CJK 编码
	中所占的字节数。在这些编码中，欧元符号、注册商标符号、希腊/西里尔字母
	由两个字节表示，因此这些字体具有这些字符的 "宽" 字形。这同样适用于一些
	用于在文本文件中创建表格的线条绘制字符。因此，当 GUI Vim 使用 CJK 字体，
	或者 Vim 在使用 CJK 字体的终端（模拟器）中运行（或者 Vim 在使用
	"-cjkwidth" 选项调用的 xterm 中运行时），此选项应设置为 "double"，
	以使 Vim 感知的宽度与字体中字形的宽度相匹配。当系统区域设置为 CJK 区域
	时，在 CJK MS-Windows 下也可能需要设置为 "double"。请参阅 Unicode 标准
	附录 #11 (https://www.unicode.org/reports/tr11)。

				*'arabic'* *'arab'* *'noarabic'* *'noarab'*
'arabic' 'arab'		布尔	(默认关闭)
			本地到窗口
	设置此选项可以开始编辑阿拉伯语文本。
	设置此选项将：
	- 设置 'rightleft' 选项，除非设置了 'termbidi'。
	- 设置 'arabicshape' 选项，除非设置了 'termbidi'。
	- 将 'keymap' 选项设置为 "arabic"；在插入模式下，CTRL-^ 可在输入
	  英语和阿拉伯语键映射之间切换。
	- 设置 'delcombine' 选项

	重置此选项将：
	- 重置 'rightleft' 选项。
	- 禁用 'keymap' 的使用（不更改其值）。
	注意，'arabicshape' 和 'delcombine' 不会被重置（它们是全局选项）。
	另请参阅 |l10n-arabic.txt|。

		*'arabicshape'* *'arshape'* *'noarabicshape'* *'noarshape'*
'arabicshape' 'arshape'	布尔	(默认开启)
			全局
	当开启且 'termbidi' 关闭时，显示阿拉伯语所需的视觉字符校正会生效。
	本质上，字形变换被启用；这是一个广义的术语，包括：
	  a) 根据字符在单词中的位置（词首、词中、词尾和独立形式）改变/变形字符。
	  b) 启用组合字符的能力。
	  c) 启用所需字符的组合。
	当禁用时，显示会显示每个字符真实的独立形式。
	阿拉伯语是一种复杂的语言，需要其他设置，更多详情请参阅 |l10n-arabic.txt|。

			*'autochdir'* *'acd'* *'noautochdir'* *'noacd'*
'autochdir' 'acd'	布尔	(默认关闭)
			全局
	开启时，每当你打开文件、切换缓冲区、删除缓冲区或打开/关闭窗口时，
	Vim 都会更改当前工作目录。它会更改为包含已打开或选定文件的目录。
	当缓冲区没有名称时，它也没有目录，因此导航到它时当前目录不会改变。
	注意：当此选项开启时，某些插件可能无法工作。

			*'autocomplete'* *'ac'* *'noautocomplete'* *'noac'*
'autocomplete' 'ac'	布尔	(默认关闭)
			全局或本地到缓冲区 |global-local|
	开启时，Vim 会在你输入时显示补全菜单，类似于使用 |i_CTRL-N|，
	但会自动触发。请参阅 |ins-autocompletion|。

					*'autocompletedelay'* *'acl'*
'autocompletedelay' 'acl'	数字	(默认 0)
			全局
	键入后自动补全菜单出现前的延迟时间（毫秒）。如果你不希望它打开得太快，
	请将此值设置得略高于你的打字速度。请参阅 |ins-autocompletion|。

					*'autocompletetimeout'* *'act'*
'autocompletetimeout' 'act'	数字	(默认 80)
			全局
	用于衰减时间片补全算法的初始超时时间（毫秒）。从此值开始，对于每个
	较慢的源减半，直到达到最小值。所有源都会运行，但较慢的源会迅速降低
	优先级。默认值经过调整，即使在慢速系统上有多个慢速源，弹出菜单也会在
	约 200 毫秒内打开。很少需要更改此值。仅 80 或更高的值有效。
	特殊情况：当 'complete' 包含 "F" 或 "o"（函数源）时，会使用更长的
	超时时间，允许像 LSP 服务器这样有时可能需要更长时间（例如，在加载
	模块时）的源有大约 1 秒的时间。请参阅 |ins-autocompletion|。

			*'autoindent'* *'ai'* *'noautoindent'* *'noai'*
'autoindent' 'ai'	布尔	(默认开启)
			本地到缓冲区
	在开始新行时（在插入模式下键入 <CR> 或使用 "o" 或 "O" 命令）从当前行
	复制缩进。如果你在新行上除了 <BS> 或 CTRL-D 之外没有键入任何内容，
	然后键入 <Esc>、CTRL-O 或 <CR>，则缩进会被再次删除。将光标移动到
	另一行也有相同的效果，除非 'cpoptions' 中包含 'I' 标志。
	当 autoindent 开启时，格式化（使用 "gq" 命令或在插入模式下达到
	'textwidth' 时）使用第一行的缩进。
	当 'smartindent' 或 'cindent' 开启时，缩进会以不同的方式更改。

				*'autoread'* *'ar'* *'noautoread'* *'noar'*
'autoread' 'ar'		布尔	(默认开启)
			全局或本地到缓冲区 |global-local|
	当检测到文件在 Vim 之外被更改，并且在 Vim 内部未被更改时，自动重新
	读取它。当文件已被删除时，不会执行此操作，因此你将拥有删除前的文本。
	当文件再次出现时，它会被读取。|timestamp|
	如果此选项有本地值，请使用以下命令切换回使用全局值： >vim
		set autoread<
<

				*'autowrite'* *'aw'* *'noautowrite'* *'noaw'*
'autowrite' 'aw'	布尔	(默认关闭)
			全局
	如果文件已被修改，则在每次 `:next`、`:rewind`、`:last`、`:first`、
	`:previous`、`:stop`、`:suspend`、`:tag`、`:!`、`:make`、CTRL-] 和
	CTRL-^ 命令时写入文件内容；以及当 `:buffer`、CTRL-O、CTRL-I、
	'{A-Z0-9} 或 `{A-Z0-9} 命令跳转到另一个文件时写入。
	如果缓冲区变为隐藏状态，则不会写入，例如，当 'bufhidden' 设置为
	"hide" 并使用 `:next` 时。
	注意，对于某些命令，不使用 'autowrite' 选项，请参见 'autowriteall'。
	某些缓冲区不会被写入，特别是当 'buftype' 为 "nowrite"、"nofile"、
	"terminal" 或 "prompt" 时。
	请小心使用：如果你对缓冲区进行了临时更改但不想保存，此选项可能会
	导致它被保存。使用 ":file {name}" 重命名缓冲区可能有助于避免这种情况。

			*'autowriteall'* *'awa'* *'noautowriteall'* *'noawa'*
'autowriteall' 'awa'	布尔	(默认关闭)
			全局
	类似于 'autowrite'，但也用于命令 ":edit"、":enew"、":quit"、":qall"、
	":exit"、":xit"、":recover" 和关闭 Vim 窗口。
	设置此选项也意味着 Vim 的行为就像设置了 'autowrite' 一样。

						*'background'* *'bg'*
'background' 'bg'	字符串	(默认 "dark")
			全局
	当设置为 "dark" 或 "light" 时，会调整该背景类型的默认颜色组。
	|TUI| 或其他 UI 在启动时（如果它能检测到背景颜色）会设置此项
	（触发 |OptionSet|）。

	此选项不会更改背景颜色，它告诉 Nvim "继承的"（终端/GUI）背景看起来
	是什么样子。如果你想要显式设置背景颜色，请参阅 |:hi-normal|。
						*g:colors_name*
	当加载颜色方案时（设置了 "g:colors_name" 变量），更改 'background'
	将导致重新加载颜色方案。如果颜色方案根据 'background' 的值进行调整，
	则此操作有效。但是，如果颜色方案自行设置 'background'，则效果可能
	会被撤销。需要时请先删除 "g:colors_name" 变量。

	通常，此选项会在 vimrc 文件中设置。可能取决于终端名称。示例： >vim
		if $TERM ==# "xterm"
		  set background=dark
		endif
<	更改此选项时，高亮组的默认设置将会改变。要使用其他设置，请在设置
	'background' 选项之后放置 ":highlight" 命令。

						*'backspace'* *'bs'*
'backspace' 'bs'	字符串	(默认 "indent,eol,start")
			全局
	影响插入模式下 <BS>、<Del>、CTRL-W 和 CTRL-U 的工作方式。这是一个
	项目列表，以逗号分隔。每个项目允许一种退格方式：
	值	效果	~
	indent	允许退格越过自动缩进
	eol	允许退格越过换行符（合并行）
	start	允许退格越过插入开始位置；CTRL-W 和 CTRL-U 在插入开始位置
		停止。
	nostop	类似于 start，但 CTRL-W 和 CTRL-U 不会在插入开始位置停止。

	当值为空时，使用 Vi 兼容的退格方式，上面提到的任何一种方式都不可用。

				*'backup'* *'bk'* *'nobackup'* *'nobk'*
'backup' 'bk'		布尔	(默认关闭)
			全局
	在覆盖文件之前创建备份。成功写入文件后保留备份。如果你不想保留备份
	文件，但希望在写入文件时有一个备份，请重置此选项并设置 'writebackup'
	选项（这是默认设置）。如果你根本不需要备份文件，请同时重置这两个选项
	（如果你的文件系统几乎已满，请使用此选项）。有关更多说明，请参阅
	|backup-table|。
	当 'backupskip' 模式匹配时，无论如何都不会创建备份。
	当设置了 'patchmode' 时，备份可能会被重命名以成为文件的最旧版本。

						*'backupcopy'* *'bkc'*
'backupcopy' 'bkc'	字符串	(默认 "auto")
			全局或本地到缓冲区 |global-local|
	写入文件并创建备份时，此选项指示如何执行操作。这是一个逗号分隔的
	单词列表。

	主要值有：
	"yes"	复制文件并覆盖原始文件
	"no"	重命名文件并写入一个新文件
	"auto"	上述之一，哪个效果最好

	可以与上述值组合的额外值有：
	"breaksymlink"	写入时始终断开符号链接
	"breakhardlink"	写入时始终断开硬链接

	复制并覆盖原始文件：
	- 复制文件需要额外时间。
	+ 当文件具有特殊属性、是（硬/符号）链接或具有资源分支时，所有这些
	  都会被保留。
	- 当文件是链接时，备份将具有链接的名称，而不是真实文件的名称。

	重命名文件并写入新文件：
	+ 速度很快。
	- 有时并非文件的所有属性都能复制到新文件。
	- 当文件是链接时，新文件将不再是链接。

	"auto" 值是折中方式：当 Vim 认为重命名文件没有副作用（属性可以传递，
	并且文件不是链接）时，就使用重命名。当预计会出现问题时，将进行复制。

	"breaksymlink" 和 "breakhardlink" 值可以与任何 "yes"、"no" 和 "auto"
	组合使用。当包含时，它们强制 Vim 始终断开符号链接或硬链接，方法是
	执行与 "no" 选项完全相同的操作：将原始文件重命名为备份文件，并写入
	一个新文件来代替它。例如，在源文件树中，所有文件都是符号链接或硬链接，
	并且任何更改都应保留在本地源树中，而不是传播回原始源，这可能很有用。
								*crontab*
	"no" 和 "auto" 会导致问题的一种情况：一个程序打开一个文件，调用 Vim
	编辑该文件，然后测试打开的文件是否被更改（通过文件描述符），它将检查
	备份文件而不是新创建的文件。"crontab -e" 就是一个例子，还有几个
	|file-watcher| 守护进程，如 inotify。在这种情况下，你可能希望切换
	此选项。

	当进行复制时，原始文件会被截断，然后填充新文本。这意味着原始文件的
	保护位、所有者和符号链接保持不变。然而，备份文件是一个新文件，
	归编辑文件的用户所有。备份文件的组设置为原始文件的组。如果失败，
	则组的保护位设置为与其他用户相同。

	当文件被重命名时，情况相反：备份具有原始文件的相同属性，而新写入的
	文件归当前用户所有。当文件是（硬/符号）链接时，新文件将不是链接！
	这就是为什么 "auto" 值在文件是链接时不进行重命名。新写入文件的所有者
	和组将设置为与原始文件相同，但系统可能拒绝这样做。在这种情况下，
	"auto" 值将再次不重命名文件。

						*'backupdir'* *'bdir'*
'backupdir' 'bdir'	字符串	(默认 ".,$XDG_STATE_HOME/nvim/backup//")
			全局
	备份文件的目录列表，以逗号分隔。
	- 备份文件将在列表中第一个可行的目录中创建。如果目录都不存在，
	  Nvim 将尝试创建列表中的最后一个目录。
	- 空值表示不会创建备份文件（'patchmode' 将无法使用！）。写入可能
	  因此失败。
	- 目录 "." 表示将备份文件放在与编辑文件相同的目录中。
	- 以 "./"（或 MS-Windows 的 ".\"）开头的目录表示将备份文件放在
	  相对于编辑文件的位置。开头的 "." 将被编辑文件的路径名替换。
	  （目录名中的 "." 没有特殊含义）。
	- 逗号后的空格会被忽略，其他空格被视为目录名的一部分。要使目录名
	  以空格开头，请在它前面加一个反斜杠。
	- 要在目录名中包含逗号，请在它前面加一个反斜杠。
	- 目录名可以以 '/' 结尾。
	- 对于 Unix 和 Win32，如果目录以两个路径分隔符 "//" 结尾，则交换
	  文件名将由文件的完整路径构建，所有路径分隔符都更改为百分号 '%'。
	  这将确保备份目录中文件名的唯一性。
	  在 Win32 上，也可以以 "\\" 结尾。但是，当后面跟有分隔逗号时，
	  必须使用 "//"，因为 "\\" 会将逗号包含在文件名中。因此，建议使用
	  '//' 而不是 '\\'。
	- 环境变量会被展开 |:set_env|。
	- 小心 '\' 字符，在空格前键入一个，在选项中键入两个才能得到一个
	  （参见 |option-backslash|），例如： >vim
	    set bdir=c:\\tmp,\ dir\\,with\\,commas,\\\ dir\ with\ spaces
<
	另请参阅 'backup' 和 'writebackup' 选项。
	如果你想在 Unix 上隐藏备份文件，请考虑此值： >vim
		set backupdir=./.backup,~/.backup,.,/tmp
<	你必须在每个目录和你的主目录中创建一个 ".backup" 目录才能使其正常工作。
	在列表中添加或删除目录时，最好使用 |:set+=| 和 |:set-=|。这可以避免
	将来版本使用其他默认值时出现的问题。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'backupext'* *'bex'* *E589*
'backupext' 'bex'	字符串	(默认 "~")
			全局
	附加到文件名后以生成备份文件名称的字符串。默认值相当不常见，因为这样
	可以避免意外地用备份文件覆盖现有文件。你可能更喜欢使用 ".bak"，但请
	确保你没有想要保留的 ".bak" 文件。
	只能使用普通的文件名字符；`/\*?[|<>` 是非法的。

	如果你喜欢保留大量备份，可以使用 BufWritePre 自动命令在写入文件前
	更改 'backupext' 以包含时间戳。 >vim
		au BufWritePre * let &bex = '-' .. strftime("%Y%b%d%X") .. '~'
<	使用 'backupdir' 将备份放在不同的目录中。

						*'backupskip'* *'bsk'*
'backupskip' 'bsk'	字符串	(默认 "$TMPDIR/*,$TMP/*,$TEMP/*"
                                 Unix: "/tmp/*,$TMPDIR/*,$TMP/*,$TEMP/*"
                                 Mac: "/private/tmp/*,$TMPDIR/*,$TMP/*,$TEMP/*")
			全局
	文件模式列表。当写入的文件名与其中一个模式匹配时，不会创建备份文件。
	会使用指定的文件名和文件的完整路径名进行匹配。模式的使用方式与
	|:autocmd| 相同，参见 |autocmd-pattern|。
	注意特殊字符，参见 |option-backslash|。
	如果未定义 $TMPDIR、$TMP 或 $TEMP，则默认值中不会使用它们。"/tmp/*"
	仅在 Unix 上使用。

	警告：没有备份文件意味着，如果 Vim 未能正确写入缓冲区，然后无论出于
	何种原因 Vim 退出，你将同时丢失原始文件和正在写入的内容。只有在你
	不介意丢失文件时才禁用备份。

	请注意，环境变量不会被展开。如果你想使用 $HOME，你必须显式展开它，
	例如： >vim
		let &backupskip = escape(expand('$HOME'), '\') .. '/tmp/*'

<	注意，默认值也确保了 "crontab -e" 的工作（如果通过重命名原始文件
	创建备份，crontab 将看不到新创建的文件）。另请参阅 'backupcopy' 和
	|crontab|。

						*'belloff'* *'bo'*
'belloff' 'bo'		字符串	(默认 "all")
			全局
	指定哪些事件不会响铃。它是一个逗号分隔的项目列表。对于每个存在的项目，
	铃声将被静音。这对于指定插入模式下要静音的特定事件最有用。
	你也可以通过使用 'visualbell' 使其闪烁。

	项目	    含义（存在时）	~
	all	    所有事件。
	backspace    按下 <BS> 或 <Del> 且删除导致错误时。
	cursor	    在 |插入模式| 中使用光标键或 <PageUp>/<PageDown> 移动失败时。
	complete	    使用 |i_CTRL-X_CTRL-K| 或 |i_CTRL-X_CTRL-T| 时发生错误。
	copy	    无法使用 |i_CTRL-Y| 或 |i_CTRL-E| 从插入模式复制字符。
	ctrlg	    在插入模式下，<C-G> 后出现未知字符。
	error	    发生其他错误（例如，尝试合并最后一行）
		    （主要用于 |普通模式| 或 |命令行模式|）。
	esc	        在 |普通模式| 下按 <Esc>。
	hangul	    已忽略。
	lang	    为 Lua/Mzscheme/TCL 调用响铃模块。
	mess	    没有可用于 |g<| 的输出。
	showmatch    'showmatch' 功能发生错误。
	operator	    空区域错误 |cpo-E|。
	register	    在 |插入模式| 中，<C-R> 后出现未知寄存器。
	shell	    来自 shell 输出的铃声 |:!|。
	spell	    拼写建议时发生错误。
	term	    来自 |:terminal| 输出的铃声。
	wildmode	    有更多 |命令行补全| 匹配项可用
		    （取决于 'wildmode' 设置）。

	这对于微调在插入模式下何时应该响铃最有用。对于普通模式和 Ex 命令，
	响铃通常用于指示发生错误。可以通过添加 "error" 关键字来静音。

				*'binary'* *'bin'* *'nobinary'* *'nobin'*
'binary' 'bin'		布尔	(默认关闭)
			本地到缓冲区
	在编辑二进制文件之前应设置此选项。你也可以使用 |-b| Vim 参数。
	当此选项打开时，一些选项会被更改（即使它已经打开）：
		'textwidth'  将被设置为 0
		'wrapmargin' 将被设置为 0
		'modeline'   将被关闭
		'expandtab'  将被关闭
	此外，'fileformat' 和 'fileformats' 选项将不会被使用，文件的读取和
	写入就像 'fileformat' 是 "unix" 一样（用单个 <NL> 分隔行）。
	'fileencoding' 和 'fileencodings' 选项将不会被使用，文件读取时不进行
	转换。
	注意：当 'bin' 选项打开时开始编辑（另一个）文件，自动命令的设置可能
	会再次更改设置（例如 'textwidth'），导致编辑时出现问题。你可能想在
	文件加载后再次设置 'bin'。
	这些选项的先前值会被记住，并在 'bin' 从打开切换到关闭时恢复。每个
	缓冲区都有自己的已保存选项值集。
	要编辑设置了 'binary' 的文件，可以使用 |++bin| 参数。这样可以避免你
	必须执行 ":set bin"，后者会影响你编辑的所有文件。
	写入文件时，仅当原始文件中存在最后一行的 <EOL> 时才会写入（通常如果
	最后一行没有 <EOL>，Vim 会附加一个 <EOL>；这会使文件变长）。参见
	'endofline' 选项。

						*'bomb'* *'nobomb'*
'bomb'			布尔	(默认关闭)
			本地到缓冲区
	写入文件且满足以下条件时，会在文件前加上 BOM（字节顺序标记）：
	- 此选项打开
	- 'binary' 选项关闭
	- 'fileencoding' 是 "utf-8"、"ucs-2"、"ucs-4" 或小端/大端变体之一。
	某些应用程序使用 BOM 来识别文件的编码。常用于 MS-Windows 上的 UCS-2
	文件。对于其他应用程序，这会引起问题，例如："cat file1 file2" 会使
	file2 的 BOM 出现在结果文件的中间。Gcc 不接受 BOM。
	当 Vim 读取文件且 'fileencodings' 以 "ucs-bom" 开头时，会检查 BOM
	的存在并相应设置 'bomb'。除非设置了 'binary'，否则它会被从第一行
	移除，这样你在编辑时就不会看到它。如果你不更改选项，写入文件时将
	恢复 BOM。

						*'breakat'* *'brk'*
'breakat' 'brk'		字符串	(默认 " ^I!@*-+;:,./?")
			全局
	此选项允许你选择哪些字符可能导致换行（如果 'linebreak' 打开）。
	仅对 ASCII 字符有效。

			*'breakindent'* *'bri'* *'nobreakindent'* *'nobri'*
'breakindent' 'bri'	布尔	(默认关闭)
			本地到窗口
	每个换行的行在视觉上将继续缩进（与该行开头相同的空格量），从而保留
	文本的水平块。

					*'breakindentopt'* *'briopt'*
'breakindentopt' 'briopt'	字符串	(默认 "")
			本地到窗口
	'breakindent' 的设置。它可以包含以下可选项目，并且必须用逗号分隔：
		min:{n}	    应用 'breakindent' 后保持的最小文本宽度，即使结果
			    文本通常应该更窄。这可以防止缩进几乎到窗口右边框的
			    文本在换行时占用大量垂直空间。
			    (默认: 20)
		shift:{n}   应用 'breakindent' 后，换行行的开头将移动给定的
			    字符数。它允许动态的法语段落缩进（负值）或强调
			    行继续（正值）。
			    (默认: 0)
		sbr	        在应用额外缩进之前显示 'showbreak' 值。
			    (默认: 关闭)
		list:{n}    为匹配编号或项目符号列表的行添加额外的缩进
			    （使用 'formatlistpat' 设置）。
			    (默认: 0)
		list:-1	    使用与 'formatlistpat' 匹配的宽度作为缩进。
		column:{n}  在列 {n} 缩进。将覆盖其他子选项。
			    注意：可能会为 'showbreak' 设置添加额外的缩进。
			    (默认: 关闭)

						*'bufhidden'* *'bh'*
'bufhidden' 'bh'	字符串	(默认 "")
			本地到缓冲区  |local-noglobal|
	此选项指定当缓冲区不再显示在窗口中时会发生什么：
	  <empty>	遵循全局的 'hidden' 选项
	  hide		隐藏缓冲区（不要卸载它），即使未设置 'hidden'
	  unload	卸载缓冲区，即使设置了 'hidden'；|:hide| 命令也会
			卸载缓冲区
	  delete	从缓冲区列表中删除缓冲区，即使设置了 'hidden'；
			|:hide| 命令也会删除缓冲区，使其行为类似于 |:bdelete|
	  wipe		从缓冲区列表中擦除缓冲区，即使设置了 'hidden'；
			|:hide| 命令也会擦除缓冲区，使其行为类似于 |:bwipeout|

	小心：当使用 "unload"、"delete" 或 "wipe" 时，缓冲区中的更改会丢失
	且不会发出警告。此外，这些值可能会破坏临时切换缓冲区的自动命令。
	此选项与 'buftype' 和 'swapfile' 一起用于指定特殊类型的缓冲区。
	参见 |special-buffers|。

			*'buflisted'* *'bl'* *'nobuflisted'* *'nobl'* *E85*
'buflisted' 'bl'	布尔	(默认开启)
			本地到缓冲区
	当设置此选项时，缓冲区会显示在缓冲区列表中。如果重置它，它不会用于
	":bnext"、"ls"、"Buffers" 菜单等。
	对于仅用于记住文件名或标记的缓冲区，Vim 会重置此选项。当开始编辑
	缓冲区时，Vim 会设置它。但使用 ":buffer" 移动到缓冲区时不会设置。

						*'buftype'* *'bt'* *E382*
'buftype' 'bt'		字符串	(默认 "")
			本地到缓冲区  |local-noglobal|
	此选项的值指定缓冲区的类型：
	  <empty>	普通缓冲区
	  acwrite	缓冲区将始终使用 |BufWriteCmd| 写入
	  help		帮助缓冲区（不要手动设置此项）
	  nofile	缓冲区与文件无关，不会被写入
	  nowrite	缓冲区不会被写入
	  prompt	缓冲区只有最后一部分可以编辑，供插件使用 |prompt-buffer|
	  quickfix	错误列表 |:cwindow| 或位置列表 |:lwindow|
	  terminal	|terminal-emulator| 缓冲区

	此选项与 'bufhidden' 和 'swapfile' 一起用于指定特殊类型的缓冲区。
	参见 |special-buffers|。
	另请参阅 |win_gettype()|，它返回窗口的类型。

	更改此选项时要小心，它可能产生许多副作用！一个这样的副作用是 Vim
	不会检查文件的时间戳，如果文件被另一个程序更改，将不会被注意到。

	"quickfix" 缓冲区仅用于错误列表和位置列表。此值由 |:cwindow| 和
	|:lwindow| 命令设置，你不应更改它。

	"nofile" 和 "nowrite" 缓冲区类似：
	两者：		缓冲区不会写入磁盘，":w" 不起作用（但 ":w filename"
			有效）。
	两者：		缓冲区永远不会被视为 'modified'。当更改将丢失时，
			例如退出 Vim 时，不会有警告。
	两者：		仅在内存使用过多时创建交换文件（当 'swapfile' 已重置时，
			绝不会创建交换文件）。
	nofile 仅：	缓冲区名称是固定的，不会像文件名一样处理。它不会响应
			|:cd| 命令而修改。
	两者：		当使用 ":e bufname" 且已经在编辑 "bufname" 时，缓冲区
			会被清空，并像 |:edit| 一样照常触发自动命令。
							*E676*
	"acwrite" 意味着缓冲区名称与文件无关，如 "nofile"，但它会被写入。
	因此，与 "nofile" 和 "nowrite" 相比，":w" 确实有效，并且不能在
	不保存的情况下放弃已修改的缓冲区。写入必须要有匹配的 |BufWriteCmd|、
	|FileWriteCmd| 或 |FileAppendCmd| 自动命令。

							*'busy'*
'busy'			数字	(默认 0)
			本地到缓冲区
	设置缓冲区的 "busy" 状态。在默认状态行中指示。当 busy 状态大于 0 时，
	状态行中会显示 busy 标志。"busy" 的语义是任意的，通常由拥有该缓冲区的
	插件决定。

						*'casemap'* *'cmp'*
'casemap' 'cmp'		字符串	(默认 "internal,keepascii")
			全局
	指定更改字母大小写的详细信息。它可以包含以下单词，以逗号分隔：
	internal	使用内部大小写映射函数，当前语言环境不会更改大小写
			映射。省略 "internal" 时，如果可用，则使用系统库函数
			towupper() 和 towlower()。
	keepascii	对于 ASCII 字符（0x00 到 0x7f），使用美国大小写映射，
			当前语言环境无效。这可能只对土耳其语有影响。

				*'cdhome'* *'cdh'* *'nocdhome'* *'nocdh'*
'cdhome' 'cdh'		布尔	(默认 Unix 上开启，Windows 上关闭)
			全局
	开启时，不带参数的 |:cd|、|:tcd| 和 |:lcd| 会将当前工作目录更改为
	|$HOME| 目录，就像在 Unix 中一样。关闭时，这些命令仅打印当前目录名。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'cdpath'* *'cd'* *E344* *E346*
'cdpath' 'cd'		字符串	(默认等同于 $CDPATH 或 ",,")
			全局
	这是一个目录列表，当使用 |:cd|、|:tcd| 和 |:lcd| 命令时，如果正在
	搜索的目录是相对路径，而不是以 "/"、"./" 或 "../" 开头的绝对路径，
	则会搜索此列表，否则不使用 'cdpath' 选项。
	'cdpath' 选项的值具有与 'path' 相同的形式和语义。另请参阅
	|file-searching|。
	默认值取自 $CDPATH，并在前面加上一个 "," 以便在当前目录中首先查找。
	如果取自 $CDPATH 的默认值不是您想要的，请在您的 vimrc 文件中包含以下
	命令的修改版本以覆盖它： >vim
	  let &cdpath = ',' .. substitute(substitute($CDPATH, '[, ]', '\\\0', 'g'), ':', ',', 'g')
<	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。
	（'cdpath' 的部分可以传递给 shell 以展开文件名）。

							*'cedit'*
'cedit'			字符串	(默认 CTRL-F)
			全局
	在命令行模式下用于打开命令行窗口的键。只允许使用非可打印键。
	该键可以指定为单个字符，但很难键入。首选方法是使用 |key-notation|
	（例如 <Up>、<C-F>）或前面带有脱字符的字母（例如 `^F` 是 CTRL-F）。
	示例： >vim
		set cedit=^Y
		set cedit=<Esc>
<	|Nvi| 也有此选项，但它只使用第一个字符。参见 |cmdwin|。

							*'channel'*
'channel'		数字	(默认 0)
			本地到缓冲区
	连接到缓冲区的 |channel|，如果没有连接通道则为 0。在 |:terminal|
	缓冲区中，这是终端通道。只读。

				*'charconvert'* *'ccv'* *E202* *E214* *E513*
'charconvert' 'ccv'	字符串	(默认 "")
			全局
	用于字符编码转换的表达式。当要读取或已写入的文件具有与期望不同的
	编码时，将评估此表达式。
	如果支持内部 iconv() 函数并且能够进行转换，则不使用 'charconvert'。
	首选使用 iconv()，因为它快得多。
	读取 stdin |--| 时不使用 'charconvert'，因为没有要转换的文件。你
	必须先将文本保存在文件中。
	表达式必须返回零、false 或空字符串表示成功，返回非零或 true 表示
	失败。
	有关可能的编码名称，请参见 |encoding-names|。
	此外，还会使用 'fileencodings' 和 'fileencoding' 中给出的名称。
	Vim 内部处理 "latin1"、"unicode"、"ucs-2"、"ucs-4" 和 "utf-8" 之间的
	转换，为此不使用 'charconvert'。也用于 Unicode 转换。
	示例： >vim
		set charconvert=CharConvert()
		fun CharConvert()
		  system("recode "
			\ .. v:charconvert_from .. ".." .. v:charconvert_to
			\ .. " <" .. v:fname_in .. " >" .. v:fname_out)
		  return v:shell_error
		endfun
<	相关的 Vim 变量是：
		v:charconvert_from	当前编码的名称
		v:charconvert_to	所需编码的名称
		v:fname_in		输入文件的名称
		v:fname_out		输出文件的名称
	注意 v:fname_in 和 v:fname_out 永远不会相同。

	使用无参数的函数调用的优点是速度更快，参见 |expr-option-function|。

	如果 'charconvert' 表达式以 s: 或 |<SID>| 开头，则会被替换为脚本 ID
	（|local-function|）。示例： >vim
		set charconvert=s:MyConvert()
		set charconvert=<SID>SomeConvert()
<	否则，表达式将在设置选项的脚本上下文中评估，因此脚本本地项目可用。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'chistory'* *'chi'* *E1542* *E1543*
'chistory' 'chi'	数字	(默认 10)
			全局
	快速修复堆栈应记住的快速修复列表数量。必须在 1 到 100 之间。
	如果该选项设置为低于快速修复列表堆栈中条目数的值，将从最旧的条目开始
	移除条目。如果当前快速修复列表被移除，则将使用堆栈顶部的快速修复列表
	（最近创建的）代替。有关其他信息，请参见 |quickfix-stack|。

				*'cindent'* *'cin'* *'nocindent'* *'nocin'*
'cindent' 'cin'		布尔	(默认关闭)
			本地到缓冲区
	启用自动 C 程序缩进。请参阅 'cinkeys' 以设置在插入模式下触发重新缩进
	的键，并参阅 'cinoptions' 以设置你喜欢的缩进样式。
	如果 'indentexpr' 不为空，它将覆盖 'cindent'。
	如果 'lisp' 未打开，且 'indentexpr' 和 'equalprg' 都为空，则 "="
	运算符使用此算法进行缩进，而不是调用外部程序。
	参见 |C-indenting|。
	如果你不喜欢 'cindent' 的工作方式，可以尝试 'smartindent' 选项或
	'indentexpr'。

						*'cinkeys'* *'cink'*
'cinkeys' 'cink'	字符串	(默认 "0{,0},0),0],:,0#,!^F,o,O,e")
			本地到缓冲区
	在插入模式下键入时会导致当前行重新缩进的键列表。仅当 'cindent' 打开
	且 'indentexpr' 为空时使用。
	有关此选项的格式，请参见 |cinkeys-format|。
	参见 |C-indenting|。

						*'cinoptions'* *'cino'*
'cinoptions' 'cino'	字符串	(默认 "")
			本地到缓冲区
	'cinoptions' 影响 'cindent' 在 C 程序中重新缩进行的方式。有关此选项
	的值，请参见 |cinoptions-values|；有关 C 缩进的一般信息，请参见
	|C-indenting|。

					*'cinscopedecls'* *'cinsd'*
'cinscopedecls' 'cinsd'	字符串	(默认 "public,protected,private")
			本地到缓冲区
	被 |cino-g| 解释为 C++ 作用域声明的关键字。例如，对于使用 Qt 框架
	（该框架定义了额外的作用域声明 "signals"、"public slots" 和
	"private slots"）的情况很有用： >vim
		set cinscopedecls+=signals,public\ slots,private\ slots
<

						*'cinwords'* *'cinw'*
'cinwords' 'cinw'	字符串	(默认 "if,else,while,do,for,switch")
			本地到缓冲区
	当设置了 'smartindent' 或 'cindent' 时，这些关键字会在下一行开始
	额外的缩进。对于 'cindent'，这仅在适当的位置（在 {} 内）进行。
	注意，'ignorecase' 不用于 'cinwords'。如果大小写无关紧要，请同时包含
	关键字的大写和小写形式："if,If,IF"。

						*'clipboard'* *'cb'*
'clipboard' 'cb'	字符串	(默认 "")
			全局
	此选项是逗号分隔的名称列表。识别以下名称：

						*clipboard-unnamed*
	unnamed		当包含此项时，Vim 会将剪贴板寄存器 "*" 用于通常进入
			无名寄存器的所有拉取、删除、更改和放置操作。当显式
			指定寄存器时，无论 'clipboard' 中是否包含 "unnamed"，
			都将始终使用该寄存器。剪贴板寄存器始终可以使用 "*"
			符号显式访问。另请参阅 |clipboard|。

						*clipboard-unnamedplus*
	unnamedplus	"unnamed" 标志的一个变体，它对通常进入无名寄存器的
			所有拉取、删除、更改和放置操作使用剪贴板寄存器 "+"
			（|quoteplus|）而不是寄存器 "*"。当选项中也包含
			"unnamed" 时，拉取和删除操作（但不包括放置）将额外
			将文本复制到寄存器 "*"。请参阅 |clipboard|。

						*'cmdheight'* *'ch'*
'cmdheight' 'ch'	数字	(默认 1)
			全局或本地到标签页
	用于命令行的屏幕行数。有助于避免 |hit-enter| 提示。
	此选项的值与标签页一起存储，因此每个标签页可以有不同的值。

	当 'cmdheight' 为零时，除非正在使用，否则没有命令行。命令行显示时
	将覆盖屏幕的最后一行。

	警告： `cmdheight=0` 是实验性功能。在启用 |ui2| 时效果更佳。

						*'cmdwinheight'* *'cwh'*
'cmdwinheight' 'cwh'	数字	(默认 7)
			全局
	用于命令行窗口的屏幕行数。|cmdwin|

						*'colorcolumn'* *'cc'*
'colorcolumn' 'cc'	字符串	(默认 "")
			本地到窗口
	'colorcolumn' 是一个逗号分隔的屏幕列列表，这些列会用 ColorColumn
	|hl-ColorColumn| 高亮显示。有助于对齐文本。会使屏幕重绘变慢。
	屏幕列可以是绝对数字，也可以是前面带有 '+' 或 '-' 的数字，表示与
	'textwidth' 相加或相减。 >vim

		set cc=+1	  " 高亮显示 'textwidth' 之后的一列
		set cc=+1,+2,+3  " 高亮显示 'textwidth' 之后的三列
		hi ColorColumn ctermbg=lightgrey guibg=lightgrey
<
	当 'textwidth' 为零时，带有 '-' 和 '+' 的项目不被使用。最多高亮显示
	256 列。

						*'columns'* *'co'* *E594*
'columns' 'co'		数字	(默认 80 或终端宽度)
			全局
	屏幕的列数。通常由终端初始化设置，无需手动设置。
	当 Vim 在 GUI 中或在可调整大小的窗口中运行时，设置此选项将导致窗口
	大小更改。如果你只想为 GUI 使用该大小，请将命令放在你的 |ginit.vim|
	文件中。
	当你设置此选项且 Vim 无法更改显示器的物理列数时，显示可能会混乱。
	对于 GUI，总是可以更改，并且 Vim 会将列数限制为适合屏幕的数量。
	你可以使用此命令获得尽可能宽的窗口： >vim
		set columns=9999
<	最小值为 12，最大值为 10000。

						*'comments'* *'com'* *E524* *E525*
'comments' 'com'	字符串	(默认 "s1:/*,mb:*,ex:*/,://,b:#,:%,:XCOMM,n:>,fb:-,fb:•")
			本地到缓冲区
	一个逗号分隔的字符串列表，这些字符串可以开始一个注释行。参见
	|format-comments|。关于使用反斜杠插入空格，请参见 |option-backslash|。

					*'commentstring'* *'cms'* *E537*
'commentstring' 'cms'	字符串	(默认 "")
			本地到缓冲区
	注释的模板。值中的 "%s" 会被替换为注释文本，并且可能的话应填充一个空格。
	用于 |commenting| 和添加折叠标记，参见 |fold-marker|。

						*'complete'* *'cpt'* *E535*
'complete' 'cpt'	字符串	(默认 ".,w,b,u,t")
			本地到缓冲区
	此选项控制使用 CTRL-P、CTRL-N 或 |ins-autocompletion| 时补全
	|ins-completion| 的行为。它也用于整行补全 |i_CTRL-X_CTRL-L|。
	它指示补全的类型和要扫描的位置。它是一个逗号分隔的标志列表：
	.	扫描当前缓冲区（忽略 'wrapscan'）
	w	扫描其他窗口中的缓冲区
	b	扫描缓冲区列表中已加载的其他缓冲区
	u	扫描缓冲区列表中未加载的缓冲区
	U	扫描不在缓冲区列表中的缓冲区
	k	扫描 'dictionary' 选项给出的文件
	kspell	使用当前激活的拼写检查 |spell|
	k{dict}	扫描文件 {dict}。可以给出多个 "k" 标志，模式也有效。例如： >vim
			set cpt=k/usr/dict/*,k~/spanish
<	s	扫描 'thesaurus' 选项给出的文件
	s{tsr}	扫描文件 {tsr}。可以给出多个 "s" 标志，模式也有效。
	i	扫描当前文件和包含的文件
	d	扫描当前文件和包含的文件以获取定义名称或宏
		|i_CTRL-X_CTRL-D|
	]	标签补全
	t	同 "]"
	f	扫描缓冲区名称（与缓冲区内容相对）
	F{func}	调用函数 {func}。可以指定多个 "F" 标志。有关如何调用函数以及
		它应返回什么的详细信息，请参阅 |complete-functions|。该值可以
		是函数名或 |Funcref|。对于 |Funcref| 值，空格必须用反斜杠
		（'\'）转义，逗号用双反斜杠（'\\'）转义（参见 |option-backslash|）。
		与其他源不同，函数可以提供从光标前非关键字字符开始的补全，
		并且它们用于替换文本的起始位置可能与其他源不同。如果 {func}
		返回的字典包含 `{"refresh": "always"}`，则每当引导文本更改时，
		将再次调用该函数。
		如果生成匹配项可能很慢，请定期调用 |complete_check()| 以保持
		Vim 的响应性。这对于 |ins-autocompletion| 尤其重要。
	F	相当于使用 "F{func}"，其中函数取自 'completefunc' 选项。
	o	相当于使用 "F{func}"，其中函数取自 'omnifunc' 选项。

	未加载的缓冲区不会被加载，因此它们的自动命令 |:autocmd| 不会被执行，
	这可能导致从某些文件（例如 gzip 压缩的文件）中获得意外的补全。未加载的
	缓冲区不会为整行补全进行扫描。

	CTRL-N、CTRL-P 和 |ins-autocompletion| 可用于任何基于 'iskeyword' 的补全
	（字典 |i_CTRL-X_CTRL-K|、包含模式 |i_CTRL-X_CTRL-I|、标签
	|i_CTRL-X_CTRL-]| 和普通展开）。通过 'complete' 中的 "F" 和 "o" 标志，
	也可以补全非关键字。

	可以通过在源标志后附加脱字符（"^"）后跟 {count} 来为补全源指定可选的
	匹配限制。例如：".^9,w,u,t^5" 将当前缓冲区的匹配限制为 9 个，标签的
	匹配限制为 5 个。其他源保持无限制。
	注意：匹配限制仅在前向补全（CTRL-N）期间生效，在后向补全（CTRL-P）期间
	被忽略。

						*'completefunc'* *'cfu'*
'completefunc' 'cfu'	字符串	(默认 "")
			本地到缓冲区
	此选项指定一个函数，用于使用 CTRL-X CTRL-U 进行插入模式补全。
	|i_CTRL-X_CTRL-U|
	有关如何调用函数以及它应返回什么的说明，请参阅 |complete-functions|。
	该值可以是函数名、|lambda| 或 |Funcref|。更多信息请参见
	|option-value-function|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'completeitemalign'* *'cia'*
'completeitemalign' 'cia'	字符串	(默认 "abbr,kind,menu")
			全局
	一个逗号分隔的字符串列表，控制插入模式补全期间弹出菜单中项目的对齐方式
	和显示顺序。支持的值有 "abbr"、"kind" 和 "menu"。这些值允许自定义
	|complete-items| 在弹出菜单中的显示方式。注意：必须始终包含这三个值，
	顺序不限。

						*'completeopt'* *'cot'*
'completeopt' 'cot'	字符串	(默认 "menu,popup")
			全局或本地到缓冲区 |global-local|
	插入模式补全 |ins-completion| 的选项列表，以逗号分隔。支持的值有：

	   fuzzy    为补全候选项启用 |fuzzy-matching|。这允许更灵活、更直观的匹配，
		    可以跳过字符，即使没有键入精确序列也能找到匹配项
		    （对同义词库补全 |compl-thesaurus| 禁用）。

	   longest  当 'autocomplete' 未激活时，仅插入匹配项的最长公共前缀
		    （对同义词库补全 |compl-thesaurus| 禁用）。如果显示弹出菜单，
		    可以使用 CTRL-L 添加更多字符。是否忽略大小写取决于补全类型。
		    对于缓冲区文本，适用 'ignorecase' 选项。

		    当 'autocomplete' 激活且未选择补全项时，光标后插入匹配项的最长
		    公共前缀。前缀取自所有显示的项目或仅取自当前缓冲区中的项目。
		    插入的文本使用 |hl-PreInsert| 高亮显示，光标位置不变
		    （类似于 `"preinsert"`）。按 CTRL-Y 接受。
		    另请参见 |preinserted()|。

	   menu	  使用弹出菜单显示可能的补全项。只有当有多个匹配项且可用颜色足够时，
		    才会显示菜单。|ins-completion-menu|

	   menuone 即使只有一个匹配项，也显示弹出菜单。当有关于匹配项的额外信息时
		    （例如它来自哪个文件），这很有用。

	   nearest  匹配项根据它们与光标位置的接近程度列出，而默认行为只考虑光标
		    下方匹配项的接近程度。这仅适用于当前缓冲区的匹配项。
		    如果存在 "fuzzy"，则无效。

	   noinsert 在用户从菜单中选择匹配项之前，不插入任何匹配文本。仅与 "menu"
		    或 "menuone" 结合使用。如果存在 "longest"，则无效。

	   noselect 与 "noinsert" 相同，只是没有预先选择菜单项。如果同时存在
		    "noinsert" 和 "noselect"，"noselect" 优先。当 'autocomplete'
		    开启时，除非也启用了 "preinsert"，否则会自动启用此选项。

	   nosort   启用 "fuzzy" 时，禁用基于模糊分数对补全候选项的排序。候选项将
		    以其原始顺序显示。

	   popup    在弹出窗口中显示有关当前所选补全项的额外信息。仅与 "menu" 或
		    "menuone" 结合使用。覆盖 "preview"。

	   preinsert
		    插入第一个补全候选项超出当前前导符的文本，使用 |hl-PreInsert|
		    高亮显示。光标不移动。
		    要求 "fuzzy" 未设置，并且 'completeopt' 中设置了 "menuone"
		    或启用了 'autocomplete'。当启用 'autocomplete' 时，如果设置了
		    'ignorecase' 而未设置 'infercase'，则此功能无效。
		    另请参见 |preinserted()|。

	   preview  在预览窗口中显示有关当前所选补全项的额外信息。仅与 "menu" 或
		    "menuone" 结合使用。

	当启用 'autocomplete' 时，只有 "fuzzy"、"longest"、"popup"、"preinsert"
	和 "preview" 有效。

	此选项不适用于 |cmdline-completion|。有关命令行补全，请参阅 'wildoptions'。

						*'completeslash'* *'csl'*
'completeslash' 'csl'	字符串	(默认 "")
			本地到缓冲区
			仅在 MS-Windows 上可修改
	设置此选项时，它将覆盖补全时的 'shellslash'：
	- 当此选项设置为 "slash" 时，插入模式下的路径补全使用正斜杠。
	  这在 MS-Windows 上编辑 HTML 标签或使用 'noshellslash' 的 Makefile
	  时很有用。
	- 当此选项设置为 "backslash" 时，使用反斜杠。这在 MS-Windows 上编辑
	  批处理文件且设置了 'shellslash' 时很有用。
	- 当此选项为空时，使用与 'shellslash' 相同的字符。
	对于插入模式补全，使用缓冲区本地值。对于命令行补全，使用全局值。

					*'completetimeout'* *'cto'*
'completetimeout' 'cto'	数字	(默认 0)
			全局
	类似于 'autocompletetimeout'，但适用于 |i_CTRL-N| 和 |i_CTRL-P| 补全。
	值为 0 禁用超时；允许正数值。

					*'concealcursor'* *'cocu'*
'concealcursor' 'cocu'	字符串	(默认 "")
			本地到窗口
	设置在哪些模式下光标行中的文本也可以被隐藏。当当前模式被列出时，
	隐藏行为与其他行相同。
	  n		普通模式
	  v		可视模式
	  i		插入模式
	  c		命令行编辑，用于 'incsearch'

	'v' 适用于可视区域中的所有行，而不仅仅是光标行。
	一个有用的值是 "nc"。这在帮助文件中使用。只要你在移动，文本就会被隐藏，
	但当开始插入文本或选择可视区域时，隐藏的文本会显示出来，这样你可以看到
	你在做什么。
	请记住，光标位置并不总是它显示的位置。例如，垂直移动时，它可能会改变列。

						*'conceallevel'* *'cole'*
'conceallevel' 'cole'	数字	(默认 0)
			本地到窗口
	确定带有 "conceal" 语法属性的文本 |:syn-conceal| 的显示方式：

	值		效果 ~
	0		文本正常显示
	1		每个隐藏文本块被替换为一个字符。如果语法项没有定义
			自定义替换字符（参见 |:syn-cchar|），则使用 'listchars'
			中定义的字符。它使用 "Conceal" 高亮组高亮显示。
	2		隐藏文本完全隐藏，除非定义了自定义替换字符
			（参见 |:syn-cchar|）。
	3		隐藏文本完全隐藏。

	注意：在光标行中，隐藏文本不会被隐藏，以便你可以编辑和复制文本。这可以
	通过 'concealcursor' 选项更改。

				*'confirm'* *'cf'* *'noconfirm'* *'nocf'*
'confirm' 'cf'		布尔	(默认关闭)
			全局
	当 'confirm' 开启时，某些通常因缓冲区未保存更改而失败的操作，例如
	":q" 和 ":e"，会弹出一个对话框询问你是否希望保存当前文件。你仍然可以
	使用 ! 无条件地 |abandon| 缓冲区。
	如果 'confirm' 关闭，你仍然可以使用 |:confirm| 命令仅为一个命令激活
	确认（这在映射中最有用）。
	另请参阅 |confirm()| 函数和 'guioptions' 中的 'v' 标志。

			*'copyindent'* *'ci'* *'nocopyindent'* *'noci'*
'copyindent' 'ci'	布尔	(默认关闭)
			本地到缓冲区
	在自动缩进新行时，复制现有行的缩进结构。通常，新的缩进是通过一系列
	制表符后跟所需的空格重建的（除非启用了 'expandtab'，在这种情况下只使用
	空格）。启用此选项使新行复制现有行上用于缩进的任何字符。'expandtab'
	对这些字符没有影响，制表符仍然是制表符。如果新缩进大于现有行的缩进，
	剩余空间将以正常方式填充。参见 'preserveindent'。

						*'cpoptions'* *'cpo'* *cpo*
'cpoptions' 'cpo'	字符串	(默认 "aABceFs_")
			全局
	单字符标志序列。当存在某个字符时，表示 Vi 兼容的行为。这用于那些
	不兼容 Vi 的行为大部分或有时更受偏好的情况。
	'cpoptions' 代表 "compatible-options"。
	可以添加逗号以提高可读性。
	为了避免将来添加标志时出现问题，请使用 ":set" 的 "+=" 和 "-=" 功能
	|add-option-flags|。

	    包含项	行为	~
								*cpo-a*
		a	当包含时，带文件名的 ":read" 命令将为当前窗口设置
			备用文件名。
								*cpo-A*
		A	当包含时，带文件名的 ":write" 命令将为当前窗口设置
			备用文件名。
								*cpo-b*
		b	":map" 命令中的 "\|" 被识别为映射命令的结束。'\' 被
			包含在映射中，'|' 之后的文本被解释为下一个命令。
			使用 CTRL-V 而不是反斜杠来在映射中包含 '|'。适用于所有
			映射、缩写、菜单和自动命令命令。另请参阅 |map_bar|。
								*cpo-B*
		B	反斜杠在映射、缩写、用户命令和菜单命令的 "to" 部分中
			没有特殊含义。移除此标志以便能够像使用 CTRL-V 一样使用
			反斜杠。例如，命令 ":map X \<Esc>" 导致 X 被映射为：
				'B' 包含：	"\^["   (^[ 是一个真正的 <Esc>)
				'B' 排除：	"<Esc>"  (5 个字符)
								*cpo-c*
		c	搜索在光标位置的任何匹配项的末尾继续，但不超过下一行的
			开头。当不存在时，搜索从光标位置起一个字符处继续。
			使用 'c' 时，"abababababab" 在重复 "/abab" 时只得到三个
			匹配项，没有 'c' 时有五个匹配项。
								*cpo-C*
		C	不要连接以反斜杠开头的引用行。参见 |line-continuation|。
								*cpo-d*
		d	在 'tags' 选项中使用 "./" 并不意味着使用相对于当前
			文件的 tags 文件，而是使用当前目录中的 tags 文件。
								*cpo-D*
		D	在普通模式命令（如 |r|、|f| 和 |t|）中使用带字符参数的
			命令后，不能使用 CTRL-K 输入二合字母。
								*cpo-e*
		e	当使用 ":@r" 执行寄存器时，总是在最后一行添加一个 <CR>，
			即使寄存器不是行方式的。如果不存在此标志，寄存器不是
			行方式的，且最后一行不以 <CR> 结尾，则最后一行将放在
			命令行上，可以在按 <CR> 之前进行编辑。
								*cpo-E*
		E	在空区域上使用 "y"、"d"、"c"、"g~"、"gu" 或 "gU" 是一个
			错误。这些操作符仅当至少有一个字符要操作时才工作。示例：
			这使得 "y0" 在第一列失败。
								*cpo-f*
		f	当包含时，如果当前缓冲区还没有文件名，带文件名的
			":read" 命令将为当前缓冲区设置文件名。
								*cpo-F*
		F	当包含时，如果当前缓冲区还没有文件名，带文件名的
			":write" 命令将为当前缓冲区设置文件名。另请参见 |cpo-P|。
								*cpo-i*
		i	当包含时，中断读取文件将使其保持已修改状态。
								*cpo-I*
		I	在为 'autoindent' 插入缩进后向上或向下移动光标时，
			不要删除缩进。
								*cpo-J*
		J	|sentence| 必须在 '.'、'!' 或 '?' 后跟两个空格。
			<Tab> 不被识别为空白字符。
								*cpo-K*
		K	当按键代码在映射中只读取了部分时，不要等待其完成。
			这会在只读取了第二个 <F1> 的一部分时中断映射 <F1><F1>。
			它允许通过键入 <F1><Esc> 取消映射。
								*cpo-l*
		l	搜索模式中 [] 范围内的反斜杠被视为字面量，只有 "\]"、
			"\^"、"\-" 和 "\\" 是特殊的。参见 |/[]|
			   'l' 包含： "/[ \t]"  找到 <Space>、'\' 和 't'
			   'l' 排除： "/[ \t]"  找到 <Space> 和 <Tab>
								*cpo-L*
		L	当设置 'list' 选项时，'wrapmargin'、'textwidth'、
			'softtabstop' 和虚拟替换模式（参见 |gR|）将 <Tab> 计为
			两个字符，而不是 <Tab> 的正常行为。
								*cpo-m*
		m	当包含时，showmatch 将始终等待半秒钟。当不包含时，
			showmatch 将等待半秒钟或直到键入一个字符。'showmatch'
								*cpo-M*
		M	当排除时，"%" 匹配将考虑反斜杠。因此，在 "( \( )" 和
			"\( ( \)" 中，外部的括号匹配。当包含时，"%" 忽略反斜杠，
			这是 Vi 兼容的。
								*cpo-n*
		n	当包含时，用于 'number' 和 'relativenumber' 的列也将用于
			换行行的文本。
								*cpo-o*
		o	搜索命令的行偏移不会为下一次搜索记住。
								*cpo-O*
		O	即使文件在编辑时不存在，也不要抱怨文件被覆盖。这是一种
			防止他人意外创建文件的保护措施。Vi 对此不抱怨。
								*cpo-P*
		P	当包含时，如果当前缓冲区还没有文件名，并且 'F' 标志也被
			包含（|cpo-F|），则追加到文件的 ":write" 命令将为当前
			缓冲区设置文件名。
								*cpo-q*
		q	当连接多行时，将光标留在连接两行时光标会处于的位置。
								*cpo-r*
		r	重做（"." 命令）使用 "/" 来重复搜索命令，而不是实际使用
			的搜索字符串。
								*cpo-R*
		R	从过滤的行中移除标记。没有此标志时，标记会被保留，就像
			使用了 |:keepmarks| 一样。
								*cpo-s*
		s	首次进入缓冲区时设置缓冲区选项。这就像 Vim 3.0 版本中的
			那样。这是默认值。如果不存在，则在创建缓冲区时设置选项。
								*cpo-S*
		S	始终在进入缓冲区时设置缓冲区选项（除了 'readonly'、
			'fileformat'、'filetype' 和 'syntax'）。这是（最）Vi 兼容
			的设置。选项被设置为当前缓冲区中的值。当你更改一个选项并
			转到另一个缓冲区时，该值会被复制。实际上使缓冲区选项对所有
			缓冲区都是全局的。

			's'    'S'     复制缓冲区选项
			否     否      创建缓冲区时
			是     否      首次进入缓冲区时（默认）
			 X     是      每次进入缓冲区时（vi 兼容）
								*cpo-t*
		t	tag 命令的搜索模式被记住用于 "n" 命令。否则，Vim 仅将
			模式放入搜索模式的历史记录中，但不更改上次使用的搜索模式。
								*cpo-u*
		u	撤销是 Vi 兼容的。参见 |undo-two-ways|。
								*cpo-v*
		v	在插入模式下，退格的字符在屏幕上仍然可见。如果没有此标志，
			字符会立即从屏幕上擦除。有此标志时，屏幕上新键入的文本会
			覆盖退格的字符。
								*cpo-W*
		W	不要覆盖只读文件。当省略时，":w!" 会覆盖只读文件（如果可能）。
								*cpo-x*
		x	命令行上的 <Esc> 执行命令行。Vim 中的默认设置是放弃
			命令行，因为 <Esc> 通常会中止命令。|c_<Esc>|
								*cpo-X*
		X	当与 "R" 一起使用计数时，替换的文本只被删除一次。当使用
			计数重复 "R" 和 "." 时也是如此。
								*cpo-y*
		y	拉取命令可以用 "." 重做。请三思是否真的想使用此功能，
			因为它可能会破坏某些插件，因为大多数人期望 "." 只重复更改。
								*cpo-Z*
		Z	当设置了 'readonly' 选项时使用 "w!"，不要重置 'readonly'。
								*cpo-!*
		!	当重做过滤器命令时，使用上次使用的外部命令，无论它是什么。
			否则，使用上次使用的 -filter- 命令。
								*cpo-$*
		$	当对一行进行更改时，不重新显示该行，而是在更改文本的末尾
			放置一个 '$'。当你键入新文本时，更改的文本将被覆盖。
			如果你键入任何将光标从插入点移开的命令，该行将重新显示。
								*cpo-%*
		%	为 "%" 命令执行 Vi 兼容的匹配。不识别 "#if"、"#endif" 等。
			不识别 "/*" 和 "*/"。
			单引号和双引号内的括号也会被计数，导致包含括号的字符串
			干扰匹配。例如，在 "if (strcmp("foo(", s))" 这样的行中，
			第一个括号与最后一个不匹配。当不包含此标志时，单引号和
			双引号内的括号会被特殊处理。当匹配引号外的括号时，引号内的
			所有内容都被忽略。当匹配引号内的括号时，它会找到匹配的括号
			（如果存在）。这对于 C 程序非常有效。
			此标志也用于其他功能，如 C 缩进。
								*cpo-+*
		+	当包含时，":write file" 命令将重置缓冲区的 'modified' 标志，
			即使缓冲区本身可能仍与其文件不同。
								*cpo->*
		>	当追加到寄存器时，在追加的文本前放置一个换行符。
								*cpo-;*
		;	当使用 |,| 或 |;| 重复上次的 |t| 搜索，并且光标正好在
			被搜索字符的前面时，光标不会移动。当不包含时，光标会跳过它
			并跳转到下一个出现的位置。
								*cpo-~*
		~	当包含时，使用 |:cd|、|:lcd| 或 |:tcd| 更改目录时不解析
			符号链接。这会在缓冲区名称和显示当前目录时保留符号链接路径。
			当排除时（默认），符号链接会被解析为其目标路径。
								*cpo-_*
		_	当在单词上使用 |cw| 时，不要将单词后面的空白包含在动作中。

						*'cursorbind'* *'crb'* *'nocursorbind'* *'nocrb'*
'cursorbind' 'crb'	布尔	(默认关闭)
			本地到窗口
	当设置此选项时，随着当前窗口中的光标移动，其他光标绑定窗口（也设置了此选项
	的窗口）会将其光标移动到相应的行和列。此选项对于查看两个版本文件之间的差异
	很有用（参见 'diff'）；在 diff 模式下，插入和删除的行（尽管不是行内的字符）
	会被考虑在内。

			*'cursorcolumn'* *'cuc'* *'nocursorcolumn'* *'nocuc'*
'cursorcolumn' 'cuc'	布尔	(默认关闭)
			本地到窗口
	使用 CursorColumn |hl-CursorColumn| 高亮显示光标的屏幕列。有助于对齐文本。
	会使屏幕重绘变慢。
	如果你只想在当前窗口中高亮显示，可以使用这些自动命令： >vim
		au WinLeave * set nocursorline nocursorcolumn
		au WinEnter * set cursorline cursorcolumn
<

			*'cursorline'* *'cul'* *'nocursorline'* *'nocul'*
'cursorline' 'cul'	布尔	(默认关闭)
			本地到窗口
	使用 CursorLine |hl-CursorLine| 高亮显示光标所在的文本行。有助于轻松定位
	光标。会使屏幕重绘变慢。
	当可视模式激活时，不使用此高亮显示，以便更容易看到选中的文本。

					*'cursorlineopt'* *'culopt'*
'cursorlineopt' 'culopt'	字符串	(默认 "both")
			本地到窗口
	用于设置 'cursorline' 显示方式的逗号分隔列表。有效值：
	"line"		使用 CursorLine |hl-CursorLine| 高亮显示光标所在的文本行。
	"screenline"	仅使用 CursorLine |hl-CursorLine| 高亮显示光标所在的屏幕行。
	"number"	使用 CursorLineNr |hl-CursorLineNr| 高亮显示光标所在的行号。

	特殊值：
	"both"		值 "line,number" 的别名。

	"line" 和 "screenline" 不能同时使用。

							*'debug'*
'debug'			字符串	(默认 "")
			全局
	可以使用以下值：
	msg	否则会被省略的错误消息将被给出。
	throw	否则会被省略的错误消息将被给出，并且还会抛出异常并设置 |v:errmsg|。
	beep	当否则只会发出哔哔声时，将给出消息。
	这些值可以组合使用，以逗号分隔。
	"msg" 和 "throw" 对于调试 'foldexpr'、'formatexpr' 或 'indentexpr' 很有用。

						*'define'* *'def'*
'define' 'def'		字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	用于查找宏定义的模式。这是一个搜索模式，就像 "/" 命令一样。此选项用于
	诸如 "[i" 和 "[d" |include-search| 之类的命令。'isident' 选项用于识别
	匹配后的定义名称： >
		{匹配 'define' 的内容}{非 ID 字符}{定义的名称}{非 ID 字符}
<	有关插入反斜杠以包含空格或反斜杠的信息，请参见 |option-backslash|。
	对于 C++，此值将很有用，以包含 const 类型声明： >
		^\(#\s*define\|[a-z]*\s*const\s*[a-z]*\)
<	你也可以在名称前使用 "\ze" 并继续模式以检查后面的内容。例如，对于
	JavaScript，如果函数是用 `func_name = function(args)` 定义的： >
		^\s*\ze\i\+\s*=\s*function(
<	如果函数是用 `func_name : function() {...` 定义的： >
	        ^\s*\ze\i\+\s*[:]\s*(*function\s*(
<	使用 ":set" 命令时，你需要将反斜杠加倍！为避免这种情况，请使用带有单引号
	字符串的 `:let`： >vim
		let &l:define = '^\s*\ze\k\+\s*=\s*function('
<

			*'delcombine'* *'deco'* *'nodelcombine'* *'nodeco'*
'delcombine' 'deco'	布尔	(默认关闭)
			全局
	如果编辑 Unicode 且设置了此选项，退格键和普通模式下的 "x" 会单独删除每个
	组合字符。当它关闭时（默认），字符及其组合字符会被一起删除。
	注意：当设置了 'delcombine' 时，"xx" 的工作方式可能与 "2x" 不同！

	这对于阿拉伯语、希伯来语和许多其他语言很有用，在这些语言中，基本字符之上
	可能有组合字符，而用户只想删除组合字符。

						*'dictionary'* *'dict'*
'dictionary' 'dict'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	文件名列表，以逗号分隔，用于查找关键字补全命令 |i_CTRL-X_CTRL-K| 的单词。
	每个文件应包含一个单词列表。可以每行一个单词，或每行多个单词，由非关键字
	字符分隔（最好使用空格）。最大行长为 510 字节。

	当此选项为空或包含条目 "spell"，并且拼写检查启用时，将使用当前激活的
	'spelllang' 的单词列表中的单词。参见 |spell|。

	要在文件名中包含逗号，请在逗号前加反斜杠。逗号后的空格被忽略，否则空格会
	被包含在文件名中。有关使用反斜杠的信息，请参见 |option-backslash|。
	环境变量会被展开 |:set_env|。
	这与 |Dictionary| 变量类型无关。
	在哪里可以找到单词列表？
	- BSD/macOS 包含 "/usr/share/dict/words" 文件。
	- 在 apt 管理的系统（Debian/Ubuntu）上，尝试 "apt install spell" 以获取
	  "/usr/share/dict/words" 文件。
	在列表中添加或删除目录时，最好使用 |:set+=| 和 |:set-=|。这可以避免将来
	版本使用其他默认值时出现的问题。
	出于安全原因，此选项中不能使用反引号。

						*'diff'* *'nodiff'*
'diff'			布尔	(默认关闭)
			本地到窗口
	将当前窗口加入到显示文件之间差异的窗口组中。参见 |diff-mode|。

						*'diffanchors'* *'dia'*
'diffanchors' 'dia'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	每个缓冲区中的 {address} 列表，以逗号分隔，这些地址在用于比较时被视为锚点。
	指定 "$+1" 表示最后一行的下一行是有效的。此选项不能使用 "%"。每个缓冲区
	最多可以设置 20 个锚点。

	每个锚点行分割缓冲区（分割发生在锚点上方），每个部分分别进行比较，然后合并
	最终结果。当提供多个 {address} 时，锚点将按行号内部排序。如果使用缓冲区
	本地选项，每个缓冲区应具有相同数量的锚点（多余的锚点将被忽略）。仅当
	'diffopt' 设置了 "anchor" 时，才使用此选项。有关更多详细信息和示例，
	请参见 |diff-anchors|。
								*E1550*
	如果某些 {address} 在每个缓冲区中都无法解析为一行（例如，模式搜索没有匹配
	任何内容），则不会使用任何锚点。
								*E1562*
	仅当没有隐藏的 diff 缓冲区时，才能使用 diff 锚点。

						*'diffexpr'* *'dex'*
'diffexpr' 'dex'	字符串	(默认 "")
			全局
	用于从文件的两个版本获取 diff 文件（ed 风格或统一风格）的表达式。
	参见 |diff-diffexpr|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'diffopt'* *'dip'*
'diffopt' 'dip'		字符串	(默认 "internal,filler,closeoff,indent-heuristic,inline:char,linematch:40")
			全局
	diff 模式的选项设置。它可以包含以下项目。所有项目都是可选的。项目必须用
	逗号分隔。

		algorithm:{text} 对内部 diff 引擎使用指定的 diff 算法。
				当前支持的算法有：
				myers      默认算法
				minimal    花费额外时间生成尽可能小的差异
				patience   patience diff 算法
				histogram  histogram diff 算法

		anchor		如果设置了 'diffanchors'，则将每个缓冲区中的特定行
				锚定以相互对齐。参见 |diff-anchors|。

		closeoff	当关闭一个设置了 'diff' 的窗口，并且同一标签页中
				只剩下一个窗口也设置了 'diff' 时，在该窗口执行
				`:diffoff`。这会撤销 `:diffsplit` 命令。

		context:{n}	在更改和包含未更改行的折叠之间使用 {n} 行的上下文。
				省略时使用六行的上下文。当使用零时，上下文实际上为 1，
				因为折叠需要中间有一行，对于删除的行也是如此。
				将其设置为一个非常大的值（999999）以完全禁用折叠。
				参见 |fold-diff|。

		filler		显示填充行，以使文本与在相同位置插入了行的窗口保持
				同步。当窗口并排且设置了 'scrollbind' 时最有用。

		foldcolumn:{n}	启动 diff 模式时，将 'foldcolumn' 选项设置为 {n}。
				没有此项时使用 2。

		followwrap	遵循 'wrap' 选项并保持不变。

		horizontal	使用水平分割启动 diff 模式（除非另有明确指定）。

		hiddenoff	当缓冲区变为隐藏时，不要对该缓冲区使用 diff 模式。

		iblank		忽略仅包含空行的更改。如果 'diffexpr' 为空，则向
				"diff" 命令添加 "-B" 标志。请查阅 "diff" 命令的
				文档以了解其确切作用。
				注意：diff 窗口将失去同步，因为没有考虑空行之间的
				差异。

		icase		忽略文本大小写的更改。"a" 和 "A" 被视为相同。
				如果 'diffexpr' 为空，则向 "diff" 命令添加 "-i" 标志。

		indent-heuristic
				对内部 diff 库使用缩进启发式算法。

		inline:{text}	在更改内部高亮显示行内差异。参见 |view-diffs|。
				支持的值有：

				none    不执行行内高亮显示。
				simple  从每行中第一个不同的字符高亮显示到
					最后一个字符。如果未设置 `inline:` 值，
					则此为默认值。
				char    使用内部 diff 执行字符级 diff 并高亮显示
					差异。
				word    使用内部 diff 执行 |word| 级 diff 并高亮显示
					差异。非字母数字的多字节字符（如表情符号
					和 CJK 字符）被视为单个单词。更改之间的
					非单词字符的小间隙（5 字节或更少）会合并到
					单个高亮显示块中。

		internal	使用内部 diff 库。当设置了 'diffexpr' 时，此项被忽略。
				*E960*
				当写入缓冲区时内存不足或 diff 大于 1 GB 时，
				对于涉及该缓冲区的差异，此项将被忽略。设置
				'verbose' 选项以查看何时发生这种情况。

		iwhite		忽略空白字符数量的变化。如果 'diffexpr' 为空，则向
				"diff" 命令添加 "-b" 标志。请查阅 "diff" 命令的
				文档以了解其确切作用。它应该忽略添加尾随空白，但
				不忽略前导空白。

		iwhiteall	忽略所有空白字符的变化。如果 'diffexpr' 为空，则向
				"diff" 命令添加 "-w" 标志。请查阅 "diff" 命令的
				文档以了解其确切作用。

		iwhiteeol	忽略行尾空白字符的变化。如果 'diffexpr' 为空，则向
				"diff" 命令添加 "-Z" 标志。请查阅 "diff" 命令的
				文档以了解其确切作用。

		linematch:{n}   在缓冲区之间最相似的行之间对齐并标记更改。当 diff
				块的总行数超过 {n} 时，由于非常大的 diff 块会
				产生明显的延迟，因此不会对齐行。合理的设置是
				"linematch:60"，因为这将启用 2 个缓冲区 diff 块
				（每个 30 行）或 3 个缓冲区 diff 块（每个 20 行）
				的对齐。设置此项时会隐式设置 "filler"。

		vertical	使用垂直分割启动 diff 模式（除非另有明确指定）。

	示例： >vim
		set diffopt=internal,filler,context:4
		set diffopt=
		set diffopt=internal,filler,foldcolumn:3
		set diffopt-=internal  " 不要使用内部 diff 解析器
<

				*'digraph'* *'dg'* *'nodigraph'* *'nodg'*
'digraph' 'dg'		布尔	(默认关闭)
			全局
	在插入模式下启用使用 {char1} <BS> {char2} 输入二合字母。参见 |digraphs|。

						*'directory'* *'dir'*
'directory' 'dir'	字符串	(默认 "$XDG_STATE_HOME/nvim/swap//")
			全局
	交换文件的目录名列表，以逗号分隔。

	可能的项目：
	- 交换文件将在列表中第一个可行的目录中创建。如果在任何目录中都不可行，
	  但选项中列出的最后一个目录不存在，则会创建它。
	- 空值表示将不使用交换文件（恢复不可能！），也不会给出 |E303| 错误。
	- 目录 "." 表示将交换文件放在与编辑文件相同的目录中。在 Unix 上，会在
	  文件名前添加一个点，因此它不会显示在目录列表中。在 MS-Windows 上，
	  如果可能，会设置 "隐藏" 属性并在前面添加一个点。
	- 以 "./"（或 MS-Windows 的 ".\"）开头的目录表示将交换文件放在相对于
	  编辑文件的位置。开头的 "." 将被编辑文件的路径名替换。
	- 对于 Unix 和 Win32，如果目录以两个路径分隔符 "//" 结尾，则交换文件名
	  将由文件的完整路径构建，所有路径分隔符（包括 Win32 上驱动器号后的冒号）
	  都替换为百分号 '%'。这将确保保留目录中文件名的唯一性。
	  在 Win32 上，也可以以 "\\" 结尾。但是，当后面跟有分隔逗号时，必须使用
	  "//"，因为 "\\" 会将逗号包含在文件名中。因此，建议使用 '//' 而不是 '\\'。
	- 逗号后的空格被忽略，其他空格被视为目录名的一部分。要使目录名以空格开头，
	  请在它前面加一个反斜杠。
	- 要在目录名中包含逗号，请在逗号前加一个反斜杠。
	- 目录名可以以 ':' 或 '/' 结尾。
	- 环境变量会被展开 |:set_env|。
	- 小心 '\' 字符，在空格前键入一个，在选项中键入两个才能得到一个
	  （参见 |option-backslash|），例如： >vim
	    set dir=c:\\tmp,\ dir\\,with\\,commas,\\\ dir\ with\ spaces
<
	两次编辑同一个文件会产生警告。不鼓励使用 "/tmp"：如果系统崩溃，你会丢失
	交换文件。并且计算机上的其他人可能能够看到这些文件。
	在列表中添加或删除目录时，请使用 |:set+=| 和 |:set-=|，这可以避免 Nvim
	默认值更改时出现问题。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'display'* *'dy'*
'display' 'dy'		字符串	(默认 "lastline")
			全局
	更改文本的显示方式。这是一个逗号分隔的标志列表：
	lastline	当包含时，将尽可能多地显示窗口中的最后一行。"@@@" 放在
			最后屏幕行的最后几列，以指示该行的其余部分未显示。
	truncate	类似于 "lastline"，但 "@@@" 显示在最后屏幕行的第一列。
			覆盖 "lastline"。
	uhex		将不可打印字符显示为 <xx> 十六进制，而不是使用 ^C 和 ~C。
	msgsep		已过时标志。允许使用但无效。|msgsep|

	当既不包含 "lastline" 也不包含 "truncate" 时，放不下的最后一行将被替换为
	"@" 行。

	"@" 字符可以通过在 'fillchars' 中设置 "lastline" 项来更改。该字符使用
	|hl-NonText| 高亮显示。

						*'eadirection'* *'ead'*
'eadirection' 'ead'	字符串	(默认 "both")
			全局
	指示 'equalalways' 选项何时生效：
		ver		垂直方向，窗口宽度不受影响
		hor		水平方向，窗口高度不受影响
		both		窗口的宽度和高度都受影响

				*'emoji'* *'emo'* *'noemoji'* *'noemo'*
'emoji' 'emo'		布尔	(默认开启)
			全局
	当开启时，所有 Unicode 表情符号字符都被视为全角。这不包括 "文本表情符号"
	字符，这些字符通常显示为单角。但是，如果此类 "文本表情符号" 后面跟着
	U+FE0F 变体选择器，则被视为全角表情符号。

	不幸的是，对此没有好的规范，它是通过反复试验确定的。使用 |setcellwidths()|
	函数来更改行为。

						*'encoding'* *'enc'*
'encoding' 'enc'	字符串	(默认 "utf-8")
			全局
	内部和 |RPC| 通信使用的字符串编码。始终为 UTF-8。

	参见 'fileencoding' 以控制文件内容的编码。

			*'endoffile'* *'eof'* *'noendoffile'* *'noeof'*
'endoffile' 'eof'	布尔	(默认关闭)
			本地到缓冲区
	指示读取文件时在文件末尾找到了 CTRL-Z 字符。通常仅在 'fileformat' 为
	"dos" 时发生。写入文件时，如果此选项关闭且 'binary' 选项开启，或 'fixeol'
	选项关闭，则不会在文件末尾写入 CTRL-Z。
	有关示例设置，请参见 |eol-and-eof|。

			*'endofline'* *'eol'* *'noendofline'* *'noeol'*
'endofline' 'eol'	布尔	(默认开启)
			本地到缓冲区
	写入文件时，如果此选项关闭且 'binary' 选项开启，或 'fixeol' 选项关闭，
	则不会为文件的最后一行写入 <EOL>。此选项在开始编辑新文件时根据文件
	最后一行是否有 <EOL> 自动设置或重置。通常你不需要设置或重置此选项。
	当 'binary' 关闭且 'fixeol' 开启时，写入文件时不使用此值。当 'binary'
	开启或 'fixeol' 关闭时，它用于记住文件最后一行是否存在 <EOL>，以便
	在写入文件时可以保留原始文件的状态。但如果你愿意，也可以更改它。
	有关示例设置，请参见 |eol-and-eof|。

			*'equalalways'* *'ea'* *'noequalalways'* *'noea'*
'equalalways' 'ea'	布尔	(默认开启)
			全局
	开启时，在分割或关闭窗口后，所有窗口会自动调整为相同大小。这在选项开启的
	瞬间也会发生。关闭时，分割窗口将减小当前窗口的大小，并保持其他窗口不变。
	关闭窗口时，多余的行会分配给相邻的窗口（取决于 'splitbelow' 和
	'splitright'）。
	当混合垂直和水平分割窗口时，会计算最小大小，如果有空间，某些窗口可能会更大。
	'eadirection' 选项指示大小在哪个方向受影响。
	可以通过分别设置 'winfixheight' 和 'winfixwidth' 来避免更改窗口的高度和宽度。
	如果在创建新窗口时指定了窗口大小，则当前不会均等化大小（这很复杂，但可能
	会在将来实现）。

						*'equalprg'* *'ep'*
'equalprg' 'ep'		字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	用于 "=" 命令的外部程序。当此选项为空时，使用内部格式化函数；可以是
	'lisp'、'cindent' 或 'indentexpr'。
	环境变量会被展开 |:set_env|。有关包含空格和反斜杠的信息，请参见
	|option-backslash|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

			*'errorbells'* *'eb'* *'noerrorbells'* *'noeb'*
'errorbells' 'eb'	布尔	(默认关闭)
			全局
	为错误消息响铃（蜂鸣或屏幕闪烁）。这只对错误消息有影响，对于许多没有消息的
	错误（例如，在普通模式下按 <Esc>），铃声将始终使用。参见 'visualbell'
	以使铃声表现为屏幕闪烁或不执行任何操作。参见 'belloff' 以微调何时响铃。

						*'errorfile'* *'ef'*
'errorfile' 'ef'	字符串	(默认 "errors.err")
			全局
	QuickFix 模式的错误文件名（参见 |:cf|）。当使用 "-q" 命令行参数时，
	'errorfile' 被设置为后面的参数。参见 |-q|。
	不用于 ":make" 命令。请参见 'makeef'。
	环境变量会被展开 |:set_env|。有关包含空格和反斜杠的信息，请参见
	|option-backslash|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'errorformat'* *'efm'*
'errorformat' 'efm'	字符串	(默认非常长)
			全局或本地到缓冲区 |global-local|
	类似 scanf 的错误文件中行格式的描述（参见 |errorformat|）。

						*'eventignore'* *'ei'*
'eventignore' 'ei'	字符串	(默认 "")
			全局
	要忽略的自动命令事件名称列表。当设置为 "all" 或 "all" 是其中一个项目时，
	忽略所有自动命令事件，自动命令将不会被执行。否则，这是一个逗号分隔的
	事件名称列表。示例： >vim
	    set ei=WinEnter,WinLeave
<
	要忽略除某些事件之外的所有事件，可以使用 "-" 前缀： >vim
	    :set ei=all,-WinLeave
<

					*'eventignorewin'* *'eiw'*
'eventignorewin' 'eiw'	字符串	(默认 "")
			本地到窗口
	类似于 'eventignore'，但适用于特定窗口及其缓冲区，可以无限期忽略与此窗口
	和缓冲区相关的自动命令，而不影响全局的 'eventignore'。

	注意：以下事件被认为发生在窗口上下文之外，因此不能被 'eventignorewin'
	忽略：

		|ChanInfo|、
		|ChanOpen|、
		|CmdUndefined|、
		|CmdlineChanged|、
		|CmdlineEnter|、
		|CmdlineLeave|、
		|CmdlineLeavePre|、
		|CmdwinEnter|、
		|CmdwinLeave|、
		|ColorScheme|、
		|ColorSchemePre|、
		|CompleteChanged|、
		|CompleteDone|、
		|CompleteDonePre|、
		|DiagnosticChanged|、
		|DiffUpdated|、
		|DirChanged|、
		|DirChangedPre|、
		|ExitPre|、
		|FocusGained|、
		|FocusLost|、
		|FuncUndefined|、
		|LspAttach|、
		|LspDetach|、
		|LspNotify|、
		|LspProgress|、
		|LspRequest|、
		|LspTokenUpdate|、
		|MarkSet|、
		|MenuPopup|、
		|ModeChanged|、
		|OptionSet|、
		|PackChanged|、
		|PackChangedPre|、
		|Progress|、
		|QuickFixCmdPost|、
		|QuickFixCmdPre|、
		|QuitPre|、
		|RemoteReply|、
		|SafeState|、
		|SessionLoadPost|、
		|SessionLoadPre|、
		|SessionWritePost|、
		|ShellCmdPost|、
		|Signal|、
		|SourceCmd|、
		|SourcePost|、
		|SourcePre|、
		|SpellFileMissing|、
		|StdinReadPost|、
		|StdinReadPre|、
		|SwapExists|、
		|Syntax|、
		|TabClosed|、
		|TabClosedPre|、
		|TabEnter|、
		|TabLeave|、
		|TabNew|、
		|TabNewEntered|、
		|TermClose|、
		|TermEnter|、
		|TermLeave|、
		|TermOpen|、
		|TermRequest|、
		|TermResponse|、
		|UIEnter|、
		|UILeave|、
		|User|、
		|VimEnter|、
		|VimLeave|、
		|VimLeavePre|、
		|VimResized|、
		|VimResume|、
		|VimSuspend|、
		|WinNew|、
		|WinNewPre|

						*'expandtab'* *'et'* *'noexpandtab'* *'noet'*
'expandtab' 'et'	布尔	(默认关闭)
			本地到缓冲区
	在插入模式下：使用适当数量的空格来插入一个 <Tab>。在使用 '>' 和 '<' 命令以及
	'autoindent' 开启时，缩进中使用空格。当 'expandtab' 开启时，要插入真正的制表符，
	请使用 CTRL-V<Tab>。另请参见 |:retab| 和 |ins-expandtab|。

	*'exrc'* *'ex'* *'noexrc'* *'noex'* *project-config* *workspace-config*
'exrc' 'ex'		布尔	(默认关闭)
			全局
	启用项目本地配置。Nvim 将执行在 |current-directory| 和所有父目录中（向上排序）
	找到的任何 .nvim.lua、.nvimrc 或 .exrc 文件，前提是这些文件在 |trust| 列表中。
	使用 |:trust| 来管理受信任的文件。另请参见 |vim.secure.read()|。

	取消设置 'exrc' 以停止在父目录中进一步搜索 'exrc' 文件，类似于 |editorconfig.root|。

	为了获取其自身位置，Lua exrc 文件可以使用 |debug.getinfo()|。参见 |lua-script-location|。

	将 'exrc' 与 |editorconfig| 进行比较：
	- 'exrc' 可以执行任何代码；editorconfig 只指定设置。
	- 'exrc' 是 Nvim 特有的；editorconfig 在其他编辑器中也能工作。

	要实现项目本地 LSP 配置：
	1. 启用 'exrc'。
	2. 将 LSP 配置放置在项目根目录的 ".nvim/lsp/*.lua"。
	3. 在项目根目录中创建 ".nvim.lua"，包含以下行： >lua
	    vim.cmd[[set runtimepath+=.nvim]]
<
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'fileencoding'* *'fenc'* *E213*
'fileencoding' 'fenc'	字符串	(默认 "")
			本地到缓冲区
	当前缓冲区的文件内容编码。使用 iconv() 或按 'charconvert' 指定的方式进行转换。

	当 'fileencoding' 不是 UTF-8 时，写入文件时将进行转换。关于读取，请参见下文。
	当 'fileencoding' 为空时，文件将以 UTF-8 编码保存（读取或写入文件时不进行转换）。

	警告：转换为非 Unicode 编码可能导致信息丢失！

	有关可能的值，请参见 |encoding-names|。此外，可以指定转换器能处理的值，请参见
	|mbyte-conversion|。

	读取文件时，'fileencoding' 会根据 'fileencodings' 设置。通过设置 'fileencoding'
	来以特定编码读取文件是无效的，请使用 |++enc| 参数。一个例外：当 'fileencodings'
	为空时，使用 'fileencoding' 的值。对于新文件，使用 'fileencoding' 的全局值。

	在此处添加 "8bit-" 和 "2byte-" 前缀没有意义，它们会被忽略。设置选项时，值会被
	转换为小写。因此，你也可以使用大写值设置。'_' 字符会被替换为 '-'。如果某个名称
	在 |encoding-names| 的列表中被识别，它会被替换为标准名称。例如，"ISO8859-2"
	变为 "iso-8859-2"。

	设置此选项后（在开始编辑文件之后），'modified' 选项会被设置，因为写入时文件会
	不同。

	请记住，从 modeline 更改 'fenc' 发生在文本被读取之后，因此它适用于文件写入时。
	如果你在 modeline 中设置了 'fenc'，你可能想要设置 'nomodified' 以避免无法使用
	":q"。

	当 'modifiable' 关闭时，不能更改此选项。

					*'fileencodings'* *'fencs'*
'fileencodings' 'fencs'	字符串	(默认 "ucs-bom,utf-8,default,latin1")
			全局
	这是一个字符编码列表，在开始编辑现有文件时会被考虑。读取文件时，Vim 会尝试使用
	列表中第一个提到的字符编码。如果检测到错误，则尝试列表中的下一个。当找到可用的
	编码时，'fileencoding' 会被设置为该编码。如果全部失败，'fileencoding' 会被设置
	为空字符串，这意味着使用 UTF-8。
		警告：转换可能导致信息丢失！你可以使用 |++bad| 参数来指定如何处理
		无法转换的字符。
	对于空文件或仅包含 ASCII 字符的文件，大多数编码都能工作，并且会使用
	'fileencodings' 的第一个条目（除了 "ucs-bom"，它要求存在 BOM）。如果你更喜欢
	另一种编码，请使用 BufReadPost 自动命令事件来测试是否应使用你偏好的编码。
	示例： >vim
		au BufReadPost * if search('\S', 'w') == 0 |
			\ set fenc=iso-2022-jp | endif
<	如果文件不包含非空白字符，此设置会将 'fileencoding' 设置为 "iso-2022-jp"。
	当使用 |++enc| 参数时，'fileencodings' 的值不会被使用。
	请注意，'fileencodings' 不用于新文件，而是使用 'fileencoding' 的全局值。
	你可以这样设置： >vim
		setglobal fenc=iso-8859-2
<	这意味着一个不存在的文件可能获得与空文件不同的编码。
	特殊值 "ucs-bom" 可用于检查文件开头的 Unicode BOM（字节顺序标记）。为了使其
	正常工作，它前面不能有 "utf-8" 或其他 Unicode 编码。
	8 位编码（例如 "latin1"）的条目应该是最后一个，因为 Vim 无法检测错误，因此
	该编码总是被接受。
	特殊值 "default" 可用于来自环境的编码。当你的环境使用非拉丁 1 编码（如俄语）时，
	这很有用。
	当文件包含非法的 UTF-8 字节序列时，它不会被识别为 "utf-8"。你可以使用 |8g8|
	命令查找非法的字节序列。
	错误的值：			错误所在：
		latin1,utf-8		"latin1" 将始终被使用
		utf-8,ucs-bom,latin1	BOM 在 utf-8 文件中不会被识别
		cp1250,latin1		"cp1250" 将始终被使用
	如果 'fileencodings' 为空，'fileencoding' 不会被修改。
	有关可能的值，请参见 'fileencoding'。
	设置此选项直到下次读取文件时才生效。

						*'fileformat'* *'ff'*
'fileformat' 'ff'	字符串	(默认 Windows: "dos", Unix: "unix")
			本地到缓冲区
	这给出了当前缓冲区的 <EOL>，用于从/向文件读取/写入缓冲区：
	    dos	    <CR><NL>
	    unix    <NL>
	    mac	    <CR>
	当使用 "dos" 时，文件末尾的 CTRL-Z 会被忽略。
	参见 |file-formats| 和 |file-read|。
	有关文件的字符编码，请参见 'fileencoding'。
	当设置了 'binary' 时，'fileformat' 的值会被忽略，文件 I/O 的行为如同设置为
	"unix"。
	当开始编辑文件且 'fileformats' 不为空且 'binary' 关闭时，此选项会自动设置。
	设置此选项后（在开始编辑文件之后），'modified' 选项会被设置，因为写入时文件会
	不同。
	当 'modifiable' 关闭时，不能更改此选项。

						*'fileformats'* *'ffs'*
'fileformats' 'ffs'	字符串	(默认 Windows: "dos,unix", Unix: "unix,dos")
			全局
	这给出了在开始编辑新缓冲区以及将文件读入现有缓冲区时将尝试的行尾（<EOL>）格式：
	- 当为空时，将始终使用 'fileformat' 定义的格式。它不会自动设置。
	- 当设置为一个名称时，每当打开新缓冲区时都会使用该格式。'fileformat' 会相应地为
	  该缓冲区设置。当将文件读入现有缓冲区时，无论该缓冲区的 'fileformat' 设置为何，
	  都会使用 'fileformats' 中的名称。
	- 当存在多个名称（以逗号分隔）时，读取文件时将进行自动 <EOL> 检测。开始编辑文件时，
	  会检查 <EOL>：
	  1. 如果所有行都以 <CR><NL> 结尾，并且 'fileformats' 包含 "dos"，则 'fileformat'
	     设置为 "dos"。
	  2. 如果找到 <NL> 并且 'fileformats' 包含 "unix"，则 'fileformat' 设置为 "unix"。
	     注意，当找到没有前置 <CR> 的 <NL> 时，"unix" 优先于 "dos"。
	  3. 如果 'fileformat' 尚未设置，并且找到 <CR>，并且 'fileformats' 包含 "mac"，
	     则 'fileformat' 设置为 "mac"。这意味着 "mac" 仅在以下情况下被选择：
	      "unix" 不存在或文件中未找到 <NL>，并且
	      "dos" 不存在或文件中未找到 <CR><NL>。
	      例外：如果选择了 "unix"，但在第一个 <NL> 之前有一个 <CR>，并且在前几行中
	      看起来 <CR> 比 <NL> 多，则使用 "mac"。
	  4. 如果仍未设置 'fileformat'，则使用 'fileformats' 中的第一个名称。
	  当将文件读入现有缓冲区时，会执行相同的操作，但效果是仅对该文件适当地设置了
	  'fileformat'，选项本身不会改变。
	当设置了 'binary' 时，'fileformats' 的值不会被使用。

	当 Vim 以空缓冲区启动时，使用第一项。你可以通过在 .vimrc 中设置 'fileformat'
	来覆盖此行为。

	对于具有类似 Dos 的 <EOL>（<CR><NL>）的系统，当读取 ":source" 的文件和 vimrc
	文件时，可能会进行自动 <EOL> 检测：
	- 当 'fileformats' 为空时，没有自动检测。将使用 Dos 格式。
	- 当 'fileformats' 设置为一或多个名称时，进行自动检测。这基于文件中的第一个
	  <NL>：如果它前面有一个 <CR>，则使用 Dos 格式，否则使用 Unix 格式。
	另请参见 |file-formats|。

		*'fileignorecase'* *'fic'* *'nofileignorecase'* *'nofic'*
'fileignorecase' 'fic'	布尔	(默认在文件名通常不区分大小写的系统上开启)
			全局
	设置后，在使用文件和目录名称时忽略大小写。

	此选项在文件系统传统上不区分大小写的系统（例如 MS-Windows 和 macOS）上默认开启。
	但是，Vim 无法在运行时确定特定文件系统是区分大小写还是不区分大小写。

	有关仅在补全时忽略大小写的信息，请参见 'wildignorecase'。

						*'filetype'* *'ft'*
'filetype' 'ft'		字符串	(默认 "")
			本地到缓冲区  |local-noglobal|
	当设置此选项时，会触发 FileType 自动命令事件。所有匹配此选项值的自动命令都将
	被执行。因此，'filetype' 的值用于代替文件名。
	否则，此选项并不总是反映当前文件类型。
	此选项通常在检测到文件类型时设置。要启用此功能，请使用 ":filetype on" 命令。
	|:filetype|
	将此选项设置为不同的值在 modeline 中最有用，适用于文件类型无法自动识别的文件。
	例如，在 IDL 文件中： >c
		/* vim: set filetype=idl : */
<	|FileType| |filetypes|
	当值中出现点时，它会分隔两个文件类型名称，因此不应将其用于文件类型。示例： >c
		/* vim: set filetype=c.doxygen : */
<	这将首先使用 "c" 文件类型，然后使用 "doxygen" 文件类型。这既适用于文件类型插件，
	也适用于语法文件。可以出现多个点。
	无论 'cpoptions' 中的 's' 或 'S' 标志如何，此选项都不会复制到另一个缓冲区。
	只能使用字母数字字符、'-' 和 '_'（并且在组合不同文件类型时，允许使用 '.' 作为
	分隔符）。

						*'fillchars'* *'fcs'*
'fillchars' 'fcs'	字符串	(默认 "")
			全局或本地到窗口 |global-local|
	用于填充状态行、垂直分隔符、窗口中的特殊行以及 |ins-completion-menu| 中截断文本
	的字符。它是一个逗号分隔的项目列表。每个项目都有一个名称、一个冒号和该项目的值：
	|E1511|

	  项目		默认值		用于 ~
	  stl		' '		当前窗口的状态行
	  stlnc		' '		非当前窗口的状态行
	  wbr		' '		窗口栏
	  horiz		'─' 或 '-'	水平分隔符 |:split|
	  horizup	'┴' 或 '-'	向上方向水平分隔符
	  horizdown	'┬' 或 '-'	向下方向水平分隔符
	  vert		'│' 或 '|'	垂直分隔符 |:vsplit|
	  vertleft	'┤' 或 '|'	向左方向垂直分隔符
	  vertright	'├' 或 '|'	向右方向垂直分隔符
	  verthoriz	'┼' 或 '+'	重叠的垂直和水平分隔符
	  fold		'·' 或 '-'	填充 'foldtext'
	  foldopen	'-'		标记折叠的开始
	  foldclose	'+'		显示关闭的折叠
	  foldsep	'│' 或 '|'	打开折叠的中间标记
	  foldinner	无		当窄的 'foldcolumn' 中重复显示数字折叠级别时，
					用于替代显示该数字的字符
	  diff		'-'		'diff' 选项中删除的行
	  msgsep	' '		消息分隔符 'display'
	  eob		'~'		缓冲区末尾的空行
	  lastline	'@'		'display' 包含 lastline/truncate
	  trunc		'>'		|ins-completion-menu| 中截断的文本。
	  truncrl	'<'		'rightleft' 模式下与 "trunc" 相同

	任何省略的项目将回退到默认值。

	请注意，只有当 'laststatus' 为 3 时，才会使用 "horiz"、"horizup"、"horizdown"、
	"vertleft"、"vertright" 和 "verthoriz"，因为否则只使用垂直窗口分隔符。

	如果 'ambiwidth' 是 "double"，则 "horiz"、"horizup"、"horizdown"、"vert"、
	"vertleft"、"vertright"、"verthoriz"、"foldsep" 和 "fold" 默认为单字节替代项。

	示例： >vim
	    set fillchars=stl:\ ,stlnc:\ ,vert:│,fold:·,diff:-
<
	所有项目都支持单字节和多字节字符。但不支持双宽度字符。|E1512|

	用于这些项目的高亮组：
	  项目		高亮组 ~
	  stl		StatusLine		|hl-StatusLine|
	  stlnc		StatusLineNC		|hl-StatusLineNC|
	  wbr		WinBar			|hl-WinBar| 或 |hl-WinBarNC|
	  horiz		WinSeparator		|hl-WinSeparator|
	  horizup	WinSeparator		|hl-WinSeparator|
	  horizdown	WinSeparator		|hl-WinSeparator|
	  vert		WinSeparator		|hl-WinSeparator|
	  vertleft	WinSeparator		|hl-WinSeparator|
	  vertright	WinSeparator		|hl-WinSeparator|
	  verthoriz	WinSeparator		|hl-WinSeparator|
	  fold		Folded			|hl-Folded|
	  foldopen	FoldColumn		|hl-FoldColumn|
	  foldclose	FoldColumn		|hl-FoldColumn|
	  foldsep	FoldColumn		|hl-FoldColumn|
	  diff		DiffDelete		|hl-DiffDelete|
	  msgsep	MsgSeparator		|hl-MsgSeparator|
	  eob		EndOfBuffer		|hl-EndOfBuffer|
	  lastline	NonText			|hl-NonText|
	  trunc		许多弹出菜单高亮组之一，如 |hl-PmenuSel|
	  truncrl	同 "trunc"

					*'findfunc'* *'ffu'* *E1514*
'findfunc' 'ffu'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	为 |:find| 命令调用的函数，用于获取文件名。当此选项为空时，使用内部
	|file-searching| 机制。

	该值可以是函数名、|lambda| 或 |Funcref|。有关更多信息，请参见
	|option-value-function|。

	该函数使用两个参数调用。第一个参数是一个 |String|，即 |:find| 命令的参数。
	第二个参数是一个 |Boolean|，当调用该函数以获取 |:find| 命令的命令行补全
	匹配项列表时，设置为 |v:true|。该函数应返回一个字符串列表。

	每次 |:find| 命令调用仅调用该函数一次。该函数可以处理 'path' 中指定的所有目录。

	如果找到匹配项，函数应返回一个包含一个或多个文件名的 |List|。如果未找到匹配项，
	函数应返回一个空列表。

	如果在函数调用期间遇到任何错误，则使用空列表作为返回值。

	执行 'findfunc' 时不允许更改文本或跳转到另一个窗口 |textlock|。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

	示例：
	>vim
	    " 使用 glob()
	    func FindFuncGlob(cmdarg, cmdcomplete)
		let pat = a:cmdcomplete ? $'{a:cmdarg}*' : a:cmdarg
		return glob(pat, v:false, v:true)
	    endfunc
	    set findfunc=FindFuncGlob

	    " 使用 'git ls-files' 输出
	    func FindGitFiles(cmdarg, cmdcomplete)
		let fnames = systemlist('git ls-files')
		return fnames->filter('v:val =~? a:cmdarg')
	    endfunc
	    set findfunc=FindGitFiles
<

		*'fixendofline'* *'fixeol'* *'nofixendofline'* *'nofixeol'*
'fixendofline' 'fixeol'	布尔	(默认开启)
			本地到缓冲区
	写入文件时，如果此选项开启且文件末尾缺少 <EOL>，则会恢复。如果你想保留原始文件
	的状态，请关闭此选项。
	当设置了 'binary' 选项时，此选项的值无关紧要。
	参见 'endofline' 选项。
	有关示例设置，请参见 |eol-and-eof|。

						*'foldclose'* *'fcl'*
'foldclose' 'fcl'	字符串	(默认 "")
			全局
	当设置为 "all" 时，如果光标不在某个折叠中，且其级别高于 'foldlevel'，则该折叠
	会关闭。如果你希望在移出折叠时自动关闭折叠，这很有用。

						*'foldcolumn'* *'fdc'*
'foldcolumn' 'fdc'	字符串	(默认 "0")
			本地到窗口
	何时以及如何绘制折叠列。有效值为：
	    "auto":       调整大小以显示最少量的折叠。
	    "auto:[1-9]": 调整大小以适应最多所选级别的多个折叠
	    "0":          禁用折叠列
	    "[1-9]":      显示固定数量的列
	参见 |folding|。

			*'foldenable'* *'fen'* *'nofoldenable'* *'nofen'*
'foldenable' 'fen'	布尔	(默认开启)
			本地到窗口
	关闭时，所有折叠都打开。此选项可用于在显示所有未折叠的文本和查看带有折叠的文本
	（包括手动打开或关闭的折叠）之间快速切换。可以使用 |zi| 命令切换。当
	'foldenable' 关闭时，'foldcolumn' 将保持空白。
	此选项由创建新折叠或关闭折叠的命令设置。参见 |folding|。

						*'foldexpr'* *'fde'*
'foldexpr' 'fde'	字符串	(默认 "0")
			本地到窗口
	当 'foldmethod' 为 "expr" 时使用的表达式。为每一行评估以获得其折叠级别。
	上下文设置为设置 'foldexpr' 的脚本，可以访问脚本本地项。有关用法，请参见
	|fold-expr|。

	如果从 modeline 设置，表达式将在 |sandbox| 中评估，请参见 |sandbox-option|。
	当 'diff' 选项开启或 'modelineexpr' 选项关闭时，此选项不能从 |modeline| 设置。

	评估 'foldexpr' 时不允许更改文本或跳转到另一个窗口 |textlock|。

						*'foldignore'* *'fdi'*
'foldignore' 'fdi'	字符串	(默认 "#")
			本地到窗口
	仅当 'foldmethod' 为 "indent" 时使用。以 'foldignore' 中字符开头的行将获得
	周围行的折叠级别。在检查此字符之前会跳过空白字符。默认值 "#" 对于 C 程序效果
	良好。参见 |fold-indent|。

						*'foldlevel'* *'fdl'*
'foldlevel' 'fdl'	数字	(默认 0)
			本地到窗口
	设置折叠级别：级别较高的折叠将被关闭。将此选项设置为零将关闭所有折叠。较高的
	数字将关闭较少的折叠。
	此选项由 |zm|、|zM| 和 |zR| 等命令设置。参见 |fold-foldlevel|。

					*'foldlevelstart'* *'fdls'*
'foldlevelstart' 'fdls'	数字	(默认 -1)
			全局
	当在窗口中开始编辑另一个缓冲区时设置 'foldlevel'。对于始终以所有折叠关闭
	（值为零）、部分折叠关闭（值为 1）或无折叠关闭（值为 99）开始编辑很有用。
	这在读取任何 modeline 之前完成，因此 modeline 中的设置会覆盖此选项。开始编辑
	|diff-mode| 的文件也会忽略此选项并关闭所有折叠。
	它也在 BufReadPre 自动命令之前完成，以允许自动命令为特定文件覆盖 'foldlevel' 值。
	当值为负数时，不使用此选项。

					*'foldmarker'* *'fmr'* *E536*
'foldmarker' 'fmr'	字符串	(默认 "{{{,}}}")
			本地到窗口
	当 'foldmethod' 为 "marker" 时使用的开始和结束标记。必须有一个逗号，用于分隔
	开始和结束标记。标记是一个字面字符串（正则表达式会太慢）。参见 |fold-marker|。

						*'foldmethod'* *'fdm'*
'foldmethod' 'fdm'	字符串	(默认 "manual")
			本地到窗口
	当前窗口使用的折叠类型。可能的值：
	|fold-manual|	manual	    手动创建折叠。
	|fold-indent|	indent	    缩进相同的行形成一个折叠。
	|fold-expr|	expr	    'foldexpr' 给出行的折叠级别。
	|fold-marker|	marker	    使用标记指定折叠。
	|fold-syntax|	syntax	    语法高亮项指定折叠。
	|fold-diff|	diff	    折叠未更改的文本。

						*'foldminlines'* *'fml'*
'foldminlines' 'fml'	数字	(默认 1)
			本地到窗口
	设置折叠可以显示为关闭状态所需的屏幕行数以上。也适用于手动关闭的折叠。使用
	默认值 1 时，折叠只有在占用两行或更多屏幕行时才能关闭。设置为零可以关闭仅占
	一行屏幕行的折叠。
	请注意，这仅对显示的内容有影响。使用 "zc" 关闭一个因小于 'foldminlines' 而
	显示为打开的折叠后，接下来的 "zc" 可能会关闭一个包含它的折叠。

						*'foldnestmax'* *'fdn'*
'foldnestmax' 'fdn'	数字	(默认 20)
			本地到窗口
	为 "indent" 和 "syntax" 方法设置折叠的最大嵌套深度。这可以避免创建过多的折叠。
	使用超过 20 的值无效，因为内部限制是 20。

						*'foldopen'* *'fdo'*
'foldopen' 'fdo'	字符串	(默认 "block,hor,mark,percent,quickfix,search,tag,undo")
			全局
	指定对于哪些类型的命令，如果命令将光标移入关闭的折叠，则会打开折叠。它是一个
	逗号分隔的项目列表。
	注意：当命令是映射的一部分时，不使用此选项。请在映射中添加 |zv| 命令以获得相同
	效果。（理由：映射可能希望自己控制打开折叠）

		项目		命令 ~
		all		任何
		block		(、{、[[、[{ 等。
		hor		水平移动："l"、"w"、"fx" 等。
		insert		插入模式下的任何命令
		jump		远跳转："G"、"gg" 等。
		mark		跳转到标记："'m"、CTRL-O 等。
		percent		"%"
		quickfix	":cn"、":crew"、":make" 等。
		search		搜索模式："/"、"n"、"*"、"gd" 等。
				（不用于 ":" 命令中的搜索模式）
				也适用于 |[s| 和 |]s|。
		tag		跳转到标签：":ta"、CTRL-T 等。
		undo		撤销或重做："u" 和 CTRL-R
	当移动命令用于操作符时（例如，"dl" 或 "y%"），不使用此选项。这意味着操作符将包含
	整个关闭的折叠。
	请注意，这里没有垂直移动，因为那会使移动到关闭的折叠上变得非常困难。
	在插入模式下，当插入文本时，包含光标的折叠将始终打开。
	要关闭折叠，你可以使用 |zx| 命令重新应用 'foldlevel'，或将 'foldclose' 选项
	设置为 "all"。
						*'foldtext'* *'fdt'*
'foldtext' 'fdt'	字符串	(默认 "foldtext()")
			本地到窗口
	用于指定关闭折叠时显示的文本的表达式。上下文设置为设置 'foldexpr' 的脚本，
	可以访问脚本本地项。有关用法，请参见 |fold-foldtext|。

	如果从 modeline 设置，表达式将在 |sandbox| 中评估，请参见 |sandbox-option|。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

	评估 'foldtext' 时不允许更改文本或跳转到另一个窗口 |textlock|。

	当设置为空字符串时，折叠文本被禁用，行将正常显示，具有高亮显示且不换行。

						*'formatexpr'* *'fex'*
'formatexpr' 'fex'	字符串	(默认 "")
			本地到缓冲区
	为 |gq| 操作符或自动格式化（参见 'formatoptions'）格式化一系列行时评估的表达式。
	当此选项为空时，使用 'formatprg'。

	|v:lnum| 变量保存要格式化的第一行。
	|v:count| 变量保存要格式化的行数。
	|v:char| 变量保存如果因自动格式化而评估表达式时将要插入的字符。可能为空。
		     尚未插入！

	示例： >vim
		set formatexpr=mylang#Format()
<	这将调用 'runtimepath' 中 autoload/mylang.vim 文件中的 mylang#Format() 函数。
	|autoload|

	使用无参数的函数调用的优点是速度更快，参见 |expr-option-function|。

	当设置了 'textwidth' 且添加的文本超过该限制时，也会评估该表达式。这发生在与
	使用内部格式化相同的条件下。请确保光标相对于文本保持在相同位置！在这种情况下，
	|mode()| 函数将返回 "i" 或 "R"。

	当表达式评估为非零时，Vim 将回退到使用内部格式化机制。

	如果表达式以 s: 或 |<SID>| 开头，则会被替换为脚本 ID（|local-function|）。
	示例： >vim
		set formatexpr=s:MyFormatExpr()
		set formatexpr=<SID>SomeFormatExpr()
<	否则，表达式将在设置选项的脚本上下文中评估，因此脚本本地项可用。

	如果从 modeline 设置，表达式将在 |sandbox| 中评估，请参见 |sandbox-option|。
	这会阻止选项工作，因为不允许更改缓冲区文本。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

						*'formatlistpat'* *'flp'*
'formatlistpat' 'flp'	字符串	(默认 "^\s*\d\+[\]:.)}\t ]\s*")
			本地到缓冲区
	用于识别列表标题的模式。这用于 'formatoptions' 中的 "n" 标志。
	模式必须精确匹配将成为其下面行缩进的文本。你可以使用 |/\ze| 来标记匹配的结束，
同时仍然检查更多字符。模式后面必须跟一个字符，如果它匹配整行，则视为没有匹配。
	默认模式识别一个数字，后跟可选的标点符号和空白。

						*'formatoptions'* *'fo'*
'formatoptions' 'fo'	字符串	(默认 "tcqj")
			本地到缓冲区
	这是一个字母序列，描述如何进行自动格式化。
	有关可能的值，请参见 |fo-table|；有关如何格式化文本，请参见 |gq|。
	可以插入逗号以提高可读性。
	为避免将来添加标志时出现问题，请使用 ":set" 的 "+=" 和 "-=" 功能 |add-option-flags|。

						*'formatprg'* *'fp'*
'formatprg' 'fp'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	用于格式化通过 |gq| 操作符选中的行的外部程序名称。该程序必须从标准输入
	接收输入，并在标准输出上产生输出。Unix 程序 "fmt" 就是这样的程序。
	如果 'formatexpr' 选项不为空，则将使用它。否则，如果 'formatprg' 选项是
	空字符串，将使用内部格式化函数 |C-indenting|。
	环境变量会被展开 |:set_env|。有关包含空格和反斜杠的信息，请参见
	|option-backslash|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'fsync'* *'fs'* *'nofsync'* *'nofs'*
'fsync' 'fs'		布尔	(默认开启)
			全局或本地到缓冲区 |global-local|
	开启时，在保存文件（|:write|、|writefile()|、…）、|swap-file|、
	|undo-persistence| 和 |shada-file| 后，将调用操作系统函数 fsync()。
	这会将文件刷新到磁盘，确保其被安全写入。在某些系统上较慢：写入缓冲区、
	退出 Nvim 和其他操作有时可能需要几秒钟。

	在以下情况下，文件总是被刷新（忽略 'fsync'）：
	- 触发 |CursorHold| 事件
	- 调用 |:preserve|
	- 系统发出电池电量不足信号
	- Nvim 异常退出

	这是一个 |global-local| 选项，因此可以为每个缓冲区设置，例如在写入慢速文件系统时。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'grepformat'* *'gfm'*
'grepformat' 'gfm'	字符串	(默认 "%f:%l:%m,%f:%l%m,%f  %l%m")
			全局或本地到缓冲区 |global-local|
	用于识别 ":grep" 命令输出的格式。
	这是一个类似 scanf 的字符串，使用与 'errorformat' 选项相同的格式：参见 |errorformat|。

	如果 ripgrep（'grepprg'）可用，则默认为 "%f:%l:%c:%m"。

						*'grepprg'* *'gp'*
'grepprg' 'gp'		字符串	(默认见下文)
			全局或本地到缓冲区 |global-local|
	用于 |:grep| 命令的程序。
	注意：如果更改此选项，则还必须更新 'grepformat'。

	可以包含 "%" 和 "#" 字符，它们会按 |cmdline-special| 展开。
	占位符 "$*" 指定包含参数的位置。
	环境变量会被展开 |:set_env|。有关包含空格和反斜杠的信息，请参见
	|option-backslash|。

	特殊值：当 'grepprg' 设置为 "internal" 时，|:grep| 命令的工作方式类似于
	|:vimgrep|，|:lgrep| 类似于 |:lvimgrep|，|:grepadd| 类似于 |:vimgrepadd|，
	|:lgrepadd| 类似于 |:lvimgrepadd|。

	另请参见 |:make_makeprg|，那里的大部分评论也适用于 'grepprg'。

	默认为：
	- 如果 ripgrep 可用（|:checkhealth|），则为 "rg --vimgrep -uu "，
	- 在 Unix 上为 "grep -HIn $* /dev/null"，
	- 在 Windows 上为 "findstr /n $* nul"。

	Ripgrep 可能会执行额外的过滤，例如使用 .gitignore 规则和跳过隐藏文件。
	这默认被禁用（通过 "-u"），以更紧密地匹配标准 grep 的行为。
	你可以使用 -i/--ignore-case 和 -S/--smart-case 选项使 ripgrep 匹配 Vim
	的大小写处理。处理 |OptionSet| 以在例如 'ignorecase' 更改时动态更新 'grepprg'。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

				*'guicursor'* *'gcr'* *E545* *E546* *E548* *E549*
'guicursor' 'gcr'	字符串	(默认 "n-v-c-sm:block,i-ci-ve:ver25,r-cr-o:hor20,t:block-blinkon500-blinkoff500-TermCursor")
			全局
	为每种模式配置光标样式。在 GUI 和许多终端中工作。参见 |tui-cursor-shape|。

	要禁用光标样式，请重置选项： >vim
		set guicursor=

<	要启用模式形状、"Cursor" 高亮和闪烁： >vim
		set guicursor=n-v-c:block,i-ci-ve:ver25,r-cr:hor20,o:hor50
		  \,a:blinkwait700-blinkoff400-blinkon250-Cursor/lCursor
		  \,sm:block-blinkwait175-blinkoff150-blinkon175

<	该选项是一个逗号分隔的部分列表。每个部分由一个模式列表和一个参数列表组成：
		mode-list:argument-list,mode-list:argument-list,..
	模式列表是一个用破折号分隔的以下模式列表：
		n	普通模式
		v	可视模式
		ve	'selection' 为 "exclusive" 的可视模式（如果未指定，同 'v'）
		o	操作符待决模式
		i	插入模式
		r	替换模式
		c	命令行普通（追加）模式
		ci	命令行插入模式
		cr	命令行替换模式
		sm	插入模式下的 showmatch
		t	终端模式
		a	所有模式
	参数列表是一个用破折号分隔的以下参数列表：
		hor{N}	水平条，占字符高度的 N%
		ver{N}	垂直条，占字符宽度的 N%
		block	块状光标，填满整个字符
			- 上述三种样式只能存在一种。
			- 每种模式的默认值是 "block"。
		blinkwait{N}				*cursor-blinking*
		blinkon{N}
		blinkoff{N}
			光标的闪烁时间：blinkwait 是光标开始闪烁前的延迟，
			blinkon 是光标显示的时间，blinkoff 是光标不显示的时间。
			时间以毫秒为单位。当其中一个数字为零时，不闪烁。例如： >vim
				set guicursor=n:blinkon0
<
			每种模式的默认值是 "blinkon0"。
		{group-name}
			决定光标颜色和字体的高亮组。
			在 |TUI| 中：
			- |inverse|/reverse 且没有组名被解释为 "主机终端默认光标颜色"，
			  通常意味着 "反转的背景和前景颜色"。
			- |ctermfg| 和 |guifg| 被忽略。
		{group-name}/{group-name}
			两个高亮组名称，第一个在未使用语言映射时使用，第二个在使用时使用。
			|language-mapping|

	部分示例：
	   n-c-v:block-nCursor	在普通模式、命令行模式和可视模式下，使用块状光标，
				颜色来自 "nCursor" 高亮组。
	   n-v-c-sm:block,i-ci-ve:ver25-Cursor,r-cr-o:hor20
				在普通等模式下，使用主机终端定义的默认颜色块状光标。
				在插入类模式下，使用垂直条光标，颜色来自 "Cursor"
				高亮组。在替换类模式下，使用带默认颜色的下划线光标。
	   i-ci:ver30-iCursor-blinkwait300-blinkon200-blinkoff150
				在插入模式和命令行插入模式下，使用 30% 的垂直条光标，
				颜色来自 "iCursor" 高亮组。闪烁速度稍快。

	'a' 模式不同。它将为所有模式设置给定的参数列表。它不会将任何内容重置为默认值。
	这可用于为所有模式进行通用设置。例如，关闭闪烁："a:blinkon0"

	光标高亮示例： >vim
	    highlight Cursor gui=reverse guifg=NONE guibg=NONE
	    " 注意：gui=reverse 会覆盖颜色。
	    highlight Cursor gui=NONE guifg=bg guibg=fg
<

					*'guifont'* *'gfn'* *E235* *E596*
'guifont' 'gfn'		字符串	(默认 "DejaVu Sans Mono,Courier New,monospace"
                                 Mac: "SF Mono,Menlo,Monaco,Courier New,monospace"
                                 Linux: "Source Code Pro,DejaVu Sans Mono,Courier New,monospace"
                                 MS-Windows: "Cascadia Code,Cascadia Mono,Consolas,Courier New,monospace")
			全局
	这是一个字体列表，将用于 Vim 的 GUI 版本。在最简单的形式中，该值只是一个字体名称。
	当找不到该字体时，你会收到错误消息。为了尝试其他字体名称，可以指定一个列表，
	字体名称之间用逗号分隔。使用第一个有效的字体。

	逗号后的空格会被忽略。要在字体名称中包含逗号，请在逗号前加反斜杠。设置选项需要在
	空格和反斜杠前加一个额外的反斜杠。另请参见 |option-backslash|。例如： >vim
	    set guifont=Screen15,\ 7x13,font\\,with\\,commas
<	这将使 Vim 首先尝试使用字体 "Screen15"，如果失败，则尝试使用 "7x13"，然后使用
	"font,with,commas"。

	如果没有一个字体可以加载，Vim 将保持当前设置。如果给出一个空的字体列表，Vim 将
	尝试使用其他资源设置（对于 X，它将使用 Vim.font 资源），最后它将尝试一些应该
	总是存在的内置默认值（对于 X 是 "7x13"）。给出的字体名称应该是 "普通" 字体。
	Vim 将尝试找到相关的粗体和斜体字体。

	对于 Win32 和 Mac OS： >vim
	    set guifont=*
<	将调出字体选择器，你可以在其中选择所需的字体。

	字体名称取决于所使用的 GUI。

	对于 Mac OSX，你可以使用类似这样的内容： >vim
	    set guifont=Monaco:h10
<								*E236*
	注意，字体必须是等宽的（所有字符具有相同的宽度）。

	要在 X11 上预览字体，你可以使用 "xfontsel" 程序。"xlsfonts" 程序给出了所有可用
	字体的列表。

	对于 Win32 GUI					*E244* *E245*
	- 字体名称中可使用以下选项：
		hXX - 高度为 XX（点，可以是浮点数）
		wXX - 宽度为 XX（点，可以是浮点数）
		b   - 粗体
		i   - 斜体
		u   - 下划线
		s   - 删除线
		cXX - 字符集 XX。有效的字符集有：ANSI、ARABIC、BALTIC、CHINESEBIG5、
		      DEFAULT、EASTEUROPE、GB2312、GREEK、HANGEUL、HEBREW、JOHAB、
		      MAC、OEM、RUSSIAN、SHIFTJIS、SYMBOL、THAI、TURKISH、VIETNAMESE
		      ANSI 和 BALTIC。通常你会使用 "cDEFAULT"。

	  使用 ':' 分隔选项。
	- 可以使用 '_' 代替空格，这样就不需要使用反斜杠来转义空格。
	- 示例： >vim
	    set guifont=courier_new:h12:w5:b:cRUSSIAN
	    set guifont=Andale_Mono:h7.5:w4.5
<

				*'guifontwide'* *'gfw'* *E231* *E533* *E534*
'guifontwide' 'gfw'	字符串	(默认 "")
			全局
	用于双宽度字符的字体列表，以逗号分隔。使用第一个可以加载的字体。
	注意：这些字体的大小必须是 'guifont' 指定字体的精确两倍宽，并且高度相同。

	当 'guifont' 有一个有效字体且 'guifontwide' 为空时，Vim 将尝试将 'guifontwide'
	设置为匹配的双宽度字体。

						*'helpfile'* *'hf'*
'helpfile' 'hf'		字符串	(默认 (MS-Windows) "$VIMRUNTIME\doc\help.txt"
                                         (其他) "$VIMRUNTIME/doc/help.txt")
			全局
	主帮助文件的名称。所有分发的帮助文件应放在一个目录中。此外，'runtimepath'
	中的所有 "doc" 目录都将被使用。
	环境变量会被展开 |:set_env|。例如："$VIMRUNTIME/doc/help.txt"。如果未设置
	$VIMRUNTIME，也会尝试 $VIM。另请参见 |$VIMRUNTIME| 以及有关包含空格和反斜杠的
	|option-backslash|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'helpheight'* *'hh'*
'helpheight' 'hh'	数字	(默认 20)
			全局
	使用 ":help" 命令打开帮助窗口时的最小初始高度。帮助窗口的初始高度是当前窗口的
	一半，或者（当 'ea' 选项开启时）与其他窗口相同。当高度小于 'helpheight' 时，
	高度将设置为 'helpheight'。设置为零以禁用。

						*'helplang'* *'hlg'*
'helplang' 'hlg'	字符串	(默认消息语言或空)
			全局
	逗号分隔的语言列表。Vim 将使用第一种可以找到所需帮助的语言。英语帮助将始终作为
	最后手段使用。你可以添加 "en" 以使英语优先于另一种语言，但这只会找到存在于该语言
	中而不存在于英语帮助中的标签。
	示例： >vim
		set helplang=de,it
<	这将首先搜索德语，然后是意大利语，最后是英语帮助文件。
	在非英语帮助文件中使用 |CTRL-]| 和 ":help!" 时，Vim 将尝试在使用此选项之前找到
	当前语言中的标签。参见 |help-translated|。

				*'hidden'* *'hid'* *'nohidden'* *'nohid'*
'hidden' 'hid'		布尔	(默认开启)
			全局
	关闭时，当缓冲区被 |abandon| 时，它会卸载（包括丢失撤销信息）。开启时，
	当缓冲区被 |abandon| 时，它会变为隐藏状态。当然，显示在另一个窗口中的缓冲区
	不会变为隐藏状态。

	当以下三个条件成立时，即使在 'hidden' 选项关闭的情况下，在缓冲区列表中移动的命令
	有时也会隐藏缓冲区：
	- 缓冲区已修改
	- 'autowrite' 关闭或无法写入
	- 使用了 '!' 标志
	另请参见 |windows|。

	要隐藏特定缓冲区，请使用 'bufhidden' 选项。
	可以通过 ":hide {command}" 为单个命令设置 'hidden' |:hide|。

						*'history'* *'hi'*
'history' 'hi'		数字	(默认 10000)
			全局
	记住 ":" 命令的历史记录和以前的搜索模式的历史记录。此选项决定每个历史记录中
	可以存储多少条目（参见 |cmdline-editing| 和 'messagesopt' 以了解要记住的消息数）。
	最大值为 10000。

				*'hlsearch'* *'hls'* *'nohlsearch'* *'nohls'*
'hlsearch' 'hls'	布尔	(默认开启)
			全局
	当存在以前的搜索模式时，高亮显示其所有匹配项。|hl-Search| 高亮组决定光标下
	所有不匹配项的高亮显示，而 |hl-CurSearch| 高亮组（如果已定义）决定光标下匹配项
	的高亮显示。如果未定义 |hl-CurSearch|，则两者都使用 |hl-Search|。请注意，只有
	匹配的文本被高亮显示，任何偏移都不会应用。
	另请参见：'incsearch' 和 |:match|。
	当你厌倦了查看高亮显示的匹配项时，可以使用 |:nohlsearch| 将其关闭。这不会改变
	选项值，一旦你使用搜索命令，高亮显示就会恢复。
	'redrawtime' 指定查找匹配项所花费的最大时间。
	当搜索模式可以匹配行尾时，Vim 将尝试高亮显示所有匹配的文本。然而，这取决于搜索
	的起始位置。这将是窗口中的第一行或关闭折叠下方的第一行。未绘制的先前行中的匹配项
	可能不会在新绘制的行中继续。
	你可以通过 'shada' 中的 'h' 标志指定启动时是否恢复高亮显示状态 |shada-h|。

						*'icon'* *'noicon'*
'icon'			布尔	(默认关闭，当标题可以恢复时开启)
			全局
	开启时，窗口的图标文本将设置为 'iconstring'（如果非空）的值，或当前正在编辑的
	文件名。只使用名称的最后部分。被 'iconstring' 选项覆盖。
	仅当终端支持设置窗口图标时才有效。

							*'iconstring'*
'iconstring'		字符串	(默认 "")
			全局
	当此选项非空时，它将用于窗口的图标文本。仅当 'icon' 选项开启时才会发生。
	仅当终端支持设置窗口图标文本时才有效。
	当此选项包含 printf 样式的 '%' 项时，它们将根据用于 'statusline' 的规则展开。
	有关示例设置，请参见 'titlestring'。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

			*'ignorecase'* *'ic'* *'noignorecase'* *'noic'*
'ignorecase' 'ic'	布尔	(默认关闭)
			全局
	在搜索模式、|cmdline-completion|、在标签文件中搜索、|expr-==| 以及插入模式补全
	|ins-completion| 中忽略大小写。
	另请参见 'smartcase' 和 'tagcase'。
	可以通过在模式中使用 "\c" 或 "\C" 来覆盖，参见 |/ignorecase|。

						*'iminsert'* *'imi'*
'iminsert' 'imi'	数字	(默认 0)
			本地到缓冲区
	指定在插入模式下是否使用 :lmap 或输入法（IM）。有效值：
		0	:lmap 关闭且 IM 关闭
		1	:lmap 开启且 IM 关闭
		2	:lmap 关闭且 IM 开启
	要在用 <Esc> 离开插入模式时始终将选项重置为零，可以使用： >vim
		inoremap <ESC> <ESC>:set iminsert=0<CR>
<	这使得离开插入模式时 :lmap 和 IM 自动关闭。
	注意，在插入模式下使用 CTRL-^ 时此选项会改变 |i_CTRL-^|。
	当将 'keymap' 设置为有效的键映射名称时，该值设置为 1。
	它也用于像 "r" 和 "f" 这样的命令的参数。

						*'imsearch'* *'ims'*
'imsearch' 'ims'	数字	(默认 -1)
			本地到缓冲区
	指定在输入搜索模式时是否使用 :lmap 或输入法（IM）。有效值：
		-1	使用 'iminsert' 的值，使得在键入搜索模式时看起来也使用了
			'iminsert'
		0	:lmap 关闭且 IM 关闭
		1	:lmap 开启且 IM 关闭
		2	:lmap 关闭且 IM 开启
	注意，在命令行模式下使用 CTRL-^ 时此选项会改变 |c_CTRL-^|。
	当它不为 -1 且将 'keymap' 选项设置为有效的键映射名称时，该值设置为 1。

						*'inccommand'* *'icm'*
'inccommand' 'icm'	字符串	(默认 "nosplit")
			全局
	当非空时，在键入时显示 |:substitute|、|:smagic|、|:snomagic| 和带有
	|:command-preview| 标志的用户命令的效果。

	可能的值：
		nosplit	在缓冲区中增量显示命令的效果。
		split	类似于 "nosplit"，但还在预览窗口中显示部分屏幕外的结果。

	如果内置命令的预览太慢（超过 'redrawtime'），则 'inccommand' 会自动禁用，
	直到 |Command-line-mode| 完成。

						*'include'* *'inc'*
'include' 'inc'		字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	用于查找包含命令的模式。这是一个搜索模式，就像 "/" 命令一样（参见 |pattern|）。
	此选项用于 "[i"、"]I"、"[d" 等命令。
	通常，'isfname' 选项用于识别匹配模式后出现的文件名。但如果模式中出现 "\zs"，
	则从 "\zs" 到末尾，或到出现的 "\ze"（如果存在）匹配的文本用作文件名。使用此选项
	可以包含不在 'isfname' 中的字符，例如空格。然后你可以使用 'includeexpr' 来处理
	匹配的文本。
	有关包含空格和反斜杠的信息，请参见 |option-backslash|。

						*'includeexpr'* *'inex'*
'includeexpr' 'inex'	字符串	(默认 "")
			本地到缓冲区
	用于将通过 'include' 选项找到的字符串转换为文件名的表达式。对于 Java，
	将 "." 更改为 "/" 最有用： >vim
		setlocal includeexpr=substitute(v:fname,'\\.','/','g')
<	"v:fname" 变量将被设置为检测到的文件名。注意双反斜杠：`:set` 命令首先将它们减半，
	然后在值中保留一个，其中 "\." 字面匹配点。对于简单的字符替换，`tr()` 可以避免
	转义的需要： >vim
		setlocal includeexpr=tr(v:fname,'.','/')
<
	如果找不到未修改的文件名，也用于 |gf| 命令。允许在 'include' 语句后对名称执行
	"gf"。注意：不用于 |<cfile>|。

	如果表达式以 s: 或 |<SID>| 开头，则会被替换为脚本 ID（|local-function|）。
	示例： >vim
		setlocal includeexpr=s:MyIncludeExpr()
		setlocal includeexpr=<SID>SomeIncludeExpr()
<	否则，表达式将在设置选项的脚本上下文中评估，因此脚本本地项可用。

	如果值只是一个无参数的函数调用，则效率更高，参见 |expr-option-function|。

	如果从 modeline 设置，表达式将在 |sandbox| 中评估，参见 |sandbox-option|。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

	评估 'includeexpr' 时不允许更改文本或跳转到另一个窗口 |textlock|。

				*'incsearch'* *'is'* *'noincsearch'* *'nois'*
'incsearch' 'is'	布尔	(默认开启)
			全局
	在键入搜索命令时，显示到目前为止键入的模式匹配的位置。匹配的字符串会被高亮显示。
	如果模式无效或未找到，则不显示任何内容。屏幕会频繁更新，这仅在快速终端上有用。
	请注意，匹配项会被显示，但未找到匹配项或按 <Esc> 时，光标将返回到其原始位置。
	你仍然需要按 <Enter> 完成搜索命令才能将光标移动到匹配项。
	你可以使用 CTRL-G 和 CTRL-T 键移动到下一个和上一个匹配项。|c_CTRL-G| |c_CTRL-T|
	Vim 只搜索大约半秒钟。对于复杂的模式和/或大量文本，可能找不到匹配项。这是为了避免
	在键入模式时 Vim 挂起。
	|hl-IncSearch| 高亮组决定高亮显示。
	当 'hlsearch' 开启时，在键入搜索命令时，所有匹配的字符串也会被高亮显示。
	另请参见：'hlsearch'。
	如果你不想开启 'hlsearch'，但希望在搜索时高亮显示所有匹配项，你可以使用自动命令
	来开启和关闭 'hlsearch'。示例： >vim
		augroup vimrc-incsearch-highlight
		  autocmd!
		  autocmd CmdlineEnter [\/\?] :set hlsearch
		  autocmd CmdlineLeave [\/\?] :set nohlsearch
		augroup END
<
	CTRL-L 可用于将当前匹配项后的一个字符添加到命令行。如果设置了 'ignorecase' 和
	'smartcase' 且命令行中没有大写字符，则添加的字符会转换为小写。
	CTRL-R CTRL-W 可用于添加当前匹配项末尾的单词，不包括已键入的字符。

						*'indentexpr'* *'inde'*
'indentexpr' 'inde'	字符串	(默认 "")
			本地到缓冲区
	评估以获得一行适当缩进的表达式。在创建新行时，用于 |=| 操作符以及插入模式下由
	'indentkeys' 选项指定的情况。当此选项非空时，它会覆盖 'cindent' 和
	'smartindent' 缩进。当设置了 'lisp' 时，仅当 'lispoptions' 包含 "expr:1" 时才
	使用此选项。
	表达式在 |v:lnum| 设置为要计算缩进的行号时进行评估。评估表达式时光标也在该行
	（但可能会移动）。

	如果表达式以 s: 或 |<SID>| 开头，则会被替换为脚本 ID（|local-function|）。
	示例： >vim
		set indentexpr=s:MyIndentExpr()
		set indentexpr=<SID>SomeIndentExpr()
<	否则，表达式将在设置选项的脚本上下文中评估，因此脚本本地项可用。

	使用无参数的函数调用的优点是速度更快，参见 |expr-option-function|。

	表达式必须返回缩进的空格数。它可以返回 "-1" 以保持当前缩进（这意味着缩进使用
	'autoindent'）。
	对计算缩进有用的函数有 |indent()|、|cindent()| 和 |lispindent()|。
	表达式的评估不能有副作用！它不能更改文本、跳转到另一个窗口等。之后光标位置总是
	会恢复，因此光标可能会移动。
	通常，此选项将设置为调用一个函数： >vim
		set indentexpr=GetMyIndent()
<	错误消息将被抑制，除非 'debug' 选项包含 "msg"。
	参见 |indent-expression|。

	如果从 modeline 设置，表达式将在 |sandbox| 中评估，参见 |sandbox-option|。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

	评估 'indentexpr' 时不允许更改文本或跳转到另一个窗口 |textlock|。

						*'indentkeys'* *'indk'*
'indentkeys' 'indk'	字符串	(默认 "0{,0},0),0],:,0#,!^F,o,O,e")
			本地到缓冲区
	在插入模式下键入时导致当前行重新缩进的键列表。仅当 'indentexpr' 非空时才会发生。
	格式与 'cinkeys' 相同，参见 |indentkeys-format|。
	参见 |C-indenting| 和 |indent-expression|。

			*'infercase'* *'inf'* *'noinfercase'* *'noinf'*
'infercase' 'inf'	布尔	(默认关闭)
			本地到缓冲区
	在插入模式下进行关键字补全 |ins-completion| 时，并且 'ignorecase' 也开启，
	匹配项的大小写会根据键入的文本进行调整。如果键入的文本包含小写字母而匹配项有
	大写字母，则补全部分将变为小写。如果键入的文本没有小写字母，且匹配项在键入文本
	有大写字母的地方有小写字母，并且该字母前有一个字母，则补全部分将变为大写。
	使用 'noinfercase' 时，匹配项原样使用。

						*'isfname'* *'isf'*
'isfname' 'isf'		字符串	(默认 Windows:
                             "@,48-57,/,\,.,-,_,+,,,#,$,%,{,},[,],@-@,!,~,="
                            其他: "@,48-57,/,.,-,_,+,,,#,$,%,~,=")
			全局
	此选项指定的字符包含在文件名和路径名中。文件名用于 "gf"、"[i" 等命令以及
	标签文件中。它也用于 |pattern| 中的 "\f"。
	多字节字符 256 及以上总是包含在内，只有 255 以下的字符通过此选项指定。
	对于 UTF-8，字符 0xa0 到 0xff 也包含在内。
	在将此选项添加空白字符之前请三思。尽管文件名中可能出现空格，但其效果是 Vim
	在进行补全时不知道文件名的起止位置。如果不在 'isfname' 中包含空格，效果可能会
	更好。

	请注意，在使用反斜杠作为路径分隔符的系统上，Vim 会尽力使其按你期望的方式工作。
	这有点棘手，因为 Vi 最初使用反斜杠来转义特殊字符。在这些系统上，Vim 不会删除
	普通文件名前面的反斜杠，但在 Unix 及类似系统上会。默认情况下不包含 '&' 和 '^'，
	因为它们对 cmd.exe 是特殊的。

	此选项的格式是一个部分列表，以逗号分隔。每个部分可以是单个字符编号或一个范围。
	范围是两个字符编号，中间用 '-' 分隔。字符编号可以是 0 到 255 之间的十进制数，
	也可以是 ASCII 字符本身（对数字无效）。示例：
		"_,-,128-140,#-43"	（包含 '_' 和 '-' 以及 128 到 140 的范围
					和 '#' 到 43）
	如果部分以 '^' 开头，则以下字符编号或范围将从选项中排除。选项从左到右解释。
	将排除的字符放在包含它的范围之后。要包含 '^' 本身，请将其用作选项的最后一个字符
	或范围的末尾。示例：
		"^a-z,#,^"	（排除 'a' 到 'z'，包含 '#' 和 '^'）
	如果字符是 '@'，则包含所有 isalpha() 返回 TRUE 的字符。通常这些是字符 a 到 z、
	A 到 Z，以及带重音的字符。要包含 '@' 本身，请使用 "@-@"。示例：
		"@,^a-z"	所有字母字符，排除小写 ASCII 字母。
		"a-z,A-Z,@-@"	所有字母加上 '@' 字符。
	可以通过在期望字符编号的地方使用它来包含逗号。示例：
		"48-57,,,_"	数字、逗号和下划线。
	可以通过添加 '^' 前缀来排除逗号。示例：
		" -~,^,,9"	所有从空格到 '~' 的字符，排除逗号，加上 <Tab>。
	有关包含空格和反斜杠的信息，请参见 |option-backslash|。

						*'isident'* *'isi'*
'isident' 'isi'		字符串	(默认 Windows:
                                           "@,48-57,_,128-167,224-235"
                                其他: "@,48-57,_,192-255")
			全局
	此选项给出的字符包含在标识符中。标识符用于识别环境变量以及在匹配 'define' 选项
	之后。它也用于 |pattern| 中的 "\i"。有关此选项格式的说明，请参见 'isfname'。
	对于 '@'，仅使用 255 以下的字符。
	注意：如果更改此选项，可能会破坏环境变量的展开。例如，当包含 '/' 且 Vim 尝试
	展开 "$HOME/.local/state/nvim/shada/main.shada" 时。也许你应该改为更改
	'iskeyword'。

						*'iskeyword'* *'isk'*
'iskeyword' 'isk'	字符串	(默认 "@,48-57,_,192-255")
			本地到缓冲区
	关键字用于许多命令的搜索和识别："w"、"*"、"[i" 等。它也用于 |pattern| 中的 "\k"。
	有关此选项格式的说明，请参见 'isfname'。对于 '@'，255 以上的字符检查 "word"
	字符类（任何根据 Unicode 通用类别被归类为字母、数字或表情符号的字符）。

	请注意，"\k" 字符类与 |word| 移动之间存在差异。前者匹配任何单词字符，而后者在
	字符类更改时停止。

	对于 C 程序，你可以使用 "a-z,A-Z,48-57,_,.,-,>"。
	对于帮助文件，它被设置为除 "*"、'"' 和 '|' 之外的所有非空白可打印字符（这样
	在命令上按 CTRL-] 可以找到该命令的帮助）。
	当 'lisp' 选项开启时，总是包含 '-' 字符。
	此选项也影响语法高亮，除非语法使用 |:syn-iskeyword|。

						*'isprint'* *'isp'*
'isprint' 'isp'		字符串	(默认 "@,161-255")
			全局
	此选项给出的字符直接显示在屏幕上。它也用于 |pattern| 中的 "\p"。从空格（ASCII 32）
	到 '~'（ASCII 126）的字符总是直接显示，即使它们未包含在 'isprint' 中或被排除。
	有关此选项格式的说明，请参见 'isfname'。

	不可打印字符用两个字符显示：
		  0 -  31	"^@" - "^_"
		 32 - 126	总是单个字符
		   127		"^?"
		128 - 159	"~@" - "~_"
		160 - 254	"| " - "|~"
		   255		"~?"
	128 到 255 之间的非法字节（无效 UTF-8）显示为 <xx>，其中 xx 是字节的十六进制值。
	当 'display' 包含 "uhex" 时，所有不可打印字符都显示为 <xx>。
	SpecialKey 高亮组将用于不可打印字符。|hl-SpecialKey|

	多字节字符 256 及以上总是包含在内，只有 255 以下的字符通过此选项指定。当一个字符
	可打印但在当前字体中不可用时，将显示替换字符。
	不可打印和零宽度的 Unicode 字符显示为 <xxxx>。没有指定这些字符的选项。

			*'joinspaces'* *'js'* *'nojoinspaces'* *'nojs'*
'joinspaces' 'js'	布尔	(默认关闭)
			全局
	使用连接命令时，在 '.'、'?' 和 '!' 后插入两个空格。否则只插入一个空格。

						*'jumpoptions'* *'jop'*
'jumpoptions' 'jop'	字符串	(默认 "clean")
			全局
	更改 |jumplist| 行为的单词列表。
	  stack         使跳转列表的行为类似于标签栈。跳转列表中条目的相对位置得以保留，
			但代价是在跳转列表中向后导航然后跳转到一个位置时，会丢弃后续条目。
			|jumplist-stack|

	  view          当在跳转列表、|changelist|、|alternate-file| 中移动、使用
			|mark-motions| 或弹出 |tagstack| 时，尝试恢复发生操作的
			|mark-view|。

	  clean         从跳转列表中移除未加载的缓冲区。

						*'keymap'* *'kmp'*
'keymap' 'kmp'		字符串	(默认 "")
			本地到缓冲区
	键盘映射的名称。参见 |mbyte-keymap|。
	将此选项设置为有效的键映射名称会产生将 'iminsert' 设置为 1 的副作用，从而使
	键映射生效。'imsearch' 也会设置为 1，除非它为 -1。
	只能使用字母数字字符、'.'、'-' 和 '_'。

						*'keymodel'* *'km'*
'keymodel' 'km'		字符串	(默认 "")
			全局
	逗号分隔的单词列表，启用按键可以执行的特殊功能。可以使用以下值：
	   startsel	使用按下的特殊键开始选择（选择模式或可视模式，取决于
			'selectmode' 中是否存在 "key"）。
	   stopsel	使用未按下的特殊键停止选择。
	此上下文中的特殊键是光标键、<End>、<Home>、<PageUp> 和 <PageDown>。

						*'keywordprg'* *'kp'*
'keywordprg' 'kp'	字符串	(默认 ":Man", Windows: ":help")
			全局或本地到缓冲区 |global-local|
	用于 |K| 命令的程序。环境变量会被展开 |:set_env|。

	特殊情况：
	- ":help" 使用 |:help| 打开光标处的 |word|。（注意，以前将全局选项设置为空值
	  会执行此操作，现在已弃用。）
	- ":help!" 对光标处的 |WORD| 执行 |:help!|（DWIM）。
	- 如果值以 ":" 开头，则将其作为带有 [count] 前缀的 Ex 命令调用。
	- 如果值为 "man" 或 "man -s"，[count] 是 man 页面章节号。

	有关包含空格和反斜杠的信息，请参见 |option-backslash|。

	示例： >vim
		set keywordprg=:help!
		set keywordprg=man\ -s
		set keywordprg=:Man
<
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'langmap'* *'lmap'* *E357* *E358*
'langmap' 'lmap'	字符串	(默认 "")
			全局
	此选项允许将键盘切换到特殊语言模式。当你在插入模式下输入文本时，字符直接插入。
	在普通模式下，'langmap' 选项负责将这些特殊字符转换为键的原始含义。这意味着你
	无需更改键盘模式即可执行普通模式命令。
	这与 'keymap' 选项相反，后者在插入模式下映射字符。
	也可以考虑将 'langremap' 设置为 off，以防止 'langmap' 应用于映射产生的字符。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

	示例（希腊语，UTF-8）：				*greek*  >vim
	    set langmap=ΑA,ΒB,ΨC,ΔD,ΕE,ΦF,ΓG,ΗH,ΙI,ΞJ,ΚK,ΛL,ΜM,ΝN,ΟO,ΠP,QQ,ΡR,ΣS,ΤT,ΘU,ΩV,WW,ΧX,ΥY,ΖZ,αa,βb,ψc,δd,εe,φf,γg,ηh,ιi,ξj,κk,λl,μm,νn,οo,πp,qq,ρr,σs,τt,θu,ωv,ςw,χx,υy,ζz
<	示例（交换命令中 z 和 y 的含义）： >vim
	    set langmap=zy,yz,ZY,YZ
<
	'langmap' 选项是一个部分列表，以逗号分隔。每个部分可以采用两种形式之一：
	1. 一对列表。每对是一个 "从" 字符后跟 "到" 字符。示例："aA"、"aAbBcC"。
	2. "从" 字符列表、分号和 "到" 字符列表。示例："abc;ABC"
	示例："aA,fgh;FGH,cCdDeE"
	特殊字符需要用反斜杠转义。这些特殊字符是：';'、','、'"'、'|' 和反斜杠本身。

	这将允许你在不来回切换语言的情况下执行 vim 操作。在以下情况下，你的语言字符将被
	理解为正常的 vim 英文字符（根据 langmap 映射）：
	 o 普通/可视模式（命令、缓冲区/寄存器名称、用户映射）
	 o 插入/替换模式：CTRL-R 后的寄存器名称
	 o 插入/替换模式：映射
	命令行模式下输入的字符不受此选项影响。注意，此选项可以随时更改，允许在不同语言/
	编码的映射之间切换。使用映射以避免每次都输入！

						*'langmenu'* *'lm'*
'langmenu' 'lm'		字符串	(默认 "")
			全局
	用于菜单翻译的语言。指示从 'runtimepath' 中的 "lang" 目录加载哪个文件： >vim
		"lang/menu_" .. &langmenu .. ".vim"
<	（不带空格）。例如，无论 $LANG 设置为何，始终使用荷兰语菜单： >vim
		set langmenu=nl_NL.ISO_8859-1
<	当 'langmenu' 为空时，使用 |v:lang|。
	只能使用普通文件名字符，`/\*?[|<>` 是非法的。
	如果你的 $LANG 设置为非英语语言，但你希望使用英语菜单： >vim
		set langmenu=none
<	必须在加载菜单、开启文件类型检测或语法高亮之前设置此选项。一旦定义了菜单，设置
	此选项将无效。但你可以这样做： >vim
		source $VIMRUNTIME/delmenu.vim
		set langmenu=de_DE.ISO_8859-1
		source $VIMRUNTIME/menu.vim
<	警告：这将删除你自己定义的所有菜单！

			*'langremap'* *'lrm'* *'nolangremap'* *'nolrm'*
'langremap' 'lrm'	布尔	(默认关闭)
			全局
	关闭时，设置 'langmap' 不会应用于映射产生的字符。如果设置 'langmap' 禁用了一些
	映射，请确保此选项关闭。

						*'laststatus'* *'ls'*
'laststatus' 'ls'	数字	(默认 2)
			全局
	此选项的值影响最后一个窗口何时会有状态行：
		0: 从不
		1: 仅当至少有两个窗口时
		2: 总是
		3: 总是且仅最后一个窗口
	如果你有多个窗口，带有状态行的屏幕看起来更好，但它会占用另一屏幕行。|status-line|

			*'lazyredraw'* *'lz'* *'nolazyredraw'* *'nolz'*
'lazyredraw' 'lz'	布尔	(默认关闭)
			全局
	设置此选项后，在执行宏、寄存器和其他未键入的命令时，屏幕不会重绘。此外，更新
	窗口标题也会推迟。要强制更新，请使用 |:redraw|。
	这偶尔可能导致显示错误。它仅旨在在执行重绘可能导致闪烁或减速的操作时临时设置。

						*'lhistory'* *'lhi'*
'lhistory' 'lhi'	数字	(默认 10)
			本地到窗口
	类似于 'chistory'，但用于与窗口关联的位置列表栈。如果在位置列表窗口本身或与
	位置列表栈关联的窗口中更改该选项，新值也将应用于另一个窗口。这意味着对于给定的
	位置列表窗口及其对应的窗口，此值将始终相同。有关其他信息，请参见 |quickfix-stack|。

			*'linebreak'* *'lbr'* *'nolinebreak'* *'nolbr'*
'linebreak' 'lbr'	布尔	(默认关闭)
			本地到窗口
	如果开启，Vim 将在 'breakat' 中的字符处换行长行，而不是在适合屏幕的最后一个
	字符处。与 'wrapmargin' 和 'textwidth' 不同，这不会在文件中插入 <EOL>，它只影响
	文件的显示方式，而不影响其内容。
	如果设置了 'breakindent'，行会在视觉上缩进。然后，使用 'showbreak' 的值放在换行
	行的前面。当 'wrap' 选项关闭时，不使用此选项。
	注意，<EOL> 之后的 <Tab> 字符大多不会以正确的空白量显示。

							*'lines'* *E593*
'lines'			数字	(默认 24 或终端高度)
			全局
	Vim 窗口的行数。通常你不需要设置此选项。它由终端初始化代码自动完成。
	当 Vim 在 GUI 中或在可调整大小的窗口中运行时，设置此选项将导致窗口大小更改。
	如果你只想为 GUI 使用该大小，请将命令放在你的 |gvimrc| 文件中。
	Vim 将行数限制为适合屏幕的数量。你可以使用此命令获得尽可能高的窗口： >vim
		set lines=999
<	最小值为 2，最大值为 1000。

						*'linespace'* *'lsp'*
'linespace' 'lsp'	数字	(默认 0)
			全局
			仅在 GUI 中
	字符之间插入的像素行数。如果字体使用完整的字符单元格高度，导致行与行之间接触，
	此选项很有用。当非零时，有空间放置下划线。
	对于某些字体，行之间可能有太多空间（为上升部和下降部留出空间）。那么将 'linespace'
	设置为负值是有意义的。不过这可能会导致显示问题！

						*'lisp'* *'nolisp'*
'lisp'			布尔	(默认关闭)
			本地到缓冲区
	Lisp 模式：在插入模式下键入 <Enter> 时，将下一行的缩进设置为 Lisp 标准（嗯，算是吧）。
	使用 "cc" 或 "S" 时也会发生。'autoindent' 也必须开启才能工作。
	另请参见 'lispwords'。
	'-' 字符包含在关键字字符中。如果 'equalprg' 为空，则重新定义 "=" 操作符以使用
	相同的缩进算法，而不是调用外部程序。

						*'lispoptions'* *'lop'*
'lispoptions' 'lop'	字符串	(默认 "")
			本地到缓冲区
	影响通过 'lisp' 选项启用的 Lisp 缩进的逗号分隔项目列表。目前只支持一个项目：
		expr:1	当设置 'indentexpr' 时，使用它进行 Lisp 缩进
		expr:0	不将 'indentexpr' 用于 Lisp 缩进（默认）
	注意，当使用 'indentexpr' 时，`=` 操作符会缩进所有行，否则第一行不会缩进
	（Vi 兼容）。

						*'lispwords'* *'lw'*
'lispwords' 'lw'	字符串	(默认非常长)
			全局或本地到缓冲区 |global-local|
	影响通过 'lisp' 选项启用的 Lisp 缩进的逗号分隔单词列表。

						*'list'* *'nolist'*
'list'			布尔	(默认关闭)
			本地到窗口
	列表模式：默认情况下，将制表符显示为 ">"，尾随空格显示为 "-"，不间断空格字符显示
	为 "+"。有助于查看制表符和空格之间的差异以及尾随空格。进一步由 'listchars' 选项
	更改。

	当 'listchars' 不包含 "tab" 字段时，制表符显示为 "^I" 或 "<09>"，就像不可打印
	字符的显示方式一样。

	光标显示在 Tab 字符占用的空间的开头，而不是像普通模式下通常在末尾显示。要在使用
	空格显示制表符时获得此光标位置，请使用： >vim
		let &list = v:true | let &lcs = 'tab:  '
<
	注意，当 'cpoptions' 包含 'L' 时，列表模式也会影响格式化（通过 'textwidth' 或
	'wrapmargin' 设置）。有关更改制表符显示方式的信息，请参见 'listchars'。

						*'listchars'* *'lcs'*
'listchars' 'lcs'	字符串	(默认 "tab:> ,trail:-,nbsp:+")
			全局或本地到窗口 |global-local|
	在 'list' 模式和 |:list| 命令中使用的字符串。它是一个逗号分隔的字符串设置列表。
	*E1511*

							*lcs-eol*
	  eol:c		在每行末尾显示的字符。省略时，行尾没有额外字符。
							*lcs-tab*
	  tab:xy[z]	用于显示制表符的两到三个字符。第三个字符是可选的。

	  tab:xy	总是使用 'x'，然后根据适合的数量使用 'y'。因此 "tab:>-" 显示： >
				>
				>-
				>--
				等等。
<
	  tab:xyz	总是使用 'z'，然后在其前面添加 'x'，然后根据适合的数量使用 'y'。
			因此 "tab:<->" 显示： >
				>
				<>
				<->
				<-->
				等等。
<
			当省略 "tab:" 时，制表符显示为 ^I。
							*lcs-space*
	  space:c		用于显示空格的字符。省略时，空格留空。
							*lcs-multispace*
	  multispace:c...	循环使用一个或多个字符来显示多个连续的空格。覆盖 "space" 设置，
				单个空格除外。省略时，使用 "space" 设置。例如，
				`:set listchars=multispace:---+` 将十个连续空格显示为： >
					---+---+--
<
							*lcs-lead*
	  lead:c		用于显示前导空格的字符。省略时，前导空格留空。覆盖前导空格的
				"space" 和 "multispace" 设置。你可以将其与 "tab:" 结合使用，
				例如： >vim
					set listchars+=tab:>-,lead:.
<
							*lcs-leadmultispace*
	  leadmultispace:c...	类似于 |lcs-multispace| 值，但仅用于前导空格。也覆盖前导
				多个空格的 |lcs-lead|。
				`:set listchars=leadmultispace:---+` 将十个连续的前导空格
				显示为： >
					---+---+--XXX
<
				其中 "XXX" 表示行中的第一个非空白字符。
							*lcs-leadtab*
	  leadtab:xy[z]		类似于 |lcs-tab|，但仅用于前导制表符。省略时，对前导制表符
				使用 "tab" 设置。此功能需要同时设置 |lcs-tab|。 *E1572*
				你可以将其与 "tab:" 结合使用，例如： >vim
					let &listchars = 'tab:>-,leadtab:. '
<				这将前导制表符显示为句点（.），其他制表符显示为 ">--"。
							*lcs-trail*
	  trail:c		用于显示尾随空格的字符。省略时，尾随空格留空。覆盖尾随空格的
				"space" 和 "multispace" 设置。
							*lcs-extends*
	  extends:c		当 'wrap' 关闭且行继续超出屏幕右侧时，在最后一列显示的字符。
							*lcs-precedes*
	  precedes:c		当第一列可见字符之前有文本时，在物理行的第一可见列显示的字符。
							*lcs-conceal*
	  conceal:c		当 'conceallevel' 设置为 1 时，用于替换隐藏文本的字符。
				省略时使用空格。
							*lcs-nbsp*
	  nbsp:c		用于显示不间断空格字符（0xA0（160 十进制）和 U+202F）的字符。
				省略时留空。

	不应使用字符 ':' 和 ','。可以使用 UTF-8 字符。所有字符必须是单宽度的。*E1512*

	每个字符可以指定为十六进制： >vim
		set listchars=eol:\\x24
		set listchars=eol:\\u21b5
		set listchars=eol:\\U000021b5
<	注意使用双反斜杠。对于 \\x，十六进制字符数必须正好为 2；对于 \\u 为 4；对于 \\U 为 8。

	示例： >vim
	    set lcs=tab:>-,trail:-
	    set lcs=tab:>-,eol:<,nbsp:%
	    set lcs=extends:>,precedes:<
<	|hl-NonText| 高亮组将用于 "eol"、"extends" 和 "precedes"。|hl-Whitespace| 用于
	"nbsp"、"space"、"tab"、"multispace"、"lead" 和 "trail"。

			*'loadplugins'* *'lpl'* *'noloadplugins'* *'nolpl'*
'loadplugins' 'lpl'	布尔	(默认开启)
			全局
	开启时，在启动时加载插件脚本 |load-plugins|。可以在你的 |vimrc| 文件中重置此选项，
	以禁用加载插件。
	注意，使用 "-u NONE" 和 "--noplugin" 命令行参数会重置此选项。|-u| |--noplugin|

						*'makeef'* *'mef'*
'makeef' 'mef'		字符串	(默认 "")
			全局
	|:make| 命令（参见 |:make_makeprg|）和 |:grep| 命令的错误文件名。
	当它为空时，将使用内部生成的临时文件。
	当包含 "##" 时，它会被替换为一个数字，以使名称唯一。这确保 ":make" 命令不会覆盖
	现有文件。
	不用于 ":cf" 命令。请参见 'errorfile'。
	环境变量会被展开 |:set_env|。有关包含空格和反斜杠的信息，请参见 |option-backslash|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'makeencoding'* *'menc'*
'makeencoding' 'menc'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	用于读取外部命令输出的编码。为空时，不转换编码。
	这用于 `:make`、`:lmake`、`:grep`、`:lgrep`、`:grepadd`、`:lgrepadd`、`:cfile`、
	`:cgetfile`、`:caddfile`、`:lfile`、`:lgetfile` 和 `:laddfile`。

	这在 MS-Windows 上最有用。如果启用了 iconv，将 'makeencoding' 设置为 "char" 与
	设置为系统区域设置编码具有相同的效果。示例： >vim
		set makeencoding=char	" 使用系统区域设置
<

						*'makeprg'* *'mp'*
'makeprg' 'mp'		字符串	(默认 "make")
			全局或本地到缓冲区 |global-local|
	用于 ":make" 命令的程序。参见 |:make_makeprg|。
	此选项可以包含 '%' 和 '#' 字符（参见 |:_%| 和 |:_#|），它们会展开为当前和备用
	文件名。使用 |::S| 来转义文件名，以防它们包含特殊字符。
	环境变量会被展开 |:set_env|。有关包含空格和反斜杠的信息，请参见 |option-backslash|。
	注意，'|' 必须转义两次：一次用于 ":set"，一次用于命令的解释。当你使用名为 "myfilter"
	的过滤器时，请这样做： >vim
	    set makeprg=gmake\ \\\|\ myfilter
<	可以给出占位符 "$*"（甚至可以多次给出）来指定包含参数的位置，例如： >vim
	    set makeprg=latex\ \\\\nonstopmode\ \\\\input\\{$*}
<	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'matchpairs'* *'mps'*
'matchpairs' 'mps'	字符串	(默认 "(:),{:},[:]")
			本地到缓冲区
	组成配对的字符。|%| 命令从一个字符跳转到另一个字符。
	只允许使用不同的字符对，因此不能在两个双引号之间跳转。
	字符必须用冒号分隔。
	对必须用逗号分隔。示例，包含 '<' 和 '>'（用于 HTML）： >vim
		set mps+=<:>

<	一个更奇特的示例，用于在赋值语句中的 '=' 和 ';' 之间跳转，对 C 和 Java 等语言很有用： >vim
		au FileType c,cpp,java set mps+==:;

<	有关使用 "%" 的更高级方法，请参阅 $VIMRUNTIME/plugin 目录中的 matchit.vim 插件。
	|add-local-help|

						*'matchtime'* *'mat'*
'matchtime' 'mat'	数字	(默认 5)
			全局
	当设置了 'showmatch' 时，显示匹配括号的十分之一秒。请注意，与其他设置时间的
	选项不同，这不是毫秒。这是为了与 Nvi 兼容。

						*'maxfuncdepth'* *'mfd'*
'maxfuncdepth' 'mfd'	数字	(默认 100)
			全局
	用户函数的函数调用最大深度。这通常用于捕获无限递归。当使用具有更深递归的递归函数时，
	将 'maxfuncdepth' 设置为更大的数字。但这将使用更多内存，存在内存耗尽时失败的风险。
	将此限制增加到 200 以上也会更改 Ex 命令递归的最大值，参见 |E169|。
	另请参见 |:function|。
	也用于回调函数的最大深度。

					*'maxmapdepth'* *'mmd'* *E223*
'maxmapdepth' 'mmd'	数字	(默认 1000)
			全局
	映射在不产生可用字符的情况下执行的最大次数。这通常用于捕获无限映射，例如
	":map x y" 与 ":map y x"。它仍然无法捕获 ":map g wg"，因为在执行下一个映射之前
	使用了 'w'。另请参见 |key-mapping|。

						*'maxmempattern'* *'mmp'*
'maxmempattern' 'mmp'	数字	(默认 1000)
			全局
	用于模式匹配的最大内存量（以 Kbyte 为单位）。最大值约为 2000000。使用此选项可以
	无限制地工作。*E363*
	当 Vim 达到限制时，它会给出错误消息，并且行为大多类似于按下了 CTRL-C。
	达到限制通常意味着模式非常低效或过于复杂。这可能在非常长的行上使用模式
	`\(.\)*` 时发生。`.*` 的效果要好得多。
	在重绘时，当语法规则试图匹配复杂的文本结构时，也可能发生这种情况。
	在达到 'maxmempattern' 限制之前，Vim 可能会耗尽内存，在这种情况下，你会收到
	"Out of memory" 错误。

					*'maxsearchcount'* *'msc'*
'maxsearchcount' 'msc'	数字	(默认 999)
			全局
	搜索计数状态 |shm-S| 显示的最大匹配数。当匹配数超过此值时，Vim 显示 ">" 而不是
	确切计数，以保持搜索速度。
	注意：较大的值可能会影响性能。
	该值必须在 1 到 9999 之间。另请参见 |searchcount()| 函数。

						*'menuitems'* *'mis'*
'menuitems' 'mis'	数字	(默认 25)
			全局
	菜单中使用的最大项目数。用于从项目列表生成的菜单，例如 Buffers 菜单。更改此选项
	不会立即生效，必须首先刷新菜单。

						*'messagesopt'* *'mopt'*
'messagesopt' 'mopt'	字符串	(默认 "hit-enter,history:500,progress:c")
			全局
	输出消息的选项设置。它可以包含以下项目。项目必须用逗号分隔。

	history:{n}	确定 |:messages| 历史记录中记住的条目数。最大值为 10000。
			设置为零会清除消息历史记录。
			此项目必须始终存在。

	hit-enter	当消息长于 'cmdheight' 大小时，使用 |hit-enter| 提示。

	progress:{s}	确定在哪里显示进度消息。有效值为：
			  - 空：命令行中不显示进度消息。
			  - "c"：进度消息显示在命令行中。

	wait:{n}	已随 |ui2| 弃用。不显示 |hit-enter| 提示，而是等待 {n} 毫秒，
			以便用户有机会阅读消息。最大 {n} 为 10000。使用 0 禁用等待
			（用户不会看到任何 "hit-enter" 消息）。
			当存在 "hit-enter" 时忽略，但当不存在 "hit-enter" 时需要。

						*'mkspellmem'* *'msm'*
'mkspellmem' 'msm'	字符串	(默认 "460000,2000,500")
			全局
	|:mkspell| 的参数。这调整何时开始压缩单词树。当单词很多时，压缩可能很慢，
	但需要压缩以避免内存耗尽。每个单词使用的内存量很大程度上取决于单词的相似程度，
	这就是为什么这个调整很复杂。

	有三个数字，用逗号分隔： >
		{start},{inc},{added}
<
	对于大多数语言，未压缩的单词树适合内存。{start} 指定在进行任何压缩之前可以使用的
	内存量（以 Kbyte 为单位）。它应该略小于 Vim 可用的内存量。

	当超过 {start} 限制时，{inc} 数字指定在进行另一次压缩之前可以分配的内存量
	（以 Kbyte 为单位）。较低的数字意味着在添加较少的单词后进行压缩，这很慢。较高的
	数字意味着将分配更多内存。

	执行压缩后，在忽略 {inc} 限制并且当需要任何额外内存时执行压缩之前，可以添加
	{added} 乘以 1024 个单词。较低的数字意味着达到 {inc} 限制的可能性较小，使用的
	内存更少，但速度更慢。

	这些数字重要的语言是意大利语和匈牙利语。默认值适用于你拥有大约 512 Mbyte 内存的情况。
	如果你有 1 Gbyte，你可以使用： >vim
		set mkspellmem=900000,3000,800
<	如果你少于 512 Mbyte，|:mkspell| 可能会对某些语言失败，无论你将 'mkspellmem'
	设置为什么。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

				*'modeline'* *'ml'* *'nomodeline'* *'noml'*
'modeline' 'ml'		布尔	(默认开启（root 用户关闭）)
			本地到缓冲区
	如果 'modeline' 开启，'modelines' 给出检查 set 命令的行数。如果 'modeline' 关闭
	或 'modelines' 为零，则不检查任何行。参见 |modeline|。

			*'modelineexpr'* *'mle'* *'nomodelineexpr'* *'nomle'*
'modelineexpr' 'mle'	布尔	(默认关闭)
			全局
	开启时，允许在 modeline 中设置一些表达式选项。检查该选项是否受 'modelineexpr'
	影响。另请参见 |modeline|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'modelines'* *'mls'*
'modelines' 'mls'	数字	(默认 5)
			全局
	如果 'modeline' 开启，'modelines' 给出检查 set 命令的行数。如果 'modeline' 关闭
	或 'modelines' 为零，则不检查任何行。参见 |modeline|。


			*'modifiable'* *'ma'* *'nomodifiable'* *'noma'* *E21*
'modifiable' 'ma'	布尔	(默认开启)
			本地到缓冲区
	关闭时，缓冲区内容不能更改。'fileformat' 和 'fileencoding' 选项也不能更改。
	可以在启动时使用 |-M| 命令行参数重置。

				*'modified'* *'mod'* *'nomodified'* *'nomod'*
'modified' 'mod'	布尔	(默认关闭)
			本地到缓冲区  |local-noglobal|
	开启时，缓冲区被认为已修改。此选项在以下情况下设置：
	1. 自上次写入以来对文本进行了更改。使用 |undo| 命令返回到原始文本将重置此选项。
	   但是，撤消写入缓冲区之前所做的更改将再次设置该选项，因为文本与写入时不同。
	2. 'fileformat' 或 'fileencoding' 与其原始值不同。原始值在读取或写入缓冲区时设置。
	   ":set nomodified" 命令也会将原始值重置为当前值，并且 'modified' 选项将被重置。
	   同样适用于 'eol' 和 'bomb'。
	此选项不会因 BufNewFile、BufRead/BufReadPost、BufWritePost、FileAppendPost 或
	VimLeave 自动命令事件而对缓冲区进行更改而设置。参见 |gzip-example| 了解说明。

	当 'buftype' 为 "prompt" 时，'modified' 不会在缓冲区更改时隐式设置，但用户或插件
	可以显式设置它。

	当 'buftype' 为 "nowrite" 或 "nofile" 时，可以设置此选项，但会被忽略。

	请注意，文本实际上可能是相同的，例如，在 "A" 上使用 "rA" 时会设置 'modified'。

						*'more'* *'nomore'*
'more'			布尔	(默认开启)
			全局
	开启时，当整个屏幕被填满时，列表会暂停。你将获得 |more-prompt|。当此选项关闭时，
	没有暂停，列表会继续直到完成。

							*'mouse'*
'mouse'			字符串	(默认 "nvi")
			全局
	启用鼠标支持。例如，在普通模式和可视模式下启用鼠标： >vim
		set mouse=nv
<
	要暂时禁用鼠标支持，请在使用鼠标时按住 shift 键。

	可以为不同模式启用鼠标支持：
		n	普通模式
		v	可视模式
		i	插入模式
		c	命令行模式
		h	编辑帮助文件时的所有先前模式
		a	所有先前模式
		r	用于 |hit-enter| 和 |more-prompt| 提示

	在文本缓冲区中任意位置单击左键可将光标置于该处。这也适用于操作符，例如，键入 |d|
	然后单击左键，即可删除从当前光标位置到单击位置之间的文本。

	拖动窗口的 |status-line| 或垂直分隔线以调整其大小。

	如果为 "v"（可视模式）启用，则双击选择单词，三击选择行，四击选择矩形块。

	有关使用鼠标滚轮滚动，请参见 |scroll-mouse-wheel|。

	注意：在终端中启用鼠标时，复制/粘贴将尽可能使用 "* 寄存器。另请参见 'clipboard'。

	相关选项：
	'mousefocus'	窗口焦点跟随鼠标指针
	'mousemodel'	鼠标按钮执行何种操作
	'mousehide'	键入文本时隐藏鼠标指针
	'selectmode'	是启动选择模式还是可视模式

		*'mousefocus'* *'mousef'* *'nomousefocus'* *'nomousef'*
'mousefocus' 'mousef'	布尔	(默认关闭)
			全局
	鼠标指针所在的窗口将自动激活。当以其他方式更改窗口布局或窗口焦点时，鼠标指针将
	移动到具有键盘焦点的窗口。默认关闭，因为它会使下拉菜单的使用有点笨拙，因为指针
	经过可能会意外激活窗口。

				*'mousehide'* *'mh'* *'nomousehide'* *'nomh'*
'mousehide' 'mh'	布尔	(默认开启)
			全局
			仅在 GUI 中
	开启时，键入字符时隐藏鼠标指针。移动鼠标时恢复鼠标指针。

						*'mousemodel'* *'mousem'*
'mousemodel' 'mousem'	字符串	(默认 "popup_setpos")
			全局
	设置用于鼠标的模型。该名称主要指定右键的用途：
	   extend	右键扩展选择。这类似于 xterm 中的工作方式。
	   popup	右键弹出菜单。Shift+左键扩展选择。这类似于 Microsoft Windows
			中的工作方式。
	   popup_setpos 类似于 "popup"，但光标会移动到鼠标单击的位置，因此所选操作将
			作用于单击的对象。如果在选择内部单击，则作用于该选择，即不移动光标。
			当然，这意味着在选区外单击右键将结束可视模式。
	每种模型下按钮的功能概览：
	mouse		    extend		popup(_setpos) ~
	左键单击	    放置光标		放置光标
	左键拖动	    开始选择		开始选择
	Shift+左键	    搜索单词		扩展选择
	右键单击	    扩展选择		弹出菜单（放置光标）
	右键拖动	    扩展选择		-
	中键单击	    粘贴		粘贴

	在 "popup" 模型中，右键会产生一个弹出菜单。Nvim 创建了一个默认的 |popup-menu|，
	但你可以重新定义它。

	请注意，你可以通过映射进一步细化按钮的含义。参见 |mouse-overview|。但映射不用于
	非模式选择。

	示例： >vim
	    map <S-LeftMouse>     <RightMouse>
	    map <S-LeftDrag>      <RightDrag>
	    map <S-LeftRelease>   <RightRelease>
	    map <2-S-LeftMouse>   <2-RightMouse>
	    map <2-S-LeftDrag>    <2-RightDrag>
	    map <2-S-LeftRelease> <2-RightRelease>
	    map <3-S-LeftMouse>   <3-RightMouse>
	    map <3-S-LeftDrag>    <3-RightDrag>
	    map <3-S-LeftRelease> <3-RightRelease>
	    map <4-S-LeftMouse>   <4-RightMouse>
	    map <4-S-LeftDrag>    <4-RightDrag>
	    map <4-S-LeftRelease> <4-RightRelease>
<
	需要 CTRL 修饰符的鼠标命令可以通过在使用鼠标前键入 "g" 键来模拟：
	    "g<LeftMouse>"  是 "<C-LeftMouse>	（跳转到鼠标单击下的标签）
	    "g<RightMouse>" 是 "<C-RightMouse>	（"CTRL-T"）

*'mousemoveevent'* *'mousemev'* *'nomousemoveevent'* *'nomousemev'* *mouse-hover*
'mousemoveevent' 'mousemev'	布尔	(默认关闭)
			全局
	开启时，鼠标移动事件被传递到输入队列，并可用于映射 |<MouseMove>|。默认关闭，
	避免鼠标移动开销，除非需要。
	警告：设置此选项可能会导致在移动鼠标时挂起的映射被中止。

						*'mousescroll'* *E5080*
'mousescroll'		字符串	(默认 "ver:3,hor:6")
			全局
	此选项控制使用鼠标滚轮滚动时滚动的行数/列数（|scroll-mouse-wheel|）。该选项是
	一个逗号分隔的列表。每个部分由方向和计数组成，如下所示：
		direction:count,direction:count
	方向是 "hor" 或 "ver" 之一。"hor" 控制水平滚动，"ver" 控制垂直滚动。计数设置
	给定方向的滚动量，应为非负整数。每个方向最多应设置一次。如果省略某个方向，
	则使用默认值（水平滚动为 6，垂直滚动为 3）。你可以使用计数 0 来禁用鼠标滚动。

	示例： >vim
		set mousescroll=ver:5,hor:2
<	这将使 Nvim 在垂直滚动时每次滚动 5 行，在水平滚动时每次滚动 2 列。

						*'mousetime'* *'mouset'*
'mousetime' 'mouset'	数字	(默认 500)
			全局
	定义两次鼠标单击之间被视为多次单击的最大时间间隔（毫秒）。

						*'nrformats'* *'nf'*
'nrformats' 'nf'	字符串	(默认 "bin,hex")
			本地到缓冲区
	这定义了在使用 CTRL-A 和 CTRL-X 命令分别对数字进行加法和减法时，Vim 将考虑的
	数字进制；有关这些命令的更多信息，请参见 |CTRL-A|。
	alpha	如果包含，单个字母字符将被递增或递减。这对于带有字母索引 a)、b) 等的列表
		很有用。						*octal-nrformats*
	octal	如果包含，以零开头的数字将被视为八进制。示例：在 "007" 上使用 CTRL-A
		结果为 "010"。
	hex	如果包含，以 "0x" 或 "0X" 开头的数字将被视为十六进制。示例：在 "0x100"
		上使用 CTRL-X 结果为 "0x0ff"。
	bin	如果包含，以 "0b" 或 "0B" 开头的数字将被视为二进制。示例：在 "0b1000"
		上使用 CTRL-X 减一，结果为 "0b0111"。
	unsigned 如果包含，数字被视为无符号。因此，前导破折号或负号不会被视作数字的一部分。
		示例：
		    在 "9-2020" 中的 "2020" 上使用 CTRL-X 结果为 "9-2019"
		    （没有 "unsigned" 时会变成 "9-2021"）。
		    在 "9-2020" 中的 "2020" 上使用 CTRL-A 结果为 "9-2021"
		    （没有 "unsigned" 时会变成 "9-2019"）。
		    在 "0" 上使用 CTRL-X 或在 "18446744073709551615"（2^64 - 1）上使用
		    CTRL-A 无效，防止溢出。
	blank	如果包含，根据前面的空白字符将数字视为有符号或无符号。如果带有前导破折号的
		数字，其破折号前面紧邻非空白字符（即不是制表符或空格），则负号不会被视作
		数字的一部分。例如：
		    在 "Carbon-14" 中的 "14" 上使用 CTRL-A 结果为 "Carbon-15"
		    （没有 "blank" 时会变成 "Carbon-13"）。
		    在 "Carbon -8" 中的 "8" 上使用 CTRL-X 结果为 "Carbon -9"
		    （因为 -8 前面有空白字符。如果设置了 "unsigned"，结果将是
		    "Carbon -7"）。
		如果包含此格式，则会像设置了 "unsigned" 一样防止溢出。如果同时包含此格式
		和 "unsigned"，"unsigned" 优先。

	仅以 1-9 范围内的数字开头的数字始终被视为十进制。对于未被识别为八进制或十六进制的
	数字也是如此。

				*'number'* *'nu'* *'nonumber'* *'nonu'*
'number' 'nu'		布尔	(默认关闭)
			本地到窗口
	在每行前面打印行号。当 'cpoptions' 中不包含 'n' 选项时，换行行将不会使用行号列。
	使用 'numberwidth' 选项调整行号的空间。当一个长换行行不以第一个字符开始时，数字前
	会显示 '-' 字符。
	有关高亮显示，请参见 |hl-LineNr|、|hl-CursorLineNr| 和 |:sign-define| 的 "numhl"
	参数。						*number_relativenumber*
	'relativenumber' 选项更改显示的数字，使其相对于光标。与 'number' 一起使用时，有四种
	组合（光标在第 3 行）：

		'nonu'          'nu'            'nonu'          'nu'
		'nornu'         'nornu'         'rnu'           'rnu'
	>
	    |apple          |  1 apple      |  2 apple      |  2 apple
	    |pear           |  2 pear       |  1 pear       |  1 pear
	    |nobody         |  3 nobody     |  0 nobody     |3   nobody
	    |there          |  4 there      |  1 there      |  1 there
<

						*'numberwidth'* *'nuw'*
'numberwidth' 'nuw'	数字	(默认 4)
			本地到窗口
	用于行号的最小列数。仅在设置了 'number' 或 'relativenumber' 选项或打印带有行号
	的行时相关。由于数字和文本之间总是有一个空格，因此数字本身少一个字符。
	该值是最小宽度。当需要适应缓冲区中最大行号或窗口中的行数时（取决于设置的是
	'number' 还是 'relativenumber'），会使用更大的宽度。因此，在 Vim 默认值为 4 的
	情况下，行号最多可容纳 999。当缓冲区有 1000 行时，将使用五列。最小值为 1，
	最大值为 20。

						*'omnifunc'* *'ofu'*
'omnifunc' 'ofu'	字符串	(默认 "")
			本地到缓冲区
	此选项指定一个函数，用于使用 CTRL-X CTRL-O 进行插入模式全能补全。|i_CTRL-X_CTRL-O|
	有关如何调用函数以及它应返回什么的说明，请参阅 |complete-functions|。该值可以是
	函数名、|lambda| 或 |Funcref|。更多信息请参见 |option-value-function|。
	此选项通常由文件类型插件设置：|:filetype-plugin-on|
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'operatorfunc'* *'opfunc'*
'operatorfunc' 'opfunc'	字符串	(默认 "")
			全局
	此选项指定一个函数，由 |g@| 操作符调用。有关更多信息和示例，请参见 |:map-operator|。
	该值可以是函数名、|lambda| 或 |Funcref|。更多信息请参见 |option-value-function|。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'packpath'* *'pp'*
'packpath' 'pp'		字符串	(默认参见 'runtimepath')
			全局
	用于查找包的目录。参见 |packages| 和 |packages-runtimepath|。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'paragraphs'* *'para'*
'paragraphs' 'para'	字符串	(默认 "IPLPPPQPP TPHPLIPpLpItpplpipbp")
			全局
	指定分隔段落的 nroff 宏。这些是两两成对的字母（参见 |object-motions|）。

						*'patchexpr'* *'pex'*
'patchexpr' 'pex'	字符串	(默认 "")
			全局
	评估用于将补丁应用于文件并生成文件新版本的表达式。参见 |diff-patchexpr|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'patchmode'* *'pm'* *E205* *E206*
'patchmode' 'pm'	字符串	(默认 "")
			全局
	当非空时，保留文件的最旧版本。如果你在更改源代码发行版中的文件，这可用于保留
	文件的原始版本。仅在首次写入文件时保留原始文件的副本。副本的名称是原始文件名
	加上 'patchmode' 选项中的字符串。此选项应以点开头。使用类似 ".orig" 或 ".org"
	的字符串。为此功能正常工作，'backupdir' 不能为空（详情：备份文件会在新文件成功
	写入后重命名为 patchmode 文件，因此必须能够写入备份文件）。如果没有要备份的文件，
	则会创建一个空文件。
	当 'backupskip' 模式匹配时，不会创建 patchmode 文件。
	对压缩文件使用 'patchmode' 会在末尾添加扩展名（例如 "file.gz.orig"），因此生成的
	名称并不总是被识别为压缩文件。
	只能使用普通文件名字符，`/\*?[|<>` 是非法的。

				*'path'* *'pa'* *E343* *E345* *E347* *E854*
'path' 'pa'		字符串	(默认 ".,,")
			全局或本地到缓冲区 |global-local|
	这是一个目录列表，当使用 |gf|、[f、]f、^Wf、|:find|、|:sfind|、|:tabfind| 和其他
	命令时，如果正在搜索的文件具有相对路径（不以 "/"、"./" 或 "../" 开头），则会搜索
	这些目录。'path' 选项中的目录可以是相对路径或绝对路径。
	- 使用逗号分隔目录名称： >vim
		set path=.,/usr/local/include,/usr/include
<	- 也可以使用空格分隔目录名称。要在目录名称中包含空格，请在前面添加一个额外的反斜杠，
	  并转义空格： >vim
		set path=.,/dir/with\\\ space
<	- 要在目录名称中包含逗号，请在前面添加一个额外的反斜杠： >vim
		set path=.,/dir/with\\,comma
<	- 要相对于当前文件的目录进行搜索，请使用： >vim
		set path=.
<	- 要在当前目录中搜索，请在两个逗号之间使用空字符串： >vim
		set path=,,
<	- 目录名称可以以 ':' 或 '/' 结尾。
	- 环境变量会被展开 |:set_env|。
	- 使用 |netrw| 时可以使用 URL。例如，添加 "https://www.vim.org" 将使 ":find index.html"
	  正常工作。
	- 使用 "*"、"**" 和 ";" 在目录树中向上和向下搜索。有关信息和语法，请参见
	  |file-searching|。
	- 小心 '\' 字符，在选项中键入两个才能得到一个： >vim
		set path=.,c:\\include
<	  或者改用 '/'： >vim
		set path=.,c:/include
<	不要忘记 "."，否则甚至无法在与文件相同的目录中找到文件！
	最大长度有限。具体长度取决于系统，通常是 256 或 1024 个字符。
	你可以使用 'path' 的值检查是否找到了所有包含文件，参见 |:checkpath|。
	在列表中添加或删除目录时，最好使用 |:set+=| 和 |:set-=|。这可以避免将来版本使用
	其他默认值时出现的问题。要删除当前目录，请使用： >vim
		set path-=
<	要添加当前目录，请使用： >vim
		set path+=
<	要使用环境变量，你可能需要替换分隔符。下面是一个追加 $INCL 的示例，其中目录名称
	用分号分隔： >vim
		let &path = &path .. "," .. substitute($INCL, ';', ',', 'g')
<	将 ';' 替换为 ':' 或使用的任何分隔符。注意，当 $INCL 包含逗号或空格时，此方法无效。

		*'preserveindent'* *'pi'* *'nopreserveindent'* *'nopi'*
'preserveindent' 'pi'	布尔	(默认关闭)
			本地到缓冲区
	更改当前行的缩进时，尽可能保留缩进结构。通常，缩进会被替换为一系列制表符后跟
	所需的空格（除非启用了 'expandtab'，在这种情况下只使用空格）。启用此选项意味着
	缩进将尽可能保留现有字符用于缩进，并且仅在需要时添加额外的制表符或空格。
	'expandtab' 不适用于保留的空白字符，制表符仍然是制表符。
	注意：多次使用 ">>" 时，结果缩进是制表符和空格的混合。你可能不喜欢这样。
	另请参见 'copyindent'。
	使用 |:retab| 清理空白字符。

						*'previewheight'* *'pvh'*
'previewheight' 'pvh'	数字	(默认 12)
			全局
	预览窗口的默认高度。用于 |:ptag| 及相关命令。在未给出计数时用于 |CTRL-W_}|。

		*'previewwindow'* *'pvw'* *'nopreviewwindow'* *'nopvw'* *E590*
'previewwindow' 'pvw'	布尔	(默认关闭)
			本地到窗口  |local-noglobal|
	标识预览窗口。只能有一个窗口设置此选项。通常不直接设置，而是通过使用 |:ptag|、
	|:pedit| 等命令之一来设置。

						*'pumblend'* *'pb'*
'pumblend' 'pb'		数字	(默认 0)
			全局
	为 |popup-menu| 启用伪透明效果。有效值范围为 0（完全不透明，禁用）到 100
	（完全透明背景）。0-30 之间的值通常最有用。

	可以使用 |highlight-blend| 覆盖弹出菜单中单个高亮组的透明度级别。例如，要启用
	透明度但强制当前选中元素完全不透明： >vim

		set pumblend=15
		hi PmenuSel blend=0
<
	依赖于 UI。与 RGB 颜色配合使用效果最佳。'termguicolors'

							*'pumborder'*
'pumborder'		字符串	(默认 "")
			全局
	定义弹出菜单窗口的默认边框样式。有效值请参见 'winborder'。|hl-PmenuBorder| 用于
	高亮边框，当样式为 "shadow" 时，使用 |hl-PmenuShadow| 和 |hl-PmenuShadowThrough|
	高亮组。

	此选项也适用于当 'mousemodel' 设置为 "popup" 或 "popup_setpos" 时的鼠标弹出菜单，
	这些菜单将使用相同的样式显示边框。

						*'pumheight'* *'ph'*
'pumheight' 'ph'	数字	(默认 0)
			全局
	弹出菜单中显示的最大项目数（|ins-completion-menu|）。零表示 "使用可用的屏幕空间"。

						*'pummaxwidth'* *'pmw'*
'pummaxwidth' 'pmw'	数字	(默认 0)
			全局
	弹出菜单的最大宽度（|ins-completion-menu|）。为零时，没有最大宽度限制，否则弹出菜单
	永远不会宽于此值。截断的文本将通过 'fillchars' 选项的 "trunc" 值指示。

	此选项优先于 'pumwidth'。

						*'pumwidth'* *'pw'*
'pumwidth' 'pw'		数字	(默认 15)
			全局
	弹出菜单的最小宽度（|ins-completion-menu|）。如果光标列 + 'pumwidth' 超过屏幕宽度，
	弹出菜单会被调整以适合屏幕。

						*'pyxversion'* *'pyx'*
'pyxversion' 'pyx'	数字	(默认 3)
			全局
	指定用于 pyx* 函数和命令的 Python 版本 |python_x|。由于仅支持 Python 3，此值始终为
	`3`。设置任何其他值都是错误的。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

					*'quickfixtextfunc'* *'qftf'*
'quickfixtextfunc' 'qftf'	字符串	(默认 "")
			全局
	此选项指定一个函数，用于获取在 quickfix 和位置列表窗口中显示的文本。这可用于
	自定义相应 quickfix 或位置列表中每个条目在 quickfix 或位置窗口中显示的信息。
	有关如何编写函数以及示例的说明，请参见 |quickfix-window-function|。该值可以是
	函数名、|lambda| 或 |Funcref|。更多信息请参见 |option-value-function|。

	评估 'qftf' 时不允许更改文本或跳转到另一个窗口 |textlock|。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'quoteescape'* *'qe'*
'quoteescape' 'qe'	字符串	(默认 "\")
			本地到缓冲区
	用于转义字符串中引号的字符。用于诸如 a'、a" 和 a` 之类的对象 |a'|。
	当在此选项中的一个字符在字符串内找到时，其后的字符将被跳过。默认值使得文本
	"foo\"bar\\" 被视为一个字符串。

				*'readonly'* *'ro'* *'noreadonly'* *'noro'*
'readonly' 'ro'		布尔	(默认关闭)
			本地到缓冲区  |local-noglobal|
	如果开启，除非使用 '!'，否则写入失败。保护你免受意外覆盖文件的影响。当 Vim 以只读
	模式启动（"vim -R"）或可执行文件名为 "view" 时，默认开启。使用 ":w!" 时，除非
	'cpoptions' 中包含 'Z' 标志，否则当前缓冲区的 'readonly' 选项会被重置。
	使用 ":view" 命令时，新编辑缓冲区的 'readonly' 选项会被设置。
	有关禁止更改缓冲区的内容，请参见 'modifiable'。

						*'redrawdebug'* *'rdb'*
'redrawdebug' 'rdb'	字符串	(默认 "")
			全局
	用于调试目的，更改重绘方式的标志。与将 'writedelay' 设置为某个合理值一起使用
	最有效。支持以下标志：
	    compositor	通过短暂闪烁重绘区域（用颜色指示重绘类型）来指示合成器处理的每个
			重绘事件。这些是使用的高亮组（及其默认颜色）：
		RedrawDebugNormal   gui=reverse   传递的普通重绘
		RedrawDebugClear    guibg=Yellow  传递的清除事件
		RedrawDebugComposed guibg=Green   合成器修改的重绘事件（由于重叠网格等）
		RedrawDebugRecompose guibg=Red    合成器本身生成的重绘，由于网格被移动或删除。
	    line	在屏幕上绘制的每一行后引入延迟。当使用 TUI 或其他单网格 UI 时，
			"compositor" 提供更多信息，应优先使用（合成器将每一行作为单独事件处理）
	    flush	在每个 "flush" 事件后引入延迟。
	    nothrottle	关闭消息网格的节流。这是一种优化，当绘制消息区域时（'display' msgsep
			标志激活），将许多小滚动合并为一次较大的滚动。
	    invalid	启用对内部屏幕状态不一致的更严格检查（中止）。主要在调试器内运行 nvim
			（以及测试套件）时有用。
	    nodelta	将所有内部重绘的单元格发送到 UI，即使它们与已显示的状态相比没有变化。

						*'redrawtime'* *'rdt'*
'redrawtime' 'rdt'	数字	(默认 2000)
			全局
	重绘显示的时间（毫秒）。适用于 'hlsearch'、'inccommand'、|:match| 高亮显示、
	语法高亮显示和异步 |LanguageTree:parse()|。
	当重绘时间超过此毫秒数时，将不再高亮显示更多匹配项。
	对于语法高亮显示，时间适用于每个窗口。当超过限制时，语法高亮显示将被禁用，直到使用
	|CTRL-L|。这是为了避免 Vim 在使用非常复杂的模式时挂起。

						*'regexpengine'* *'re'*
'regexpengine' 're'	数字	(默认 0)
			全局
	选择默认的正则表达式引擎。|two-engines|
	可能的值有：
		0	自动选择
		1	旧引擎
		2	NFA 引擎
	注意，当使用 NFA 引擎且模式包含不支持的内容时，模式将不匹配。这仅对调试正则
	表达式引擎有用。
	使用自动选择使 Vim 能够在默认引擎成本过高时切换引擎。例如，当 NFA 引擎使用过多
	状态时。这应该可以防止 Vim 在复杂模式与长文本的组合上挂起。

		*'relativenumber'* *'rnu'* *'norelativenumber'* *'nornu'*
'relativenumber' 'rnu'	布尔	(默认关闭)
			本地到窗口
	在每行前面显示相对于光标所在行的行号。相对行号有助于你在某些垂直移动命令
	（例如 j k + -）前使用 |count|，而无需自己计算。尤其与其他命令（例如 y d c < > gq gw =）
	结合使用时非常有用。
	当 'cpoptions' 中不包含 'n' 选项时，换行行将不会使用行号列。
	可以使用 'numberwidth' 选项设置行号使用的空间。当一个长换行行不以第一个字符开始时，
	数字前会显示 '-' 字符。
	有关用于数字的高亮显示，请参见 |hl-LineNr| 和 |hl-CursorLineNr|。

	光标行前面的数字也取决于 'number' 的值，参见 |number_relativenumber| 了解这两个
	选项的所有组合。

							*'report'*
'report'		数字	(默认 2)
			全局
	报告更改行数的阈值。当更改的行数超过 'report' 时，大多数 ":" 命令会显示一条消息。
	如果你希望始终显示，请将 'report' 设置为 0。
	对于 ":substitute" 命令，使用替换次数而不是行数。

				*'revins'* *'ri'* *'norevins'* *'nori'*
'revins' 'ri'		布尔	(默认关闭)
			全局
	在插入模式下插入字符将反向工作。参见 "反向输入" |ins-reverse|。当设置了
	'allowrevins' 时，可以在插入模式下使用 CTRL-_ 命令切换此选项。

				*'rightleft'* *'rl'* *'norightleft'* *'norl'*
'rightleft' 'rl'	布尔	(默认关闭)
			本地到窗口
	开启时，显示方向变为从右到左，即存储在文件中的字符从右到左出现。使用此选项，
	可以编辑从右到左书写的语言（如希伯来语和阿拉伯语）的文件。此选项是窗口本地的，
	因此可以同时编辑混合文件，或以两种方式查看同一个文件（当你有一个混合文本文件，
	其中包含从右到左和从左到右的字符串，以便在不同的窗口中正确显示两组字符串时，
	这很有用）。另请参见 |rileft.txt|。

						*'rightleftcmd'* *'rlc'*
'rightleftcmd' 'rlc'	字符串	(默认 "search")
			本地到窗口
	此选项中的每个单词都会为一组命令启用从右到左模式的命令行编辑：

		search		"/" 和 "?" 命令

	这对于希伯来语、阿拉伯语和波斯语等语言很有用。必须设置 'rightleft' 选项才能使
	'rightleftcmd' 生效。

					*'ruler'* *'ru'* *'noruler'* *'noru'*
'ruler' 'ru'		布尔	(默认开启)
			全局
	显示光标位置的行号和列号，用逗号分隔。当有空间时，显示文本在文件中的相对位置
	会显示在最右边：
		Top	第一行可见
		Bot	最后一行可见
		All	第一行和最后一行都可见
		45%	在文件中的相对位置
	如果设置了 'rulerformat'，它将决定标尺的内容。每个窗口都有自己的标尺。如果窗口
	有状态行，标尺会显示在那里。如果窗口没有状态行且 'cmdheight' 为零，则不显示标尺。
	否则，标尺会显示在屏幕的最后一行。如果状态行由 'statusline'（即非空）给出，
	此选项优先于 'ruler' 和 'rulerformat'。
	如果显示的字符数与文本中的字节数不同（例如，对于制表符或多字节字符），则会显示
	文本列（字节数）和屏幕列，用破折号分隔。
	对于空行，显示 "0-1"。
	对于空缓冲区，行号也将为零："0,0-1"。
	如果你不想一直看到标尺，但想知道自己的位置，请使用 "g CTRL-G" |g_CTRL-G|。

						*'rulerformat'* *'ruf'*
'rulerformat' 'ruf'	字符串	(默认 "")
			全局
	当此选项非空时，它决定标尺字符串的内容，如 'ruler' 选项所示。此选项的格式类似于
	'statusline'。当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

	默认标尺宽度为 17 个字符。要使标尺宽度为 15 个字符，请在开头放置 "%15("，在结尾
	放置 "%)"。示例： >vim
		set rulerformat=%15(%c%V\ %p%%%)
<

						*'runtimepath'* *'rtp'* *vimfiles*
'runtimepath' 'rtp'	字符串	(默认 "$XDG_CONFIG_HOME/nvim,
                                               $XDG_CONFIG_DIRS[1]/nvim,
                                               $XDG_CONFIG_DIRS[2]/nvim,
                                               …
                                               $XDG_DATA_HOME/nvim[-data]/site,
                                               $XDG_DATA_DIRS[1]/nvim/site,
                                               $XDG_DATA_DIRS[2]/nvim/site,
                                               …
                                               $VIMRUNTIME,
                                               …
                                               $XDG_DATA_DIRS[2]/nvim/site/after,
                                               $XDG_DATA_DIRS[1]/nvim/site/after,
                                               $XDG_DATA_HOME/nvim[-data]/site/after,
                                               …
                                               $XDG_CONFIG_DIRS[2]/nvim/after,
                                               $XDG_CONFIG_DIRS[1]/nvim/after,
                                               $XDG_CONFIG_HOME/nvim/after")
			全局
	要搜索以下运行时文件的目录列表：
	  filetype.lua	文件类型 |new-filetype|
	  autoload/	自动加载的脚本 |autoload-functions|
	  colors/	颜色方案文件 |:colorscheme|
	  compiler/	编译器文件 |:compiler|
	  doc/		文档 |write-local-help|
	  ftplugin/	文件类型插件 |write-filetype-plugin|
	  indent/	缩进脚本 |indent-expression|
	  keymap/	键映射文件 |mbyte-keymap|
	  lang/		菜单翻译 |:menutrans|
	  lsp/		LSP 客户端配置 |lsp-config|
	  lua/		|Lua| 插件
	  menu.vim	GUI 菜单 |menu.vim|
	  pack/		包 |:packadd|
	  parser/	|treesitter| 语法解析器
	  plugin/	插件脚本 |write-plugin|
	  queries/	|treesitter| 查询
	  rplugin/	|remote-plugin| 脚本
	  spell/	拼写检查文件 |spell|
	  syntax/	语法文件 |mysyntaxfile|
	  tutor/	教程文件 |:Tutor|

	以及使用 |:runtime| 命令搜索的任何其他文件。

	默认设置搜索以下位置：
	1. 你的主目录，用于个人偏好。由 `stdpath("config")` 给出。|$XDG_CONFIG_HOME|
	2. 根据 |xdg| 必须包含配置文件的目录（$XDG_CONFIG_DIRS，默认为 /etc/xdg）。
	   这也包含系统管理员的偏好。
	3. 数据主目录，用于用户安装的插件。由 `stdpath("data")/site` 给出。|$XDG_DATA_HOME|
	4. $XDG_DATA_DIRS 中每个目录的 nvim/site 子目录。这是给由系统管理员安装但不是
	   Nvim 发行版一部分的插件使用的。XDG_DATA_DIRS 默认为 /usr/local/share/:/usr/share/，
	   因此系统管理员应将站点插件安装到 /usr/share/nvim/site。
	5. $VIMRUNTIME，用于 Nvim 分发的文件。
							*after-directory*
	6, 7, 8, 9. 在 1、2、3 和 4 的 after/ 子目录中，顺序相反。这用于覆盖或添加到
	   发行版默认设置或系统范围设置的偏好（很少需要）。

							*packages-runtimepath*
	"start" 包也会在这些之后被搜索（|runtime-search-path|）以查找运行时文件，
	尽管这些包没有显式地列在 &runtimepath 中。但 "opt" 包会通过 |:packadd| 显式地
	添加到 &runtimepath。

	请注意，与 'path' 不同，不允许使用 "**" 等通配符。允许使用普通通配符，但可能会
	显著减慢运行时文件的搜索速度。为了速度，请使用尽可能少的项目并避免使用通配符。
	参见 |:runtime|。
	示例： >vim
		set runtimepath=~/vimruntime,/mygroup/vim,$VIMRUNTIME
<	这将首先使用目录 "~/vimruntime"（包含你的个人 Nvim 运行时文件），然后使用
	"/mygroup/vim"，最后使用 "$VIMRUNTIME"（默认运行时文件）。
	你可以将一个目录放在 $VIMRUNTIME 之前，以查找替换发行版运行时文件的文件。
	你可以将一个目录放在 $VIMRUNTIME 之后，以查找添加到发行版运行时文件的文件。

	使用 |--clean| 时，不包含主目录条目。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'scroll'* *'scr'*
'scroll' 'scr'		数字	(默认窗口高度的一半)
			本地到窗口  |local-noglobal|
	使用 CTRL-U 和 CTRL-D 命令滚动的行数。当窗口大小改变时，它会被设置为窗口行数的一半。
	这可能在设置 'scroll' 选项后启用 |status-line| 或 'tabline' 选项时发生。
	如果你给 CTRL-U 或 CTRL-D 命令一个计数，它将被用作 'scroll' 的新值。使用
	":set scroll=0" 重置为窗口高度的一半。

						*'scrollback'* *'scbk'*
'scrollback' 'scbk'	数字	(默认 10000)
			本地到缓冲区
	保留在可视屏幕之外的最大行数。如果新行超过此限制，顶部的行将被删除。
	最小值为 1，最大值为 1000000。
	仅在 |terminal| 缓冲区中。

	注意：不可见且保留在回滚中的行在终端缓冲区水平调整大小时不会重新排版。

			*'scrollbind'* *'scb'* *'noscrollbind'* *'noscb'*
'scrollbind' 'scb'	布尔	(默认关闭)
			本地到窗口
	另请参见 |scroll-binding|。设置此选项后，滚动当前窗口也会滚动其他滚动绑定窗口
	（也设置了此选项的窗口）。此选项对于查看两个版本文件之间的差异很有用，参见 'diff'。
	参见 'scrollopt' 以了解决定如何解释此选项的选项。
	分割窗口以编辑另一个文件时，此选项大多会被重置。这意味着 ":split | edit file"
	会产生两个滚动绑定的窗口，而 ":split file" 则不会。

						*'scrolljump'* *'sj'*
'scrolljump' 'sj'	数字	(默认 1)
			全局
	当光标离开屏幕时（例如，使用 "j"）滚动的最小行数。不用于滚动命令（例如，CTRL-E、
	CTRL-D）。如果你的终端滚动非常慢，此选项很有用。
	当设置为 -1 到 -100 之间的负数时，将其用作窗口高度的百分比。因此 -50 滚动半个窗口
	高度。

						*'scrolloff'* *'so'*
'scrolloff' 'so'	数字	(默认 0)
			全局或本地到窗口 |global-local|
	光标上方和下方保持的最小屏幕行数。这会使你工作的地方周围有一些上下文可见。
	如果你将其设置为一个非常大的值（999），光标行将始终位于窗口中间（除非在文件开头
	或结尾，或者长行换行时）。
	使用本地值后，可以通过以下两种方式之一返回全局值： >vim
		setlocal scrolloff<
		setlocal scrolloff=-1
<	有关水平滚动，请参见 'sidescrolloff'。

						*'scrollopt'* *'sbo'*
'scrollopt' 'sbo'	字符串	(默认 "ver,jump")
			全局
	这是一个逗号分隔的单词列表，指定 'scrollbind' 窗口应如何表现。'sbo' 代表
	ScrollBind Options（滚动绑定选项）。
	以下单词可用：
	    ver		绑定 'scrollbind' 窗口的垂直滚动
	    hor		绑定 'scrollbind' 窗口的水平滚动
	    jump	适用于两个窗口之间垂直滚动的偏移量。此偏移量是绑定窗口的
			第一个显示行的差值。在一个窗口中移动时，另一个 'scrollbind'
			窗口可能会到达缓冲区开始之前或结束之后的位置。然而偏移量不会
			改变，当移回时，'scrollbind' 窗口将尽可能尝试滚动到所需位置。
			当现在将该窗口设为当前窗口时，可以对相对偏移量做两件事：
			1. 当不包含 "jump" 时，相对偏移量会根据新当前窗口的滚动位置
			   进行调整。当返回到另一个窗口时，将使用新的相对偏移量。
			2. 当包含 "jump" 时，其他窗口会滚动以保持相同的相对偏移量。
			   当返回到另一个窗口时，它仍然使用相同的相对偏移量。
	另请参见 |scroll-binding|。
	当 'diff' 模式激活时，总是存在垂直滚动绑定，即使 "ver" 不存在。

						*'sections'* *'sect'*
'sections' 'sect'	字符串	(默认 "SHNHH HUnhsh")
			全局
	指定分隔章节的 nroff 宏。这些是两两成对的字母（参见 |object-motions|）。默认值
	使得章节在 nroff 宏 ".SH"、".NH"、".H"、".HU"、".nh" 和 ".sh" 处开始。

						*'selection'* *'sel'*
'selection' 'sel'	字符串	(默认 "inclusive")
			全局
	此选项定义选择的行为。仅在可视模式和选择模式下使用。
	可能的值：
	   值		超越行尾	   包含性 ~
	   old		   否		   是
	   inclusive	   是		   是
	   exclusive	   是		   否
	"超越行尾" 意味着允许光标定位在行尾之后一个字符的位置。
	"包含性" 意味着选择的最后一个字符包含在操作中。例如，当使用 "x" 删除选择时。
	当使用 "old" 且 'virtualedit' 允许光标移动到行尾之后时，换行符仍不会被包含。
	当使用 "exclusive" 时，可视模式下的光标位置将针对包含性移动进行调整
	|inclusive-motion-selection-exclusive|。

	注意：
	- 当使用 "exclusive" 且从末尾向前选择时，如果从普通模式开始且 'virtualedit' 为空，
	  你无法包含一行的最后一个字符。
	- 当 "exclusive" 与单个字符的可视选择一起使用时，Vim 的行为将如同 'selection' 是
	  "inclusive"（换句话说，你无法可视地选择一个空区域）。

						*'selectmode'* *'slm'*
'selectmode' 'slm'	字符串	(默认 "")
			全局
	这是一个逗号分隔的单词列表，指定在选择开始时，何时启动选择模式而不是可视模式。
	可能的值：
	   mouse	使用鼠标时
	   key		使用按下的特殊键时
	   cmd		使用 "v"、"V" 或 CTRL-V 时
	参见 |Select-mode|。

					*'sessionoptions'* *'ssop'*
'sessionoptions' 'ssop'	字符串	(默认 "blank,buffers,curdir,folds,help,tabpages,winsize,terminal")
			全局
	更改 |:mksession| 命令的效果。它是一个逗号分隔的单词列表。每个单词启用保存和恢复
	某些内容：
	   单词		保存和恢复 ~
	   blank		空窗口
	   buffers		隐藏和未加载的缓冲区，而不仅仅是窗口中的那些
	   curdir		当前目录
	   folds		手动创建的折叠、打开/关闭的折叠和本地折叠选项
	   globals		以大写字母开头且至少包含一个小写字母的全局变量。
				只存储字符串和数字类型。
	   help			帮助窗口
	   localoptions		窗口或缓冲区本地的选项和映射（不是本地选项的全局值）
	   options		所有选项和映射（也包括本地选项的全局值）
	   skiprtp		从选项中排除 'runtimepath' 和 'packpath'
	   resize		Vim 窗口的大小：'lines' 和 'columns'
	   sesdir		会话文件所在的目录将成为当前目录（对于通过网络从不同
				系统访问的项目很有用）
	   tabpages		所有标签页；没有此项则只恢复当前标签页，这样你可以为每个
				标签页单独创建一个会话
	   terminal		包含可以恢复命令的终端窗口
	   winpos		整个 Vim 窗口的位置
	   winsize		窗口大小
	   slash		|deprecated| 始终启用。在文件名中使用 "/"。
	   unix		|deprecated| 始终启用。使用 "\n" 行尾。

	不要同时包含 "curdir" 和 "sesdir"。当两者都不包含时，文件名存储为绝对路径。
	如果你省略了 "options"，许多内容在恢复会话后将无法正常工作。

					*'shada'* *'sd'* *E526* *E527* *E528*
'shada' 'sd'		字符串	(默认 "!,'100,<50,s10,h,r/tmp/,r/private/")
			全局
	当非空时，在启动时读取 shada 文件，并在退出 Vim 时写入（参见 |shada-file|）。
	该字符串应是一个逗号分隔的参数列表，每个参数由一个标识特定参数的单个字符组成，
	后跟一个指定该参数值的数字或字符串。如果某个特定字符被省略，则对该参数使用默认值。
	以下是标识字符及其值效果的列表。
	CHAR	值	~
							*shada-!*
	!	如果包含，保存和恢复以大写字母开头且不包含小写字母的全局变量。因此，
		"KEEPTHIS" 和 "K_L_M" 被存储，但 "KeepThis" 和 "_K_L_M" 不被存储。
		嵌套的 List 和 Dict 项可能无法正确读回，最终会得到一个空项。
							*shada-quote*
	"	为每个寄存器保存的最大行数。'<' 项的旧名称，缺点是你需要在 " 前加一个
		反斜杠，否则它将被识别为注释的开始！
							*shada-%*
	%	如果包含，保存和恢复缓冲区列表。如果 Vim 以文件名参数启动，则不会恢复
		缓冲区列表。如果 Vim 启动时没有文件名参数，则从 shada 文件恢复缓冲区列表。
		Quickfix（'buftype'）、未列出（'buflisted'）、未命名和可移动介质上的缓冲区
		（|shada-r|）不会被保存。如果后面跟一个数字，该数字指定存储的最大缓冲区数。
		如果没有数字，则存储所有缓冲区。
							*shada-'*
	'	记住其标记的先前编辑文件的最大数量。当 'shada' 非空时，此参数必须始终包含。
		如果非零，则 |jumplist| 和 |changelist| 也会存储在 shada 文件中。
							*shada-/*
	/	要保存的搜索模式历史记录中的最大项数。如果非零，则也会保存先前的搜索和替换
		模式。如果不包含，则使用 'history' 的值。
							*shada-:*
	:	要保存的命令行历史记录中的最大项数。如果不包含，则使用 'history' 的值。
							*shada-<*
	<	为每个寄存器保存的最大行数。如果为零，则不保存寄存器。如果不包含，则保存所有行。
		'"' 是此项的旧名称。另请参见下面的 's' 项：以 KiB 为单位限制。
							*shada-@*
	@	要保存的输入行历史记录中的最大项数。如果不包含，则使用 'history' 的值。
							*shada-c*
	c	虚拟选项，为兼容性保留。没有实际效果：ShaDa 始终使用 UTF-8，且 'encoding' 值
		也固定为 UTF-8。
							*shada-f*
	f	是否需要存储文件标记。如果为零，则不存储文件标记（'0 到 '9、'A 到 'Z）。
		当不存在或非零时，它们都被存储。'0 用于当前光标位置（退出时或执行 |:wshada| 时）。
							*shada-h*
	h	加载 shada 文件时禁用 'hlsearch' 的效果。如果不包含，则取决于自上次搜索命令以来
		是否使用了 ":nohlsearch"。
							*shada-n*
	n	shada 文件的名称。名称必须紧跟在 'n' 之后。必须在选项的末尾！如果设置了
		'shadafile' 选项，则该文件名会覆盖此处通过 'shada' 给出的文件名。打开文件时
		展开环境变量，而不是设置选项时。
							*shada-r*
	r	可移动介质。参数是一个字符串（直到下一个 ','）。此参数可以多次给出。每个参数
		指定一个路径的开头，对于该路径不存储标记。这是为了避免可移动介质。对于 Windows，
		你可以使用 "ra:,rb:"。你也可以将其用于临时文件，例如，对于 Unix："r/tmp"。
		忽略大小写。
							*shada-s*
	s	项目内容的最大大小（以 KiB 为单位）。如果为零，则不保存任何内容。与 Vim 不同，
		这适用于所有项目，除了缓冲区列表和头部。完整项目大小减去三个无符号整数：
		使用 `s10` 时，最大项目大小可能为 1 字节（类型：7 位整数）+ 9 字节
		（时间戳：最多 64 位整数）+ 3 字节（项目大小：最多 16 位整数，因为 2^8 < 10240 < 2^16）
		+ 10240 字节（请求的最大项目内容大小）= 10253 字节。

	示例： >vim
	    set shada='50,<1000,s100,:0,n~/nvim/shada
<
	'50		将为你编辑的最后 50 个文件记住标记。
	<1000		将记住寄存器的内容（每个最多 1000 行）。
	s100		跳过内容占用超过 100 KiB 的项目。
	:0		命令行历史记录将不会被保存。
	n~/nvim/shada	要使用的文件名是 "~/nvim/shada"。
	no /		由于未指定 '/'，将使用默认值，即保存所有搜索历史记录，以及先前的
			搜索和替换模式。
	no %		缓冲区列表将不会被保存或读回。
	no h		'hlsearch' 高亮显示将被恢复。

	当从空值设置 'shada' 时，你可以使用 |:rshada| 加载文件的内容，这不会自动完成。

	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shadafile'* *'sdf'*
'shadafile' 'sdf'	字符串	(默认 "")
			全局
	当非空时，覆盖用于 |shada|（viminfo）的文件名。当等于 "NONE" 时，将不会读取或写入
	shada 文件。此选项可以使用 |-i| 命令行标志设置。|--clean| 命令行标志将其设置为 "NONE"。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shell'* *'sh'* *E91*
'shell' 'sh'		字符串	(默认 $SHELL 或 "sh", Win32: "cmd.exe")
			全局
	用于 ! 和 :! 命令的 shell 名称。更改该值时，还需检查以下选项：'shellpipe'、
	'shellslash'、'shellredir'、'shellquote'、'shellxquote' 和 'shellcmdflag'。
	允许给命令添加参数，例如 "csh -f"。有关包含空格和反斜杠的信息，请参见 |option-backslash|。
	环境变量会被展开 |:set_env|。

	如果 shell 名称包含空格，你需要将其括在引号中。带引号的示例： >vim
		set shell=\"c:\program\ files\unix\sh.exe\"\ -f
<	注意每个引号前的反斜杠（以避免开始注释）和每个空格前的反斜杠（以避免结束选项值），
	因此最好像这样使用 |:let-&|： >vim
		let &shell='"C:\Program Files\unix\sh.exe" -f'
<	另请注意，"-f" 不在引号内，因为它不是命令名的一部分。
							*shell-unquoting*
	有关引号的规则：
	1. 选项在不在引号内的空格和制表符上分割："abc def" 运行名为 "abc" 的 shell，
	   附加参数 "def"；'"abc def"' 运行名为 "abc def" 的 shell，没有附加参数
	   （这里及下文：附加意味着 “附加到 'shellcmdflag'”）。
	2. 选项中的引号可以出现在任何位置和任何数量：'"abc"'、'"a"bc'、'a"b"c'、'ab"c"'
	   和 '"a"b"c"' 都等同于 "abc"。
	3. 在引号内，反斜杠后跟反斜杠表示一个反斜杠。反斜杠后跟引号表示一个引号。
	   反斜杠后跟任何其他内容表示反斜杠和下一个字符字面量：'"a\\b"' 等同于 "a\b"，
	   '"a\\"b"' 运行字面名为 'a"b' 的 shell，'"a\b"' 再次等同于 "a\b"。
	4. 在引号外，反斜杠总是表示它自身，不能用于转义引号：'a\"b"' 等同于 "a\b"。
	注意，这种处理是在 |:set| 完成其自身的取消转义之后进行的，因此为了保持头脑清醒，
	请像上面所示那样使用 |:let-&|。
							*shell-powershell*
	要使用 PowerShell： >vim
	   set noshelltemp
	   let &shell = 'powershell'
	   let &shellcmdflag = '-NoLogo -NoProfile -ExecutionPolicy RemoteSigned -Command '
	   let &shellcmdflag .= '[Console]::InputEncoding=[Console]::OutputEncoding=[System.Text.UTF8Encoding]::new();'
	   let &shellcmdflag .= '$PSDefaultParameterValues[''Out-File:Encoding'']=''utf8'';'
	   let &shellpipe  = '> %s 2>&1'
	   set shellquote= shellxquote=
<
							*shell-pwsh*
	要使用 pwsh，使用上述设置 `let &shell = 'pwsh'`，并添加： >vim
	   let &shellcmdflag .= '$PSStyle.OutputRendering = ''PlainText'';'
	   " 解决方法（可能在 pwsh 的未来版本中不需要）：
	   let $__SuppressAnsiEscapeSequences = 1
<
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shellcmdflag'* *'shcf'*
'shellcmdflag' 'shcf'	字符串	(默认 "-c"; Windows: "/s /c")
			全局
	传递给 shell 以执行 "!" 和 ":!" 命令的标志；例如，`bash.exe -c ls` 或
	`cmd.exe /s /c "dir"`。对于 MS-Windows，默认值根据 'shell' 的值设置，以减少用户
	设置此选项的需要。
	在 Unix 上，它可能有多个标志。每个由空格分隔的部分都作为参数传递给 shell 命令。
	有关包含空格和反斜杠的信息，请参见 |option-backslash|。
	参见 |shell-unquoting|，其中讨论了将此选项分割成多个参数。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shellpipe'* *'sp'*
'shellpipe' 'sp'	字符串	(默认 ">", "| tee", "|& tee" 或 "2>&1| tee")
			全局
	用于将 ":make" 命令的输出放入错误文件的字符串。另请参见 |:make_makeprg|。
	有关包含空格和反斜杠的信息，请参见 |option-backslash|。
	如果需要，临时文件的名称可以用 "%s" 表示（如果此选项的值中没有出现 %s，则文件名会
	自动附加）。
	对于 MS-Windows，默认值是 "2>&1| tee"。标准输出和标准错误被保存在文件中并回显到屏幕。
	对于 Unix，默认值是 "| tee"。编译器的标准输出被保存在文件中并回显到屏幕。如果初始化后
	'shell' 选项是 "csh" 或 "tcsh"，默认值变为 "|& tee"。如果 'shell' 选项是 "sh"、"ksh"、
	"mksh"、"pdksh"、"zsh"、"zsh-beta"、"bash"、"fish"、"ash" 或 "dash"，默认值变为
	"2>&1| tee"。这意味着标准错误也被包含。在使用 'shell' 选项之前会移除路径，因此 "/bin/sh"
	使用 "sh"。
	此选项的初始化是在读取 vimrc 和其他初始化之后完成的，因此当 'shell' 选项在那里设置时，
	'shellpipe' 选项会自动更改，除非它之前被显式设置过。
	当 'shellpipe' 设置为空字符串时，将不会对 ":make" 输出进行重定向。如果你使用的 'makeprg'
	自己写入 'makeef'，这很有用。如果你不想使用管道，但希望包含 'makeef'，请将 'shellpipe'
	设置为单个空格。不要忘记在空格前加一个反斜杠：":set sp=\ "。
	将来，管道可能用于过滤，并且此选项将过时（至少在 Unix 上如此）。
	注意：当使用像 "| tee" 这样的管道时，你将丢失 shell 命令的退出代码。这可以通过你的 shell
	进行配置，查找 pipefail 选项（对于 bash 和 zsh，使用 ":set -o pipefail"）。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shellquote'* *'shq'*
'shellquote' 'shq'	字符串	(默认 ""; Windows, 当 'shell' 某处包含 "sh" 时: "\"")
			全局
	引号字符，放在传递给 shell 的命令周围，用于 "!" 和 ":!" 命令。重定向保持在引号之外。
	参见 'shellxquote' 以包含重定向。同时设置两个选项可能没有用处。
	默认情况下这是一个空字符串。已知仅对 Windows 系统上的第三方 shell 有用，例如 MKS Korn Shell
	或 bash，它应该是 "\""。默认值根据 'shell' 的值进行调整，以减少用户设置此选项的需要。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shellredir'* *'srr'*
'shellredir' 'srr'	字符串	(默认 ">", ">&" 或 ">%s 2>&1")
			全局
	用于将过滤命令的输出放入临时文件的字符串。另请参见 |:!|。有关包含空格和反斜杠的
	信息，请参见 |option-backslash|。
	如果需要，临时文件的名称可以用 "%s" 表示（如果此选项的值中没有出现 %s，则文件名会
	自动附加）。
	默认值是 ">"。对于 Unix，如果在初始化期间 'shell' 选项是 "csh" 或 "tcsh"，则默认值
	变为 ">&"。如果 'shell' 选项是 "sh"、"ksh"、"mksh"、"pdksh"、"zsh"、"zsh-beta"、
	"bash" 或 "fish"，则默认值变为 ">%s 2>&1"。这意味着标准错误也被包含。对于 Win32，
	会执行 Unix 检查，并且额外检查 "cmd"，这使得默认值为 ">%s 2>&1"。此外，还会检查
	带有 ".exe" 后缀的相同名称。
	此选项的初始化是在读取 vimrc 和其他初始化之后完成的，因此当 'shell' 选项在那里设置时，
	'shellredir' 选项会自动更改，除非它之前被显式设置过。
	将来，管道可能用于过滤，并且此选项将过时（至少在 Unix 上如此）。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

			*'shellslash'* *'ssl'* *'noshellslash'* *'nossl'*
'shellslash' 'ssl'	布尔	(默认开启, Windows: 关闭)
			全局
			仅在 MS-Windows 上可修改
	设置后，在展开文件名时使用正斜杠。当使用类 Unix shell 而不是 cmd.exe 时，这很有用。
	仍然可以键入反斜杠，但 Vim 会将它们更改为正斜杠。
	注意，设置或重置此选项对某些现有文件名没有影响，因此为了获得最佳效果，需要在打开
	任何文件之前设置此选项。这可能会在将来改变。
	'shellslash' 仅在反斜杠可以用作路径分隔符时有效。要测试是否如此，请使用： >vim
		if exists('+shellslash')
<	另请参见 'completeslash'。

			*'shelltemp'* *'stmp'* *'noshelltemp'* *'nostmp'*
'shelltemp' 'stmp'	布尔	(默认关闭)
			全局
	开启时，为 shell 命令使用临时文件。关闭时，使用管道。当无法使用管道时，无论如何都会
	使用临时文件。使用管道的优点是没有人可以读取临时文件，并且 'shell' 命令不需要支持重定向。
	使用临时文件的优点是文件类型和编码可以被检测到。
	当 'shelltemp' 关闭时，|FilterReadPre|、|FilterReadPost| 和 |FilterWritePre|、
	|FilterWritePost| 自动命令事件不会被触发。
	|system()| 不遵守此选项，它总是使用管道。

						*'shellxescape'* *'sxe'*
'shellxescape' 'sxe'	字符串	(默认 "")
			全局
	当 'shellxquote' 设置为 "(" 时，此选项中列出的字符将使用 '^' 字符进行转义。
	这使得可以使用 cmd.exe 执行大多数外部命令。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'shellxquote'* *'sxq'*
'shellxquote' 'sxq'	字符串	(默认 "", Windows: "\"")
			全局
	引号字符，放在传递给 shell 的命令周围，用于 "!" 和 ":!" 命令。包括重定向。
	参见 'shellquote' 以排除重定向。同时设置两个选项可能没有用处。
	当值为 '(' 时，附加 ')'。当值为 '"(' 时，附加 ')"'。
	当值为 '(' 时，另请参见 'shellxescape'。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

			*'shiftround'* *'sr'* *'noshiftround'* *'nosr'*
'shiftround' 'sr'	布尔	(默认关闭)
			全局
	将缩进舍入到 'shiftwidth' 的倍数。适用于 > 和 < 命令。插入模式下的 CTRL-T 和 CTRL-D
	总是将缩进舍入到 'shiftwidth' 的倍数（这是 Vi 兼容的）。

						*'shiftwidth'* *'sw'*
'shiftwidth' 'sw'	数字	(默认 8)
			本地到缓冲区
	构成一级（自动）缩进的列数。由 'cindent'、|<<|、|>>| 等使用。
	如果设置为 0，Vim 使用当前的 'tabstop' 值。使用 |shiftwidth()| 在脚本中获取有效值。

					*'shortmess'* *'shm'* *E1336*
'shortmess' 'shm'	字符串	(默认 "ltToOCF")
			全局
	此选项有助于避免由文件消息引起的所有 |hit-enter| 提示，例如使用 CTRL-G 时，并避免一些
	其他消息。它是一个标志列表：
	 标志	存在时的含义	~
	  l	使用 "999L, 888B" 而不是 "999 lines, 888 bytes"	*shm-l*
	  m	使用 "[+]" 而不是 "[Modified]"			*shm-m*
	  r	使用 "[RO]" 而不是 "[readonly]"			*shm-r*
	  w	对于文件写入消息，使用 "[w]" 而不是 "written"；	*shm-w*
		对于 ':w >> file' 命令，使用 "[a]" 而不是 "appended"
	  a	上述所有缩写						*shm-a*

	  o	写入文件的消息会覆盖随后的读取文件消息		*shm-o*
		（对 ":wn" 或 'autowrite' 开启时有用）
	  O	读取文件的消息会覆盖任何先前的消息；也适用于	*shm-O*
		quickfix 消息（例如 ":cn"）
	  s	不显示 "search hit BOTTOM, continuing at TOP" 或	*shm-s*
		"search hit TOP, continuing at BOTTOM" 消息；
		当使用搜索计数时，不显示计数消息前的 "W"（见下面的 |shm-S|）
	  t	如果文件消息太长而无法放入命令行，则在开头截断，	*shm-t*
		最左列会出现 "<"；在 Ex 模式下忽略
	  T	如果其他消息太长而无法放入命令行，则在中间截断，	*shm-T*
		中间会出现 "..."；在 Ex 模式下忽略
	  W	写入文件时不显示 "written" 或 "[w]"		*shm-W*
	  A	找到现有交换文件时不显示 "ATTENTION" 消息	*shm-A*
	  I	启动 Vim 时不显示介绍消息，参见 |:intro|	*shm-I*
	  c	不显示 |ins-completion-menu| 消息；例如		*shm-c*
		"-- XXX completion (YYY)"、"match 1 of 2"、"The only
		match"、"Pattern not found"、"Back at original" 等。
	  C	扫描插入补全项时不显示消息，例如 "scanning tags"	*shm-C*
	  q	录制宏时不显示 "recording @a"			*shm-q*
	  F	编辑文件时不显示文件信息，如同为该命令使用了	*shm-F*
		`:silent`；注意这也影响来自 'autoread' 重新加载的消息
	  S	搜索时不显示搜索计数消息，例如 "[1/5]"。		*shm-S*
		当不存在 "S" 标志时（即显示搜索计数），
		"search hit BOTTOM, continuing at TOP" 和
		"search hit TOP, continuing at BOTTOM" 消息仅通过搜索计数
		统计前的 "W"（助记：Wrapped）字母指示。最大限制可以通过
		'maxsearchcount' 选项设置，另请参见 |searchcount()| 函数。

	这使你有机会避免在缓冲区之间切换时需要按 <Enter>，但仍尽可能为可用空间提供有用的消息。
	要获取在 'shm' 为空时你会得到的完整消息，请使用 ":file!"
	有用的值：
	    shm=	不缩写消息。
	    shm=a	缩写，但不丢失信息。
	    shm=at	缩写，并在必要时截断消息。

					*'showbreak'* *'sbr'* *E595*
'showbreak' 'sbr'	字符串	(默认 "")
			全局或本地到窗口 |global-local|
	放在已换行行开头的字符串。有用的值是 "> " 或 "+++ "： >vim
		let &showbreak = "> "
		let &showbreak = '+++ '
<	只允许使用可打印的单单元格字符，不包括 <Tab> 和逗号（在将来的版本中，逗号可能用于分隔
	显示在行尾和行首的部分）。|hl-NonText| 高亮组决定高亮显示。
	请注意，showbreak 后的制表符将显示不同。
	如果你希望 'showbreak' 出现在行号之间，请将 "n" 标志添加到 'cpoptions'。
	窗口本地值会覆盖全局值。如果设置了全局值，并且你希望当前窗口中没有值，请使用 NONE： >vim
		setlocal showbreak=NONE
<

				*'showcmd'* *'sc'* *'noshowcmd'* *'nosc'*
'showcmd' 'sc'		布尔	(默认开启)
			全局
	在屏幕的最后一行显示（部分）命令。如果你的终端速度慢，请关闭此选项。
	在可视模式下，显示所选区域的大小：
	- 当在一行内选择字符时，显示字符数。如果字节数不同，也会显示："2-6" 表示两个字符和
	  六个字节。
	- 当选择多行时，显示行数。
	- 当选择一个块时，显示屏幕字符大小：{行数}x{列数}。
	此信息可以使用 'showcmdloc' 选项显示在其他位置，当 'cmdheight' 为 0 时很有用。

						*'showcmdloc'* *'sloc'*
'showcmdloc' 'sloc'	字符串	(默认 "last")
			全局
	此选项可用于在其他位置显示（部分）输入的命令。可能的值有：
	  last		屏幕的最后一行（默认）。
	  statusline	当前窗口的状态行。
	  tabline	如果启用了 'showtabline'，则为屏幕的第一行。
	将此选项设置为 "statusline" 或 "tabline" 意味着每当命令更改时（可能在每次按键时），
	这些位置都会重绘。
	'statusline' 或 'tabline' 中的 %S 'statusline' 项可用于放置文本。如果没有自定义的
	'statusline' 或 'tabline'，它将显示在方便的位置。

			*'showfulltag'* *'sft'* *'noshowfulltag'* *'nosft'*
'showfulltag' 'sft'	布尔	(默认关闭)
			全局
	在插入模式下从标签文件补全单词时（参见 |ins-completion|），同时显示标签名称和整理后的
	搜索模式形式（如果有）作为可能的匹配项。因此，如果你匹配了一个 C 函数，你可以看到
	所需参数的模板（取决于编码风格）。
	注意，这与 'completeopt' 中的 "longest" 配合不佳，因为来自搜索模式的补全可能不匹配
	键入的文本。

				*'showmatch'* *'sm'* *'noshowmatch'* *'nosm'*
'showmatch' 'sm'	布尔	(默认关闭)
			全局
	当插入一个括号时，短暂跳转到匹配的括号。仅当匹配项在屏幕上可见时才会跳转。
	显示匹配的时间可以通过 'matchtime' 设置。如果没有匹配项，会发出蜂鸣声（无论匹配项
	是否可见）。当 'cpoptions' 中不包含 'm' 标志时，键入一个字符会立即将光标移回原位。
	有关设置显示匹配时光标形状和闪烁的信息，请参见 'guicursor' 中的 "sm" 字段。
	'matchpairs' 选项可用于指定要显示匹配的字符。'rightleft' 和 'revins' 用于查找相反的
	匹配项。
	另请参见 matchparen 插件，用于在移动时高亮显示匹配项 |pi_paren.txt|。
	注意：使用缩写形式的评分是 PG 级。

				*'showmode'* *'smd'* *'noshowmode'* *'nosmd'*
'showmode' 'smd'	布尔	(默认开启)
			全局
	如果在插入、替换或可视模式下，在最后一行显示一条消息。|hl-ModeMsg| 高亮组决定
	高亮显示。当 'cmdheight' 为零时，此选项无效。

						*'showtabline'* *'stal'*
'showtabline' 'stal'	数字	(默认 1)
			全局
	此选项的值指定何时显示带有标签页标签的行：
		0: 从不
		1: 仅当至少有两个标签页时
		2: 总是
	这适用于标签页行的 GUI 和非 GUI 实现。有关标签页的更多信息，请参见 |tab-page|。

						*'sidescroll'* *'ss'*
'sidescroll' 'ss'	数字	(默认 1)
			全局
	水平滚动的最小列数。仅当 'wrap' 选项关闭且光标移出屏幕时使用。当它为零时，光标将
	置于屏幕中间。当使用慢速终端时，将其设置为较大的数字或 0。不用于 "zh" 和 "zl" 命令。

					*'sidescrolloff'* *'siso'*
'sidescrolloff' 'siso'	数字	(默认 0)
			全局或本地到窗口 |global-local|
	如果设置了 'nowrap'，光标左侧和右侧保持的最小屏幕列数。将此项设置为大于 0 的值，
	同时 'sidescroll' 也设置为非零值，会使你在水平滚动的行中看到一些上下文（行首除外）。
	将此选项设置为一个较大的值（如 999）会产生将光标水平居中在窗口中的效果，只要不
	太靠近行首。
	使用本地值后，可以通过以下两种方式之一返回全局值： >vim
		setlocal sidescrolloff<
		setlocal sidescrolloff=-1
<
	示例：尝试将此选项与 'sidescroll' 和 'listchars' 一起使用，如下例所示，以永远
	不允许光标移动到 "extends" 字符上： >vim

		 set nowrap sidescroll=1 listchars=extends:>,precedes:<
		 set sidescrolloff=1
<

						*'signcolumn'* *'scl'*
'signcolumn' 'scl'	字符串	(默认 "auto")
			本地到窗口
	何时以及如何绘制符号列。有效值为：
	   "auto"		仅当有要显示的符号时
	   "auto:[1-9]"		调整大小以容纳最多给定数量的多个符号（最多 9 个），
				例如 "auto:4"
	   "auto:[1-8]-[2-9]"	调整大小以容纳最多给定最大数量（最多 9 个）的多个符号，
				同时保持至少给定最小数量（最多 8 个）的固定空间。
				最小数量应始终小于最大数量，例如 "auto:2-5"
	   "no"			从不
	   "yes"		总是
	   "yes:[1-9]"		总是，为符号保留固定空间，最多给定数量（最多 9 个），
				例如 "yes:3"
	   "number"		在 'number' 列中显示符号。如果不存在数字列，则行为类似于 "auto"。

			*'smartcase'* *'scs'* *'nosmartcase'* *'noscs'*
'smartcase' 'scs'	布尔	(默认关闭)
			全局
	如果搜索模式包含大写字符，则覆盖 'ignorecase' 选项。仅当搜索模式是键入的且
	'ignorecase' 选项开启时使用。用于命令 "/"、"?"、"n"、"N"、":g" 和 ":s"，以及为补全菜单
	过滤匹配项时 |compl-states|。
	不用于 "*"、"#"、"gd"、标签搜索等。在 "*" 和 "#" 之后，你可以通过执行 "/" 命令、
	从历史记录中调出搜索模式并按 <Enter> 来使用 'smartcase'。

			*'smartindent'* *'si'* *'nosmartindent'* *'nosi'*
'smartindent' 'si'	布尔	(默认关闭)
			本地到缓冲区
	在开始新行时进行智能自动缩进。适用于类 C 程序，但也可用于其他语言。'cindent' 执行
	类似的操作，在大多数情况下效果更好，但更严格，参见 |C-indenting|。当 'cindent' 开启
	或设置了 'indentexpr' 时，设置 'si' 无效。'indentexpr' 是一个更高级的替代方案。
	通常，在使用 'smartindent' 时也应开启 'autoindent'。
	在以下情况下会自动插入缩进：
	- 在以 "{" 结尾的行之后。
	- 在以 'cinwords' 中的关键字开头的行之后。
	- 在以 "}" 开头的行之前（仅使用 "O" 命令）。
	当在新行中键入 '}' 作为第一个字符时，该行将获得与匹配的 "{" 相同的缩进。
	当在新行中键入 '#' 作为第一个字符时，该行的缩进将被删除，'#' 放在第一列。下一行的缩进
	会被恢复。如果你不希望这样，请使用此映射：":inoremap # X^H#"，其中 ^H 是用 CTRL-V CTRL-H
	输入的。使用 ">>" 命令时，以 '#' 开头的行不会向右移动。

				*'smarttab'* *'sta'* *'nosmarttab'* *'nosta'*
'smarttab' 'sta'	布尔	(默认开启)
			全局
	启用后，如果光标位于前导空白中，<Tab> 键将缩进 'shiftwidth' 距离。<BS> 键具有相反
	的效果。在前导空白中，这与将 'softtabstop' 设置为 'shiftwidth' 的值具有相同的效果。
	注意：在大多数情况下，使用 'softtabstop' 是更好的选择。请查看用户指南的 |30.5| 节，
	了解 Vim 如何处理制表符和空格的详细说明。

			*'smoothscroll'* *'sms'* *'nosmoothscroll'* *'nosms'*
'smoothscroll' 'sms'	布尔	(默认关闭)
			本地到窗口
	滚动时处理屏幕行。当设置了 'wrap' 且窗口中的第一行换行时，其部分可能不可见，就像它
	位于窗口上方一样。第一行开头显示 "<<<"，使用 |hl-NonText| 高亮显示。
	你可能还希望将 "lastline" 添加到 'display' 选项中，以尽可能多地显示最后一行。
	注意：部分实现，尚不支持 |gj| 和 |gk|。

						*'softtabstop'* *'sts'*
'softtabstop' 'sts'	数字	(默认 0)
			本地到缓冲区
	创建软制表位，间隔为 'softtabstop' 列数。在插入模式下，按 <Tab> 键会将光标移动到
	下一个软制表位，而不是插入字面制表符。<BS> 键的作用相反。Vim 插入最少混合的制表符和
	空格字符以达到视觉效果。

	此设置不影响现有制表符的显示。

	值为 0 禁用此行为。负值使 Vim 使用 'shiftwidth'。如果你计划将 'sts' 和 'shiftwidth'
	与不同的值一起使用，可以考虑设置 'smarttab'。
	当启用 'list' 时，'cpoptions' 中的 'L' 标志会改变制表符的行为。另请参见 |ins-expandtab|
	和用户手册 |30.5| 节以获取深入说明。

	如果 'varsofttabstop' 设置为非空字符串以外的任何值，'softtabstop' 的值将被忽略。

						*'spell'* *'nospell'*
'spell'			布尔	(默认关闭)
			本地到窗口
	开启时，将进行拼写检查。参见 |spell|。语言通过 'spelllang' 指定。

						*'spellcapcheck'* *'spc'*
'spellcapcheck' 'spc'	字符串	(默认 "[.?!]\_[\])'"\t ]\+")
			本地到缓冲区
	用于定位句子结尾的模式。将检查下一个单词是否以大写字母开头。如果不是，则使用
	SpellCap |hl-SpellCap| 高亮显示（除非该单词也拼写错误）。当不需要此检查时，请将此选项
	设为空。仅当设置了 'spell' 时使用。注意特殊字符，有关包含空格和反斜杠的信息，请参见
	|option-backslash|。要自动根据语言设置此选项，请参见 |set-spc-auto|。

						*'spellfile'* *'spf'*
'spellfile' 'spf'	字符串	(默认 "")
			本地到缓冲区
	单词列表文件的名称，|zg| 和 |zw| 命令添加的单词将写入此文件。它必须以
	".{encoding}.add" 结尾。你需要包含路径，否则文件将放在当前目录中。
	路径可以包含来自 'isfname'、' '、','、'@' 和 ':' 的字符。*E765*
	它也可以是一个逗号分隔的名称列表。|zg| 和 |zw| 命令前的计数可用于访问每个列表。
	这允许使用个人单词列表文件和项目单词列表文件。
	当此选项为空时添加单词，Nvim 将使用（并自动创建）`stdpath('data')/site/spell/`。
	对于文件名，使用 'spelllang' 中出现的第一个语言名称，忽略区域。
	生成的 ".spl" 文件将用于拼写检查，它不必出现在 'spelllang' 中。
	通常，一个文件用于所有区域，但如果你愿意，可以添加区域名称。然而，只有当 'spellfile'
	设置为其时才会使用，对于 'spelllang' 中的条目，只会找到没有区域名称的文件。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'spelllang'* *'spl'*
'spelllang' 'spl'	字符串	(默认 "en")
			本地到缓冲区
	一个逗号分隔的单词列表名称列表。当 'spell' 选项开启时，将对这些语言进行拼写检查。
	示例： >vim
		set spelllang=en_us,nl,medical
<	这意味着识别美国英语、荷兰语和医学术语。未识别的单词将被高亮显示。
	单词列表名称必须由字母数字字符、破折号或下划线组成。它不应包含逗号或点。建议使用
	破折号将两个字母的语言名称与规范分开。因此 "en-rare" 用于罕见的英语单词。
	区域名称必须最后出现，形式为 "_xx"，其中 "xx" 是两个字母的小写区域名称。你可以通过列出
	它们来使用多个区域："en_us,en_ca" 支持美国和加拿大英语，但不支持澳大利亚、新西兰或英国
	特有的单词。（注意：目前 en_au 和 en_nz 字典比 en_ca、en_gb 和 en_us 旧）。
	如果名称中包含 "cjk"，则东亚字符被排除在拼写检查之外。这在编辑同时包含亚洲单词的文本时
	很有用。
	注意，"medical" 字典不存在，它只是一个较长名称的示例。*E757*
	作为一种特殊情况，可以直接给出 .spl 文件的名称。名称中的第一个 "_xx" 被移除并用作
	区域名称（_xx 是下划线、两个字母，后跟一个非字母字符）。这主要用于测试目的。
	你必须确保使用正确的编码，Vim 不会检查它。
	如何找到相关的拼写文件在此解释：|spell-load|。

	如果 |package-spellfile| 插件处于活动状态，并且你使用了 Vim 在 'runtimepath' 中找不到
	.spl 文件的语言名称，该插件将询问你是否要下载该文件。

	在此选项成功设置后，Vim 将加载 'runtimepath' 中的文件 "spell/LANG.vim"。"LANG" 是
	'spelllang' 的值，直到第一个不是 ASCII 字母或数字且不是破折号的字符。另请参见
	|set-spc-auto|。

						*'spelloptions'* *'spo'*
'spelloptions' 'spo'	字符串	(默认 "")
			本地到缓冲区
	拼写检查选项的逗号分隔列表：
	camel		当一个单词是驼峰式时，假定 "Cased" 是一个单独的单词：单词中每个在大写
			字母之后出现的小写字母表示一个新单词的开始。
	noplainbuffer	仅当启用了 'syntax' 或在缓冲区中设置了 extmarks 时才对缓冲区进行拼写检查。
			在这种情况下，仅对缓冲区的指定区域进行拼写检查。

						*'spellsuggest'* *'sps'*
'spellsuggest' 'sps'	字符串	(默认 "best")
			全局
	用于拼写建议的方法。适用于 |z=| 命令和 |spellsuggest()| 函数。这是一个逗号分隔的
	项目列表：

	best		对英语效果最好的内部方法。查找像 "fast" 这样的更改，并使用一些类似发音的
			评分来改进排序。

	double		内部方法，使用两种方法并混合结果。第一种方法是 "fast"，另一种方法计算
			建议词与错误词的发音相似度。这仅当语言指定了声音折叠时才有效。可能很慢，
			并且并不总是给出更好的结果。

	fast		内部方法，只检查简单的更改：字符插入/删除/交换。适用于简单的打字错误。

	{number}	|z=| 列出的最大建议数。不用于 |spellsuggest()|。建议数永远不会超过
			'lines' 的值减二。

	timeout:{millisec}	将搜索建议的时间限制为 {millisec} 毫秒。适用于以下方法。省略时，
				限制为 5000。为负值时没有限制。

	file:{filename}	读取文件 {filename}，该文件必须有两列，用斜杠分隔。第一列包含错误单词，
			第二列包含建议的正确单词。示例：
				theribal/terrible ~
			对于内部方法未显示在建议列表顶部的常见错误，请使用此选项。
			没有斜杠的行将被忽略，用于注释。
			第二列中的单词必须是正确的，否则将不会被使用。如果该单词当前被标记为拼写错误，
			请将其添加到 ".add" 文件中。
			该文件适用于所有语言。

	expr:{expr}	评估表达式 {expr}。使用函数可以避免空格带来的麻烦。最好调用一个无参数的函数，
			参见 |expr-option-function|。|v:val| 包含拼写错误的单词。表达式必须评估为
			一个列表的列表，每个列表包含一个建议和一个分数。
			示例：
				[['the', 33], ['that', 44]] ~
			设置 'verbose' 并使用 |z=| 查看内部方法使用的分数。分数越低越好。
			如果你临时将 'spellsuggest' 设置为排除 "expr:" 部分，这可能会调用 |spellsuggest()|。
			错误会被静默忽略，除非你将 'verbose' 选项设置为非零值。

	只能使用 "best"、"double" 或 "fast" 中的一个。其他可以按任意顺序多次出现。示例： >vim
		set sps=file:~/.config/nvim/sugg,best,expr:MySuggest()
<	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'splitbelow'* *'sb'* *'nosplitbelow'* *'nosb'*
'splitbelow' 'sb'	布尔	(默认关闭)
			全局
	开启时，分割窗口会将新窗口放在当前窗口下方。|:split|

						*'splitkeep'* *'spk'*
'splitkeep' 'spk'	字符串	(默认 "cursor")
			全局
	此选项的值决定了打开、关闭或调整水平分割时的滚动行为。

	可能的值有：
	  cursor	保持相同的相对光标位置。
	  screen	保持文本在同一屏幕行上。
	  topline	保持顶行不变。

	对于 "screen" 和 "topline" 值，光标位置将在必要时改变。在这种情况下，跳转列表
	会填充先前的光标位置。对于 "screen"，当启用 'wrap' 时，文本不能总是保持在相同的
	屏幕行上。

			*'splitright'* *'spr'* *'nosplitright'* *'nospr'*
'splitright' 'spr'	布尔	(默认关闭)
			全局
	开启时，分割窗口会将新窗口放在当前窗口右侧。|:vsplit|

			*'startofline'* *'sol'* *'nostartofline'* *'nosol'*
'startofline' 'sol'	布尔	(默认关闭)
			全局
	当 "on" 时，下面列出的命令将光标移动到行的第一个非空白字符。当关闭时，光标保持
	在同一列（如果可能）。这适用于以下命令：
	- CTRL-D、CTRL-U、CTRL-B、CTRL-F、"G"、"H"、"M"、"L"、"gg"
	- 带有行方式操作符的 "d"、"<<"、"==" 和 ">>"（|operator-resulting-pos|）
	- 带有计数的 "%"
	- 更改缓冲区的命令（CTRL-^、:bnext、:bNext 等）
	- 只有行号的 Ex 命令，例如 ":25" 或 ":+"。
	对于更改缓冲区的命令，光标将放置在上次编辑该缓冲区时的列位置。

						*'statuscolumn'* *'stc'*
'statuscolumn' 'stc'	字符串	(默认 "")
			本地到窗口
	当非空时，此选项决定了窗口侧边区域的内容，该区域通常包含折叠列、符号列和行号列。
	此选项的格式类似于 'statusline'。

	'statusline' 格式中的一些项目在 'statuscolumn' 中有所不同：

	%l	当前绘制行的行号列
	%s	当前绘制行的符号列
	%C	当前绘制行的折叠列

	'statuscolumn' 的宽度遵循默认列的宽度，并适应 'numberwidth'、'signcolumn' 和
	'foldcolumn' 选项的值（无论符号和折叠项目是否存在）。此外，'statuscolumn' 会随着
	评估格式字符串的大小而增长，直到某个点（遵循默认折叠列、符号列和行号列的最大大小）。
	仅在缓冲区中的行数发生变化或设置 'statuscolumn' 选项时才会缩小。

	|v:lnum|    变量保存要绘制的行号。
	|v:relnum|  变量保存要绘制的相对行号。
	|v:virtnum| 变量在绘制虚拟行时为负数，在绘制实际缓冲区行时为零，在绘制缓冲区行的
		     换行部分时为正数。

	使用 |v:relnum| 时，请记住，除非设置了 'relativenumber'，否则光标移动本身不会导致
	'statuscolumn' 更新。

	注意：支持 %@ 点击执行函数项，但指定的函数对于同一列的每一行都是相同的。它不能通过
	动态的 'statuscolumn' 格式切换出来，编写处理程序时应考虑到这一点。

	示例： >vim
		" 带有条形分隔符和点击处理程序的行号：
		set statuscolumn=%@SignCb@%s%=%T%@NumCb@%l│%T

		" 非换行部分的行号以十六进制显示：
		let &stc='%=%{v:virtnum>0?"":printf("%x",v:lnum)} '

		" 带有千位分隔符的人类可读行号：
		let &stc='%{substitute(v:lnum,"\\d\\zs\\ze\\'
			   . '%(\\d\\d\\d\\)\\+$",",","g")}'

		" 相对行号和绝对行号，奇偶相对行号不同高亮：
		let &stc='%#NonText#%{&nu?v:lnum:""}' .
		 '%=%{&rnu&&(v:lnum%2)?"\ ".v:relnum:""}' .
		 '%#LineNr#%{&rnu&&!(v:lnum%2)?"\ ".v:relnum:""}'

<	警告：此表达式对每个屏幕行进行评估，因此定义一个开销大的表达式可能会对渲染性能
	产生负面影响。

					*'statusline'* *'stl'* *E540* *E542*
'statusline' 'stl'	字符串	(默认非常长)
			全局或本地到窗口 |global-local|
	设置 |status-line|。

	该选项由 printf 样式的 '%' 项目与普通文本交错组成。每个状态行项目的形式为：
	  %-0{minwid}.{maxwid}{item}
	除 {item} 外的所有字段都是可选的。单个百分号可以写为 "%%"。

							*stl-%!*
	当选项以 "%!" 开头时，它被用作一个表达式，评估后的结果用作选项值。示例： >vim
		set statusline=%!MyStatusLine()
<	*g:statusline_winid* 变量将设置为状态行所属窗口的 |window-ID|。
	结果可以包含也会被评估的 %{} 项。
	注意，"%!" 表达式在当前窗口和缓冲区的上下文中评估，而 %{} 项在状态行所属窗口的
	上下文中评估。

	评估选项时如果出现错误，它将被重置为其默认值以避免进一步的错误。否则屏幕更新将
	循环。当结果包含不可打印字符时，结果不可预测。

	请注意，当设置了此选项（且 'laststatus' 为 2 或 3）时，'ruler' 的唯一作用是控制
	|CTRL-G| 的输出。

	字段	    含义 ~
	-	    左对齐项目。默认情况下，当 minwid 大于项目长度时，项目右对齐。
	0	    数字项目中的前导零。被 "-" 覆盖。
	minwid	    项目的最小宽度，按 "-" 和 "0" 设置填充。值必须 <= 50。
	maxwid	    项目的最大宽度。对于文本项目，截断时会在左侧出现 "<"。数字项目将缩减为
		    maxwid-2 位数字，后跟 ">"number，其中 number 是缺失的数字数，类似于
		    指数表示法。
	item	    如下所述的单字母代码。

	以下是可能的状态行项目的描述。"item" 中的第二个字符是类型：
		N 表示数字
		S 表示字符串
		F 表示如下所述的标志
		- 不适用

	项目  含义 ~
	f S   缓冲区中文件的路径，按键入或相对于当前目录。
	F S   缓冲区中文件的完整路径。
	t S   缓冲区中文件的文件名（尾部）。
	m F   修改标志，文本为 "[+]"；如果 'modifiable' 关闭，则为 "[-]"。
	M F   修改标志，文本为 ",+" 或 ",-"。
	r F   只读标志，文本为 "[RO]"。
	R F   只读标志，文本为 ",RO"。
	h F   帮助缓冲区标志，文本为 "[help]"。
	H F   帮助缓冲区标志，文本为 ",HLP"。
	w F   预览窗口标志，文本为 "[Preview]"。
	W F   预览窗口标志，文本为 ",PRV"。
	y F   缓冲区中文件的类型，例如 "[vim]"。参见 'filetype'。
	Y F   缓冲区中文件的类型，例如 ",VIM"。参见 'filetype'。
	q S   "[Quickfix List]"，"[Location List]" 或空。
	k S   当使用 |:lmap| 映射时，"b:keymap_name" 或 'keymap' 的值："<keymap>"
	n N   缓冲区编号。
	b N   光标下字符的值。
	B N   同上，但以十六进制显示。
	o N   光标下字节在文件中的字节数，第一个字节为 1。助记：从文件开头的偏移量（加一）
	O N   同上，但以十六进制显示。
	l N   行号。
	L N   缓冲区中的行数。
	c N   列号（字节索引）。
	v N   虚拟列号（屏幕列）。
	V N   虚拟列号，显示为 -{num}。如果等于 'c' 则不显示。
	p N   文件中行数的百分比，如 |CTRL-G| 所示。
	P S   显示窗口在文件中的百分比。这类似于 'ruler' 中描述的百分比。长度总是 3，
	      除非被翻译。
	S S   'showcmd' 的内容，参见 'showcmdloc'。
	a S   参数列表状态，如默认标题中所示。（{当前} 共 {最大}）
	      如果参数文件计数为零或一，则为空。
	{ NF  评估 "%{" 和 "}" 之间的表达式并替换结果。注意，结束的 "}" 前没有 "%"。
	      表达式不能包含 "}" 字符，可以通过调用函数来解决。参见下面的 |stl-%{|。
	`{%` - 这与 "{" 几乎相同，只是表达式的结果会被重新评估为状态行格式字符串。因此，
	      如果表达式的返回值包含 "%" 项，它们将被展开。表达式可以包含 "}" 字符，
	      表达式结束由 "%}" 表示。例如： >vim
		func! Stl_filename() abort
		    return "%t"
		endfunc
<	        `stl=%{Stl_filename()}`   结果在 `"%t"`
	        `stl=%{%Stl_filename()%}` 结果在 `"当前文件的名称"`
	%} -   "{%" 表达式的结束
	( -   项目组的开始。可用于设置部分的宽度和对齐方式。后面必须跟有 %) 某处。
	) -   项目组的结束。不允许有宽度字段。
	T N   对于 'tabline'：标签页 N 标签的开始。使用 %T 或 %X 结束标签。用鼠标左键单击此标签
	      切换到指定的标签页，用鼠标中键单击则关闭指定的标签页。
	X N   对于 'tabline'：关闭标签 N 标签的开始。使用 %X 或 %T 结束标签，例如：%3Xclose%X。
	      使用 %999X 作为 "关闭当前标签" 标签。用鼠标左键单击此标签关闭指定的标签页。
	@ N   执行函数标签的开始。使用 %X 或 %T 结束标签，例如：%10@SwitchBuffer@foo.c%X。
	      单击此标签会运行指定的函数：在示例中，当用鼠标左键单击 "foo.c" 一次时，将运行
	      `SwitchBuffer(10, 1, 'l', '    ')` 表达式。指定的函数按顺序接收以下参数：
	      1. minwid 字段值，如果未指定 N 则为零
	      2. 鼠标单击次数，用于检测多次单击
	      3. 使用的鼠标按钮："l"、"r" 或 "m"，分别表示左键、右键或中键；不应依赖第三个
	         参数仅为 "l"、"r" 或 "m"：其他鼠标按钮可能会提供仅包含 ASCII 小写字母的
	         任何其他非空字符串值
	      4. 按下的修饰键：字符串，如果按下了 shift 修饰键则包含 "s"，control 为 "c"，
	         alt 为 "a"，meta 为 "m"；如果未按下修饰键，当前字符串包含空格，但不应依赖
	         空格的存在或修饰键的特定顺序：使用 |stridx()| 测试是否存在某个修饰键；
	         保证字符串仅包含 ASCII 字母和空格，每个修饰键一个字母；"?" 修饰键也可能存在，
	         但其存在是一个错误，表示添加了新的鼠标按钮识别，但未修改对此标签上鼠标单击
	         做出反应的代码。
	      在指定的函数中使用 |getmousepos()|.winid 获取单击项对应的窗口 ID。
	< -   如果行太长，在此处截断。默认在开头截断。不允许有宽度字段。
	= -   对齐部分之间的分隔点。每个部分将由相等数量的空格分隔。使用一个 %= 时，其后的
	      内容将右对齐。使用两个 %= 时，中间有一个部分，其左右两边有空白。不允许有宽度字段。
	# -   设置高亮组。名称必须紧跟其后，然后再跟一个 #。因此使用 %#HLname# 表示高亮组 HLname。
	      使用相同的高亮显示，也用于非当前窗口的状态行。
	$ -   与 `#` 相同，除了 `%$HLname$` 组将从先前的高亮属性继承。
	* -   将高亮组设置为 User{N}，其中 {N} 取自 minwid 字段，例如 %1*。使用 %* 或 %0*
	      恢复正常高亮。User{N} 和 StatusLine 之间的差异将应用于非当前窗口状态行的
	      StatusLineNC。数字 N 必须在 1 到 9 之间。参见 |hl-User1..9|

	显示标志时，如果该标志紧跟在纯文本之后，Vim 会移除前导逗号（如果有）。这会在像下面
	示例中使用标志时产生良好的显示效果。

	当一个组中的所有项目都变成空字符串（即未设置的标志）且没有为组设置 minwid 时，
	整个组将变为空。这将使像下面这样的组在没有任何标志设置时完全从状态行中消失。 >vim
		set statusline=...%(\ [%M%R%H]%)...
<	请注意，每次显示状态行时都会评估表达式。*stl-%{* *g:actual_curbuf* *g:actual_curwin*
	在评估 %{} 时，当前缓冲区和当前窗口将临时设置为正在绘制状态行的窗口（和缓冲区）。
	表达式将在该上下文中评估。变量 "g:actual_curbuf" 设置为真实当前缓冲区的 `bufnr()`
	编号，"g:actual_curwin" 设置为真实当前窗口的 |window-ID|。这些值是字符串。

	如果从 modeline 设置，'statusline' 选项将在 |sandbox| 中评估，参见 |sandbox-option|。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

	评估 'statusline' 时不允许更改文本或跳转到另一个窗口 |textlock|。

	如果状态行未在需要时更新（例如，在设置了用于表达式的变量后），你可以通过使用
	`:redrawstatus` 强制更新。

	全数字的结果在显示时被视为数字。否则，结果被视为标志文本，并应用上述规则。

	注意表达式中的错误。它们可能使 Vim 无法使用！如果你卡住了，按住 ':' 或 'Q' 获取提示，
	然后退出并使用 "vim --clean" 编辑你的 vimrc 或其他文件以修正错误。

	示例：
	模拟带有 'ruler' 设置的标准状态行 >vim
	  set statusline=%<%f\ %h%w%m%r%=%-14.(%l,%c%V%)\ %P
<	类似，但添加光标下字符的 ASCII 值（如 "ga"） >vim
	  set statusline=%<%f%h%m%r%=%b\ 0x%B\ \ %l,%c%V\ %P
<	显示字节计数和字节值，修改标志为红色。 >vim
	  set statusline=%<%f%=\ [%1*%M%*%n%R%H]\ %-19(%3l,%02c%03V%)%O'%02b'
	  hi User1 term=inverse,bold cterm=inverse,bold ctermfg=red
<	如果加载了压缩文件，显示 ,GZ 标志 >vim
	  set statusline=...%r%{VarExists('b:gzflag','\ [GZ]')}%h...
<	在 |:autocmd| 中： >vim
	  let b:gzflag = 1
<	以及： >vim
	  unlet b:gzflag
<	并定义此函数： >vim
	  function VarExists(var, val)
	      if exists(a:var) | return a:val | else | return '' | endif
	  endfunction
<

						*'suffixes'* *'su'*
'suffixes' 'su'		字符串	(默认 ".bak,~,.o,.h,.info,.swp,.obj")
			全局
	当多个文件匹配通配符时，具有这些后缀的文件优先级较低。参见 |suffixes|。
	可以使用逗号分隔后缀。逗号后的空格被忽略。点也被视为后缀的开始。为避免点或逗号
	被识别为分隔符，请在其前面加反斜杠（关于包含空格和反斜杠的信息，请参见
	|option-backslash|）。
	参见 'wildignore' 以完全忽略文件。
	在列表中添加或删除后缀时，最好使用 |:set+=| 和 |:set-=|。这可以避免将来版本使用
	其他默认值时出现的问题。

						*'suffixesadd'* *'sua'*
'suffixesadd' 'sua'	字符串	(默认 "")
			本地到缓冲区
	逗号分隔的后缀列表，用于 "gf"、"[I" 等命令搜索文件时。示例： >vim
		set suffixesadd=.java
<

				*'swapfile'* *'swf'* *'noswapfile'* *'noswf'*
'swapfile' 'swf'	布尔	(默认开启)
			本地到缓冲区
	为缓冲区使用交换文件。当特定缓冲区不需要交换文件时，可以重置此选项。例如，对于即使
	root 也无权访问的机密信息。注意：所有文本都将保留在内存中：
		- 不要对大文件使用此选项。
		- 将无法恢复！
	只有当 'updatecount' 非零且设置了 'swapfile' 时，交换文件才会存在。
	当 'swapfile' 重置时，当前缓冲区的交换文件会立即被删除。当 'swapfile' 设置且
	'updatecount' 非零时，会立即创建交换文件。
	另请参见 |swap-file|。
	如果你想打开一个新缓冲区而不为其创建交换文件，请使用 |:noswapfile| 修饰符。
	有关交换文件的创建位置，请参见 'directory'。

	此选项与 'bufhidden' 和 'buftype' 一起用于指定特殊类型的缓冲区。参见 |special-buffers|。

						*'switchbuf'* *'swb'*
'switchbuf' 'swb'	字符串	(默认 "uselast")
			全局
	此选项控制在缓冲区之间切换时的行为。在以下情况下检查此选项：
	- 使用 |quickfix| 命令跳转到错误时（|:cc|、|:cn|、|:cp| 等）。
	- 使用 |:stag| 命令跳转到标签时。
	- 使用 |CTRL-W_f| 或 |CTRL-W_F| 命令打开文件时。
	- 使用缓冲区分割命令（例如 |:sbuffer|、|:sbnext| 或 |:sbrewind|）跳转到缓冲区时。
	可能的值（逗号分隔列表）：
	   useopen	如果包含，跳转到当前标签页中包含指定缓冲区的第一个打开窗口
			（如果有）。否则：不检查其他窗口。
	   usetab	类似于 "useopen"，但也考虑其他标签页中的窗口。
	   split	如果包含，在为显示错误的 |quickfix| 命令加载缓冲区之前分割当前窗口。
			否则：不分割，使用当前窗口（在 quickfix 窗口中使用时：先前使用的窗口，
			如果没有其他窗口则分割）。
	   vsplit	类似于 "split"，但垂直分割。
	   newtab	类似于 "split"，但打开一个新的标签页。当两者都存在时，覆盖 "split"。
	   uselast	如果包含，当使用 |quickfix| 命令跳转到错误时，跳转到先前使用的窗口。
	如果窗口启用了 'winfixbuf'，'switchbuf' 目前不应用于分割的窗口。

						*'synmaxcol'* *'smc'*
'synmaxcol' 'smc'	数字	(默认 3000)
			本地到缓冲区
	搜索语法项的最大列数。在长行中，此列之后的文本不会高亮显示，并且后续行可能无法
	正确高亮显示，因为语法状态被清除。这有助于避免在只有一长行的 XML 文件中重绘过慢。
	设置为零以移除限制。

						*'syntax'* *'syn'*
'syntax' 'syn'		字符串	(默认 "")
			本地到缓冲区  |local-noglobal|
	设置此选项时，将加载具有此名称的语法，除非已通过 ":syntax off" 关闭语法高亮显示。
	否则，此选项并不总是反映当前语法（b:current_syntax 变量反映）。
	此选项在 modeline 中最有用，适用于语法无法自动识别的文件。例如，在 IDL 文件中： >c
		/* vim: set syntax=idl : */
<	当值中出现点时，这会分隔两个文件类型名称。示例： >c
		/* vim: set syntax=c.doxygen : */
<	这将首先使用 "c" 语法，然后使用 "doxygen" 语法。注意，第二个语法必须准备好作为
	附加项加载，否则将被跳过。可以出现多个点。
	要关闭当前文件的语法高亮显示，请使用： >vim
		set syntax=OFF
<	要根据当前 'filetype' 选项的值打开语法高亮显示： >vim
		set syntax=ON
<	设置 'syntax' 选项时实际发生的情况是，使用该值作为参数触发 Syntax 自动命令事件。
	无论 'cpoptions' 中的 's' 或 'S' 标志如何，此选项都不会复制到另一个缓冲区。
	只能使用字母数字字符、'.'、'-' 和 '_'。

						*'tabclose'* *'tcl'*
'tabclose' 'tcl'	字符串	(默认 "")
			全局
	此选项控制在关闭标签页时的行为（例如，使用 |:tabclose|）。当为空时，Vim 转到下一个
	（右侧）标签页。

	可能的值（逗号分隔列表）：
	   left		如果包含，转到上一个标签页而不是下一个。
	   uselast	如果包含，尽可能转到先前使用的标签页。此选项优先于其他选项。

						*'tabline'* *'tal'*
'tabline' 'tal'		字符串	(默认 "")
			全局
	当非空时，此选项决定 Vim 窗口顶部标签页行的内容。当为空时，Vim 将使用默认的标签页行。
	更多信息请参见 |setting-tabline|。

	标签页行仅在 'showtabline' 选项指定的情况下出现，并且仅当没有 GUI 标签行时出现。
	当 'guioptions' 中包含 'e' 且 GUI 支持标签行时，使用 'guitablabel' 代替。注意，
	这两种标签页行非常不同。

	该值的评估方式与 'statusline' 相同。你可以使用 |tabpagenr()|、|tabpagewinnr()| 和
	|tabpagebuflist()| 来确定要显示的文本。使用 "%1T" 表示第一个标签，"%2T" 表示第二个，
	等等。使用 "%X" 项目表示关闭标签。

	当更改了 'tabline' 中使用但未触发其更新的内容时，请使用 |:redrawtabline|。
	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

	请记住，只有一个标签页是当前标签页，其他标签页不可见，你无法跳转到它们的窗口。

						*'tabpagemax'* *'tpm'*
'tabpagemax' 'tpm'	数字	(默认 50)
			全局
	通过 |-p| 命令行参数或 ":tab all" 命令可以打开的最大标签页数。|tabpage|

						*'tabstop'* *'ts'*
'tabstop' 'ts'		数字	(默认 8)
			本地到缓冲区
	定义用于显示水平制表符（ASCII 9）的列倍数；水平制表符总是前进到下一个制表位。
	该值必须至少为 1，最多为 9999。如果设置了 'vartabstop'，则忽略此选项。
	除非有充分的理由，否则请保持为 8（参见 usr |30.5|）。

			*'tagbsearch'* *'tbs'* *'notagbsearch'* *'notbs'*
'tagbsearch' 'tbs'	布尔	(默认开启)
			全局
	搜索标签时（例如，对于 |:ta| 命令），Vim 可以在标签文件中使用二分搜索或线性搜索。
	二分搜索使标签搜索快得多，但如果标签文件未正确排序，线性搜索会找到更多标签。
	Vim 通常假设你的标签文件已排序，或指示它们未排序。仅当不是这种情况时，才需要关闭
	'tagbsearch' 选项。

	当 'tagbsearch' 开启时，首先在标签文件中使用二分搜索。在某些情况下，Vim 会对某些
	文件进行线性搜索，或者使用线性搜索重试所有文件。当 'tagbsearch' 关闭时，仅进行
	线性搜索。

	对于单个文件，当 Vim 在文件开头找到指示其未排序的行时，无论如何都会进行线性搜索： >
	   !_TAG_FILE_SORTED	0	/some comment/
<	['0' 前后的空白必须是一个 <Tab>]

	当进行了二分搜索但在 'tags' 中列出的任何文件中都未找到匹配项，并且忽略了大小写或使用
	模式而不是普通标签名称时，会使用线性搜索重试。未排序标签文件中的标签以及大小写不同
	的匹配项仅在重试时才会找到。

	如果标签文件指示其已按忽略大小写排序，则在忽略大小写时可以避免第二次线性搜索。在
	"!_TAG_FILE_SORTED" 行中使用值 '2'。在大多数 Unix 系统中，可以使用 "sort" 的 -f 开关
	对标签文件进行忽略大小写排序，例如命令："sort -f -o tags tags"。对于 Universal ctags
	和 Exuberant ctags 5.x 或更高版本（至少 5.5），也可以使用 --sort=foldcase 开关。
	注意，为此，必须将大小写折叠为大写。

	默认情况下，标签搜索区分大小写。当设置了 'ignorecase' 且 'tagcase' 为 "followic"，
	或 'tagcase' 为 "ignore" 时，忽略大小写。同样，当 'tagcase' 为 "followscs" 且设置了
	'smartcase'，或 'tagcase' 为 "smart"，并且模式仅包含小写字符时。

	当 'tagbsearch' 关闭时，如果存在完全匹配，标签搜索较慢；但如果不存在完全匹配，则较快。
	未排序标签文件中的标签可能仅在 'tagbsearch' 关闭时才能找到。
	当标签文件未排序，或排序方式错误（不按 ASCII 字节值）时，应关闭 'tagbsearch'，或者
	必须在标签文件中包含上面给出的行。
	此选项不影响查找所有匹配标签的命令（例如，命令行补全和 ":help"）。

						*'tagcase'* *'tc'*
'tagcase' 'tc'		字符串	(默认 "followic")
			全局或本地到缓冲区 |global-local|
	此选项指定在搜索标签文件时如何处理大小写：
	   followic	遵循 'ignorecase' 选项
	   followscs	遵循 'smartcase' 和 'ignorecase' 选项
	   ignore	忽略大小写
	   match	匹配大小写
	   smart	除非使用大写字母，否则忽略大小写

						*'tagfunc'* *'tfu'*
'tagfunc' 'tfu'		字符串	(默认 "")
			本地到缓冲区
	此选项指定一个用于执行标签搜索的函数（包括 |taglist()|）。
	该函数获取标签模式并应返回匹配标签的列表。有关如何编写函数以及示例的说明，
	请参见 |tag-function|。该值可以是函数名、|lambda| 或 |Funcref|。
	更多信息请参见 |option-value-function|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'taglength'* *'tl'*
'taglength' 'tl'	数字	(默认 0)
			全局
	如果非零，标签的有效长度限制在此字符数内。

			*'tagrelative'* *'tr'* *'notagrelative'* *'notr'*
'tagrelative' 'tr'	布尔	(默认开启)
			全局
	如果开启，并且使用另一个目录中的标签文件，则该标签文件中的文件名相对于该标签文件
	所在的目录。

						*'tags'* *'tag'* *E433*
'tags' 'tag'		字符串	(默认 "./tags;,tags")
			全局或本地到缓冲区 |global-local|
	标签命令的文件名，以空格或逗号分隔。要在文件名中包含空格或逗号，请在其前面加反斜杠
	（关于包含空格/逗号和反斜杠的信息，请参见 |option-backslash|）。
	当文件名以 "./" 开头时，'.' 被替换为当前文件的路径。但仅当 'cpoptions' 中不包含
	'd' 标志时。环境变量会被展开 |:set_env|。另请参见 |tags-option|。
	"*"、"**" 和其他通配符可用于在目录树中搜索标签文件。参见 |file-searching|。
	例如，"/lib/**/tags" 将在 "/lib" 下找到所有名为 "tags" 的文件。文件名本身不能包含
	通配符，它按原样使用。例如，"/lib/**/tags?" 将找到名为 "tags?" 的文件。
	|tagfiles()| 函数可用于获取实际使用的文件名列表。
	在列表中添加或删除文件名时，最好使用 |:set+=| 和 |:set-=|。这可以避免将来版本
	使用其他默认值时出现的问题。

			*'tagstack'* *'tgst'* *'notagstack'* *'notgst'*
'tagstack' 'tgst'	布尔	(默认开启)
			全局
	开启时，正常使用 |tagstack|。关闭时，带参数的 ":tag" 或 ":tselect" 命令不会将
	标签推入标签栈。随后的无参数 ":tag"、":pop" 命令或任何其他使用标签栈的命令将使用
	未修改的标签栈，但会更改指向活动条目的指针。在不应更改标签栈的映射中使用 ":tag"
	命令时，重置此选项很有用。

			*'termbidi'* *'tbidi'* *'notermbidi'* *'notbidi'*
'termbidi' 'tbidi'	布尔	(默认关闭)
			全局
	终端负责文本的双向性（由 Unicode 指定）。终端还应执行某些语言（如阿拉伯语）所需的
	字形变换。设置此选项意味着当设置 'arabic' 时，不会设置 'rightleft'，并且会忽略
	'arabicshape' 的值。注意，设置 'termbidi' 会立即产生忽略 'arabicshape' 的效果，
	但 'rightleft' 不会自动更改。更多详情请参见 |l10n-arabic.txt|。

		*'termguicolors'* *'tgc'* *'notermguicolors'* *'notgc'*
'termguicolors' 'tgc'	布尔	(默认关闭)
			全局
	在 |TUI| 中启用 24 位 RGB 颜色。使用 "gui" |:highlight| 属性而不是 "cterm" 属性。
	|guifg|
	需要兼容 ISO-8613-3 的终端。

	Nvim 将自动尝试确定主机终端是否支持 24 位颜色，如果支持，将启用此选项
	（除非用户明确禁用）。

					*'termpastefilter'* *'tpf'*
'termpastefilter' 'tpf'	字符串	(默认 "BS,HT,ESC,DEL")
			全局
	一个逗号分隔的选项列表，用于指定要从粘贴到终端窗口的文本中移除的控制字符。
	支持的值有：

	   BS	    退格

	   HT	    TAB

	   FF	    换页

	   ESC	    Escape

	   DEL	    DEL

	   C0	    其他控制字符，不包括换行和回车 < ' '

	   C1	    控制字符 0x80...0x9F

					*'termsync'* *'notermsync'*
'termsync'		布尔	(默认开启)
			全局
	如果主机终端支持，则缓冲在重绘周期期间进行的所有屏幕更新，以便每个屏幕一次性显示
	在终端中。当终端更新速度快于 Nvim 重绘速度时，这可以防止撕裂或闪烁。

						*'textwidth'* *'tw'*
'textwidth' 'tw'	数字	(默认 0)
			本地到缓冲区
	正在插入的文本的最大宽度。较长的行将在空白处断开以达到此宽度。零值禁用此功能。
	当 'textwidth' 为零时，可以使用 'wrapmargin'。另请参见 'formatoptions' 和
	|ins-textwidth|。如果设置了 'formatexpr'，将使用它来断开行。

						*'thesaurus'* *'tsr'*
'thesaurus' 'tsr'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	文件名列表，以逗号分隔，用于查找同义词库补全命令 |i_CTRL-X_CTRL-T| 的单词。
	参见 |compl-thesaurus|。

	如果为缓冲区或全局设置了 'thesaurusfunc'，则不使用此选项。

	要在文件名中包含逗号，请在逗号前加反斜杠。逗号后的空格被忽略，否则空格包含在
	文件名中。有关使用反斜杠的信息，请参见 |option-backslash|。在列表中添加或删除目录时，
	最好使用 |:set+=| 和 |:set-=|。这可以避免将来版本使用其他默认值时出现的问题。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项中不能使用反斜引号。

					*'thesaurusfunc'* *'tsrfu'*
'thesaurusfunc' 'tsrfu'	字符串	(默认 "")
			全局或本地到缓冲区 |global-local|
	此选项指定一个函数，用于使用 CTRL-X CTRL-T 进行同义词库补全。|i_CTRL-X_CTRL-T|
	参见 |compl-thesaurusfunc|。该值可以是函数名、|lambda| 或 |Funcref|。
	更多信息请参见 |option-value-function|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

				*'tildeop'* *'top'* *'notildeop'* *'notop'*
'tildeop' 'top'		布尔	(默认关闭)
			全局
	开启时，波浪号命令 "~" 的行为类似于操作符。

				*'timeout'* *'to'* *'notimeout'* *'noto'*
'timeout' 'to'		布尔	(默认开启)
			全局
	此选项和 'timeoutlen' 决定了当接收到部分映射的键序列时的行为。例如，如果按下了
	<c-f> 并且设置了 'timeout'，Nvim 将等待 'timeoutlen' 毫秒，以获取映射中可以跟随
	<c-f> 的任何键。

						*'timeoutlen'* *'tm'*
'timeoutlen' 'tm'	数字	(默认 1000)
			全局
	等待映射序列完成的毫秒数。

						*'title'* *'notitle'*
'title'			布尔	(默认关闭)
			全局
	如果启用，Nvim 将更新（GUI 或终端）窗口标题。格式由 'titlestring' 配置。默认情况下，
	它看起来像： >
		filename [+=-] (path) - Nvim
<
	其中： >
		filename	正在编辑的文件名
		-		表示文件不可修改，'ma' 关闭
		+		表示文件已被修改
		=		表示文件是只读的
		=+		表示文件是只读的且已被修改
		(path)		正在编辑文件的路径
		- Nvim		服务器名 |v:servername| 或 "Nvim"
<

							*'titlelen'*
'titlelen'		数字	(默认 85)
			全局
	给出用于窗口标题长度的 'columns' 的百分比。当标题较长时，仅显示路径名的结尾。
	路径名前使用 '<' 字符表示此情况。使用百分比使其适应窗口宽度。但它不能完美工作，
	因为实际可用的字符数还取决于使用的字体和标题栏中的其他内容。当 'titlelen' 为零时，
	使用完整路径。否则，可以使用 1 到 30000 之间的百分比值。
	'titlelen' 也用于 'titlestring' 选项。

							*'titleold'*
'titleold'		字符串	(默认 "")
			全局
	如果非空，此选项将在退出时用于设置窗口标题。仅当启用了 'title' 时。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

							*'titlestring'*
'titlestring'		字符串	(默认 "")
			全局
	格式化窗口标题，由 'title' 选项启用。

	包含 printf 样式的 "%" 项目，根据 'statusline' 的规则展开。如果 "%" 格式无效，
	则按原样使用，并且不会给出错误。

	默认（空）行为等同于： >vim
	    set titlestring=%t%(\ %M%)%(\ \(%{expand(\"%:~:h\")}\)%)%a\ -\ Nvim
<
	示例： >vim
	    auto BufEnter * let &titlestring = hostname() .. "/" .. expand("%:p")
	    set title titlestring=%<%F%=%l/%L-%P titlelen=70
<	'titlelen' 的值用于在可用空间的中间或右侧对齐项目。

	示例：使文件名位于首位： >vim
	    set titlestring=%t%(\ %M%)%(\ (%{expand(\"%:~:.:h\")})%)%(\ %a%)
<	注意使用 "%{ }" 和表达式来获取文件的路径，不包括文件名。只有在需要时，
	"%( %)" 结构才会添加分隔空格。

	注意：在 'titlestring' 中使用特殊字符可能会导致显示乱码（例如，当它包含 CR 或 NL 字符时）。

	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

					*'ttimeout'* *'nottimeout'*
'ttimeout'		布尔	(默认开启)
			全局
	此选项和 'ttimeoutlen' 决定了当 |TUI| 接收到部分按键代码序列时的行为。

	例如，如果接收到 <Esc>（\x1b 字节）并且设置了 'ttimeout'，Nvim 将等待 'ttimeoutlen'
	毫秒，以便终端完成按键代码序列。如果在超时之前没有输入到达，则假定为单个 <Esc>。
	许多 TUI 光标键代码以 <Esc> 开头。

	在非常慢的系统上，这可能会失败，导致光标键有时无法工作。如果你发现此问题，
	可以设置 ":set ttimeoutlen=9999"。Nvim 将等待 <Esc> 之后的下一个字符到达。

						*'ttimeoutlen'* *'ttm'*
'ttimeoutlen' 'ttm'	数字	(默认 50)
			全局
	等待按键代码序列完成的毫秒数。当部分命令已键入时，也用于 CTRL-\ CTRL-N 和
	CTRL-\ CTRL-G。

					*'undodir'* *'udir'* *E5003*
'undodir' 'udir'	字符串	(默认 "$XDG_STATE_HOME/nvim/undo//")
			全局
	撤销文件的目录名列表，以逗号分隔。有关格式的详细信息，请参见 'backupdir'。
	"." 表示使用文件所在的目录。"file.txt" 的撤销文件名是 ".file.txt.un~"。
	对于其他目录，文件名是编辑文件的完整路径，路径分隔符替换为 "%"。
	写入时：使用第一个存在的目录。"." 总是有效，"." 之后的目录不会用于写入。
	如果目录都不存在，Nvim 将尝试创建列表中的最后一个目录。
	读取时，尝试所有条目以查找撤销文件。使用第一个存在的撤销文件。如果无法读取，
	则给出错误，不再使用后续条目。
	参见 |undo-persistence|。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

	请注意，与 'directory' 和 'backupdir' 不同，'undodir' 总是表现得好像存在尾部斜杠
	（参见 'backupdir' 了解其含义）。

				*'undofile'* *'udf'* *'noundofile'* *'noudf'*
'undofile' 'udf'	布尔	(默认关闭)
			本地到缓冲区
	开启时，当将缓冲区写入文件时，Vim 自动将撤销历史保存到撤销文件，并在读取缓冲区时
	从同一文件恢复撤销历史。撤销文件存储的目录由 'undodir' 指定。有关此功能的更多信息，
	请参见 |undo-persistence|。当 'undoreload' 导致重新加载前的缓冲区被保存用于撤销时，
	不会读取撤销文件。当 'undofile' 关闭时，撤销文件不会被删除。

						*'undolevels'* *'ul'*
'undolevels' 'ul'	数字	(默认 1000)
			全局或本地到缓冲区 |global-local|
	可以撤销的最大更改数。由于撤销信息保存在内存中，较高的数字将导致使用更多内存。
	尽管如此，单个更改可能已经使用了大量内存。设置为 0 以实现 Vi 兼容性：一级撤销，
	"u" 撤销自身： >vim
		set ul=0
<	但你也可以通过将 'u' 标志包含在 'cpoptions' 中来实现 Vi 兼容性，同时仍然能够使用
	CTRL-R 重复撤销。另请参见 |undo-two-ways|。
	设置为 -1 以完全禁用撤销。你可能只想对当前缓冲区这样做： >vim
		setlocal ul=-1
<	当单个更改耗尽内存时，这很有帮助。

	当要使用全局值时，本地值设置为 -123456。

	另请参见 |clear-undo|。

						*'undoreload'* *'ur'*
'undoreload' 'ur'	数字	(默认 10000)
			全局
	重新加载缓冲区时，保存整个缓冲区以便撤销。这适用于 ":e!" 命令以及当缓冲区在 Vim
	外部发生更改时重新加载。|FileChangedShell|
	仅当此选项为负数或行数小于此选项的值时，才会进行保存。将此选项设置为零以禁用
	重新加载时的撤销。

	为重新加载保存撤销时，不会读取任何撤销文件。

	注意，这会导致整个缓冲区存储在内存中。如果内存不足，请将此选项设置为较低的值。

						*'updatecount'* *'uc'*
'updatecount' 'uc'	数字	(默认 200)
			全局
	键入这么多字符后，交换文件将被写入磁盘。当为零时，根本不会创建交换文件
	（参见恢复章节 |crash-recovery|）。使用 "-n" 选项启动 Vim 时，'updatecount' 被
	设置为零，参见 |startup|。以只读模式编辑时，此选项将初始化为 10000。
	可以通过 'swapfile' 按缓冲区禁用交换文件。
	当 'updatecount' 从零设置为非零时，将为所有设置了 'swapfile' 的缓冲区创建交换文件。
	当 'updatecount' 设置为零时，现有交换文件不会被删除。
	在 'buftype' 为 "nofile" 或 "nowrite" 的缓冲区中，此选项没有意义。

						*'updatetime'* *'ut'*
'updatetime' 'ut'	数字	(默认 4000)
			全局
	如果在此毫秒数内没有键入任何内容，交换文件将被写入磁盘（参见 |crash-recovery|）。
	也用于 |CursorHold| 自动命令事件。

					*'varsofttabstop'* *'vsts'*
'varsofttabstop' 'vsts'	字符串	(默认 "")
			本地到缓冲区
	定义可变宽度的软制表位。该值是一个逗号分隔的列宽列表。每个宽度定义到下一个软制表位
	之前的列数。最后一个值无限重复。

	例如，在编辑汇编语言文件时，语句从第 9 列开始，注释从第 41 列开始，使用以下设置可能
	很有用： >vim
		set varsofttabstop=8,32,8
<	这将在第 8 列设置软制表位，然后在第 40 列（8 + 32），之后每 8 列一个。

	注意：此设置会覆盖 'softtabstop'。有关 Vim 如何处理制表符和空格的详细说明，
	请参见用户手册 |30.5| 节。

						*'vartabstop'* *'vts'*
'vartabstop' 'vts'	字符串	(默认 "")
			本地到缓冲区
	定义可变宽度的制表位。该值是一个逗号分隔的列宽列表。每个宽度定义到下一个制表位
	之前的列数；最后一个值无限重复。

	例如： >
		:set vartabstop=4,8
<	这将第一个制表位放在从行首开始的第 4 列，随后的每个制表位间隔 8 列。

	注意：此设置会覆盖 'tabstop'。在 UNIX 上，建议保持默认的 tabstop 值 8。考虑设置
	'varsofttabstop'。有关 Vim 如何处理制表符和空格的详细说明，请参见用户手册 |30.5| 节。

						*'verbose'* *'vbs'*
'verbose' 'vbs'		数字	(默认 0)
			全局
	设置详细级别。也可通过 |-V| 和 |:verbose| 设置。

	对选项、映射等赋值进行 Lua 脚本跟踪在级别 1 启用；当 'verbose' 为 0 时，
	不跟踪 Lua 脚本，以保持性能。

	如果大于或等于给定级别，Nvim 会产生以下消息：

	级别   消息 ~
	----------------------------------------------------------------------
	1	启用 Lua 跟踪（见上文）。不产生消息。
	2	当文件被 ":source" 引用时，或读取或写入 |shada| 文件时。
	3	UI 信息，终端能力。
	4	Shell 命令。
	5	每个被搜索的标签文件和包含文件。
	8	执行一组自动命令的文件。
	9	已执行的自动命令。
	11	在路径中查找项目。
	12	Vimscript 函数调用。
	13	当抛出、捕获、完成或丢弃异常时。
	14	":finally" 子句中任何待处理的内容。
	15	来自脚本的 Ex 命令（截断为 200 个字符）。
	16	Ex 命令。

	如果设置了 'verbosefile'，则不会显示详细消息。

						*'verbosefile'* *'vfile'*
'verbosefile' 'vfile'	字符串	(默认 "")
			全局
	当非空时，所有消息都写入具有此名称的文件中。当文件存在时，消息被追加。
	当 Vim 退出或 'verbosefile' 被置空时，写入文件结束。写入是缓冲的，因此可能不会
	立即显示。将 'verbosefile' 设置为新值相当于先将其置空。
	与 |:redir| 的区别在于，当设置了 'verbosefile' 时，不会显示详细消息。
	环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'viewdir'* *'vdir'*
'viewdir' 'vdir'	字符串	(默认 "$XDG_STATE_HOME/nvim/view//")
			全局
	存储 |:mkview| 文件的目录名称。环境变量会被展开 |:set_env|。
	出于安全原因，此选项不能从 |modeline| 或 |sandbox| 中设置。

						*'viewoptions'* *'vop'*
'viewoptions' 'vop'	字符串	(默认 "folds,cursor,curdir")
			全局
	更改 |:mkview| 命令的效果。它是一个逗号分隔的单词列表。每个单词启用保存和恢复
	某些内容：
	   单词		保存和恢复 ~
	   cursor	文件中的光标位置和窗口中的光标位置
	   curdir	本地当前目录，如果使用 |:lcd| 设置
	   folds	手动创建的折叠、打开/关闭的折叠和本地折叠选项
	   options	窗口或缓冲区本地的选项和映射（不是本地选项的全局值）
	   localoptions	同 "options"
	   slash	|deprecated| 始终启用。在文件名中使用 "/"。
	   unix		|deprecated| 始终启用。使用 "\n" 行尾。

						*'virtualedit'* *'ve'*
'virtualedit' 've'	字符串	(默认 "")
			全局或本地到窗口 |global-local|
	一个逗号分隔的单词列表：
	    block	允许在可视块模式下进行虚拟编辑。
	    insert	允许在插入模式下进行虚拟编辑。
	    all		允许在所有模式下进行虚拟编辑。
	    onemore	允许光标移动到行尾之后的一个位置。
	    none	当用作本地值时，即使设置了全局值，也不允许虚拟编辑。
			当用作全局值时，"none" 等同于 ""。
	    NONE	"none" 的替代拼写。

	虚拟编辑意味着光标可以定位在没有实际字符的位置。这可以是制表符的中间或行尾之后。
	这对于在可视模式下选择矩形和编辑表格很有用。
	"onemore" 不同，它只允许将光标移动到行最后一个字符之后。这使得一些命令更加一致。
	以前，如果行为空，光标总是在行尾之后。但这与 Vi 兼容性相去甚远。它也可能会破坏
	一些插件或 Vim 脚本。例如，因为 |l| 可以将光标移动到最后一个字符之后。请小心使用！
	使用 `$` 命令将移动到行中的最后一个字符，而不是其之后。这实际上可能将光标向左移动！
	`g$` 命令将移动到屏幕行的末尾。
	将 "all" 与 "onemore" 结合没有意义，但不会收到警告。
	当与其他单词结合时，"none" 被忽略。

			*'visualbell'* *'vb'* *'novisualbell'* *'novb'*
'visualbell' 'vb'	布尔	(默认关闭)
			全局
	使用可视响铃而不是蜂鸣声。另请参见 'errorbells'。

						*'warn'* *'nowarn'*
'warn'			布尔	(默认开启)
			全局
	当缓冲区已被修改时使用 shell 命令时给出警告消息。

						*'whichwrap'* *'ww'*
'whichwrap' 'ww'	字符串	(默认 "b,s")
			全局
	允许指定的左右移动光标键在光标位于行首/行尾时移动到上一行/下一行。连接字符以允许
	这些键具有此行为：
	 字符   键	 模式	~
	 b    <BS>	 普通和可视
	 s    <Space>	 普通和可视
	 h    "h"	 普通和可视（不推荐）
	 l    "l"	 普通和可视（不推荐）
	 <    <Left>	 普通和可视
	 >    <Right>	 普通和可视
	 ~    "~"	 普通
	 [    <Left>	 插入和替换
	 ]    <Right>	 插入和替换
	例如： >vim
		set ww=<,>,[,]
<	仅当使用光标键时才允许换行。
	当移动键与删除或更改操作符结合使用时，<EOL> 也计为一个字符。这使得当光标越过行尾时，
	"3h" 与 "3dh" 不同。对于 "x" 和 "X" 也是如此，因为它们与 "dl" 和 "dh" 作用相同。
	如果你使用此功能，你可能还想使用映射 ":map <BS> X" 使退格键删除光标前的字符。
	当包含 'l' 并且它在行尾（非空行）的操作符之后使用时，它将不会移动到下一行。这使得
	"dl"、"cl"、"yl" 等正常工作。

						*'wildchar'* *'wc'*
'wildchar' 'wc'		数字	(默认 <Tab>)
			全局
	你必须键入以在命令行中开始通配符展开的字符，如 'wildmode' 所指定。
	更多信息请见：|cmdline-completion|。
	在宏内部使用时，该字符不会被识别。请参见 'wildcharm'。
	某些键不起作用，例如 CTRL-C、<CR> 和 Enter。可以使用 <Esc>，但连续按两次仍会退出
	命令行，作为一种安全措施。
	虽然 'wc' 是一个数字选项，但它可以指定为数字、单个字符、|key-notation|
	（例如 <Up>、<C-F>）或前面带有脱字符的字母（例如 `^F` 是 CTRL-F）： >vim
		:set wc=27
		:set wc=X
		:set wc=^I
		set wc=<Tab>
<	'wildchar' 也在搜索模式上下文中启用补全，例如 |/|、|?|、|:s|、|:g|、|:v| 和 |:vim|。
	要插入字面 <Tab> 而不是触发补全，请键入 <C-V><Tab> 或 "\t"。
	另请参见 'wildoptions' 和 |wildtrigger()|。

						*'wildcharm'* *'wcm'*
'wildcharm' 'wcm'	数字	(默认 0)
			全局
	'wildcharm' 的工作方式与 'wildchar' 完全相同，只是它在宏内部使用时会被识别。
	你可以通过查看 |ex-edit-index| 找到适合此选项的“备用”命令行键。通常你永远不会真正
	键入 'wildcharm'，只在自动调用补全模式的映射中使用它，例如： >vim
		set wcm=<C-Z>
		cnoremap ss so $vim/sessions/*.vim<C-Z>
<	然后，在输入 :ss 后，你可以使用 CTRL-P 和 CTRL-N。

						*'wildignore'* *'wig'*
'wildignore' 'wig'	字符串	(默认 "")
			全局
	一个文件模式列表。当展开 |wildcards|、补全文件或目录名称时，匹配这些模式之一的文件
	将被忽略，并且会影响 |expand()|、|glob()| 和 |globpath()| 的结果，除非传递标志以
	禁用此行为。模式的使用方式与 |:autocmd| 相同，参见 |autocmd-pattern|。
	另请参见 'suffixes'。
	示例： >vim
		set wildignore=*.o,*.obj
<	在列表中添加或删除模式时，最好使用 |:set+=| 和 |:set-=|。这可以避免将来版本使用
	其他默认值时出现的问题。

		*'wildignorecase'* *'wic'* *'nowildignorecase'* *'nowic'*
'wildignorecase' 'wic'	布尔	(默认关闭)
			全局
	设置后，在补全文件名和目录时忽略大小写。当设置了 'fileignorecase' 时无效。
	当使用 shell 展开通配符时（当存在特殊字符时发生）不适用。

			*'wildmenu'* *'wmnu'* *'nowildmenu'* *'nowmnu'*
'wildmenu' 'wmnu'	布尔	(默认开启)
			全局
	当 'wildmenu' 开启时，命令行补全以增强模式运行。按下 'wildchar'（通常是 <Tab>）
	调用补全时，会显示可能的匹配项。
	当 'wildoptions' 包含 "pum" 时，补全匹配项显示在弹出菜单中。否则，它们显示在命令行
	正上方，第一个匹配项高亮显示（覆盖状态行，如果有的话）。显示上一个/下一个匹配项的键，
	例如 <Tab> 或 CTRL-P/CTRL-N，会使高亮移动到相应的匹配项。'wildmode' 必须指定 "full"：
	"longest" 和 "list" 不会启动 'wildmenu' 模式。你可以使用 |wildmenumode()| 检查当前模式。
	当按下一个不用于选择补全的键时，菜单将被取消。

	当菜单激活时，这些键具有特殊含义：
	CTRL-P		- 转到上一个条目
	CTRL-N		- 转到下一个条目
	<Left> <Right>	- 选择上一个/下一个匹配项（如 CTRL-P/CTRL-N）
	<PageUp>	- 选择后面几个条目
	<PageDown>	- 选择前面几个条目
	<Up>		- 在文件名/菜单名补全中：向上移动到父目录或父菜单。
	<Down>		- 在文件名/菜单名补全中：向下移动到子目录或子菜单。
	<CR>		- 在菜单补全中，当光标紧跟在点之后时：向下移动到子菜单。
	CTRL-E		- 结束补全，返回到选择匹配项之前的内容。
	CTRL-Y		- 接受当前选中的匹配项并停止补全。

	如果你希望 <Left> 和 <Right> 移动光标而不是选择不同的匹配项，请使用： >vim
		cnoremap <Left> <Space><BS><Left>
		cnoremap <Right> <Space><BS><Right>
<
	|hl-WildMenu| 高亮显示当前匹配项。

						*'wildmode'* *'wim'*
'wildmode' 'wim'	字符串	(默认 "full")
			全局
	用于 'wildchar' 指定字符的补全模式。此选项是一个最多四部分的逗号分隔列表，分别对应
	第一次、第二次、第三次和第四次按下 'wildchar'。每个部分是一个冒号分隔的补全行为列表，
	这些行为在该阶段同时应用。

	可能的补全行为值是：
	""		仅补全（插入）第一个匹配项。不循环或列出更多匹配项。
	"full"		补全下一个完整匹配项。循环遍历所有匹配项，在最后一个匹配项后返回
			原始输入。如果启用了 'wildmenu'，则会显示它。
	"longest"	补全到最长公共子串。如果这没有扩展输入，则使用下一个 'wildmode' 部分。
	"list"		如果找到多个匹配项，则列出所有匹配项。
	"lastused"	补全缓冲区名称时，按最近使用排序（排除当前缓冲区）。仅适用于缓冲区
			名称补全。
	"noselect"	如果启用了 'wildmenu'，则显示菜单但不预选第一个项目。
	如果只有一个匹配项，则完全补全它，除非指定了 "noselect"。

	一些有用的冒号分隔值组合：
	"longest:full"		以最长公共字符串开始，并显示 'wildmenu'（如果启用）。不循环
				遍历完整匹配项。
	"list:full"		列出所有匹配项并补全第一个匹配项。
	"list:longest"		列出所有匹配项并补全到最长公共前缀。
	"list:lastused"		列出所有匹配项。补全缓冲区时，按最近使用排序（排除当前缓冲区）。
	"noselect:lastused"	如果 'wildmenu' 激活，则不预选第一个项目。补全缓冲区时，
				按最近使用排序（排除当前缓冲区）。

	示例： >vim
		set wildmode=full
<	每次按下都补全完整匹配项（默认行为） >vim
		set wildmode=longest,full
<	第一次按下：最长公共子串
	第二次按下：循环遍历完整匹配项 >vim
		set wildmode=list:full
<	第一次按下：列出所有匹配项并补全第一个 >vim
		set wildmode=list,full
<	第一次按下：仅列出匹配项
	第二次按下：补全完整匹配项 >vim
		set wildmode=longest,list
<	第一次按下：最长公共子串
	第二次按下：列出所有匹配项 >vim
		set wildmode=noselect:full
<	第一次按下：显示 'wildmenu'，但不补全或选择
	第二次按下：循环完整匹配项 >vim
		set wildmode=noselect:lastused,full
<	同上，但缓冲区匹配项按最后使用时间排序
	更多信息请见：|cmdline-completion|。

						*'wildoptions'* *'wop'*
'wildoptions' 'wop'	字符串	(默认 "pum,tagfile")
			全局
	一个单词列表，用于改变 |cmdline-completion| 的执行方式。支持以下值：
	  exacttext	当存在此标志时，搜索模式补全（例如在 |/|、|?|、|:s|、|:g|、|:v| 和
			|:vim| 中）将确切的缓冲区文本显示为菜单项，而不保留正则表达式构件，
			如位置锚点（例如 |/\<|）。这提供了更直观的菜单项，与实际的缓冲区文本
			匹配。然而，由于模式未被精确保留，搜索可能不太准确。默认情况下，
			Vim 保留键入的模式（带锚点）并附加匹配的单词。这保留了搜索的正确性，
			尤其是在使用正则表达式或启用 'smartcase' 时。然而，附加的匹配单词的
			大小写可能与缓冲区中单词的大小写不完全匹配。
	  fuzzy		使用 |fuzzy-matching| 查找补全匹配项。指定此值时，通配符展开将不会
			用于补全。匹配项将按“最佳匹配”排序，而不是按字母顺序排序。这将比
			通配符展开找到更多的匹配项。目前，基于模糊匹配的补全不支持文件和目录名，
			而是使用通配符展开。
	  pum		使用弹出菜单显示补全匹配项，样式与 |ins-completion-menu| 相同。
	  tagfile	使用 CTRL-D 列出匹配的标签时，列出标签的种类和标签的文件。每行只显示
			一个匹配项。常用的标签种类有：
				d	#define
				f	function

	此选项不适用于 |ins-completion|。请参见 'completeopt'。

						*'winaltkeys'* *'wak'*
'winaltkeys' 'wak'	字符串	(默认 "menu")
			全局
			仅在 Win32 中使用
	某些 GUI 版本允许通过使用 ALT 键与菜单中带下划线的字符组合来访问菜单条目。这与将
	ALT 键用于映射和输入特殊字符冲突。此选项指示如何处理：
	  no	不要将 ALT 键用于菜单。可以映射 ALT 键组合，但没有自动处理。
	  yes	ALT 键处理由窗口系统完成。ALT 键组合无法映射。
	  menu	ALT 与菜单快捷键字符的组合将由窗口系统处理。其他键可以映射。
	如果通过从 'guioptions' 中排除 'm' 来禁用菜单，则 ALT 键永远不会用于菜单。
	此选项不用于 <F10>；在 Win32 上。

						*'winbar'* *'wbr'*
'winbar' 'wbr'		字符串	(默认 "")
			全局或本地到窗口 |global-local|
	当非空时，此选项启用窗口栏并确定其内容。窗口栏是在每个启用它的窗口顶部显示的一个栏。
	'winbar' 的值评估方式与 'statusline' 相同。

	当更改 'winbar' 中使用的内容但未触发其更新时，请使用 |:redrawstatus|。

	浮动窗口不使用 'winbar' 的全局值。要使浮动窗口具有窗口栏，必须为其设置窗口本地值
	的 'winbar'。

	当 'modelineexpr' 关闭时，此选项不能在 modeline 中设置。

						*'winblend'* *'winbl'*
'winblend' 'winbl'	数字	(默认 0)
			本地到窗口
	为浮动窗口启用伪透明效果。有效值范围为 0（完全不透明，禁用）到 100（完全透明背景）。
	0-30 之间的值通常最有用。

	依赖于 UI。与 RGB 颜色配合使用效果最佳。'termguicolors'

							*'winborder'*
'winborder'		字符串	(默认 "")
			全局
	定义浮动窗口的默认边框样式。默认值为空，等同于 "none"。有效值包括：
	- "bold": 粗线框。
	- "double": 双线框。
	- "none": 无边框。
	- "rounded": 类似于 "single"，但带圆角（"╭" 等）。
	- "shadow": 阴影效果，通过与背景混合实现。
	- "single": 单线框。
	- "solid": 通过单个空白单元格添加填充。
	- 自定义: 逗号分隔的恰好 8 个字符的列表，按顺时针顺序从左上角开始。示例： >lua
	     vim.o.winborder='+,-,+,|,+,-,+,|'
<

							*'window'* *'wi'*
'window' 'wi'		数字	(默认屏幕高度 - 1)
			全局
	当只有一个窗口且该值小于 'lines' 减一时，用于 |CTRL-F| 和 |CTRL-B| 的窗口高度。
	屏幕将滚动 'window' 减两行，最少一行。当 'window' 等于 'lines' 减一时，CTRL-F 和
	CTRL-B 以更智能的方式滚动，处理换行。当调整 Vim 窗口大小，且该值小于 1 或大于等于
	'lines' 时，它将被设置为 'lines' 减 1。注意：不要将此与 Vim 窗口的高度混淆，
	请使用 'lines'。

			*'winfixbuf'* *'wfb'* *'nowinfixbuf'* *'nowfb'*
'winfixbuf' 'wfb'	布尔	(默认关闭)
			本地到窗口
	如果启用，窗口及其显示的缓冲区将被配对。例如，尝试使用 |:edit| 更改缓冲区将失败。
	其他更改窗口缓冲区的命令，如 |:cnext|，也将跳过任何启用了 'winfixbuf' 的窗口。
	然而，如果 Ex 命令带有 "!" 修饰符，则可以强制切换缓冲区。

			*'winfixheight'* *'wfh'* *'nowinfixheight'* *'nowfh'*
'winfixheight' 'wfh'	布尔	(默认关闭)
			本地到窗口  |local-noglobal|
	当打开或关闭窗口且设置了 'equalalways' 时，保持窗口高度。也适用于 |CTRL-W_=|。
	默认情况下为 |preview-window| 和 |quickfix-window| 设置。当空间不足时，高度仍可能改变。

			*'winfixwidth'* *'wfw'* *'nowinfixwidth'* *'nowfw'*
'winfixwidth' 'wfw'	布尔	(默认关闭)
			本地到窗口  |local-noglobal|
	当打开或关闭窗口且设置了 'equalalways' 时，保持窗口宽度。也适用于 |CTRL-W_=|。
	当空间不足时，宽度仍可能改变。

						*'winheight'* *'wh'* *E591*
'winheight' 'wh'	数字	(默认 1)
			全局
	当前窗口的最小行数。这不是一个硬性最小值，如果空间不足，Vim 将使用更少的行。
	如果焦点转移到较小的窗口，其大小会增加，代价是其他窗口的高度。对于正常编辑，
	将 'winheight' 设置为一个小数字。将其设置为 999 使当前窗口填满屏幕的大部分区域。
	其他窗口将只有 'winminheight' 高。这有一个缺点，即 ":all" 将只创建两个窗口。
	为了避免 "vim -o 1 2 3 4" 只创建两个窗口，请在启动完成后使用 |VimEnter| 事件设置
	此选项： >vim
		au VimEnter * set winheight=999
<	最小值为 1。在更改当前窗口高度的命令之一之后，高度不会被调整。'winheight' 适用于
	当前窗口。使用 'winminheight' 设置其他窗口的最小高度。

					*'winhighlight'* *'winhl'*
'winhighlight' 'winhl'	字符串	(默认 "")
			本地到窗口
	窗口本地高亮。逗号分隔的高亮 |group-name| 对列表 "{hl-from}:{hl-to},..."，
	其中每个 {hl-from} 是一个 |highlight-groups| 项，在窗口中被 {hl-to} 组覆盖。

	注意：高亮命名空间优先于 'winhighlight'。参见 |nvim_win_set_hl_ns()| 和
	|nvim_set_hl()|。

	垂直分隔符的高亮由分隔符左侧的窗口决定。标签页的 'tabline' 高亮由标签页中最后聚焦的
	窗口决定。弹出菜单的高亮由当前窗口决定。消息区域中的高亮无法覆盖。

	示例：为非当前窗口显示不同的颜色： >vim
		set winhighlight=Normal:MyNormal,NormalNC:MyNormalNC
<

						*'winminheight'* *'wmh'*
'winminheight' 'wmh'	数字	(默认 1)
			全局
	当窗口不是当前窗口时，窗口的最小高度。这是一个硬性最小值，窗口永远不会变得更小。
	当设置为零时，如有必要，窗口可能会被“压缩”到零行（即仅状态栏）。当它们变为活动状态时，
	它们将恢复到至少一行（因为光标必须有地方去）。使用 'winheight' 设置当前窗口的最小高度。
	此选项仅在缩小窗口时检查。不要使用大数字，否则打开多个窗口时会出错。0 到 3 的值是
	合理的。

						*'winminwidth'* *'wmw'*
'winminwidth' 'wmw'	数字	(默认 1)
			全局
	当窗口不是当前窗口时，窗口的最小宽度。这是一个硬性最小值，窗口永远不会变得更小。
	当设置为零时，如有必要，窗口可能会被“压缩”到零列（即仅垂直分隔符）。当它们变为
	活动状态时，它们将恢复到至少一列（因为光标必须有地方去）。使用 'winwidth' 设置当前
	窗口的最小宽度。此选项仅在缩小窗口时检查。不要使用大数字，否则打开多个窗口时会出错。
	0 到 12 的值是合理的。

						*'winwidth'* *'wiw'* *E592*
'winwidth' 'wiw'	数字	(默认 20)
			全局
	当前窗口的最小列数。这不是一个硬性最小值，如果空间不足，Vim 将使用更少的列。
	如果当前窗口较小，其大小会增加，代价是其他窗口的宽度。将其设置为 999 使当前窗口始终
	填满屏幕。对于正常编辑，将其设置为一个小数字。在更改当前窗口宽度的命令之一之后，
	宽度不会被调整。'winwidth' 适用于当前窗口。使用 'winminwidth' 设置其他窗口的最小宽度。

						*'wrap'* *'nowrap'*
'wrap'			布尔	(默认开启)
			本地到窗口
	此选项更改文本的显示方式。它不更改缓冲区中的文本，请参见 'textwidth'。
	开启时，长于窗口宽度的行将换行，并在下一行继续显示。关闭时，行不会换行，只有长行的
	部分会显示。当光标移动到未显示的部分时，屏幕将水平滚动。如有必要，行会在单词中间断开。
	参见 'linebreak' 以在单词边界处断开。为了使水平滚动更有用，请尝试： >vim
		set sidescroll=5
		set listchars+=precedes:<,extends:>
<	参见 'sidescroll'、'listchars' 和 |wrap-off|。
	当 'diff' 选项开启时，此选项不能从 |modeline| 设置。
	如果 'nowrap' 是从 |modeline| 或 |sandbox| 中设置的，无论 'list' 和 'listchars' 选项
	的值如何，都将使用 '>' 作为 |lcs-extends| 字符。这是为了防止视口外的恶意代码不被注意。
	之后可以使用 `:setlocal nowrap` 手动禁用此行为。

						*'wrapmargin'* *'wm'*
'wrapmargin' 'wm'	数字	(默认 0)
			本地到缓冲区
	从右窗口边框开始换行的字符数。当键入的文本超过此限制时，将插入一个 <EOL>，并继续在
	下一行插入。添加边距的选项，如 'number' 和 'foldcolumn'，会导致文本宽度进一步减小。
	当 'textwidth' 非零时，不使用此选项。另请参见 'formatoptions' 和 |ins-textwidth|。

			*'wrapscan'* *'ws'* *'nowrapscan'* *'nows'* *E384* *E385*
'wrapscan' 'ws'		布尔	(默认开启)
			全局
	搜索在文件末尾回绕。也适用于 |]s| 和 |[s|，搜索拼写错误。

						*'write'* *'nowrite'*
'write'			布尔	(默认开启)
			全局
	允许写入文件。未设置时，不允许写入文件。可用于只读模式，其中仍然允许修改文本。
	可以使用 |-m| 或 |-M| 命令行参数重置。过滤文本仍然是可能的，尽管这需要写入临时文件。

				*'writeany'* *'wa'* *'nowriteany'* *'nowa'*
'writeany' 'wa'		布尔	(默认关闭)
			全局
	允许写入任何文件，无需使用 "!" 覆盖。

			*'writebackup'* *'wb'* *'nowritebackup'* *'nowb'*
'writebackup' 'wb'	布尔	(默认开启)
			全局
	在覆盖文件之前创建备份。文件成功写入后，备份将被删除，除非 'backup' 选项也开启。
	警告：关闭此选项意味着当 Vim 未能正确写入缓冲区，然后无论出于何种原因 Vim 退出时，
	你将同时丢失原始文件和正在写入的内容。仅当文件系统几乎已满且导致写入失败时，才重置
	此选项（并确保在写入成功之前不要退出 Vim）。参见 |backup-table| 了解另一种解释。
	当 'backupskip' 模式匹配时，无论如何都不会创建备份。根据 'backupcopy'，备份可以是
	一个新文件或重命名的原始文件（并写入一个新文件）。

						*'writedelay'* *'wd'*
'writedelay' 'wd'	数字	(默认 0)
			全局
	仅与 'redrawdebug' 一起生效。每行或每次刷新后等待的毫秒数。

 vim:tw=78:ts=8:noet:ft=help:norl:
