*pattern.txt*   Nvim


		  VIM 参考手册	  by Bram Moolenaar


模式和搜索命令				*pattern-searches*

基础知识可以在用户手册的 |03.9| 节找到。更多解释在第二十七章 |usr_27.txt|。

                                      输入 |gO| 查看目录。

==============================================================================
1. 搜索命令				*search-commands*

							*/*
/{pattern}[/]<CR>	向前搜索第 [count] 次出现的 {pattern} |exclusive|。

/{pattern}/{offset}<CR>	向前搜索第 [count] 次出现的 {pattern}，并向上或向下移动 |{offset}| 行。|linewise|。

							*/<CR>*
/<CR>			向前搜索最新使用的模式 |last-pattern| 的第 [count] 次出现，使用最新使用的 |{offset}|。

//{offset}<CR>		向前搜索最新使用的模式 |last-pattern| 的第 [count] 次出现，使用新的 |{offset}|。如果 {offset} 为空，则不使用偏移量。

							*?*
?{pattern}[?]<CR>	向后搜索第 [count] 次出现的 {pattern} |exclusive|。

?{pattern}?{offset}<CR>	向后搜索第 [count] 次出现的 {pattern}，并向上或向下移动 |{offset}| 行 |linewise|。

							*?<CR>*
?<CR>			向后搜索最新使用的模式 |last-pattern| 的第 [count] 次出现，使用最新使用的 |{offset}|。

??{offset}<CR>		向后搜索最新使用的模式 |last-pattern| 的第 [count] 次出现，使用新的 |{offset}|。如果 {offset} 为空，则不使用偏移量。

							*n*
n			重复最新的 "/" 或 "?" [count] 次。
			如果光标没有移动，则以 count + 1 重复搜索。
			|last-pattern|

							*N*
N			以相反方向重复最新的 "/" 或 "?" [count] 次。|last-pattern|

							*star* *E348* *E349*
*			向前搜索距离光标最近的单词的第 [count] 次出现。用于搜索的单词是以下中的第一个：
				1. 光标下的关键字 'iskeyword'
				2. 光标之后，在当前行中的第一个关键字
				3. 光标下的非空白单词
				4. 光标之后，在当前行中的第一个非空白单词
			仅搜索整个关键字，如同使用命令 "/\<keyword\>" 一样。|exclusive|
			使用 'ignorecase'，不使用 'smartcase'。
							*v_star-default*
{Visual}*		在可视模式下，向前搜索当前选中的内容。
			|default-mappings|

							*#*
#			与 "*" 相同，但向后搜索。井号（字符 163）同样有效。如果 "#" 键作为退格键工作，请在启动 Vim 前尝试使用 "stty erase <BS>"。
							*v_#-default*
{Visual}#		在可视模式下，向后搜索当前选中的内容。
			|default-mappings|

							*gstar*
g*			类似于 "*"，但不在单词周围添加 "\<" 和 "\>"。
			这使得搜索也能找到不是完整单词的匹配项。

							*g#*
g#			类似于 "#"，但不在单词周围添加 "\<" 和 "\>"。
			这使得搜索也能找到不是完整单词的匹配项。

							*gd*
gd			跳转到局部声明。当光标位于局部变量上时，此命令将跳转到其声明处。
			这是为 C 代码设计的，在其他语言中可能效果不佳。
			首先 Vim 搜索当前函数的开始，就像 "[[". 一样。如果未找到，则在第 1 行停止搜索。如果找到，Vim 会往回搜索直到找到一个空行。从这个位置开始，Vim 搜索光标下的关键字，如同使用 "*" 一样，但会忽略看起来像注释的行（参见 'comments' 选项）。
			请注意，这不能保证有效，Vim 实际上并不检查语法，它只搜索与关键字的匹配。如果需要搜索包含的文件，请使用 |include-search| 中列出的命令。
			在此命令之后，|n| 向前搜索下一个匹配项（不是向后）。

							*gD*
gD			跳转到全局声明。当光标位于文件中定义的全局变量上时，此命令将跳转到其声明处。其工作方式类似于 "gd"，只是关键字的搜索总是从第 1 行开始。

							*1gd*
1gd			类似于 "gd"，但忽略在光标位置之前结束的 {} 块内的匹配项。

							*1gD*
1gD			类似于 "gD"，但忽略在光标位置之前结束的 {} 块内的匹配项。

							*CTRL-C*
CTRL-C			中断当前（搜索）命令。
			在普通模式下，任何挂起的命令都会被中止。

							*:noh* *:nohlsearch*
:noh[lsearch]		停止为 'hlsearch' 选项高亮显示。再次使用搜索命令或设置 'hlsearch' 选项时，它会自动重新开启。
			此命令在自动命令中不起作用，因为执行自动命令时会保存和恢复高亮状态 |autocmd-searchpat|。
			调用用户函数时也是如此。


在输入搜索模式时，如果 'incsearch' 选项开启，则会显示当前匹配项。请记住，您仍然需要用 <CR> 完成搜索命令，才能将光标实际定位到显示的匹配项上。或者使用 <Esc> 放弃搜索。

							*nohlsearch-auto*
如果设置了 'hlsearch' 选项，最后使用的搜索模式的所有匹配项都会被高亮显示。这可以通过 |:nohlsearch| 命令暂停，或者通过 nohlsearch 插件自动暂停。参见 |nohlsearch-install|。


当 'shortmess' 不包含 "S" 标志时，Vim 会自动显示光标所在位置的索引。可能如下所示： >

  [1/5]		光标位于 5 个匹配项中的第一个。
  [1/>99]	光标位于超过 99 个匹配项中的第一个。
  [>99/>99]	光标位于超过 99 个匹配项中的第 99 个之后。
  [?/??]	未知存在多少个匹配项，由于搜索超时，生成统计信息被中止。

注意：计数不考虑偏移量。

当找不到匹配项时，您会收到错误：*E486* 未找到模式
请注意，对于 `:global` 命令，为了 Vi 兼容性，您会收到一条普通消息 "Pattern not found"。
对于 |:s| 命令，可以使用 "e" 标志来避免错误消息 |:s_flags|。

					*search-options*
以下选项影响 Vim 中执行搜索的方式：
    'hlsearch'		高亮显示匹配项
    'ignorecase'	搜索时忽略大小写
    'imsearch'		输入搜索模式时使用输入法
    'incsearch'		在键入模式时增量显示匹配项
    'maxsearchcount'	搜索计数的最大值 |shm-S|
    'shortmess'		抑制消息 |shm-s|；显示搜索计数 |shm-S|
    'smartcase'		如果模式包含大写字母，则覆盖 'ignorecase'
    'wrapscan'		从文件开头继续搜索

					*search-offset* *{offset}*
这些命令搜索指定的模式。对于 "/" 和 "?"，可以指定一个额外的偏移量。有两种类型的偏移量：行偏移量和字符偏移量。

偏移量给出光标相对于找到的匹配项的位置：
    [num]	向下 [num] 行，在第 1 列
    +[num]	向下 [num] 行，在第 1 列
    -[num]	向上 [num] 行，在第 1 列
    e[+num]	在匹配项结束后向右 [num] 个字符
    e[-num]	在匹配项结束后向左 [num] 个字符
    s[+num]	在匹配项开始后向右 [num] 个字符
    s[-num]	在匹配项开始后向左 [num] 个字符
    b[+num]	与上面的 s[+num] 相同（助记符：begin）
    b[-num]	与上面的 s[-num] 相同（助记符：begin）
    ;{pattern}  执行另一次搜索，参见 |//;|

如果给出了 '-' 或 '+' 但省略了 [num]，则使用计数 1。
当包含带有 'e' 的偏移量时，搜索变为包含性（光标所在的字符包含在操作中）。

示例：

模式			光标位置 ~
/test/+1		"test" 下面一行，第 1 列
/test/e			在 "test" 的最后一个 t 上
/test/s+2		在 "test" 的 's' 上
/test/b-3		"test" 前三个字符

如果在操作符之后使用这些命令之一，则会影响光标之前和之后搜索位置之间的字符。但是，如果给出了行偏移量，则会影响两个光标位置之间的所有行。

一个使用模式搜索匹配项并用另一个单词更改匹配项的示例： >
	/foo<CR>	查找 "foo"
	c//e<CR>	更改到匹配项末尾
	bar<Esc>	输入替换文本
	//<CR>		转到下一个匹配项的开始
	c//e<CR>	更改到匹配项末尾
	beep<Esc>	输入另一个替换文本
			等等。
<
							*//;* *E386*
一个非常特殊的偏移量是 ';' 后跟另一个搜索命令。例如： >

   /test 1/;/test
   /test.*/+1;?ing?

第一个命令首先找到下一个出现的 "test 1"，然后找到其后第一个出现的 "test"。

这就像依次执行两个搜索命令，除了：
- 它可以在操作符之后用作单个移动命令。
- 后续 "n" 或 "N" 命令的方向来自第一个搜索命令。
- 发生错误时，光标根本不会移动。

							*last-pattern*
最后使用的模式和偏移量会被记住。它们可用于重复搜索，可能以另一个方向或另一个计数进行。请注意，会记住两个模式：一个用于“普通”搜索命令，一个用于替换命令 ":s"。每次给出空模式时，都会使用先前使用的模式。但是，如果没有先前的搜索命令，则尽可能使用先前的替换模式。

'magic' 选项与最后使用的模式保持一致。如果您更改了 'magic'，这不会改变最后使用的模式的解释方式。
'ignorecase' 选项则不然。当 'ignorecase' 更改时，它将导致模式匹配不同的文本。

如果设置了 'hlsearch' 选项，最后使用的搜索模式的所有匹配项都会被高亮显示。

要清除最后使用的搜索模式： >
	:let @/ = ""
这不会将模式设置为空字符串，因为那会匹配任何位置。模式确实被清除了，就像启动 Vim 时一样。

搜索通常会跳过那些不会移动光标的匹配项。下一个匹配项是在下一个字符处找到，还是在跳过的匹配项之后找到，取决于 'cpoptions' 中的 'c' 标志。参见 |cpo-c|。
	   带有 'c' 标志：   "/..." 前进 1 到 3 个字符
	   不带 'c' 标志：   "/..." 前进 1 个字符
带有 'c' 标志时不可预测性是由于从第一列开始搜索，跳过匹配项，直到找到光标位置之后的匹配项。

当向后搜索时，搜索从行的开头开始，使用上述 'cpoptions' 中的 'c' 标志。然后使用光标位置之前的最后一个匹配项。

在 Vi 中，":tag" 命令在搜索标签时会设置最后的搜索模式。在 Vim 中，除非 'cpoptions' 中存在 't' 标志，否则不会这样做，之前搜索的模式仍会被记住。搜索模式始终被放入搜索历史中。

如果 'wrapscan' 选项开启（默认开启），搜索会绕回缓冲区的末尾。如果 'wrapscan' 未设置，则向后搜索在开头停止，向前搜索在缓冲区末尾停止。如果 'wrapscan' 已设置但未找到模式，则会给出错误消息 "pattern not found"，并且光标不会移动。如果 'wrapscan' 未设置，向前搜索时消息变为 "search hit BOTTOM without match"，向后搜索时变为 "search hit TOP without match"。如果 wrapscan 已设置并且搜索绕过了文件末尾，则在向后或向前搜索时分别会给出消息 "search hit TOP, continuing at BOTTOM" 或 "search hit BOTTOM, continuing at TOP"。可以通过设置 'shortmess' 选项中的 's' 标志来关闭此消息。此消息使用高亮方法 'w'（默认：standout）。

							*search-range*
您可以通过包含 \%>l 项将搜索命令 "/" 限制在特定的行范围内。例如，匹配第 199 行以下和第 300 行以上的单词 "limit"： >
	/\%>199l\%<300llimit
另请参见 |/\%>l|。

另一种方法是使用带有 'c' 标志的 ":substitute" 命令。示例： >
   :.,300s/Pattern//gc
此命令将从光标位置搜索到第 300 行，查找 "Pattern"。在匹配项处，系统会提示您输入一个字符。输入 'q' 在此匹配项处停止，输入 'n' 查找下一个匹配项。

"*"、"#"、"g*" 和 "g#" 命令按以下顺序查找光标附近的单词，使用找到的第一个：
- 当前光标下的关键字。
- 光标右侧，同一行中的第一个关键字。
- 当前光标下的单词。
- 光标右侧，同一行中的第一个单词。
关键字只能包含字母和 'iskeyword' 中的字符。
单词可以包含任何非空白字符（<Tab> 和/或 <Space>）。
请注意，如果您用十指打字，这些字符很容易记住："#" 位于您的左手（搜索左侧和上方），而 "*" 位于您的右手（搜索右侧和下方）。（但这取决于您的键盘布局）。

								*E956*
在极少数情况下，正则表达式会被递归使用。这可能发生在执行模式需要很长时间，并且在检查通道上的消息时调用了也使用模式或触发了自动命令的回调时。在大多数情况下，这应该没问题，但是当某个模式正在使用时又被再次使用，它会失败。通常这意味着模式有问题。

==============================================================================
2. 模式的定义		*search-pattern* *pattern* *[pattern]*
					*regular-expression* *regexp* *Pattern*
					*E383* *E476*

首先，请阅读用户手册第 27 章 |usr_27.txt|。

						*/bar* */\bar* */pattern*
1. 模式是一个或多个分支，由 "\|" 分隔。它匹配与任何一个分支匹配的内容。示例："foo\|beep" 匹配 "foo" 和 "beep"。如果多个分支匹配，则使用第一个。

   pattern ::=	    branch
		or  branch \| branch
		or  branch \| branch \| branch
		等等。

						*/branch* */\&*
2. 分支是一个或多个连接体，由 "\&" 分隔。它匹配最后一个连接体，但前提是前面的所有连接体也在同一位置匹配。示例： >
	"foobeep\&..." 匹配 "foobeep" 中的 "foo"。
	".*Peter\&.*Bob" 匹配同时包含 "Peter" 和 "Bob" 的行。
<
   branch ::=	    concat
		or  concat \& concat
		or  concat \& concat \& concat
		等等。

						*/concat*
3. 连接体是一个或多个片段连接而成。它匹配第一个片段的匹配项，后跟第二个片段的匹配项，依此类推。示例："f[0-9]b"，首先匹配 "f"，然后匹配一个数字，然后匹配 "b"。

   concat  ::=	    piece
		or  piece piece
		or  piece piece piece
		等等。

						*/piece*
4. 片段是一个原子，后面可能跟一个量词，指示原子可以匹配的次数。示例："a*" 匹配任意序列的 "a" 字符：""、"a"、"aa" 等。参见 |/multi|。

   piece   ::=	    atom
		or  atom  multi

						*/atom*
5. 原子可以是长列表中的一项。许多原子匹配文本中的一个字符。它通常是一个普通字符或一个字符类。括号可用于将模式变成一个原子。"\z(\)" 结构仅用于语法高亮。

   atom    ::=	    普通原子		|/ordinary-atom|
		or  \( pattern \)		|/\(|
		or  \%( pattern \)		|/\%(|
		or  \z( pattern \)		|/\z(|


				*/\%#=* *two-engines* *NFA*
Vim 包含两个正则表达式引擎：
1. 一个旧的、回溯的引擎，支持所有特性。
2. 一个新的、NFA 引擎，在某些模式上运行速度更快，在某些模式上可能更慢。
								 *E1281*
Vim 将自动为您选择合适的引擎。但是，如果您遇到问题或想特别选择一个引擎，您可以在模式前添加以下内容之一：

	\%#=0	强制自动选择。仅当 'regexpengine' 被设置为非零值时有效。
	\%#=1	强制使用旧的引擎。
	\%#=2	强制使用 NFA 引擎。

您也可以使用 'regexpengine' 选项来更改默认值。

			 *E864* *E868* *E874* *E875* *E876* *E877* *E878*
如果选择 NFA 引擎并且它遇到了未实现的内容，则模式将不匹配。这仅在调试 Vim 时有用。

==============================================================================
3. 魔法							*/magic*

模式中的某些字符（例如字母）是按字面意思理解的。它们与文本中的完全相同字符匹配。然而，当在它们前面加上反斜杠时，这些字符可能会获得特殊含义。例如，"a" 匹配字母 "a"，而 "\a" 匹配任何字母字符。

其他字符在没有反斜杠时具有特殊含义。它们需要在前面加上反斜杠才能按字面意思匹配。例如 "." 匹配任何字符，而 "\." 匹配一个点。

一个字符是按字面意思理解还是具有特殊含义，取决于 'magic' 选项和接下来提到的模式中的项。'magic' 选项应该始终设置。我们为了完整性在此提及 'nomagic' 的效果，但我们建议不要使用它。
							*/\m* */\M*
使用 "\m" 使得它之后的模式被解释为如同设置了 'magic' 一样，忽略 'magic' 选项的实际值。
使用 "\M" 使得它之后的模式被解释为如同使用了 'nomagic' 一样。
							*/\v* */\V*
使用 "\v" 意味着它之后，除 '0'-'9'、'a'-'z'、'A'-'Z' 和 '_' 之外的所有 ASCII 字符都具有特殊含义："非常魔法"

使用 "\V" 意味着它之后，只有反斜杠和终止字符（通常是 / 或 ?）具有特殊含义："非常不魔法"

示例：
之后:	  \v	   \m	    \M	     \V		匹配 ~
		'magic' 'nomagic'
	  a	   a	    a	     a		字面量 'a'
	  \a	   \a	    \a	     \a		任何字母字符
	  .	   .	    \.	     \.		任何字符
	  \.	   \.	    .	     .		字面量点
	  $	   $	    $	     \$		行尾
	  *	   *	    \*	     \*		前一个原子的任意次数
	  ~	   ~	    \~	     \~		最新的替换字符串
	  ()	   \(\)     \(\)     \(\)	分组作为一个原子
	  |	   \|	    \|	     \|		空：分隔替代项
	  \\	   \\	    \\	     \\		字面量反斜杠
	  \{	   {	    {	     {		字面量花括号

如果您愿意，您可以通过在模式开头放置 "\m" 或 "\M" 来使模式不受 'magic' 选项设置的影响。

==============================================================================
4. 模式项概述				*pattern-overview*
						*E865* *E866* *E867* *E869*

量词概述。				*/multi* *E61* *E62*
更多解释和示例如下，请点击链接。		*E64* *E871*

	  量词 ~
     'magic' 'nomagic'	匹配前一个原子 ~
|/star|	*	\*	0 或多次	尽可能多
|/\+|	\+	\+	1 或多次	尽可能多
|/\=|	\=	\=	0 或 1		尽可能多
|/\?|	\?	\?	0 或 1		尽可能多

|/\{|	\{n,m}	\{n,m}	n 到 m		尽可能多
	\{n}	\{n}	n		精确
	\{n,}	\{n,}	至少 n		尽可能多
	\{,m}	\{,m}	0 到 m		尽可能多
	\{}	\{}	0 或多次	尽可能多（与 "*" 相同）

|/\{-|	\{-n,m}	\{-n,m}	n 到 m		尽可能少
	\{-n}	\{-n}	n		精确
	\{-n,}	\{-n,}	至少 n		尽可能少
	\{-,m}	\{-,m}	0 到 m		尽可能少
	\{-}	\{-}	0 或多次	尽可能少

							*E59*
|/\@>|	\@>	\@>	1，如同匹配整个模式
|/\@=|	\@=	\@=	空，需要匹配 |/zero-width|
|/\@!|	\@!	\@!	空，需要不匹配 |/zero-width|
|/\@<=|	\@<=	\@<=	空，需要后面匹配 |/zero-width|
|/\@<!|	\@<!	\@<!	空，需要后面不匹配 |/zero-width|


普通原子概述。				*/ordinary-atom*
更多解释和示例如下，请点击链接。

      普通原子 ~
      魔法   不魔法	匹配 ~
|/^|	^	^	行首（在模式开头）|/zero-width|
|/\^|	\^	\^	字面量 '^'
|/\_^|	\_^	\_^	行首（可在任何位置使用）|/zero-width|
|/$|	$	$	行尾（在模式结尾）|/zero-width|
|/\$|	\$	\$	字面量 '$'
|/\_$|	\_$	\_$	行尾（可在任何位置使用）|/zero-width|
|/.|	.	\.	任意单个字符（非换行符）
|/\_.|	\_.	\_.	任意单个字符或换行符
|/\<|	\<	\<	单词开始 |/zero-width|
|/\>|	\>	\>	单词结束 |/zero-width|
|/\zs|	\zs	\zs	任意内容，设置匹配开始
|/\ze|	\ze	\ze	任意内容，设置匹配结束
|/\%^|	\%^	\%^	文件开始 |/zero-width|		*E71*
|/\%$|	\%$	\%$	文件结束 |/zero-width|
|/\%V|	\%V	\%V	在可视区域内 |/zero-width|
|/\%#|	\%#	\%#	光标位置 |/zero-width|
|/\%'m|	\%'m	\%'m	标记 m 位置 |/zero-width|
|/\%l|	\%23l	\%23l	在第 23 行 |/zero-width|
|/\%c|	\%23c	\%23c	在第 23 列 |/zero-width|
|/\%v|	\%23v	\%23v	在第 23 虚拟列 |/zero-width|

字符类：					*/character-classes*
      魔法   不魔法	匹配 ~
|/\i|	\i	\i	标识符字符（参见 'isident' 选项）
|/\I|	\I	\I	类似于 "\i"，但不包括数字
|/\k|	\k	\k	关键字字符（参见 'iskeyword' 选项）
|/\K|	\K	\K	类似于 "\k"，但不包括数字
|/\f|	\f	\f	文件名字符（参见 'isfname' 选项）
|/\F|	\F	\F	类似于 "\f"，但不包括数字
|/\p|	\p	\p	可打印字符（参见 'isprint' 选项）
|/\P|	\P	\P	类似于 "\p"，但不包括数字
|/\s|	\s	\s	空白字符：<Space> 和 <Tab>
|/\S|	\S	\S	非空白字符；与 \s 相反
|/\d|	\d	\d	数字：				[0-9]
|/\D|	\D	\D	非数字：			[^0-9]
|/\x|	\x	\x	十六进制数字：			[0-9A-Fa-f]
|/\X|	\X	\X	非十六进制数字：		[^0-9A-Fa-f]
|/\o|	\o	\o	八进制数字：			[0-7]
|/\O|	\O	\O	非八进制数字：			[^0-7]
|/\w|	\w	\w	单词字符：			[0-9A-Za-z_]
|/\W|	\W	\W	非单词字符：			[^0-9A-Za-z_]
|/\h|	\h	\h	单词首字符：			[A-Za-z_]
|/\H|	\H	\H	非单词首字符：			[^A-Za-z_]
|/\a|	\a	\a	字母字符：			[A-Za-z]
|/\A|	\A	\A	非字母字符：			[^A-Za-z]
|/\l|	\l	\l	小写字符：			[a-z]
|/\L|	\L	\L	非小写字符：			[^a-z]
|/\u|	\u	\u	大写字符：			[A-Z]
|/\U|	\U	\U	非大写字符			[^A-Z]
|/\_|	\_x	\_x	其中 x 是上述任意字符：包含换行符的字符类
（字符类结束）

      魔法   不魔法	匹配 ~
|/\e|	\e	\e	<Esc>
|/\t|	\t	\t	<Tab>
|/\r|	\r	\r	<CR>
|/\b|	\b	\b	<BS>
|/\n|	\n	\n	换行符
|/~|	~	\~	最后给出的替换字符串
|/\1|	\1	\1	与第一个 \(\) 匹配的相同字符串
|/\2|	\2	\2	类似于 "\1"，但使用第二个 \(\) 
	   ...
|/\9|	\9	\9	类似于 "\1"，但使用第九个 \(\)
								*E68*
|/\z1|	\z1	\z1	仅用于语法高亮，参见 |:syn-ext-match|
	   ...
|/\z1|	\z9	\z9	仅用于语法高亮，参见 |:syn-ext-match|

	x	x	没有特殊含义的字符匹配自身

|/[]|	[]	\[]	在 [] 内指定的任意字符
|/\%[]|	\%[]	\%[]	一个可选匹配的原子序列

|/\c|	\c	\c	忽略大小写，不使用 'ignorecase' 选项
|/\C|	\C	\C	匹配大小写，不使用 'ignorecase' 选项
|/\Z|	\Z	\Z	忽略 Unicode "组合字符" 的差异。
			在搜索带元音符号的希伯来语或阿拉伯语文本时很有用。

      魔法   不魔法	匹配 ~
|/\m|	\m	\m	对模式中后面的字符开启 'magic'
|/\M|	\M	\M	对模式中后面的字符关闭 'magic'
|/\v|	\v	\v	模式中后面的字符是 "非常魔法"
|/\V|	\V	\V	模式中后面的字符是 "非常不魔法"
|/\%#=|   \%#=1   \%#=1   选择正则表达式引擎 |/zero-width|

|/\%d|	\%d	\%d	匹配指定的十进制字符（例如 \%d123）
|/\%x|	\%x	\%x	匹配指定的十六进制字符（例如 \%x2a）
|/\%o|	\%o	\%o	匹配指定的八进制字符（例如 \%o040）
|/\%u|	\%u	\%u	匹配指定的多字节字符（例如 \%u20ac）
|/\%U|	\%U	\%U	匹配指定的大多字节字符（例如 \%U12345678）
|/\%C|	\%C	\%C	匹配任何组合字符

示例			匹配 ~
\<\I\i*		或
\<\h\w*
\<[a-zA-Z_][a-zA-Z0-9_]*
			一个标识符（例如，在 C 程序中）。

\(\.$\|\. \)		一个句点后跟 <EOL> 或一个空格。

[.!?][])"']*\($\|[ ]\)	一个查找句子结尾的搜索模式，其定义与 ")" 命令几乎相同。

cat\Z			匹配 "cat" 和 "càt"（"a" 后跟 0x0300）
			不匹配 "càt"（字符 0x00e0），即使它们看起来可能相同。


==============================================================================
5. 量词项						*pattern-multi-items*

一个原子后面可以跟随一个指示，说明该原子可以匹配多少次以及以何种方式匹配。这被称为量词。参见 |/multi| 了解概览。

							*/star* */\star*
*	（当 'magic' 未设置时使用 \*）
	匹配前一个原子的 0 次或多次，尽可能多。
	示例	'nomagic'	匹配 >
	a*	   a\*		"", "a", "aa", "aaa", 等。
	.*	   \.\*		任意内容，也包括空字符串，不包括换行符
	\_.*	   \_.\*	一直到缓冲区末尾的所有内容
	\_.*END	   \_.\*END	一直到并包括缓冲区中最后一个 "END" 的所有内容
<
	例外：当 "*" 在模式开头或紧接在 "^" 之后使用时，它匹配星号字符。

	请注意，重复 "\_." 可能匹配大量文本并花费很长时间。例如，`\_.*END` 匹配从当前位置到文件中最后一个 "END" 的所有文本。由于 "*" 会匹配尽可能多的内容，它首先会跳过所有行直到文件末尾，然后尝试匹配 "END"，一次回退一个字符。

							*/\+*
\+	匹配前一个原子的 1 次或多次，尽可能多。
	示例		匹配 ~
	^.\+$		任意非空行
	\s\+		至少一个字符的空白

							*/\=*
\=	匹配前一个原子的 0 次或 1 次，尽可能多。
	示例		匹配 ~
	foo\=		"fo" 和 "foo"

							*/\?*
\?	与 \= 相同。不能与 "?" 命令一起用于向后搜索。

					*/\{* *E60* *E554* *E870*
\{n,m}	匹配前一个原子的 n 到 m 次，尽可能多
\{n}	匹配前一个原子的 n 次
\{n,}	匹配前一个原子的至少 n 次，尽可能多
\{,m}	匹配前一个原子的 0 到 m 次，尽可能多
\{}	匹配前一个原子的 0 次或多次，尽可能多（类似于 "*"）
							*/\{-*
\{-n,m}	匹配前一个原子的 n 到 m 次，尽可能少
\{-n}	匹配前一个原子的 n 次
\{-n,}	匹配前一个原子的至少 n 次，尽可能少
\{-,m}	匹配前一个原子的 0 到 m 次，尽可能少
\{-}	匹配前一个原子的 0 次或多次，尽可能少

	n 和 m 是正十进制数或零
								*non-greedy*
	如果在 "{" 之后紧接着出现 "-"，则使用最短匹配优先算法（参见下面的示例）。特别地，"\{-}" 与 "*" 相同，但使用最短匹配优先算法。但是：起始更早的匹配优先于更短的匹配："a\{-}b" 在 "xaaab" 中匹配 "aaab"。

	示例			匹配 ~
	ab\{2,3}c		"abbc" 或 "abbbc"
	a\{5}			"aaaaa"
	ab\{2,}c		"abbc", "abbbc", "abbbbc", 等。
	ab\{,3}c		"ac", "abc", "abbc" 或 "abbbc"
	a[bc]\{3}d		"abbbd", "abbcd", "acbcd", "acccd", 等。
	a\(bc\)\{1,2}d		"abcd" 或 "abcbcd"
	a[bc]\{-}[cd]		"abcd" 中的 "abc"
	a[bc]*[cd]		"abcd" 中的 "abcd"

	} 前面可以选择加一个反斜杠：\{n,m\}。

							*/\@=*
\@=	以零宽度匹配前面的原子。
	类似于 Perl 中的 "(?=pattern)"。
	示例			匹配 ~
	foo\(bar\)\@=		"foobar" 中的 "foo"
	foo\(bar\)\@=foo	无
							*/zero-width*
	使用 "\@="（或 "^"、"$"、"<"、">"）时，匹配中不包含任何字符。这些项仅用于检查是否可以匹配。这可能比较棘手，因为后续项的匹配将在同一位置进行。上面的最后一个示例不会匹配 "foobarfoo"，因为它尝试在 "bar" 匹配的同一位置匹配 "foo"。

	注意，使用 "\&" 与使用 "\@=" 效果相同："foo\&.." 与 "\(foo\)\@=.." 相同。但使用 "\&" 更简单，不需要括号。


							*/\@!*
\@!	如果前一个原子在当前 位置不匹配，则以零宽度匹配。|/zero-width|
	类似于 Perl 中的 "(?!pattern)"。
	示例			匹配 ~
	foo\(bar\)\@!		任何后跟的不是 "bar" 的 "foo"
	a.\{-}p\@!		"a", "ap", "app", "appp", 等，后跟的不是 "p"
	if \(\(then\)\@!.\)*$	"if " 后跟的不是 "then"

	使用 "\@!" 很棘手，因为有很多地方模式不匹配。`a.*p\@!` 将从 "a" 匹配到行尾，因为 `.*` 可以匹配行中的所有字符，而 "p" 在行尾不匹配。"a.\{-}p\@!" 将匹配任何 "a"、"ap"、"app" 等，只要后面不跟 "p"，因为 "." 可以匹配 "p"，而 "p\@!" 在那之后不匹配。

	不能使用 "\@!" 来查找匹配位置之前的非匹配项："\(foo\)\@!bar" 会匹配 "foobar" 中的 "bar"，因为在 "bar" 匹配的位置，"foo" 不匹配。要避免匹配 "foobar"，可以使用 "\(foo\)\@!...bar"，但这不匹配行首的 bar。请使用 "\(foo\)\@<!bar"。

	有用的示例：在不包含 "bar" 的行中查找 "foo"： >
		/^\%(.*bar\)\@!.*\zsfoo
<	此模式首先检查行中没有一个位置能让 "bar" 匹配。如果 `.*bar` 在某处匹配，\@! 将拒绝该模式。当没有匹配时，任何 "foo" 都会被找到。"\zs" 是为了让匹配在 "foo" 之前开始。

							*/\@<=*
\@<=	如果前一个原子刚好在后面的内容之前匹配，则以零宽度匹配。|/zero-width|
	类似于 Perl 中的 "(?<=pattern)"，但 Vim 允许非固定宽度的模式。
	示例			匹配 ~
	\(an\_s\+\)\@<=file	"an" 后跟空白或换行符后的 "file"
	为了提高速度，通常最好避免使用此量词。尝试改用 "\zs" |/\zs|。要匹配与上面示例相同的内容：
		an\_s\+\zsfile
	至少要为向后查找设置一个限制，见下文。

	"\@<=" 和 "\@<!" 检查刚好在后面的内容之前的匹配。理论上，这些匹配可以在此位置之前的任何地方开始。但为了限制所需时间，只搜索与后面内容匹配的行，以及该行之前的一行（如果存在）。这应该足以匹配大多数内容，且不会太慢。

	在旧的正则表达式引擎中，首先检查 "\@<=" 和 "\@<!" 之后的部分模式是否匹配，因此像 "\1" 这样的引用无法在内部引用 \(\)。反过来则可以：
	坏示例			匹配 ~
	\%#=1\1\@<=,\([a-z]\+\)		"abc,abc" 中的 ",abc"

	然而，新的正则表达式引擎工作方式不同，最好不要依赖此行为，如果可能避免使用 \@<=：
	示例			匹配 ~
	\([a-z]\+\)\zs,\1		"abc,abc" 中的 ",abc"

\@123<=
	类似于 "\@<="，但仅向后查找 123 字节。这避免了尝试许多已知会失败且使模式执行非常慢的匹配。示例，检查 "span" 之前是否有 "<"：
		/<\@1<=span
	这将仅尝试匹配 "span" 之前一个字节处的 "<"，这无论如何都是唯一有效的位置。
	跨越行边界后，限制是相对于行尾的。因此，匹配所在行开头的字符不被计数（这只是为了保持简单）。
	数字零等同于无限制。

							*/\@<!*
\@<!	如果前一个原子不在后面的内容之前匹配，则以零宽度匹配。因此，如果当前行或上一行中没有原子匹配的位置使其刚好结束于后面内容之前，则此匹配成立。|/zero-width|
	类似于 Perl 中的 "(?<!pattern)"，但 Vim 允许非固定宽度的模式。
	与前一个原子的匹配被设置为刚好结束于与后面内容的匹配之前，因此以 `.*` 结束的原子将有效。
	警告：这可能很慢（因为需要检查许多位置是否匹配）。如果可能，请使用限制，见下文。
	示例			匹配 >
	\(foo\)\@<!bar		任何不在 "foobar" 中的 "bar"
	\(\/\/.*\)\@<!in	不在 "//" 之后的 "in"
<
\@123<!
	类似于 "\@<!"，但仅向后查找 123 字节。这避免了尝试许多已知会失败且使模式执行非常慢的匹配。

							*/\@>*
\@>	匹配前一个原子，如同匹配整个模式。
	类似于 Perl 中的 "(?>pattern)"。
	示例		匹配 ~
	\(a*\)\@>a	无（"a*" 占用了所有 "a"，后面不能再有另一个 "a"）

	这匹配前一个原子，就好像它本身是一个模式。如果它不匹配，则不会用更短的子匹配等进行重试。观察这个区别："a*b" 和 "a*ab" 都匹配 "aaab"，但在第二种情况下，"a*" 只匹配前两个 "a"。"\(a*\)\@>ab" 不匹配 "aaab"，因为 "a*" 匹配了 "aaa"（尽可能多的 "a"），所以 "ab" 无法匹配。


==============================================================================
6. 普通原子					*pattern-atoms*

普通原子可以是：

							*/^*
^	在模式开头或 "\|"、"\)"、"\%(" 或 "\n" 之后：匹配行首；在其他位置，匹配字面量 '^'。|/zero-width|
	示例		匹配 ~
	^beep(		C 函数 "beep" 的开头（可能）。

							*/\^*
\^	匹配字面量 '^'。可以在模式的任何位置使用，但不能在 [] 内使用。

							*/\_^*
\_^	匹配行首。|/zero-width| 可以在模式的任何位置使用，但不能在 [] 内使用。
	示例		匹配 ~
	\_s*\_^foo	空白和空行，然后是行首的 "foo"

							*/$*
$	在模式结尾或 "\|"、"\)" 或 "\n" 之前（'magic' 开启时）：匹配换行符 <EOL>；在其他位置，匹配字面量 '$'。|/zero-width|

							*/\$*
\$	匹配字面量 '$'。可以在模式的任何位置使用，但不能在 [] 内使用。

							*/\_$*
\_$	匹配换行符。|/zero-width| 可以在模式的任何位置使用，但不能在 [] 内使用。注意 "a\_$b" 永远不匹配，因为 "b" 无法匹配换行符。请改用 "a\nb" |/\n|。
	示例		匹配 ~
	foo\_$\_s*	行尾的 "foo" 以及后面的空白和空行

.	（使用 'nomagic' 时：\.）				*/.* */\.*
	匹配任意单个字符，但不匹配换行符。

							*/\_.*
\_.	匹配任意单个字符或换行符。
	注意：`\_.*` 匹配直到缓冲区末尾的所有文本！

							*/\<*
\<	匹配单词的开始：下一个字符是单词的第一个字符。'iskeyword' 选项指定什么是单词字符。|/zero-width|

							*/\>*
\>	匹配单词的结束：前一个字符是单词的最后一个字符。'iskeyword' 选项指定什么是单词字符。|/zero-width|

							*/\zs*
\zs	在任何位置匹配，但不能在 [] 内，并在此处设置匹配的开始：下一个字符是整个匹配的第一个字符。|/zero-width|
	示例： >
		/^\s*\zsif
<	匹配行首的 "if"，忽略空白。
	可以多次使用，使用匹配分支中遇到的最后一个。示例： >
		/\(.\{-}\zsFab\)\{3}
<	查找第三次出现的 "Fab"。
	这后面不能跟量词。*E888*

							*/\ze*
\ze	在任何位置匹配，但不能在 [] 内，并在此处设置匹配的结束：前一个字符是整个匹配的最后一个字符。|/zero-width|
	可以在匹配分支中多次使用，使用遇到的最后一个。
	示例："end\ze\(if\|for\)" 匹配 "endif" 和 "endfor" 中的 "end"。
	这后面不能跟量词。|E888|

						*/\%^* *start-of-file*
\%^	匹配文件开头。当匹配字符串时，匹配字符串的开头。
	例如，查找文件中第一个 "VIM"： >
		/\%^\_.\{-}\zsVIM
<
						*/\%$* *end-of-file*
\%$	匹配文件结尾。当匹配字符串时，匹配字符串的结尾。
	注意，这不会查找文件中最后一个 "VIM"： >
		/VIM\_.\{-}\%$
<	它会找到下一个 VIM，因为它之后的部分总是匹配。下面这个会找到文件中最后一个 "VIM"： >
		/VIM\ze\(\(VIM\)\@!\_.\)*\%$
<	这使用了 |/\@!| 来确定第一个 "VIM" 之后的任何位置都不匹配 "VIM"。
	从文件末尾向后搜索更容易！

						*/\%V*
\%V	在可视区域内匹配。当可视模式已经停止时，在 |gv| 将重新选择的区域内匹配。
	这是一个 |/zero-width| 匹配。为了确保整个模式都在可视区域内，请将其放在模式的开头和结尾之前，例如： >
		/\%Vfoo.*ba\%Vr
<	如果仅可视选择了 "foo bar"，这也有效。这个： >
		/\%Vfoo.*bar\%V
<	如果可视选择在 "r" 之后继续，则会匹配 "foo bar"。
	仅对当前缓冲区有效。

						*/\%#* *cursor-position*
\%#	匹配光标位置。仅在窗口显示的缓冲区中匹配时有效。
	警告：当光标在模式使用后移动时，结果将无效。Vim 不会自动更新匹配项。这对于语法高亮和 'hlsearch' 尤其重要。换句话说：当光标移动时，显示不会针对此更改进行更新。对于已更改的行（整行更新）或使用 |CTRL-L| 命令时（整个屏幕更新），会执行更新。示例，高亮光标下的单词： >
		/\k*\%#\k*
<	当设置了 'hlsearch' 并且您移动光标并进行更改时，这将清楚地显示匹配项何时更新或不更新。

						*/\%'m* */\%<'m* */\%>'m*
\%'m	匹配标记 m 的位置。
\%<'m	匹配标记 m 的位置之前。
\%>'m	匹配标记 m 的位置之后。
	示例，高亮从标记 's 到 'e 的文本： >
		/.\%>'s.*\%<'e..
<	注意需要两个点才能将标记 'e 包含在匹配中。这是因为 "\%<'e" 匹配标记 'e 之前的一个字符，并且由于它是 |/zero-width| 匹配，它不包含该字符。
	警告：当标记在模式使用后移动时，结果将无效。Vim 不会自动更新匹配项。类似于 "\%#" 的光标移动 |/\%#|。

					*/\%l* */\%>l* */\%<l* *E951* *E1204*
\%23l	匹配特定行。
\%<23l	匹配特定行之上（行号较小）。
\%>23l	匹配特定行之下（行号较大）。
\%.l	匹配光标所在行。
\%<.l	匹配光标行之上。
\%>.l	匹配光标行之下。
	这六种用法可以匹配缓冲区中的特定行。"23" 可以是任何行号。第一行是 1。
	警告：当插入或删除行时，Vim 不会自动更新匹配项。这意味着语法高亮会很快变得不正确。同样，当引用光标位置 (".") 并且光标移动时，显示不会针对此更改进行更新。使用 |CTRL-L| 命令时（整个屏幕更新）会执行更新。
	示例，高亮光标当前所在行： >
		:exe '/\%' .. line(".") .. 'l'
<	或者使用： >
		/\%.l
<	当设置了 'hlsearch' 并且您移动光标并进行更改时，这将清楚地显示匹配项何时更新或不更新。

						*/\%c* */\%>c* */\%<c*
\%23c	匹配特定列。
\%<23c	匹配特定列之前。
\%>23c	匹配特定列之后。
\%.c	匹配光标所在列。
\%<.c	匹配光标列之前。
\%>.c	匹配光标列之后。
	这六种用法可以匹配缓冲区或字符串中的特定列。"23" 可以是任何列号。第一列是 1。实际上，列是字节编号（因此对于多字节字符不完全正确）。
	警告：当插入或删除文本时，Vim 不会自动更新匹配项。这意味着语法高亮会很快变得不正确。同样，当引用光标位置 (".") 并且光标移动时，显示不会针对此更改进行更新。使用 |CTRL-L| 命令时（整个屏幕更新）会执行更新。
	示例，高亮光标当前所在列： >
		:exe '/\%' .. col(".") .. 'c'
<	或者使用： >
		/\%.c
<	当设置了 'hlsearch' 并且您移动光标并进行更改时，这将清楚地显示匹配项何时更新或不更新。
	匹配第 44 列的单个字节的示例： >
		/\%>43c.\%<46c
<	注意，当 "." 匹配第 44 列的一个字节时，"\%<46c" 匹配第 45 列。
						*/\%v* */\%>v* */\%<v*
\%23v	匹配特定虚拟列。
\%<23v	匹配特定虚拟列之前。
\%>23v	匹配特定虚拟列之后。
\%.v	匹配当前虚拟列。
\%<.v	匹配当前虚拟列之前。
\%>.v	匹配当前虚拟列之后。
	这六种用法可以匹配缓冲区或字符串中的特定虚拟列。当不与窗口中的缓冲区匹配时，使用当前窗口的选项值（例如 'tabstop'）。
	"23" 可以是任何列号。第一列是 1。
	注意，某些虚拟列位置永远不会匹配，因为它们位于制表符或其他占用多个屏幕字符的字符的中间。
	警告：当插入或删除文本时，Vim 不会自动更新高亮匹配项。这意味着语法高亮会很快变得不正确。同样，当引用光标位置 (".") 并且光标移动时，显示不会针对此更改进行更新。使用 |CTRL-L| 命令时（整个屏幕更新）会执行更新。
	示例，高亮虚拟列 72 之后的所有字符： >
		/\%>72v.*
<	当设置了 'hlsearch' 并且您移动光标并进行更改时，这将清楚地显示匹配项何时更新或不更新。
	要匹配到第 17 列的文本： >
		/^.*\%17v
<	要匹配当前虚拟列之后的所有字符（光标所在位置）： >
		/\%>.v.*
<	第 17 列未包含在内，因为这是一个 |/zero-width| 匹配。要包含该列，请使用： >
		/^.*\%17v.
<	此命令执行相同操作，但当第 17 列没有字符时也会匹配： >
		/^.*\%<18v.
<	注意，如果没有 "^" 将匹配锚定在第一列，这也会高亮第 17 列： >
		/.*\%17v
<	第 17 列会被 'hlsearch' 高亮，因为存在另一个 `.*` 匹配零个字符的匹配项。


字符类：
\i	标识符字符（参见 'isident' 选项）		*/\i*
\I	类似于 "\i"，但不包括数字			*/\I*
\k	关键字字符（参见 'iskeyword' 选项）		*/\k*
\K	类似于 "\k"，但不包括数字			*/\K*
\f	文件名字符（参见 'isfname' 选项）		*/\f*
\F	类似于 "\f"，但不包括数字			*/\F*
\p	可打印字符（参见 'isprint' 选项）		*/\p*
\P	类似于 "\p"，但不包括数字			*/\P*

注意：上述规则也适用于多字节字符。下面的规则仅匹配 ASCII 字符，如范围所示。

						*whitespace* *white-space*
\s	空白字符：<Space> 和 <Tab>		*/\s*
\S	非空白字符；与 \s 相反			*/\S*
\d	数字：				[0-9]		*/\d*
\D	非数字：			[^0-9]		*/\D*
\x	十六进制数字：			[0-9A-Fa-f]	*/\x*
\X	非十六进制数字：		[^0-9A-Fa-f]	*/\X*
\o	八进制数字：			[0-7]		*/\o*
\O	非八进制数字：			[^0-7]		*/\O*
\w	单词字符：			[0-9A-Za-z_]	*/\w*
\W	非单词字符：			[^0-9A-Za-z_]	*/\W*
\h	单词首字符：			[A-Za-z_]	*/\h*
\H	非单词首字符：			[^A-Za-z_]	*/\H*
\a	字母字符：			[A-Za-z]	*/\a*
\A	非字母字符：			[^A-Za-z]	*/\A*
\l	小写字符：			[a-z]		*/\l*
\L	非小写字符：			[^a-z]		*/\L*
\u	大写字符：			[A-Z]		*/\u*
\U	非大写字符：			[^A-Z]		*/\U*

	注意：使用原子比使用 [] 形式更快。

	注意：字符类不使用 'ignorecase'、"\c" 和 "\C"。

			*/\_* *E63* */\_i* */\_I* */\_k* */\_K* */\_f* */\_F*
			*/\_p* */\_P* */\_s* */\_S* */\_d* */\_D* */\_x* */\_X*
			*/\_o* */\_O* */\_w* */\_W* */\_h* */\_H* */\_a* */\_A*
			*/\_l* */\_L* */\_u* */\_U*
\_x	其中 "x" 是上述任意字符：添加了换行符的字符类
（字符类结束）

\e	匹配 <Esc>					*/\e*
\t	匹配 <Tab>					*/\t*
\r	匹配 <CR>					*/\r*
\b	匹配 <BS>					*/\b*
\n	匹配换行符					*/\n*
	当在字符串而不是缓冲区文本中匹配时，匹配字面换行符。

~	匹配最后给出的替换字符串			*/~* */\~*

\(\)	由转义括号括起来的模式。			*/\(* */\(\)* */\)*
	例如，"\(^a\)" 匹配行首的 'a'。
	最多只能有九个这样的括号。您可以使用 "\%(" 来添加更多，但不会将其计为子表达式。
	*E51* *E54* *E55* *E872* *E873*

\1      匹配与第一个 \( 和 \) 中的子表达式匹配的相同字符串。	*/\1* *E65*
	示例："\([a-z]\).\1" 匹配 "ata"、"ehe"、"tot" 等。
\2      类似于 "\1"，但使用第二个子表达式，		*/\2*
   ...							*/\3*
\9      类似于 "\1"，但使用第九个子表达式。		*/\9*
	注意：分组的编号基于模式中 "\(" 出现的顺序（从左到右），而不是基于首先匹配的内容。

\%(\)	由转义括号括起来的模式。			*/\%(\)* */\%(* *E53*
	就像 \(\)，但不将其计为子表达式。这允许使用更多组，并且速度稍快。

x	一个没有任何特殊含义的单个字符，匹配自身

							*/\* */\\*
\x	一个反斜杠后跟一个没有特殊含义的单个字符，保留用于将来的扩展

[]	（使用 'nomagic' 时：\[]）		*/[]* */\[]* */\_[]* */collection* *E76*
\_[]
	一个集合。这是括在方括号中的一系列字符。它匹配集合中的任意单个字符。
	示例		匹配 ~
	[xyz]		任意 'x'、'y' 或 'z'
	[a-zA-Z]$	行尾的任意字母字符
	\c[a-z]$	同上
	[А-яЁё]		俄文字母（使用 utf-8 和 cp1251）

								*/[\n]*
	前面加上 "\_" 后，集合也包括换行符。通过在集合中包含 "\n" 也可以达到同样的效果。当集合以 "^" 开头时，换行符也会被匹配！因此 "\_[^ab]" 匹配换行符以及除 "a" 和 "b" 之外的任何字符。
	这使其与 Vi 兼容：如果没有 "\_" 或 "\n"，集合不匹配换行符。
								*E769*
	当缺少 ']' 时，Vim 不会给出错误消息，而是假定没有使用集合。这对于搜索 '[' 很有用。但是，对于内部搜索，您确实会收到 E769 错误。并且请注意，在 `:substitute` 命令中，整个命令会成为模式。例如，":s/[/x/" 搜索 "[/x" 并将其替换为空。它不会搜索 "[" 并将其替换为 "x"！

								*E944* *E945*
	如果序列以 "^" 开头，则它匹配不在集合中的任意单个字符："[^xyz]" 匹配除 'x'、'y' 和 'z' 之外的任何字符。
	- 如果序列中的两个字符由 '-' 分隔，则这是它们之间 ASCII 字符完整列表的简写。例如，"[0-9]" 匹配任何十进制数字。如果起始字符大于结束字符，例如 [c-a]，则会发生 E944 错误。可以使用非 ASCII 字符，但在旧的正则表达式引擎中，字符值之间的差异不得超过 256。例如，在设置 re=1 后搜索 [\u3000-\u4000] 会发出 E945 错误。在前面加上 \%#=2 将解决此问题。
	- 字符类表达式被求值，结果为属于该字符类的字符集。支持以下字符类：
		  名称	      函数	内容 ~
*[:alnum:]*	  [:alnum:]   isalnum	ASCII 字母和数字
*[:alpha:]*	  [:alpha:]   isalpha	ASCII 字母
*[:blank:]*	  [:blank:]		空格和制表符
*[:cntrl:]*	  [:cntrl:]   iscntrl	ASCII 控制字符
*[:digit:]*	  [:digit:]		十进制数字 '0' 到 '9'
*[:graph:]*	  [:graph:]   isgraph	ASCII 可打印字符（不包括空格）
*[:lower:]*	  [:lower:]   (1)	小写字母（当使用 'ignorecase' 且使用旧引擎时，为所有字母 |two-engines|）
*[:print:]*	  [:print:]   (2)	包括空格在内的可打印字符
*[:punct:]*	  [:punct:]   ispunct	ASCII 标点字符
*[:space:]*	  [:space:]		空白字符：空格、制表符、CR、NL、垂直制表符、换页符
*[:upper:]*	  [:upper:]   (3)	大写字母（当使用 'ignorecase' 且使用旧引擎时，为所有字母 |two-engines|）
*[:xdigit:]*	  [:xdigit:]		十六进制数字：0-9、a-f、A-F
*[:return:]*	  [:return:]		<CR> 字符
*[:tab:]*	  [:tab:]		<Tab> 字符
*[:escape:]*	  [:escape:]		<Esc> 字符
*[:backspace:]*	  [:backspace:]		<BS> 字符
*[:ident:]*	  [:ident:]		标识符字符（与 "\i" 相同）
*[:keyword:]*	  [:keyword:]		关键字字符（与 "\k" 相同）
*[:fname:]*	  [:fname:]		文件名字符（与 "\f" 相同）
	  字符类表达式中的方括号是除界定集合的方括号之外的。例如，以下是一个 UNIX 文件名的合理模式："[-./[:alnum:]_~]\+"。也就是说，至少一个字符的列表，每个字符是 '-'、'.'、'/'、字母、数字、'_' 或 '~'。
	  这些项仅适用于 8 位字符，除了 [:lower:] 和 [:upper:] 在使用新正则表达式引擎时也适用于多字节字符。请参阅 |two-engines|。将来这些项可能适用于多字节字符。目前，要获取所有 "alpha" 字符，您可以使用：[[:lower:][:upper:]]。

	  "函数" 列显示了使用的库函数。实现取决于系统。否则：
	  (1) 对于 ASCII 使用 islower()，对于其他字符使用 Vim 内置规则。
	  (2) 使用 Vim 内置规则
	  (3) 与 (1) 相同，但使用 isupper()
							*/[[=* *[==]*
	- 等价类。这意味着匹配具有几乎相同含义的字符，例如，当忽略重音时。这仅适用于 Unicode、latin1 和 latin9。形式为：
		[=a=]
							*/[[.* *[..]*
	- 排序元素。目前仅接受以下形式的单个字符：
		[.a.]
							  */\]*
	- 要在集合中包含字面量 ']'、'^'、'-' 或 '\'，请在其前面加上反斜杠："[xyz\]]"、"[\^xyz]"、"[xy\-z]" 和 "[xyz\\]"。
	  （注意：POSIX 不支持以这种方式使用反斜杠）。对于 ']'，您也可以使其成为第一个字符（在可能的 "^" 之后）："[]xyz]" 或 "[^]xyz]"。
	  对于 '-'，您也可以使其成为第一个或最后一个字符："[-xyz]"，"[^-xyz]" 或 "[xyz-]"。对于 '\'，您也可以让其后跟任何不在 "^]-\bdertnoUux" 中的字符。"[\xyz]" 匹配 '\'、'x'、'y' 和 'z'。不过最好使用 "\\"，因为将来的扩展可能会在 '\' 后使用其他字符。
	- 省略结尾的 ] 不被视为错误。"[]" 的工作方式类似于 "[]]"，它匹配 ']' 字符。
	- 当 'cpoptions' 中不包含 'l' 标志时，接受以下转义：
		\e	<Esc>
		\t	<Tab>
		\r	<CR>	（不是换行符！）
		\b	<BS>
		\n	换行符，参见上文 |/[\n]|
		\d123	字符的十进制数
		\o40	字符的八进制数，最大为 0o377
		\x20	字符的十六进制数，最大为 0xff
		\u20AC	多字节字符的十六进制数，最大为 0xffff
		\U1234	多字节字符的十六进制数，最大为 8 个字符 0xffffffff |E1541|
	  注意：上面提到的其他反斜杠代码在 [] 内部不起作用！
	- 使用集合匹配可能很慢，因为文本中的每个字符都必须与集合中的每个字符进行比较。如果可能，请改用上面的其他原子。示例："\d" 比 "[0-9]" 快得多，并且匹配相同的字符。但是，新的 |NFA| 正则表达式引擎比旧的引擎能更好地处理这个问题。

						*/\%[]* *E69* *E70* *E369*
\%[]	一个可选匹配的原子序列。它总是匹配。
	它尽可能多地匹配其所包含的原子列表。因此，它在第一个不匹配的原子处停止。例如： >
		/r\%[ead]
<	匹配 "r"、"re"、"rea" 或 "read"。使用最长匹配。
	要匹配 Ex 命令 "function"，其中 "fu" 是必需的，而 "nction" 是可选的，可以这样： >
		/\<fu\%[nction]\>
<	使用单词结束原子 "\>" 是为了避免在 "full" 中匹配 "fu"。
	当原子不是普通字符时，情况会变得更加复杂。您不必经常使用它，但它是可能的。示例： >
		/\<r\%[[eo]ad]\>
<	匹配单词 "r"、"re"、"ro"、"rea"、"roa"、"read" 和 "road"。
	在 [] 内部不能有 \(\)、\%(\) 或 \z(\) 项，并且 \%[] 不能嵌套。
	要包含 "["，请使用 "[[]"，要包含 "]"，请使用 "[]]"，例如： >
		/index\%[[[]0[]]]
<	匹配 "index"、"index["、"index[0" 和 "index[0]"。

				*/\%d* */\%x* */\%o* */\%u* */\%U* *E678*

\%d123	匹配由十进制数指定的字符。后面必须跟一个非数字。
\%o40	匹配由八进制数指定的字符，最大为 0o377。低于 0o40 的数字后面必须跟一个非八进制数字或非数字。
\%x2a	匹配由最多两个十六进制字符指定的字符。
\%u20AC	匹配由最多四个十六进制字符指定的字符。
\%U1234abcd	匹配由最多八个十六进制字符指定的字符，最大为 0x7fffffff（最大允许值为 INT_MAX |E1541|，但最大有效 Unicode 码点为 U+10FFFF）。

==============================================================================
7. 模式中忽略大小写					*/ignorecase*

如果 'ignorecase' 选项开启，则普通字母的大小写将被忽略。可以设置 'smartcase'，以便在模式仅包含小写字母时忽略大小写。
							*/\c* */\C*
当模式中任何位置出现 "\c" 时，整个模式的处理方式如同开启了 'ignorecase'。忽略 'ignorecase' 和 'smartcase' 的实际值。"\C" 的作用相反：强制整个模式匹配大小写。
请注意，字符类不使用 'ignorecase'、"\c" 和 "\C"。

示例：
      模式	'ignorecase'  'smartcase'	匹配 ~
	foo	  off		-		foo
	foo	  on		-		foo Foo FOO
	Foo	  on		off		foo Foo FOO
	Foo	  on		on		    Foo
	\cfoo	  -		-		foo Foo FOO
	foo\C	  -		-		foo

技术细节：				*NL-used-for-Nul*
文件中的 <Nul> 字符在内存中存储为 <NL>。在显示中，它们显示为 "^@"。读写文件时会进行转换。要使用搜索模式匹配 <Nul>，您可以直接输入 CTRL-@ 或 "CTRL-V 000"。这可能正是您所期望的。在内部，该字符在搜索模式中被替换为 <NL>。不寻常的是，输入 CTRL-V CTRL-J 也会插入 <NL>，因此也会搜索文件中的 <Nul>。

						*CR-used-for-NL*
当 'fileformat' 为 "mac" 时，文件中的 <NL> 字符在内部存储为 <CR>。在文本中，它们显示为 "^J"。除此之外，这与使用 <NL> 表示 <Nul> 的用法类似。

当使用表达式求值时，模式中的 <NL> 字符匹配字符串中的 <NL>。使用 "\n"（反斜杠 n）匹配 <NL> 在那里不起作用，它仅用于匹配缓冲区中的文本。

				*pattern-multi-byte* *pattern-multibyte*
模式也可以处理多字节字符，大多数情况下如您所愿。但无效字节可能会引起麻烦，包含无效字节的模式可能永远不会匹配。

==============================================================================
8. 组合字符					*patterns-composing*

							*/\Z*
当模式中任何位置出现 "\Z" 时，所有组合字符都将被忽略。因此，只需匹配基本字符，组合字符可以不同，组合字符的数量也可以不同。例外：如果模式以一个或多个组合字符开头，则这些组合字符必须匹配。
							*/\%C*
使用 "\%C" 跳过任何组合字符。例如，模式 "a" 在 "càt"（其中 a 带有组合字符 0x0300）中不匹配，但 "a\%C" 匹配。请注意，这不匹配 "cát"（其中 á 是字符 0xe1，它没有组合字符）。它匹配 "cat"（其中 a 只是 a）。

当组合字符出现在模式开头或在不包含该组合字符的项之后时，会在包含此组合字符的任何字符处找到匹配。

当使用点号和组合字符时，其工作方式与单独的组合字符相同，只是它之前是什么并不重要。

组合字符的顺序无关紧要。此外，文本可能比模式有更多的组合字符，它仍然匹配。但模式中的所有组合字符都必须在文本中找到。

假设 B 是基本字符，x 和 y 是组合字符：
	模式		文本		匹配 ~
	Bxy		Bxy		是（完美匹配）
	Bxy		Byx		是（顺序忽略）
	Bxy		By		否（缺少 x）
	Bxy		Bx		否（缺少 y）
	Bx		Bx		是（完美匹配）
	Bx		By		否（缺少 x）
	Bx		Bxy		是（忽略额外的 y）
	Bx		Byx		是（忽略额外的 y）

==============================================================================
9. 与 Perl 模式比较				*perl-patterns*

Vim 的正则表达式在功能上与 Perl 最为相似。它们之间的区别主要是符号上的；以下是它们不同之处的总结：

功能			在 Vim 中		在 Perl 中 ~
强制大小写不敏感	\c		(?i)
强制大小写敏感		\C		(?-i)
无反向引用的分组	\%(atom\)	(?:atom)
保守量词		\{-n,m}		`*?,` +?, ??, {}?
零宽度匹配		atom\@=		(?=atom)
零宽度非匹配		atom\@!		(?!atom)
零宽度前面匹配		atom\@<=	(?<=atom)
零宽度前面非匹配	atom\@<!	(?<!atom)
不回溯匹配		atom\@>		(?>atom)

Vim 和 Perl 处理字符串中的换行符的方式略有不同：

在 Perl 中，默认情况下，^ 和 $ 仅在文本的最开头和最结尾匹配，但您可以设置 'm' 标志，使它们也能匹配嵌入的换行符。您还可以设置 's' 标志，使 . 也能匹配换行符。（顺便说一下，这两个标志都可以在模式内部使用与上面 i 标志相同的语法进行更改。）

另一方面，Vim 的 ^ 和 $ 总是在嵌入的换行符处匹配，并且您有两个独立的原子 \%^ 和 \%$，它们分别只在文本的最开头和最结尾匹配。Vim 通过提供 \_ "修饰符" 解决了第二个问题：将它放在 . 或字符类前面，它们也将匹配换行符。

最后，这些结构是 Perl 独有的：
- 在正则表达式中执行任意代码：(?{perl code})
- 条件表达式：(?(condition)true-expr|false-expr)

……而这些是 Vim 独有的：
- 更改模式的魔法性：\v \V \m \M
   （对于避免反斜杠转义非常有用）
- 可选匹配的原子序列：\%[atoms]
- \&（之于 \| 如同 "与" 之于 "或"；它强制几个分支在一个位置匹配）
- 按行/列匹配：\%5l \%5c \%5v
- 设置匹配的开始和结束：\zs \ze

==============================================================================
10. 高亮匹配				*match-highlight*

							*syntax-vs-match*
		注意，匹配高亮机制独立于 |syntax-highlighting|，后者（通常）是缓冲区本地的高亮，而匹配是窗口本地的，两种方法可以自由混合使用。匹配高亮函数在何时以及如何应用方面为您提供了更多的灵活性，但通常仅用于临时高亮，没有严格的规则。两种方法都可以用于隐藏文本。

		因此，像 |matchadd()| 这样的匹配函数不会考虑语法规则，像 |synconcealed()| 这样的函数也不会考虑匹配规则。

							*:mat* *:match*
:mat[ch] {group} /{pattern}/
		在当前窗口中定义一个高亮模式。它将使用 {group} 进行高亮。示例： >
			:highlight MyGroup ctermbg=green guibg=green
			:match MyGroup /TODO/
<		可以使用任何字符代替 // 来标记 {pattern} 的开始和结束。注意使用特殊字符，如 '"' 和 '|'。

		执行此命令时，{group} 必须存在。

		当某个字符同时需要为 'hlsearch' 高亮时，{group} 高亮仍然生效，因为匹配高亮的优先级高于 'hlsearch' 高亮。匹配也会覆盖语法高亮（参见 'syntax'）。

		请注意，使用 'hlsearch' 高亮最后使用的搜索模式在所有窗口中生效，而使用 ":match" 定义的模式仅存在于当前窗口中。切换到其他缓冲区时会保留该模式。

		'ignorecase' 不适用，请在模式中使用 |/\c| 来忽略大小写。否则不会忽略大小写。

		'redrawtime' 定义了搜索模式匹配的最大时间。

		当匹配行尾且 Vim 仅重绘部分显示时，您可能会得到意外的结果。这是因为 Vim 在开始重绘的行中查找匹配。

		另请参见 |matcharg()| 和 |getmatches()|。前者返回先前 |:match| 命令的高亮组和模式。后者返回由 |matchadd()| 和 |:match| 定义的高亮组和模式的列表。

		使用 |:match| 的高亮匹配仅限于三个匹配（除了 |:match|，还可以使用 |:2match| 和 |:3match|）。|matchadd()| 没有此限制，并且还可以设置匹配的优先级。

		另一个示例，高亮虚拟列 72 及之后的所有字符： >
			:highlight rightMargin term=bold ctermfg=blue guifg=blue
			:match rightMargin /.\%>72v/
<		要高亮虚拟列 7 中的所有字符： >
			:highlight col8 ctermbg=grey guibg=grey
			:match col8 /\%<8v.\%>7v/
<		注意使用两个项来匹配占用多个虚拟列的字符，例如 TAB。

:mat[ch]
:mat[ch] none
		清除先前定义的匹配模式。


:2mat[ch] {group} /{pattern}/					*:2match*
:2mat[ch]
:2mat[ch] none
:3mat[ch] {group} /{pattern}/					*:3match*
:3mat[ch]
:3mat[ch] none
		与上面的 |:match| 类似，但设置一个独立的匹配。因此可以同时激活三个匹配。如果多个匹配在同一个位置，编号最小的匹配具有优先权。它使用匹配 ID 3。
		":3match" 命令被（旧版 Vim）|matchparen| 插件使用。建议您使用 ":match" 进行手动匹配，使用 ":2match" 用于另一个插件，或者更好的是使用更灵活的 |matchadd()|（及类似）函数。

==============================================================================
11. 模糊匹配					*fuzzy-matching*

模糊匹配根据字符串与模式的匹配程度打分，其中模式字符按顺序出现但不一定连续。

示例： >
    模式：	"vim"
    候选：	"vim"	     -> 完美
		"vimeo"	     -> 良好（v i m）
		"voice mail" -> 较弱（v _ i _ _ _ m）
		"vintage"    -> 不匹配（没有 "m"）
<
如果搜索字符串有多个单词，每个单词分别匹配，并且可以以任意顺序出现在候选项中。例如，"get pat" 匹配 "GetPattern"、"PatternGet"、"getPattern"、"patGetter"、"getSomePattern"、"MatchpatternGet" 等。

'ignorecase' 和 'smartcase' 选项不适用，如果模式全部是小写，则忽略大小写。

Vim 的实现基于 fzy 项目的算法：
https://github.com/jhawthorn/fzy

它使用动态规划来计算给定模式和候选的最佳得分。

该算法分两个阶段工作：

1. 正向传递
   从左到右扫描候选，跟踪每个模式位置的最佳得分。当匹配发生在候选的开头、单词的开头（空格、下划线、破折号、驼峰式）或紧接在前一个匹配之后时，得分更高。

2. 反向传递
   从得分最高的结束位置开始，逐步返回以找到匹配位置，确保对齐是最优的。

Vim 扩展了原始算法以支持多字节码点，从而能够正确匹配 UTF-8 和其他编码。

时间复杂度为 O(模式 * 候选)。内存使用量与它成正比。

|matchfuzzy()| 和 |matchfuzzypos()| 函数在字符串列表中进行模糊搜索。|matchfuzzy()| 返回匹配的字符串，而 |matchfuzzypos()| 返回匹配项及其位置和得分。

`:vimgrep` 的 "f" 标志启用模糊匹配。

要为 |ins-completion| 启用模糊匹配，请将 "fuzzy" 添加到 'completeopt' 选项中。对于 |cmdline-completion|，请将 "fuzzy" 添加到 'wildoptions' 选项中。

 vim:tw=78:ts=8:noet:ft=help:norl:
