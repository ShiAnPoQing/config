*helphelp.txt*  Nvim


      VIM 参考手册    by Bram Moolenaar


帮助文件的帮助          *helphelp*

                                      输入 |gO| 查看目录。

==============================================================================
帮助标签约定          *help-context*

获取特定帮助： 可以通过向 |:help| 命令传递参数，直接转到你想要的任何帮助主题。在参数前添加一些前缀来指定上下文： >

    内容        前缀      示例
      普通模式命令              :help x
      可视模式命令           v_     :help v_u
      插入模式命令           i_     :help i_<Esc>
      命令行命令            :    :help :quit
      命令行编辑            c_     :help c_<Del>
      Vim 命令参数        -    :help -r
      选项            '    :help 'textwidth'
      正则表达式        /    :help /[

更多上下文和说明请参见 |help-summary|。
关于帮助语法的说明请参见 |notation|。

搜索帮助

输入 “:help word”，然后按 CTRL-D 查看 “word” 的匹配帮助条目。或者使用 “:helpgrep word”。 |:helpgrep|

==============================================================================
表示法            *notation*

当使用语法高亮阅读本文时，非字面输入的文本通常会使用 Special 组高亮。这些是 []、{} 和 <> 中的项目，以及 CTRL-X。

请注意，Vim 在命令中使用所有可能的字符。有时 []、{} 和 <> 是你输入内容的一部分，上下文应该能清楚说明这一点。


- []            方括号中的字符是可选的。

                                                    *count* *[count]*
- [count]       一个可选的数字，可以放在命令前面以倍增或迭代命令。如果未提供数字，除非另有说明，否则使用计数 1。请注意，在本手册中，[count] 不在命令描述中提及，而仅在解释中提及。这样做是为了更容易查找命令。如果 'showcmd' 选项已启用，则窗口底部会显示（部分）输入的计数。你可以使用 <Del> 删除最后一位数字（|N<Del>|）。

                                                        *[quotex]*
- ["x]          一个可选的寄存器指定，用于存储文本。请参阅 |registers|。x 是 'a' 到 'z' 或 'A' 到 'Z' 或 '"' 之间的单个字符，并且在某些情况下（使用 put 命令）是 '0' 到 '9'、'%'、'#' 或其他字符之间。大写和小写字母指定相同的寄存器，但小写字母用于覆盖之前的寄存器内容，而大写字母用于追加到之前的寄存器内容。如果没有 “"x” 或使用 “""”，则存储的文本将放入未命名寄存器。

                                                        *{}*
- {}            花括号表示必须出现的命令部分，但可以采用多个不同的值。Vim 和 Vi 之间的差异也在花括号中给出（这从上下文中可以清楚看出）。

                                                        *{char1-char2}*
- {char1-char2} 从 char1 到 char2 范围内的单个字符。例如：{a-z} 是一个小写字母。可以连接多个范围。例如，{a-zA-Z0-9} 是任何字母数字字符。

                                                *{motion}* *movement*
- {motion}      移动光标的命令。这些在 |motion.txt| 中解释。
                - 示例：
                  - `w`           到下一个单词的开头
                  - `b`           到当前单词的开头
                  - `4j`          向下四行
                  - `/The<CR>`    到下一个 “The” 出现的位置
                - 这用在 |operator| 命令之后，以移动经过要操作的文本。
                  - 如果移动包含计数，并且操作符也有计数，则两个计数相乘。例如：“2d3w” 删除六个单词。
                  - 移动可以是向后的，例如 “db” 删除到单词开头。
                  - 移动也可以是鼠标点击。但并非每个终端都支持鼠标。
                  - “:omap” 命令可用于在操作符挂起时映射字符。
                  - 可以使用 Ex 命令来移动光标。这可用于调用执行某些复杂移动的函数。无论使用什么 “:” 命令，移动始终是字符排他的。这意味着无法包含行尾的最后一个字符（除非设置了 'virtualedit'）。如果 Ex 命令更改了操作符起始位置之前的文本或跳转到另一个缓冲区，则结果不可预测。可以更改更下游的文本。如果当前缓冲区未被卸载，则可以跳转到另一个缓冲区。

                                                        *{Visual}*
- {Visual}      一个选定的文本区域。它以 “v”、“V” 或 CTRL-V 命令开始，然后可以使用任何光标移动命令来更改选定文本的结束位置。
                这在 |operator| 命令之前使用，以高亮要操作的文本。
                请参阅 |Visual-mode|。

                                                        *<character>*
- <character>   下表中的特殊字符，可选带有修饰符，或带有修饰符的单个 ASCII 字符。

                                                        *'character'*
- 'c'           一个 ASCII 字符。

                                                        *CTRL-{char}*
- CTRL-{char}   {char} 作为控制字符输入；也就是说，在按住 CTRL 键的同时输入 {char}。{char} 的大小写被忽略；因此 CTRL-A 和 CTRL-a 是等价的。但在某些终端和环境中，使用 SHIFT 键会产生不同的代码（例如 CTRL-SHIFT-a）；在这些环境中，使用 SHIFT 键不会触发像 CTRL-A 这样的命令。

                                                        *'option'*
- 'option'      可以设置为某个值的选项或参数，用单引号括起来。请参阅 |options|。

                                                        *quotecommandquote*
- “command”     对你可以输入的命令的引用，用双引号括起来。
- `command`     新风格的命令，将其与其他引用文本和字符串区分开来。

==============================================================================
帮助命令           *online-help*

      *help* *<Help>* *:h* *:help* *<F1>* *i_<F1>* *i_<Help>*
<Help>    或
:h[elp]     打开一个窗口并以只读模式显示帮助文件。如果已经打开了一个帮助窗口，则使用该窗口。否则，如果当前窗口使用屏幕的全宽或至少 80 个字符宽，则帮助窗口将出现在当前窗口的正上方。否则，新窗口将放在最顶部。
      'helplang' 选项用于选择语言，如果主帮助文件有多种语言版本可用。

      输入 |gO| 查看目录。

              *:help!*
:h[elp]!    以 DWIM（“Do What I Mean”）方式根据光标处的 |WORD| 猜测帮助标签：使用各种启发式方法修剪标点，直到找到有效的帮助标签。

      例如，将光标移动到此代码中的任何位置，然后运行 `:help!`： >

        local v = vim.version.parse(vim.system({'foo'}):wait().stdout)
<
      然后与 `:exe 'help' expand('<cword>')` 进行比较。

            *{subject}* *E149* *E661*
:h[elp] {subject} 类似于 “:help”，此外还跳转到标签 {subject}。例如：  >
        :help options

<     {subject} 可以包含通配符，如 “*”、“?” 和 “[a-z]”：
         :help z? 跳转到任何 “z” 命令的帮助
         :help z. 跳转到 “z.” 的帮助
      但是当标签存在时，它会被字面理解：
         :help :? 跳转到 “:?” 的帮助

      如果模式没有完全匹配，或者有多个匹配，将使用 “最佳” 匹配。使用复杂的算法来决定哪个匹配优于另一个。在计算中考虑这些项目：
      - 相同大小写的匹配远优于不同大小写的匹配。
      - 在非字母数字字符之后开始的匹配优于单词中间的匹配。
      - 在标签开头或附近开始的匹配优于更靠后的匹配。
      - 匹配的字母数字字符越多越好。
      - 匹配的长度越短越好。

      如果 {subject} 有多种语言版本可用，'helplang' 选项用于选择语言。要在特定语言中查找标签，请附加 “@ab”，其中 “ab” 是两个字母的语言代码。参见 |help-translated|。

      请注意，你提供的 {subject} 越长，找到的匹配项就越少。你可以通过使用命令行补全来了解其工作原理（在 “:help subject” 后输入 CTRL-D |c_CTRL-D|）。
      如果有多个匹配项，你可以通过按 CTRL-D 来列出它们。示例： >
        :help cont<Ctrl-D>

<     与其输入 “:help CTRL-V” 来搜索 CTRL-V 的帮助，你可以输入： >
        :help ^V
<     这也适用于其他字符，例如在插入模式下查找 CTRL-V 的帮助： >
        :help i^V
<
      也可以先执行 “:help”，然后在帮助窗口中使用 “:tag {pattern}”。然后可以使用 “:tnext” 命令跳转到其他匹配项，使用 “tselect” 列出匹配项并选择一个。 >
        :help index
        :tselect /.*mode

<     当没有参数时，你将看到 “help” 的匹配项，以避免列出所有可能的匹配项（那会非常慢）。
      显示的匹配项数量限制为 300。

      `:help` 命令后面可以跟 ‘|’ 和另一个命令，但你不需要在帮助命令中转义 ‘|’。所以这两个都可以工作： >
        :help |
        :help k| only
<     请注意，‘|’ 之前的空格被视为 “:help” 参数的一部分。
      你也可以使用 <NL> 或 <CR> 来分隔帮助命令和后面的命令。你需要先输入 CTRL-V 来插入 <NL> 或 <CR>。示例： >
        :help so<C-V><CR>only
<

:h[elp]! {subject}  类似于 “:help”，但优先选择与当前文件相同 |help-translated| 语言的标签。

      与 |:help!|（无 {subject}）无关。

              *:helpc* *:helpclose*
:helpc[lose]    关闭一个帮助窗口（如果存在）。
      Vim 将尝试将窗口布局（包括光标位置）恢复到最初打开帮助窗口之前的布局。这可能会导致触发多个自动命令。

              *:helpg* *:helpgrep*
:helpg[rep] {pattern}[@xx]
      搜索所有帮助文本文件，并列出包含 {pattern} 匹配项的行。跳转到第一个匹配项。
      可选的 [@xx] 指定仅查找 “xx” 语言中的匹配项。
      你可以使用 |quickfix| 命令浏览匹配项，例如，使用 |:cnext| 跳转到下一个。或者使用 |:cwindow| 在 quickfix 窗口中获取匹配项列表。
      {pattern} 用作 Vim 正则表达式 |pattern|。
      不使用 'ignorecase'，添加 “\c” 可忽略大小写。
      区分大小写搜索的示例： >
        :helpgrep Uganda
<     忽略大小写搜索的示例： >
        :helpgrep uganda\c
<     在法语帮助中搜索的示例： >
        :helpgrep backspace@fr
<     该模式不支持换行，必须在一行内匹配。你可以改用 |:grep|，但是你需要以一种复杂的方式获取帮助文件列表。
      后面不能跟另一个命令，所有内容都用作模式的一部分。但必要时可以使用 |:execute|。
      不会搜索压缩的帮助文件（Fedora 会压缩帮助文件）。

              *:lh* *:lhelpgrep*
:lh[elpgrep] {pattern}[@xx]
      与 “:helpgrep” 相同，不同之处在于使用位置列表而不是 quickfix 列表。如果帮助窗口已经打开，则使用该窗口的位置列表。否则，将打开一个新的帮助窗口，并设置该窗口的位置列表。当前窗口的位置列表不会改变。

              *:exu* *:exusage*
:exu[sage]    显示 Ex 命令的帮助。添加此命令是为了模拟 Nvi 命令。

              *:viu* *:viusage*
:viu[sage]    显示普通模式命令的帮助。添加此命令是为了模拟 Nvi 命令。

当未向 |:help| 提供参数时，将打开由 'helpfile' 选项给定的文件。否则，将在 'runtimepath' 选项中指定目录的所有 “doc/tags” 文件中搜索指定的标签。

如果你想在当前窗口中打开帮助，请参阅此技巧：|help-curwin|。

帮助窗口的初始高度可以通过 'helpheight' 选项设置（默认 20）。
            *help-buffer-options*
当创建帮助缓冲区时，会设置几个局部选项以确保帮助文本按预期显示：
    'iskeyword'   几乎所有 ASCII 字符，除了 ' '、'*'、'"' 和 '|'
    'foldmethod'  “manual”
    'tabstop'   8
    'arabic'    关闭
    'binary'    关闭
    'buflisted'   关闭
    'cursorbind'  关闭
    'diff'     关闭
    'foldenable'  关闭
    'list'      关闭
    'modifiable'  关闭
    'number'    关闭
    'relativenumber'  关闭
    'rightleft'   关闭
    'scrollbind'  关闭
    'spell'     关闭

通过使用标签跳转到特定主题。这可以通过两种方式完成：
- 在命令或选项的名称上使用 “CTRL-]” 命令。这仅在标签是关键字时有效。“<C-Leftmouse>” 和 “g<LeftMouse>” 的工作方式与 “CTRL-]” 完全相同。
- 使用 “:ta {subject}” 命令。这也适用于非关键字字符。

使用 CTRL-T 或 CTRL-O 跳回。
使用 “:q” 关闭帮助窗口。
使用 `g==` 执行当前的 Lua/Vimscript 代码块。

如果你正在查找的项目有多个匹配项，你可以通过以下方式跳转到每一个：
1. 打开一个帮助窗口
2. 使用 “:tag” 命令，并在标签前加斜杠。例如： >
  :tag /min
3. 使用 “:tnext” 跳转到下一个匹配的标签。

可以为插件和其他项目添加帮助文件。你不需要为此更改分发的帮助文件。请参阅 |add-local-help|。

要编写本地帮助文件，请参阅 |write-local-help|。

请注意，本地帮助文件中的标题行会自动添加到 “help.txt” 帮助文件的 “LOCAL ADDITIONS” 部分 |local-additions|。这是在 Vim 中查看文件时完成的，文件本身不会更改。它是通过遍历所有帮助文件并获取每个文件的第一行来完成的。$VIMRUNTIME/doc 中的文件会被跳过。

          *:helpt* *:helptags*
      *E150* *E151* *E152* *E153* *E154* *E670* *E856*
:helpt[ags] [++t] {dir}
      为目录 {dir} 生成帮助标签文件。
      当 {dir} 为 ALL 时，将使用 'runtimepath' 中的所有 “doc” 目录。

      扫描目录和子目录中的所有 “*.txt” 和 “*.??x” 文件，以查找星号之间的帮助标签定义。“*.??x” 文件用于翻译文档，它们生成 “tags-??” 文件，参见 |help-translated|。生成的标签文件已排序。
      当存在重复项时，会给出错误消息。现有的标签文件会被静默覆盖。

      可选的 “++t” 参数强制添加 “help-tags” 标签。当 {dir} 等于 $VIMRUNTIME/doc 时，也会执行此操作。

      要重建运行时目录中的帮助标签（需要具有写入权限）： >
        :helptags $VIMRUNTIME/doc
<


==============================================================================
翻译的帮助文件         *help-translated*

可以在原始英文帮助文件旁边添加翻译的帮助文件。Vim 将在 'runtimepath' 中的 “doc” 目录中搜索所有帮助。

目前有以下语言的翻译版本：
  中文   - 多位作者
  法语   - 由 David Blanchet 翻译
  意大利语 - 由 Antonio Colombo 翻译
  日语   - 多位作者
  波兰语   - 由 Mikolaj Machowski 翻译
  俄语   - 由 Vassily Ragosin 翻译
请访问 Vim 网站找到它们：https://www.vim.org/translations.php

一套翻译的帮助文件由以下文件组成：

  help.abx
  howto.abx
  ...
  tags-ab

“ab” 是两个字母的语言代码。因此对于意大利语，名称是：

  help.itx
  howto.itx
  ...
  tags-it

'helplang' 选项可以设置为首选语言。默认根据环境设置。Vim 将首先尝试在首选语言中找到匹配的标签。如果找不到，则使用英语。

要在特定语言中查找标签，请在标签后附加 “@ab”，其中 “ab” 是两个字母的语言代码。示例： >
  :he user-manual@it
  :he user-manual@en
第一个查找意大利语用户手册，即使 'helplang' 为空。第二个查找英语用户手册，即使 'helplang' 设置为 “it”。

当为 “:help” 命令使用命令行补全时，仅当标签存在多种语言版本时才会显示 “@en” 扩展名。当标签仅存在英语版本时，省略 “@en”。当第一个候选项具有 “@ab” 扩展名并且它与 'helplang' 中的第一种语言匹配时，也会省略 “@ab”。

在非英语帮助文件中使用 |CTRL-]| 或 “:help!” 时，Vim 将尝试以相同语言查找标签。如果找不到，则将使用 'helplang' 来选择语言。

帮助文件必须使用 latin1 或 utf-8 编码。当在第一行中发现非 ASCII 字符时，Vim 假定编码为 utf-8。因此，你必须翻译带有 “For Vim version” 的标题。

对于同一目录中的一种语言的帮助文件，必须使用相同的编码。你可以为不同语言使用不同的编码，并且可以为同一语言但在不同目录中的帮助文件使用不同的编码。

给翻译人员的提示：
- 不要翻译标签。这样可以使得使用 'helplang' 来指定首选语言成为可能。你可以用你的语言添加新标签。
- 当你不翻译文件的一部分时，请使用 “tag@en” 表示法将标签添加到英文版本中。
- 制作一个包含所有文件和标签文件的包供下载。用户可以将其放入其中一个 “doc” 目录并开始使用。报告给开发团队，以便他们可以在 www.vim.org 上添加链接。
- 使用 |:helptags| 命令生成标签文件。它将在指定目录中找到所有语言。

==============================================================================
编写帮助文件          *help-writing*

为了方便使用，插件的 Vim 帮助文件应遵循标准 Vim 帮助文件的格式，第一行除外。如果你正在编写一个新的帮助文件，最好复制一个现有文件并将其用作模板。

帮助文件的第一行应具有以下格式： >

  *plugin_name.txt* {插件的简短描述}

第一个字段是一个帮助标签，其中 “:help plugin_name” 将跳转到该标签。该行的其余部分（在 Tab 之后）以简短的方式描述插件的用途。这将显示在主帮助文件的 “LOCAL ADDITIONS” 部分。检查它是否正确显示：|local-additions|。

如果你想添加版本号或最后修改日期，请将其放在第二行，右对齐。

在帮助文件的底部，放置一个 Vim 模型行来设置 'textwidth' 和 'tabstop' 选项以及将 'filetype' 设置为 “help”。切勿在此类模型行中设置全局选项，这可能会产生不良后果。


标签

要定义帮助标签，请将名称放在星号之间（“*tag-name*”）。标签名称应与所有 Vim 帮助标签名称不同，并且理想情况下应以 Vim 插件的名称开头。标签名称通常在一行上右对齐。

当引用现有的帮助标签并创建热链接时，请将名称放在两个竖线之间（“|”），例如 |help-writing|。

当在帮助文件中引用 Vim 选项时，请将选项名称放在两个单引号之间，例如 'statusline'

当引用任何其他技术术语或符号（例如文件名或函数参数）时，请用反引号将其括起来，例如 `~/.path/to/init.vim`。这会将其呈现为“内联”代码（与 “代码块” |help-codeblock| 相对）。


高亮

要定义列标题，请在该行末尾使用波浪号字符，前面加一个空格。这将以不同的颜色高亮列标题。例如：

列标题 ~

要分隔帮助文件中的章节，请在一行中放置一系列 ‘=’ 字符，从第一列开始。节分隔线将以不同方式高亮。

                    *help-codeblock*
要逐字引用 ex 命令块，请在块之前的行尾放置一个大于号（>）字符，并在块之后的行中，将小于号（<）字符作为第一个非空白字符。任何从第一列开始的行也会在其之前隐式停止 ex 命令块。例如： >
  function Example_Func()
    echo "Example"
  endfunction
<
要为代码块启用语法高亮，请在大于号（>）字符后放置一个语言名称注释（例如 “vim”）。例如： >vim
  function Example_Func()
    echo "Example"
  endfunction
<
            *help-notation*
以下内容在 Vim 帮助文件中以不同方式高亮：
  - 特殊键名，要么以 <> 表示法表示，如 <PageDown>，要么以 Ctrl 字符表示，如 CTRL-X
  - {braces} 之间的任何内容，例如 {lhs} 和 {rhs}

“Note”、“Notes” 及类似的词会自动获得独特的高亮。以下内容也是如此：
  Todo  要做的事情
  Error 错误的事情

你可以在 $VIMRUNTIME/syntax/help.vim 中找到详细信息


文件类型补全         *ft-help-omni*

要在编写标签引用时获取帮助标签的补全，可以使用 |i_CTRL-X_CTRL-O| 命令。

vim:tw=78:ts=8:noet:ft=help:norl:
