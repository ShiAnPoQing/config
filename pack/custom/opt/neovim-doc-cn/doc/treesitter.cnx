*treesitter.txt*    Nvim


                            NVIM 参考手册


Treesitter 集成                                               *treesitter*

Nvim 集成了 `tree-sitter` 库，用于缓冲区的增量解析：
https://tree-sitter.github.io/tree-sitter/

警告：Treesitter 支持仍是实验性的，并且会频繁更改。本文档可能也无法完全反映最新的更改。

                                     输入 |gO| 查看目录。

==============================================================================
解析器文件                                                  *treesitter-parsers*

解析器是 treesitter 的核心。它们是 treesitter 在 `parser` 运行时目录中搜索的库。

Nvim 包含这些解析器：

- C
- Lua
- Markdown
- Vimscript
- Vimdoc
- Treesitter 查询文件 |ft-query-plugin|

您可以手动安装更多解析器，或使用像 https://github.com/nvim-treesitter/nvim-treesitter
这样的插件。

解析器作为 `parser/{lang}.*` 在任何 'runtimepath' 目录中被搜索。
如果找到同一语言的多个解析器，则使用第一个。
（注意：这通常意味着优先级为“用户配置 > 插件 > 内置”。）

要从其文件路径加载解析器： >lua

    vim.treesitter.language.add('python', { path = "/path/to/python.so" })
<
如果文件系统区分大小写，解析器名称假定为小写。

要将某些 |filetypes| 与 treesitter 语言（解析器名称）关联，请使用
|vim.treesitter.language.register()|。例如，要对文件类型为 `svg` 或 `xslt` 的缓冲区
使用 `xml` treesitter 解析器： >lua

    vim.treesitter.language.register('xml', { 'svg', 'xslt' })
<
                                                    *treesitter-parsers-wasm*

如果 Nvim 使用 `ENABLE_WASMTIME` 构建，那么 wasm 解析器也可以被加载： >lua

    vim.treesitter.language.add('python', { path = "/path/to/python.wasm" })
<

==============================================================================
TREESITTER 查询                                          *treesitter-query*

Treesitter 查询是一种从解析后的 |TSTree| 中提取信息的方法，例如用于语法高亮。
简而言之，一个 `query` 由一个或多个模式组成。一个 `pattern` 是根据语法树中的节点类型
定义的。一个 `match` 对应于语法树中匹配某个模式的特定元素。模式可以可选地定义捕获
和谓词。`capture`（捕获）允许您将名称与模式中的特定节点关联。`predicate`（谓词）向
匹配添加任意的元数据和条件数据。

查询是用一种类似 Lisp 的语言编写的，该语言在
https://tree-sitter.github.io/tree-sitter/using-parsers/queries/1-syntax.html
中有文档说明。注意：那里列出的谓词与 Nvim 支持的谓词不同。有关 Nvim 支持的谓词
完整列表，请参见 |treesitter-predicates|。

Nvim 在 `runtimepath` 下的 `queries` 目录中查找 `*.scm` 文件作为查询，
每个文件包含特定语言和用途的查询，例如 `queries/lua/highlights.scm` 用于高亮 Lua 文件。
默认情况下，使用 `runtimepath` 上的第一个查询（这通常意味着用户配置优先于插件，
插件优先于 Nvim 捆绑的查询）。如果一个查询应该扩展其他查询而不是替换它们，
请使用 |treesitter-query-modeline-extends|。

Lua 接口在 |lua-treesitter-query| 中描述。


TREESITTER 查询谓词                                  *treesitter-predicates*

谓词是特殊的 scheme 节点，用于条件性地捕获节点。例如，`eq?` 谓词可以如下使用： >query

    ((identifier) @variable.builtin
      (#eq? @variable.builtin "self"))
<
以仅匹配文本对应于 `"self"` 的标识符。例如，此类查询可用于以不同方式高亮内置函数
或变量。

以下谓词是内置的：

    `eq?`                                            *treesitter-predicate-eq?*
        将字符串与节点对应的文本进行匹配： >query
            ((identifier) @variable.builtin (#eq? @variable.builtin "self"))
            ((node1) @left (node2) @right (#eq? @left @right))
<
    `any-eq?`                                    *treesitter-predicate-any-eq?*
        类似于 `eq?`，但对于量词模式，只需有一个被捕获的节点匹配即可。

    `match?`                                      *treesitter-predicate-match?*
    `vim-match?`                              *treesitter-predicate-vim-match?*
         将 |regexp| 与节点对应的文本进行匹配： >query
            ((identifier) @constant (#match? @constant "^[A-Z_]+$"))
<
         注意：`^` 和 `$` 锚点将匹配节点文本的开头和结尾。

    `any-match?`                              *treesitter-predicate-any-match?*
    `any-vim-match?`                      *treesitter-predicate-any-vim-match?*
        类似于 `match?`，但对于量词模式，只需有一个被捕获的节点匹配即可。

    `lua-match?`                              *treesitter-predicate-lua-match?*
         将 |lua-pattern| 与节点对应的文本进行匹配，类似于 `match?`

    `any-lua-match?`                      *treesitter-predicate-any-lua-match?*
         类似于 `lua-match?`，但对于量词模式，只需有一个被捕获的节点匹配即可。

    `contains?`                                *treesitter-predicate-contains?*
        将字符串与节点对应文本的部分内容进行匹配： >query
            ((identifier) @foo (#contains? @foo "foo"))
            ((identifier) @foo-bar (#contains? @foo-bar "foo" "bar"))
<
    `any-contains?`                        *treesitter-predicate-any-contains?*
        类似于 `contains?`，但对于量词模式，只需有一个被捕获的节点匹配即可。

    `any-of?`                                    *treesitter-predicate-any-of?*
        将任意给定的字符串与节点对应的文本进行匹配： >query
            ((identifier) @foo (#any-of? @foo "foo" "bar"))
<
        这是检查节点是否匹配多个关键字之一的推荐方式，因为它已为此进行了优化。

    `has-ancestor?`                        *treesitter-predicate-has-ancestor?*
        将任意给定的节点类型与节点的所有祖先进行匹配： >query
            ((identifier) @variable.builtin
              (#any-of? @variable.builtin "begin" "end")
              (#has-ancestor? @variable.builtin range_expression))
<
    `has-parent?`                            *treesitter-predicate-has-parent?*
        将任意给定的节点类型与节点的直接父节点进行匹配： >query
            (((field_expression
                 (field_identifier) @method)) @_parent
             (#has-parent? @_parent template_method function_declarator))
<
                                                    *treesitter-predicate-not*
每个谓词都有一个带 `not-` 前缀的谓词，即该谓词的否定形式。

                                                    *treesitter-predicate-all*
                                                    *treesitter-predicate-any*
查询可以使用量词来捕获多个节点。当捕获包含多个节点时，只有捕获中包含的 ALL（所有）
节点都匹配谓词时，谓词才匹配。某些谓词（`eq?`、`match?`、`lua-match?`、`contains?`）
接受一个 `any-` 前缀，以改为在捕获中包含的 ANY（任一）节点匹配谓词时即匹配。

例如，考虑以下 Lua 代码： >lua

  -- TODO: 这是一个
  -- 非常长的
  -- 注释（想象一下）
<
使用以下带谓词的查询：
>query
    (((comment)+ @comment)
     (#match? @comment "TODO"))
<
此查询不会匹配，因为并非所有被 @comment 捕获的节点都匹配谓词。应改用：
>query
    (((comment)+ @comment)
     (#any-match? @comment "TODO"))
<

可以通过 |vim.treesitter.query.add_predicate()| 添加更多谓词。
使用 |vim.treesitter.query.list_predicates()| 列出所有可用的谓词。


TREESITTER 查询指令                                  *treesitter-directives*

Treesitter 指令为节点或匹配存储元数据，并执行副作用。例如，`set!` 指令设置匹配或节点的
元数据： >query

        ((identifier) @foo (#set! type "parameter"))
<
以下指令是内置的：

    `set!`                                          *treesitter-directive-set!*
        为特定的匹配或捕获设置键/值元数据。值可以作为 `metadata[key]`
        （匹配特定）或 `metadata[capture_id][key]`（捕获特定）访问。

        参数: ~
            {capture_id} （可选）
            {key}
            {value}

        示例: >query
            ((identifier) @foo (#set! @foo kind "parameter"))
            ((node1) @left (node2) @right (#set! type "pair"))
            ((codeblock) @markup.raw.block (#set! priority 90))
<
    `offset!`                                      *treesitter-directive-offset!*
        获取被捕获节点的范围并应用偏移量。这将为带有 `capture_id` 的被捕获节点设置一个新范围，
        形式为 `metadata[capture_id].offset` 中的列表，如 { {start_row}, {start_col},
        {end_row}, {end_col} }。此偏移量将应用于 |vim.treesitter.get_range()| 返回的范围。
        对于 |treesitter-language-injections| 很有用。

        参数: ~
            {capture_id}
            {start_row}
            {start_col}
            {end_row}
            {end_col}

        示例: >query
            ((identifier) @constant (#offset! @constant 0 1 0 -1))
<
    `gsub!`                                          *treesitter-directive-gsub!*
        使用 |lua-pattern| 转换节点的内容。这将设置一个新的 `metadata[capture_id].text`。

        参数: ~
            {capture_id}
            {pattern}
            {replacement}

        示例: >query
            (#gsub! @_node ".*%.(.*)" "%1")
<
    `trim!`                                          *treesitter-directive-trim!*
        从节点修剪空白。设置一个新的 `metadata[capture_id].range`。
        接受一个捕获 ID，并可选的四个整数来自定义修剪行为（`1` 表示修剪，`0` 表示不修剪）。
        当仅给定捕获 ID 时，从节点末尾修剪空白行（仅包含空白或为空的行）
        （为了向后兼容）。如果给定参数，可以从节点的两侧修剪所有空白。

        示例: >query
            ; 仅从节点末尾修剪空白行
            ; （等同于 (#trim! @fold 0 0 1 0)）
            (#trim! @fold)

            ; 从节点两侧修剪空白行
            (#trim! @fold 1 0 1 0)

            ; 修剪节点周围的所有空白
            (#trim! @fold 1 1 1 1)
<
        参数: ~
            {capture_id}
            {trim_start_linewise}
            {trim_start_charwise}
            {trim_end_linewise}（如果仅给出 {capture_id}，默认为 `1`）
            {trim_end_charwise}

可以通过 |vim.treesitter.query.add_directive()| 添加更多指令。
使用 |vim.treesitter.query.list_directives()| 列出所有可用的指令。


TREESITTER 查询模型线                            *treesitter-query-modeline*

Nvim 支持使用一组“模型线”来自定义查询的行为，即查询中以 `;` 开头的注释。
以下是当前支持的模型线替代方案：

    `inherits: {lang}...`                     *treesitter-query-modeline-inherits*
        指定此查询应继承自 {lang} 的查询。这将递归地深入 {lang} 的查询，除非用括号包裹：
        `({lang})`。
        注意：这用于包含来自另一种语言的查询。如果您希望您的查询扩展同一种语言的查询，
        请使用 `extends`。

    `extends`                                  *treesitter-query-modeline-extends*
        指定此查询应作为查询的扩展，即它应该与其他查询合并。
        注意：扩展的顺序以及将用作基础的查询取决于您的 'runtimepath' 值。

注意：这些模型线注释必须位于查询的顶部，但可以重复，例如，以下两个模型线块都是有效的：
>query
    ;; inherits: typescript,jsx
    ;; extends
<
>query
    ;; extends
    ;;
    ;; inherits: css
<
==============================================================================
TREESITTER 语法高亮                              *treesitter-highlight*

语法高亮通过名为 `highlights.scm` 的查询指定，该查询将解析后的 |TSTree| 中的
|TSNode| 匹配到一个可以分配高亮组的 `capture`。例如，查询 >query

    (parameters (identifier) @variable.parameter)
<
将函数 `parameters` 节点内的任何 `identifier` 节点匹配到名为 `@variable.parameter` 的捕获。
例如，对于 Lua 代码 >lua

    function f(foo, bar) end
<
它将被解析为（参见 |:InspectTree|）： >query

    (function_declaration ; [1:1 - 24]
      name: (identifier) ; [1:10 - 10]
      parameters: (parameters ; [1:11 - 20]
        name: (identifier) ; [1:12 - 14]
        name: (identifier))) ; [1:17 - 19]
<
上述查询将高亮 `foo` 和 `bar` 为 `@variable.parameter`。

也可以匹配字面表达式（前提是解析器返回它们）：
>query
    [
      "if"
      "else"
    ] @keyword.conditional
<
假设在 runtimepath 中找到了合适的解析器和 `highlights.scm` 查询，可以通过
|vim.treesitter.start()| 简单地为当前缓冲区启用 treesitter 高亮。

                                                 *treesitter-highlight-groups*
捕获名称，以 `@` 为前缀，可以直接用作高亮组。对于许多常用的捕获，相应的高亮组默认
链接到 Nvim 的标准 |highlight-groups|（例如，`@comment` 链接到 `Comment`），但可以
在配色方案中被覆盖。

实现了一个回退系统，因此更具体的组会回退到更通用的组。例如，在具有独立文档注释的语言中
（如 c、java 等），可以使用 `@comment.documentation`。如果未定义此组，则使用普通
`@comment` 的高亮。这样，现有的配色方案已经可以开箱即用，但可以为提供这些更具体变体的
查询添加它们。

作为附加规则，捕获高亮始终可以通过在附加点后附加语言名称来按语言特化。例如，要按语言
不同地高亮注释： >vim

    hi @comment.c guifg=Blue
    hi @comment.lua guifg=DarkBlue
    hi link @comment.documentation.java String
<
以下是 Nvim 查询中使用的标准捕获列表，根据当前配色方案高亮显示（使用 |:Inspect|
查看确切定义）：

@variable                       各种变量名
@variable.builtin                内置变量名（例如 `this`、`self`）
@variable.parameter              函数的参数
@variable.parameter.builtin      特殊参数（例如 `_`、`it`）
@variable.member                 对象和结构体字段

@constant               常量标识符
@constant.builtin       内置常量值
@constant.macro         由预处理器定义的常量

@module                 模块或命名空间
@module.builtin         内置模块或命名空间
@label                   `GOTO` 和其他标签（例如 C 中的 `label:`），包括 heredoc 标签

@string                 字符串字面量
@string.documentation   记录代码的字符串（例如 Python 文档字符串）
@string.regexp          正则表达式
@string.escape          转义序列
@string.special         其他特殊字符串（例如日期）
@string.special.symbol  符号或原子
@string.special.path    文件名
@string.special.url     URI（例如超链接）

@character              字符字面量
@character.special      特殊字符（例如通配符）

@boolean                布尔字面量
@number                 数字字面量
@number.float           浮点数数字字面量

@type                   类型或类定义及注解
@type.builtin           内置类型
@type.definition        类型定义中的标识符（例如 C 中的 `typedef <type> <identifier>`）

@attribute              属性注解（例如 Python 装饰器、Rust 生命周期）
@attribute.builtin      内置注解（例如 Python 中的 `@property`）
@property               键值对中的键

@function               函数定义
@function.builtin       内置函数
@function.call          函数调用
@function.macro         预处理器宏

@function.method        方法定义
@function.method.call   方法调用

@constructor            构造函数调用和定义
@operator               符号运算符（例如 `+`、`*`）

@keyword                不属于特定类别的关键字
@keyword.coroutine      与协程相关的关键字（例如 Go 中的 `go`，Python 中的 `async/await`）
@keyword.function       定义函数的关键字（例如 Go 中的 `func`，Python 中的 `def`）
@keyword.operator       作为英文单词的运算符（例如 `and`、`or`）
@keyword.import         用于包含或导出模块的关键字（例如 Python 中的 `import`、`from`）
@keyword.type           描述命名空间和复合类型的关键字（例如 `struct`、`enum`）
@keyword.modifier       修改其他结构的关键字（例如 `const`、`static`、`public`）
@keyword.repeat         与循环相关的关键字（例如 `for`、`while`）
@keyword.return         像 `return` 和 `yield` 这样的关键字
@keyword.debug          与调试相关的关键字
@keyword.exception      与异常相关的关键字（例如 `throw`、`catch`）

@keyword.conditional         与条件语句相关的关键字（例如 `if`、`else`）
@keyword.conditional.ternary 三元运算符（例如 `?`、`:`）

@keyword.directive           各种预处理器指令和 shebang
@keyword.directive.define    预处理器定义指令

@punctuation.delimiter  分隔符（例如 `;`、`.`、`,`）
@punctuation.bracket    括号（例如 `()`、`{}`、`[]`）
@punctuation.special    特殊符号（例如字符串插值中的 `{}`）

@comment                行注释和块注释
@comment.documentation  记录代码的注释

@comment.error          错误类型注释（例如 `ERROR`、`FIXME`、`DEPRECATED`）
@comment.warning        警告类型注释（例如 `WARNING`、`FIX`、`HACK`）
@comment.todo           todo 类型注释（例如 `TODO`、`WIP`）
@comment.note           备注类型注释（例如 `NOTE`、`INFO`、`XXX`）

@markup.strong          粗体文本
@markup.italic          斜体文本
@markup.strikethrough   删除线文本
@markup.underline       下划线文本（仅用于字面下划线标记！）

@markup.heading         标题（包括标记）
@markup.heading.1       顶级标题
@markup.heading.2       章节标题
@markup.heading.3       子章节标题
@markup.heading.4       依此类推
@markup.heading.5       等等
@markup.heading.6       六级标题对任何人来说都应该足够了

@markup.quote           块引用
@markup.math            数学环境（例如 LaTeX 中的 `$ ... $`）

@markup.link            文本引用、脚注、引文等
@markup.link.label      链接、引用描述
@markup.link.url        URL 风格的链接

@markup.raw             字面量或原样文本（例如内联代码）
@markup.raw.block       作为独立块的字面量或原样文本

@markup.list            列表标记
@markup.list.checked    已勾选的待办事项列表标记
@markup.list.unchecked  未勾选的待办事项列表标记

@diff.plus              添加的文本（用于 diff 文件）
@diff.minus             删除的文本（用于 diff 文件）
@diff.delta             更改的文本（用于 diff 文件）

@tag                    XML 风格的标签名（例如在 XML、HTML 等中）
@tag.builtin            内置标签名（例如 HTML5 标签）
@tag.attribute          XML 风格的标签属性
@tag.delimiter          XML 风格的标签分隔符

                                                  *treesitter-highlight-spell*
特殊的 `@spell` 捕获可用于指示 Nvim 的内置 |spell| 检查器应对该节点进行拼写检查。
例如，以下捕获标记注释为待检查： >query

    (comment) @spell
<

还有 `@nospell`，用于禁用带有 `@spell` 的区域的拼写检查。

                                                *treesitter-highlight-conceal*
Treesitter 高亮通过 `conceal` 和 `conceal_lines` 元数据支持 |conceal|。
按照惯例，要隐藏的节点被捕获为 `@conceal`，但任何捕获都可以使用。
例如，以下查询可用于隐藏 Markdown 中的代码块分隔符： >query

    ((fenced_code_block_delimiter) @conceal (#set! conceal ""))
<
也可以用单个字符替换一个节点，该字符（与旧式语法不同）可以赋予自定义高亮。
例如，以下（不推荐的）查询将 `!=` 运算符替换为一个 Unicode 字形，该字形仍然与
其他运算符一样被高亮： >query

    "!=" @operator (#set! conceal "≠")
<
要隐藏整行（完全不绘制它），可以使用带有 `conceal_lines` 元数据的查询： >query

    ((comment) @comment @spell
      (#set! conceal_lines ""))
<
以这种方式指定的隐藏遵循 'conceallevel' 和 'concealcursor'。

请注意，虽然您可以为 `conceal` 使用任何字符串，但只会使用第一个字符： >query

    ; 标识符将被隐藏为 'f'。
    ((identifier) @conceal (#set! conceal "foo"))
<

                                               *treesitter-highlight-priority*
Treesitter 使用 |nvim_buf_set_extmark()| 来设置高亮，默认优先级为 100。
这使得插件可以设置低于或高于 treesitter 的高亮优先级。也可以手动更改单个查询模式的
优先级，通过设置其 `"priority"` 元数据属性： >query

    ((super_important_node) @superimportant (#set! priority 105))
<

                                          *treesitter-highlight-commentstring*
Treesitter 高亮支持更细粒度的 'commentstring'，由内置的 |commenting| 插件使用。
当光标位于设置了 `bo.commentstring` 元数据属性（|treesitter-directive-set!|）的
节点内时，该属性定义注释分隔符（“最内层获胜”）。这对于像 `JSX` 这样根据代码区域
具有不同注释语法的语言很有用，例如： >query

    ((jsx_element) @_tag (#set! @_tag bo.commentstring "{/* %s */}"))
<
当多个捕获在一个区域内设置此元数据时，只有最内层（最具体）的捕获会被应用于给定区域。
==============================================================================
TREESITTER 语言注入                          *treesitter-language-injections*

注意以下信息改编自：
https://tree-sitter.github.io/tree-sitter/3-syntax-highlighting.html#language-injection

某些源文件包含用多种不同语言编写的代码。示例包括：

    • HTML 文件，可以在 `<script>` 标签内包含 JavaScript，在 `<style>` 标签内包含 CSS
    • ERB 文件，在 `<%` `%>` 标签内包含 Ruby，在这些标签外包含 HTML
    • PHP 文件，可以在 `<php` 标签之间包含 HTML
    • JavaScript 文件，在正则表达式字面量中包含正则表达式语法
    • Ruby，可以在 heredoc 字面量中包含代码片段，其中 heredoc 分隔符通常指示语言
    • Lua，可以在 |vim.cmd()| 调用中包含 Vimscript 代码片段。
    • Vimscript，可以在 |:lua-heredoc| 块中包含 Lua 代码片段。

所有这些示例都可以用一个父语法树和位于父树中某些节点内的一个或多个注入语法树来建模。
语言注入查询允许您使用以下捕获来指定这些“注入”：

    • `@injection.content` - 指示应使用另一种语言重新解析被捕获节点的内容。
      如果一个模式中有多个 `@injection.content` 捕获，所有范围将被收集并作为一个树
      进行解析。这允许查询作者使用注入查询量词创建“有作用域的”注入。
    • `@injection.language` - 指示被捕获节点的文本可能包含语言名称，该语言应用于
      重新解析 `@injection.content`。
    • `@injection.filename` - 指示被捕获节点的文本可能包含文件名；然后通过
      |vim.filetype.match()| 查找相应的文件类型，并将其视为应用于重新解析
      `@injection.content` 的语言名称。

语言注入行为也可以通过一些与模式关联的属性进行配置：

    • `injection.language` - 可用于硬编码特定语言的名称。
    • `injection.combined` - 指示树中所有匹配的节点的内容应作为一个嵌套文档进行解析。
    • `injection.include-children` - 指示应重新解析 `@injection.content` 节点的完整文本，
      包括其子节点的文本。默认情况下，子节点的文本将从注入的文档中排除。
    • `injection.self` - 指示节点的文本应使用与该节点的 LanguageTree 相同的语言进行解析。
    • `injection.parent` - 指示被捕获节点的文本应使用与该节点的父级 LanguageTree
      相同的语言进行解析。

注入查询当前在整个缓冲区上运行，这对于大型缓冲区可能很慢。要为例如 `c` 禁用注入，
只需在您的 'runtimepath' 中放置一个空的 `queries/c/injections.scm` 文件。

==============================================================================
默认值                                                    *treesitter-defaults*

							*v_an*
an			选择第 [count] 个父节点。

			如果缓冲区没有 treesitter 解析器，
			则回退到 |vim.lsp.buf.selection_range()|。

							*v_in*
in			选择第 [count] 个前一个（或第一个）子节点。

			如果缓冲区没有 treesitter 解析器，
			则回退到 |vim.lsp.buf.selection_range()|。

							*v_]n*
]n			选择第 [count] 个下一个节点。

							*v_[n*
[n			选择第 [count] 个前一个节点。

==============================================================================
VIM.TREESITTER                                                *lua-treesitter*

本文档的其余部分是 `vim.treesitter` Lua 模块的参考手册，该模块是 Nvim 的
treesitter 集成的主要接口。以下大部分内容是从函数文档自动生成的。


                                             *vim.treesitter.language_version*
捆绑的 treesitter 库支持的最新解析器 ABI 版本。

                                     *vim.treesitter.minimum_language_version*
捆绑的 treesitter 库支持的最早解析器 ABI 版本。

==============================================================================
TREESITTER 树                                    *treesitter-tree* *TSTree*

“treesitter 树”表示缓冲区解析后的内容，可用于执行进一步的分析。它是 treesitter 库
持有的对象的 |userdata| 引用。

treesitter 树的实例 `TSTree` 支持以下方法。


TSTree:copy()                                                  *TSTree:copy()*
    返回 `TSTree` 的副本。

    返回值: ~
        (`TSTree`)

TSTree:root()                                                  *TSTree:root()*
    返回此树的根节点。

    返回值: ~
        (`TSNode`)


==============================================================================
TREESITTER 节点                                    *treesitter-node* *TSNode*

“treesitter 节点”表示缓冲区解析内容的一个特定元素，可以被 |Query| 捕获用于例如高亮。
它是 treesitter 库持有的对象的 |userdata| 引用。

treesitter 节点的实例 `TSNode` 支持以下方法。


TSNode:byte_length()                                    *TSNode:byte_length()*
    返回此节点跨越的字节数。

    返回值: ~
        (`整数`)

TSNode:child({index})                                         *TSNode:child()*
    获取节点在给定 {index} 处的子节点，其中零代表第一个子节点。

    参数: ~
      • {index}  (`整数`)

    返回值: ~
        (`TSNode?`)

TSNode:child_count()                                    *TSNode:child_count()*
    获取节点的子节点数量。

    返回值: ~
        (`整数`)

                                              *TSNode:child_with_descendant()*
TSNode:child_with_descendant({descendant})
    获取节点中包含 {descendant} 的子节点（包括 {descendant}）。

    例如，对于以下节点层次结构： >
        a -> b -> c

        a:child_with_descendant(c) == b
        a:child_with_descendant(b) == b
        a:child_with_descendant(a) == nil
<

    参数: ~
      • {descendant}  (`TSNode`)

    返回值: ~
        (`TSNode?`)

                                               *TSNode:descendant_for_range()*
TSNode:descendant_for_range({start_row}, {start_col}, {end_row}, {end_col})
    获取此节点内跨越给定（行，列）位置范围的最小节点。

    参数: ~
      • {start_row}  (`整数`)
      • {start_col}  (`整数`)
      • {end_row}    (`整数`)
      • {end_col}    (`整数`)

    返回值: ~
        (`TSNode?`)

TSNode:end_()                                                  *TSNode:end_()*
    获取节点的结束位置。返回三个值：行、列和总字节数（均从零开始）。

    返回（多个）: ~
        (`整数`)
        (`整数`)
        (`整数`)

TSNode:equal({node})                                          *TSNode:equal()*
    检查 {node} 是否指向同一棵树内的相同节点。

    参数: ~
      • {node}  (`TSNode`)

    返回值: ~
        (`布尔值`)

TSNode:extra()                                                *TSNode:extra()*
    检查节点是否为额外节点。额外节点表示像注释这样的内容，它们不是语法所必需的，但可以出现在任何地方。

    返回值: ~
        (`布尔值`)

TSNode:field({name})                                          *TSNode:field()*
    返回具有给定字段名称的节点所有子节点的列表。

    参数: ~
      • {name}  (`字符串`)

    返回值: ~
        (`TSNode[]`)

TSNode:has_changes()                                    *TSNode:has_changes()*
    检查语法节点是否已被编辑。

    返回值: ~
        (`布尔值`)

TSNode:has_error()                                        *TSNode:has_error()*
    检查节点是否是语法错误或包含任何语法错误。

    返回值: ~
        (`布尔值`)

TSNode:id()                                                      *TSNode:id()*
    获取节点在其自身树内的唯一标识符。

    对于此标识符的内部表示不做任何保证，除了它是一个具有值相等性的原始 Lua 类型
    （因此不是一个表）。目前它是一个（不可打印的）字符串。

    注意：`id` 不保证对于来自不同树的节点是唯一的。

    返回值: ~
        (`字符串`)

TSNode:iter_children()                                *TSNode:iter_children()*
    遍历 {TSNode} 的所有直接子节点，无论它们是否命名。返回子节点以及该子节点对应的
    最终字段名。

    返回值: ~
        (`fun(): TSNode, 字符串`)

TSNode:missing()                                            *TSNode:missing()*
    检查节点是否缺失。缺失节点是解析器为了从某些类型的语法错误中恢复而插入的。

    返回值: ~
        (`布尔值`)

TSNode:named()                                                *TSNode:named()*
    检查节点是否命名。命名节点对应于语法中的命名规则，而匿名节点对应于语法中的字符串字面量。

    返回值: ~
        (`布尔值`)

TSNode:named_child({index})                             *TSNode:named_child()*
    获取节点在给定 {index} 处的命名子节点，其中零代表第一个命名子节点。

    参数: ~
      • {index}  (`整数`)

    返回值: ~
        (`TSNode?`)

TSNode:named_child_count()                        *TSNode:named_child_count()*
    获取节点的命名子节点数量。

    返回值: ~
        (`整数`)

TSNode:named_children()                              *TSNode:named_children()*
    返回节点命名子节点的列表。

    返回值: ~
        (`TSNode[]`)

                                         *TSNode:named_descendant_for_range()*
TSNode:named_descendant_for_range({start_row}, {start_col}, {end_row},
                                  {end_col})
    获取此节点内跨越给定（行，列）位置范围的最小命名节点。

    参数: ~
      • {start_row}  (`整数`)
      • {start_col}  (`整数`)
      • {end_row}    (`整数`)
      • {end_col}    (`整数`)

    返回值: ~
        (`TSNode?`)

TSNode:next_named_sibling()                      *TSNode:next_named_sibling()*
    获取节点的下一个命名兄弟节点。

    返回值: ~
        (`TSNode?`)

TSNode:next_sibling()                                  *TSNode:next_sibling()*
    获取节点的下一个兄弟节点。

    返回值: ~
        (`TSNode?`)

TSNode:parent()                                              *TSNode:parent()*
    获取节点的直接父节点。对于遍历节点的祖先，优先使用 |TSNode:child_with_descendant()|。

    返回值: ~
        (`TSNode?`)

TSNode:prev_named_sibling()                      *TSNode:prev_named_sibling()*
    获取节点的上一个命名兄弟节点。

    返回值: ~
        (`TSNode?`)

TSNode:prev_sibling()                                  *TSNode:prev_sibling()*
    获取节点的上一个兄弟节点。

    返回值: ~
        (`TSNode?`)

TSNode:range({include_bytes})                                 *TSNode:range()*
    获取节点的范围。

    返回四个或六个值：
    • 起始行
    • 起始列
    • 起始字节（如果 {include_bytes} 为 `true`）
    • 结束行
    • 结束列
    • 结束字节（如果 {include_bytes} 为 `true`）

    参数: ~
      • {include_bytes}  (`false?`)

    重载: ~
      • `fun(self: TSNode, include_bytes: true): 整数, 整数, 整数, 整数, 整数, 整数`

    返回（多个）: ~
        (`整数`)
        (`整数`)
        (`整数`)
        (`整数`)

TSNode:sexpr()                                                *TSNode:sexpr()*
    获取表示节点的 S 表达式字符串。

    返回值: ~
        (`字符串`)

TSNode:start()                                                *TSNode:start()*
    获取节点的起始位置。返回三个值：行、列和总字节数（均从零开始）。

    返回（多个）: ~
        (`整数`)
        (`整数`)
        (`整数`)

TSNode:symbol()                                              *TSNode:symbol()*
    将节点的类型作为数字 ID 获取。

    返回值: ~
        (`整数`)

TSNode:tree()                                                  *TSNode:tree()*
    获取节点的 |TSTree|。

    返回值: ~
        (`TSTree`)

TSNode:type()                                                  *TSNode:type()*
    将节点的类型作为字符串获取。

    返回值: ~
        (`字符串`)


==============================================================================
Lua 模块: vim.treesitter                               *lua-treesitter-core*

foldexpr({lnum})                                   *vim.treesitter.foldexpr()*
    返回当前缓冲区中 {lnum} 的折叠级别。可以直接设置为 'foldexpr'： >lua
        vim.wo.foldexpr = 'v:lua.vim.treesitter.foldexpr()'
<

    属性: ~
        始于: 0.9.0

    参数: ~
      • {lnum}  (`整数?`) 要计算折叠级别的行号

    返回值: ~
        (`字符串`)

                                     *vim.treesitter.get_captures_at_cursor()*
get_captures_at_cursor({winnr})
    返回光标下的高亮捕获名称列表

    参数: ~
      • {winnr}  (`整数?`) |window-ID| 或 0 表示当前窗口（默认）

    返回值: ~
        (`字符串[]`) 捕获名称列表

                                        *vim.treesitter.get_captures_at_pos()*
get_captures_at_pos({bufnr}, {row}, {col})
    返回给定位置处的高亮捕获列表

    每个捕获由一个表表示，包含捕获名称字符串、捕获的语言、元数据表（`priority`、`conceal` 等；
    如果没有定义则为空），以及捕获的 ID。

    参数: ~
      • {bufnr}  (`整数`) 缓冲区编号（0 表示当前缓冲区）
      • {row}    (`整数`) 位置行
      • {col}    (`整数`) 位置列

    返回值: ~
        (`{capture: 字符串, lang: 字符串, metadata: vim.treesitter.query.TSMetadata, id: 整数}[]`)

get_node({opts})                                   *vim.treesitter.get_node()*
    返回给定位置处最小的命名节点

    注意：在未解析的树上调用此方法可能会产生无效节点。如果该树未通过例如活动的高亮器进行解析，
    请先通过以下方式解析树 >lua
        vim.treesitter.get_parser(bufnr):parse(range)
<

    参数: ~
      • {opts}  (`表?`) 可选关键字参数：
                • {bufnr} (`整数?`) 缓冲区编号（nil 或 0 表示当前缓冲区）
                • {pos} (`[整数, 整数]?`) 从零开始索引的 (row, col) 元组。
                  默认为当前窗口中的光标位置。如果 {bufnr} 不是当前缓冲区，则为必需。
                • {lang} (`字符串?`) 解析器语言。（默认：根据缓冲区文件类型）
                • {ignore_injections} (`布尔值?`) 忽略注入的语言（默认 true）
                • {include_anonymous} (`布尔值?`) 包含匿名节点（默认 false）

    返回值: ~
        (`TSNode?`) 给定位置处的节点

get_node_range({node_or_range})              *vim.treesitter.get_node_range()*
    返回节点的范围或解包的范围表

    参数: ~
      • {node_or_range}  (`TSNode|Range4`) 节点或位置表

    返回（多个）: ~
        (`整数`) start_row
        (`整数`) start_col（字节偏移量）
        (`整数`) end_row
        (`整数`) end_col（字节偏移量）

                                              *vim.treesitter.get_node_text()*
get_node_text({node}, {source}, {opts})
    获取给定节点对应的文本

    参数: ~
      • {node}    (`TSNode`)
      • {source}  (`整数|字符串`) 提取 {node} 的缓冲区或字符串
      • {opts}    (`表?`) 可选参数。
                  • metadata (表) 特定捕获的元数据。当使用
                    |vim.treesitter.query.add_directive()| 时，这将被设置为
                    `metadata[capture_id]`。

    返回值: ~
        (`字符串`)

get_parser({bufnr}, {lang}, {opts})              *vim.treesitter.get_parser()*
    返回特定缓冲区的解析器并将其附加到该缓冲区

    如果需要，这将创建解析器。

    如果无法创建解析器，则返回 nil（和一条错误消息）。

    参数: ~
      • {bufnr}  (`整数?`) 解析器应绑定到的缓冲区（默认：当前缓冲区）
      • {lang}   (`字符串?`) 此解析器的语言（默认：根据缓冲区文件类型）
      • {opts}   (`表?`) 传递给创建的 LanguageTree 的选项

    返回（多个）: ~
        (`vim.treesitter.LanguageTree?`) 用于解析的对象
        (`字符串?`) 错误消息（如果适用）

get_range({node}, {source}, {metadata})           *vim.treesitter.get_range()*
    获取 |TSNode| 的范围。也可以提供 {source} 和 {metadata} 以获取应用了指令后的范围。

    参数: ~
      • {node}      (`TSNode`)
      • {source}    (`整数|字符串?`) 提取 {node} 的缓冲区或字符串
      • {metadata}  (`vim.treesitter.query.TSMetadata?`)

    返回值: ~
        (`表`) 包含以下字段的表：
        • {[1]} (`整数`) 起始行
        • {[2]} (`整数`) 起始列
        • {[3]} (`整数`) 起始字节
        • {[4]} (`整数`) 结束行
        • {[5]} (`整数`) 结束列
        • {[6]} (`整数`) 结束字节

get_string_parser({str}, {lang}, {opts})       *vim.treesitter.get_string_parser()*
    返回一个字符串解析器

    参数: ~
      • {str}   (`字符串`) 要解析的文本
      • {lang}  (`字符串`) 此字符串的语言
      • {opts}  (`表?`) 传递给创建的 LanguageTree 的选项

    返回值: ~
        (`vim.treesitter.LanguageTree`) 用于解析的对象

inspect_tree({opts})                           *vim.treesitter.inspect_tree()*
    打开一个窗口，显示语言树中节点的文本表示形式。

    在此窗口中时，按 "a" 切换显示匿名节点，按 "I" 切换显示每个节点的源语言，
    按 "o" 切换查询编辑器，然后按 <Enter> 跳转到源缓冲区中光标下的节点。
    折叠也可以使用（尝试 |zo|、|zc| 等）。

    也可以通过 `:InspectTree` 显示。                         *:InspectTree*

    属性: ~
        始于: 0.9.0

    参数: ~
      • {opts}  (`表?`) 可选的选项表，可能包含以下键：
                • lang (字符串|nil): 源缓冲区的语言。如果省略，则从源缓冲区的文件类型检测。
                • bufnr (整数|nil): 用于绘制树的缓冲区。如果省略，则创建一个新缓冲区。
                • winid (整数|nil): 用于显示树缓冲区的窗口 ID。如果省略，则使用 {command}
                  创建一个新窗口。
                • command (字符串|nil): 创建窗口的 Vimscript 命令。默认值为 "60vnew"。
                  仅在 {winid} 为 nil 时使用。
                • title (字符串|fun(bufnr:整数):字符串|nil): 窗口的标题。如果是一个函数，
                  它接受源缓冲区的缓冲区编号作为其唯一参数，并应返回一个字符串。

is_ancestor({dest}, {source})                   *vim.treesitter.is_ancestor()*
    确定一个节点是否是另一个节点的祖先

    参数: ~
      • {dest}    (`TSNode`) 可能的祖先
      • {source}  (`TSNode`) 可能的子孙

    返回值: ~
        (`布尔值`) 如果 {dest} 是 {source} 的祖先，则为 True

                                           *vim.treesitter.is_in_node_range()*
is_in_node_range({node}, {line}, {col})
    确定 (line, col) 位置是否在节点范围内

    参数: ~
      • {node}  (`TSNode`) 定义范围的节点
      • {line}  (`整数`) 行（从零开始）
      • {col}   (`整数`) 列（从零开始）

    返回值: ~
        (`布尔值`) 如果位置在节点范围内，则为 True

node_contains({node}, {range})                *vim.treesitter.node_contains()*
    确定一个节点是否包含某个范围

    参数: ~
      • {node}   (`TSNode`)
      • {range}  (`表`)

    返回值: ~
        (`布尔值`) 如果 {node} 包含 {range}，则为 True

start({bufnr}, {lang})                                *vim.treesitter.start()*
    为缓冲区启动 treesitter 高亮

    可以在 ftplugin 或 FileType 自动命令中使用。

    注意：默认情况下，会禁用正则表达式语法高亮，这对于某些插件可能是必需的。
    在这种情况下，在调用 `start` 后添加 `vim.bo.syntax = 'ON'`。

    注意：默认情况下，高亮器使用 3ms 的段时间异步解析代码。

    示例: >lua
        vim.api.nvim_create_autocmd( 'FileType', { pattern = 'tex',
            callback = function(ev)
                vim.treesitter.start(ev.buf, 'latex')
                vim.bo[ev.buf].syntax = 'ON'  -- 仅在需要额外旧式语法时
            end
        })
<

    参数: ~
      • {bufnr}  (`整数?`) 要高亮的缓冲区（默认：当前缓冲区）
      • {lang}   (`字符串?`) 解析器的语言（默认：根据缓冲区文件类型）

stop({bufnr})                                          *vim.treesitter.stop()*
    停止缓冲区的 treesitter 高亮

    参数: ~
      • {bufnr}  (`整数?`) 停止高亮的缓冲区（默认：当前缓冲区）


==============================================================================
Lua 模块: vim.treesitter.language                      *treesitter-language*

add({lang}, {opts})                            *vim.treesitter.language.add()*
    加载名称为 {lang} 的解析器

    解析器在 `parser` 运行时目录或提供的 {path} 中搜索。可用于在启用 treesitter
    功能之前检查可用解析器，例如： >lua
          if vim.treesitter.language.add('markdown') then
            vim.treesitter.start(bufnr, 'markdown')
          end
<

    参数: ~
      • {lang}  (`字符串`) 解析器名称（仅限字母数字和 `_`）
      • {opts}  (`表?`) 选项：
                • {path}? (`字符串`) 解析器所在的可选路径
                • {symbol_name}? (`字符串`) 要加载的语言的内部符号名称

    返回（多个）: ~
        (`布尔值?`) 如果解析器已加载则为 True
        (`字符串?`) 如果无法加载解析器，则返回错误信息

get_filetypes({lang})                *vim.treesitter.language.get_filetypes()*
    返回使用名为 {lang} 的解析器的文件类型。

    该列表包括 {lang} 本身以及通过 |vim.treesitter.language.register()| 注册的所有
    文件类型。

    参数: ~
      • {lang}  (`字符串`) 解析器名称

    返回值: ~
        (`字符串[]`) 文件类型

get_lang({filetype})                      *vim.treesitter.language.get_lang()*
    返回加载 {filetype} 的解析器时应使用的语言名称。

    如果没有通过 |vim.treesitter.language.register()| 显式注册语言，则默认为 {filetype}。
    对于像 `html.glimmer` 这样的复合文件类型，只返回主文件类型。

    参数: ~
      • {filetype}  (`字符串`)

    返回值: ~
        (`字符串?`)

inspect({lang})                            *vim.treesitter.language.inspect()*
    检查提供的语言。

    检查提供一些关于语言的有用信息，如 ABI 版本、解析器状态计数（解析器复杂度的度量）、
    节点和字段名称，以及语言是否来自 WASM 模块。

    节点名称在一个表中返回，将每个节点名称映射到一个 `布尔值`，指示该节点是否命名
    （即非匿名）。匿名节点用双引号 (`"`) 括起来。

    对于 ABI 15 解析器，还显示解析器元数据（主版本、次版本、补丁版本）和超类型及其
    相应子类型的表。

    参数: ~
      • {lang}  (`字符串`) 语言

    返回值: ~
        (`TSLangInfo`)

register({lang}, {filetype})              *vim.treesitter.language.register()*
    注册名为 {lang} 的解析器以用于 {filetype}。

    注意：这会添加或覆盖 {filetype} 的映射，从其他文件类型到 {lang} 的任何现有映射
    将被保留。

    参数: ~
      • {lang}      (`字符串`) 解析器名称
      • {filetype}  (`字符串|字符串[]`) 要与 lang 关联的文件类型


==============================================================================
Lua 模块: vim.treesitter.languagetree              *treesitter-languagetree*

一个 *LanguageTree* 包含一个解析器树：根 treesitter 解析器用于 {lang} 和任何
“注入的”语言解析器，它们本身可能递归地注入其他语言。例如，一个包含一些 Vimscript 命令的
Lua 缓冲区需要多个解析器才能完全理解其内容。

要为给定的缓冲区和语言创建一个 LanguageTree（解析器对象），请使用： >lua
    local parser = vim.treesitter.get_parser(bufnr, lang)
<

（其中 `bufnr=0` 表示当前缓冲区）。`lang` 默认为 'filetype'。注意：当前解析器在缓冲区
的生命周期内被保留，但这可能会改变；如果一个插件想要增量更新，它应该保留对解析器对象的引用。

每当您需要访问当前的语法树时，解析缓冲区： >lua
    local tree = parser:parse({ start_row, end_row })
<

这将返回一个表，其中包含表示缓冲区当前状态的不可变 |treesitter-tree| 对象。当插件想要
在（可能的）编辑后访问状态时，必须再次调用 `parse()`。如果缓冲区没有被编辑，将再次返回
相同的树，无需额外工作。如果缓冲区之前被解析过，将对更改的部分进行增量解析。

注意：要直接在 |nvim_buf_attach()| Lua 回调中使用解析器，您必须在注册回调之前调用
|vim.treesitter.get_parser()|。但最好还是不要直接在更改回调中进行解析，因为它们会非常频繁。
相反，对树进行任何类型分析的插件应该使用计时器来限制过于频繁的更新。


LanguageTree:children()                              *LanguageTree:children()*
    返回语言到子树的映射。

    返回值: ~
        (`表<string,vim.treesitter.LanguageTree>`)

LanguageTree:contains({range})                       *LanguageTree:contains()*
    确定 {range} 是否包含在 |LanguageTree| 中。

    参数: ~
      • {range}  (`表`) 包含以下字段的表：
                 • {[1]} (`整数`) 起始行
                 • {[2]} (`整数`) 起始列
                 • {[3]} (`整数`) 结束行
                 • {[4]} (`整数`) 结束列

    返回值: ~
        (`布尔值`)

LanguageTree:destroy()                                *LanguageTree:destroy()*
    销毁此 |LanguageTree| 及其所有子节点。

    任何清理逻辑都应在此执行。

    注意：这不会将此树从父节点中移除。相反，必须在父节点上调用 `remove_child` 来移除它。

LanguageTree:for_each_tree({fn})                *LanguageTree:for_each_tree()*
    为每个 |LanguageTree| 递归地调用回调函数。

    注意：这包括调用树的子树。

    参数: ~
      • {fn}  (`fun(tree: TSTree, ltree: vim.treesitter.LanguageTree)`)

LanguageTree:included_regions()              *LanguageTree:included_regions()*
    获取由此 LanguageTree 管理的包含区域集。这可能与通过注入查询设置的区域不同，
    因为部分 |LanguageTree:parse()| 会丢弃请求范围之外的区域。每个列表表示一个范围，
    形式为 { {start_row}, {start_col}, {start_bytes}, {end_row}, {end_col}, {end_bytes} }。

    返回值: ~
        (`表<整数, Range6[]>`)

LanguageTree:invalidate({reload})                  *LanguageTree:invalidate()*
    使此解析器及其子节点失效。

    仅当 LanguageTree 的跟踪状态与 treesitter 中的解析树不一致时才应调用。
    不清除文件系统缓存。经常被调用，因此需要快速。

    参数: ~
      • {reload}  (`布尔值?`)

                                                     *LanguageTree:is_valid()*
LanguageTree:is_valid({exclude_children}, {range})
    返回此 LanguageTree 是否有效，即 |LanguageTree:trees()| 是否反映了源的最新状态。
    如果无效，用户应调用 |LanguageTree:parse()|。

    参数: ~
      • {exclude_children}  (`布尔值?`) 是否忽略子节点的有效性（默认 `false`）
      • {range}             (`Range|Range[]?`) 要检查有效性的范围（或按起始点升序排序的
                            范围列表）

    返回值: ~
        (`布尔值`)

LanguageTree:lang()                                      *LanguageTree:lang()*
    获取此树节点的语言。

    返回值: ~
        (`字符串`)

                                           *LanguageTree:language_for_range()*
LanguageTree:language_for_range({range})
    获取包含 {range} 的适当语言。

    参数: ~
      • {range}  (`表`) 包含以下字段的表：
                 • {[1]} (`整数`) 起始行
                 • {[2]} (`整数`) 起始列
                 • {[3]} (`整数`) 结束行
                 • {[4]} (`整数`) 结束列

    返回值: ~
        (`vim.treesitter.LanguageTree`) 管理 {range} 的树

                                         *LanguageTree:named_node_for_range()*
LanguageTree:named_node_for_range({range}, {opts})
    获取包含 {range} 的最小命名节点。

    参数: ~
      • {range}  (`表`) 包含以下字段的表：
                 • {[1]} (`整数`) 起始行
                 • {[2]} (`整数`) 起始列
                 • {[3]} (`整数`) 结束行
                 • {[4]} (`整数`) 结束列
      • {opts}   (`表?`) 包含以下字段的表：
                 • {ignore_injections}? (`布尔值`, 默认: `true`) 忽略注入的语言

    返回值: ~
        (`TSNode?`)

                                               *LanguageTree:node_for_range()*
LanguageTree:node_for_range({range}, {opts})
    获取包含 {range} 的最小节点。

    参数: ~
      • {range}  (`表`) 包含以下字段的表：
                 • {[1]} (`整数`) 起始行
                 • {[2]} (`整数`) 起始列
                 • {[3]} (`整数`) 结束行
                 • {[4]} (`整数`) 结束列
      • {opts}   (`表?`) 包含以下字段的表：
                 • {ignore_injections}? (`布尔值`, 默认: `true`) 忽略注入的语言

    返回值: ~
        (`TSNode?`)

LanguageTree:parent()                                  *LanguageTree:parent()*
    返回父树。对于根树返回 `nil`。

    返回值: ~
        (`vim.treesitter.LanguageTree?`)

LanguageTree:parse({range}, {on_parse})                 *LanguageTree:parse()*
    使用相应语言的 |treesitter-parsers| 递归解析语言树中的所有区域，并在解析的树上
    运行注入查询，以确定是否应创建和解析子节点。

    任何具有空范围（`{}`，通常只有根树）的区域始终被解析；否则（通常是注入）只有当它
    与 {range} 相交时才被解析。

    参数: ~
      • {range}     (`布尔值|Range|Range[]?`) 在解析器的源中解析此范围（或按起始点升序
                    排序的范围列表）。设置为 `true` 以对源执行完全解析（注意：可能很慢！）
                    设置为 `false|nil` 以仅解析具有空范围的区域（通常只有没有注入的根树）。
      • {on_parse}  (`fun(err?: 字符串, trees?: 表<整数, TSTree>)?`)
                    解析完成时调用的函数。当提供此函数且未设置 `vim.g._ts_force_sync_parsing`
                    时，解析将异步运行。函数的第一个参数是一个字符串，表示失败时的错误类型
                    （目前仅可能发生超时）。第二个参数是解析返回的树列表（成功时），
                    如果解析超时（由 'redrawtime' 决定），则为 `nil`。

                    如果解析仍然能够同步完成（在 3ms 内），`parse()` 返回树列表。否则，
                    返回 `nil`。

    返回值: ~
        (`表<整数, TSTree>?`)

                                                 *LanguageTree:register_cbs()*
LanguageTree:register_cbs({cbs}, {recursive})
    为 |LanguageTree| 注册回调。

    参数: ~
      • {cbs}        (`表<TSCallbackNameOn,function>`) 类似于 |nvim_buf_attach()| 的
                     表参数，包含以下处理器：
                     • `on_bytes` : 参见 |nvim_buf_attach()|。
                     • `on_changedtree` : 每次树发生语法更改时都会调用的回调。
                       它将传递两个参数：一个已更改的范围表（作为节点范围）和已更改的树。
                     • `on_child_added` : 当子节点被添加到树时发出。
                     • `on_child_removed` : 当子节点从树中移除时发出。
                     • `on_detach` : 当缓冲区被分离时发出，参见 |nvim_buf_detach_event|。
                       接受一个参数，缓冲区的编号。
      • {recursive}  (`布尔值?`) 递归地为所有子节点应用回调。任何新的子节点也将继承这些回调。

LanguageTree:source()                                  *LanguageTree:source()*
    返回语言树的源内容（bufnr 或字符串）。

    返回值: ~
        (`整数|字符串`)

                                               *LanguageTree:tree_for_range()*
LanguageTree:tree_for_range({range}, {opts})
    获取包含 {range} 的树。

    参数: ~
      • {range}  (`表`) 包含以下字段的表：
                 • {[1]} (`整数`) 起始行
                 • {[2]} (`整数`) 起始列
                 • {[3]} (`整数`) 结束行
                 • {[4]} (`整数`) 结束列
      • {opts}   (`表?`) 包含以下字段的表：
                 • {ignore_injections}? (`布尔值`, 默认: `true`) 忽略注入的语言

    返回值: ~
        (`TSTree?`)

LanguageTree:trees()                                    *LanguageTree:trees()*
    返回由此解析器解析的所有区域的树。不包括子语言。如果满足以下条件，则结果是类似列表的：
    • 此 LanguageTree 是根树，在这种情况下结果是一个空列表或单例列表；或者
    • 根 LanguageTree 被完全解析。

    返回值: ~
        (`表<整数, TSTree>`)


==============================================================================
Lua 模块: vim.treesitter.query                        *lua-treesitter-query*

此 Lua |treesitter-query| 接口允许您创建查询并使用它们解析文本。
参见 |vim.treesitter.query.parse()| 获取工作示例。


*vim.treesitter.Query*
    已解析的查询，参见 |vim.treesitter.query.parse()|

    字段: ~
      • {lang}                     (`字符串`) 解析器语言名称
      • {captures}                 (`字符串[]`) 查询中定义的（唯一）捕获名称列表
      • {info}                     (`vim.treesitter.QueryInfo`) 查询上下文
                                   （例如捕获、谓词、指令）
      • {has_conceal_line}         (`布尔值`) 查询是否设置了 conceal_lines 元数据
      • {has_combined_injections}  (`布尔值`) 查询是否包含组合注入
      • {query}                    (`TSQuery`) userdata 查询对象
      • {iter_captures}            (`fun(self: vim.treesitter.Query, node: TSNode, source: 整数|字符串, start_row: 整数?, end_row: 整数?, opts: 表?): fun(end_line: 整数?, end_col: 整数?): 整数, TSNode, vim.treesitter.query.TSMetadata, TSQueryMatch, TSTree`)
                                   参见 |Query:iter_captures()|。
      • {iter_matches}             (`fun(self: vim.treesitter.Query, node: TSNode, source: 整数|字符串, start: 整数?, stop: 整数?, opts: 表?): fun(): 整数, 表<整数, TSNode[]>, vim.treesitter.query.TSMetadata, TSTree`)
                                   参见 |Query:iter_matches()|。


                                        *vim.treesitter.query.add_directive()*
add_directive({name}, {handler}, {opts})
    添加一个新的指令以在查询中使用

    处理器可以通过直接在元数据对象上设置 `metadata.key = value` 来设置匹配级别的数据。
    此外，处理器可以通过在元数据表 `metadata[capture_id].key = value` 上使用捕获 ID
    来设置节点级别的数据。

    参数: ~
      • {name}     (`字符串`) 指令名称，不带前导 #
      • {handler}  (`fun(match: 表<整数,TSNode[]>, pattern: 整数, source: 整数|字符串, predicate: 任意[], metadata: vim.treesitter.query.TSMetadata)`)
                   • match: 将捕获 ID 映射到捕获节点列表的表
                   • pattern: 查询文件中匹配模式的索引
                   • predicate: 包含正在调用的完整指令的字符串列表，例如
                     `(node (#set! conceal "-"))` 将得到谓词
                     `{ "#set!", "conceal", "-" }`
      • {opts}     (`表`) 包含以下字段的表：
                   • {force}? (`布尔值`) 覆盖同名的现有谓词
                   • {all}? (`布尔值`) 使用匹配表的正确实现，其中捕获 ID 映射到
                     节点列表而不是单个节点。默认为 true。此选项将在未来版本中移除。

                                        *vim.treesitter.query.add_predicate()*
add_predicate({name}, {handler}, {opts})
    添加一个新的谓词以在查询中使用

    参数: ~
      • {name}     (`字符串`) 谓词名称，不带前导 #
      • {handler}  (`fun(match: 表<整数,TSNode[]>, pattern: 整数, source: 整数|字符串, predicate: 任意[], metadata: vim.treesitter.query.TSMetadata): 布尔值?`)
                   • 参数含义参见 |vim.treesitter.query.add_directive()|
      • {opts}     (`表?`) 包含以下字段的表：
                   • {force}? (`布尔值`) 覆盖同名的现有谓词
                   • {all}? (`布尔值`) 使用匹配表的正确实现，其中捕获 ID 映射到
                     节点列表而不是单个节点。默认为 true。此选项将在未来版本中移除。

edit({lang})                                     *vim.treesitter.query.edit()*
    打开一个实时编辑器，用于查询您启动时的缓冲区。

    也可以通过 *:EditQuery* 命令显示。`:EditQuery <tab>` 补全可用的解析器。

    如果您将光标移动到捕获名称（"@foo"），源缓冲区中匹配该捕获的文本将用
    |hl-DiagnosticVirtualTextHint| 高亮显示。查询编辑器是一个临时缓冲区，使用 `:write`
    保存它。您可以在 `$VIMRUNTIME/queries/` 找到示例查询。

    参数: ~
      • {lang}  (`字符串?`) 为其打开查询编辑器的语言。如果省略，则从当前缓冲区的
                文件类型推断。

get({lang}, {query_name})                         *vim.treesitter.query.get()*
    返回 {lang} 的运行时查询 {query_name}。

    参数: ~
      • {lang}        (`字符串`) 用于查询的语言
      • {query_name}  (`字符串`) 查询名称（例如 "highlights"）

    返回值: ~
        (`vim.treesitter.Query?`) 已解析的查询。如果未找到查询文件，则为 `nil`。
        参见 |vim.treesitter.Query|。

                                            *vim.treesitter.query.get_files()*
get_files({lang}, {query_name}, {is_included})
    获取构成一个查询的文件列表

    参数: ~
      • {lang}         (`字符串`) 要获取查询的语言
      • {query_name}   (`字符串`) 要加载的查询名称（例如 "highlights"）
      • {is_included}  (`布尔值?`) 内部参数，大多数情况下保留为 `nil`

    返回值: ~
        (`字符串[]`) query_files 要为给定查询和语言加载的文件列表

lint({buf}, {opts})                              *vim.treesitter.query.lint()*
    使用已安装的解析器检查 treesitter 查询的错误，或清除 lint 错误。

    使用 runtimepath 中的 |treesitter-parsers| 来检查 {buf} 中的查询文件是否存在错误：
    • 验证使用的节点是否是语法中的有效标识符。
    • 验证谓词和指令是否有效。
    • 验证顶层 S 表达式是否有效。

    发现的诊断信息使用 |diagnostic-api| 报告。默认情况下，用于验证的解析器由查询文件
    所在的文件夹决定，例如，如果路径以 `/lua/highlights.scm` 结尾，将使用 `lua` 语言的
    解析器。

    参数: ~
      • {buf}   (`整数`) 缓冲区句柄
      • {opts}  (`表?`) 可选关键字参数：
                • {langs}? (`字符串|字符串[]`) 用于检查查询的语言。如果指定了多种语言，
                  则对所有语言验证查询。
                • {clear} (`布尔值`) 仅清除当前的 lint 错误

list_directives()                     *vim.treesitter.query.list_directives()*
    列出当前可用于查询的指令。

    返回值: ~
        (`字符串[]`) 支持的指令。

list_predicates()                     *vim.treesitter.query.list_predicates()*
    列出当前可用于查询的谓词。

    返回值: ~
        (`字符串[]`) 支持的谓词。

omnifunc({findstart}, {base})                *vim.treesitter.query.omnifunc()*
    用于在 treesitter 查询中补全节点名称和谓词的 Omnifunc。

    通过以下方式使用 >lua
        vim.bo.omnifunc = 'v:lua.vim.treesitter.query.omnifunc'
<

    参数: ~
      • {findstart}  (`0|1`)
      • {base}       (`字符串`)

parse({lang}, {query})                          *vim.treesitter.query.parse()*
    解析 {query} 字符串并返回一个 `Query` 对象（|lua-treesitter-query|），
    该对象可用于搜索树中的查询模式（通过 |Query:iter_captures()|、|Query:iter_matches()|），
    或通过以下字段检查/修改查询：
    • `captures`：查询中定义的唯一捕获名称列表（别名：`info.captures`）。
    • `info.patterns`：关于谓词的信息。
    • `query`：底层的 |TSQuery|，可用于禁用模式或捕获。

    示例: >lua
        local query = vim.treesitter.query.parse('vimdoc', [[
          ; 查询
          ((h1) @str
            (#trim! @str 1 1 1 1))
        ]])
        local tree = vim.treesitter.get_parser():parse()[1]
        for id, node, metadata in query:iter_captures(tree:root(), 0) do
           -- 打印节点名称和源文本。
           vim.print({node:type(), vim.treesitter.get_node_text(node, vim.api.nvim_get_current_buf())})
        end
<

    参数: ~
      • {lang}   (`字符串`) 用于查询的语言
      • {query}  (`字符串`) 查询文本，采用 S 表达式语法

    返回值: ~
        (`vim.treesitter.Query`) 已解析的查询。参见 |vim.treesitter.Query|。

    另请参阅: ~
      • |vim.treesitter.query.get()|

                                                       *Query:iter_captures()*
Query:iter_captures({node}, {source}, {start_row}, {end_row}, {opts})
    遍历 {node} 中所有匹配的所有捕获。

    如果查询包含谓词，则需要 {source}；然后调用者必须确保使用与缓冲区当前文本
    （如果相关）一致的新解析树。{start} 和 {stop} 可用于限制行范围内的匹配
    （这通常与根节点作为 {node} 一起使用，即获取当前视口中的语法高亮匹配）。
    省略时，{start} 和 {stop} 行值将使用给定节点的值。

    迭代器返回四个值：
    1. 标识捕获的数字 ID
    2. 捕获的节点
    3. 处理匹配的任何指令的元数据
    4. 匹配本身

    示例：如何按名称获取捕获： >lua
        for id, node, metadata, match in query:iter_captures(tree:root(), bufnr, first, last) do
          local name = query.captures[id] -- 查询中捕获的名称
          -- 关于节点的典型有用信息：
          local type = node:type() -- 捕获节点的类型
          local row1, col1, row2, col2 = node:range() -- 捕获的范围
          -- ... 在此处使用信息 ...
        end
<

    注意: ~
      • 仅当特定捕获的查询模式包含谓词时，才会返回捕获。

    参数: ~
      • {node}       (`TSNode`) 将在其下进行搜索
      • {source}     (`整数|字符串`) 从中提取文本的源缓冲区或字符串
      • {start_row}  (`整数?`) 搜索的起始行。默认为 `node:start()`。
      • {end_row}    (`整数?`) 搜索的停止行（结束包含，除非提供了 `stop_col`）。
                     默认为 `node:end_()`。
      • {opts}       (`表?`) 可选关键字参数：
                     • max_start_depth (整数) 如果非零，设置每个匹配的最大起始深度。
                       这用于防止在树中遍历过深。
                     • match_limit (整数) 设置进行中的匹配的最大数量（默认：256）。
                     • start_col (整数) 搜索的起始列。
                     • end_col (整数) 搜索的停止列（结束不包含）。

    返回值: ~
        (`fun(end_line: 整数?, end_col: 整数?): 整数, TSNode, vim.treesitter.query.TSMetadata, TSQueryMatch, TSTree`)
        捕获 ID、捕获节点、元数据、匹配、树

                                                        *Query:iter_matches()*
Query:iter_matches({node}, {source}, {start}, {stop}, {opts})
    在给定范围内迭代自身的匹配。

    遍历 {node} 内的所有匹配。参数与 |Query:iter_captures()| 相同，但迭代的值不同：
    查询中模式的（从 1 开始的）索引、将捕获索引映射到节点列表的表，以及处理匹配的
    任何指令的元数据。

    示例: >lua
        for pattern, match, metadata in cquery:iter_matches(tree:root(), bufnr, 0, -1) do
          for id, nodes in pairs(match) do
            local name = query.captures[id]
            for _, node in ipairs(nodes) do
              -- `node` 被匹配中的 `name` 捕获捕获

              local node_data = metadata[id] -- 节点级别元数据
              -- ... 在此处使用信息 ...
            end
          end
        end
<

    参数: ~
      • {node}    (`TSNode`) 将在其下进行搜索
      • {source}  (`整数|字符串`) 要搜索的源缓冲区或字符串
      • {start}   (`整数?`) 搜索的起始行。默认为 `node:start()`。
      • {stop}    (`整数?`) 搜索的停止行（结束不包含）。默认为 `node:end_()`。
      • {opts}    (`表?`) 可选关键字参数：
                  • max_start_depth (整数) 如果非零，设置每个匹配的最大起始深度。
                    这用于防止在树中遍历过深。
                  • match_limit (整数) 设置进行中的匹配的最大数量（默认：256）。
                  • all (布尔值) 当为 `false` 时（默认 `true`），返回的表将捕获 ID
                    映射到单个（最后一个）节点，而不是完整的匹配节点列表。此选项仅
                    用于向后兼容，将在未来版本中移除。

    返回值: ~
        (`fun(): 整数, 表<整数, TSNode[]>, vim.treesitter.query.TSMetadata, TSTree`)
        模式 ID、匹配、元数据、树

set({lang}, {query_name}, {text})                 *vim.treesitter.query.set()*
    为 {lang} 设置名为 {query_name} 的运行时查询

    这允许用户覆盖或扩展插件设置的任何运行时文件和/或配置。

    例如，您可以通过以下代码为 `C` 标识符启用拼写检查： >lua
        vim.treesitter.query.set(
          'c',
          'highlights',
          [[;inherits c
          (identifier) @spell]])
        ]])
<

    参数: ~
      • {lang}        (`字符串`) 用于查询的语言
      • {query_name}  (`字符串`) 查询名称（例如 "highlights"）
      • {text}        (`字符串`) 查询文本（未解析）。




*TSQuery*
    扩展: |userdata|

    对 treesitter 库持有的对象的引用，用作 |vim.treesitter.Query| 的组件以支持语言功能。
    有关查询的更多信息，请参见 |treesitter-query|，或参见 |vim.treesitter.query.parse()|
    以获取如何获取查询对象的示例。

    字段: ~
      • {disable_capture}  (`fun(self: TSQuery, capture_name: 字符串)`) 参见
                           |TSQuery:disable_capture()|。
      • {disable_pattern}  (`fun(self: TSQuery, pattern_index: 整数)`) 参见
                           |TSQuery:disable_pattern()|。


TSQuery:disable_capture({capture_name})            *TSQuery:disable_capture()*
    在此查询中禁用特定捕获；一旦禁用，捕获无法重新启用。{capture_name} 不应包含前导的 "@"。

    示例：从 vimdoc 高亮查询中禁用 `@variable.parameter` 捕获： >lua
        local query = vim.treesitter.query.get('vimdoc', 'highlights')
        query.query:disable_capture("variable.parameter")
        vim.treesitter.get_parser():parse()
<

    参数: ~
      • {capture_name}  (`字符串`)

TSQuery:disable_pattern({pattern_index})           *TSQuery:disable_pattern()*
    在此查询中禁用特定模式；一旦禁用，模式无法重新启用。特定匹配的 {pattern_index} 可以通过
    |:Inspect!| 获取，或者通过读取查询源（即从 |vim.treesitter.query.get_files()|）获取。

    示例：在 vimdoc 中禁用 `|` 链接，但保持其他 `@markup.link` 的高亮： >lua
        local link_pattern = 9 -- 来自 :Inspect!
        local query = vim.treesitter.query.get('vimdoc', 'highlights')
        query.query:disable_pattern(link_pattern)
        local tree = vim.treesitter.get_parser():parse()[1]
<

    参数: ~
      • {pattern_index}  (`整数`)

 vim:tw=78:ts=8:sw=4:sts=4:et:ft=help:norl:
