*intro.txt*     Nvim


                            NVIM 参考手册


Nvim                                                    *ref* *reference*

                                      输入 |gO| 查看目录。

==============================================================================
简介                                            *intro*

Vim 是一个文本编辑器，它包含了 Unix 程序 “Vi” 的大部分命令以及许多新命令。

本手册的概述可以在文件 “help.txt” 中找到，|help.txt|。可以在 Vim 内部使用 <Help> 或 <F1> 键以及 |:help| 命令（只需输入 “:help”，不带竖线或引号）来访问它。
   如果帮助文件不在默认位置，可以将 'helpfile' 选项设置为其文件名。你可以像使用标签一样跳转到主题：使用 CTRL-] 跳转到光标下的主题，使用 CTRL-T 跳回。

                                                        *pronounce*
Vim 读作一个单词，像 Jim 一样。所以 Nvim 读作 “En-Vim”，两个音节。

本手册是所有 Nvim 编辑器和 API 特性的参考。它不是入门介绍；相反，对于初学者，有实践性的 |tutor|、|lua-guide| 和 |user-manual|。

------------------------------------------------------------------------------
资源                                               *resources*

                                                *internet* *www* *distribution*
Nvim 主页：

        https://neovim.io/

                                                *book*
有很多学习 Vi、Vim 和 Nvim 的资源。我们推荐：

- “Practical Vim” by Drew Neil。因其专注于快速学习使用 Vim 进行常见编辑任务而广受好评。
- “Modern Vim” by Drew Neil。探讨了 Nvim 和 Vim 8 中的新特性。
- http://vimcasts.org
- “Vim - Vi Improved” by Steve Oualline。这是第一本专门介绍 Vim 的书。部分内容收录在 Vim 用户手册中。|frombook|  ISBN: 0735710015
- 更多信息请访问 https://www.vim.org/iccf/click5.html
- Vim FAQ: https://vimhelp.org/vim_faq.txt.html

                                                *bugs* *bug-report* *feature-request*
在此处报告错误和请求功能：https://github.com/neovim/neovim/issues
请简洁而完整。始终提供可重现的示例，并尝试找出哪些设置或其他因素触发了该错误。如果 Nvim 崩溃，请尝试获取回溯跟踪（参见 |dev-tools-backtrace|）。

==============================================================================
安装 Nvim                                         *install*

                                                *download* *upgrade* *ubuntu*
要安装或升级 Nvim，你可以...
- 下载预构建的压缩包：
  https://github.com/neovim/neovim/releases
- 使用你的系统包管理器：
  https://github.com/neovim/neovim/blob/master/INSTALL.md#install-from-package
- 从源代码构建：
  https://github.com/neovim/neovim/blob/master/INSTALL.md#install-from-source

------------------------------------------------------------------------------
卸载 Nvim                                      *uninstall*

要卸载 Nvim：
- 如果你下载了预构建的压缩包或从源代码构建了 Nvim（例如 `make install`），只需删除其文件，通常位于： >
  /usr/local/bin/nvim
  /usr/local/share/nvim
<
  - 要查找 Nvim 的安装位置，请运行以下命令： >
    :echo v:progpath
    :echo $VIMRUNTIME
<
- 如果你通过包管理器安装，请阅读你的包管理器的文档。常见示例：
  - APT (Debian, Ubuntu, …)： `apt-get remove neovim`
  - Homebrew (macOS)： `brew uninstall neovim`
  - Scoop (Windows)： `scoop uninstall neovim`

==============================================================================
赞助 Vim/Nvim 开发                            *sponsor*

修复错误和添加新特性需要大量的时间和精力。为了表达你对开发工作的赞赏并激励开发者继续在 Vim 上工作，请捐赠。

你捐赠的资金将主要用于帮助乌干达的儿童。参见 |uganda|。但同时，捐赠会提高开发团队继续在 Vim 上工作的积极性！

有关赞助的最新信息，请访问 Vim 网站：

        https://www.vim.org/sponsor/


Nvim 的开发资金与 Vim 分开筹集：

        https://neovim.io/#sponsor

==============================================================================
Bram Moolenaar                  *Bram* *Moolenaar* *Bram-Moolenaar* *brammool*

Nvim 是 Vim (“Vi IMproved”) 文本编辑器的一个分支，该编辑器最初由 Bram Moolenaar 开发。在 Nvim 的源代码中搜索他的名字，你会发现他的工作成果在 Nvim 中仍然保留了多少。
2023 年 8 月 3 日，他去世，享年 62 岁。如果 Vim 或 Nvim 在你的生活中曾对你有用，请阅读 |Uganda|，并考虑以你认为合适的任何方式纪念他。

- 讣告文章：https://github.com/vim/vim/discussions/12742
- 告别：https://github.com/vim/vim/discussions/12737

==============================================================================
键码                                 *key-notation* *key-codes* *keycodes*

这些键的名称在文档中使用。它们也可以与 “:map” 命令一起使用。

表示法        含义                等价的键      十进制值         ~
<Nul>          零                    CTRL-@    0 (存储为 10) *<Nul>*
<BS>            退格键               CTRL-H    8     *backspace*
<Tab>           制表键               CTRL-I    9     *tab* *Tab*
                                                        *linefeed*
<NL>            换行                 CTRL-J   10 (用于 <Nul>)
<CR>            回车                 CTRL-M   13     *carriage-return*
<Return>        同 <CR>                            *<Return>*
<Enter>         同 <CR>                            *<Enter>*
<Esc>           转义                 CTRL-[   27     *escape* *<Esc>*
<Space>         空格                          32     *space*
<lt>            小于号               <        60     *<lt>*
<Bslash>        反斜杠               \        92     *backslash* *<Bslash>*
<Bar>           竖线                 |       124     *<Bar>*
<Del>           删除                         127
<CSI>           命令序列开始符       ALT-Esc 155     *<CSI>*

<EOL>           行尾 (可以是 <CR>、<NL> 或 <CR><NL>，取决于系统和 'fileformat')     *<EOL>*
<Ignore>        取消等待字符               *<Ignore>*
<NOP>           不执行任何操作（无操作）。在映射中有用。 *<Nop>*
                <Ignore> 是一个键，<NOP> 是 “没有键”。

<Up>            光标上移                       *cursor-up* *cursor_up*
<Down>          光标下移                     *cursor-down* *cursor_down*
<Left>          光标左移                     *cursor-left* *cursor_left*
<Right>         光标右移                    *cursor-right* *cursor_right*
<S-Up>          Shift-光标上移
<S-Down>        Shift-光标下移
<S-Left>        Shift-光标左移
<S-Right>       Shift-光标右移
<C-Left>        Control-光标左移
<C-Right>       Control-光标右移
<F1> - <F12>    功能键 1 到 12           *function_key* *function-key*
<S-F1> - <S-F12> Shift-功能键 1 到 12    *<S-F1>*
<Help>          帮助键
<Undo>          撤销键
<Find>          查找键
<Select>        选择键
<Insert>        插入键
<Home>          起始键                            *home*
<End>           结束键                             *end*
<PageUp>        上翻页                         *page_up* *page-up*
<PageDown>      下翻页                       *page_down* *page-down*
<kUp>           小键盘光标上移                *keypad-cursor-up*
<kDown>         小键盘光标下移              *keypad-cursor-down*
<kLeft>         小键盘光标左移              *keypad-cursor-left*
<kRight>        小键盘光标右移             *keypad-cursor-right*
<kHome>         小键盘起始键 (左上)        *keypad-home*
<kEnd>          小键盘结束键 (左下)         *keypad-end*
<kOrigin>       小键盘原点 (中间)          *keypad-origin*
<kPageUp>       小键盘上翻页 (右上)    *keypad-page-up*
<kPageDown>     小键盘下翻页 (右下)  *keypad-page-down*
<kDel>          小键盘删除                   *keypad-delete*
<kPlus>         小键盘 +                        *keypad-plus*
<kMinus>        小键盘 -                        *keypad-minus*
<kMultiply>     小键盘 *                        *keypad-multiply*
<kDivide>       小键盘 /                        *keypad-divide*
<kPoint>        小键盘 .                        *keypad-point*
<kComma>        小键盘 ,                        *keypad-comma*
<kEqual>        小键盘 =                        *keypad-equal*
<kEnter>        小键盘 Enter                    *keypad-enter*
<k0> - <k9>     小键盘 0 到 9                   *keypad-0* *keypad-9*
<S-…>           Shift 键                       *shift* *<S-*
<C-…>           Control 键                     *control* *ctrl* *<C-*
<M-…>           Alt 键或 Meta 键             *META* *ALT* *<M-*
<A-…>           同 <M-…>                   *<A-*
<T-…>           Meta 键，当它不是 alt 时     *<T-*
<D-…>           Command 键或 “super” 键      *<D-*


注意：

- 某些键（<Help>、<S-Right>、……）的可用性取决于 UI 或宿主终端。
- 如果 numlock 处于开启状态，|TUI| 接收的是普通 ASCII 值，因此映射 <k0>、<k1>、……、<k9> 和 <kPoint> 将不起作用。
- Nvim 支持映射带有修饰符的多字节字符，例如 `<M-ä>`。哪些组合实际有效取决于 UI 或宿主终端。
- 当使用 meta 或 alt 修饰符按下某个键，并且没有为该按键定义映射时，Nvim 的行为可能如同在该键之前按下了 <Esc>。
- 可以表示组合修饰符（例如 <M-C-T> 表示 CTRL-ALT-T），但你的终端必须对该输入进行编码才能使其工作。 |tui-input|

                                                                *<>*
示例通常以 <> 表示法给出。有时这只是为了明确你需要输入的内容，但通常可以字面输入，例如使用 “:map” 命令。规则如下：
1.  可打印字符直接输入，反斜杠和 “<” 除外。
2.  反斜杠用 “\\”、双反斜杠或 “<Bslash>” 表示。
3.  字面意义上的 “<” 用 “\<” 或 “<lt>” 表示。当不可能产生混淆时，可以直接使用 “<”。
4.  “<key>” 表示输入的特殊键（见上表）。示例：
    - <Esc>             转义键
    - <C-G>             CTRL-G
    - <Up>              光标上移键
    - <C-LeftMouse>     Control-鼠标左键点击
    - <S-F11>           Shift 功能键 11
    - <M-a>             Meta-a（设置了第 8 位的 ‘a’）
    - <M-A>             Meta-A（设置了第 8 位的 ‘A’）

<> 表示法使用 <lt> 来转义键名的特殊含义。使用反斜杠也可以，但仅在 ‘cpoptions’ 不包含 ‘B’ 标志时有效。

将 CTRL-H 映射为六个字符 “<Home>” 的示例： >vim
        :imap <C-H> \<Home>
        :imap <C-H> <lt>Home>
第一个仅在 ‘cpoptions’ 中没有 ‘B’ 标志时有效。第二个始终有效。
要在映射中获得字面意义上的 “<lt>”： >vim
        :map <C-L> <lt>lt>

该表示法可以在双引号字符串中使用，在开头使用 “\<”，例如 “\<C-Space>”。这会生成一个特殊键码。要将其转换回可读文本，请使用 `keytrans()`。

==============================================================================
模式，简介                             *vim-modes-intro* *vim-modes*

Vim 有七种基本模式：

                                        *Normal* *Normal-mode* *command-mode*
- 普通模式：          在普通模式下，你可以输入所有普通的编辑器命令。启动编辑器时，你处于此模式。这也称为命令模式。

- 可视模式：          这类似于普通模式，但移动命令会扩展一个高亮区域。当使用非移动命令时，它将针对高亮区域执行。参见 |Visual-mode|。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- VISUAL --”。

- 选择模式：          这看起来最像 MS-Windows 的选择模式。输入一个可打印字符会删除选择并启动插入模式。参见 |Select-mode|。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- SELECT --”。

- 插入模式：          在插入模式下，你输入的文本将插入到缓冲区中。参见 |Insert-mode|。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- INSERT --”。

- 命令行模式：        在命令行模式（也称为 Cmdline 模式）下，你可以在窗口底部输入一行文本。这用于 Ex 命令 “:”、模式搜索命令 “?” 和 “/” 以及过滤命令 “!”。 |Cmdline-mode|

- Ex 模式：            类似于命令行模式，但在输入命令后，你仍处于 Ex 模式。对命令行的编辑非常有限。 |Ex-mode|

                                                        *Terminal-mode*
- 终端模式：          在终端模式下，所有输入（除了 CTRL-\）都被发送到在当前 |terminal| 缓冲区中运行的进程。如果按下 CTRL-\，则下一个键将被发送，除非它是 CTRL-N（|CTRL-\_CTRL-N|）或 CTRL-O（|t_CTRL-\_CTRL-O|）。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- TERMINAL --”。

还有六种附加模式。它们是基本模式的变体：

                                *Operator-pending* *Operator-pending-mode*
- 操作符待决模式：    这类似于普通模式，但在操作符命令已启动后，Vim 正在等待一个 {motion} 来指定操作符将处理的文本。

- 替换模式：          替换模式是插入模式的一种特殊情况。你可以执行与插入模式相同的操作，但每输入一个字符，现有文本中的一个字符就会被删除。参见 |Replace-mode|。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- REPLACE --”。

- 虚拟替换模式：      虚拟替换模式类似于替换模式，但你不是替换文件字符，而是替换屏幕位置。参见 |Virtual-Replace-mode|。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- VREPLACE --”。

- 插入普通模式：      在插入模式下输入 CTRL-O 时进入（参见 |i_CTRL-O|）。这类似于普通模式，但在执行一个命令后，Vim 会返回到插入模式。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- (insert) --”。

- 插入可视模式：      从插入模式开始可视选择时进入，例如，通过使用 CTRL-O 然后 “v”、“V” 或 CTRL-V。当可视选择结束时，Vim 返回到插入模式。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- (insert) VISUAL --”。

- 插入选择模式：      从插入模式开始选择模式时进入。例如，通过拖动鼠标或 <S-Right>。
                        当选择模式结束时，Vim 返回到插入模式。
                        如果 ‘showmode’ 选项开启，窗口底部会显示 “-- (insert) SELECT --”。

==============================================================================
模式切换                             *mode-switching*

如果由于任何原因你不知道自己处于哪种模式，你总是可以通过按两次 <Esc> 返回到普通模式。但这不适用于 Ex 模式，请使用 “:visual”。
当你按 <Esc> 后看到屏幕闪烁或听到蜂鸣声时，你将知道你已回到普通模式。但是，当在插入模式下使用 CTRL-O 后按 <Esc> 时，你会听到蜂鸣声，但你仍然处于插入模式，请再按一次 <Esc>。

                                                        *i_esc*
 >
 FROM 模式       TO 模式
                普通   可视   选择   插入    替换   命令行  Ex >
 普通                   v V ^V    *4     *1        R gR     : / ? !   gQ
 可视            *2               ^G     c C        --        :       --
 选择            *5     ^O ^G            *6         --        --      --
 插入            <Esc>    --      --              <Insert>    --      --
 替换            <Esc>    --      --    <Insert>              --      --
 命令行          *3       --      --     :start     --                --
 Ex              :vi      --      --     --         --        --

 -- 不可能
<

- 1 通过命令 “i”、“I”、“a”、“A”、“o”、“O”、“c”、“C”、“s” 或 S” 从普通模式进入插入模式。
- 2 从可视模式进入普通模式：通过一个非移动命令（这将导致该命令被执行），或按 <Esc>、“v”、“V” 或 “CTRL-V”（参见 |v_v|），这只会停止可视模式而没有副作用。
- 3 从命令行模式进入普通模式：
    - 按 <CR> 或 <NL>，这将导致输入的命令被执行。
    - 删除整行（例如，使用 CTRL-U）并输入一个最终的 <BS>。
    - 按 CTRL-C 或 <Esc>，这将退出命令行而不执行命令。
    在最后一种情况下，<Esc> 可能是用 ‘wildchar’ 选项定义的字符，在这种情况下，它将启动命令行补全。你可以忽略它并再次按 <Esc>。
- 4 从普通模式进入选择模式：
    - 当 ‘selectmode’ 包含 “mouse” 时，使用鼠标选择文本
    - 当 ‘selectmode’ 选项包含 “key” 时，在按住 Shift 键的同时使用非打印命令移动光标
    - 当 ‘selectmode’ 包含 “cmd” 时，使用 “v”、“V” 或 “CTRL-V”
    - 使用 “gh”、“gH” 或 “g CTRL-H”  |g_CTRL-H|
- 5 从选择模式进入普通模式：使用非打印命令移动光标，而不按住 Shift 键。
- 6 从选择模式进入插入模式：输入一个可打印字符。选择被删除，并插入该字符。

                        *CTRL-\_CTRL-N* *i_CTRL-\_CTRL-N* *c_CTRL-\_CTRL-N*
                                      *v_CTRL-\_CTRL-N* *t_CTRL-\_CTRL-N*
此外，命令 CTRL-\ CTRL-N 或 <C-\><C-N> 可用于从任何其他模式进入普通模式。这可以用来确保 Vim 处于普通模式，而不会像 <Esc> 那样引起蜂鸣声。但是，这不适用于 Ex 模式。当在需要参数的命令（如 |f| 或 |m|）之后使用时，将应用 ‘ttimeoutlen’ 设置的超时时间。

        *CTRL-\_CTRL-G* *i_CTRL-\_CTRL-G* *c_CTRL-\_CTRL-G* *v_CTRL-\_CTRL-G*
CTRL-\ CTRL-G 的工作方式与 |CTRL-\_CTRL-N| 相同，是为了向后兼容。

                                *gQ* *mode-Ex* *Ex-mode* *Ex* *EX* *E501*
gQ                      切换到 Ex 模式。这类似于逐个输入 “:” 命令，不同之处在于：
                        - 你不必一直按 “:”。
                        - 屏幕不会在每个命令后更新。
                        使用 `:vi` 命令（|:visual|）退出此模式。

==============================================================================
窗口内容                                         *window-contents*

在普通模式和插入/替换模式下，屏幕窗口将显示缓冲区的当前内容：所见即所得。有两个例外：
- 当 ‘cpoptions’ 选项包含 ‘$’ 时，并且更改发生在一行内，文本不会直接被删除，而是在最后一个被删除的字符处放置一个 ‘$’。
- 在一个窗口中插入文本时，同一文本的其他窗口在插入完成之前不会更新。

行长于窗口宽度时会换行，除非 ‘wrap’ 选项关闭（见下文）。可以设置 ‘linebreak’ 选项以便在空白字符处换行。

如果窗口在缓冲区的最后一行之后还有空间，Vim 将在窗口最后几行的第一列显示 ‘~’，如下所示：
>
        +-----------------------+
        |some line              |
        |last line              |
        |~                      |
        |~                      |
        +-----------------------+
<
因此，‘~’ 行表示已到达缓冲区的末尾。

如果窗口中的最后一行放不下，Vim 将在窗口最后几行的第一列用 ‘@’ 表示，如下所示：
>
        +-----------------------+
        |first line             |
        |second line            |
        |@                      |
        |@                      |
        +-----------------------+
<
因此，‘@’ 行表示有一行内容无法在窗口中完整显示。

当 ‘display’ 选项中存在 “lastline” 标志时，你将不会在窗口左侧看到 ‘@’ 字符。如果最后一行没有完全显示，则只显示适合的部分，最后一行的最后三个字符将被替换为 “@@@”，如下所示：
>
        +-----------------------+
        |first line             |
        |second line            |
        |a very long line that d|
        |oesn't fit in the wi@@@|
        +-----------------------+
<
如果有一行太长而无法在窗口中显示，这是一种特殊情况。Vim 将只显示该行光标周围的部分。不会显示特殊字符，因此你可以编辑该行的所有部分。

|hl-NonText| 高亮组可用于为 ‘@’ 和 ‘~’ 字符设置特殊高亮。这使得它们可以与缓冲区中的真实字符区分开来。

‘showbreak’ 选项包含要放在换行行前面的字符串。

                                                        *wrap-off*
如果 ‘wrap’ 选项关闭，长行将不会换行。只显示适合屏幕的部分。如果光标移动到未显示的行部分，屏幕将水平滚动。这种方法的优点是列按原样显示，并且可以编辑无法在屏幕上完整显示的行。缺点是你不能同时看到一行的所有字符。可以将 ‘sidescroll’ 选项设置为要滚动的最小列数。

所有普通 ASCII 字符都直接显示在屏幕上。<Tab> 被替换为其表示的空格数。其他非打印字符被替换为 “^{char}”，其中 {char} 是非打印字符加 64。因此，字符 7（响铃）将显示为 “^G”。127 到 160 之间的字符被替换为 “~{char}”，其中 {char} 是减去 64 后的字符。这些字符在屏幕上占据多个位置。光标只能定位在第一个位置上。

如果你设置了 ‘number’ 选项，所有行前面都会带有行号。提示：如果你不喜欢换行与行号混在一起，请将 ‘showbreak’ 选项设置为八个空格： >
        ":set showbreak=\ \ \ \ \ \ \ \ "

如果你设置了 ‘list’ 选项，<Tab> 字符将不会显示为多个空格，而是显示为 “^I”。行尾将放置一个 ‘$’，以便你可以找到尾随空白。

在命令行模式下，只有命令行本身正确显示。一旦你返回到命令模式，缓冲区内容的显示就会更新。

窗口的最后一行用于状态和其他消息。只有在以下选项开启时，才会显示状态消息： >

  状态消息                选项        默认值    Unix 默认值
  当前模式                  'showmode'      开启          开启
  命令字符                  'showcmd'       开启          关闭
  光标位置                  'ruler'         关闭          关闭

当前模式是 “-- INSERT --” 或 “-- REPLACE --”，参见 ‘showmode’。命令字符是那些你已经输入但尚未使用的字符。

如果你有一个慢速终端，你可以关闭状态消息以加快编辑速度： >
        :set nosc noru nosm

如果出现错误，错误消息将至少显示一秒钟（以反白视频方式）。

一些命令会显示受影响的行数。触发此显示的行数阈值可以通过 'report' 选项控制（默认为 2）。

名称 Vim 和当前文件的完整名称将显示在标题栏中。当窗口大小调整时，Vim 将自动重绘窗口。你可以将窗口调整为你喜欢的任何大小，但如果它太小，连一行都无法容纳。请使其宽度至少为 40 个字符，以便能够在最后一行阅读大多数消息。

==============================================================================
定义                                             *definitions* *jargon*

- 缓冲区（buffer）：包含文本行，通常来自一个文件。
- 屏幕（screen）：Nvim 用来显示内容的整个区域。
- 窗口（window）：缓冲区的一个视图。一个缓冲区可以有多个窗口。
- 帧（frame）：窗口保存在一个帧树中。每个帧包含一个列、行或窗口（“叶子”帧）。

一个屏幕包含一个或多个窗口，窗口之间由状态行分隔，底部是命令行。
>
        +-------------------------------+
 屏幕   | 窗口 1        | 窗口 2        |
        |               |               |
        |               |               |
        |= 状态行 =|= 状态行 =|
        | 窗口 3                      |
        |                               |
        |                               |
        |==== 状态行 ==============|
        |命令行                   |
        +-------------------------------+
<
命令行也用于显示消息。当命令行空间不足时，它会向上滚动屏幕。

四种类型的行之间存在区别：

- 缓冲区行（buffer lines）：缓冲区中的行。这与从文件读取/写入文件的行相同。它们可能有数千个字符长。
- 逻辑行（logical lines）：应用了折叠后的缓冲区行。关闭的折叠中的缓冲区行会变为单个逻辑行：“+-- 99 lines folded”。它们可能有数千个字符长。
- 窗口行（window lines）：窗口中显示的行：一系列应用了换行、断行等处理的逻辑行。它们只能与窗口宽度允许的长度相同，较长的行会被换行或截断。
- 屏幕行（screen lines）：Nvim 使用的屏幕行。由所有窗口的窗口行、添加的状态行和命令行组成。它们只能与屏幕宽度允许的长度相同。当命令行变长时，它会换行，并且滚动行以腾出空间。

>
  缓冲区行      逻辑行       窗口行       屏幕行
  -----------------------------------------------------------------------
  1. one          1. one          1. +-- folded   1.  +-- folded
  2. two          2. +-- folded   2. five         2.  five
  3. three        3. five         3. six          3.  six
  4. four         4. six          4. seven        4.  seven
  5. five         5. seven                        5.  === 状态行 ===
  6. six                                          6.  aaa
  7. seven                                        7.  bbb
                                                  8.  ccc ccc c
  1. aaa          1. aaa          1. aaa          9.  cc
  2. bbb          2. bbb          2. bbb          10. ddd
  3. ccc ccc ccc  3. ccc ccc ccc  3. ccc ccc c    11. ~
  4. ddd          4. ddd          4. cc           12. === 状态行 ===
                                  5. ddd          13. (命令行)
                                  6. ~
<

API 客户端 ~
所有外部 UI 和远程插件（与常规 Vim 插件不同）通常都是 “客户端”；但如果某物的目的是为其他应用程序抽象或包装 RPC API 以方便其使用（就像 REST 客户端或 SDK，例如 AWS 的 boto3：你可以使用像 curl 这样的 HTTP 客户端调用 AWS REST，但 boto3 将其包装在一个方便的 Python 接口中），我们就称其为 “API 客户端”。例如，Nvim node-client 就是一个 API 客户端：
    https://github.com/neovim/node-client


宿主 ~
插件 “宿主” 既是（Nvim API 的）客户端，也是（外部平台，例如 Python 的）服务器。它是一个承载其他插件的远程插件。


远程插件 ~
通过 |:UpdateRemotePlugins| 注册的任意代码，在单独的进程中运行，并通过 |api| 与 Nvim 通信。

 vim:tw=78:ts=8:et:sw=4:ft=help:norl:
