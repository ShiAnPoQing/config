*pack.txt*                            Nvim

                            NVIM 参考手册

                                扩展 Nvim


                                       输入 |gO| 查看目录。

==============================================================================
使用 Vim 包                                           *packages*

Vim "包" 是一个包含 |plugin| 的目录。与普通插件相比，一个包可以...
- 作为归档文件下载并解压到其自己的目录中，这样文件不会与其他插件的文件混在一起。
- 是一个 git、mercurial 等仓库，因此易于更新。
- 包含多个相互依赖的插件。
- 包含在启动时自动加载的插件（"start" 包，位于 "pack/*/start/*" 下）以及仅在需要时通过 |:packadd| 加载的插件（"opt" 包，位于 "pack/*/opt/*" 下）。

                                                        *runtime-search-path*
Nvim 在以下位置搜索 |:runtime| 文件：
- 1. 'runtimepath' 中的所有路径
- 2. 所有 "pack/*/start/*" 目录

注意，"pack/*/start/*" 路径并未显式包含在 'runtimepath' 中，因此它们不会通过 ":set rtp" 或 "echo &rtp" 报告。
脚本可以使用 |nvim_list_runtime_paths()| 列出所有使用的目录，以及使用 |nvim_get_runtime_file()| 在运行时路径中查询特定文件或子文件夹。示例：>
    " 列出所有运行时目录和包含 Lua 路径的包。
    :echo nvim_get_runtime_file("lua/", v:true)

使用包并自动加载 ~

假设你的 Nvim 文件位于 "~/.local/share/nvim/site"，并且你想从压缩包 "/tmp/foopack.zip" 添加一个包： >
    % mkdir -p ~/.local/share/nvim/site/pack/foo
    % cd ~/.local/share/nvim/site/pack/foo
    % unzip /tmp/foopack.zip

目录名称 "foo" 是任意的，你可以选择任何你喜欢的名称。

现在你将在 ~/.local/share/nvim/site 下拥有这些文件： >
    pack/foo/README.txt
    pack/foo/start/foobar/plugin/foo.vim
    pack/foo/start/foobar/syntax/some.vim
    pack/foo/opt/foodebug/plugin/debugger.vim

在启动时处理你的 |config| 之后，Nvim 扫描 'packpath' 中的所有目录以查找 "pack/*/start/*" 中的插件，然后加载这些插件。

为了允许在解析你的 |vimrc| 时调用包功能，|:colorscheme| 和 |autoload| 除了 'runtimepath' 之外，也会自动在 'packpath' 下进行搜索。详情请参见各自的文档。

在示例中，Nvim 将找到 "pack/foo/start/foobar/plugin/foo.vim" 并加载它。

如果 "foobar" 插件激活并将 'filetype' 设置为 "some"，Nvim 将找到 syntax/some.vim 文件，因为其目录位于运行时搜索路径中。

Nvim 也会加载 ftdetect 文件（如果有的话）。

注意，"pack/foo/opt" 下的文件不会自动加载，只有 "pack/foo/start" 下的文件会。关于如何使用 "opt" 目录，请参见下面的 |pack-add|。

如果禁用加载插件，则不会自动加载包，请参阅 |load-plugins|。

要更早地加载包，以便对 plugin/ 文件进行源操作：
    :packloadall
即使禁用加载插件，这也可以工作。自动加载只会发生一次。

如果包有一个 "after" 目录，则该目录会被添加到 'runtimepath' 的末尾，以便其中的任何内容都会在稍后加载。


使用单个插件并自动加载 ~

如果你没有包，只有一个单独的插件，你需要创建额外的目录层级： >
    % mkdir -p ~/.local/share/nvim/site/pack/foo/start/foobar
    % cd ~/.local/share/nvim/site/pack/foo/start/foobar
    % unzip /tmp/someplugin.zip

现在你将拥有这些文件： >
    pack/foo/start/foobar/plugin/foo.vim
    pack/foo/start/foobar/syntax/some.vim

从这里开始，它的工作方式与上面类似。


可选插件 ~
                                                        *pack-add*
要加载包中的可选插件，请使用 `:packadd` 命令： >
    :packadd foodebug
这将在 'packpath' 中搜索 "pack/*/opt/foodebug"，并找到
~/.local/share/nvim/site/pack/foo/opt/foodebug/plugin/debugger.vim 并对其执行源操作。

这可以在满足某些条件时执行。例如，取决于 Nvim 是否支持某个功能或某个依赖项是否缺失。

你也可以在启动时加载可选插件，方法是将此命令放入你的 |config|： >
    :packadd! foodebug
额外的 "!" 是为了确保如果 Nvim 以 |--noplugin| 启动，则不会加载该插件。

一个包只在 "opt" 目录中包含文件是完全正常的。然后你需要在想要使用每个插件时加载它。


把什么放在哪里 ~

由于通过 `:colorscheme` 加载的颜色方案可以在 "pack/*/start" 和 "pack/*/opt" 下找到，你可以将它们放在任何地方。我们建议你将它们放在 "pack/*/opt" 下，例如
"~/.config/nvim/pack/mycolors/opt/dark/colors/very_dark.vim"。

文件类型插件应该放在 "pack/*/start" 下，以便始终能被找到。除非你有多个针对同一文件类型的插件，并希望选择使用 `:packadd` 加载哪一个。例如，取决于编译器版本： >
    if foo_compiler_version > 34
      packadd foo_new
    else
      packadd foo_old
    endif

"after" 目录在包中很可能没有用。不过也不禁止使用它。

==============================================================================
创建 Vim 包                                           *package-create*

这假设你编写了一个或多个作为包分发的插件。

如果你有两个不相关的插件，你会使用两个包，以便 Vim 用户可以选择包含或不包含哪些。或者你可以决定使用一个包含可选插件的包，并告诉用户使用 `:packadd` 添加他们偏好的插件。

决定如何分发你的包。你可以创建一个归档文件，或者你可以使用一个仓库。归档文件可以被更多用户使用，但更新到新版本有点困难。仓库通常可以很容易地保持更新，但它需要一个像 "git" 这样的程序可用。你可以两者都做，github 可以为发布版本自动创建一个归档文件。

你的目录布局将如下所示： >
   start/foobar/plugin/foo.vim          " 始终加载，定义命令
   start/foobar/plugin/bar.vim          " 始终加载，定义命令
   start/foobar/autoload/foo.vim        " 使用 foo 命令时加载
   start/foobar/doc/foo.txt             " foo.vim 的帮助文件
   start/foobar/doc/tags                " 帮助标签
   opt/fooextra/plugin/extra.vim        " 可选插件，定义命令
   opt/fooextra/autoload/extra.vim      " 使用 extra 命令时加载
   opt/fooextra/doc/extra.txt           " extra.vim 的帮助文件
   opt/fooextra/doc/tags                " 帮助标签
<
这允许用户执行： >
    mkdir ~/.local/share/nvim/site/pack
    cd ~/.local/share/nvim/site/pack
    git clone https://github.com/you/foobar.git myfoobar

这里的 "myfoobar" 是用户可以选择的一个名称，唯一的条件是它与其他包不同。

在你的文档中，你解释插件的作用，并告诉用户如何加载可选插件： >
    :packadd! fooextra

你可以将此 packadd 命令添加到你的一個插件中，以便在需要可选插件时执行。

运行 `:helptags` 命令以生成 doc/tags 文件。在包中包含此生成的文件意味着用户可以立即将包放入 pack 目录，并且帮助命令立即生效。更改插件帮助后，不要忘记重新运行该命令： >
    :helptags path/start/foobar/doc
    :helptags path/opt/fooextra/doc


插件之间的依赖关系 ~
                                                        *packload-two-steps*
假设你有两个依赖于相同功能的插件。你可以将通用功能放在 autoload 目录中，以便自动找到它。你的包将包含这些文件：

pack/foo/start/one/plugin/one.vim  >
    call foolib#getit()
pack/foo/start/two/plugin/two.vim >
    call foolib#getit()
pack/foo/start/lib/autoload/foolib.vim >
    func foolib#getit()

这是有效的，因为在源操作插件时，会搜索 start 包以查找 autoload 文件。

==============================================================================
插件管理器                                                      *vim.pack*

安装、更新和删除外部插件。警告：它仍被视为实验性的，但应该足够稳定，可以日常使用。

仅在专用的 *vim.pack-directory*（参见 |packages|）中管理插件：
`site/pack/core/opt`，这是 "data" |standard-path| 的子目录。"data" 标准路径的子目录 `site` 需要是 'packpath' 的一部分。通常是这样，但在 |--clean| 或启动期间设置 |$XDG_DATA_HOME| 等情况下可能不是。插件子目录名称与规范中的插件名称匹配。假定该目录中的所有插件都完全由 `vim.pack` 管理。

使用 Git 管理插件，并且需要存在 `git` 可执行文件。目标插件应该是 Git 仓库，其版本是遵循 semver 约定 `v<major>.<minor>.<patch>`（带或不带 `v` 前缀）的命名标签。例如 `v1.2.0` 或 `1.2.0`，但不是 `1.2` 或 `v1`。

所有受管理插件的最新状态存储在位于 `$XDG_CONFIG_HOME/nvim/nvim-pack-lock.json` 的 *vim.pack-lockfile* 中。这是一个 JSON 文件，用于持久跟踪插件数据。为了更健壮的配置，请将锁文件视为其一部分：放入版本控制等。在这种情况下，锁文件中的所有插件将一次性全部安装（按字母顺序）并处于锁文件的修订版本（而不是从 `version` 推断）。这是在第一次调用 `vim.pack` 函数时完成的，以确保锁文件与磁盘上的实际内容一致。锁文件不应手动编辑。已安装插件的损坏数据会被修复（包括在删除整个文件后），但对于尚未添加的插件，`version` 字段将丢失。

                                                           *vim.pack-examples*

基本安装和管理 ~
• 在 'init.lua' 中添加 |vim.pack.add()| 调用： >lua

    vim.pack.add({
      -- 安装 "plugin1" 并使用默认分支（通常是 `main` 或 `master`）
      'https://github.com/user/plugin1',

      -- 与上面相同，但使用表（允许设置其他选项）
      { src = 'https://github.com/user/plugin1' },

      -- 指定插件名称（这里插件将被称为 "plugin2"
      -- 而不是 "generic-name"）
      { src = 'https://github.com/user/generic-name', name = 'plugin2' },

      -- 指定在安装和更新期间遵循的版本
      {
        src = 'https://github.com/user/plugin3',
        -- 版本约束，参见 |vim.version.range()|
        version = vim.version.range('1.0'),
      },
      {
        src = 'https://github.com/user/plugin4',
        -- Git 分支、标签或提交哈希
        version = 'main',
      },
    })

    -- `add()` 之后可以直接使用插件代码
    plugin1 = require('plugin1')
<
• 重启 Nvim（例如，使用 |:restart|）。在 `add()` 调用之后，尚未安装的插件将在磁盘上可用。它们的修订版本取自 |vim.pack-lockfile|（如果存在）或从 `version` 推断。
• 使用新更改更新所有插件：
  • 执行 |:packupdate|。这将从源下载更新并在单独的标签页中显示确认缓冲区。
  • 审查更改。要确认所有更新，请执行 |:write|。要放弃更新，请执行 |:quit|。
  • （可选）|:restart| 以开始使用更新后插件的代码。

使用较短的源 ~

创建自定义 Lua 辅助函数： >lua

    local gh = function(x) return 'https://github.com/' .. x end
    local cb = function(x) return 'https://codeberg.org/' .. x end
    vim.pack.add({ gh('user/plugin1'), cb('user/plugin2') })
<

另一种方法是利用 Git 的 `insteadOf` 配置：
• `git config --global url."https://github.com/".insteadOf "gh:"`
• `git config --global url."https://codeberg.org/".insteadOf "cb:"`
• 在 'init.lua' 中：`vim.pack.add({ 'gh:user/plugin1', 'cb:user/plugin2' })`。
  这些源将在 |vim.pack-lockfile| 中逐字使用，因此在不同的机器上重用配置将需要相同的 Git 配置。

探索已安装的插件 ~
• `:packupdate ++offline`
• 使用 `[[` 和 `]]` 在插件之间导航。使用 `gO`（|vim.lsp.buf.document_symbol()|）列出它们。

切换插件的版本和/或源 ~
• 更新 'init.lua' 中插件的所需 `version` 和/或 `src`。假设切换的是名为 'plugin1' 的插件。
• |:restart|。磁盘上插件的状态（修订版本和/或跟踪的源）尚未更改。仅更新 |vim.pack-lockfile| 中插件的 `version`。
• 执行 `:packupdate plugin1`。插件的源已更新。如果仅切换版本，也传递 `++offline` 参数。
• 审查更改并确认或放弃。如果放弃，也请恢复 'init.lua' 中的 `version` 更改，否则下次运行 `:packupdate` 时会再次提示你。

冻结插件以阻止其更新 ~
• 更新 'init.lua' 中插件，将其 `version` 设置为当前修订版本。从 |vim.pack-lockfile|（插件的 `rev` 字段；看起来像 `abc12345`）获取。
• |:restart|。

解冻插件以开始接收更新 ~
• 更新 'init.lua' 中插件，将其 `version` 设置为你希望其更新到的任何版本。
• |:restart|。

更新后恢复插件 ~
• 将 |vim.pack-lockfile| 恢复到更新前的状态：
  • 如果由 Git 跟踪：`git checkout HEAD -- nvim-pack-lock.json`
  • 如果未跟踪：检查日志文件（"log" |standard-path| 中的 "nvim-pack.log"），找到最新更新之前的修订版本，并（小心地）调整当前锁文件以包含这些修订版本。
• |:restart|。
• `:packupdate ++offline ++lockfile plugin`。阅读并确认。

跨机器同步配置 ~
• 在主机器上：
  • 将 |vim.pack-lockfile| 添加到 VCS。
  • 推送到远程服务器。
• 在辅助机器上：
  • 从服务器拉取。
  • |:restart|。新插件（本地不存在但在锁文件中存在）将以正确的修订版本安装。如果某个安装失败但你知道不应该失败（例如由于网络连接不良），请恢复 |vim.pack-lockfile| 并再次 |:restart|。
  • `:packupdate ++lockfile`。阅读并确认。
  • 手动删除过时的插件（本地存在但在重启之前锁文件中不存在的插件），使用 `:packdel plugin`。可以通过检查锁文件的 VCS 差异来定位它们（对于 Git 为 `git diff -- nvim-pack-lock.json`）。

从磁盘移除插件 ~
• 从 'init.lua' 的 |vim.pack.add()| 调用中移除插件规范，否则它们稍后会被重新安装。
• |:restart|。
• 使用 |:packdel| 并附上插件名称进行移除。使用 `:packdel ++all` 删除所有非活跃插件。

检查待处理的更新 ~
• 运行 `vim.pack.get(nil, { offline = false })` 并检查输出中具有不同 `rev` 和 `rev_to` 字段的项目。为了不从源下载新更新，请使用普通的 `vim.pack.get()`。

命令                                             *vim.pack-commands* *E5807*

                                                    *:packu* *:packupdate* *E5808*
:packu[pdate][!] [++offline] [++lockfile] [name]

    以交互方式更新指定的插件。当给出 `!` 时跳过确认。如果未提供插件名称，则更新所有插件。

    当给出 `++offline` 时，跳过下载新更新。

    当给出 `++lockfile` 时，使用锁文件中的修订版本。

                                           *:packd* *:packdel* *E5809* *E5810* *E5811*
:packd[el][!] {name}

    移除指定的插件。除非给出 `!`，否则只能移除非活跃插件。

:packd[el][!] ++all

    移除所有非活跃插件。当给出 `!` 时，改为移除所有插件。

                                                             *vim.pack-events*

通过 `vim.pack` 函数执行操作可以触发这些事件：
• *PackChangedPre* - 在尝试更改插件状态之前。
• *PackChanged* - 在插件状态更改之后。

事件按照插件提供的顺序批量触发。首先触发所有 `PackChangedPre`，然后执行所有操作，最后仅为成功的操作触发所有 `PackChanged`。这为连接的插件提供了更多控制，例如通过在插件本身之前指定依赖项。

|event-data| 具有以下键（类型：`vim.event.packchanged.data`）：
• `active` - 插件是否通过 |vim.pack.add()| 添加到当前会话。
• `kind` - "install"（在磁盘上安装；加载之前）、"update"（更新已安装的插件；可能未加载）、"delete"（从磁盘删除）之一。
• `spec` - 插件规范，显式设置了默认值。
• `path` - 插件目录的完整路径。

这些事件可用于执行插件钩子。例如： >lua
    local hooks = function(ev)
      -- 使用可用的 |event-data|
      local name, kind = ev.data.spec.name, ev.data.kind

      -- 在插件代码更改后运行构建脚本
      if name == 'plug-1' and (kind == 'install' or kind == 'update') then
        -- 如果需要同步执行，请附加 `:wait()`
        vim.system({ 'make' }, { cwd = ev.data.path })
      end

      -- 如果操作依赖于插件的代码（如用户命令或
      -- Lua 代码），请确保先显式加载它
      if name == 'plug-2' and kind == 'update' then
        if not ev.data.active then
          vim.cmd.packadd('plug-2')
        end
        vim.cmd('PlugTwoUpdate')
        require('plug2').after_update()
      end
    end

    -- 如果钩子需要在安装时运行，请在 `vim.pack.add()` 之前运行
    -- 为了对从锁文件安装进行操作，请在第一次 `vim.pack.add()` 之前运行
    vim.api.nvim_create_autocmd('PackChanged', { callback = hooks })
<


*vim.pack.Spec*

    字段： ~
      • {data}?     (`any`) 与插件关联的任意数据。
      • {name}?     (`string`) 插件名称。将用作目录名称。
                    默认值：`src` 的仓库名称。
      • {src}       (`string`) 用于安装和拉取更新的 URI。允许 `git clone` 支持的任何格式。
      • {version}?  (`string|vim.VersionRange`) 用于安装和更新的版本。可以是：
                    • `nil`（无值，默认）使用仓库的默认分支（通常是 `main` 或 `master`）。
                    • 字符串，用于指定具体的分支、标签或提交哈希。
                    • |vim.version.range()| 的输出，用于安装版本约束内最新的 semver 标签。


add({specs}, {opts})                                          *vim.pack.add()*
    将插件添加到当前会话
    • 对于每个规范，检查插件是否存在于 |vim.pack-directory| 的磁盘上：
      • 如果存在，检查其 `src` 是否与输入相同。如果不同 - 立即删除以全新安装新源。否则不执行任何操作。
      • 如果不存在，通过从 `src` 下载到 `name` 子目录（通过部分无 blob 的 `git clone`）来安装它，并更新修订版本以匹配 `version`（通过 `git checkout`）。如果任何步骤导致错误，插件将不会出现在磁盘上。
    • 对于每个插件执行 |:packadd|（或可自定义的 `load` 函数），使其可被 Nvim 访问。

    注意：
    • 安装是并行进行的，但会等待所有安装完成后再继续执行后续代码。
    • 如果插件已存在于磁盘上，则不会检查其当前修订版本。指定的 `version` 可能不是磁盘上实际存在的版本。执行 |vim.pack.update()| 以同步。
    • 在单次会话中第二次及以后添加插件不会执行任何操作：只会注册第一次添加时的数据。

    参数： ~
      • {specs}  (`(string|vim.pack.Spec)[]`) 插件规范列表。
                 字符串项被视为 `src`。
      • {opts}   (`table?`) 包含以下字段的表：
                 • {confirm}? (`boolean`) 是否询问用户确认初始安装。默认 `true`。
                 • {load}?
                   (`boolean|fun(plug_data: {spec: vim.pack.Spec, path: string})`)
                   加载 `plugin/` 文件和 `ftdetect/` 脚本。如果为 `false`，则类似于 `:packadd!`。如果为函数，则使用插件数据调用，并全权负责加载插件。默认：在 |init.lua| 源操作期间为 `false`，之后为 `true`。

del({names}, {opts})                                          *vim.pack.del()*
    从磁盘移除插件

    参数： ~
      • {names}  (`string[]`) 要从磁盘移除的插件名称列表。必须由 |vim.pack| 管理，不一定已添加到当前会话。
      • {opts}   (`table?`) 包含以下字段的表：
                 • {force}? (`boolean`) 是否允许删除活跃插件。默认 `false`。

get({names}, {opts})                                          *vim.pack.get()*
    获取 |vim.pack| 插件信息，可选择按 `names` 过滤。

    参数： ~
      • {names}  (`string[]?`) 插件名称列表。默认值：由 |vim.pack| 管理的所有插件。
      • {opts}   (`table?`) 包含以下字段的表：
                 • {info}? (`boolean`) 是否包含额外的插件信息。默认 `true`。
                 • {offline}? (`boolean`) 是否跳过下载新更新。需要 `info=true`。默认值：`true`。

    返回值： ~
        (`table[]`) 对象列表，包含以下字段：
        • {active} (`boolean`) 插件是否通过 |vim.pack.add()| 添加到当前会话。
        • {branches}? (`string[]`) 可用的 Git 分支（第一个是默认分支）。
          如果 `info=false` 则缺失。
        • {path} (`string`) 插件在磁盘上的路径。
        • {rev} (`string`) 当前的 Git 修订版本。如果 `info=false`，则取自 |vim.pack-lockfile|。
        • {rev_to}? (`string`) 待处理更新的 Git 修订版本。与 |vim.pack.update()| 期间使用的相同，指向已解析的 `spec.version`。如果 `info=false` 则缺失。
        • {spec} (`vim.pack.SpecResolved`) 具有已解析 `name` 的 |vim.pack.Spec|。
        • {tags}? (`string[]`) 可用的 Git 标签。如果 `info=false` 则缺失。

update({names}, {opts})                                    *vim.pack.update()*
    更新插件
    • 从源下载新更改。
    • 推断更新信息（当前/目标修订版本、变更日志等）。
    • 如果 `force` 为 `false`（默认），显示确认缓冲区。如果 `force` 为 `true`，立即执行更新。

    注意：
    • 每次实际更新都会记录在 "log" |standard-path| 中的 "nvim-pack.log" 文件内。
    • 如果源的默认分支已更改（例如从 `master` 变为 `main`），它不会更新。要使 `version = nil` 指向新的默认分支，请重新安装插件（|vim.pack.del()| + |vim.pack.add()|）。

    确认缓冲区 ~

    确认缓冲区的目的是向用户显示更新详情，供用户阅读、确认（执行 |:write|）或拒绝（执行 |:quit|）更新。

    以 `>` 开头的待处理更改将被应用，而以 `<` 开头的更改将被还原。

    提供了便捷的缓冲区本地映射：
    • |]]| 和 |[[| 用于在插件章节间导航。

    一些功能通过 LSP 提供：
    • 'textDocument/documentLink' - 计算插件路径、源、提交和标签的链接。尽最大努力对链接结构进行有根据的猜测。使用 |gx| 打开光标处对象的链接。
    • 'textDocument/documentSymbol'（通过 |lsp-defaults| 或 |vim.lsp.buf.document_symbol()| 使用 `gO`）- 显示缓冲区的结构。
    • 'textDocument/hover'（通过 |lsp-defaults| 或 |vim.lsp.buf.hover()| 使用 `K`）- 在光标处显示更多信息。例如特定待处理更改或更新标签的详情。
    • 'textDocument/codeAction'（通过 |lsp-defaults| 或 |vim.lsp.buf.code_action()| 使用 `gra`）- 显示与“光标处的插件”相关的代码操作。例如“删除”（对于活跃插件需额外确认）、“更新”或“跳过更新”（如果有待处理更新）。

    参数： ~
      • {names}  (`string[]?`) 要更新的插件名称列表。必须由 |vim.pack| 管理，不一定已添加到当前会话。默认值：由 |vim.pack| 管理的所有插件的名称。
      • {opts}   (`table?`) 包含以下字段的表：
                 • {force}? (`boolean`) 是否跳过确认并立即执行更新。默认 `false`。
                 • {offline}? (`boolean`) 是否跳过下载新更新。默认值：`false`。
                 • {target}? (`string`) 如何计算新的插件修订版本。选项：
                   • "version"（默认）：使用插件规范中匹配 `version` 的最新修订版本。
                   • "lockfile"：使用锁文件中的修订版本。用于还原或执行受控更新。


 vim:tw=78:ts=8:sw=4:sts=4:et:ft=help:norl:
