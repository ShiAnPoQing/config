*pack.txt*                            Nvim

                            NVIM 参考手册

                                扩展 Nvim


                                       输入 |gO| 查看目录。

==============================================================================
使用 Vim 包                                            *packages*

Vim "包" 是一个包含 |plugin| 的目录。与普通插件相比，包可以...
- 作为归档文件下载并解压到自己的目录中，这样文件就不会与其他插件的文件混合。
- 可以是 git、mercurial 等仓库，因此易于更新。
- 包含多个相互依赖的插件。
- 包含在启动时自动加载的插件（"start" 包，位于 "pack/*/start/*"）和仅在需要时通过
  |:packadd| 加载的插件（"opt" 包，位于 "pack/*/opt/*"）。

                                                        *runtime-search-path*
Nvim 在以下位置搜索 |:runtime| 文件：
- 1. 'runtimepath' 中的所有路径
- 2. 所有 "pack/*/start/*" 目录

注意，"pack/*/start/*" 路径并未显式包含在 'runtimepath' 中，因此它们不会被
":set rtp" 或 "echo &rtp" 报告。脚本可以使用 |nvim_list_runtime_paths()| 列出所有
使用的目录，并使用 |nvim_get_runtime_file()| 查询运行时路径中的特定文件或子文件夹。
示例： >
    " 列出所有包含 Lua 路径的运行时目录和包。
    :echo nvim_get_runtime_file("lua/", v:true)

使用包并自动加载 ~

假设您的 Nvim 文件位于 "~/.local/share/nvim/site"，并且您想从 zip 归档文件
"/tmp/foopack.zip" 添加一个包： >
    % mkdir -p ~/.local/share/nvim/site/pack/foo
    % cd ~/.local/share/nvim/site/pack/foo
    % unzip /tmp/foopack.zip

目录名 "foo" 是任意的，您可以选择任何您喜欢的名称。

您现在在 ~/.local/share/nvim/site 下会有这些文件： >
    pack/foo/README.txt
    pack/foo/start/foobar/plugin/foo.vim
    pack/foo/start/foobar/syntax/some.vim
    pack/foo/opt/foodebug/plugin/debugger.vim

启动时，在处理完您的 |config| 之后，Nvim 会扫描 'packpath' 中所有位于 "pack/*/start/*"
的目录中的插件，然后加载这些插件。

为了在解析 |vimrc| 时能够调用包的功能，|:colorscheme| 和 |autoload| 除了在
'runtimepath' 中搜索外，也会自动在 'packpath' 下搜索。详情请参见各自的文档。

在示例中，Nvim 会找到 "pack/foo/start/foobar/plugin/foo.vim" 并加载它。

如果 "foobar" 插件启动并将 'filetype' 设置为 "some"，Nvim 会找到 syntax/some.vim 文件，
因为其目录在运行时搜索路径中。

Nvim 也会加载 ftdetect 文件（如果有的话）。

请注意，"pack/foo/opt" 下的文件不会自动加载，只有 "pack/foo/start" 下的文件才会。
有关 "opt" 目录的使用方法，请参见下面的 |pack-add|。

如果插件加载被禁用，则不会自动加载包，参见 |load-plugins|。

要提前加载包，以便 plugin/ 文件被来源化：
    :packloadall
这在插件加载被禁用时也有效。自动加载只会发生一次。

如果包有一个 "after" 目录，该目录会被添加到 'runtimepath' 的末尾，以便其中的任何内容
将在之后加载。


使用单个插件并自动加载 ~

如果您没有包，而只有一个插件，您需要创建额外的目录层级： >
    % mkdir -p ~/.local/share/nvim/site/pack/foo/start/foobar
    % cd ~/.local/share/nvim/site/pack/foo/start/foobar
    % unzip /tmp/someplugin.zip

您现在会有这些文件： >
    pack/foo/start/foobar/plugin/foo.vim
    pack/foo/start/foobar/syntax/some.vim

从这里开始，其工作原理与上面相同。


可选插件 ~
                                                        *pack-add*
要加载包中的可选插件，请使用 `:packadd` 命令： >
    :packadd foodebug
这会在 'packpath' 中搜索 "pack/*/opt/foodebug"，并找到
~/.local/share/nvim/site/pack/foo/opt/foodebug/plugin/debugger.vim 并来源化它。

可以在满足某些条件时执行此操作。例如，取决于 Nvim 是否支持某个功能或缺少依赖项。

您也可以在启动时加载可选插件，方法是将此命令放入您的 |config|： >
    :packadd! foodebug
额外的 "!" 是为了在 Nvim 以 |--noplugin| 启动时不加载该插件。

一个包只在 "opt" 目录下有文件是完全正常的。那么您需要在想要使用每个插件时加载它。


什么放在哪里 ~

由于使用 `:colorscheme` 加载的配色方案可以在 "pack/*/start" 和 "pack/*/opt" 下找到，
您可以将它们放在任何地方。我们建议您将它们放在 "pack/*/opt" 下，例如
"~/.config/nvim/pack/mycolors/opt/dark/colors/very_dark.vim"。

文件类型插件应该放在 "pack/*/start" 下，这样它们就总能被找到。除非您有多个针对同一
文件类型的插件，并希望选择使用 `:packadd` 加载哪一个。例如，取决于编译器版本： >
    if foo_compiler_version > 34
      packadd foo_new
    else
      packadd foo_old
    endif

"after" 目录在包中很可能没什么用。不过也不是不允许。

==============================================================================
创建 Vim 包                                          *package-create*

这假设您编写了一个或多个插件，并将它们作为包分发。

如果您有两个不相关的插件，您会使用两个包，这样 Vim 用户可以选择包含或不包含哪些。
或者您可以决定使用一个带有可选插件的包，并告诉用户使用 `:packadd` 添加他们偏好的插件。

决定如何分发包。您可以创建一个归档文件，也可以使用一个仓库。归档文件可以被更多用户使用，
但更新到新版本稍微困难一些。仓库通常可以很容易地保持更新，但它需要一个像 "git" 这样的
程序可用。您可以两者都做，github 可以为发布版本自动创建归档文件。

您的目录布局将如下所示： >
   start/foobar/plugin/foo.vim          " 总是加载，定义命令
   start/foobar/plugin/bar.vim          " 总是加载，定义命令
   start/foobar/autoload/foo.vim        " 当使用 foo 命令时加载
   start/foobar/doc/foo.txt             " foo.vim 的帮助
   start/foobar/doc/tags                " 帮助标签
   opt/fooextra/plugin/extra.vim        " 可选插件，定义命令
   opt/fooextra/autoload/extra.vim      " 当使用 extra 命令时加载
   opt/fooextra/doc/extra.txt           " extra.vim 的帮助
   opt/fooextra/doc/tags                " 帮助标签
<
这允许用户执行： >
    mkdir ~/.local/share/nvim/site/pack
    cd ~/.local/share/nvim/site/pack
    git clone https://github.com/you/foobar.git myfoobar

这里 "myfoobar" 是用户可以选择的名称，唯一条件是它与其他包不同。

在您的文档中，您解释每个插件的作用，并告诉用户如何加载可选插件： >
    :packadd! fooextra

您可以将这个 packadd 命令添加到您的某个插件中，以便在需要可选插件时执行。

运行 `:helptags` 命令生成 doc/tags 文件。在包中包含这个生成的文件意味着用户可以
将包放入 pack 目录，帮助命令就能立即工作。在更改插件帮助后，不要忘记重新运行该命令： >
    :helptags path/start/foobar/doc
    :helptags path/opt/fooextra/doc


插件间的依赖关系 ~
                                                        *packload-two-steps*
假设您有两个依赖于同一功能的插件。您可以将公共功能放在 autoload 目录中，这样它会被
自动找到。您的包将有这些文件：

pack/foo/start/one/plugin/one.vim  >
    call foolib#getit()
pack/foo/start/two/plugin/two.vim >
    call foolib#getit()
pack/foo/start/lib/autoload/foolib.vim >
    func foolib#getit()

这是可行的，因为在来源化插件时，会自动搜索 start 包以查找 autoload 文件。

==============================================================================
插件管理器                                                      *vim.pack*

安装、更新和删除外部插件。警告：它仍被认为是实验性的，但应该足够稳定以供日常使用。

仅在专用的 *vim.pack-directory*（参见 |packages|）中管理插件：
"data" |standard-path| 下的 `site/pack/core/opt` 子目录。"data" 标准路径的
子目录 `site` 需要是 'packpath' 的一部分。通常是这样，但在像 |--clean| 或在启动期间
设置 |$XDG_DATA_HOME| 的情况下可能不是。插件的子目录名称与规范中的插件名称匹配。
假定该目录中的所有插件都完全由 `vim.pack` 管理。

使用 Git 来管理插件，并且需要存在 `git` 可执行文件。目标插件应该是 Git 仓库，
版本为遵循 semver 约定 `v<major>.<minor>.<patch>` 的命名标签。

所有已管理插件的最新状态存储在一个 *vim.pack-lockfile* 中，位于
`$XDG_CONFIG_HOME/nvim/nvim-pack-lock.json`。这是一个 JSON 文件，用于持久跟踪
插件的数据。为了更健壮的配置，将 lockfile 视为其一部分：将其置于版本控制下等。
在这种情况下，所有来自 lockfile 的插件将一次性安装，并且使用 lockfile 中的修订版本
（而不是从 `version` 推断）。这是在第一次调用 `vim.pack` 函数时完成的，以确保
lockfile 与实际磁盘上的内容保持一致。不应手动编辑 lockfile。已安装插件的损坏数据
会被修复（包括在删除整个文件之后），但对于尚未添加的插件，`version` 字段将丢失。

                                                           *vim.pack-examples*

基本安装和管理 ~
• 在 'init.lua' 中添加 |vim.pack.add()| 调用： >lua

    vim.pack.add({
      -- 安装 "plugin1" 并使用默认分支（通常是 `main` 或 `master`）
      'https://github.com/user/plugin1',

      -- 同上，但使用表（允许设置其他选项）
      { src = 'https://github.com/user/plugin1' },

      -- 指定插件名称（这里插件将被命名为 "plugin2"
      -- 而不是 "generic-name"）
      { src = 'https://github.com/user/generic-name', name = 'plugin2' },

      -- 指定在安装和更新时遵循的版本
      {
        src = 'https://github.com/user/plugin3',
        -- 版本约束，参见 |vim.version.range()|
        version = vim.version.range('1.0'),
      },
      {
        src = 'https://github.com/user/plugin4',
        -- Git 分支、标签或提交哈希
        version = 'main',
      },
    })

    -- 插件代码可以在 `add()` 之后直接使用
    plugin1 = require('plugin1')
<
• 重启 Nvim（例如，使用 |:restart|）。尚未安装的插件将在 `add()` 调用后出现在磁盘上。
  其修订版本取自 |vim.pack-lockfile|（如果存在）或从 `version` 推断。
• 用新更改更新所有插件：
  • 执行 |vim.pack.update()|。这将从源下载更新，并在一个新标签页中显示确认缓冲区。
  • 审查更改。要确认所有更新，执行 |:write|。要放弃更新，执行 |:quit|。
  • （可选）|:restart| 以开始使用更新后的插件代码。

使用更短的源 ~

创建自定义 Lua 助手： >lua

    local gh = function(x) return 'https://github.com/' .. x end
    local cb = function(x) return 'https://codeberg.org/' .. x end
    vim.pack.add({ gh('user/plugin1'), cb('user/plugin2') })
<

另一种方法是利用 Git 的 `insteadOf` 配置：
• `git config --global url."https://github.com/".insteadOf "gh:"`
• `git config --global url."https://codeberg.org/".insteadOf "cb:"`
• 在 'init.lua' 中：`vim.pack.add({ 'gh:user/plugin1', 'cb:user/plugin2' })`。
  这些源将在 |vim.pack-lockfile| 中按原样使用，因此在不同机器上重用配置将需要相同的
  Git 配置。

探索已安装的插件 ~
• `vim.pack.update(nil, { offline = true })`
• 使用 `[[` 和 `]]` 在插件间导航。使用 `gO`（|vim.lsp.buf.document_symbol()|）列出它们。

切换插件的版本和/或源 ~
• 在 'init.lua' 中更新插件的 `version` 和/或 `src`。假设切换是针对名为 'plugin1' 的插件。
• |:restart|。磁盘上插件的状态（修订版本和/或跟踪的源）尚未更改。只有 |vim.pack-lockfile|
  中的插件 `version` 被更新。
• 执行 `vim.pack.update({ 'plugin1' })`。插件的源被更新。如果仅切换版本，请使用
  `{ offline = true }` 选项表。
• 审查更改，然后确认或放弃它们。如果放弃，也请恢复 'init.lua' 中的 `version` 更改，
  否则下次运行 |vim.pack.update()| 时会再次提示您。

冻结插件，使其不被更新 ~
• 在 'init.lua' 中，将插件的 `version` 设置为当前修订版本。从 |vim.pack-lockfile|
  中获取它（插件的 `rev` 字段；看起来像 `abc12345`）。
• |:restart|。

解冻插件以开始接收更新 ~
• 在 'init.lua' 中，将插件的 `version` 设置为您希望它更新到的任何版本。
• |:restart|。

在更新后回退插件 ~
• 将 |vim.pack-lockfile| 回退到更新前的状态：
  • 如果 Git 跟踪：`git checkout HEAD -- nvim-pack-lock.json`
  • 如果未跟踪：检查日志文件（"log" |standard-path| 下的 "nvim-pack.log"），
    定位到最新更新之前的修订版本，并（小心地）调整当前的 lockfile 以使用那些修订版本。
• |:restart|。
• `vim.pack.update({ 'plugin' }, { offline = true, target = 'lockfile' })`。
  读取并确认。

跨机器同步配置 ~
• 在主机器上：
  • 将 |vim.pack-lockfile| 添加到版本控制系统。
  • 推送到远程服务器。
• 在辅助机器上：
  • 从服务器拉取。
  • |:restart|。新的插件（本地不存在，但存在于 lockfile 中）将以适当的修订版本安装。
  • `vim.pack.update(nil, { target = 'lockfile' })`。读取并确认。
  • 使用 `vim.pack.del( { 'plugin' })` 手动删除过时的插件（本地存在，但在重启前的
    lockfile 中不存在）。可以通过检查 lockfile 的版本控制差异来定位它们
    （对于 Git，使用 `git diff -- nvim-pack-lock.json`）。

从磁盘移除插件 ~
• 从 'init.lua' 的 |vim.pack.add()| 调用中移除插件规范，否则它们稍后会被重新安装。
• |:restart|。
• 使用带有插件名称列表的 |vim.pack.del()| 来移除。使用 |vim.pack.get()| 获取所有
  非活动插件： >lua
    vim.iter(vim.pack.get())
     :filter(function(x) return not x.active end)
     :map(function(x) return x.spec.name end)
     :totable()
<

                                                             *vim.pack-events*

通过 `vim.pack` 函数执行操作可能会触发这些事件：
• *PackChangedPre* - 在尝试更改插件状态之前。
• *PackChanged* - 在插件状态更改之后。

每个事件填充以下 |event-data| 字段：
• `active` - 插件是否通过 |vim.pack.add()| 添加到当前会话。
• `kind` - 操作类型之一："install"（安装到磁盘；加载前）、"update"（更新已安装插件；
  可能未加载）、"delete"（从磁盘删除）。
• `spec` - 插件的规范，包含显式设置的默认值。
• `path` - 插件目录的完整路径。

这些事件可用于执行插件钩子。例如： >lua
    local hooks = function(ev)
      -- 使用可用的 |event-data|
      local name, kind = ev.data.spec.name, ev.data.kind

      -- 插件代码更改后运行构建脚本
      if name == 'plug-1' and (kind == 'install' or kind == 'update') then
        -- 如果需要同步执行，请附加 `:wait()`
        vim.system({ 'make' }, { cwd = ev.data.path })
      end

      -- 如果操作依赖于插件的代码（如用户命令或 Lua 代码），
      -- 请确保先显式加载它
      if name == 'plug-2' and kind == 'update' then
        if not ev.data.active then
          vim.cmd.packadd('plug-2')
        end
        vim.cmd('PlugTwoUpdate')
        require('plug2').after_update()
      end
    end

    -- 如果钩子需要在安装时运行，请在 `vim.pack.add()` 之前执行此操作
    -- 要对来自 lockfile 的安装进行操作，请在第一次 `vim.pack.add()` 之前运行
    vim.api.nvim_create_autocmd('PackChanged', { callback = hooks })
<


*vim.pack.Spec*

    字段: ~
      • {src}       (`字符串`) 从中安装和拉取更新的 URI。`git clone` 支持的任何格式
                    都允许。
      • {name}?     (`字符串`) 插件名称。将用作目录名称。默认：`src` 的仓库名称。
      • {version}?  (`字符串|vim.VersionRange`) 用于安装和更新的版本。可以是：
                    • `nil`（无值，默认）使用仓库的默认分支（通常是 `main` 或 `master`）。
                    • 字符串，使用特定的分支、标签或提交哈希。
                    • |vim.version.range()| 的输出，用于安装版本约束内
                      最大/最新的 semver 标签。
      • {data}?     (`任意`) 与插件关联的任意数据。


add({specs}, {opts})                                          *vim.pack.add()*
    将插件添加到当前会话
    • 对于每个规范，检查插件是否存在于 |vim.pack-directory| 的磁盘上：
      • 如果存在，检查其 `src` 是否与输入的相同。如果不是 - 立即删除以便从新源
        全新安装。否则不做任何事。
      • 如果不存在，通过从 `src` 下载到 `name` 子目录（通过部分无 blob 的 `git clone`）
        来安装它，并更新修订版本以匹配 `version`（通过 `git checkout`）。如果任何步骤
        导致错误，插件将不会出现在磁盘上。
    • 对于每个插件，执行 |:packadd|（或可自定义的 `load` 函数），使其可被 Nvim 访问。

    注意：
    • 安装是并行进行的，但在继续执行后续代码之前会等待所有安装完成。
    • 如果插件已存在于磁盘上，不会检查其当前修订版本。指定的 `version` 可能不是磁盘上
      实际存在的版本。执行 |vim.pack.update()| 以同步。
    • 在单次会话中第二次及以后添加插件不会执行任何操作：只有第一次添加的数据被注册。

    参数: ~
      • {specs}  (`(字符串|vim.pack.Spec)[]`) 插件规范列表。字符串项被视为 `src`。
      • {opts}   (`表?`) 包含以下字段的表：
                 • {load}?
                   (`布尔值|fun(plug_data: {spec: vim.pack.Spec, path: string})`)
                   加载 `plugin/` 文件和 `ftdetect/` 脚本。如果为 `false`，其行为类似
                   `:packadd!`。如果是函数，则用插件数据调用它，并由它完全负责加载插件。
                   在 |init.lua| 来源化期间默认为 `false`，之后默认为 `true`。
                 • {confirm}? (`布尔值`) 是否在初始安装时询问用户确认。默认为 `true`。

del({names}, {opts})                                          *vim.pack.del()*
    从磁盘移除插件

    参数: ~
      • {names}  (`字符串[]`) 要从磁盘移除的插件名称列表。必须由 |vim.pack| 管理，
                 但不一定已添加到当前会话。
      • {opts}   (`表?`) 包含以下字段的表：
                 • {force}? (`布尔值`) 是否允许删除活动插件。默认为 `false`。

get({names}, {opts})                                          *vim.pack.get()*
    获取 |vim.pack| 插件信息，可选择按 `names` 过滤。

    参数: ~
      • {names}  (`字符串[]?`) 插件名称列表。默认：由 |vim.pack| 管理的所有插件。
      • {opts}   (`表?`) 包含以下字段的表：
                 • {info} (`布尔值`) 是否包含额外的插件信息。默认为 `true`。

    返回值: ~
        (`表[]`) 对象列表，包含以下字段：
        • {active} (`布尔值`) 插件是否通过 |vim.pack.add()| 添加到当前会话。
        • {branches}? (`字符串[]`) 可用的 Git 分支（第一个是默认的）。
          如果 `info=false` 则缺失。
        • {path} (`字符串`) 插件在磁盘上的路径。
        • {rev} (`字符串`) 当前的 Git 修订版本。
        • {spec} (`vim.pack.SpecResolved`) 一个 |vim.pack.Spec|，其 `name` 已解析。
        • {tags}? (`字符串[]`) 可用的 Git 标签。如果 `info=false` 则缺失。

update({names}, {opts})                                    *vim.pack.update()*
    更新插件
    • 从源下载新更改。
    • 推断更新信息（当前/目标修订版本、变更日志等）。
    • 如果 `force` 为 `false`（默认），则显示确认缓冲区。如果 `force` 为 `true`，
      则立即进行更新。

    注意：
    • 每次实际更新都会记录在 "log" |standard-path| 下的 "nvim-pack.log" 文件中。
    • 如果源的默认分支已更改（例如从 `master` 变为 `main`），它不会更新。
      要使 `version = nil` 指向新的默认分支，请重新安装插件
      （|vim.pack.del()| + |vim.pack.add()|）。

    确认缓冲区 ~

    确认缓冲区的目的是向用户显示更新详情，供用户阅读、确认（执行 |:write|）或拒绝
    （执行 |:quit|）更新。

    以 `>` 开头的待处理更改将被应用，而以 `<` 开头的待处理更改将被还原。

    有一些方便的缓冲区局部映射：
    • |]]| 和 |[[| 用于浏览插件部分。

    某些功能通过 LSP 提供：
    • 'textDocument/documentSymbol'（通过 |lsp-defaults| 或 |vim.lsp.buf.document_symbol()|
      的 `gO`）- 显示缓冲区结构。
    • 'textDocument/hover'（通过 |lsp-defaults| 或 |vim.lsp.buf.hover()| 的 `K`）- 在光标处
      显示更多信息。例如特定待处理更改或更新标签的详细信息。
    • 'textDocument/codeAction'（通过 |lsp-defaults| 或 |vim.lsp.buf.code_action()| 的
      `gra`）- 显示与“光标处的插件”相关的代码操作。例如“删除”（如果插件未激活）、
      “更新”或“跳过更新”（如果有待处理的更新）。

    参数: ~
      • {names}  (`字符串[]?`) 要更新的插件名称列表。必须由 |vim.pack| 管理，
                 但不一定已添加到当前会话。默认：由 |vim.pack| 管理的所有插件的名称。
      • {opts}   (`表?`) 包含以下字段的表：
                 • {force}? (`布尔值`) 是否跳过确认并立即进行更新。默认为 `false`。
                 • {offline}? (`布尔值`) 是否跳过下载新更新。默认为 `false`。
                 • {target}? (`字符串`) 如何计算新的插件修订版本。为以下之一：
                   • "version"（默认）：使用与插件规范中的 `version` 匹配的最新修订版本。
                   • "lockfile"：使用 lockfile 中的修订版本。用于回退或执行受控更新。


 vim:tw=78:ts=8:sw=4:sts=4:et:ft=help:norl:
