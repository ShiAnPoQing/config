return {
  magic = {
    move_up_down = require("custom.plugins.move.magic-move-up-down").move_up_down,
    move_start_end = require("custom.plugins.move.magic-move-start-end").move_start_end,
  },
  move_start_end = require("custom.plugins.move.move-start-end").move_start_end,
}
